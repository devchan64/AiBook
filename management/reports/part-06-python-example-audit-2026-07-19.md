# Part 6 Python 예제 감사 리포트

작성일: 2026-07-19

## 감사 범위

- 대상: `docs/parts/part-06/`의 한국어 Section 본문
- Section 수: 52개
- 본문 Python 예제 수: 36개
- 감사 기준: `management/guidelines/python-example-guidelines.md`
- 중심 질문: Python 예제가 실제 조작 변수와 관찰 출력을 가진 실험형인지, 아니면 고정 답안·후보 목록·라벨 집계를 반복하므로 표나 사례로 대체해야 하는지

## 요약 판정

Part 6의 본문 Python 예제는 대체로 `temperature`, `context budget`, `candidate_budget`, `version_filter`, `required_fields`, `constraints`, `max_retries`처럼 독자가 바꿔 볼 수 있는 값이나 비교 조건을 갖는다. 실행 결과도 단순 정답표가 아니라 후보 분포, 누락 후보, 정책 통과 여부, 실패 계열, 운영 조치처럼 현재 Section의 판단 기준을 드러낸다.

이번 감사에서 즉시 표로 대체해야 할 고정 답안형 Python 블록은 발견하지 않았다. 다만 일부 예제는 CSV 입력 자산이 36행 기본 하한보다 작거나, `transformers` 토크나이저처럼 외부 모델 파일 의존성이 있어 실행 검증 범위를 별도로 관리해야 한다.

## Section별 판정

| Section | 판정 | 근거 |
| --- | --- | --- |
| `P6-1.6` | 유지, 외부 의존성 | `transformers.AutoTokenizer`로 실제 토크나이저 차이를 보지만 로컬 모델 파일 의존성이 있어 자동 실행 검증에서는 제외 |
| `P6-2.4` | 유지 | `coarse_window`를 바꾸며 후보 축소 비용, `recall@5`, 누락 후보가 달라지는 ANN 감각 실험 |
| `P6-3.1` | 유지 | 문맥 특징 변화가 후보 점수와 softmax 분포를 바꾸는 실험 |
| `P6-3.2` | 유지 | context budget 안에서 원순서 선택과 우선순위 선택의 남는 단서 차이를 비교 |
| `P6-3.4` | 유지 | KV cache 유무에 따른 재투영 토큰 수와 계산량 차이를 비교 |
| `P6-4.1` | 유지 | 첫 토큰 선택이 이후 후보 경로와 최종 문장 흐름을 갈라 놓는 생성 경로 실험 |
| `P6-4.2` | 유지 | 자동완성과 대화형 지시 응답의 형식·역할·안전 제약 반영 차이를 검사 |
| `P6-5.1` | 유지 | 작은 학습 문장 묶음에서 직접 다음 토큰 빈도를 세고 생성 경로를 비교 |
| `P6-5.2` | 유지 | temperature별 조정 확률, greedy 응답, sampling 다양성 차이를 비교 |
| `P6-6.1` | 유지 | 일반 코퍼스와 도메인 문장 추가 전후의 다음 토큰 후보 이동을 비교 |
| `P6-6.2` | 유지 | scale 단계별 context window, 추론 비용, 지연 시간, 검증 부담을 나누어 비교 |
| `P6-7.2` | 유지 | 전체 미세조정과 LoRA의 업무 수 증가에 따른 학습·저장 비용 차이를 계산 |
| `P6-8.1` | 유지 | 지시 형식 반영 응답과 기본 응답의 요청 충족률을 비교 |
| `P6-8.2` | 유지 | 후보 응답을 도움됨·안전성·사실성 규칙으로 직접 평가하며 최소 안전 기준 통과 여부를 계산 |
| `P6-8.3` | 유지 | 실패 신호를 형식·지식·실행·지속 스타일 축으로 읽어 대응 수단 점수를 비교 |
| `P6-8.4` | 유지 | LoRA rank 변화에 따른 조정 파라미터 수와 전체 행렬 대비 비율을 계산 |
| `P6-9.1` | 유지 | 단순 프롬프트와 구조화 프롬프트의 슬롯 충족률, 형식 충족률을 비교 |
| `P6-9.2` | 유지 | prompt-only와 구조 보강의 검사 통과 수를 비교해 프롬프트 한계를 확인 |
| `P6-10.1` | 유지 | 기억 기반 답변과 RAG 답변의 최신 신호 회수와 상위 근거 문서 일치를 비교 |
| `P6-10.2` | 유지, CSV 보강 후보 | CSV 문서와 실험 조건을 읽어 검색 실패와 생성 실패를 분리하지만 입력 행 수는 작음 |
| `P6-11.1` | 유지 | 벡터 검색 결과가 원문과 메타데이터를 함께 반환하는지 질문별로 검사 |
| `P6-11.2` | 유지, CSV 보강 후보 | `candidate_budget`, `version_filter`에 따라 top-k 포함률, top-1 정합률, 지연 시간 대체값이 달라짐 |
| `P6-12.1` | 유지 | 요청 문장에서 도구 호출 필요 여부, 도구 선택, 결과 반영 여부를 규칙으로 확인 |
| `P6-12.2` | 유지 | 함수 호출 구조의 필수 인자 누락과 실행 가능 여부를 검사 |
| `P6-13.1` | 유지 | 상태 객체가 갱신될 때 다음 계획이 바뀌는 agent 실행 흐름을 확인 |
| `P6-13.2` | 유지 | 관찰 결과에 따라 계속 진행, 종료, 사람 검토 전환으로 분기 |
| `P6-14.1` | 유지 | MCP 연결 계층에서 도구·자원 해석 성공 여부와 실패 이유를 검사 |
| `P6-14.2` | 유지, 경계 사례 | 실행 로그에 일부 운영 상태가 들어 있지만, 최신 문서 사용·승인·재현 가능성을 다시 계산해 원인과 다음 조치를 분리 |
| `P6-15.1` | 유지 | 답변 텍스트를 근거 문장과 비교해 정확성, 근거성, 형식, 도움됨을 계산 |
| `P6-15.2` | 유지 | 자동 평가 통과 뒤 사람 검토 질문과 최종 조치를 분리 |
| `P6-16.1` | 유지 | 품질, 지연 시간, 비용, 처리량 제약을 바꾸면 운영 적합 후보가 달라지는 서비스 설계 실험 |
| `P6-16.2` | 유지 | 실패 계열, retry 수, fallback 가능성에 따라 복구 경로가 달라지는 운영 분기 실험 |
| `P6-17.2` | 유지 | 문서 근거 수와 검색 실패 여부에 따라 실행 상태와 사람 검토 필요성을 계산 |
| `P6-18.1` | 유지 | bigram, 정규화 검색, 부정 표현, attention-like focus를 비교하는 역사 연결 실험 |
| `P6-18.2` | 유지, CSV 보강 후보 | CSV 항목을 규칙으로 direct lineage와 surrounding evidence로 나누지만 입력 행 수는 작음 |
| `P6-19.2` | 유지 | 분류, 문장쌍 비교, 검색 순위를 규칙 기반 점수로 직접 계산 |

## 후속 보강 후보

| 항목 | 현재 상태 | 후속 판단 |
| --- | --- | --- |
| `P6-1.6` 토크나이저 예제 | `transformers`와 모델 파일 의존성 때문에 자동 실행 검증 제외 | 로컬 캐시 전제, 설치 전제, 실행 실패 시 대체 관찰표를 본문에 명시할지 검토 |
| `p6-10-rag-documents.csv` | 3개 데이터 행 | 검색 실패와 생성 실패 구분은 가능하지만 CSV 36행 기준에는 부족 |
| `p6-10-rag-experiments.csv` | 3개 데이터 행 | 정상·검색 실패·생성 실패 조건은 있으나 반복 실험 규모는 작음 |
| `p6-11-index-documents.csv` | 9개 데이터 행 | 버전 혼입과 target 누락은 드러나지만 36행 기준에는 부족 |
| `p6-11-index-queries.csv` | 3개 데이터 행 | top-k 포함률 계산은 가능하지만 질문 수가 적음 |
| `p6-18-lineage-items.csv` | 7개 데이터 행 | 계보 분류 규칙 확인에는 충분하나 CSV 규모 기준에는 부족 |

## 검증 결과

- `find docs/parts/part-06 -name 'section-[0-9][0-9].md' | wc -l`: 52
- `rg -n '^```python' docs/parts/part-06 -g 'section-[0-9][0-9].md' | wc -l`: 36
- 본문 Python 블록 자동 실행: 35개 통과, 1개 외부 의존성으로 제외
  - 제외: `docs/parts/part-06/chapter-01/section-06.md`
  - 사유: `transformers.AutoTokenizer`가 외부 토크나이저/모델 파일에 의존
- `.venv/bin/python -m mkdocs build`: 실행하지 않음. 이번 작업은 Python 예제 감사와 리포트 생성이며, 전체 사이트 빌드는 Part 마감 검증에서 실행하는 편이 적합하다.

## 결론

Part 6의 현재 Python 예제는 대부분 Section 중심 질문과 직접 연결된 실험형으로 유지 가능하다. 후보 응답이나 로그를 쓰는 예제도 단순 라벨 집계가 아니라, 텍스트 조건·운영 조건·임계값·상태 전이를 코드에서 다시 계산해 관찰 결과를 만든다.

즉시 제거하거나 표로 대체해야 할 Python 블록은 없었다. 다만 CSV 기반 예제의 행 수와 외부 토크나이저 의존성은 후속 보강 후보로 남긴다.
