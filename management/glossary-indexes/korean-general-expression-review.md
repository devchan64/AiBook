# 한국어 개념사전 표제 검토 리포트

작성일: 2026-07-26

## 목적

이 리포트는 현재 `management/glossary-indexes/concept-glossary-index.ko.md`에 남아 있는 한국어 개념사전 표제를 다시 검토해, 일반 사전적 의미와 원고 안의 의미가 분리될 수 있는 표현을 관리하기 위한 문서다.

표제 검토의 목적은 링크 여부를 판단하는 데 있지 않다. 표제만 보면 독자가 일반 사전적 의미로 읽기 쉽지만 원고에서는 특정 기술 문맥, 학습 문맥, 실행 절차, 평가 구조 안에서 좁게 쓰이는 경우가 혼란을 만든다. 이런 표현은 독립 표제로 계속 둘지, 더 큰 대표 개념의 하위 설명으로 돌릴지, 아니면 일반화된 학술·기술 표현으로 표제어를 바꿀지 구분해야 한다.

따라서 후속 처리는 두 갈래로 나눈다.

- 삭제 후보: 원고 내부의 산출물명, 상태명, 절차명, 옵션명, 문서 필드명에 가까워 독립 표제로 관리할 실익이 낮은 표현이다. 필요한 설명은 대표 항목의 하위 설명이나 본문 문맥으로 흡수한다.
- 표제 변경 후보: 독립 연결점은 필요하지만 현재 표제가 너무 일반적이거나 문맥이 드러나지 않는 표현이다. 즉흥적인 새 이름을 만들지 않고, 영어 기준 용어와 일반화된 학술·기술 표현을 확인해 대표 표제어를 통일한다.

이 리포트는 자동 삭제 목록이 아니라 표제 정리 우선순위다. 실제 삭제, 병합, 표제 변경, slug 변경은 각 항목의 중심 Section과 등장 Section을 다시 확인한 뒤 별도 작업으로 처리한다.

## 검토 범위

- 기준 파일: `management/glossary-indexes/concept-glossary-index.ko.md`
- 단어별 원고 위치: `docs/reference/concept-glossary-terms/*.ko.md`
- 현재 한국어 인덱스 항목 수: 517개
- 현재 `표제 관리 제외 검토` 표시: 0개
- 현재 `표제 통일 관리 검토` 표시: 27개
- 검토 방식: 인덱스의 `비고` 표시, 대표 표제어, 영어 기준 용어, 중심 Section, 단어별 원고의 뜻·왜 중요한가를 함께 대조

## 판정 기준

- 삭제 후보: 독립 개념보다 특정 Section의 산출물, 검토 상태, 실행 문서, 운영 필드에 가깝다.
- 표제 변경 후보: 독립 표제로 둘 수 있으나 표제만으로는 문맥이 부족하다. 표제어에 대상 영역이나 기술 층위를 보강한다.
- 병합 후보: 둘 이상의 표제가 같은 개념 흐름을 나누어 중복 설명한다. 대표 항목 하나를 세우고 나머지는 그 항목 안의 표현으로 흡수한다.
- 현행 유지 후보: 일반어처럼 보이지만 해당 분야에서 표준 기술 용어로 쓰이거나, 초심자에게 일반 의미와 기술 의미의 차이를 설명할 필요가 크다.

## 삭제 정리 완료

아래 항목은 독립 표제로 관리할 실익이 낮다고 판단해 한국어 인덱스, 한국어 자음별 색인 include, 한국어 단어별 원고에서 제거했다. 필요한 설명은 흡수 후보 항목이나 본문 문맥에서 다룬다.

| 정리 상태 | 삭제 표제 | 파일 slug | 영어 기준 용어 | 삭제 판단 | 흡수 후보 |
| --- | --- | --- | --- | --- | --- |
| 완료 | 개선 계획 | `improvement-plan` | improvement plan | Part 7 실행 문서의 산출물명에 가깝다. 독립 개념보다 확인 결과를 다음 작업으로 바꾸는 문서 필드로 볼 수 있다. | 본문 문맥, `문서 재현성`, `상태 점검` |
| 완료 | 실행 요약 | `execution-summary` | execution summary | 특정 실행 기록의 요약 양식이다. 독립 표제보다 실행 로그의 하위 구조로 흡수할 여지가 크다. | `실행 로그`, `비교표` |
| 완료 | 경고 등급 | `alert-grade` | alert grade | 운영 신호 분류 체계지만 일반화된 표준 용어라기보다 Part 7 프로젝트의 검토 강도 표시로 보인다. | `상태 점검` |
| 완료 | 다음 행동 | `next-action` | next action | 에이전트 실행이나 프로젝트 운영에서 쓰이지만 현재 표제는 절차 문장에 가깝다. | `종료 조건`, `상태 점검` |
| 완료 | 개인 학습 | `personal-learning` | personal learning | 책의 제작·학습 맥락에는 중요하지만 AI/ML 개념사전의 독립 표제로 관리하기에는 범위가 넓고 회고성 표현에 가깝다. | `근거`, `작업 가설` |
| 완료 | 복습 | `relearning` | relearning | `개인 학습`과 설명 범위가 겹치며 독립 AI/ML 개념보다 학습 행위 설명에 가깝다. | 본문 학습 흐름 |
| 완료 | 평가 실행 | `eval-run` | eval run | 정해 둔 입력 세트와 기준으로 돌린 한 번의 실행 단위이므로 독립 개념보다 운영 기록 단위에 가깝다. | 본문 문맥, `하네스`, `실행 로그` |
| 완료 | 평가 기록 | `evaluation-record` | evaluation record | 점수, 실패 샘플, 설정, 후속 조치를 묶은 기록으로 독립 개념보다 실행 로그나 평가 지표 설명의 하위 구조에 가깝다. | `실행 로그`, `평가 지표` |
| 완료 | 검토 후보 | `review-candidate` | review candidate | 사람이 다시 볼 가능성이 있는 사례라는 운영 상태명이다. 독립 표제보다 확인 절차 안의 단계로 둘 수 있다. | `인간 감독`, `상태 점검` |
| 완료 | 검토 대상 | `review-target` | review target | 후보에서 실제 재확인 표적으로 좁힌 상태명이다. 독립 표제보다 오류 사례나 인간 감독 설명 안에서 다룬다. | `오류 사례`, `인간 감독` |
| 완료 | 우선순위 | `priority` | priority | 일반 중요도와 겹치며 Part 7 운영 순서 기준으로만 쓰인다. 독립 표제보다 상태 점검이나 문서 재현성 문맥에서 설명한다. | `상태 점검`, `문서 재현성` |
| 완료 | 사람 검토 | `human-review` | human review | 자동화 경계의 절차명으로는 중요하지만 독립 표제보다 `인간 감독`이나 본문 설명에 두는 편이 정확하다. | `인간 감독`, `책임` |
| 완료 | 권리와 책임 | `rights-and-responsibility` | rights and responsibility | 여러 검토 관점을 묶은 표현이다. 표준 단일 개념 표제보다 저작권, 개인정보, 책임 설명 안에서 다룬다. | `저작권`, `개인정보`, `책임` |
| 완료 | 평가 | `evaluation` | evaluation | 일반 사전 의미가 강하고 하위 표제가 계속 늘어날 수 있다. 모델 성능 판단은 더 구체적인 `평가 지표`, `검증`, `사람 평가`, `자동 평가`로 관리한다. | `평가 지표`, `검증`, `사람 평가`, `자동 평가` |
| 완료 | 검토 | `review` | review | 일반 확인 행위로 읽히기 쉽다. AI 결과 확인은 `인간 감독`, `근거`, `책임` 등 더 구체 항목에서 다룬다. | `인간 감독`, `근거`, `책임` |
| 완료 | 작업 흐름 | `workflow` | workflow | 일반 절차 의미가 강하다. 문서 운영 맥락은 `문서 재현성`, `버전 관리`, `배포`로 나누어 관리한다. | `문서 재현성`, `버전 관리`, `배포` |

## 재검토 결과 요약

강화된 기준을 적용하면 남은 27개 `표제 통일 관리 검토` 항목은 다음 두 부류로 나뉜다.

- 문맥 한정 표제 변경: 26개. 단독 일반어 표제를 피하고 대상 영역이나 기술 층위를 표제에 붙인다.
- 제한 유지: 1개. `메타데이터`는 표준 기술 용어로 볼 수 있지만 일반 사전적 표현도 있으므로, 첫 문장에서 문서·검색 맥락의 부가 정보로 범위를 좁혀 관리한다.

`평가`, `검토`, `작업 흐름`은 일반 사전 의미가 강해 독립 표제로 두지 않는다. 평가 관련 설명은 `평가 지표(metric)`, `검증(validation)`, `사람 평가(human evaluation)`, `자동 평가(automatic evaluation)`처럼 더 구체적인 표제로 나누고, 검토 관련 설명은 `인간 감독(human oversight)`, `근거(evidence)`, `책임(accountability)`으로 흡수한다. `환경(environment)`은 단독 일반어로 두지 않고 `강화학습 환경(reinforcement learning environment)`처럼 문맥을 붙여 관리한다. `질의(query)`도 단독 표제보다 `검색 질의(query)` 또는 `k-NN 질의(query)`처럼 쓰임을 좁히는 것이 맞다.

## 문맥 한정 표제 변경 후보

아래 항목은 독립 표제 유지 가능성이 있지만, 현재 표제가 일반어에 가까워 문맥 한정 표제로 바꾸는 편이 안전하다.

| 현재 표제 | 파일 slug | 현재 영어 기준 용어 | 권장 표제 | 판단 |
| --- | --- | --- | --- | --- |
| 비교표 | `comparison-table` | comparison table | `모델 비교표` | 단순 표가 아니라 기준선·모델·샘플 차이를 읽는 비교 문서 구조다. |
| 산출물 | `output-artifact` | output artifact | `출력 산출물` | 일반 산출물이 아니라 생성형 AI 출력 결과물의 검토 대상이다. |
| 점수 | `score` | score | `모델 점수` | 일반 점수가 아니라 후보 출력 비교와 임계값 판단에 쓰이는 모델 수치다. |
| 후처리 | `postprocessing` | postprocessing | `모델 후처리` | 일반 작업 후처리가 아니라 모델 출력 뒤의 라벨 선택, 임계값 적용, 표시 변환 단계다. |
| 앱 | `application` | application | `AI 앱` | 일반 앱이 아니라 모델·도구·데이터를 묶어 사용자가 만나는 AI 서비스 표면이다. |
| 도구 | `tool` | tool | `도구 호출` | 일반 도구보다 모델 밖 기능을 조회·실행하는 연결 수단에 초점을 둔다. |
| 목표 | `goal` | goal | `에이전트 목표` | 일반 목표가 아니라 에이전트 실행과 종료 판단의 기준이다. |
| 리소스 | `resource` | resource | `MCP 리소스` | 일반 자원이 아니라 모델·앱이 읽는 외부 맥락 데이터다. |
| 실행 로그 | `log` | log | `실행 로그` | 현재 인덱스 표기와 대표 표제어가 엇갈린다. 대표 표제어를 `실행 로그`로 맞춘다. |
| 운영 | `service-operation` | operation | `서비스 운영` | 일반 운영이 아니라 AI 서비스의 비용·오류·속도·품질 관리 층위다. |
| 생산성 | `productivity` | productivity | `업무 생산성` | 생성 속도뿐 아니라 검토·실패·운영 비용까지 포함하는 업무 평가 기준이다. |
| 검토 비용 | `review-cost` | review cost | `AI 검토 비용` | 일반 검토 비용보다 AI 초안·검색 결과를 사용 가능 상태로 만드는 비용이다. |
| 보고서 | `report` | report | `연구 보고서` | 일반 보고서보다 AI 전망·정책 논의의 근거 자료 유형으로 좁혀 쓴다. |
| 방법론 | `methodology` | methodology | `조사 방법론` | 일반 방법론이 아니라 보고서 수치가 만들어진 조사·분석 절차다. |
| 코드 | `code` | code | `프로그램 코드` | 일반 암호나 규칙 코드가 아니라 컴퓨터가 실행할 절차다. |
| 업데이트 | `update` | update | `파라미터 업데이트` | 일반 갱신이 아니라 계산 결과에 맞춰 파라미터나 설정값을 바꾸는 단계다. |
| 패키지 | `package` | package | `Python 패키지` | 일반 묶음이나 배송 패키지가 아니라 Python 생태계의 배포 코드 묶음이다. |
| 누적 | `accumulation` | accumulation | `반복 누적` | 일반 누적보다 반복 계산 중 결과를 계속 쌓는 패턴이다. |
| 테이블 | `table` | table | `데이터 테이블` | 일반 표보다 행과 열의 교차로 사례와 변수를 정리하는 데이터 구조다. |
| 출력 구조 | `output-structure` | output structure | `모델 출력 구조` | 일반 출력 형식이 아니라 계산 결과를 문제 결과로 내보내는 설계 틀이다. |
| 테스트 | `test` | test | `테스트 데이터` | 일반 시험이 아니라 선택이 끝난 뒤 마지막으로 확인하는 데이터 또는 절차다. |
| 커밋 | `commit` | commit | `Git 커밋` | 일반 약속이 아니라 버전 관리의 변경 이력 단위다. |
| 질의 | `query` | query | `검색 질의` | 검색과 k-NN에서 쓰는 새 입력 기준임을 드러낸다. 중심 Section이 k-NN이면 `k-NN 질의`도 검토한다. |
| 환경 | `environment` | environment | `강화학습 환경` | 일반 환경이 아니라 강화학습 에이전트가 행동하고 보상을 받는 상호작용 대상이다. |
| automatic prompt optimization | `automatic-prompt-optimization` | automatic prompt optimization | `자동 프롬프트 최적화` | 한국어 개념사전의 표제이므로 영문 단독 표제보다 한국어 대표 표제어를 세운다. |
| trustworthiness | `trustworthiness` | trustworthiness | `이웃 보존 신뢰도` | 일반 신뢰성이 아니라 차원 축소 시각화에서 원래 이웃 관계가 보존되는 정도를 보는 지표다. |

## 제한 유지 후보

아래 항목은 독립 표제 유지 이유가 있으나, 표제와 첫 설명에서 일반 의미와 원고 의미의 경계를 명확히 유지해야 한다.

| 표제 | 파일 slug | 유지 이유 | 관리 조건 |
| --- | --- | --- | --- |
| 메타데이터 | `metadata` | 검색, 필터링, 출처 추적, 접근 권한에서 반복되는 표준 기술 용어다. | 일반 사전적 의미도 있으므로 `문서 메타데이터` 또는 `검색 메타데이터`로 범위를 좁힐 필요가 있는지 중심 Section에서 확인한다. |

## 후속 작업 순서

1. 문맥 한정 표제 변경 후보 26개는 `대표 표제어`, 단어별 원고 제목, 한국어 인덱스 표기를 같은 작업에서 맞춘다.
2. `질의`, `환경`, `평가` 계열은 단독 일반어를 남기지 않는 대표 사례로 먼저 정리한다. `질의`는 `검색 질의`, `환경`은 `강화학습 환경`, 평가 관련 표현은 `평가 지표`, `검증`, `사람 평가`, `자동 평가`로 나눈다.
3. 제한 유지 후보인 `메타데이터`는 항목 첫 문장과 함께 볼 개념을 보강해 일반 사전 의미로 읽히지 않게 한다.
4. 변경 뒤 `concept-glossary-index.ko.md`의 `비고`에서 완료된 검토 표시를 제거하거나 후속 상태로 바꾼다.
