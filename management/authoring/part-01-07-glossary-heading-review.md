# Part 1~7 한글 Section 표제어 정리 검토 리포트

작성일: 2026-07-29

## 검토 목적

Part 1~7의 한글 Section 원고를 기준으로, 개념사전 표제어로 계속 관리할 표현과 본문 설명 또는 상위 표제 안으로 흡수할 표현을 분리한다. 이번 검토의 목적은 표제어 수를 늘리는 것이 아니라, 일반 사전적 의미가 강한 단어와 작업·도구·문서 형식 이름이 개념사전 표제로 과잉 관리되지 않게 하는 것이다.

## 검토 범위

- 기준 원고: `docs/parts/part-01/`부터 `docs/parts/part-07/`까지의 한국어 원고
- Section 파일 수: 364개
  - Part 1: 57개
  - Part 2: 63개
  - Part 3: 52개
  - Part 4: 59개
  - Part 5: 53개
  - Part 6: 54개
  - Part 7: 26개
- 참고로 Part 시작/마무리 페이지까지 포함하면 378개다.
- 대조 대상: `docs/reference/concept-glossary-terms/*.ko.md`
- 적용 기준: `management/guidelines/concept-glossary-guidelines.md`의 역할 분류, 제외 조건, 표준화 테스트

## 판정 기준

표제어는 다음 네 조건을 모두 만족할 때만 유지하거나 새로 세운다.

- 표준성: 학술·기술 문맥에서 안정적으로 정의되는 용어인가.
- 독립성: 상위 표제의 하위 설명으로 흡수하면 의미 혼선이 생기는가.
- 지속성: 특정 예제, 도구 사용법, 원고 작업 절차가 아니라 후속 Part에서도 반복되는 개념 기준점인가.
- 범위 안정성: 한글 표제, 영어 기준 용어, slug가 같은 의미 범위를 가리키는가.

다음 표현은 표제로 세우지 않는다.

- 일반어가 우세한 단어: 평가, 검토, 질의, 환경, 상태, 행동, 계획, 경로 등
- 문서·작업 형식 이름: 테이블, 로그, 테스트, 패키지, 브랜치, 서버, 작업흐름 등
- 코드 예제 설명에 묶인 구현 단위: 배열, 트리, 커널, 실행 환경, 의존성 등
- 상위 개념의 구성요소 이름: 외부 리소스, 외부 도구, 외부 시스템 등

단, 위 단어가 표준 개념의 일부로 좁혀진 경우에는 단독 표제가 아니라 문맥 한정 표제로만 검토한다. 예를 들어 `환경`은 제외하지만 `강화학습 환경(reinforcement learning environment)`은 강화학습 문제 정의의 표준 구성요소이므로 유지할 수 있다. `입력`과 `출력`도 단독 표제는 만들지 않지만, 모델링 문제 정의의 기준 축으로 쓰이는 `모델 입력 정의(model input)`, `모델 출력 정의(model output)`은 유지할 수 있다.

## 즉시 삭제 후보

아래 항목은 현재 Part 1~7 원고에서 표준 개념 기준점보다 작업·형식·도구 설명 또는 하위 설명 성격이 강하다. 단어별 파일과 공개 색인 include 삭제를 우선 검토한다.

정리 상태: 2026-07-29에 아래 후보의 단어별 원고와 언어별 공개 색인 include를 제거했다. 이후 같은 의미가 다시 필요해 보이면 새 항목을 바로 만들지 않고 `신규 표제 검토 큐`에서 재검토한다.

| 현재 slug | 현재 표제 | 대표 위치 | 판정 | 근거 |
| --- | --- | --- | --- | --- |
| `code` | 프로그램 코드(program code) | `P2-1.2` | 삭제 후보 | 본문에서는 Python 예제를 설명하는 매체로 쓰인다. 정확 표제 `프로그램 코드`는 Part 1~7 Section 본문에서 별도 기준점으로 반복되지 않는다. |
| `runtime` | 실행 환경(runtime) | `P2-7.1` | 삭제 후보 | P2의 로컬/노트북 실행 조건 설명에 묶인다. AI 개념보다 Python 학습 환경 관리어에 가깝다. |
| `dependency` | 의존성(dependency) | `P2-7.5` | 삭제 후보 | 패키지·환경 재현 설명에 묶인 구현 관리어다. 개념사전보다 P2 실행 환경 본문에서 설명하는 편이 낫다. |
| `version-control` | 버전 관리(version control) | `P2-14.1` | 삭제 후보 | Git 학습을 위한 도구 운용 개념이다. AI 핵심 개념사전 표제로 두면 범위가 과도하게 넓어진다. |
| `document-reproducibility` | 문서 재현성(document reproducibility) | `P2-14.2` | 삭제 후보 | 이 저장소와 문서 제작 절차에 가까운 관리 개념이다. 재현성 자체는 `reproducibility` 아래에서 설명하면 충분하다. |
| `calculation-language` | 계산 언어(calculation language) | `P2-1.1` | 삭제 후보 | 수학을 읽는 비유적 설명 축이다. 표준 학술 용어로 독립 표제화하기 어렵다. |
| `notation` | 압축 표기(notation) | `P2-1.1` | 삭제 후보 | 수식 읽기 보조 설명이다. `수식`, `표기`, `인덱스`를 각각 표제로 늘리는 흐름을 만들 수 있다. |
| `expression` | 식(expression) | `P2-2.1` | 삭제 후보 | 수학 기초 설명의 하위 요소다. `함수`, `변수`, `손실 함수` 같은 실제 중심 개념 안에서 처리한다. |
| `service-operation` | 서비스 운영(service operation) | `P1-14.6` | 삭제 후보 | 운영 일반어 성격이 강하다. 본문에서는 비용, 실패, 지연, 제한을 묶어 설명하는 장 제목 성격에 가깝다. |
| `business-outcome` | 업무 성과(business outcome) | `P1-4.4` | 삭제 후보 | 문제 정의의 바깥 결과를 설명하는 표현이다. 독립 표제보다 `문제 정의` 또는 `모델 평가` 문맥에서 설명한다. |
| `orchestration` | 오케스트레이션(orchestration) | `P1-14.1` | 삭제 후보 | Part 1에서는 모델·데이터·도구 연결 흐름 설명에 국소적으로 쓰인다. 독립 표준 표제보다 `AI 에이전트`, `도구 사용`, `MCP` 하위 설명이 적합하다. |
| `external-resource` | 외부 리소스(external resource) | `P1-14.4` | 삭제 후보 | MCP 구성요소 이름에 가깝다. `MCP` 또는 `도구 사용` 항목 안에서 `리소스`로 설명한다. |
| `external-tool` | 외부 도구(external tool) | `P1-14.1` | 삭제 후보 | `에이전트 도구 사용(tool use)`의 하위 구성요소다. 별도 표제로 두면 도구/리소스/시스템이 모두 표제로 늘어난다. |
| `external-system` | 외부 시스템(external system) | `P1-14.2` | 삭제 후보 | 일반 시스템 용어가 우세하다. RAG와 에이전트 도구 사용 설명 안에서 필요한 만큼만 둔다. |
| `ai-application` | AI 앱(application) | `P1-14.1` | 삭제 후보 | 앱/서비스 구분 설명의 편의 표현이다. 표준 학술 개념보다 제품·구성요소 명칭에 가깝다. |

## 상위 표제 흡수 후보

아래 항목은 원고에서 자주 등장하지만 단독 표제로 두면 일반어 표제가 늘어난다. 삭제보다 상위 표제 흡수 또는 대표 표제 좁힘을 우선 검토한다.

정리 상태: 2026-07-29에 `array`, `tree`, `graph`, `route`, `planning`은 단어별 원고와 공개 색인 include를 제거했다. `kernel`, `search`, `state`, `event`, `visualization`, `metadata`, `permission`, `output-structure`, `model-input`, `model-output`, `model-score`, `evaluation-design`, `accountability`, `guardrail`, `tool-use`, `data-structure`는 한국어 표제를 문맥 한정 표현으로 좁혔다. 본문 직접 링크가 있는 항목은 slug와 앵커를 유지하고 표제·정의 범위를 먼저 좁힌다.

| 현재 slug | 현재 표제 | 권장 처리 | 근거 |
| --- | --- | --- | --- |
| `array` | 배열(array) | 삭제 완료 | P2에서 NumPy와 자료구조 기초를 설명하는 역할이 크다. 사용자가 지적한 `테이블`, `패키지`, `테스트`, `로그`와 같은 과잉 표제 흐름을 만들 수 있다. |
| `tree` | 트리(tree) | 삭제 완료 | P2 자료구조 설명과 P4 결정트리 설명이 섞인다. 단독 `트리`보다 `결정트리`가 표제 안정성이 높다. |
| `graph` | 그래프(graph) | 삭제 완료 | 그래프 이론·자료구조·시각화 그래프가 섞일 위험이 있다. P1 탐색과 P2 자료구조에서는 하위 설명으로 충분하다. |
| `kernel` | SVM 커널(kernel) | 표제명 좁힘 완료 | P2 노트북 커널과 P4 SVM 커널이 같은 표면형을 공유한다. 단독 `커널` 표제는 혼란이 크다. |
| `route` | 경로(route, path) | 삭제 완료 | 파일 경로, 학습 경로, 탐색 경로가 원고 전체에서 함께 등장한다. 단독 `경로`는 일반어 우세다. |
| `search` | 상태공간 탐색(search) | 표제명 좁힘 완료 | `검색(retrieval/search)`과 `탐색(search)`이 같이 쓰인다. 단독 표제는 검색·탐색 혼동이 생긴다. |
| `planning` | 계획(planning) | 삭제 완료 | 일반 계획과 AI planning이 분리되어야 한다. 현재 Part 1에서는 길찾기·자율주행 예시 설명이 중심이다. |
| `state` | 에이전트 상태(state) | 표제명 좁힘 완료 | 상태는 일반어 사용이 매우 많다. 현재 표제는 강화학습과 에이전트 실행 문맥으로 좁혀 관리한다. |
| `action` | 에이전트 행동(action) | 유지 | 이미 단독 `행동`이 아니라 에이전트 문맥으로 좁혀져 있다. 다만 새 표제 추가 때 `행동` 단독 표제는 금지한다. |
| `event` | 확률 사건(event) | 표제명 좁힘 완료 | 일반 사건, 뉴스 사건, 확률 사건이 섞인다. P2-5.1의 확률 문맥을 제목에서 드러내야 한다. |
| `visualization` | 데이터 시각화(visualization) | 표제명 좁힘 완료 | 일반 그림·도식·차트 제작과 데이터 분석 시각화가 섞인다. 표제로 유지하려면 데이터 분석 문맥을 드러낸다. |
| `metadata` | 문서 검색 메타데이터(metadata) | 표제명 좁힘 완료 | 표준 용어로 볼 수 있지만 일반 부가정보 의미도 강하다. P1-13.4와 P6의 검색·출처 관리 문맥으로 좁히는 편이 안전하다. |
| `permission` | 도구 실행 권한(permission) | 표제명 좁힘 완료 | 일반 권한과 보안·에이전트 실행 권한이 섞인다. 단독 표제는 일반어 우세다. |
| `output-structure` | 모델링 출력 구조(output structure) | 표제명 좁힘 완료 | Part 3에서는 모델 결과의 그릇을 정하는 모델링 개념이다. 현재 제목은 일반 모델 출력과 혼동된다. |
| `model-input` | 모델 입력 정의(model input) | 표제명 좁힘 완료 | 일반 `입력` 단독 표제는 금지한다. Part 1·3·4에서 문제 정의의 기준 축으로 쓰이므로 모델링 입력 정의 문맥으로 유지한다. |
| `model-output` | 모델 출력 정의(model output) | 표제명 좁힘 완료 | 일반 `출력` 단독 표제는 금지한다. Part 1·4·5에서 문제 정의와 평가 기준을 잇는 축으로 쓰이므로 모델링 출력 정의 문맥으로 유지한다. |
| `model-score` | 모델 후보 점수(model score) | 표제명 좁힘 완료 | `score` 일반어와 평가 점수, 모델 내부 점수가 섞인다. P1-5.2의 후보 비교 수치로 좁힌다. |
| `evaluation-design` | 모델 평가 설계(evaluation design) | 표제명 좁힘 완료 | 단독 `평가` 확장을 막으려면 모델 평가 문맥이 제목에 들어가야 한다. |

## 유지하되 표제 범위를 명시할 후보

아래 항목은 일반어를 포함하지만 표준 학술·기술 개념으로 쓰이는 범위가 비교적 분명하다. 삭제보다 제목과 첫 문장에 문맥을 더 드러내는 방향이 좋다.

| 현재 slug | 현재 표제 | 유지 조건 |
| --- | --- | --- |
| `reinforcement-learning-environment` | 강화학습 환경(reinforcement learning environment) | 유지 가능. 단, `환경` 단독 표제 추가를 금지하고 첫 문장에서 일반 실행 환경과 구분한다. |
| `human-oversight` | 인간 감독(human oversight) | 유지 가능. `사람 검토`가 아니라 중단·수정·반려 권한이 있는 감독 구조로 설명해야 한다. |
| `accountability` | AI 책임성(accountability) | 유지. `책임` 단독 표제와 `권리와 책임` 같은 묶음 표제는 만들지 않는다. |
| `safety` | AI 시스템 안전성(safety) | 유지 가능. 단독 `안전`은 제외하고 AI 시스템의 피해 경로와 제한 조건으로 설명한다. |
| `guardrail` | AI 가드레일(guardrail) | 유지. 정책·실행 제약 문맥을 제목과 첫 문장에서 드러낸다. |
| `model-context-protocol-mcp` | 모델 컨텍스트 프로토콜(Model Context Protocol, MCP) | 특정 공개 프로토콜이므로 유지 가능. 단, 하위 구성요소인 서버·리소스·도구를 각각 독립 표제로 늘리지 않는다. |
| `tool-use` | 에이전트 도구 사용(tool use) | 유지. 외부 도구/외부 리소스/외부 시스템은 이 항목 안으로 흡수한다. |
| `data-structure` | 자료구조 선택(data structure) | 유지. Part 2 기초 복구의 기준점으로만 관리하고, 배열·테이블·트리·그래프 같은 하위 구조명은 독립 표제로 늘리지 않는다. |

## 현재는 새 표제로 만들지 않을 표현

Part 1~7 원고에서 반복되지만, 현재 기준으로는 새 표제를 만들지 않는다.

| 표현 | 원고상 역할 | 처리 |
| --- | --- | --- |
| 브랜치(branch) | Git 사용과 문서 재현성 설명 | 표제 제외. `버전 관리`도 삭제 후보이므로 새 표제 금지 |
| 테이블(table) | 표 데이터, Markdown 표, 자료구조 예시가 섞임 | 표제 제외. 필요하면 `자료구조` 또는 `데이터셋` 안에서 설명 |
| 패키지(package) | Python 환경 관리 | 표제 제외 |
| 테스트(test) | 평가 데이터, 코드 테스트, 배포 확인이 섞임 | 표제 제외. 필요하면 `테스트 데이터`만 유지 |
| 로그(log) | 수학 로그와 실행 로그가 섞임 | 단독 표제 금지. 수학은 `수학 로그(logarithm)`, 실행 기록은 본문 설명 |
| 서버(server) | MCP 서버, 웹 서버, 원격 서버가 섞임 | 표제 제외. MCP 구성요소 설명으로만 처리 |
| 수식(formula) | 수학 설명 형식 | 표제 제외. `함수`, `손실 함수`, `목적 함수` 같은 실제 개념으로 연결 |
| 평가(evaluation) | 일반 검토/시험/모델 평가가 섞임 | 단독 표제 금지. `평가 지표`, `모델 평가 설계`, `모델 검증`처럼 좁힌 표제만 허용 |
| 검토(review) | 사람 검토, 원고 검토, 모델 결과 검토가 섞임 | 표제 제외. `인간 감독`이나 본문 절차로 처리 |
| 질의(query) | 검색 입력, 사용자 질문, DB 질의가 섞임 | 표제 제외. 필요 시 `검색 질의`처럼 문맥 한정 후 재검토 |
| 환경(environment) | 실행 환경, 강화학습 환경, 배포 환경이 섞임 | 단독 표제 금지. `강화학습 환경`만 유지 가능 |
| 작업흐름(workflow) | 에이전트 설명과 원고·운영 절차가 섞임 | 표제 제외. `AI 에이전트`, `도구 사용` 안에서 설명 |
| 사람 검토 | 검토 절차 | 표제 제외. `인간 감독`과 구분해 본문 절차로만 설명 |
| 권리와 책임 | 여러 법·윤리 항목 묶음 | 표제 제외. `저작권`, `개인정보`, `책임성` 등으로 분해 |

## 신규 표제 검토 큐

새로운 개념사전 표제가 필요해 보일 때는 단어별 파일을 바로 만들지 않고 이 큐에 먼저 추가한다. 후보는 `보류`를 기본값으로 두며, 등재 조건을 모두 통과하고 기존 상위 표제로 흡수하기 어렵다는 근거가 확인된 뒤에만 개념사전 항목 생성으로 넘긴다.

| 후보 표제 | 영어 기준 용어 | 제안 Section | 역할 분류 | 기존 상위 표제/관련 표제 | 1차 판정 | 필요한 확인 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  | 보류 | 표준성, 독립성, 지속성, slug 안정성 확인 |

### 신규 표제 검토 절차

1. 후보가 나온 Section ID와 문맥을 먼저 적는다.
2. 후보 표현을 역할 분류표의 `표준 개념`, `문맥 한정 개념`, `하위 설명`, `작업·형식 이름`, `구현·도구·사례`, `임시 표현` 중 하나로 분류한다.
3. 기존 개념사전 단어별 원고와 공개 색인에서 같은 의미를 이미 다루는 표제가 있는지 확인한다.
4. 기존 상위 표제의 하위 설명으로 충분하면 새 항목을 만들지 않고 상위 표제 흡수 후보로 옮긴다.
5. 일반어가 전문 문맥에서 좁혀진 경우에는 단독 표제 대신 문맥 한정 표제명을 적는다.
6. 영어 기준 용어와 slug가 같은 범위를 안정적으로 가리키는지 확인한다.
7. 등재 조건을 모두 통과한 후보만 개념사전 단어별 원고 생성 대상으로 넘긴다.

## 후속 작업 제안

1. `상위 표제 흡수 후보`는 삭제 전에 중심 Section의 본문 링크가 끊기는지 확인하고, 필요한 경우 상위 표제의 `함께 볼 개념` 또는 본문 첫 설명으로 흡수한다.
2. `유지하되 표제 범위를 명시할 후보`는 제목·첫 문장·slug가 같은 범위를 가리키도록 변경 후보로 누적 관리한다.
3. 신규 표제가 필요해 보이면 먼저 `신규 표제 검토 큐`에 추가하고, 검토 절차를 통과한 후보만 개념사전 항목으로 만든다.
4. 이후 새 표제 추가 검토 때는 이 리포트의 `현재는 새 표제로 만들지 않을 표현` 목록을 금지 예시가 아니라 역할 판정 기준의 빠른 참조로 사용한다.
