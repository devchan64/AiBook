# Part 6 오픈 체크리스트

작성일: 2026-07-21

이 문서는 Part 6 개편 후 남은 원고 품질 점검 항목을 관리한다. 기준 문서는 `management/authoring/part-06-restructure-proposal.md`이다.

## 적용 기준

- Part 6은 토큰이 아니라 생성형 AI 산출물에서 시작한다.
- LLM은 생성형 AI 전체가 아니라 대표 사례로 둔다.
- 각 Section은 비교, 구분, 판정, 기록 읽기 중 하나의 학습 산출물을 남겨야 한다.
- Chapter와 Section 분량은 균등하게 맞추지 않는다. 중요도, 학습 병목, 후속 회수 빈도, 흡수구간 필요성으로 판단한다.
- strict build는 배포 전 또는 큰 구조 패치 종료 후 한 번에 확인한다.

## 구조 적용 체크

- [x] 기존 Part 6 원고 아카이브 생성
- [x] 기준 목차 문서 생성
- [x] 새 Chapter 1 추가
- [x] 토큰 장을 P6-2.1~P6-2.5로 통합 재구성
- [x] 기존 Section을 새 번호 체계로 재정렬
- [x] release notes를 새 Section ID 기준으로 초기 정리
- [x] 각 Module 첫 Section의 낯섦 완충 문단 정밀 검토
- [x] Python 예제, CSV, 그래프, Mermaid가 새 Section 중심 감각을 지지하는지 재검토
- [x] 내부 링크와 개념사전 링크 정밀 점검
- [x] 다국어 index/summary/nav 동기화 검토
- [ ] 배포 전 strict build 확인

## Module별 후속 검토

| Module | 후속 검토 질문 |
| --- | --- |
| Module 1 | 생성형 AI 산출물, LLM 대표 경로, 후보 선택 반복이 초심자에게 충분히 분리되어 보이는가? |
| Module 2 | 토큰 장 통합 후 정의, token ID, 토큰화 영향, 활용, 보충학습의 흐름이 자연스러운가? |
| Module 3 | Transformer/GPT/next-token 설명이 구조 이름 나열이 아니라 다음 후보 생성 감각으로 읽히는가? |
| Module 4 | 학습과 조정이 방법 이름 나열이 아니라 무엇을 배우고 맞추는가로 읽히는가? |
| Module 5 | 프롬프트 입력 설계, 한계, 답변 경로 전략, 프롬프트 실험 루프가 구분되고 RAG와 벡터 DB로 넘어가는 이유가 충분히 열리는가? |
| Module 6 | 도구 사용, 함수 호출, 에이전트, MCP, 하네스가 제품 기능 목록처럼 보이지 않는가? |
| Module 7 | 좋은 답변과 서비스 가능한 답변의 차이가 평가, 운영, 실패 대응으로 이어지는가? |
| Module 8 | 발전사와 BERT가 본류 뒤의 배경 지도 역할을 하는가? |

### Module 2. 텍스트 생성의 입력 단위와 표현

#### Chapter 2. 토큰과 토큰화

개편 전 토큰 장은 8개 Section이었고, 현재 목차에서는 이를 5개 Section으로 통합했다. 이 Chapter는 단순 분량보다 `기존 중심 질문이 현재 Section 안에서 어디로 회수되는가`를 기준으로 검토한다.

| 현재 Section | 회수한 아카이브 기준 | 구조상 주의할 지점 | 확인 기준 |
| --- | --- | --- | --- |
| P6-2.1 왜 LLM 입력과 출력을 토큰 단위로 읽는가 | P6-1.1 왜 토큰이 필요한가 | `바로 적용해 보면`의 사람 기준 -> 모델 기준 전환이 압축되기 쉬움 | 비용, 잘림, 문맥 보존 문제를 독자가 토큰 관점으로 바꾸어 말할 수 있어야 한다. |
| P6-2.2 토큰과 token ID는 모델 입력에서 어떻게 쓰이나 | P6-1.2 토큰은 무엇인가 + P6-1.3 토큰은 모델 입력에서 어떻게 쓰이나 | 정의, symbolic AI와의 구분, token ID 순서열이 한 Section에 합쳐짐 | 토큰 문자열, 토큰 수, token ID, 순서와 위치를 서로 다른 층위로 구분해야 한다. |
| P6-2.3 토큰화는 무엇을 바꾸는가 | P6-1.4 토큰화는 무엇인가 + P6-1.5 토큰화는 무엇을 바꾸는가 | 절차 설명과 운영 영향 설명이 한 Section에 합쳐져 중심 질문이 둘로 갈라지기 쉬움 | 토큰화 절차 설명이 비용, 청크, 출력 보존 판단으로 자연스럽게 이어져야 한다. |
| P6-2.4 토큰 관점은 어떤 운영 판단으로 이어지는가 | P6-1.8 토큰 관점은 어디에 활용되는가 | 활용처 나열이 운영 레버 선택으로 재구성됨 | 실패 장면을 입력 압축, 문맥 재묶기, 비용 관측, 입력 분할, 출력 축약, 평가 로그 해석 중 하나로 바꿀 수 있어야 한다. |
| P6-2.5 보충학습: 토크나이저 계열 차이는 언제 드러나는가 | P6-1.6 대표 토크나이저 계열은 무엇인가 + P6-1.7 토큰화 종류 차이는 언제 드러나는가 | 계열 정의, 실행 예제, 운영 장면이 보충학습 하나로 묶임 | BPE, WordPiece, SentencePiece를 이름 암기가 아니라 절단 기준과 운영 관찰값 차이로 읽어야 한다. |

Chapter 2 검토에서 우선 확인할 구조적 차이는 다음 세 가지다.

- P6-2.2와 P6-2.3은 통합 Section이므로 중간 소제목이 반복되거나 두 Section의 중심 질문이 서로 겹치지 않는지 확인한다.
- P6-2.3은 절차 설명과 운영 영향 설명이 모두 들어 있으므로, 도식과 사례가 각각 어느 질문을 맡는지 분리되어야 한다.
- P6-2.5는 보충학습으로 길어질 수 있지만, 본류를 새로 여는 절이 아니라 토크나이저 계열 차이를 비용, 청크, 혼합 표기 길이 예측으로 회수하는 절이어야 한다.

### Module 5. 프롬프트와 근거 보강

#### Chapter 10. 프롬프트 엔지니어링

Chapter 10은 프롬프트를 더 잘 쓰는 요령 목록이 아니라, 입력에서 조정할 수 있는 것과 시스템 구조로 넘겨야 하는 것을 가르는 장이다. P6-10.3과 P6-10.4를 분리한 뒤에는 `답변 경로를 더 보는 전략`과 `프롬프트 후보를 반복 비교하는 전략`이 섞이지 않는지 확인한다.

| 현재 Section | 중심 구분 | 확인 기준 |
| --- | --- | --- |
| P6-10.1 프롬프트 엔지니어링은 무엇을 입력에서 조정하는가 | 지시, 맥락, 예시, 출력 형식 | 프롬프트를 주문 문장이 아니라 모델 입력을 설계하는 제어 지점으로 읽을 수 있어야 한다. |
| P6-10.2 프롬프트 한계는 언제 시스템 구조로 넘어가는가 | 최신성, 근거성, 계산, 실행, 재현성 | 프롬프트를 고칠 문제와 RAG, 도구, 로그, 평가 구조로 넘길 문제를 구분해야 한다. |
| P6-10.3 보충학습: 답변 경로 관찰과 비교 | 한 경로 관찰과 여러 경로 비교 | CoT와 self-consistency를 근거 보장이나 실행 보장이 아니라 답변 경로 관찰 전략으로 설명해야 한다. |
| P6-10.4 보충학습: automatic prompt optimization은 프롬프트 실험을 어떻게 반복 개선하는가 | 후보 프롬프트 평가 루프 | 자동화보다 평가 기준, 검증 세트, 실패 누락 위험이 먼저 읽혀야 한다. |

### Module 7. 평가, 운영, 실패 대응

#### Chapter 16-17. LLM 결과 평가와 운영 가능성 판단

Chapter 16은 LLM 내부 알고리즘을 평가하는 장이 아니라, LLM이 실제로 내놓은 답변, 요약, 안내문 같은 결과물을 검토 가능한 기준으로 읽는 장이다. Chapter 17은 그 결과물을 실제 서비스 후보로 남길 수 있는지 운영 제약과 실패 대응 기준으로 다시 거르는 장이다. P6-16.2는 자동 평가를 휴리스틱 점수표로 소개하는 데서 멈추지 않고, 사람이 읽을 검토 패킷을 만드는 구조까지 보여 주어야 하며, P6-17.1과 P6-17.2는 평가 통과 이후에도 운영 후보 통과와 실패 복구 경로가 별도로 필요하다는 점을 보여 주어야 한다.

| 현재 Section | 중심 구분 | 확인 기준 |
| --- | --- | --- |
| P6-16.1 자연스러운 답과 품질 기준을 나누는 LLM 평가 | 자연스러운 답변과 평가 가능한 품질 축 | 모델별 출력 후보를 정확성, 근거성, 안전성, 형식 적합성, 유용성 기준으로 다시 읽을 수 있어야 한다. |
| P6-16.2 자동 평가와 사람 평가의 분업 | 반복 가능한 자동 채점기와 맥락을 읽는 사람 검토 | CSV나 코드가 사전 정답 라벨로 승인 경로를 만들지 않아야 한다. 자동 평가는 LLM 출력 후보의 표면 기준과 선택적 LLM 판정기 신호를 기록하고, 사람 평가는 코드가 만든 검토 패킷을 읽는 구조로 보여야 한다. |
| P6-17.1 비용·지연·사용량으로 다시 거르는 운영 제약 | 평가 통과와 운영 후보 통과의 분리 | 품질 점수만으로 배포 후보를 고르지 않아야 한다. 비용, 지연 시간, 처리량, 복구 가능성을 함께 보고, 실패 축 전체와 먼저 조정할 병목 축을 구분해야 한다. |
| P6-17.2 오류를 복구 경로로 나누는 운영 중 실패 대응 | 실패 신호와 복구 경로의 분리 | 실패를 오류 문구 처리로 축소하지 않고 retry, fallback, stop, approval 중 어느 경로로 보낼지 trace와 함께 설명해야 한다. |

Module 7 검토에서 우선 확인할 구조적 차이는 다음과 같다.

- 평가 대상은 LLM 알고리즘 내부가 아니라 LLM 결과물이어야 한다.
- 자동 평가는 최종 승인 판단이 아니라 형식, 길이, 근거 신호, 금지 표현 같은 반복 가능한 채점 기준이어야 한다.
- 사람 평가는 코드가 대신 채점하지 않고, 후보 출력과 검토 질문을 함께 남긴 패킷을 사람이 읽는 구조로 설명되어야 한다.
- 운영 제약은 평가 점수의 부록이 아니라 비용, 지연 시간, 처리량, 복구 가능성을 기준으로 배포 후보를 다시 거르는 별도 판단이어야 한다.
- 실패 대응은 실패를 한 종류로 뭉개지 않고, 원인 신호와 복구 경로를 함께 남기는 구조로 설명되어야 한다.
