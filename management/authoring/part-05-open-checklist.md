# Part 5 체크포인트 노트

이 문서는 Part 5를 목차 순서대로 읽을 때 각 Section이 담아야 할 중심 주제를 개괄식으로 정리한 체크포인트 노트입니다.

## 사용 원칙

- 이 노트는 `docs/table-of-contents.md`를 기준으로 Part 5의 시작/마무리 페이지, Chapter, Section을 같은 순서로 훑기 위한 개요 문서입니다.
- 순차 점검 기록, 품질 상태 메모, 부실 판정 로그는 이 문서에서 운영하지 않습니다.
- 체크포인트는 `딥러닝 계산 구조 -> 손실과 gradient -> 학습 루프 -> 구조 분기 -> 생성` 흐름을 각 Section이 어떻게 닫는지 확인하는 데 목적을 둡니다.
- `학습 관점 누락 점검`은 Part 원고가 잃기 쉬운 설명 흐름을 점검하고, `목차 기준 체크포인트`는 Part 원고가 유지해야 할 구조 흐름을 관리합니다.

## 학습 관점 누락 점검

- 각 Chapter를 볼 때 `이 구조가 왜 나왔는가`와 `이전 구조의 어떤 한계를 메우는가`가 함께 드러나는지 확인합니다.
- 각 Section를 볼 때 수식이나 이름 소개만 있고 `입력값이 어떻게 바뀌는가`, `어떤 실패가 줄어드는가`, `어떤 한계가 남는가`가 빠지지 않았는지 확인합니다.
- 학습 루프 장에서는 `forward`, `loss`, `backward`, `update`가 실제로 어디서 다른 역할을 하는지 구분이 유지되는지 확인합니다.
- CNN, RNN, attention, Transformer 장에서는 구조 이름보다 `어떤 입력 구조 문제를 푸는가`가 먼저 읽히는지 확인합니다.
- 생성 모델 장에서는 `학습 목표`와 `샘플링 선택`이 섞이지 않고 분리되어 있는지 확인합니다.
- Part 5에서는 특히 `오버뷰화`, `구조 이름 나열`, `예시 없는 계산 설명`이 빠지지 않는지 다시 봅니다.
- 딥러닝을 `직관의 복제`로 단정하지 않고, 사람이 설계하던 특징에서 모델이 학습하는 표현으로 무게중심이 옮겨간 계산 방식으로 설명하는지 확인합니다.
- 후속 원고 검토에서는 `직관`, `추론`, `inference`, `reasoning`이 섞이지 않게 용어를 점검합니다.

## Section 경계 정리

- Part 5 전체 경계는 딥러닝 용어 사전이 아니라 `기본 계산 구조 -> 출력과 손실 -> gradient와 학습 루프 -> 안정화 -> 계산 확장 -> 표현 학습과 구조 분기 -> 생성 모델 입구`를 따라, 값이 어떻게 흐르고 무엇이 학습되는지 읽게 하는 데 둡니다.
- P5-1~P5-3은 신경망의 최소 계산 구조를 닫는 구간입니다. 퍼셉트론, 선형 결합, 활성화 함수는 Part 4의 선형 모델과 연결하되, 딥러닝 전체 구조나 학습 알고리즘을 앞당겨 설명하지 않습니다.
- P5-3의 활성화 함수 절들은 비선형성, 출력 범위, 0 중심성, ReLU의 실용성과 한계, 출력층 선택 기준을 나누어 둡니다. 함수 목록 암기나 미분 공식 확장으로 흐르지 않게 합니다.
- P5-4~P5-5는 `정답과 출력의 차이`가 손실이 되고, 손실이 gradient 신호로 바뀌어 뒤쪽 계산으로 전달되는 경계를 닫습니다. 통계적 추정 이론이나 자동미분 구현 세부는 필요한 직관 수준에서만 다룹니다.
- P5-6은 학습 루프의 역할 분리를 담당합니다. forward, loss, backward, optimizer step, step, batch, epoch, training mode, evaluation mode를 서로 다른 위치의 개념으로 구분하고, 모델 실행을 reasoning이나 LLM 추론 설명과 섞지 않습니다.
- P5-7~P5-8은 학습 루프를 안정적으로 움직이게 하는 제어 장치 구간입니다. optimizer, learning rate, adaptive update, scheduler, clipping, regularization, dropout, initialization, normalization은 `무엇을 조절하는가` 기준으로 나누고, 고급 최적화 이론이나 프레임워크 설정 매뉴얼로 확장하지 않습니다.
- P5-7.4~P5-7.8과 P5-8.3~P5-8.4는 보충학습으로 유지합니다. 본편에서 닫은 학습 루프를 더 잘 읽기 위한 주장 구분, 계열 지도, 상태값, 불안정성 확인에 한정하고, optimizer 비교 실험 보고서나 튜닝 레시피가 되지 않게 합니다.
- P5-9는 하드웨어 설명 자체가 아니라 딥러닝이 큰 계산으로 확장될 수 있었던 조건을 설명하는 구간입니다. GPU, 병렬 처리, batch, tensor shape는 계산 구조 이해에 필요한 만큼만 다루고, 시스템 아키텍처나 CUDA 프로그래밍으로 넘어가지 않습니다.
- P5-10은 Part 3의 사람이 만든 특징, Part 4의 전통적 머신러닝 입력 판단에서 Part 5의 표현 학습으로 무게중심이 옮겨가는 연결점입니다. `Software 2.0`은 표준 정의가 아니라 설명 보조 비유로만 취급합니다.
- P5-11~P5-13은 입력 구조 문제별 분기입니다. CNN은 지역 패턴, RNN/LSTM/GRU는 순차 상태와 장기 의존성, attention은 필요한 위치 재참조로 경계를 나누고, 각 구조를 모델 이름 나열이 아니라 어떤 입력 문제를 덜어 주는가로 설명합니다.
- P5-13.3은 QKV와 multi-head attention 이름을 처음 읽게 하는 보충학습입니다. attention 직관을 수식 이름으로 회수하는 자리로 두고, Transformer 블록 전체 설명은 P5-14로 넘깁니다.
- P5-14는 self-attention 하나가 아니라 self-attention, feed-forward, residual connection, layer normalization이 반복되는 Transformer 블록을 읽는 장입니다. P5-14.1~P5-14.5는 블록 구조, 안정화, 병렬 처리, 긴 문맥 재참조를 닫고, P5-14.6~P5-14.8은 부품별 보충학습으로 유지합니다.
- P5-14의 Python 예제는 실제 Transformer 구현이나 성능 측정이 아니라, 토큰 표현이 attention, feed-forward, residual, normalization을 지나며 어떻게 관찰되는지 확인하는 용도로 제한합니다.
- P5-15는 생성형 AI 본편이 아니라 Part 6으로 넘어가기 전 필요한 딥러닝 관점의 입구입니다. 생성물 중심 출력, 후보 분포, 샘플링 선택을 분리하고, 프롬프트, 토큰화, LLM 학습·정렬·RAG·에이전트 설명은 Part 6으로 넘깁니다.
- P5-15.3의 Ollama 예제는 선택 예제로 유지합니다. 로컬 실행 환경 안내나 LLM 서비스 사용법이 아니라, 같은 후보 분포에서도 샘플링 설정이 실제 출력을 바꿀 수 있다는 관찰에 한정합니다.
- Part 5에는 한국어, 영어, 중국어 Section이 함께 존재하므로 경계 수정 시 Section ID와 Version 대응을 같이 확인합니다. 특히 gradient, inference, reasoning, training, evaluation, attention, Transformer, sampling은 언어별 번역어가 개념 경계를 흐리지 않게 맞춥니다.
- 긴 Section은 길이 자체보다 중심 질문이 하나인지 먼저 봅니다. 여러 예제와 보충 설명이 한 Section 안에 있더라도 계산 역할이 하나로 닫히면 유지하고, 서로 다른 구조나 Part 6 내용까지 들어오면 분리 후보로 둡니다.

## 목차 기준 체크포인트

- `index.md`: Part 5가 딥러닝 용어 나열이 아니라 계산 구조와 표현 학습 흐름을 읽는 Part라는 점을 분명히 해야 합니다.
- Part 흐름: Module 1부터 Module 3까지는 `기본 계산 구조`, `손실`, `gradient`, `학습 루프`, `안정화`가 한 계산 흐름으로 이어져야 합니다.
- Part 흐름: Module 4와 Module 5는 계산 규모 확장과 구조 분기가 각각 어떤 입력 문제를 풀기 위해 나왔는지 연결되어야 합니다.
- `summary.md`: Module 6에서 분리한 `학습 목표`와 `샘플링`을 Part 6의 생성형 AI 설명으로 넘겨야 합니다.

### Module 1. 신경망의 기본 계산 구조

#### Chapter 1. 퍼셉트론 `딥러닝 구조`

- 중심축: 신경망 계산의 최소 단위로서 입력, 가중치, 경계 판단 구조를 붙잡습니다.
- `P5-1.1`: 퍼셉트론을 입력, 가중치, 합산, 경계 판단의 최소 계산 구조로 설명해야 합니다.
- `P5-1.2`: 선형 결합과 활성화가 왜 함께 있어야 하는지 보여 주어야 합니다.

#### Chapter 2. 다층 신경망 `딥러닝 구조`

- 중심축: 층을 깊게 쌓을수록 중간 표현이 어떻게 풍부해지는지 보여 줍니다.
- `P5-2.1`: 다층 구조가 왜 더 복잡한 패턴을 표현할 수 있는지 설명해야 합니다.
- `P5-2.2`: 은닉층이 중간 표현을 만든다는 점을 직관적으로 보여 주어야 합니다.

#### Chapter 3. 활성화 함수 `딥러닝 구조`

- 중심축: 비선형성을 넣는 방식에 따라 어떤 출력 특성과 학습 차이가 생기는지 비교합니다.
- `P5-3.1`: 활성화 함수가 비선형성을 넣는 이유를 잡아야 합니다.
- `P5-3.2`: sigmoid의 출력 범위와 한계를 설명해야 합니다.
- `P5-3.3`: tanh가 0 중심 출력이라는 점에서 무엇이 다른지 보여 주어야 합니다.
- `P5-3.4`: ReLU가 왜 널리 쓰이는지와 한계를 함께 정리해야 합니다.
- `P5-3.5`: 대표 활성화 함수 수식을 비교할 때 어떤 판단 차이가 생기는지 정리해야 합니다.
- `P5-3.6`: 출력층 활성화 선택이 문제 유형과 연결된다는 점을 닫아야 합니다.

### Module 2. 출력과 손실 신호

#### Chapter 4. 손실 함수 `학습 원리`

- 중심축: 모델이 무엇을 더 크게 고쳐야 하는지 손실 숫자가 어떻게 알려 주는지 정리합니다.
- `P5-4.1`: 손실 함수가 무엇을 더 크게 고쳐야 하는지 알려 주는 신호라는 점을 설명해야 합니다.
- `P5-4.2`: 문제 유형별 손실이 왜 다른지와 어떤 오류를 더 민감하게 보는지 정리해야 합니다.

#### Chapter 5. 손실에서 gradient로 `학습 원리`

- 중심축: 손실 숫자가 역전파 가능한 변화 신호로 바뀌는 계산 구조를 이해시킵니다.
- `P5-5.1`: 손실이 gradient 신호로 바뀌는 흐름을 분명히 보여 주어야 합니다.
- `P5-5.2`: 계산 그래프와 자동미분이 backward를 가능하게 하는 구조라는 점을 설명해야 합니다.

### Module 3. 학습 루프와 안정화

#### Chapter 6. 학습 루프와 모델 실행 `학습 원리`

- 중심축: 학습 반복과 실행 단계를 한 루프로 묶고, 각 반복 단위의 역할을 분리해 읽게 합니다.
- `P5-6.1`: forward, loss, backward, optimizer step의 학습 루프를 하나의 반복 구조로 묶어야 합니다.
- `P5-6.2`: step, batch, epoch가 서로 다른 반복 단위라는 점을 구분해야 합니다.
- `P5-6.3`: 학습과 모델 실행의 차이를 파라미터 변화 유무로 분리해야 합니다.
- `P5-6.4`: training mode와 evaluation mode가 왜 달라지는지 설명해야 합니다.

#### Chapter 7. 옵티마이저 `학습 원리`

- 중심축: gradient를 실제 파라미터 update로 바꾸는 규칙과 그 조절 변수를 다룹니다.
- `P5-7.1`: 옵티마이저가 gradient를 실제 update 규칙으로 바꾸는 장치라는 점을 잡아야 합니다.
- `P5-7.2`: 학습률이 update 보폭을 정한다는 점을 설명해야 합니다.
- `P5-7.3`: Adam 같은 적응형 업데이트가 무엇을 추가로 기억하는지 보여 주어야 합니다.
- `P5-7.4`: adaptive optimization의 수렴 보장과 경험적 성능 주장을 구분해 읽는 법을 보충해야 합니다.
- `P5-7.5`: momentum, AdaGrad, RMSProp, Adam의 차이를 큰 그림에서 보충해야 합니다.
- `P5-7.6`: scheduler, warmup, decay가 학습률 조절 전략이라는 점을 보충해야 합니다.
- `P5-7.7`: optimizer state와 parameter-wise update를 처음 읽는 법을 보충해야 합니다.
- `P5-7.8`: gradient clipping이 불안정한 update를 어떻게 제어하는지 보충해야 합니다.

#### Chapter 8. 학습 루프를 안정적으로 만드는 제어 장치 `학습 원리`

- 중심축: 정규화, 드롭아웃, 초기화, 정규화 계층처럼 학습을 흔들리지 않게 하는 제어 위치를 나눕니다.
- `P5-8.1`: 정규화가 목적 함수에 제약을 더하는 방식이라는 점을 설명해야 합니다.
- `P5-8.2`: 드롭아웃이 경로 의존을 줄이는 아이디어라는 점을 보여 주어야 합니다.
- `P5-8.3`: 초기화, 수치 안정성, 배치 정규화를 같은 안정화 문제의 서로 다른 위치로 보충해야 합니다.
- `P5-8.4`: 큰 초기화 스케일이 계산 범위를 어떻게 흔드는지 직접 보여 주어야 합니다.

### Module 4. 계산 확장

#### Chapter 9. GPU와 병렬 처리 `역사와 패러다임`

- 중심축: 딥러닝이 큰 계산 규모로 확장될 수 있었던 하드웨어와 병렬 처리 조건을 설명합니다.
- `P5-9.1`: GPU와 병렬 처리가 딥러닝을 실용적 규모로 확장시킨 이유를 설명해야 합니다.
- `P5-9.2`: 배치와 텐서 계산이 실제로 어떤 데이터 shape를 다루는지 보여 주어야 합니다.

### Module 5. 표현 학습과 구조 분기

#### Chapter 10. 표현 학습 `딥러닝 구조`

- 중심축: 사람이 만든 특징에서 모델이 학습한 표현으로 무게중심이 옮겨간 이유를 정리합니다.
- `P5-10.1`: 표현 학습이 사람이 특징을 직접 설계하던 방식과 어떻게 다른지 정리하고, `Software 2.0`식 비유는 표준 정의가 아니라 소프트웨어 관점의 설명 보조로만 다뤄야 합니다.
- `P5-10.2`: 깊은 층의 표현이 원시 단서에서 과업 가까운 구조로 바뀐다는 점을 보여 주어야 합니다.

#### Chapter 11. CNN `알고리즘`

- 중심축: 지역 패턴을 읽는 구조로 CNN을 이해시키고 ViT와의 입력 단위 차이를 대비시킵니다.
- `P5-11.1`: CNN이 지역 패턴을 읽는 구조라는 점을 설명해야 합니다.
- `P5-11.2`: 합성곱과 풀링이 어떤 계산 역할을 맡는지 분명히 해야 합니다.
- `P5-11.3`: CNN과 ViT 비교를 입력 단위와 구조 차이 중심으로 보충해야 합니다.

#### Chapter 12. RNN, LSTM, GRU `알고리즘`

- 중심축: 순차 상태를 들고 가는 구조가 왜 필요하고 어디서 한계가 드러나는지 다룹니다.
- `P5-12.1`: 순차 구조에서 상태를 들고 가야 하는 이유를 설명해야 합니다.
- `P5-12.2`: 장기 의존성이 왜 어려운지와 LSTM, GRU가 무엇을 보강하는지 보여 주어야 합니다.

#### Chapter 13. Attention `딥러닝 구조`

- 중심축: 필요한 위치를 다시 참조하는 계산으로 attention을 이해시키고 self-attention으로 연결합니다.
- `P5-13.1`: attention이 필요한 위치를 다시 보는 계산이라는 점을 직관적으로 보여 주어야 합니다.
- `P5-13.2`: self-attention으로 이어지는 흐름을 순차 구조 한계와 연결해 설명해야 합니다.
- `P5-13.3`: query, key, value와 multi-head attention을 처음 읽는 최소 기준을 보충해야 합니다.

#### Chapter 14. Transformer `딥러닝 구조`

- 중심축: Transformer를 `관계 읽기`, `위치별 가공`, `정보 흐름 보존`, `값 범위 안정화`가 한 블록으로 반복되는 구조로 먼저 닫고, 그 구조가 왜 `병렬 처리와 긴 문맥 직접 재참조`에서 중요해졌는지 보여 주어야 합니다.
- `P5-14.1 Transformer는 왜 attention 하나로 설명되지 않는가`: Transformer가 self-attention 하나로 닫히지 않고, attention 블록과 feed-forward가 반복되는 기본 구성으로 읽혀야 한다는 문제의식을 세워야 합니다.
- `P5-14.2 Transformer 블록의 네 부품은 각각 무엇을 맡는가`: self-attention, feed-forward, residual connection, layer normalization의 역할을 `관계 읽기`, `위치별 가공`, `정보 흐름 보존`, `값 범위 안정화`로 나누어 설명해야 합니다. 통합된 Python 예제는 현재 토큰 표현이 `input -> after attention -> after feed-forward -> after residual`로 이동하는 흐름을 확인하는 데만 쓰고, normalization의 값 범위 정리는 P5-14.3으로 넘겨야 합니다.
- `P5-14.3 residual과 normalization은 깊은 반복을 어떻게 안정화하는가`: residual과 normalization을 부차적 덧셈·정리가 아니라 깊은 블록 반복에서 정보 흐름과 값 범위를 안정화하는 장치로 설명해야 합니다.
- `P5-14.4 RNN의 상태 전달과 Transformer의 관계 계산은 병렬 처리에서 어떻게 갈라지는가`: RNN의 순차 상태 전달과 Transformer의 토큰 관계 계산을 비교하고, 그 차이가 GPU 병렬 처리와 대규모 학습에 왜 유리했는지 설명해야 합니다. Python 예제는 속도 측정이 아니라 순차 trace, 관계 score 행렬, 배치 score 텐서 shape를 비교하는 데 집중해야 하며, 같은 판단을 반복하는 연습은 늘리지 않습니다.
- `P5-14.5 긴 문맥에서 순차 상태와 직접 재참조는 어떻게 갈라지는가`: 긴 문맥 문제를 단순 기억력보다 현재 위치가 필요한 앞 단서를 직접 다시 참고하는 문제로 읽게 하고, 순차 상태 방식과 직접 재참조 방식이 최종 판단을 다르게 만들 수 있음을 실험과 해설로 보여 주어야 합니다. Python 예제는 Transformer 구현이 아니라 `sequential_support`와 `direct_decision`의 관찰 차이를 닫는 용도로 유지합니다.
- `P5-14.6 보충학습: feed-forward network는 왜 위치별 표현 가공을 맡는가`: feed-forward network가 attention 뒤에서 위치별 표현 가공을 맡는다는 점을 보충해야 합니다. Python 예제는 같은 FFN 가중치를 여러 위치에 공유 적용해도 입력 표현 차이에 따라 hidden/output이 달라지는 점을 보여 주어야 합니다.
- `P5-14.7 보충학습: residual connection은 왜 원래 표현의 길을 남기는가`: residual connection이 새 계산 생략이 아니라 원래 표현의 길을 함께 남긴다는 점을 보충해야 합니다.
- `P5-14.8 보충학습: layer normalization은 왜 값의 기준선을 맞추는가`: layer normalization이 의미 선택이 아니라 값 기준선 정리라는 점을 보충해야 합니다. Python 예제는 평균·표준편차와 다음 계산 점수의 전후 차이를 보여 주는 데 한정하고, P5-14.2의 역할 분담을 반복하는 연습은 늘리지 않습니다.

### Module 6. 생성 모델과 샘플링

#### Chapter 15. 생성 모델의 직관 `딥러닝 구조`

- 중심축: Part 5에서 생성형 AI를 깊이 설명하지 않고, Part 6로 넘어가기 전에 필요한 딥러닝 준비 관점만 닫습니다. 출력 자체를 만드는 모델, 후보 분포, 샘플링 선택을 순서대로 분리해야 합니다.
- `P5-15.1`: 생성형 AI를 딥러닝 관점에서 보려면 라벨·점수 중심 출력에서 문장·이미지·코드 같은 생성물 중심 출력으로 관점이 어떻게 달라지는지 정리해야 합니다.
- `P5-15.2`: 생성 모델이 정답 하나를 외우는 것이 아니라 가능한 출력 후보들의 상대적 그럴듯함을 후보 분포로 남긴다는 점을 설명해야 합니다.
- `P5-15.3`: 샘플링을 후보 분포에서 실제 출력을 꺼내는 절차로 설명하고, 다양성과 안정성의 균형이 왜 결과를 바꾸는지 보여 주어야 합니다.

- `summary.md`: Part 5가 기본 계산 구조에서 표현 학습, attention, Transformer, 생성으로 이어지는 흐름을 다시 묶어 주어야 합니다.
