# Part 5 미반영 체크리스트

이 문서는 Part 5 관련 재검토 메모에서 아직 남아 있는 작업만 관리합니다.

## 품질 상태 메모

- Part 5의 큰 구조 판단은 이미 닫혔고, 현재 리스크는 `재배치`보다 `후반 구조 장의 독해 호흡`에 가깝습니다.
- Chapter 11~15는 모델 이름 소개보다 `어떤 데이터 구조 문제 때문에 이런 설계가 나왔는가`로 읽혀야 합니다.
- 현재 남은 과제는 구조 축과 학습 절차 축이 다시 섞이지 않게 유지하고, 이미 넣은 표와 예시가 같은 말을 반복하지 않는지 고르는 일입니다.
- 따라서 다음 라운드는 새 구조 추가보다 장 내부 중복, 예시 밀도, 차이 표지 유지 점검이 우선입니다.
- 다만 초심자가 Part 5 본문으로 진입하는 데 실제 공백이 있으면, 수학·계산·구조 사이를 잇는 학술적·교육적 보강 Section을 새로 두는 편을 우선 검토합니다.
- Part 5 시작 페이지와 초반 장에는 후반 장과 다른 종류의 리스크도 남아 있습니다. 현재 구조와 맞지 않는 `Part 3` 참조, `다음 Part`를 `Part 5`로 잘못 가리키는 연결 문장, 같은 설명을 두 번 눌러 쓰는 중복 문단이 대표적입니다.

## 순차 점검 기록

- `2026-07-05`: `index.md`, `chapter-02/section-01.md`, `chapter-02/section-02.md`를 다시 읽으며 시작 구간의 공개 본문 톤과 관리 메모성 표현 잔존 여부를 재점검했습니다.
- 구조와 다음 Part 연결은 전반적으로 안정적이었고, 현재 범위에서 특정 사업 도메인 침범은 보이지 않았습니다. 다만 `다음 Part 이후 프로젝트 문서에서 무엇을 남겨야 하는지`처럼 독자용 본문보다 작업 관리 흐름에 가까운 문장과, 예제 설명의 `feature` 선행 표현이 조금 남아 있었습니다.
- 조치: `index.md`의 해당 문장을 `이후 Part에서 실험 결과와 분석 메모를 읽는 기준`으로 일반화했고, `P5-2.1`, `P5-2.2`의 예제 입력 설명도 `입력 특징(feature)` 순서로 맞췄습니다.
- 판단: Part 5 시작 구간은 새 챕터 추가보다 공개 본문 톤과 용어 제시 순서를 계속 맞추는 쪽이 적절하며, 현재 단계에서 추가 구조 보강 필요성은 크지 않습니다.

- `2026-07-05`: `index.md`, `summary.md`를 다시 읽으며 Part 도입/마무리 페이지의 메타 표현과 개요 형식을 재점검했습니다.
- 시작/마무리 페이지의 구조와 다음 Part 연결은 대체로 안정적이었고, Part 5를 `구조 설명 자체를 닫는 파트`로 유지하려는 방향도 현재 의도와 맞았습니다.
- 다만 `읽히지 않도록 관리합니다`처럼 집필 메모에 가까운 문장과, `summary.md`의 화살표형 개요가 공개 본문보다 작업 노트처럼 읽힐 수 있었습니다.
- 조치: 시작 페이지의 메타 표현을 일반 본문 문장으로 낮추고, 마무리 페이지의 핵심 흐름 개요를 번호 목록으로 정리했습니다.
- 판단: Part 5 시작/마무리 페이지는 새 구조 추가보다, 현재 설명을 줄이지 않으면서 본문 호흡을 일반 문장과 표 중심으로 안정화하는 쪽이 더 적절합니다.

- `2026-07-05`: 현재 워크트리의 `index.md`, `chapter-01/section-01.md`, `chapter-01/section-02.md`, `chapter-02/section-01.md`, `chapter-02/section-02.md`를 다시 대조했습니다.
- 기존 점검 메모와 달리 실제 파일에는 `Section ID`, `Version` 메타데이터가 빠져 있었고, 시작 페이지에는 `project_run`, `retrieval_records`, `selected_evidence`, `review_summary` 같은 관리 메모성 표현이 남아 있었습니다.
- Chapter 1-2 초반 절에는 `이 절은 다음 질문에 답합니다`, `이 질문에서 시작하는 것이`, `...구조이다` 같은 도입 메타 문장과 화살표식 메모 블록이 남아 있어 공개 본문보다 작업 노트에 가까운 호흡이 일부 보였습니다.
- 조치: `P5-1.1`, `P5-1.2`, `P5-2.1`, `P5-2.2`에 `Section ID`, `Version` 메타데이터와 릴리즈노트를 추가했고, 정의 문장과 흐름 메모는 일반 본문 문장 또는 번호 목록으로 정리했습니다. `index.md`의 관리 메모성 표현도 `생성 결과 비교`, `근거 선택`, `운영 평가` 같은 일반 문장으로 낮추었습니다.
- `2026-07-05`: Chapter 1 재점검에서 질문 한 줄이 본문에서 분리된 도입 문장과 `handoff` 같은 작업 용어가 아직 일부 남아 있는지 다시 확인했습니다.
- `P5-1.1`, `P5-1.2`는 구조 자체는 안정적이었지만, 도입 질문이 일반 문단에서 떨어져 있어 공개 본문보다는 작성 메모에 가까운 호흡이 약하게 남아 있었습니다. 시작 페이지의 `handoff`도 독자용 설명보다 집필자 작업 용어에 가까웠습니다.
- 조치: `index.md`의 `handoff`를 `연결 기준`으로 바꾸었고, `P5-1.1`, `P5-1.2`의 도입 질문을 일반 문단 안으로 다시 연결했습니다. `P5-1.1`은 McCulloch-Pitts를 직접 언급하고 있어 참고 자료도 함께 보강했습니다.

## 체크리스트

- [x] P5-11~P5-15가 `모델 이름 소개`보다 `어떤 데이터 구조 문제에서 이런 설계가 나왔는가`로 더 읽히는지 다시 점검한다.
- [x] `뒤에서 다시 본다` 같은 유예 문장이 많은 절은 현재 절에서 실제로 어디까지 설명을 끝내는지 더 분명히 쓴다.
- [x] CNN, RNN, LSTM 같은 Transformer 이전 구조가 뒤 Part 예고가 아니라 Part 5 자체 설명으로 닫히는지 점검한다.
- [x] self-attention/역전파 수학 보충학습이 실제로 필요한지, 본문 보강으로 충분한지 다시 판단한다.

## 이관한 기존 메모

- `beginner-reinforcement-review.md`
- `part-05-curriculum-review.md`
- `part-05-05-toc-restructure-review.md`

## 현재 결정

- Part 5는 뒤 Part 예고보다 `구조 설명 자체를 닫는 파트`로 유지합니다.
- CNN, RNN, Attention, Transformer는 각 장 안에서 `어떤 데이터 구조 문제 때문에 이런 설계가 나왔는가`를 먼저 설명하고, Part 5 연결은 짧은 다음 단계 안내로만 남깁니다.
- 이후 보강은 Chapter 11~15 본문에서 실제 유예 문장을 줄이는 방향으로 진행합니다.
- 현재 본문을 다시 읽다가 `표현 학습이 왜 필요한가`, `손실과 출력층이 왜 함께 읽혀야 하는가`, `배치·텐서·GPU가 왜 구조 설명과 분리되면서도 필요한가` 같은 입문 공백이 보이면 새 학술·교육 보강 Section 추가를 허용합니다.

## 이번 반영

- P5-11.1, P5-12.1, P5-13.1, P5-14.1, P5-15.1의 첫 도입부와 마무리 문장을 보강해 `현재 장에서 닫아야 하는 구조 문제`를 더 앞에 고정했습니다.
- 같은 절들에서 뒤 Part 직접 예고를 줄이고, 현재 절의 책임을 `이미지 지역 구조`, `순차 상태`, `선택적 참조`, `Transformer 블록`, `생성 문제` 설명으로 다시 묶었습니다.
- P5-12.2, P5-13.2, P5-14.2, P5-15.2에서도 같은 원칙으로 도입부·마무리 문장을 보강해 `장기 의존성`, `self-attention`, `병렬 처리와 긴 문맥`, `샘플링`을 현재 절 안에서 더 분명히 닫았습니다.
- P5-13.2, P5-14.2, P5-15.2에 `현재 절에서 실제로 닫아야 하는 계산 감각`을 한 번 더 명시해, 뒤 Part 참조가 남더라도 핵심 설명 책임은 현재 절 안에서 끝난다는 경계를 분명히 했습니다.
- Part 5 index와 summary에도 Chapter 11~15를 `이미지의 지역 구조`, `순차 상태`, `관계 재참조`, `병렬 블록`, `생성 후보 선택` 문제로 다시 묶어, 후반부가 모델 이름 소개가 아니라 데이터 구조 문제에 대한 답으로 읽히게 했습니다.
- P5-11.2, P5-12.1, P5-12.2에도 CNN은 `지역 패턴 탐지 + 요약`, RNN/LSTM/GRU는 `순차 상태와 장기 기억 유지` 문제에 대한 자체 답이라는 문장을 보강해, Transformer 이전 구조가 뒤 Part 예고가 아니라 Part 5 안에서 먼저 닫히도록 정리했습니다.
- P5-5.2에는 별도 역전파 수학 보충학습을 두지 않는다는 판단을 반영해, Part 5 안에서는 `손실 책임 분배`, `연쇄 법칙`, `계산 그래프` 감각까지 본문으로 닫는다고 명시했습니다.
- P5-13.3에는 self-attention 보충학습을 유지하는 이유를 덧붙여, 이 절이 수식 메모가 아니라 QKV와 multi-head 이름을 Transformer 본문에 연결해 주는 입문 회수 위치임을 분명히 했습니다.
- 아직 남은 과제는 장 내부 중복과 `모델 이름 소개`처럼 읽히는 일부 예시 정리 쪽에 더 가깝고, `유예 문장 때문에 현재 절 책임이 흐려지는 문제`는 우선 닫았습니다.
- `2026-07-05`: `index.md`, `chapter-01/section-01.md`, `chapter-01/section-02.md`, `chapter-02/section-01.md`, `chapter-02/section-02.md`, `chapter-03/section-01.md`, `chapter-03/section-02.md`, `chapter-03/section-03.md`, `chapter-04/section-01.md`, `chapter-04/section-02.md`를 순차 점검했습니다.
- Part 5 초반부는 퍼셉트론 -> 선형 결합/활성화 -> 다층 구조 -> 은닉 표현 -> 출력층/손실로 이어지는 계산 흐름이 비교적 안정적이었고, 현재 워크트리 메모나 특정 사업 도메인 문장이 직접 새어 들어간 부분은 크지 않았습니다.
- 현재 구간의 주요 문제는 오래된 Part 번호 참조와 다음 Part 연결 문장 오기였습니다. `Part 3의 선형 모델`, `Part 3에서는 머신러닝`, `Part 5의 LLM` 같은 문장들이 실제 목차 구조와 맞지 않았습니다.
- 조치: `index.md`, `P5-1.1`, `P5-1.2`, `P5-2.1`, `P5-2.2`, `P5-3.2`, `P5-4.1`, `P5-4.2`의 참조 문장을 현재 Part 구조에 맞게 고쳤고, `P5-3.2`의 중복 설명 문단도 한 번 정리했습니다.
- `2026-07-05`: `chapter-03/section-01.md`, `chapter-03/section-02.md`, `chapter-03/section-03.md`와 `index.md`를 현재 상태 기준으로 다시 확인했습니다.
- Chapter 3은 활성화 함수 -> 대표 함수 비교 -> 출력층 활성화 선택으로 이어지는 구조가 안정적이었고, 이 범위에서 특정 사업 도메인 침범이나 관리 메모 누수는 크지 않았습니다.
- 다만 `P5-3.1`부터 `P5-3.3`까지 `Section ID`, `Version` 메타데이터가 빠져 있었고, `P5-3.3`에는 calibration 회수 위치를 `P3-6.4`로 잘못 적은 문장이 남아 있었습니다. `index.md` 끝의 `이 문서는`도 Part 개요 페이지보다는 작업 문서처럼 읽힐 수 있었습니다.
- 조치: `P5-3.1`부터 `P5-3.3`까지 메타데이터를 추가했고, `P5-3.3`의 calibration 참조를 `P4-6.4`로 바로잡았습니다. `index.md` 끝 문장도 `이 개요 페이지` 기준으로 낮췄습니다.
- 판단: Part 5 초반부는 새 챕터나 새 Section 추가보다, 뒤 절에서도 같은 밀도로 `구조 설명`과 `학습 절차 설명`이 섞이지 않는지 이어서 점검하는 편이 더 중요합니다.
- `2026-07-05`: `chapter-05/section-01.md`, `chapter-05/section-02.md`, `chapter-06/section-01.md`, `chapter-06/section-02.md`, `chapter-07/section-01.md`, `chapter-07/section-02.md`, `chapter-08/section-01.md`, `chapter-08/section-02.md`, `chapter-08/section-03.md`를 순차 점검했습니다.
- 이 구간은 역전파 -> 계산 그래프 -> 학습/평가 모드 -> optimizer -> regularization -> 학습 루프 재정리로 이어지는 흐름이 비교적 안정적이었고, 구조 설명과 학습 절차 설명도 대체로 분리되어 있었습니다.
- 다만 `P5-5.2`, `P5-7.2`에는 범위 설명이 거의 같은 뜻으로 두 번 눌려 있었고, `P5-8.3`의 Python 예제는 `batch 학습`이라고 설명하면서 실제로는 샘플 1건씩 업데이트하고 있어 본문 설명과 코드가 어긋나 있었습니다.
- 또 초반 재점검 과정에서 `P5-1.1`, `P5-1.2`, `P5-7.1`, `P5-8.1`에 오래된 `Part 3` 참조가 일부 남아 있었고, `P5-8.2` 체크리스트에는 다음 절 연결이 `GPU와 병렬 처리`로 잘못 적혀 있었습니다.
- 조치: `P5-5.2`, `P5-7.2`의 중복 범위 문장을 정리했고, 남아 있던 `Part 3` 참조를 `Part 4` 기준으로 바로잡았습니다. `P5-8.2`의 다음 절 연결 오기도 수정했고, `P5-8.3` 예제는 실제 batch 2개를 평균 loss/gradient로 처리하는 코드와 출력으로 교체해 설명-예제 일치를 맞췄습니다.
- `2026-07-05`: `chapter-04/section-01.md`, `chapter-04/section-02.md`, `chapter-05/section-01.md`, `chapter-05/section-02.md`, `chapter-06/section-01.md`, `chapter-06/section-02.md`, `chapter-07/section-01.md`, `chapter-07/section-02.md`, `chapter-08/section-01.md`, `chapter-08/section-02.md`, `chapter-08/section-03.md`를 현재 워크트리 기준으로 다시 대조했습니다.
- Chapter 4-8은 손실 -> 역전파 -> 학습/평가 모드 -> optimizer -> regularization -> 학습 루프 재정리로 이어지는 학습 절차 축이 비교적 안정적이었고, 이 범위에서 특정 사업 도메인 침범이나 관리 메모 직접 누수는 크지 않았습니다.
- 이번 재점검에서 가장 큰 공통 문제는 `P5-4.1`부터 `P5-8.3`까지 `Section ID`, `Version` 메타데이터가 빠져 있었다는 점이었습니다. 또한 `P5-4.2`에는 범위 설명 문단이 거의 같은 뜻으로 두 번 눌려 있었고, `P5-6.1`의 `이 책에서는` 같은 메타 문장과 `P5-8.3`의 `이 문서는 ... 자체 구성 문서` 같은 내부 메모성 출처 문구는 공개 원고 톤과 맞지 않았습니다.
- 조치: `P5-4.1`부터 `P5-8.3`까지 현재 범위의 모든 Section에 `Section ID`, `Version` 메타데이터를 추가했습니다. `P5-4.2`의 중복 범위 문단은 한 번으로 정리했고, `P5-6.1`의 메타 문장은 `여기서는` 기준으로 낮췄습니다. `P5-8.3`의 출처와 참고 자료는 내부 메모 문장을 제거하고 일반 참고자료 형식으로 교체했습니다.
- 판단: 현재 범위는 새 챕터나 새 Section을 추가해야 할 정도의 흐름 결손은 보이지 않았습니다. 다음 라운드에서는 Chapter 9 이후에서도 같은 수준으로 메타데이터, 연결 문장, 공개 본문 톤, 참고자료 형식을 유지하는지 이어서 점검하는 편이 더 중요합니다.
- `2026-07-05`: `chapter-09/section-01.md`, `chapter-09/section-02.md`, `chapter-10/section-01.md`, `chapter-10/section-02.md`를 순차 점검했습니다.
- Chapter 9-10은 `학습 계산의 규모`에서 `모델이 실제로 배우는 내부 표현`으로 시야를 옮기는 전환 구간인데, 현재 본문은 GPU/배치/텐서 -> 표현 학습 -> 계층적 표현으로 비교적 자연스럽게 이어지고 있습니다.
- 현재 구간에서는 관리 메모 누수나 특정 사업 도메인 침범은 크지 않았습니다. 다만 `P5-9.1`에는 `Part 5의 LLM`처럼 다음 Part를 잘못 가리키는 문장이 남아 있었고, `P5-9.2`의 연결 목록은 같은 Part 안 참조 표기가 다소 느슨했습니다.
- 조치: `P5-9.1`의 다음 Part 연결 오기를 고쳤고, `P5-9.2`는 `P5-9.1`을 직접 가리키도록 연결 표기를 정리했습니다.
- `2026-07-05`: `chapter-11/section-01.md`, `chapter-11/section-02.md`, `chapter-11/section-03.md`, `chapter-12/section-01.md`, `chapter-12/section-02.md`를 순차 점검했습니다.
- Chapter 11-12는 이미지의 지역 구조에서 순차 상태 구조로 넘어가는 전환이 비교적 분명했고, CNN -> convolution/pooling -> ViT 비교 -> RNN/LSTM/GRU -> 장기 의존성 순서도 현재 Part의 후반 설계 의도와 잘 맞았습니다.
- 현재 구간에서는 관리 메모 누수나 특정 사업 도메인 침범은 크지 않았습니다. 다만 `P5-11.1`에는 같은 요지를 반복하는 문장 묶음이 있어 독해 호흡이 조금 늘어지는 문제가 있었습니다.
- 조치: `P5-11.1`의 `지역 패턴 -> 큰 시각 단서` 요지를 한 번으로 압축하고, 지역 패치 예제 아래 핵심 포인트를 더 직접적으로 읽히게 정리했습니다. Chapter 12는 현재 단계에서는 즉시 수정이 필요한 번호 오기나 규칙 위반보다, 설명 밀도 관리 쪽이 더 큰 과제로 보입니다.
- `2026-07-05`: `P5-11.1`의 `generate_cnn_diagrams.py` 관련 하위 절은 Section 학습 목표와 맞지 않는 자산 생성 예제로 판단했습니다.
- 조치: 자산 생성 스크립트 소개와 SVG 합성 코드를 본문 학습 예제에서 제거하고, 해당 파일은 `docs/assets/part-05/chapter-11/`의 자산 생성 원본으로만 유지했습니다. 동시에 Section 상단에 버전 메타데이터를 추가하고 `management/release-notes/sections/part-05/P5-11.1.md`에 개정 이력을 남겼습니다.
- `2026-07-05`: `chapter-13/section-01.md`, `chapter-13/section-02.md`, `chapter-13/section-03.md`를 순차 점검했습니다.
- Chapter 13은 장기 의존성에서 attention -> self-attention -> QKV/multi-head 보충학습으로 넘어가는 손잡이가 비교적 안정적이었고, 현재 Part 5 후반부의 `직접 참조 구조 -> 전체 관계 계산 -> Transformer 준비` 흐름과도 잘 맞았습니다.
- 현재 구간에서는 관리 메모 누수나 특정 사업 도메인 침범은 크지 않았습니다. 다만 `P5-13.1` 예제 구간에는 `문제 상황` 표지가 중복으로 한 번 더 들어가 있어 형식 호흡이 깨지는 부분이 있었습니다.
- 조치: `P5-13.1`의 중복 `문제 상황` 표기를 제거했습니다. Chapter 13은 현재 단계에서 큰 구조 수정 대상이라기보다, Chapter 14-15와의 연결에서 `Transformer 블록 설명`이 실제로 본문 안에서 충분히 닫히는지 점검하는 쪽이 더 중요합니다.
- `2026-07-05`: `chapter-14/section-01.md`, `chapter-14/section-02.md`, `chapter-15/section-01.md`, `chapter-15/section-02.md`, `summary.md`를 순차 점검했습니다.
- Chapter 14-15와 summary는 전체적으로 `Transformer 블록 -> 병렬 처리와 긴 문맥 -> 생성 모델 -> 샘플링 -> 다음 Part 연결` 흐름이 유지되고 있었고, 후반부가 단순 제품 예고편으로 무너지지는 않았습니다.
- 다만 summary에는 오래된 Part 번호 참조와 `Part 5의 LLM` 같은 잘못된 다음 Part 표기, 그리고 공개 본문보다는 관리 메모에 가까운 기록 필드 이름 표기가 섞여 있었습니다. `P5-15.1` 예제 출력에도 실제 샘플링 결과와 맞지 않는 counts 값이 남아 있었습니다.
- 조치: summary에서 `Part 3` 참조를 `Part 4`로 바로잡고, `Part 5의 LLM`류 오기를 `다음 Part` 기준으로 정리했습니다. 또한 `project_run`, `selected_evidence`, `retrieval_records` 같은 메모성 기록 표기를 제거해 공개 본문 톤에 맞췄고, `P5-15.1`의 샘플링 예제 출력값도 실제 실행 결과로 수정했습니다.

## 다음 유지 점검

- Chapter 11~15에서 각 장의 `새로 해결하는 데이터 구조 문제`가 짧은 문장과 표로 계속 직접적으로 보이는지 점검합니다.
- 구조 설명과 optimizer, regularization, 배치 같은 학습 절차 설명이 다시 한 장면에서 섞이지 않는지 점검합니다.
- 후반 비교 표와 예시 아래 설명이 `새 차이`를 더하는지, 아니면 이미 말한 내용을 반복하는지 점검합니다.
- 초반부에서는 역전파, 학습/평가 모드, optimizer 구간으로 가면서 `구조 설명`과 `학습 절차 설명`이 다시 한 문단 안에서 섞이지 않는지 점검합니다.
