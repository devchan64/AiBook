# Part 2 미반영 체크리스트

이 문서는 Part 2 관련 섹션 메모에 남아 있던 `추가 보강 필요`, `남은 검토 사항`만 모아 둔 작업 체크리스트입니다.
이미 본문에 반영된 설명과 집필 판단은 개별 섹션 메모에서 계속 반복하지 않고, 실제 후속 작업만 여기서 관리합니다.

## 목차 기준 체크리스트

이 블록은 `docs/book/table-of-contents.md`를 기준으로 Part 2의 시작/마무리 페이지, Chapter, Section을 같은 순서로 대조하기 위한 인덱스입니다. 아래의 `품질 상태 메모`, `순차 점검 기록`, `현재 결정`은 이 인덱스를 따라 읽은 뒤 남기는 근거 메모로 사용합니다.

- [x] `index.md` Part 시작 페이지와 현재 목차 설명이 일치하는지 확인한다.

### Module 1. 수학을 계산 언어로 다시 읽기

#### Chapter 1. AI에서 수학은 왜 필요한가 `수학 기초`

- [x] `P2-1.1` AI 계산에서 수학이 하는 일
- [x] `P2-1.2` 수식, 코드, 데이터가 만나는 자리

#### Chapter 2. 수식 표기 다시 읽기 `수학 기초`

- [x] `P2-2.1` 변수(variable), 함수(function), 식(expression) 다시 읽기
- [x] `P2-2.2` 시그마(sigma)와 반복 계산
- [x] `P2-2.3` 극한(limit)과 변화의 직관
- [x] `P2-2.4` 로그(log)와 지수(exp)는 왜 반복해서 등장하는가

### Module 2. 모델 계산을 읽기 위한 핵심 수학

#### Chapter 3. 선형대수는 무엇을 모델링하는가 `수학 기초`

- [x] `P2-3.1` 스칼라(scalar), 벡터(vector), 행렬(matrix)
- [x] `P2-3.2` 벡터 공간(vector space)과 위치의 직관
- [x] `P2-3.3` 행렬 곱(matrix multiplication)은 무엇을 재사용하는가
- [x] `P2-3.4` 내적(dot product), 길이(norm), 거리(distance), 유사도(similarity)
- [x] `P2-3.5` 파이썬 실행 환경: Colab과 로컬 PC
- [x] `P2-3.6` 선형대수를 NumPy로 확인하기

#### Chapter 4. 미분은 무엇을 찾기 위한 도구인가 `수학 기초`

- [x] `P2-4.1` 미분을 어떻게 배웠는지 다시 보기
- [x] `P2-4.2` 변화율(rate of change)과 기울기(slope)
- [x] `P2-4.3` 미분(derivative)과 그래디언트(gradient)
- [x] `P2-4.4` 학습에서 미분이 필요한 이유
- [x] `P2-4.5` 그래디언트 보충학습: 고등학교 미분에서 다변수 미분으로
- [x] `P2-4.6` 합성함수(composite function)와 연쇄 법칙(chain rule)

#### Chapter 5. 확률과 통계는 왜 필요한가 `수학 기초`

- [x] `P2-5.1` 확률(probability)은 불확실성을 어떻게 숫자로 표현하는가
- [x] `P2-5.2` 분포(distribution), 평균(mean), 분산(variance)
- [x] `P2-5.3` 표본(sample), 추정(estimation), 오차(error)
- [x] `P2-5.4` 확률과 통계를 작은 데이터로 확인하기
- [x] `P2-5.5` 보충학습: 표준편차, 상관, 신뢰구간을 처음 읽는 법

#### Chapter 6. 최적화는 어떤 문제를 푸는가 `학습 원리`

- [x] `P2-6.1` 최적화(optimization)는 무엇을 찾는가
- [x] `P2-6.2` 손실 함수(loss function)와 목적 함수(objective function)
- [x] `P2-6.3` 경사하강법(gradient descent)의 직관

### Module 3. 실행 환경과 Python 기초

#### Chapter 7. 학습 환경 세팅 `SW 도구`

- [x] `P2-7.1` 로컬 환경(local environment)과 실행 환경(runtime)
- [x] `P2-7.2` 터미널(terminal), 셸(shell), 작업 폴더(working directory)
- [x] `P2-7.3` Python 인터프리터(interpreter)와 스크립트(script)
- [x] `P2-7.4` 가상환경(virtual environment)과 패키지(package)
- [x] `P2-7.5` 의존성(dependency)과 재현성(reproducibility)
- [x] `P2-7.6` 보충학습: Windows, macOS, Linux 터미널 사용법
- [x] `P2-7.7` 보충학습: Python 설치는 언제 필요한가
- [x] `P2-7.8` 보충학습: 셸 스크립트(shell script), 파이프(pipe), 리다이렉션(redirection), 환경 변수(environment variable)를 처음 읽는 법
- [x] `P2-7.9` 보충학습: 자주 막히는 로컬 Python 환경 문제를 점검하는 법

#### Chapter 8. Python 핵심 문법 복습 `SW 도구`

- [x] `P2-8.1` 값(value), 변수(variable), 타입(type)
- [x] `P2-8.2` 리스트(list): 순서가 있는 값 묶음
- [x] `P2-8.3` 딕셔너리(dictionary): 키(key)로 값을 찾는 구조
- [x] `P2-8.4` 반복(loop): 이터러블(iterable)을 하나씩 처리하기
- [x] `P2-8.5` 함수(function)와 작은 재사용
- [x] `P2-8.6` 보충학습: 클래스(class)와 객체(object)를 처음 만날 때
- [x] `P2-8.7` 보충학습: 참조(reference), 얕은 복사(shallow copy), 깊은 복사(deep copy)를 처음 구분하는 법

#### Chapter 9. 자료구조와 그래프(graph) 직관 `SW 도구`

- [x] `P2-9.1` 자료구조(data structure)는 왜 필요한가
- [x] `P2-9.2` 배열(array), 표(table), 트리(tree), 그래프(graph) 직관
- [x] `P2-9.3` 그래프(graph)는 관계를 어떻게 표현하는가
- [x] `P2-9.4` 보충학습: 전통적인 자료구조를 처음 읽는 법

#### Chapter 10. Jupyter와 Colab 노트북 `SW 도구`

- [x] `P2-10.1` 노트북(notebook)은 왜 학습에 유용한가
- [x] `P2-10.2` Jupyter, Colab, 로컬 실행의 차이
- [x] `P2-10.3` 노트북을 재실행 가능한 기록으로 정리하기

### Module 4. 데이터 계산과 시각화 도구

#### Chapter 11. NumPy로 벡터와 행렬 재사용 `SW 도구`

- [x] `P2-11.1` NumPy 배열(array)로 벡터와 행렬 만들기
- [x] `P2-11.2` 인덱싱(indexing), 슬라이싱(slicing), 축(axis)
- [x] `P2-11.3` 브로드캐스팅(broadcasting)과 벡터화(vectorization)
- [x] `P2-11.4` 보충학습: NumPy에서 모양(shape)과 원본 공유를 함께 읽는 법

#### Chapter 12. Pandas로 데이터 구조 다루기 `SW 도구`

- [x] `P2-12.1` Pandas DataFrame은 무엇을 표현하는가
- [x] `P2-12.2` 선택, 필터링, 집계
- [x] `P2-12.3` 학습용 데이터셋(dataset) 준비의 직관

#### Chapter 13. Matplotlib으로 개념 시각화 `SW 도구`

- [x] `P2-13.1` 그래프(plot)는 무엇을 드러내는가
- [x] `P2-13.2` 기본 차트와 수식의 모양 확인
- [x] `P2-13.3` 여러 그래프를 비교하고 저장하기

### Module 5. 재현성과 통합 복습

#### Chapter 14. Git과 문서 관리 `SW 도구`

- [x] `P2-14.1` Git은 변경 이력을 관리하는 도구
- [x] `P2-14.2` 브랜치(branch), 커밋(commit), 문서 재현성

#### Chapter 15. SW로 수학 다시 보기 `프로젝트 실습`

- [x] `P2-15.1` 수식을 코드로 옮기는 작은 절차
- [x] `P2-15.2` Part 3로 넘어가기 전 점검

- [x] `summary.md` Part 마무리 페이지와 현재 목차 설명이 일치하는지 확인한다.

## 품질 상태 메모

- Part 2의 핵심 책임은 수학, Python, 데이터 도구, Git을 따로 흩어진 과목처럼 두지 않고 `Part 3 데이터 모델링을 읽기 위한 공통 계산 언어`로 다시 묶는 데 있습니다.
- 따라서 순차 점검의 우선 기준도 `개념을 너무 깊게 새지 않았는가`보다 `계산 언어 복구라는 중심축을 유지하는가`, `초심자가 실제로 다시 따라갈 수 있는 연결 순서를 제공하는가`에 둡니다.

## 순차 점검 기록

### 2026-07-18 Part 2 시작 페이지와 Chapter 1 순차 점검

- `P2-index`는 Part 2를 `수학`, `Python`, `데이터 도구`, `Git`의 나열이 아니라 Part 3으로 넘어가기 위한 공통 기반으로 설명하고 있어, 현재 목차 구조와 큰 충돌 없이 맞물리고 있었습니다.
  - 판단: 시작 페이지가 세부 증명이나 도구 사용법보다 `왜 지금 이 기초가 필요한가`를 먼저 세우고 있어 입구 역할이 안정적입니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 시작 페이지 역할 유지, 목차 정합성 양호.
- `P2-1.1`은 수학을 `시험 과목`이 아니라 `AI 계산 문해력`으로 다시 읽게 만드는 중심축이 분명했습니다.
  - 판단: 공부 시간-퀴즈 점수 공통 장면을 계속 재사용해 추상 정의가 공중에 뜨지 않게 붙잡고 있어 초심자 전개도 안정적입니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 계산 언어 복구 축 유지, 공통 장면 활용 양호.
- `P2-1.2`는 수식, 코드, 데이터가 같은 계산의 다른 표현이라는 점을 평균, `y = f(x)`, `shape` 예시로 일관되게 회수하고 있었습니다.
  - 판단: Chapter 1의 두 절이 `왜 필요한가 -> 어디서 만나는가` 순서로 자연스럽게 이어져, 현재 차수에서는 과도한 축소나 확장이 필요하지 않습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 표현 연결 구조 양호, 독자 전개 안정.
- 다음 순차 점검 대상은 Chapter 2 `P2-2.1`부터 `P2-2.4`입니다.

### 2026-07-18 Part 2 Chapter 2 순차 점검

- `P2-2.1`은 변수, 함수, 식을 `AI 문서에서 수식 한 줄을 읽는 최소 문법`으로 묶어 두고, 코드 변수와 수학 변수를 비교해 Part 2의 계산 문해력 축을 잘 유지하고 있었습니다.
  - 판단: 표기 정의만 나열하지 않고 `값-관계-계산` 구분으로 독자의 읽기 기준을 먼저 세운 뒤 작은 코드 예시로 이어져, 초심자 전개가 안정적입니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 최소 기호 문법 정리 양호, 수식-코드 연결 안정.
- `P2-2.2`는 시그마를 `무엇을 몇 번 더하는가`라는 질문으로 고정해 반복 계산, 평균, 손실 집계로 이어 주고 있어 목차상 역할이 분명했습니다.
  - 판단: 기호 분해, 펼쳐 쓰기, 코드 반복문 연결 순서가 자연스러워 Part 3 이전의 계산 언어 복구라는 목적을 벗어나지 않습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 반복 계산 독해 축 유지, 예시 밀도 양호.
- `P2-2.3`은 극한을 증명 중심으로 새지 않고 `가까워짐 -> 작은 변화 -> 미분 입구`로 연결해 Chapter 4를 준비하는 역할에 충실했습니다.
  - 판단: 대입과 극한의 차이를 짧은 정리 예시로 회수하고, `변화율` 질문을 먼저 심어 주는 전개가 독자 기준에 맞습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 미분 입구 정렬 양호, 범위 통제 안정.
- `P2-2.4`는 로그와 지수를 고급 수학 설명으로 밀어 올리지 않고 `점수 -> 비율 -> 손실` 계산 언어로 묶어 뒤 Part 연결을 선명하게 유지하고 있었습니다.
  - 판단: `softmax`, `log loss`, `sigmoid`로 이어질 이유를 먼저 고정해 두고, 계산 예시도 과도한 수식 전개 없이 목적에 맞게 배치되어 있습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 생성형 AI 이전 공통 수학 언어 축 유지, 후속 Part 연결 양호.
- 다음 순차 점검 대상은 Chapter 3 `P2-3.1`부터 `P2-3.6`입니다.

### 2026-07-18 Part 2 Chapter 3 순차 점검

- `P2-3.1`은 스칼라, 벡터, 행렬을 `AI 데이터가 계산 가능한 모양으로 바뀌는 방식`으로 묶고 있어, 선형대수를 독립 과목처럼 새기보다 Part 3 준비용 계산 언어로 잘 고정하고 있었습니다.
  - 판단: 숫자 하나 -> 값 목록 -> 표 -> 비교 가능한 위치로 이어지는 흐름이 분명해, 초심자도 모양과 역할을 함께 따라가기 좋습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 데이터 모양 축 유지, 독자 전개 양호.
- `P2-3.2`는 벡터 공간을 엄밀한 공리 설명보다 `같은 공간 안의 위치와 가까움`으로 번역해, 임베딩과 검색 설명의 수학 입구 역할에 맞게 범위를 잘 통제하고 있었습니다.
  - 판단: 위치와 위상을 섞지 않도록 경계를 짚으면서도, 벡터 비교 질문을 먼저 세우는 전개가 독자 기준에 적절합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 위치 직관 정렬 양호, 후속 Part 연결 안정.
- `P2-3.3`은 행렬 곱을 `가중합의 재사용`으로 재해석해, 계산 규칙과 shape 확인의 이유를 모델 계산과 직접 연결하고 있었습니다.
  - 판단: 위치별 곱과 행렬 곱을 분리하고 입력/출력 차원 질문으로 이어지는 구조가 Part 2 목적과 잘 맞습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 행렬 곱 독해 축 유지, shape 중심 설명 양호.
- `P2-3.4`는 내적, 길이, 거리, 유사도를 `무엇을 같다고 볼 것인가`라는 비교 질문으로 묶고 있어, 계산 정의 나열보다 판단 기준 복구에 더 충실했습니다.
  - 판단: k-NN과 임베딩 검색의 비교 질문을 분리해 주는 구성이 독자 혼동을 줄여 줍니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 비교 기준 정리 양호, 검색 연결 안정.
- `P2-3.5`는 Colab과 로컬 PC 차이를 `어디에 무엇을 입력하는가`로 정리해 다음 NumPy 실습의 실행 위치 혼동을 막는 데 집중하고 있었습니다.
  - 판단: 도구 사용법 전체로 새지 않고, 바로 다음 절 실습 준비 범위로 묶은 점이 적절합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 실행 환경 입구 정리 양호, 범위 통제 안정.
- `P2-3.6`은 앞 절 개념을 NumPy 배열, `shape`, `*`, `@`로 다시 확인하게 해, 수식과 코드가 같은 구조라는 Part 2의 공통 계산 언어 축을 잘 회수하고 있었습니다.
  - 판단: 각 코드 블록이 어느 앞 절 개념을 다시 확인하는지 명시해 두어, 예제가 단순 문법 소비로 흐르지 않습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 수식-코드 대응 회수 양호, 학습 밀도 안정.
- 다음 순차 점검 대상은 Chapter 4 `P2-4.1`부터 `P2-4.6`입니다.

### 2026-07-18 Part 2 Chapter 4 순차 점검

- `P2-4.1`은 미분 공식을 앞세우지 않고 `변화 질문`을 먼저 복구해, Chapter 6의 손실과 업데이트 설명으로 이어질 입구 역할을 안정적으로 수행하고 있었습니다.
  - 판단: 과거 학습 기억을 현재 질문으로 다시 번역하는 방식이 재학습 독자 기준에 잘 맞습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 변화 질문 복구 양호, 재학습 입구 안정.
- `P2-4.2`는 변화율과 기울기를 그래프 문제가 아니라 `입력을 조금 바꾸면 결과가 얼마나 달라지는가`라는 계산 판단으로 다시 묶고 있었습니다.
  - 판단: 평균 변화율에서 순간 변화율로 좁혀 가는 흐름이 다음 절 미분과 그래디언트 설명의 준비로 충분합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 변화율 연결 양호, 학습 연결 안정.
- `P2-4.3`은 미분, 편미분, 그래디언트를 한 변수에서 다변수로 확장되는 하나의 흐름으로 정리해, AI 학습 문맥의 용어 전환을 무리 없이 붙이고 있었습니다.
  - 판단: `그래디언트가 왜 벡터인가`를 조절값 묶음으로 읽게 해 주는 설명이 독자 혼동을 줄입니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 다변수 확장 설명 양호, 용어 연결 안정.
- `P2-4.4`는 손실이 `얼마나 틀렸는가`만 말하고, 미분과 그래디언트가 `어느 방향으로 바꿀 것인가`를 알려 준다는 역할 분리를 분명히 하고 있었습니다.
  - 판단: 역전파를 학습 전체와 구분해 예고하는 방식도 후속 Part 연결에 적절합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 학습 절차 연결 양호, 역할 구분 안정.
- `P2-4.5`는 분량이 긴 보충학습이지만, 고등학교 미분 기억에서 그래디언트와 경사하강법으로 넘어가는 간격을 메우는 데 집중하고 있어 보충학습의 목적을 벗어나지는 않았습니다.
  - 판단: 분량은 크지만 대표 설명을 복제하기보다 `왜 낯선가`, `어디서 끊기는가`, `어떻게 이어 읽을 것인가`를 단계적으로 보강하고 있어 현재는 유지하는 편이 맞습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 보충학습 목적 부합, 분량 확장 허용 범위.
- `P2-4.6`은 합성함수와 연쇄 법칙을 역전파의 최소 배경으로만 다루어, 수학 세부 유도로 새지 않고 필요한 연결만 남기고 있었습니다.
  - 판단: `chain rule = backprop`로 오해하지 않게 역할을 분리한 구성이 적절합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 연쇄 법칙 입구 정리 양호, 범위 통제 안정.
- 다음 순차 점검 대상은 Chapter 5 `P2-5.1`부터 `P2-5.5`입니다.

### 2026-07-18 Part 2 Chapter 5 순차 점검

- `P2-5.1`은 확률을 동전·주사위 계산에 가두지 않고 `불확실성을 숫자로 표현하는 언어`로 재정리해, 이후 모델 점수와 의사결정 구분까지 자연스럽게 이어지고 있었습니다.
  - 판단: 사건, 결과, 표본공간을 기본으로 두되 AI 예측 점수와 운영 판단을 분리해 읽게 하는 구성이 초심자 기준에 적절합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 불확실성 언어 복구 양호, AI 연결 안정.
- `P2-5.2`는 분포, 평균, 분산을 `값 묶음의 모양-중심-퍼짐`으로 정리해 평균만 보는 오독을 막고 있었습니다.
  - 판단: 데이터 분포와 확률분포를 구분하고, 평균이 같아도 데이터 성격이 다를 수 있음을 초심자도 따라갈 수 있게 배치했습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 통계 기초 구조 양호, 데이터 해석 입구 안정.
- `P2-5.3`은 모집단, 표본, 추정, 오차를 데이터셋과 평가 점수 해석으로 곧바로 연결해, Part 3 일반화와 평가로 넘어갈 준비를 잘 하고 있었습니다.
  - 판단: 표본추출 변동과 표본 편향을 분리해 주는 설명이 실제 머신러닝 독해에 유용합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 데이터셋 표본 관점 양호, 평가 해석 연결 안정.
- `P2-5.4`는 앞 절 개념을 작은 숫자와 코드 출력으로 다시 확인하게 해, 통계 용어가 추상 정의로만 남지 않도록 밀도를 보완하고 있었습니다.
  - 판단: 평균, 중위값, 분산, 표본 평균 변화를 개념-코드-출력 순서로 배치한 점이 학습 밀도를 높입니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 개념-출력 대응 양호, 실습 밀도 안정.
- `P2-5.5`는 표준편차, 상관, 표준오차, 신뢰구간, 가설검정을 모두 계산으로 밀지 않고 `무엇을 더 보려는가`라는 독해 기준으로 정리해 보충학습 목적에 부합하고 있었습니다.
  - 판단: 분량은 다소 크지만 퍼짐, 함께 움직임, 추정 흔들림, 차이 해석을 분리해 읽게 만드는 보충 축이 분명해 현재는 유지가 적절합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 보충학습 역할 구분 양호, 후속 실험 독해 준비 안정.
- 다음 순차 점검 대상은 Chapter 6 `P2-6.1`부터 `P2-6.3`입니다.

### 2026-07-18 Part 2 Chapter 6 순차 점검

- `P2-6.1`은 최적화를 추상 수학 정의보다 `후보-기준-제약`의 장면으로 먼저 번역해, 학습을 더 나은 값 찾기 문제로 읽게 하는 입구 역할을 안정적으로 수행하고 있었습니다.
  - 판단: 역사 배경도 현재 절의 목적을 흐리지 않는 범위에서 `오래된 계산 사고가 AI 학습으로 이어졌다`는 정도로만 배치되어 있습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 최적화 입구 정리 양호, 범위 통제 안정.
- 다음 순차 점검 대상은 Chapter 11 `P2-11.1`부터 `P2-11.4`입니다.

### 2026-07-18 Part 2 Chapter 11 순차 점검

- `P2-11.1`은 NumPy를 독립 라이브러리 소개로 길게 새지 않고 `계산 -> 표 -> 그래프 -> 기록` 흐름의 첫 단계로 배치해, Part 2의 공통 계산 언어 복구 축을 안정적으로 유지하고 있었습니다.
  - 판단: 벡터, 행렬, `shape`, `ndim`, `dtype`을 바로 뒤 Pandas와 Matplotlib로 이어지는 손잡이로 배치해 두어, 도구 설명이 중심축을 밀어내지 않습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 계산 가능한 숫자 모양 입구 양호, 후속 도구 연결 안정.
- `P2-11.2`는 인덱싱, 슬라이싱, 축을 문법 설명으로만 두지 않고 `샘플 하나를 보는가`, `특징 열 전체를 보는가`, `어느 방향으로 요약하는가`라는 질문으로 번역하고 있었습니다.
  - 판단: 배열 읽기를 데이터셋 독해와 곧바로 연결하고 있어 초심자 기준의 전개가 안정적이며, Chapter 12 표 구조 설명으로 넘어갈 준비도 충분합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 배열 읽기 손잡이 정리 양호, 독자 전개 안정.
- `P2-11.3`은 브로드캐스팅과 벡터화를 성능 자랑이나 구현 세부로 밀지 않고 `shape이 맞으면 배열 전체 계산을 읽을 수 있다`는 관점에 고정하고 있었습니다.
  - 판단: `(4, 3) + (3,)`과 오류 예시를 함께 두어 편의 문법과 shape 확인 습관을 같이 회수하고 있어, 보충 밀도는 충분하지만 본문 중심축을 벗어나지 않습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: shape 호환성 설명 양호, 배열 단위 계산 독해 안정.
- `P2-11.4`는 선택 방식, `shape` 변화, 원본 공유를 함께 묶어 앞 절들에서 남는 혼동 지점을 회수하는 보충학습 역할에 충실했습니다.
  - 판단: 분량은 적지 않지만 `왜 (3,)`, `(3, 1)`, `(1, 3)`이 갈리는가`, `왜 어떤 선택은 원본을 같이 바꾸는가`를 정리해 주는 보강이라 Part 2의 실습 문해력 축과 잘 맞습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 보충학습 목적 부합, shape-view 혼동 회수 양호.
- 다음 순차 점검 대상은 Chapter 12 `P2-12.1`부터 `P2-12.3`입니다.

### 2026-07-18 Part 2 Chapter 12 순차 점검

- `P2-12.1`은 Pandas DataFrame을 NumPy와 경쟁하는 별도 도구로 세우지 않고, `숫자 배열 -> 사례와 변수의 표`로 읽기를 전환하는 자리로 분명히 배치하고 있었습니다.
  - 판단: 한 행이 곧 최종 샘플이 아닐 수 있다는 시계열 예시까지 포함해 `표 구조`와 `분석 단위`를 구분하게 만드는 점이 초심자 전개에 특히 유효합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 표 해석 입구 양호, 분석 단위 구분 안정.
- `P2-12.2`는 선택, 필터링, 집계를 Pandas 문법 나열로 흩뜨리지 않고 `무엇을 읽는가`, `무엇을 남기는가`, `무엇을 요약하는가`라는 세 질문으로 정리하고 있었습니다.
  - 판단: `Series`와 `DataFrame`, `loc`와 `iloc`, 조건 필터와 `groupby`를 각각 다른 읽기 동작으로 구분해 두어, Chapter 12의 목적이 도구 사용법 소비보다 표 독해 복구에 있음을 잘 유지합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 표 선택/요약 구조 양호, 독자 판단 흐름 안정.
- `P2-12.3`은 데이터셋 준비를 Pandas 조작 기술이 아니라 `질문에 맞게 X와 y의 경계를 다시 긋는 일`로 설명해 Part 3 직전 준비라는 역할을 선명하게 수행하고 있었습니다.
  - 판단: 열 선택, 식별자 제외, train/validation/test 분리, 데이터 누수 순서를 함께 묶어 두어, 단순 예고가 아니라 실제 학습용 입력 구성의 기준을 충분히 제공합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 데이터셋 준비 직관 양호, Part 3 연결 안정.
- 다음 순차 점검 대상은 Chapter 13 `P2-13.1`부터 `P2-13.3`입니다.

### 2026-07-18 Part 2 Chapter 13 순차 점검

- `P2-13.1`은 그래프를 꾸미기 도구가 아니라 `표에서 바로 안 보이는 변화, 관계, 분포를 읽는 도구`로 정리해, Chapter 11-12의 계산과 표 해석을 자연스럽게 이어 주고 있었습니다.
  - 판단: `Figure`, `Axes` 설명도 Matplotlib 문법 소개에 머무르지 않고 `어떤 질문을 그림으로 확인할 것인가`를 위한 최소 구조로 배치되어 있어 초심자 전개가 안정적입니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 시각화 입구 정리 양호, 질문 중심 설명 안정.
- `P2-13.2`는 기본 차트 이름을 나열하기보다 `순서에 따른 변화`, `두 값의 관계`, `값의 몰림` 같은 질문과 차트를 직접 대응시키고 있었습니다.
  - 판단: 선 그래프, 산점도, 히스토그램이 각각 앞선 미분·분포·손실 곡선 해석과 연결되어 있어, 시각화 구간이 본문 중심축 밖의 별도 디자인 수업으로 새지 않습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 질문-차트 대응 양호, 후속 Part 연결 안정.
- `P2-13.3`은 여러 그래프 비교와 저장을 `그래프 파일 만들기` 자체보다 `비교 질문을 남기고 재현 가능한 기록을 만든다`는 흐름으로 설명하고 있었습니다.
  - 판단: `savefig`, 범례, train/validation 비교를 문서 관리와 실험 기록으로 연결해 두어, 바로 다음 Git/문서 관리 Chapter 14의 입구로도 적절합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 비교·저장 흐름 양호, 재현성 연결 안정.
- 다음 순차 점검 대상은 Chapter 14 `P2-14.1`부터 `P2-14.2`입니다.

### 2026-07-18 Part 2 Chapter 14 순차 점검

- `P2-14.1`은 Git을 개발자 전용 명령어 묶음이 아니라 `무엇을 왜 바꿨는지`를 남기는 학습 기록 도구로 설명해, Part 2의 재현성 축을 분명하게 회수하고 있었습니다.
  - 판단: `status`, `add`, `commit`, `log`가 모두 문서, 코드, 이미지의 변경 묶음을 설명하기 위한 손잡이로 배치되어 있어, 도구 설명이 본문 중심축을 벗어나지 않습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 변경 이력 입구 양호, 문서 프로젝트 연결 안정.
- `P2-14.2`는 브랜치와 배포를 협업 고급 기능으로 밀지 않고 `작성 중 판단`과 `배포 가능한 설명`을 분리하는 운영 기준으로 정리하고 있었습니다.
  - 판단: 문서 재현성을 원고·코드·이미지·설정의 연결로 넓혀 두어, Part 3 이후 실험 비교 기록으로 이어지는 설명 축도 안정적으로 유지합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 브랜치 운영 기준 양호, 공개/작성 분리 설명 안정.
- 다음 순차 점검 대상은 Chapter 15 `P2-15.1`부터 `P2-15.2`, 그리고 `summary.md`입니다.

### 2026-07-18 Part 2 Chapter 15 및 마무리 페이지 순차 점검

- `P2-15.1`은 Part 2 전반을 다시 복습하는 데 그치지 않고, 수식의 기호를 작은 계산 절차와 코드 변수로 옮기는 흐름을 실제 예제로 묶어 주고 있었습니다.
  - 판단: 반복문과 NumPy를 나란히 두어 `의미를 먼저 읽고 표현을 줄인다`는 기준을 세우고 있어, Part 3 진입 전 필요한 계산 문해력 복구 목적에 잘 맞습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 수식-코드 연결 양호, 프로젝트 실습 입구 안정.
- `P2-15.2`는 Part 2를 완전 학습 점검표로 만들기보다 `무엇을 들고 넘어가고 어디로 다시 돌아올 것인가`를 정리하는 재방문 기준점 역할에 충실했습니다.
  - 판단: `X`, `y`, `shape`, `fit`, `predict`, 손실과 오차의 구분을 최소선으로 압축해 두어, 독자 기준의 진입 장벽을 낮추면서도 중심축을 흐리지 않습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: Part 3 진입 기준 양호, 복귀 동선 정리 안정.
- `P2-summary`는 목차상 Part 2 마무리 역할과 현재 본문 구조가 잘 맞물리고 있었습니다.
  - 판단: 수학, Python, 배열, 표, 그래프, Git을 다시 나열하되 `모든 기초 완료`가 아니라 `Part 3 재진입 기준 확보`로 묶고 있어 마무리 페이지의 기능이 선명합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: Part 마무리 역할 양호, 목차 정합성 안정.
- `P2-6.2`는 손실 함수와 목적 함수를 `틀림을 숫자로 바꾸는 기준`과 `학습이 실제로 움직이는 전체 기준`으로 구분해, 뒤 최적화 절차를 읽기 쉽게 준비하고 있었습니다.
  - 판단: 개별 오차, 평균 손실, 평가 지표를 분리해 주는 구성도 독자 혼동을 줄입니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 학습 기준 정리 양호, 평가 구분 안정.
- `P2-6.3`은 경사하강법을 공식 유도보다 `손실이 줄어드는 쪽으로 조금씩 움직이는 반복`으로 정리해, P2-4 미분 설명과 자연스럽게 맞물리고 있었습니다.
  - 판단: 그래디언트, 반대 방향 이동, 학습률을 모두 `현재 위치 근처의 정보`라는 하나의 감각으로 묶어 설명해 Part 2 목적에 잘 맞습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 최적화-학습 연결 양호, 반복 업데이트 직관 안정.
- 다음 순차 점검 대상은 Chapter 7 `P2-7.1`부터 `P2-7.9`입니다.

### 2026-07-18 Part 2 Chapter 7 순차 점검

- `P2-7.1`은 수학과 최적화 설명에서 곧바로 설치 화면으로 뛰지 않고 `그 계산을 어디서 실행하는가`라는 질문으로 전환해, Module 3의 입구를 안정적으로 열고 있었습니다.
  - 판단: Chapter 7~10의 층위를 실행 환경, 코드 문법, 노트북 기록으로 나눠 둔 점이 독자 방향 감각에 도움이 됩니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 실행 환경 전환 양호, 장 경계 정리 안정.
- `P2-7.2`는 터미널, 셸, 작업 폴더를 `위치 문제와 코드 문제를 나누는 기준`으로 정리해 초심자 막힘을 줄이는 방향으로 쓰여 있었습니다.
  - 판단: 운영체제별 세부 차이를 앞세우지 않고 공통 개념을 먼저 세운 점이 적절합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 명령 실행 기초 양호, 위치 감각 정리 안정.
- `P2-7.3`은 인터프리터, 대화형 실행, 스크립트 실행, 노트북 셀을 같은 Python 실행 흐름 안에서 구분해 주어, 입력 자리 혼동을 줄이고 있었습니다.
  - 판단: `python`, `python file.py`, `print(...)`를 한 자리에서 분리해 읽게 하는 구성은 실습 준비용으로 충분합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 실행 방식 구분 양호, 실습 입구 안정.
- `P2-7.4`와 `P2-7.5`는 가상환경, 패키지, 의존성, 재현성을 `프로젝트별 Python 공간`과 `다시 실행 가능한 조건 기록`으로 묶어, 설치 절차보다 환경 관리 원리를 우선하고 있었습니다.
  - 판단: `pip install`과 `import`를 분리하고, `requirements.txt`의 역할을 환경 재현 관점에서 설명한 점이 Part 2 목적에 잘 맞습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 환경 분리와 재현성 설명 양호, 협업 준비 안정.
- `P2-7.6`부터 `P2-7.9`까지의 보충학습은 운영체제별 차이, Python 설치 판단, 셸 기호, 로컬 환경 점검 순서를 각각 분리해 두어, 본편 흐름을 깨지 않으면서 실제 막힘 지점을 보완하고 있었습니다.
  - 판단: 보충학습 분량이 작지 않지만 모두 `실행 환경을 읽고 점검하는 법`으로 수렴하고 있어 중심축을 훼손하지 않습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 보충학습 구조 양호, 실전 막힘 대응 안정.
- 다음 순차 점검 대상은 Chapter 8 `P2-8.1`부터 `P2-8.7`입니다.

### 2026-07-18 Part 2 Chapter 8 순차 점검

- `P2-8.1`은 값, 변수, 타입을 Python 입문 문법 자체보다 `수식과 데이터 실습을 읽을 때 무엇을 다루는가`라는 기준으로 정리해, Part 2의 공통 계산 언어 복구 축을 유지하고 있었습니다.
  - 판단: `=`을 등식보다 이름 붙이기로 읽게 하는 설명과 타입 확인 습관을 함께 두어 초심자 전개가 안정적입니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 기본 문법 복구 양호, 계산 언어 연결 안정.
- `P2-8.2`는 리스트를 일반 목적 순서 묶음으로 설명하고 NumPy 배열과 구분해 두어, Python 기초와 수치 계산 구조를 혼동하지 않게 하고 있었습니다.
  - 판단: 리스트를 바로 배열로 등치하지 않는 점이 뒤 Chapter 11과의 경계를 지켜 줍니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 자료구조 경계 정리 양호, 후속 장 연결 안정.
- `P2-8.3`은 딕셔너리를 구현 용어보다 `키로 값을 찾는 매핑`으로 먼저 설명해, 설정값·라벨 맵·API 응답 해석으로 자연스럽게 이어지고 있었습니다.
  - 판단: 리스트와 딕셔너리의 차이를 위치와 키의 차이로 분명히 세워 초심자 혼동을 줄입니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 매핑 구조 설명 양호, 실습 연결 안정.
- `P2-8.4`는 반복을 문법 암기보다 `묶음에서 무엇을 꺼내 무엇을 만들고 있는가`라는 처리 흐름으로 정리해, 자료구조 절과 함수 절 사이를 잘 이어 주고 있었습니다.
  - 판단: `enumerate()`, `.items()`, `zip()`도 모두 처리 질문으로 묶어 설명하고 있어 학습 밀도가 안정적입니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 처리 흐름 설명 양호, 반복 패턴 정리 안정.
- `P2-8.5`는 함수(function)를 입력-처리-출력 계약과 작은 재사용 단위로 설명해, 수학의 함수와 Python 함수를 연결하는 입구 역할을 잘 하고 있었습니다.
  - 판단: `print`와 `return`의 차이를 분리하고, 반복 처리 코드를 작은 함수로 묶는 흐름이 뒤 라이브러리 독해 준비에 적절합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 함수 입구 정리 양호, 재사용 감각 안정.
- `P2-8.6`과 `P2-8.7` 보충학습은 각각 메서드 호출과 참조/복사 구분처럼 실제 초심자 막힘이 큰 지점을 보완하면서도, Python 전체 객체지향 이론으로 과하게 새지 않고 필요한 범위에 머물러 있었습니다.
  - 판단: 분량이 있지만 둘 다 뒤 라이브러리 코드와 데이터 전처리 장면에서 실제로 다시 쓰일 기준을 회수하는 데 집중하고 있어 보충학습 목적에 맞습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 보충학습 목적 부합, 실전 오류 예방 안정.
- 다음 순차 점검 대상은 Chapter 9 `P2-9.1`부터 `P2-9.4`입니다.

### 2026-07-18 Part 2 Chapter 9 순차 점검

- `P2-9.1`은 자료구조를 구현 항목 암기보다 `데이터를 어떤 모양으로 담고 어떤 연산을 자주 할 것인가`라는 질문으로 다시 세워, Chapter 8 문법 복습을 구조 선택 감각으로 자연스럽게 확장하고 있었습니다.
  - 판단: 리스트와 딕셔너리를 이미 배운 뒤 `왜 더 많은 구조를 알아야 하는가`를 설명하는 입구 역할이 적절합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 구조 선택 질문 정리 양호, 장 전환 안정.
- `P2-9.2`는 배열, 표, 트리, 그래프를 이름이 아니라 `위치`, `행열`, `계층`, `관계`라는 서로 다른 질문으로 비교해, 뒤 NumPy·Pandas·그래프 설명의 공통 지도를 잘 마련하고 있었습니다.
  - 판단: 같은 데이터를 다른 구조로 볼 수 있다는 설명이 초심자에게도 무리 없이 전달됩니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 네 구조 비교 양호, 후속 도구 연결 안정.
- `P2-9.3`은 그래프를 통계 차트와 구분해 `노드-엣지-방향-가중치`의 관계 구조로 정리하고 있어, 검색·추천·지식 연결 쪽 예고를 과도하게 확장하지 않고 필요한 범위에 머물러 있었습니다.
  - 판단: 표와 그래프가 같은 데이터를 두고도 다른 질문을 다룬다는 설명이 특히 유효합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 관계 구조 설명 양호, 그래프 입구 안정.
- `P2-9.4` 보충학습은 전통적인 자료구조 이름을 구현 세부보다 대표 질문으로 다시 읽게 해, Python 기초 뒤에 배열·스택·큐·해시 테이블 같은 용어가 왜 필요한지 무리 없이 회수하고 있었습니다.
  - 판단: 보충학습 분량은 있지만 `옛 용어 목록`이 아니라 `질문 지도`로 정리해 중심축을 유지합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 보충학습 목적 부합, 용어 복귀 안정.
- 다음 순차 점검 대상은 Chapter 10 `P2-10.1`부터 `P2-10.3`입니다.

### 2026-07-18 Part 2 Chapter 10 순차 점검

- `P2-10.1`은 노트북을 새 도구 설명보다 `코드, 설명, 출력이 함께 있는 계산 문서`로 정리해, Chapter 7의 실행 환경 설명과 Chapter 8~9의 코드·구조 설명을 자연스럽게 연결하고 있었습니다.
  - 판단: 노트북의 장점과 주의점을 함께 두어 학습용 도구로서의 위치를 과장하지 않습니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 노트북 형식 설명 양호, 학습 기록 입구 안정.
- `P2-10.2`는 Jupyter, Colab, 로컬 실행을 기능 비교보다 `실행 위치`, `런타임`, `파일 접근` 차이로 설명해, Chapter 7의 환경 구분과 중복 없이 이어지고 있었습니다.
  - 판단: 같은 `.ipynb` 파일과 실제 런타임 상태를 분리해 읽게 하는 점이 재현성 관점에서 적절합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 실행 위치 비교 양호, 환경 구분 안정.
- `P2-10.3`은 노트북을 `다시 실행 가능한 기록`으로 정리하는 기준을 제시해, 편의성 설명으로 끝나지 않고 재현성·숨은 상태·셀 순서 문제까지 실제 학습 습관으로 연결하고 있었습니다.
  - 판단: Chapter 11~14로 넘어가기 전에 계산, 데이터, 시각화, 기록을 어떤 흐름으로 남길지 준비시키는 역할이 충분합니다.
  - 조치: 본문 수정 없이 유지합니다.
  - 분류: 재실행 기록 기준 양호, 재현성 습관 정리 안정.
- 다음 순차 점검 대상은 Chapter 11 `P2-11.1`부터 `P2-11.4`입니다.

## 현재 결정

- Part 2 순차 점검은 `docs/book/table-of-contents.md` 기준으로 시작 페이지, Chapter 1~15, 마무리 페이지 순서로 진행합니다.
- 영어 원어 병기는 유지하되, 초심자 기준에서 영어 기능어가 한국어 설명보다 먼저 튀는 문장은 우선 점검 대상으로 봅니다.
- 수학·도구 Part라고 해서 세부 증명이나 설치 절차가 본문 중심을 밀어내면 범위 이탈로 보고 줄입니다. 반대로 Part 3으로 이어지는 실제 계산 장면, 작은 예시, 점검 질문이 부족하면 보강 쪽을 우선합니다.
