# Section Release Note

- Section ID: `P6-1.1`
- Source File: `docs/parts/part-06/chapter-01/section-01.md`

### v2026.07.20
- 저작권 검토: Part 6 Module 1 순차 검토 범위에서 토큰 필요성, 비용·길이·문맥 보존 사례, 체크리스트를 확인했다. 사례와 비교 문장은 자체 설명 자료로 판단했다.
- 출처 사용 점검: OpenAI Help Center, OpenAI `tiktoken` README, Jurafsky & Martin `Speech and Language Processing` 초안은 토큰 개념과 단어 경계의 배경 근거로만 쓰였고, 원문 표·도식·예제 구조를 복제한 정황은 확인하지 않았다.
- 관련 자산: Chapter 1 Mermaid 자산 README 기준으로 한국어 본문용 `-ko.mmd`와 향후 번역용 `-en.mmd`가 분리되어 있음을 확인했다.
- 본문 반영: 없음. 기존 원문 버전 `v2026.07.19` 유지.
- 번역 동기화 메모: 번역본 영향 없음.

### v2026.07.19
- 본문 반영: `토큰`을 단어 하나나 의미 하나로 단정하지 않고 `모델이 세는 계산 조각`으로 먼저 붙잡아야 한다는 오해 방지 문단을 도입부와 사례 정리 뒤에 보강했다.
- 연습 보강: 단어 수와 토큰 수를 같은 것으로 봐도 되는지 판단하는 연습 행과 체크리스트 항목을 추가해, P6-1.1 체크포인트의 `토큰을 단어 자체나 의미 단위로 단정하지 않기` 기준을 현재 절 안에서 먼저 닫게 했다.
- 근거 정리: OpenAI Help Center의 토큰 설명, OpenAI `tiktoken` README, Jurafsky & Martin `Speech and Language Processing` 2026 초안 링크와 확인 날짜를 명시해 토큰·비용·한도·단어 경계 설명의 근거를 문서 단위로 좁혔다.
- 번역 동기화 메모: 대응 번역본이 생기면 토큰을 word/meaning unit으로 단정하지 않는 오해 방지 문장과 새 연습 행을 함께 반영해야 한다.

### v2026.07.18
- 본문 반영: 사례 뒤에 `바로 적용해 보면` 표를 추가해, 초심자가 `사람 기준 질문`을 `모델 기준 질문`으로 바꾸는 연습을 바로 해 볼 수 있게 했다.; 종결 문장도 `지금 단계에서 무엇을 붙잡으면 충분한가`를 더 직접 적어, 다음 절로 넘어가기 전 이해 닫힘을 보강했다.
- 사례 재정렬: 사례가 토큰화 내부 단계 설명으로 기울어 현재 절의 질문을 약하게 받치던 문제를 고쳐, `비용 감각`, `긴 문서 잘림`, `검색 문맥 보존`의 세 장면이 각각 왜 토큰 기준을 요구하는지 직접 보여 주도록 다시 정렬했다.
- 연습 재정렬: 연습을 답을 읽는 표에서 `비용 문제`, `잘림 문제`, `문맥 보존 문제`를 먼저 분류하는 형식으로 바꿔, 사례와 현재 절의 중심 질문이 같은 판단 축으로 이어지게 했다.
- 번역 동기화 메모: 대응 번역본이 생기면 사례 뒤의 적용 표, 연습 분류표, 마지막 닫힘 문장도 함께 반영해야 한다.

### v2026.07.12
- 본문 반영: `P6-1.1`을 `왜 토큰이 필요한가`라는 출발 절로 재작성했다. 사람의 길이 감각과 모델의 계산 단위를 구분하고, 길이 제한·비용·문맥 보존 문제 때문에 토큰이 등장한다는 흐름만 남기도록 본문을 크게 줄였다.
- 추가 반영: 재구성 뒤 사례층이 너무 얇아진 문제를 보완하기 위해, 짧은 한 줄 문장, 긴 문서 한 파일, 검색용 청크 분할의 세 장면을 다시 보강하고 비교표를 복원했다.
- 번역 동기화 메모: 대응 번역본이 생기면 `P6-1.1`은 정의 소개가 아니라 필요성부터 여는 절로 유지해야 한다.

### v2026.07.11
- 본문 반영: 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리했다.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 종결부 일반형 제목을 `언제 토큰 관점을 먼저 떠올려야 하는가`로 구체화했다.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
