# Section Release Note

- Section ID: `P6-1.8`
- Source File: `docs/parts/part-06/chapter-01/section-08.md`

### v2026.07.20
- 저작권 검토: Part 6 Module 1 순차 검토 범위에서 프롬프트 길이, RAG 청크, 출력 형식, 평가 로그 활용 사례를 확인했다. 운영 레버 선택 표와 연습은 자체 구성 설명 자료로 판단했다.
- 출처 사용 점검: OpenAI Help Center, OpenAI API Reference, Jurafsky & Martin 초안은 토큰·응답 길이·vector store chunking·evals 관찰값의 근거 확인에만 사용되며, 외부 문서의 절차표나 예제 데이터를 복제한 정황은 확인하지 않았다.
- 관련 자산: Chapter 1 Mermaid 자산 중 P6-1.8 대응 도식이 언어별 파일로 분리되어 있음을 확인했다.
- 본문 반영: 없음. 기존 원문 버전 `v2026.07.19` 유지.
- 번역 동기화 메모: 번역본 영향 없음.

### v2026.07.19
- 사례 중심축 보강: 프롬프트 길이, RAG 청크, 출력 형식 사례를 `사람이 먼저 보는 기준 -> 기준의 한계 -> 토큰 관점이 바꾸는 판단 -> 먼저 손볼 운영 레버` 흐름으로 재정렬해, 토큰 관점을 운영 레버 선택으로 연결했다.
- 연습 해설 보강: 운영 레버 고르기, 관찰값을 운영 질문으로 바꾸기, 남길 것과 줄일 것 구분하기의 세 연습으로 확장하고, 각 질문 뒤에 해설을 붙여 Section 중심 판단으로 닫았다.
- 추가 반영: Part 6 체크포인트의 평가 연결 축을 반영해 `평가 로그 해석` 운영 레버를 추가했다. 실패 케이스의 입력 길이, 검색 근거 잘림, 출력 한도, 사용 토큰 수를 함께 보며 모델 품질 문제와 토큰 한도 문제를 가르도록 보강했다.
- 차트 보강: 토큰 관련 실패를 `토큰 근거 확인 -> 압박 위치 판단 -> 입력/검색/출력/평가 레버 선택` 흐름으로 읽게 하는 Mermaid 도식 `p6-c01-s08-diagram-01-en.mmd` / `p6-c01-s08-diagram-01-ko.mmd`를 추가했다.
- 근거 정리: OpenAI Help Center의 토큰·응답 길이 제어 문서, OpenAI API Reference의 vector store chunking과 evals 문서, Jurafsky & Martin `Speech and Language Processing` 2026 초안 링크와 확인 날짜를 출처에 반영해 운영 레버 설명의 근거를 좁혔다.
- 번역 동기화 메모: 대응 번역본이 생기면 평가 로그 해석 행과 연습 2의 평가 실패 관찰값도 함께 반영해야 한다.

### v2026.07.18
- 본문 반영: `초심자가 먼저 해야 할 일`, `초심자가 자주 막히는 지점` 같은 문장을 낮춰, 토큰 활용처를 `어느 운영 레버를 먼저 손볼 것인가`라는 직접 질문으로 더 곧게 읽히게 다듬었다.; `## 바로 적용해 보면` 표를 추가해, 초심자가 `프롬프트 길이`, `RAG 청크`, `비용 예산`, `출력 형식` 가운데 지금 어느 활용처부터 다시 봐야 하는지 바로 고르게 했다.; 토큰 관점을 추상 원칙으로만 남기지 않고, 현재 보이는 실패를 운영 질문으로 바꾸게 하는 닫힘 문장을 덧붙였다.
- 추가 반영: `P6-1.3`과의 경계가 흐려지지 않도록, 이 절의 역할을 `토큰이 어디에 쓰이는가` 반복이 아니라 `어느 운영 레버부터 손볼 것인가`를 고르는 절로 다시 좁혔다.; 긴 문서 입력 전략을 같은 레벨의 운영 레버로 표와 적용 블록에 다시 올려, Chapter 1 마지막 절이 `길이·청크·비용·출력`을 하나의 운영 분기표로 묶게 했다.
- 구조 보강: 기존 `작게 확인해 보면` 블록을 `연습 및 예제`로 바꿔, 토큰 관점을 운영 레버 선택 문제로 직접 적용하게 했다.
- 번역 동기화 메모: 대응 번역본이 생기면 적용 표와 운영 질문 전환 문장도 함께 반영해야 한다.

### v2026.07.12
- 본문 반영: `P6-1.8`을 새로 추가하고, 프롬프트 길이 조정, RAG 청크 설계, 비용 예산 추정, 긴 문서 입력, 출력 형식 설계에 토큰 관점을 어떻게 활용하는지 정리했다.
- 추가 반영: 활용 절의 실감이 약해진 문제를 보완하기 위해, 프롬프트 다듬기, RAG 청크 분할, 응답 마지막 문장 보존의 세 사례와 `작게 확인해 보면` 표를 다시 덧붙였다.
- 번역 동기화 메모: 대응 번역본이 생기면 `P6-1.8`도 같은 독립 구조로 추가해야 한다.
