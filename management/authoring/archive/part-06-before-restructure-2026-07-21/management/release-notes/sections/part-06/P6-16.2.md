# Section Release Note

- Section ID: `P6-16.2`
- Source File: `docs/parts/part-06/chapter-16/section-02.md`

### v2026.07.20
- Python 예제 주석 보강: 운영 실패 사례별 retry, fallback, human review, stop 경로 결정 목적을 코드 블록 첫 줄 주석으로 명시했다.
- 저작권 검토: Part 6 Module 6 순차 검토 범위에서 운영 중 실패 대응, retry/fallback/stop/approval/model fix 분기 Python 예제와 복구 결정 분포 차트를 확인했다. 실패 사례, 복구 조건, routing 결과는 자체 구성 운영 판단 자료로 판단했다.
- 출처 사용 점검: OpenAI Integrations and observability, Evaluate agent workflows, Production best practices 문서는 실패 추적·평가·운영 대응 배경 근거로만 쓰였고, 외부 incident 로그·운영 룰셋·도식을 복제한 정황은 확인하지 않았다.
- 관련 자산: Chapter 16 README 기준으로 Mermaid 파일쌍과 `failure-recovery-routing-ko.png` / `failure-recovery-routing-en.png`, 생성 스크립트가 로컬 자산으로 관리됨을 확인했다.
- 본문 반영: 없음. 기존 원문 버전 `v2026.07.19` 유지.
- 번역 동기화 메모: 번역본 영향 없음.

### v2026.07.19
- 본문 반영: 연습 및 예제의 `문제 상황`, `입력`, `출력`, `입력(input)`, `확인할 개념` 목록을 문단형 도입으로 정리해, 실패 대응 분기 설명이 앞 적용 문단과 같은 형식으로 다시 반복되지 않게 다듬었다.
- 본문 정리: retry, fallback, stop, approval triage 표가 거의 같은 내용으로 다시 반복되던 부분을 짧은 운영 판단 문장으로 줄였다.
- 근거 정합성: OpenAI Integrations and observability, Evaluate agent workflows, Production best practices 참고 자료의 확인 날짜를 현재 검토일로 갱신했다.
- 차트 보강: Python 예제 결과를 기준으로 실패 계열과 복구 결정 분포를 나란히 보여 주는 차트를 추가해, 같은 실패도 retry, fallback, stop, human review, model fix로 갈라진다는 점을 시각화했다.
- 메타데이터: 본문 `Version`을 `v2026.07.19`로 갱신했다.
- 번역 동기화 메모: Future translations should avoid duplicating the exercise problem statement immediately before the input block.

### v2026.07.18

- 체크리스트 정리: 체크리스트 안에 섞여 있던 요약 표, 전환 문단, 중복 요약 항목을 걷고 현재 Section 중심 질문을 닫는 자기 확인 항목만 남겼다.
- 본문 반영: `## 이 절의 범위`, `## 이 절의 목표` 같은 메타 소제목을 `실패 경로 판단이 맡는 일`, `여기서 남겨야 할 구분`으로 바꿔, 실패 대응 설명이 진행 메모보다 내용 중심 제목으로 먼저 보이게 정리했다.; 실패 대응 설명에서 `초심자 기준에서는` 같은 메타 표지를 줄이고, retry/fallback/stop/approval 분기 기준이 바로 본문 질문으로 읽히게 다듬었다.; 실패 대응 장의 전환 문장에 `좋은 답 판단` 다음에 `실패한 답을 어떤 운영 경로로 보낼 것인가`를 정하는 자리라는 점을 더 직접 적고, 그 판단이 P6-17의 요청 상태값으로 남는다는 연결도 보강했다.; 도입부에서 `이 절을 읽는 순서` 같은 메타 안내를 더 줄이고, `먼저 보인 실패 신호 -> 바로 이어지는 대응 경로 -> 왜 이렇게 갈라지는가` 표를 앞쪽에 넣어 초심자가 실패 대응을 용어 나열보다 `retry`, `fallback`, `stop`, `approval` 분기 구조로 먼저 읽게 정리했다.
- 추가 반영: 제목 아래 `Section ID`, `Version` 표기를 저장소 공통 형식에 맞춰 blockquote 대신 일반 메타데이터 줄로 정리했다.; 평가와 운영 한도를 알아도 왜 실패 경로 판단이 별도로 더 필요한지 먼저 보이게 하려고, `지금 부족한 것 -> 이번 장이 붙이는 구조 -> 아직 남는 문제 -> 바로 이어지는 위치` 비교 블록을 추가했다.; `이 절은 다음 질문에 답합니다` 문장을 `이 절에서 먼저 붙잡을 질문`으로 바꿔, 실패 대응 장의 도입이 작업 메모처럼 읽히지 않게 정리했다.; `이 절은 Part 6에서 ...`와 `이 절에서는 ...로 설명합니다` 같은 메타 서술을 더 직접적인 설명 문장으로 바꿔, 실패 경로 판단이 바로 읽히게 정리했다.
- 정리 반영: `지금 읽는 층위는`, `이 절의 핵심은`처럼 절의 역할을 따로 표지하던 문장을 줄이고, 좋은 답 판단에서 실패 경로 결정으로 넘어가는 전환이 더 직접적으로 보이도록 앞부분과 종결부를 다듬었다.; 세 사례 요약 뒤에 `바로 적용해 보면` 블록을 추가해, 초심자가 `retry`, `fallback`, `stop`, `approval`을 실제 분기 질문으로 읽고 실패 대응 경로를 먼저 가르도록 보강했다.; 예제 실행 결과 바로 아래에 `failure_case`별 운영 경로 대응표를 덧붙여, `retry`, `fallback`, `stop`, 사람 검토, 모델 수정 중 어느 갈래로 보내야 하는지 각 실행 이름만 보고도 바로 읽히게 정리했다.; `이 절이 맡는 역할` 같은 설명 밖 문장을 줄이고, 좋은 답 판정과 실패 후 복구 경로 결정을 왜 분리해야 하는지가 본문 문장 자체로 보이게 다듬었다.
- 추가 정리: 적용 문단의 `초심자가 ...` 독자 표지를 줄이고, 실패 대응을 한 가지 해결책이 아니라 `retry`, `fallback`, `stop`, `approval` 분기 판단으로 읽게 다듬었다.
- 번역 동기화 메모: Future translations should preserve the new comparison block that separates failure routing from later request-flow and retrospective records.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 온콜 조직, 장애 등급, 관측성 스택 같은 생략 목록을 걷어내고, 이번 절이 `어디를 추적해야 실패 원인을 구분할 것인가`와 `retry/fallback/stop/approval 중 어디로 보낼 것인가`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 운영 판단의 후속 handoff를 `P6-17.1`, `P6-17.2`의 요청 흐름과 회고 기록으로 직접 연결했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P6-16.2 closes failure-routing judgment first and hands the resulting record structure to P6-17.

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: `실패 유형 -> retry/fallback/stop/approval` 1차 triage 표와 정리 문장을 추가했고, 이어서 `먼저 보인 신호 -> 1차 triage` 운영 표를 더해 일시 오류, 검색 실패, 권한 실패, 모델 실패를 서로 다른 운영 경로로 읽는 기준을 더 빠르게 닫히게 보강함. 종결 표지는 저장소 공통 기준에 맞춰 `체크리스트`로 통일함. 추가로 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 종결부에서는 `언제 실패 대응 관점을 먼저 떠올려야 하는가`만 남기도록 일반형 `언제 이 관점을 먼저 떠올려야 하는가`를 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
