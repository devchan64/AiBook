# Part 3 체크포인트 노트

이 문서는 Part 3을 목차 순서대로 읽을 때 각 Section이 담아야 할 중심 주제를 개괄식으로 정리한 체크포인트 노트입니다.

## 사용 원칙

- 이 노트는 `docs/table-of-contents.md`를 기준으로 Part 3의 시작/마무리 페이지, Chapter, Section을 같은 순서로 훑기 위한 개요 문서입니다.
- 순차 점검 기록, 품질 상태 메모, 후속 작업 로그는 이 문서에서 운영하지 않습니다.
- 체크포인트는 `문제 -> 샘플 -> 표 -> 특징 -> 비교 -> 해석 -> Part 4 인계` 흐름을 Section 단위로 잡는 데 목적을 둡니다.
- `학습 관점 누락 점검`은 Part 원고가 잃기 쉬운 설명 흐름을 점검하고, `목차 기준 체크포인트`는 Part 원고가 유지해야 할 구조 흐름을 관리합니다.

## 학습 관점 누락 점검

- 각 Chapter를 볼 때 `모델을 고르기 전에 왜 데이터 구조를 먼저 닫아야 하는가`가 분명한지 확인합니다.
- 각 Section를 볼 때 `샘플 단위`, `비교 기준`, `결과 열`, `운영 문맥` 가운데 무엇을 정하는 절인지 독자가 바로 구분할 수 있는지 확인합니다.
- 표와 사례는 단순 예시가 아니라 `무엇을 먼저 보고 무엇을 나중에 미뤄야 하는가`를 실제 판단 순서로 보여 주는지 확인합니다.
- 해석 관련 Section는 `무엇을 말할 수 있는가`와 `무엇은 아직 말하면 안 되는가`가 함께 적혀 있는지 확인합니다.
- Part 4로 넘기는 연결에서는 `특징`, `목표 라벨`, `분할`, `누출 위험` 가운데 무엇이 아직 미정인지 숨기지 않는지 확인합니다.
- Part 3에서는 특히 `샘플 단위 흔들림`, `원시 로그와 데이터셋 혼동`, `비교 리포트와 예측 문제 혼동`이 빠지지 않는지 다시 봅니다.
- `샘플 단위 -> 원시 로그와 요약 표 -> 특징과 중간 표현 -> 기준선 비교 -> 해석 경계 -> 예측 문제 승격` 흐름이 Part 3의 데이터 모델링 중심선으로 유지되는지 확인합니다.
- 후속 원고 검토에서는 `샘플 단위`, `원시 로그`, `기준선 비교`가 각각 다른 층위라는 점이 본문에서 반복적으로 유지되는지 확인합니다.

## Section 경계 정리

- Part 3의 전체 경계는 `모델 학습 전 문제 구조를 데이터 구조로 고정하는 일`입니다. 데이터 정리 기술, EDA 절차, 통계 검정, 특정 머신러닝 알고리즘 학습법으로 확장하지 않고, Part 4가 이어받을 샘플, 특징, 목표 후보, 분할 전제를 닫는 역할로 유지합니다.
- Module 1은 문제 구조를 세우는 입구입니다. P3-1.1은 데이터 모델링의 역할, P3-1.2는 진행 순서, P3-1.3은 질문 문장 작성 기준을 맡으므로, 모델 이름이나 알고리즘 후보가 앞서 나오지 않게 합니다.
- Chapter 2와 Chapter 3은 저장된 기록과 데이터셋 후보의 경계를 나눕니다. P3-2는 `저장 구조를 읽는 법`, P3-3은 `질문에 맞게 다시 설계하는 법`으로 두고, 원천데이터를 곧바로 학습 문제로 승격하지 않는 기준을 반복해서 유지합니다.
- Chapter 4는 Part 3의 바닥 규칙인 샘플 단위를 닫습니다. P3-4.1은 샘플 1건의 정의, P3-4.2~P3-4.4는 단위가 흔들릴 때 나타나는 신호, P3-4.5는 샘플 단위가 맞더라도 대표성은 별도 판단이라는 점을 맡습니다.
- Chapter 5는 원시 로그를 표 구조로 바꾸는 구간입니다. P3-5.1은 원시 로그 -> 요약 표 -> 집계 표의 표현 전환, P3-5.2는 평균 밖 패턴 보존, P3-5.3~P3-5.6은 시계열 입력 창과 누락·중첩 문제, P3-5.7은 여러 후속 사건을 결과 열로 접는 규칙으로 나눠 둡니다.
- Chapter 6은 특징과 중간 표현의 경계를 세웁니다. 사람이 만든 특징, 세그먼트 표현, 뒤 Part에서 학습될 표현을 한 덩어리로 섞지 말고, 요약 표의 식별·문맥·비교·결과 후보 열도 모두 특징처럼 읽지 않게 합니다.
- Chapter 7은 기준선 비교를 다룹니다. 기준선은 현재 샘플을 해석하기 위한 참조 구간이고, 기준 모델은 Part 4 이후의 모델 평가 기준이므로 둘을 같은 말로 쓰지 않습니다.
- Chapter 8은 해석 경계입니다. 표본 수, 반복성, 선택적으로 붙은 라벨, 운영 개입이 만드는 편향을 보며 `변화 신호`, `검토 필요`, `원인 확정`, `자동 조치`를 단계별로 분리해야 합니다.
- Chapter 9는 비교 리포트를 예측 문제로 올릴지 판단하는 인계 구간입니다. P3-9.1~P3-9.3은 산출물 층위 구분, P3-9.4~P3-9.6은 검토 결과와 라벨 후보의 안정화, P3-9.7~P3-9.13은 입력/결과 경계, target 정의, 오류 비용, 시간·개체·정보 경계의 Part 4 인계 조건으로 읽습니다.
- P3-9.13은 Part 4의 평가·분할·문제 유형을 미리 강의하는 절이 아니라 마지막 점검표입니다. time split, group split, data leakage, evaluation design, ranking, multiclass/regression은 이름과 필요성만 잡고 세부 절차는 Part 4로 넘깁니다.
- Part 3은 명시적 `보충학습` Section보다 짧은 본편 Section을 많이 둔 구조입니다. 짧은 Section을 합치거나 늘릴 때는 작업 로그처럼 세분화하지 말고, `현재 Section이 닫는 산출물 경계가 하나인가`를 기준으로 판단합니다.
- 코드 예제와 도식은 데이터 구조 판단을 보강하는 용도입니다. 예제가 모델 성능 비교나 알고리즘 튜닝으로 넘어가면 Part 3 경계를 넘는 것이므로, 샘플 재구성, 열 역할 확인, 기준선 비교, 라벨 후보 점검 수준으로 닫습니다.
- 다국어 본문이 이미 함께 있으므로, 한국어 원문에서 Section 경계나 소제목을 바꾸면 영어·중국어 번역본도 같은 `Section ID` 기준으로 후속 동기화 대상에 올려야 합니다.

## 목차 기준 체크포인트

- `index.md`: Part 3이 데이터 정리 기술 나열이 아니라 문제 구조를 데이터 구조로 바꾸는 Part라는 점을 분명히 해야 합니다.
- Part 흐름: Module 1은 `질문 정의`, `저장 구조 읽기`, `데이터셋 재설계`가 특징 설계보다 먼저 닫혀야 합니다.
- Part 흐름: Module 2와 Module 3은 `샘플`, `표`, `입력 구간`, `특징`, `기준선`이 서로 다른 층위라는 점을 유지한 채 이어져야 합니다.
- `summary.md`: Module 4와 Module 5에서 닫힌 해석 경계와 예측 문제 승격 조건을 Part 4 인계 문장으로 다시 묶어야 합니다.

### Module 1. 역할과 문제 장면

#### Chapter 1. 데이터 모델링은 무엇을 달성하고 어떻게 진행하는가 `모델링 관점`

- 중심축: 모델 선택보다 먼저 문제를 데이터 구조로 바꾸는 순서를 세우는 장입니다.
- `P3-1.1`: 데이터 모델링이 모델 선택보다 먼저 문제 구조를 드러내는 일이라는 점을 잡아야 합니다.
- `P3-1.2`: 질문에서 샘플, 표, 특징, 출력 구조로 이어지는 기본 순서를 정리해야 합니다.
- `P3-1.3`: 좋은 데이터 질문이 어떤 비교 구조와 출력 구조를 함께 드러내야 하는지 보여 주어야 합니다.

#### Chapter 2. 저장 구조와 설계 축을 먼저 읽기 `모델링 관점`

- 중심축: 저장된 기록과 분석·학습에 쓸 데이터셋 후보를 구분하는 읽기 기준을 만듭니다.
- `P3-2.1`: 저장된 기록이 곧바로 학습용 데이터셋이 아니라는 점을 분명히 해야 합니다.
- `P3-2.2`: 데이터셋 후보 안에 샘플, 특징, 기준선, 출력 구조가 함께 들어간다는 점을 보여 주어야 합니다.
- `P3-2.3`: 새 표를 처음 받았을 때 무엇부터 읽고 적어야 하는지 점검 순서를 남겨야 합니다.

#### Chapter 3. 데이터셋은 처음부터 주어지지 않는다 `모델링 관점`

- 중심축: 같은 원천데이터도 질문에 따라 다른 데이터셋으로 다시 설계된다는 점을 보여 줍니다.
- `P3-3.1`: 원천데이터를 바로 학습 문제로 승격하면 안 되는 이유를 설명해야 합니다.
- `P3-3.2`: 질문에 맞는 데이터셋이 어떻게 다시 설계되는지 비교 장면으로 보여 주어야 합니다.
- `P3-3.3`: 첫 표 초안을 만들 때 어떤 열 묶음부터 스케치해야 하는지 정리해야 합니다.

### Module 2. 샘플과 표 구조

#### Chapter 4. 샘플 단위를 어떻게 정하는가 `모델링 관점`

- 중심축: 한 건의 샘플을 무엇으로 볼지 정하는 일이 뒤의 특징, 라벨, 평가 전체를 좌우한다는 점을 다룹니다.
- `P3-4.1`: 비교 가능한 샘플 한 건을 무엇으로 잡을지 기준을 세워야 합니다.
- `P3-4.2`: 샘플 단위가 흔들리면 특징, 라벨, 평가가 함께 흔들린다는 점을 보여 주어야 합니다.
- `P3-4.3`: 한 행, 샘플 1건, 최근 구간 1개가 서로 다른 층위라는 점을 구분해야 합니다.
- `P3-4.4`: 샘플 단위를 잘못 잡았다는 신호를 실제 이상 징후와 연결해 설명해야 합니다.
- `P3-4.5`: 샘플 정의와 대표성 판단은 별개라는 점을 분명히 해야 합니다.

#### Chapter 5. 원시 로그를 요약 표로 바꾸는 법 `모델링 관점`

- 중심축: 원시 기록을 비교 가능한 표와 입력 구조로 바꾸는 표현 전환 과정을 정리합니다.
- `P3-5.1`: 원시 로그를 비교 가능한 표로 바꾸는 표현 전환 과정을 보여 주어야 합니다.
- `P3-5.2`: 요약 표가 평균 밖의 패턴까지 남겨야 하는 이유를 설명해야 합니다.
- `P3-5.3`: 원시 시계열이 바로 학습 입력 구조가 될 수 없는 이유를 정리해야 합니다.
- `P3-5.4`: 입력 창을 어디서 자르고 길이를 어떻게 맞출지 기준을 세워야 합니다.
- `P3-5.5`: 결측과 빈 구간을 샘플 유지 여부 판단과 연결해 다뤄야 합니다.
- `P3-5.6`: 겹치는 입력 창이 샘플 수를 부풀려 보이게 만드는 이유를 설명해야 합니다.
- `P3-5.7`: 한 샘플 뒤의 여러 후속 사건을 표 구조 안에서 어떻게 접어야 하는지 정리해야 합니다.

### Module 3. 특징과 비교 구조

#### Chapter 6. 특징과 중간 표현을 설계하는 법 `모델링 관점`

- 중심축: 비교 가능한 구조를 어떤 특징과 중간 표현으로 남길지 결정하는 기준을 세웁니다.
- `P3-6.1`: 비교할 구조를 어떤 특징으로 남길지 결정하는 기준을 잡아야 합니다.
- `P3-6.2`: 특징만으로 부족할 때 어떤 중간 표현을 추가할 수 있는지 보여 주어야 합니다.
- `P3-6.3`: 사람이 만든 특징과 뒤에서 학습될 표현이 어떻게 이어지는지 설명해야 합니다.
- `P3-6.4`: 요약 표의 모든 열이 특징은 아니라는 점을 분명히 해야 합니다.
- `P3-6.5`: 단위와 크기가 다른 특징을 함께 읽고 남기는 기본 원칙을 제시해야 합니다.
- `P3-6.6`: 같은 열 이름이어도 측정 방식이 바뀌면 다른 특징일 수 있다는 점을 잡아야 합니다.

#### Chapter 7. 기준선 비교 구조를 읽는 법 `모델링 관점`

- 중심축: 변화 신호를 보기 위해 무엇과 비교해야 하는지와 그 비교를 문장으로 옮기는 법을 다룹니다.
- `P3-7.1`: 남겨 둔 구조를 무엇과 비교해야 변화가 보이는지 설명해야 합니다.
- `P3-7.2`: 비교표를 사람이 검토하는 문장으로 어떻게 옮길지 보여 주어야 합니다.
- `P3-7.3`: 기준선 비교와 기준 모델이 왜 다른데 둘 다 필요한지 구분해야 합니다.
- `P3-7.4`: 기준선을 어떤 구간과 조건으로 잡아야 하는지 정리해야 합니다.
- `P3-7.5`: 기준선을 고정할지, 최근 평소 기준으로 갱신할지 판단 기준을 남겨야 합니다.

### Module 4. 해석 경계

#### Chapter 8. 표본 수와 반복성은 어떻게 읽는가 `모델링 관점`

- 중심축: 데이터 해석에서 어디까지 말하고 어디서 멈춰야 하는지 보수적 해석 경계를 세웁니다.
- `P3-8.1`: 해석 강도를 무엇으로 조절해야 하는지 분명히 해야 합니다.
- `P3-8.2`: 변화 신호 해석과 원인 단정의 경계를 나눠야 합니다.
- `P3-8.3`: 보수적 해석 문장이 어떤 순서와 어휘로 쓰이는지 보여 주어야 합니다.
- `P3-8.4`: 보수적 해석 문장을 경고 열과 검토 큐 기준으로 연결해야 합니다.
- `P3-8.5`: 여러 비교 열을 하나의 검토 우선순위 후보로 묶는 기준을 제시해야 합니다.
- `P3-8.6`: 확정 라벨이 일부 사례에만 있을 때 무엇을 함께 적어야 하는지 정리해야 합니다.
- `P3-8.7`: 현재 검토 규칙과 조치가 데이터 자체를 바꾸면 해석도 달라져야 한다는 점을 보여 주어야 합니다.

### Module 5. 문제 유형과 뒤 Part 연결

#### Chapter 9. 어떤 문제를 예측 문제로 만들 것인가 `모델링 관점`

- 중심축: 비교 리포트 수준의 문제를 예측 문제로 올릴지와 Part 4로 무엇을 넘길지 결정합니다.
- `P3-9.1`: 지금 문제를 어디까지 예측 문제로 올릴지 판단 기준을 세워야 합니다.
- `P3-9.2`: 어떤 문제는 왜 비교 리포트로 남겨야 하는지 설명해야 합니다.
- `P3-9.3`: 비교 리포트, 검토 후보 큐, 목표 라벨 후보 표의 차이를 구분해야 합니다.
- `P3-9.4`: 검토 결과가 운영 메모에서 목표 라벨 후보로 바뀌는 과정을 보여 주어야 합니다.
- `P3-9.5`: 같은 사건을 여러 산출물 사이에서 무엇으로 추적할지 정리해야 합니다.
- `P3-9.6`: 같은 사건에 다른 라벨이 붙을 때 무엇을 먼저 고정해야 하는지 다뤄야 합니다.
- `P3-9.7`: 입력과 결과를 Part 4로 넘기기 전에 무엇을 먼저 닫아야 하는지 분명히 해야 합니다.
- `P3-9.8`: 예측 1회가 실제로 무엇을 결정하는지와 점수와 정책의 차이를 구분해야 합니다.
- `P3-9.9`: 실제 목표와 대리 target의 차이를 정리해야 합니다.
- `P3-9.10`: 늦게 확정되는 라벨과 닫히지 않은 음성 라벨을 구분해야 합니다.
- `P3-9.11`: target 후보가 여러 개이거나 기준이 바뀔 때 무엇을 먼저 고정해야 하는지 설명해야 합니다.
- `P3-9.12`: 같은 target 이름이어도 어떤 오류가 더 아픈지 먼저 적어야 하는 이유를 보여 주어야 합니다.
- `P3-9.13`: 시간 순서 분할, 개체 분리, 데이터 누출, ranking 문제를 왜 여기서는 예고 수준으로만 다루는지 경계를 분명히 해야 합니다.

- `summary.md`: Part 3에서 만든 문제 구조와 데이터 구조를 Part 4 머신러닝 학습 구조로 어떻게 넘길지 다시 묶어 주어야 합니다.
