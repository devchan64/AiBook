# Part 3 미반영 체크리스트

이 문서는 Part 3 관련 작업 노트에서 아직 남아 있는 후속 항목만 모은 체크리스트입니다.

## 체크리스트

- [x] SVM 수학 보충학습을 별도 `보충학습`으로 만들지 검토한다.
- [x] 강화학습 수학 보충학습을 별도 `보충학습`으로 만들지 검토한다.
- [x] calibration, reliability diagram, Brier score, log loss를 Part 3 본편에서 어디까지 다룰지 결정한다.

## 이관한 기존 메모

- `beginner-reinforcement-review.md`
- `section-p3-6-3-plan.md`

## 현재 결정

- SVM은 별도 수학 `보충학습`을 추가하지 않습니다. Part 3에서는 margin, support vector, soft margin, kernel 직관까지를 본문 책임으로 두고, 라그랑주 승수·듀얼 최적화 같은 엄밀한 전개는 현재 책 범위 밖에 둡니다.
- 강화학습도 별도 수학 `보충학습`을 추가하지 않습니다. Part 3에서는 가치, 정책, 보상 설계, 탐험, sim-to-real, 후속 계보의 큰 흐름까지를 책임지고, 벨만 방정식의 엄밀한 전개나 수렴 증명은 현재 책 범위 밖에 둡니다.
- Part 3 본편에서는 정확도, 정밀도, 재현율, F1, MAE, RMSE, R²처럼 `문제 유형별로 먼저 어떤 질문을 던져야 하는가`를 세우는 지표를 우선 다룹니다.
- ROC, PR, log loss, calibration, silhouette의 첫 해설은 P3-6.4 보충학습으로 모읍니다.
- reliability diagram, Brier score 같은 더 세밀한 확률 점수 읽기는 P3-6.4 보충학습 범위에 둡니다.
- threshold 조정과 calibration의 서비스 판단 연결은 P3-15.3에서 다시 회수합니다.

## 이번 반영

- Part 3 시작/마무리 문서에 평가 지표 범위를 `본편 질문`과 `보충학습 해설`로 나누어 명시했습니다.
- P3-6.1, P3-6.2에도 같은 기준을 넣어 평가 장을 읽을 때 본편과 보충학습의 역할 차이가 바로 보이게 했습니다.
- Part 3 시작/마무리 문서와 P3-6.1, P3-8.1, P3-8.2에 `혼동 행렬과 오류 사례 -> baseline 비교` 흐름을 명시해 baseline과 오류 구조가 따로 읽히지 않게 정리했습니다.
- P3-13.1에 SVM 수학 범위를 다시 명시해 별도 `보충학습` 없이도 Part 3에서 어디까지 책임지는지 닫았습니다.
- P3-19.1에 강화학습 수학 범위를 다시 명시해 별도 `보충학습` 없이도 Part 3에서 어디까지 책임지는지 닫았습니다.
