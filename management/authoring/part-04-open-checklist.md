# Part 4 미반영 체크리스트

이 문서는 Part 4를 순차 점검하면서 남은 후속 항목을 모은 체크리스트입니다.

## 품질 상태 메모

- Part 4는 머신러닝을 알고리즘 목록이 아니라 공통 구조로 묶는 힘이 강한 Part입니다.
- 현재 남은 과제는 후반 선택 기준, baseline, 오류 사례, 평가 지표 연결이 계속 직접적으로 읽히는지 유지하는 일입니다.
- 구조는 안정적이므로 새 절 추가보다 예시와 요약 문장이 `무엇을 비교하고 무엇을 판단하는가`를 더 선명하게 보여 주는 편이 중요합니다.
- 다만 초심자 이해를 실제로 밀어 올리는 공백이 보이면, 알고리즘 이름만 늘리지 않는 선에서 학술적·교육적 배경 Section이나 연결 Chapter를 추가할 수 있습니다.
- 다만 시작 페이지처럼 개요 문서에서는 인용형 질문 나열이나 메타 문장이 길어지기 쉬워, 일반 본문 문장과 표 중심으로 다시 눌러 주는 점검이 계속 필요합니다.
- `P4-12.1`, `P4-12.2`는 주제 자체의 밀도보다 오버뷰성 브리지 문단이 앞서는 구간이 있어, 상세 항목 보강 계획을 별도 문서로 분리해 관리합니다.

## 순차 점검 기록

- `2026-07-05`: `chapter-03/section-01.md`, `chapter-03/section-02.md`, `chapter-04/section-01.md`, `chapter-04/section-02.md`, `chapter-05/section-01.md`, `chapter-05/section-02.md`, `chapter-06/section-01.md`, `chapter-06/section-02.md`, `chapter-06/section-03.md`, `chapter-06/section-04.md`를 현재 상태 기준으로 다시 확인했습니다.
- Chapter 3-6은 `휴리스틱 -> 데이터 분리 -> 과적합/일반화 -> 평가 지표` 흐름이 안정적이었고, Part 3에서 넘긴 사례·특징·목표값·기준선 감각도 평가와 검증 장면까지 비교적 자연스럽게 이어졌습니다.
- 새 챕터나 새 Section이 꼭 필요한 공백은 보이지 않았습니다. 다만 이 범위의 여러 Section에는 `Section ID`, `Version` 메타데이터가 빠져 있었고, `P4-6.3` 제목의 `매트릭스(metrics)`는 의미상 `지표(metrics)`로 읽히는 편이 맞았습니다.
- `P4-3.1`에는 `이 책에서는`, `사용자의 경험적 판단`처럼 공개 본문보다 작업 메모에 가까운 표현이 작은 수준으로 남아 있었습니다.
- 조치: `P4-3.1`부터 `P4-6.4`까지 메타데이터를 추가했고, `P4-3.1`의 메타 표현은 `여기서는`, `현장의 경험적 판단`으로 낮췄습니다. `P4-6.3` 제목도 `지표(metrics)`로 바로잡았습니다.
- 판단: Part 4 중반의 남은 과제는 구조 수정이 아니라, 뒤 Chapter에서도 `검증 -> 일반화 -> 오류 구조 -> 기준선 비교` 흐름이 계속 메모형 블록보다 본문형 설명으로 유지되는지 확인하는 일입니다.

- `2026-07-05`: `index.md`, `summary.md`, `chapter-01/section-01.md`, `chapter-01/section-02.md`, `chapter-02/section-01.md`, `chapter-02/section-02.md`, `chapter-02/section-03.md`를 현재 상태 기준으로 다시 확인했습니다.
- Part 4 시작 구간은 Part 3에서 넘긴 `사례 1건`, `입력 특징`, `목표값 후보`, `식별·운영 열`, `기준선 비교` 전제를 비교적 안정적으로 이어받고 있었고, 현재 범위에서 특정 사업 도메인 침범이나 관리 메모 누수는 크지 않았습니다.
- 새 챕터나 새 Section이 꼭 필요한 공백도 보이지 않았습니다. 다만 시작/마무리 페이지에는 `baseline`, `handoff`, `이 문서는 ... 자체 정리`처럼 관리 문서나 용어 표면이 먼저 보이는 표현이 일부 남아 있었습니다.
- 조치: `summary.md`의 `baseline 비교`는 `기준선(baseline) 비교`로, `handoff`는 `인계 기준`으로 바꾸고, `index.md`와 `summary.md` 끝의 안내 문장도 `이 개요 페이지`, `이 요약 페이지`로 낮춰 공개 본문 톤에 맞췄습니다.
- 판단: Part 4 초반의 남은 과제는 구조 보강보다, 뒤 Chapter에서도 `문제 구조 -> 데이터 분리 -> 평가 읽기 -> 기준선 비교 -> 다음 Part 연결` 흐름이 같은 밀도로 유지되는지 이어서 확인하는 일입니다.

- `2026-07-05`: `index.md`, `chapter-02/section-01.md`를 다시 읽으며 시작 구간 표현의 한국어 우선 설명과 인덱스 표기 흔들림을 재점검했습니다.
- 구조 자체는 안정적이었고, 현재 범위에서 관리 메모 누수나 특정 사업 도메인 침범은 크지 않았습니다. 다만 개요 표와 Part 3 연결 표에서 `feature`, `target`, `baseline`, `tuning`이 영어 중심으로 먼저 보이는 줄과 `Part 4은` 같은 작은 표기 흔들림이 남아 있었습니다.
- 조치: `index.md`에서 `기준선(baseline)`, `튜닝(tuning)`, `입력 특징`, `목표값`처럼 한국어 설명이 먼저 오도록 다듬었고, `Part 4은`을 `Part 4는`으로 정리했습니다. `P4-2.1`에서도 `입력 특징 열`, `목표값 후보`처럼 같은 용어 순서를 맞췄습니다.
- 판단: 시작 구간은 새 챕터나 새 섹션이 필요한 상태는 아니며, 현재는 용어 제시 순서를 통일하면서 Part 3에서 넘어온 사례-특징-목표값 구분을 더 직접적으로 읽히게 유지하는 편이 적절합니다.

- `2026-07-05`: `index.md`, `summary.md`를 다시 읽으며 Part 도입/마무리 페이지의 다음 Part 연결과 메타 표현을 재점검했습니다.
- 시작/마무리 페이지의 큰 구조는 안정적이었고, 평가 읽기 순서와 baseline 중심 설명도 현재 방향에 맞았습니다.
- 다만 `Part 5 이후`, `Part 4와 Part 5에서 회수`, `Part 6의 회고 템플릿`처럼 현재 Part 구조와 어긋나거나 너무 뭉뚱그린 연결 문장이 일부 남아 있었습니다.
- 조치: 표현 학습·신경망 계산 구조는 `Part 5`, 생성 모델·LLM 확장은 `Part 6`, 회고 기록 연결은 `Part 7`로 분리해 적었습니다. `summary.md`의 화살표형 개요와 `이 책에서는` 표현도 일반 본문 톤으로 정리했습니다.
- 판단: Part 4 시작/마무리 페이지는 새 챕터 추가보다 현재 연결 문장을 정확히 유지하고, 평가 읽기 순서를 메모형 블록보다 일반 문장과 표로 안정화하는 쪽이 더 적절합니다.

- `2026-07-05`: 현재 워크트리의 `chapter-01/section-01.md`, `chapter-01/section-02.md`, `chapter-02/section-01.md`, `chapter-02/section-02.md`, `chapter-02/section-03.md`를 다시 대조했습니다.
- 기존 점검 메모와 달리 실제 파일에는 `Section ID`, `Version` 메타데이터가 빠져 있었고, 초반 도입부에는 `여기서는 다음 질문에 답합니다`, `이 책에서는` 같은 메타 문구와 입문 비유가 다소 앞서는 부분이 남아 있었습니다.
- 조치: `P4-1.1`, `P4-1.2`, `P4-2.1`, `P4-2.2`, `P4-2.3`에 `Section ID`, `Version` 메타데이터와 릴리즈노트를 추가했고, 메타 문구는 일반 본문 흐름으로 낮추었습니다. `P4-2.1`의 `문제집과 해설지` 비유와 `P4-2.2`의 구어적 도입 표현도 더 일반적인 공개 본문 문장으로 정리했습니다.
- `2026-07-05`: `index.md`, `chapter-01/section-01.md`, `chapter-01/section-02.md`, `chapter-02/section-01.md`, `chapter-02/section-02.md`를 순차 점검했습니다.
- 시작 페이지는 구조 자체는 풍부했지만, 집필 규칙 기준으로 인용형 질문 나열과 메타 문장이 다소 많았습니다.
- 조치: 시작 페이지의 인용형 질문 나열을 일반 본문과 번호 목록으로 정리했고, `Part 1의 8장`을 `P1-8`로 정리했으며, `Part 4은` 같은 문장도 고쳤습니다.
- `P4-1.2`의 인용형 정의 나열도 한 문단으로 정리해, 정의를 일반 본문 안에서 자연스럽게 읽히도록 수정했습니다.
- `P4-1.1`, `P4-2.1`, `P4-2.2`는 현재 저장소 작업 흔적이나 관리 메모성 표현이 본문으로 직접 새어 들어간 부분은 크지 않았습니다.
- `2026-07-05`: `chapter-02/section-03.md`, `chapter-03/section-01.md`, `chapter-03/section-02.md`를 이어서 점검했습니다.
- `P4-2.3`은 강화학습의 `에이전트`와 LLM 서비스의 `에이전트`를 혼동하지 않게 미리 경계를 그어 두고 있어 흐름이 안정적이었습니다.
- `P4-3.1`, `P4-3.2`는 휴리스틱을 메모성 조언이 아니라 검증 가능한 작업 가설로 설명하고 있어 현재 방향이 적절했습니다.
- 이 범위에서는 추가 본문 수정이 필요할 정도의 인덱스 표기 흔들림, 현재 워크트리 작업 누수, 도메인 과잉 침범은 크지 않았습니다.
- `2026-07-05`: `chapter-04/section-01.md`, `chapter-04/section-02.md`, `chapter-05/section-01.md`, `chapter-05/section-02.md`를 이어서 점검했습니다.
- 데이터 분리, 검증/테스트, 과적합/과소적합, 일반화 설명은 Part 3에서 넘긴 `sample`, `feature`, `baseline`, `새 데이터에서 버티는가` 관점과 비교적 자연스럽게 이어졌습니다.
- 이 범위의 주요 수정은 인덱스 규칙 쪽 작은 정리였습니다. `P4-4.1`에서 `Part 2의 확률·통계`처럼 장 이름식으로 남아 있던 참조를 일반화 문장으로 바꾸어, Part/Chapter 번호 체계와 직접 충돌하지 않게 정리했습니다.
- 현재까지 평가 장 초반에서는 현재 워크트리 작업 흔적이나 관리 메모성 문장이 본문으로 직접 새어 들어간 부분은 크지 않았습니다.
- `2026-07-05`: `chapter-06/section-01.md`부터 `chapter-06/section-04.md`까지 이어서 점검했습니다.
- 평가 장은 `혼동 행렬과 오류 사례 -> 지표 해석 -> baseline 비교` 흐름이 비교적 잘 유지되고 있어, 모델 평가를 운영 판단과 분리해 읽게 하는 방향이 안정적이었습니다.
- `P4-6.4` 보충학습에는 ROC, PR, log loss, calibration, silhouette 정의를 인용형 세 줄로 세운 부분이 있었고, 이는 본문 정의를 가능하면 일반 문단 안에 넣는 규칙과 약간 어긋났습니다.
- 조치: 의미는 유지한 채 인용형 정의를 일반 본문 문장으로 바꾸어, 보충학습 절도 본편과 같은 문장 흐름으로 읽히도록 정리했습니다.
- `2026-07-05`: `chapter-07/section-01.md`부터 `chapter-08/section-02.md`까지 이어서 점검했습니다.
- 특징 선택과 전처리 절은 현재 도메인 일반화 방향을 잘 유지하고 있었고, 원시 시계열을 동작 1회 요약 행으로 바꾼 뒤 특징을 고른다는 흐름도 Part 3과 자연스럽게 연결되었습니다.
- `P4-8.1`에서는 `한 행이 무엇을 뜻하는가`, `동작 1회 요약 행`을 Part 2에서 다룬 것처럼 잘못 연결한 문장이 있었고, 이는 실제 책 흐름상 Part 3을 가리켜야 했습니다.
- 조치: 해당 연결을 `Part 3과 P4-7의 흐름`으로 바로잡았습니다. `P4-8.2`의 `baseline은 이길 상대가 아니라, 점수를 읽는 자이다` 같은 비유 문장도 더 직접적인 기준선 설명 문장으로 다듬었습니다.
- `2026-07-05`: `chapter-09/section-01.md`부터 `chapter-13/section-02.md`까지 이어서 점검했습니다.
- 하이퍼파라미터, 튜닝, 선형회귀, 로지스틱 회귀, k-NN, SVM 절은 전반적으로 `baseline -> 오류 사례 -> 비교 기준 -> 다음 조정 질문` 흐름을 유지하고 있어, 후반 모델 절이 단순 알고리즘 카탈로그로 무너지지는 않았습니다.
- 현재 구간에서는 관리 메모 문장이나 현재 저장소 작업 흔적이 본문으로 직접 새어 들어간 부분은 크지 않았습니다. 다만 참조 표기에서 `11.2`처럼 Part 접두어 없이 남은 표현과, 정규화 연결 위치를 `P4-8.1`로 잘못 가리키는 작은 흔들림이 보였습니다.
- 조치: `P4-10.1`, `P4-10.2`, `P4-11.1`의 참조 문장을 현재 번호 체계와 실제 회수 위치에 맞게 정리했습니다.
- `2026-07-05`: `chapter-07/section-01.md`부터 `chapter-09/section-03.md`까지 현재 상태 기준으로 다시 확인했습니다.
- 특징 선택, 전처리, 모델 선택, 기준 모델, 하이퍼파라미터, 튜닝 절은 전반적으로 `입력 표현 -> 후보군 -> 기준선 비교 -> 설정 비교` 흐름을 유지하고 있어 구조 자체는 안정적이었습니다.
- 현재 범위에서는 관리 메모가 본문으로 직접 새어 들어간 부분이나 특정 사업 도메인 침범은 크지 않았습니다. 다만 `P4-7.1`부터 `P4-9.3`까지 여러 Section에 `Section ID`, `Version` 메타데이터가 빠져 있었고, `P4-9.3`에는 GPU·분산 학습 회수 위치를 `Part 4와 Part 5`로 잘못 적은 문장이 남아 있었습니다.
- 조치: `P4-7.1`부터 `P4-9.3`까지 메타데이터를 추가했고, `P4-9.3`의 연결 문장은 실제 구조에 맞게 `Part 5`, `Part 6` 기준으로 바로잡았습니다.
- 판단: Chapter 7-9 범위는 새 챕터나 새 Section을 추가해야 할 공백보다, 뒤 알고리즘 장에서도 같은 밀도로 `입력 표현`, `baseline`, `오류 사례`, `비교 비용` 흐름이 이어지는지 계속 점검하는 편이 더 중요합니다.
- `2026-07-05`: `chapter-14/section-01.md`, `chapter-14/section-02.md`, `chapter-15/section-01.md`, `chapter-15/section-02.md`, `chapter-15/section-03.md`를 이어서 점검했습니다.
- 결정트리, 트리 과적합, 랜덤포레스트, 특징 중요도, OOB 절은 `질문 흐름 -> 복잡도 위험 -> 앙상블 안정화 -> 해석 경계 -> 내부 점검`으로 잘 이어져, 후반 트리 계열 설명도 카탈로그식 나열보다 비교 기준 중심으로 유지되고 있었습니다.
- 이 범위에서는 현재 워크트리 메모가 본문으로 직접 새어 들어간 문장이나, 에스프레소/특정 장비 같은 도메인 침범 표현은 크지 않았습니다.
- 당장 본문 수정을 요구할 정도의 인덱스 표기 흔들림도 두드러지지 않았고, 현재 과제는 Part 4 후반부가 같은 밀도를 유지하는지 계속 확인하는 쪽에 가깝습니다.
- `2026-07-05`: `chapter-16/section-01.md`부터 `chapter-19/section-04.md`, `summary.md`까지 이어서 점검했습니다.
- 부스팅, 클러스터링, 차원 축소, 강화학습 절은 전반적으로 `모델 소개 -> 해석 경계 -> 적용 위험 -> 다음 검증 질문` 구조를 유지하고 있어, Part 4 후반도 큰 틀에서는 비교 기준 중심으로 읽혔습니다.
- 현재 구간에서도 현재 저장소 작업 메모나 특정 사업 도메인 문장이 본문으로 직접 새어 들어간 부분은 크지 않았습니다. 다만 `summary.md`에는 오래된 `Part 1의 8장`식 표현, `Part 4의 딥러닝` 같은 잘못된 다음 Part 연결 문장, `Part 4은` 같은 표기 흔들림이 남아 있었습니다.
- 조치: `summary.md`의 번호 참조와 다음 Part 연결 문장을 현재 구조에 맞게 정리했고, `P4-16.1`, `P4-16.2`의 정규화·시스템 최적화 연결 문장도 실제 회수 위치에 맞게 다듬었습니다.
- `2026-07-05`: Part 3에서 Part 4로 넘어오는 인계 전제가 시작부에서 충분히 직접적으로 보이는지 다시 점검했습니다.
- 시작 페이지와 `P4-2.1`은 전반적으로 안정적이었지만, `한 행이 무엇을 뜻하는가`, `feature와 target 후보를 어떻게 가르는가`, `식별·운영 열은 왜 모델 입력과 분리하는가`가 초반 독서 흐름에 한 번 더 드러나면 Part 3과의 연결이 더 자연스러웠습니다.
- 조치: `index.md`에 `Part 3에서 넘겨받는 기준` 표를 추가했고, `P4-2.1`에는 사례 1건, feature 열, target 후보, 식별·운영 열을 먼저 가르는 표와 설명 문단을 보강했습니다.
- `2026-07-06`: `index.md`, `chapter-01/section-01.md`, `chapter-01/section-02.md`, `chapter-02/section-01.md`, `chapter-02/section-02.md`를 다시 읽으며 Part 3 인계 전제가 Part 4 초반에서 충분히 천천히 풀리는지 점검했습니다.
- 시작 페이지와 초반 본문은 전반적으로 안정적이었고, 현재 범위에서 관리 메모 누수나 특정 사업 도메인 재침범은 다시 보이지 않았습니다.
- 다만 `데이터에서 규칙을 배운다`는 설명 구간에서는 특징 열, 라벨 후보, 식별 열을 한 번 더 직접 가를 필요가 있었고, 지도학습/비지도학습 도입부에서는 `라벨이 있다`, `군집 ID가 붙었다`는 사실만으로 곧바로 문제 정의를 확정하는 오해를 더 분명히 막을 필요가 있었습니다.
- 조치: `P4-1.2`에는 특징 열, 라벨 후보, 식별 열, 운영 메모 열을 나누는 비교 표와 표현-학습-평가 한 문장 요약을 추가했습니다. `P4-2.1`에는 라벨 존재와 라벨 품질 점검을 나누는 비교 표를 넣었습니다. `P4-2.2`에는 군집 ID, 2차원 그림의 가까움, 이상치 후보를 `정답`이 아닌 `구조 후보`로 읽게 하는 대비 표와 해석 순서를 보강했습니다.
- 판단: Part 4 초반은 새 챕터나 새 Section이 필요한 상태는 아니며, 현재는 Part 3에서 넘긴 `사례 1건`, `입력 열`, `목표값 후보`, `보류할 문맥 열` 구분이 머신러닝 도입 설명 안에서 계속 살아 있도록 밀도를 조절하는 편이 더 중요합니다.

- `2026-07-09`: `P4-12.1`, `P4-12.2`를 레포 원칙 기준으로 다시 검토했습니다.
- 두 Section 모두 중심 주제는 분명했지만, `학습 배경`과 중간 비교 문단에서 커리큘럼 안내, 다음 장 연결, 운영 프레임 설명이 길어 본문이 오버뷰처럼 읽히는 구간이 있었습니다.
- 조치: 본문 수정 전에 `management/authoring/p4-12-density-improvement-plan.md`를 만들어, 오버뷰 비중을 줄이고 상세 항목 밀도를 높이는 기준과 수정 순서를 따로 정리했습니다.
- 판단: 이 범위의 다음 작업은 새 절 추가가 아니라, `k-NN의 판단 순서`, `k의 역할`, `거리 함수`, `스케일 왜곡`, `표준화의 의미`를 더 직접적으로 설명하도록 본문 밀도를 재배치하는 일입니다.
- `2026-07-09`: Chapter 11의 로지스틱 회귀 보충학습 구조를 다시 검토했습니다.
- 기존 `P4-11.3`은 log-odds, MLE, 다중 클래스, solver와 regularization을 한 절에서 함께 회수하고 있어 중심 질문이 분산되어 있었고, 사례와 예제도 각 소주제를 한 번에 떠받치기에는 집중도가 약했습니다.
- 조치: Chapter 11 보충학습을 세 갈래로 재구성했습니다. `P4-11.3`은 `log-odds와 MLE를 처음 읽는 법`으로 범위를 좁혔고, 새 `P4-11.4`에는 다중 클래스(multinomial) 확장, 새 `P4-11.5`에는 solver와 regularization 설명을 분리했습니다. `P4-11.1`, `P4-11.2`의 후속 연결 문장, `docs/book/table-of-contents.md`, `mkdocs.yml`, 대응 릴리즈노트와 `BOOK-toc` 릴리즈노트도 함께 갱신했습니다.
- 판단: 이번 경우는 단순 알고리즘 이름 추가가 아니라, 초심자 이해 공백을 메우기 위한 보충학습 책임 분리로 보는 편이 맞았습니다. 보충학습은 본편보다 더 잘게 나눌 수 있지만, 각 절은 여전히 `하나의 중심 질문`을 유지해야 한다는 기준을 확인했습니다.
- `2026-07-09`: `management/authoring/p4-12-density-improvement-plan.md` 기준으로 Chapter 12를 실제 재구성했습니다.
- `P4-12.1`은 `k-NN의 직관`, `k`, `학습의 의미`에 다시 집중하도록 오버뷰성 문단과 역사 블록을 줄이고, `P4-12.2`는 거리 규칙, 이웃 순서 변화, 스케일 왜곡, 표준화 설명에 집중하도록 재작성했습니다.
- 새 `P4-12.3`을 추가해 `언제 k-NN을 후보로 올릴까`, `무엇을 먼저 점검할까`, `review query를 어떻게 읽을까`를 별도 Section으로 분리했습니다.
- `docs/book/table-of-contents.md`, `mkdocs.yml`, 대응 릴리즈노트도 함께 갱신했습니다.

## 체크리스트

- [x] SVM 수학 보충학습을 별도 `보충학습`으로 만들지 검토한다.
- [x] 강화학습 수학 보충학습을 별도 `보충학습`으로 만들지 검토한다.
- [x] calibration, reliability diagram, Brier score, log loss를 Part 4 본편에서 어디까지 다룰지 결정한다.
- [x] Chapter 14 이후에서도 시작 페이지와 비슷한 인용형 나열, 메타 문장 과다, Part/Section 인덱스 표기 흔들림이 반복되는지 점검한다.
- [x] Part 3에서 넘긴 sample, feature, target, baseline 구분이 트리·앙상블·비지도·강화학습 장에서도 계속 자연스럽게 이어지는지 확인한다.
- [x] Chapter 16 이후에서 모델 비교가 다시 목록식 나열로 흐르지 않는지, 보충학습 경계가 흐려지지 않는지 점검한다.
- [x] Part 3에서 Part 4로 넘어오는 인계 전제가 시작 페이지와 지도학습 도입부에서 충분히 직접적으로 보이는지 다시 점검한다.
- [x] `P4-12.1`, `P4-12.2`에서 오버뷰성 `학습 배경`과 운영 프레임 문단을 줄이고, 필요하면 새 Section 추가까지 포함해 상세 항목 보강 계획에 따라 본문을 재작성한다.

## 이관한 기존 메모

- `beginner-reinforcement-review.md`
- `section-p3-6-3-plan.md`

## 현재 결정

- SVM은 별도 수학 `보충학습`을 추가하지 않습니다. Part 4에서는 margin, support vector, soft margin, kernel 직관까지를 본문 책임으로 두고, 라그랑주 승수·듀얼 최적화 같은 엄밀한 전개는 현재 책 범위 밖에 둡니다.
- 강화학습도 별도 수학 `보충학습`을 추가하지 않습니다. Part 4에서는 가치, 정책, 보상 설계, 탐험, sim-to-real, 후속 계보의 큰 흐름까지를 책임지고, 벨만 방정식의 엄밀한 전개나 수렴 증명은 현재 책 범위 밖에 둡니다.
- Part 4 본편에서는 정확도, 정밀도, 재현율, F1, MAE, RMSE, R²처럼 `문제 유형별로 먼저 어떤 질문을 던져야 하는가`를 세우는 지표를 우선 다룹니다.
- ROC, PR, log loss, calibration, silhouette의 첫 해설은 P4-6.4 보충학습으로 모읍니다.
- reliability diagram, Brier score 같은 더 세밀한 확률 점수 읽기는 P4-6.4 보충학습 범위에 둡니다.
- threshold 조정과 calibration의 서비스 판단 연결은 P4-15.3에서 다시 회수합니다.
- 현재 Part 4를 다시 읽다가 `왜 이런 모델 계열이 갈라지는가`, `확률적 출력은 어떻게 읽는가`, `문제 유형과 손실 선택은 어떻게 이어지는가`처럼 입문 공백이 보이면 새 학술·교육 보강 Section 추가를 허용합니다.

## 이번 반영

- Part 4 시작/마무리 문서에 평가 지표 범위를 `본편 질문`과 `보충학습 해설`로 나누어 명시했습니다.
- P4-6.1, P4-6.2에도 같은 기준을 넣어 평가 장을 읽을 때 본편과 보충학습의 역할 차이가 바로 보이게 했습니다.
- Part 4 시작/마무리 문서와 P4-6.1, P4-8.1, P4-8.2에 `혼동 행렬과 오류 사례 -> baseline 비교` 흐름을 명시해 baseline과 오류 구조가 따로 읽히지 않게 정리했습니다.
- Chapter 11의 로지스틱 회귀 보충학습을 `P4-11.3 log-odds와 MLE`, `P4-11.4 다중 클래스`, `P4-11.5 solver와 regularization`의 세 갈래로 분리해, 한 절에 너무 많은 회수 과제가 몰리지 않게 재구성했습니다.
- P4-13.1에 SVM 수학 범위를 다시 명시해 별도 `보충학습` 없이도 Part 4에서 어디까지 책임지는지 닫았습니다.
- P4-19.1에 강화학습 수학 범위를 다시 명시해 별도 `보충학습` 없이도 Part 4에서 어디까지 책임지는지 닫았습니다.

## 다음 유지 점검

- 대표 모델 비교가 다시 `모델 카탈로그`처럼 보이지 않고 선택 기준 중심으로 읽히는지 점검합니다.
- baseline, 혼동 행렬, 오류 사례, threshold 판단이 서로 분리된 절차가 아니라 하나의 운영 질문으로 이어지는지 점검합니다.
- 본편과 보충학습의 경계가 다시 흐려지지 않는지 점검합니다.
