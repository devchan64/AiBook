# Manuscript Release Note

- Document ID: `MANUSCRIPT-concept-glossary`
- Source File: `docs/reference/concept-glossary.md`

### v2026.07.06

- 변경 이유: 책 본문을 순차 보강하는 동안 반복 등장하는 핵심 개념을 중복 없이 빠르게 다시 찾을 수 있는 원고형 개념사전이 필요해졌다.
- 문서 반영: `docs/reference/concept-glossary.md`를 새로 만들고, Part 1 초반에서 반복 등장한 핵심 개념을 가나다순으로 정리했으며 각 항목에 `중심 Section`을 함께 기록했다.
- 번역 동기화 메모: 향후 다국어 원고가 생기면 표제어 기준, 원어 병기, Section ID 연결 정보를 언어별 개념사전에도 대응시켜야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `management/authoring/concept-glossary-guidelines.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-2

- 변경 이유: Part 1 Chapter 13과 Chapter 14를 보강하면서 임베딩, RAG, 앱, 도구, 에이전트, MCP, 하네스 같은 핵심 용어도 개념사전에서 바로 찾을 수 있게 할 필요가 생겼다.
- 문서 반영: `검색 증강 생성`, `도구`, `모델 컨텍스트 프로토콜`, `앱`, `에이전트`, `임베딩`, `오케스트레이션`, `유사도 검색`, `하네스` 항목을 추가하고 중심 Section를 연결했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 약어(RAG, MCP)와 한영 병기 형식을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-13/section-01.md`, `docs/parts/part-01/chapter-13/section-02.md`, `docs/parts/part-01/chapter-13/section-03.md`, `docs/parts/part-01/chapter-14/section-01.md`, `docs/parts/part-01/chapter-14/section-02.md`, `docs/parts/part-01/chapter-14/section-03.md`, `docs/parts/part-01/chapter-14/section-04.md`, `docs/parts/part-01/chapter-14/section-05.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-3

- 변경 이유: Part 1 Chapter 15를 보강하면서 윤리, 저작권, 개인정보 관련 핵심 용어도 개념사전에서 바로 연결할 필요가 생겼다.
- 문서 반영: `디지털 안전성`, `저작권`, `인용`, `인간 감독`, `개인정보` 항목을 추가하고 중심 Section를 연결했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `safety`, `human oversight`, `privacy` 같은 용어의 한국어 표제어 선택을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-15/section-01.md`, `docs/parts/part-01/chapter-15/section-02.md`, `docs/parts/part-01/chapter-15/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-4

- 변경 이유: Part 1 Chapter 16을 보강하면서 개인 학습, 업무 자동화, 검색, 문서화, 생산성 같은 적용·검증 용어도 개념사전에서 바로 이어질 필요가 생겼다.
- 문서 반영: `문서화`, `생산성`, `업무 자동화`, `개인 학습`, `검토`, `검색` 항목을 추가하고 중심 Section를 연결했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `documentation`, `productivity`, `work automation`, `review` 표제어 선택을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-16/section-01.md`, `docs/parts/part-01/chapter-16/section-02.md`, `docs/parts/part-01/chapter-16/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-5

- 변경 이유: Part 1 Chapter 17과 13.4를 보강하면서 전망, 예측, 작업 가설, 지표, 벡터 검색 구현 직관과 연결되는 용어도 개념사전에서 바로 이어질 필요가 생겼다.
- 문서 반영: `미래 전망`, `사실 주장`, `시나리오`, `예측`, `해석`, `작업 가설`, `지표` 항목을 추가하고 중심 Section를 연결했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `forecast`, `prediction`, `indicator`, `working hypothesis` 표제어 선택을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-13/section-04.md`, `docs/parts/part-01/chapter-17/section-01.md`, `docs/parts/part-01/chapter-17/section-02.md`, `docs/parts/part-01/chapter-17/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-6

- 변경 이유: Part 2 Chapter 1부터 Chapter 14까지를 순차 보강하면서 수학 기초, Python 실행 환경, 자료구조, NumPy, Pandas, Matplotlib, Git 관련 핵심 개념이 반복 등장해 개념사전의 Part 2 연결 범위를 크게 넓힐 필요가 생겼다.
- 문서 반영: 대표 Section 기준으로 수식 읽기, 벡터·행렬, 미분, 확률·통계, 최적화, 실행 환경, 노트북, NumPy, Pandas, Matplotlib, Git 관련 핵심 표제어를 추가하고 `중심 Section` 연결을 Part 2 개정본과 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 수학·도구 혼합 표제어의 한영 병기, 대표 Section 연결, 후속 Section 최소 연결 원칙을 같은 `Section ID` 기준으로 함께 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-02/index.md`, `management/authoring/concept-glossary-guidelines.md`, `management/release-notes/sections/part-02/`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-7

- 변경 이유: Part 1 Chapter 1의 첫 Section를 다시 점검하면서 `AI` 자체를 개념사전에서 곧바로 찾을 수 있어야 대표 설명 위치와 책 전체의 가장 넓은 바깥 범주를 안정적으로 연결할 수 있다고 판단했다.
- 문서 반영: `AI(인공지능, artificial intelligence)` 항목을 추가하고, 넓은 분야이자 시스템 범주라는 뜻, 왜 중요한지, 함께 볼 개념, `P1-1.1` 중심 Section를 연결했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `AI`, `artificial intelligence`, `인공지능` 표제어 선택과 설명 범위를 같은 `Section ID` 기준으로 함께 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-01/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-8

- 변경 이유: Part 1 Chapter 1 Section 2를 다시 점검하면서 `문제 유형` 대표 설명 위치에 맞춰 분류, 군집화, 인식, 생성, 순위화, 추천, 제어 같은 핵심 표제어를 개념사전에서도 바로 찾을 수 있어야 한다고 판단했다.
- 문서 반영: `군집화(clustering)`, `분류(classification)`, `생성(generation)`, `순위화(ranking)`, `인식(recognition)`, `제어(control)`, `추천(recommendation)` 항목을 추가하고 중심 Section를 `P1-1.2`로 연결했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 문제 유형 표제어의 한영 병기와 `P1-1.2` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-01/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-9

- 변경 이유: Part 1 Chapter 1 Section 3를 다시 점검하면서 `생성형 AI`와 `LLM`이 용어 관계의 대표 설명 위치와 직접 연결되어 있어야 Chapter 1 초반의 층위 구분이 개념사전에서도 바로 이어진다고 판단했다.
- 문서 반영: `생성형 AI(generative AI)`와 `LLM(대규모 언어 모델)` 항목을 추가하고, 각각 출력 범주와 대표 모델 계열이라는 구분을 반영해 중심 Section를 `P1-1.3`으로 연결했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `generative AI`, `LLM`, `large language model` 표제어 선택과 `P1-1.3` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-01/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-10

- 변경 이유: Part 1 Chapter 2 Section 1을 다시 점검하면서 `추론 엔진`도 규칙 기반 시스템의 기본 구조를 설명하는 이 절과 직접 연결되어 있어야 대표 설명 위치 체계가 더 일관적이라고 판단했다.
- 문서 반영: `추론 엔진(inference engine)` 항목의 중심 Section에 `P1-2.1`을 추가해 `규칙 기반 시스템`, `기호 기반 AI`와 같은 대표 설명 위치 흐름으로 읽히도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `inference engine` 항목의 대표 Section 연결도 같은 기준으로 반영해야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-02/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-11

- 변경 이유: Part 1 Chapter 2 Section 2를 다시 점검하면서 `탐색`, `확률 추론`, `휴리스틱`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 세 흐름의 역할 차이가 개념사전에서도 바로 이어진다고 판단했다.
- 문서 반영: `탐색(search)`, `확률 추론(probabilistic reasoning)`, `휴리스틱(heuristic)` 항목을 추가하고 중심 Section를 `P1-2.2`로 연결했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `search`, `probabilistic reasoning`, `heuristic` 표제어의 한영 병기와 `P1-2.2` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-02/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-12

- 변경 이유: Part 1 Chapter 3 Section 1을 다시 점검하면서 `규칙 기반 시스템`의 구성 요소와 한계를 복습할 때 `사실`, `설명 기능`, `지식 기반`, `지식 획득`, `전문가 시스템`도 개념사전에서 바로 찾아야 최소 연결 원칙이 유지된다고 판단했다.
- 문서 반영: `사실(fact)`, `설명 기능(explanation facility)`, `지식 기반(knowledge base)`, `지식 획득(knowledge acquisition)`, `전문가 시스템(expert system)` 항목을 추가하고 중심 Section를 `P1-3.1`로 연결했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 규칙 기반 AI 관련 용어의 한영 병기와 `P1-3.1` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-03/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-13

- 변경 이유: Part 1 Chapter 3 Section 2를 다시 점검하면서 `예시-특징-라벨-학습-일반화` 구조를 가장 직접적으로 묶는 `지도학습`도 개념사전에서 바로 찾아야 이후 Section의 최소 연결 원칙이 더 안정적으로 유지된다고 판단했다.
- 문서 반영: `지도학습(supervised learning)` 항목을 추가하고 뜻, 중요성, 관련 개념, 중심 Section를 `P1-3.2`로 연결했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `supervised learning` 표제어 병기와 `P1-3.2` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-03/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-14

- 변경 이유: Part 1 Chapter 3 Section 3을 다시 점검하면서 `표현`, `벡터`, `활성값`의 내부 역할 구분이 이 절의 대표 설명 위치와 직접 연결되어 있어야 규칙 기반 접근과 표현 학습의 차이를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `활성값(activation)` 항목을 추가하고 `벡터(vector)` 항목의 중심 Section에 `P1-3.3`을 보강해 `표현(representation)`과 함께 이 절의 대표 설명 위치 흐름으로 읽히도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `vector`, `activation`, `representation` 표제어 병기와 `P1-3.3` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-03/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-15

- 변경 이유: Part 1 Chapter 4 Section 3을 다시 점검하면서 `특징`, `표현`, `파라미터`, `활성값`이 모델 계산 관점의 대표 설명 위치와 직접 연결되어 있어야 3.3의 개념적 대비와 4.3의 계산 관점을 개념사전에서도 분리해 복습할 수 있다고 판단했다.
- 문서 반영: `특징(feature)`, `벡터(vector)`, `표현(representation)`, `파라미터(parameter)`, `활성값(activation)` 항목의 중심 Section에 `P1-4.3`을 보강하고, `가중치(weight)` 항목은 그래프 문맥과 모델 계산 문맥을 함께 설명하도록 정리했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `feature`, `representation`, `parameter`, `activation`, `weight` 표제어 병기와 `P1-4.3` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-04/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-16

- 변경 이유: Part 1 Chapter 4 Section 4를 다시 점검하면서 `문제 정의`, `평가`, `회귀`가 이 절의 대표 설명 위치와 직접 연결되어 있어야 현실 목표와 모델링 과제, 출력 정의, 평가 기준의 관계를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `문제 정의(task definition)`, `평가(evaluation)`, `회귀(regression)` 항목을 추가하고 중심 Section를 `P1-4.4`로 연결했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `task definition`, `evaluation`, `regression` 표제어 병기와 `P1-4.4` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-04/section-04.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-17

- 변경 이유: Part 1 Chapter 5 Section 1을 다시 점검하면서 `학습`, `적합`, `편향`, `가중치`가 `learning/training/fitting` 구분과 내부 값 조정의 대표 설명 위치와 직접 연결되어 있어야 이후 `inference`와의 경계도 개념사전에서 바로 복습할 수 있다고 판단했다.
- 문서 반영: `학습(training)` 항목에 `learning`과 `training`의 넓고 좁은 뜻을 함께 반영하고 중심 Section에 `P1-5.1`을 추가했으며, `적합(fitting)`과 `편향(bias)` 항목을 새로 추가했다. `가중치(weight)` 항목에도 `P1-5.1` 중심 Section를 보강했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `learning`, `training`, `fitting`, `bias`, `weight` 용어 경계와 `P1-5.1` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-05/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-18

- 변경 이유: Part 1 Chapter 5 Section 2를 다시 점검하면서 `inference`가 `모델 실행`의 대표 설명 위치와 직접 연결되어 있어야 `학습/훈련`과 `모델 실행`의 경계를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `추론(inference)` 항목의 중심 Section에 `P1-5.2`를 추가해 기호 기반 추론과 학습된 모델 실행이라는 두 문맥을 함께 읽도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `inference` 표제어의 문맥 구분과 `P1-5.2` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-05/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-19

- 변경 이유: Part 1 Chapter 5 Section 3을 다시 점검하면서 `추론(inference)` 항목도 한국어 `추론`이 덮는 여러 영어 표현의 충돌을 설명하는 대표 위치와 직접 연결되어 있어야 같은 표제어를 좁고 넓은 문맥에서 다시 읽을 수 있다고 판단했다.
- 문서 반영: `추론(inference)` 항목의 중심 Section에 `P1-5.3`을 추가해 모델 실행 문맥과 용어 충돌 정리 문맥을 함께 읽도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `inference` 표제어의 문맥 구분과 `P1-5.3` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-05/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-20

- 변경 이유: Part 1 Chapter 6 Section 1을 다시 점검하면서 `불완전한 정보`, `부분 관측`, `불확실성`, `잡음`, `예외`, `후보 폭증`이 규칙만으로 처리하기 어려운 문제 조건의 대표 설명 위치와 직접 연결되어 있어야 이후 확률·탐색 장으로 넘어가는 흐름을 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `불완전한 정보(incomplete information)`, `부분 관측(partial observability)`, `불확실성(uncertainty)`, `잡음(noise)`, `예외(exception)`, `후보 폭증(combinatorial explosion)` 항목을 추가하고 중심 Section를 `P1-6.1`로 연결했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `incomplete information`, `partial observability`, `uncertainty`, `noise`, `exception`, `combinatorial explosion` 표제어 병기와 `P1-6.1` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-06/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-21

- 변경 이유: Part 1 Chapter 6 Section 2를 다시 점검하면서 `확률`, `근거`, `보정`, `무작위`, `비결정적`, `확률적 과정`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 `불확실성/확률/stochastic` 구분과 확률 점수 해석의 경계를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `근거(evidence)`, `보정(calibration)`, `비결정적(nondeterministic)`, `무작위(random)`, `확률적 과정(stochastic process)` 항목을 추가하고, `확률(probability)` 항목의 중심 Section에 `P1-6.2`를 보강했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `evidence`, `calibration`, `nondeterministic`, `random`, `stochastic process`, `probability` 표제어 병기와 `P1-6.2` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-06/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-22

- 변경 이유: Part 1 Chapter 7 Section 1을 다시 점검하면서 `탐색`, `탐색 공간`, `계산 한계`, `완전 탐색`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 7.2의 휴리스틱으로 넘어가기 전 탐색 문제의 구조를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `계산 한계(computational limit)`, `완전 탐색(exhaustive search)`, `탐색 공간(search space)` 항목을 추가하고, `탐색(search)` 항목의 중심 Section에 `P1-7.1`을 보강했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `computational limit`, `exhaustive search`, `search space`, `search` 표제어 병기와 `P1-7.1` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-07/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-23

- 변경 이유: Part 1 Chapter 7 Section 2를 다시 점검하면서 `휴리스틱`, `휴리스틱 함수`, `충분히 좋은 해`가 이 절의 대표 설명 위치와 직접 연결되어 있어야 7.1의 탐색 구조와 7.3의 경계 설명 사이에서 휴리스틱의 실무적 의미를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `충분히 좋은 해(good-enough solution)`, `휴리스틱 함수(heuristic function)` 항목을 추가하고, `휴리스틱(heuristic)` 항목의 중심 Section에 `P1-7.2`를 보강했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `good-enough solution`, `heuristic function`, `heuristic` 표제어 병기와 `P1-7.2` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-07/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-24

- 변경 이유: Part 1 Chapter 7 Section 3을 다시 점검하면서 `휴리스틱 점수`, `확률 모델`, `확률 추정값`, `임계값`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 점수, 확률, 운영 기준의 경계를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `휴리스틱 점수(heuristic score)`, `확률 모델(probabilistic model)`, `확률 추정값(probability estimate)`, `임계값(threshold)` 항목을 추가했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `heuristic score`, `probabilistic model`, `probability estimate`, `threshold` 표제어 병기와 `P1-7.3` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-07/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-25

- 변경 이유: Part 1 Chapter 8 Section 1을 다시 점검하면서 `지도학습`, `라벨`, `라벨링`, `타깃`, `분류`, `회귀`가 이 절의 대표 설명 위치와 직접 연결되어 있어야 Chapter 8의 학습 유형 도입을 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `라벨링(labeling)` 항목을 추가하고, `지도학습(supervised learning)`, `라벨(label)`, `타깃(target)`, `분류(classification)`, `회귀(regression)` 항목의 중심 Section에 `P1-8.1`을 보강했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `labeling`, `supervised learning`, `label`, `target`, `classification`, `regression` 표제어 병기와 `P1-8.1` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-08/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-26

- 변경 이유: Part 1 Chapter 8 Section 2를 다시 점검하면서 `비지도학습`, `구조`, `군집화`, `차원 축소`, `클러스터 라벨`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 라벨 없는 데이터에서 구조를 찾는 학습이라는 관점을 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `구조(structure)`, `비지도학습(unsupervised learning)`, `차원 축소(dimensionality reduction)`, `클러스터 라벨(cluster label)` 항목을 추가하고, `군집화(clustering)`와 `표현(representation)` 항목의 중심 Section에 `P1-8.2`를 보강했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `structure`, `unsupervised learning`, `dimensionality reduction`, `cluster label`, `clustering`, `representation` 표제어 병기와 `P1-8.2` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-08/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-27

- 변경 이유: Part 1 Chapter 8 Section 3을 다시 점검하면서 `강화학습`, `에이전트`, `환경`, `상태`, `행동`, `보상`, `정책`, `탐험`, `활용`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 행동과 보상 중심의 학습 구조를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `강화학습(reinforcement learning)`, `환경(environment)`, `상태(state)`, `행동(action)`, `보상(reward)`, `정책(policy)`, `탐험(exploration)`, `활용(exploitation)` 항목을 추가하고, `에이전트(agent)` 항목의 중심 Section에 `P1-8.3`을 보강했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `reinforcement learning`, `environment`, `state`, `action`, `reward`, `policy`, `exploration`, `exploitation`, `agent` 표제어 병기와 `P1-8.3` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-08/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-28

- 변경 이유: Part 1 Chapter 9 Section 1을 다시 점검하면서 `이미지 인식`, `수작업 특징`, `학습된 표현`, `CNN`, `AlexNet`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 딥러닝의 표현 학습 전환을 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `이미지 인식(image recognition)`, `수작업 특징(hand-crafted features)`, `학습된 표현(learned representation)`, `합성곱 신경망(CNN, convolutional neural network)`, `알렉스넷(AlexNet)` 항목을 추가하고, `표현 학습(representation learning)` 항목의 중심 Section에 `P1-9.1`을 보강했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `image recognition`, `hand-crafted features`, `learned representation`, `CNN`, `AlexNet`, `representation learning` 표제어 병기와 `P1-9.1` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-09/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-29

- 변경 이유: Part 1 Chapter 9 Section 2를 다시 점검하면서 `객체 검출`, `바운딩 박스`, `YOLO`, `음성 생성`, `WaveNet`, `TTS`가 이 절의 대표 설명 위치와 직접 연결되어 있어야 딥러닝 패러다임이 다른 출력 구조로 확산된 사례를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `객체 검출(object detection)`, `바운딩 박스(bounding box)`, `욜로(YOLO)`, `음성 생성(speech generation)`, `웨이브넷(WaveNet)`, `텍스트 음성 변환(TTS, text-to-speech)` 항목을 추가했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `object detection`, `bounding box`, `YOLO`, `speech generation`, `WaveNet`, `TTS` 표제어 병기와 `P1-9.2` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-09/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-30

- 변경 이유: Part 1 Chapter 9 Section 3을 다시 점검하면서 `직접 계보`, `주변 근거`, `언어 모델링`, `Seq2Seq`, `Attention`, `Transformer`가 이 절의 대표 설명 위치와 직접 연결되어 있어야 LLM의 직접 발전사와 딥러닝 확산 배경을 개념사전에서도 바로 구분해 복습할 수 있다고 판단했다.
- 문서 반영: `직접 계보(direct lineage)`, `언어 모델링(language modeling)`, `시퀀스 투 시퀀스(Seq2Seq)`, `어텐션(Attention)`, `트랜스포머(Transformer)` 항목을 추가했다. `주변 근거(surrounding evidence)` 항목도 함께 반영해 직접 계보와의 대비가 드러나도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `direct lineage`, `surrounding evidence`, `language modeling`, `Seq2Seq`, `Attention`, `Transformer` 표제어 병기와 `P1-9.3` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-09/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-31

- 변경 이유: Part 1 Chapter 10 Section 1을 다시 점검하면서 `예측(prediction)` 표제어가 현재는 전망 문맥으로만 좁게 읽힐 수 있어, 분류·회귀·생성의 차이를 설명하는 머신러닝 문맥도 함께 반영해야 개념사전과 본문 연결이 자연스럽다고 판단했다.
- 문서 반영: `예측(prediction)` 항목을 입력 기반 추정이라는 넓은 뜻으로 다시 정리하고 중심 Section에 `P1-10.1`을 추가했다. `생성(generation)`과 `생성형 AI(generative AI)` 항목의 중심 Section에도 `P1-10.1`을 보강했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `prediction`, `generation`, `generative AI` 표제어의 넓고 좁은 문맥 구분과 `P1-10.1` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-10/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-32

- 변경 이유: Part 1 Chapter 10 Section 2를 다시 점검하면서 `다음 출력 생성`, `다음 토큰 예측`, `토큰`, `샘플링`, `디퓨전 모델`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 생성 과정의 반복적 직관과 모델별 차이를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `다음 출력 생성(next-output generation)`, `다음 토큰 예측(next-token prediction)`, `디퓨전 모델(diffusion model)`, `샘플링(sampling)`, `토큰(token)` 항목을 추가했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `next-output generation`, `next-token prediction`, `diffusion model`, `sampling`, `token` 표제어 병기와 `P1-10.2` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-10/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-33

- 변경 이유: Part 1 Chapter 10 Section 3을 다시 점검하면서 `품질`, `환각`, `confabulation`, `권리와 책임`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 생성 결과의 해석과 위험 검토를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `품질(quality)`, `환각(hallucination)`, `confabulation`, `권리와 책임(rights and responsibility)`, `책임(accountability)` 항목을 추가하고, `근거(evidence)`, `검토(review)`, `저작권(copyright)`, `인간 감독(human oversight)` 항목의 중심 Section에 `P1-10.3`을 보강했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `quality`, `hallucination`, `confabulation`, `rights and responsibility`, `accountability` 표제어 병기와 `P1-10.3` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-10/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-34

- 변경 이유: Part 1 Chapter 11 Section 1을 다시 점검하면서 `언어 모델`, `통계적 언어 모델`, `엔그램`, `데이터 희소성`, `분산 표현`, `말뭉치`, `워드투벡`, `임베딩`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 LLM 이전 언어 모델 흐름을 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `언어 모델(language model)`, `통계적 언어 모델(statistical language model)`, `엔그램(n-gram)`, `데이터 희소성(sparsity)`, `분산 표현(distributed representation)`, `말뭉치(corpus)`, `워드투벡(word2vec)` 항목을 추가했다. `임베딩(embedding)` 항목은 중심 Section를 `P1-11.1`, `P1-13.1`로 재정렬하고 설명을 언어 모델 맥락과 검색 맥락이 함께 보이도록 다듬었다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `language model`, `statistical language model`, `n-gram`, `sparsity`, `distributed representation`, `corpus`, `word2vec`, `embedding` 표제어 병기와 `P1-11.1` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-11/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-35

- 변경 이유: Part 1 Chapter 11 Section 2를 다시 점검하면서 `RNN`, `hidden state`, `LSTM`, `GRU`, `Seq2Seq`, `Encoder-Decoder`, `fixed-length vector`, `Attention`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 Transformer 이전 순차 모델 흐름을 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `고정 길이 벡터(fixed-length vector)`, `게이트 순환 유닛(GRU, gated recurrent unit)`, `순환 신경망(RNN, recurrent neural network)`, `인코더-디코더(Encoder-Decoder)`, `장단기 메모리(LSTM, long short-term memory)` 항목을 추가했다. `숨은 상태(hidden state)` 항목은 RNN 내부 상태와 노트북 런타임 문맥을 함께 구분하도록 넓혔고, `시퀀스 투 시퀀스(Seq2Seq)`와 `어텐션(Attention)` 항목의 중심 Section를 `P1-11.2` 기준으로 재정렬했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `RNN`, `hidden state`, `LSTM`, `GRU`, `Seq2Seq`, `Encoder-Decoder`, `fixed-length vector`, `Attention` 표제어 병기와 `P1-11.2` 대표 연결, `hidden state`의 문맥 구분을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-11/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-36

- 변경 이유: Part 1 Chapter 11 Section 3을 다시 점검하면서 `Transformer`, `self-attention`, `positional encoding`, `Encoder`, `Decoder`, `사전학습`, `fine-tuning`, `BERT`, `GPT`, `in-context learning`, `문맥적 표현`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 현대 LLM의 구조와 학습 절차를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `BERT`, `GPT`, `디코더(decoder)`, `문맥 내 학습(in-context learning)`, `문맥적 표현(contextual representation)`, `미세조정(fine-tuning)`, `사전학습(pretraining)`, `셀프 어텐션(self-attention)`, `위치 인코딩(positional encoding)`, `인코더(encoder)` 항목을 추가했다. `트랜스포머(Transformer)` 항목의 중심 Section는 `P1-11.3`, `P1-9.3` 순으로 재정렬했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `Transformer`, `self-attention`, `positional encoding`, `Encoder`, `Decoder`, `pretraining`, `fine-tuning`, `BERT`, `GPT`, `in-context learning`, `contextual representation` 표제어 병기와 `P1-11.3` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-11/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-37

- 변경 이유: Part 1 Chapter 12 Section 1을 다시 점검하면서 `프롬프트`, `지시`, `맥락`, `제약`, `출력 형식`, `예시`, `프롬프트 vs fine-tuning`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 LLM 사용 단계의 입력 설계를 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `맥락(context)`, `지시(instruction)`, `제약(constraint)`, `출력 형식(output format)`, `프롬프트(prompt)` 항목을 추가했다. `사례(example)` 항목의 중심 Section에 `P1-12.1`을 보강했고, `미세조정(fine-tuning)` 항목도 `P1-12.1`에서의 비교 문맥이 드러나도록 중심 Section를 재정렬했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `prompt`, `instruction`, `context`, `constraint`, `output format`, `example`, `fine-tuning` 표제어 병기와 `P1-12.1` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-12/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-38

- 변경 이유: Part 1 Chapter 12 Section 2를 다시 점검하면서 `few-shot prompting`과 `프롬프트 구조화`가 이 절의 대표 설명 위치와 직접 연결되어 있어야 지시·맥락·예시를 배치하는 실제 입력 구성 방식을 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `프롬프트 구조화(prompt structuring)`와 `퓨샷 프롬프팅(few-shot prompting)` 항목을 추가했다. `문맥 내 학습(in-context learning)` 항목의 중심 Section에도 `P1-12.2`를 보강해 예시 기반 입력 유도와의 연결이 드러나도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `prompt structuring`, `few-shot prompting`, `in-context learning` 표제어 병기와 `P1-12.2` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-12/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-39

- 변경 이유: Part 1 Chapter 12 Section 3을 다시 점검하면서 `프롬프트의 한계`, `사실성`, `최신성`, `일관성`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 프롬프트 이후의 검토 기준을 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `사실성(factuality)`, `최신성(recency)`, `일관성(consistency)`, `프롬프트의 한계(limit of prompting)` 항목을 추가했다. `근거(evidence)`, `재현성(reproducibility)`, `평가(evaluation)`, `환각(hallucination)` 항목의 중심 Section에도 `P1-12.3`을 보강했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `factuality`, `recency`, `consistency`, `limit of prompting`, `evidence`, `reproducibility`, `evaluation`, `hallucination` 표제어 병기와 `P1-12.3` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-12/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-40

- 변경 이유: Part 1 Chapter 13 Section 1을 다시 점검하면서 `벡터`, `벡터 공간`, `문장·문단·문서 임베딩`, `청크`가 이 절의 대표 설명 위치와 직접 연결되어 있어야 임베딩을 서비스 관점의 검색 준비 단계로 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `벡터 공간(vector space)`, `문장·문단·문서 임베딩(sentence, paragraph, and document embedding)`, `청크(chunk)` 항목을 추가했다. `벡터(vector)` 항목의 중심 Section에도 `P1-13.1`을 보강해 임베딩 장의 대표 연결이 드러나도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `vector space`, `sentence embedding`, `paragraph embedding`, `document embedding`, `chunk`, `vector` 표제어 병기와 `P1-13.1` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-13/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-41

- 변경 이유: Part 1 Chapter 13 Section 2를 다시 점검하면서 `유사도`, `거리`, `코사인 유사도`, `최근접 이웃`, `상위 k개`가 이 절의 대표 설명 위치와 직접 연결되어 있어야 유사도 검색의 비교 기준과 후보 선정 직관을 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `유사도(similarity)`, `코사인 유사도(cosine similarity)`, `최근접 이웃(nearest neighbor)`, `상위 k개(top-k)` 항목을 추가했다. `거리(distance)` 항목은 이미 있음을 확인했고, `순위화(ranking)` 항목의 중심 Section에도 `P1-13.2`를 보강해 후보 정렬 문맥이 드러나도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `similarity`, `distance`, `cosine similarity`, `nearest neighbor`, `top-k`, `ranking` 표제어 병기와 `P1-13.2` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-13/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-42

- 변경 이유: Part 1 Chapter 13 Section 3을 다시 점검하면서 `보강`, `parametric memory`, `non-parametric memory`, `출처 추적`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 RAG를 검색-생성 연결 구조로 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `보강(augmentation)`, `비파라메트릭 메모리(non-parametric memory)`, `출처 추적(provenance)`, `파라메트릭 메모리(parametric memory)` 항목을 추가했다. `검색(search)`과 `생성(generation)` 항목의 중심 Section에도 `P1-13.3`을 보강해 RAG 문맥이 드러나도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `augmentation`, `parametric memory`, `non-parametric memory`, `provenance`, `search`, `generation` 표제어 병기와 `P1-13.3` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-13/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-43

- 변경 이유: Part 1 Chapter 13 Section 4를 다시 점검하면서 `검색 인덱스`, `근사 최근접 이웃`, `메타데이터`, `벡터 데이터베이스`, `전체 비교`, `그래프 기반 검색`이 이 절의 대표 설명 위치와 직접 연결되어 있어야 벡터 검색 구현의 직관을 개념사전에서도 바로 복습할 수 있다고 판단했다.
- 문서 반영: `검색 인덱스(search index)`, `근사 최근접 이웃(ANN, approximate nearest neighbor)`, `메타데이터(metadata)`, `벡터 데이터베이스(vector database)`, `전체 비교(brute-force search)`, `그래프 기반 검색(graph-based search)` 항목을 추가했다. `필터링(filtering)` 항목의 중심 Section에도 `P1-13.4`를 보강해 검색 운영 문맥이 드러나도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `search index`, `ANN`, `metadata`, `vector database`, `brute-force search`, `graph-based search`, `filtering` 표제어 병기와 `P1-13.4` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-13/section-04.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-44

- 변경 이유: Part 1 Chapter 14 Section 1을 다시 점검하면서 `모델`과 `데이터`가 여전히 학습 문맥 중심으로만 읽히면 서비스 구성요소 설명과 충돌할 수 있어, 대표 설명 위치인 `P1-14.1` 기준으로 뜻을 넓혀 둘 필요가 있었다.
- 문서 반영: `데이터(data)` 항목 정의를 학습 데이터셋뿐 아니라 근거 자료, 상태 정보, 사용자 입력까지 포괄하도록 확장하고 중심 Section에 `P1-14.1`을 추가했다. `모델(model, 모형)` 항목도 서비스 안에서 예측·분류·생성·판단을 수행하는 계산 부품 의미를 함께 드러내도록 보강하고 중심 Section에 `P1-14.1`을 추가했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `data`, `model` 표제어가 학습 문맥과 서비스 문맥을 함께 포괄한다는 점, 그리고 `P1-14.1` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-14/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-45

- 변경 이유: Part 1 Chapter 14 Section 2를 다시 점검하면서 `RAG`와 `도구 사용`의 경계를 개념사전에서도 바로 복습할 수 있어야 했는데, `도구 사용`, `도구 호출`, `승인` 표제어가 비어 있어 실행 구조 설명이 약하게 남아 있었다.
- 문서 반영: `도구 사용(tool use)`, `도구 호출(tool call)`, `승인(approval)` 항목을 추가해 `P1-14.2`의 핵심 구분인 읽기 구조와 실행 구조, 실행 전 확인 절차를 개념사전에서 다시 따라갈 수 있게 했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `tool use`, `tool call`, `approval` 표제어와 `P1-14.2` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-14/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-46

- 변경 이유: Part 1 Chapter 14 Section 3을 다시 점검하면서 `에이전트` 설명은 들어와 있었지만, 루프를 이루는 `목표`, `관찰`, `종료 조건` 표제어가 비어 있고 `상태`, `행동`도 강화학습 문맥으로만 좁게 읽혀 서비스형 에이전트 설명과 연결이 약했다.
- 문서 반영: `목표(goal)`, `관찰(observation)`, `종료 조건(stop condition)` 항목을 추가했다. `상태(state)`와 `행동(action)` 항목은 강화학습 의미를 유지하되 서비스형 에이전트의 작업 진행 상태와 실행 단계까지 함께 읽히도록 정의와 중심 Section를 `P1-14.3` 기준으로 확장했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `goal`, `observation`, `stop condition`, `state`, `action` 표제어가 강화학습 문맥과 서비스형 에이전트 문맥을 함께 포괄한다는 점, 그리고 `P1-14.3` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-14/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-47

- 변경 이유: Part 1 Chapter 14 Section 4를 다시 점검하면서 `MCP` 본표제어는 있었지만, 초심자가 구조를 따라가기 위한 `호스트`, `클라이언트`, `서버`, `리소스`, `발견` 표제어가 비어 있어 연결 표준의 구성요소를 개념사전에서 바로 복습하기 어려웠다.
- 문서 반영: `리소스(resource)`, `발견(discovery)`, `클라이언트(client)`, `서버(server)`, `호스트(host)` 항목을 추가해 `P1-14.4`의 핵심인 연결 주체, 제공 대상, 발견 흐름을 개념사전에서 다시 따라갈 수 있게 했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `resource`, `discovery`, `client`, `server`, `host` 표제어와 `P1-14.4` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-14/section-04.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-48

- 변경 이유: Part 1 Chapter 14 Section 5를 다시 점검하면서 `하네스` 본표제어는 있었지만, 실행을 관찰하고 반복 비교하는 핵심 표제어인 `그레이더`, `로그`, `추적`, `실행 환경`이 비어 있었고 `평가`, `재현성`도 아직 `P1-14.5` 대표 위치를 직접 가리키지 않았다.
- 문서 반영: `그레이더(grader)`, `로그(log)`, `추적(trace)`, `실행 환경(execution environment)` 항목을 추가했다. `평가(evaluation)`와 `재현성(reproducibility)` 항목의 중심 Section에도 `P1-14.5`를 보강해 하네스, 기록, 반복 검증 흐름이 개념사전에서 함께 읽히도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `grader`, `log`, `trace`, `execution environment`, `evaluation`, `reproducibility` 표제어와 `P1-14.5` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-14/section-05.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-49

- 변경 이유: Part 1 Chapter 14 Section 6을 다시 점검하면서 서비스 제약을 설명하는 핵심 표제어인 `비용`, `지연 시간`, `처리량`, `레이트 리밋`, `재시도`, `배치`, `캐싱`, `운영`이 개념사전에 비어 있어 14.6의 현실 제약 관점을 본문 밖에서 다시 복습하기 어려웠다.
- 문서 반영: `배치(batch)`, `비용(cost)`, `운영(operation)`, `재시도(retry)`, `지연 시간(latency)`, `처리량(throughput)`, `레이트 리밋(rate limit)`, `캐싱(caching)` 항목을 추가해 `P1-14.6`의 대표 서비스 제약 개념을 개념사전에서 다시 따라갈 수 있게 했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `batch`, `cost`, `operation`, `retry`, `latency`, `throughput`, `rate limit`, `caching` 표제어와 `P1-14.6` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-14/section-06.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-50

- 변경 이유: Part 1 Chapter 15 Section 1을 다시 점검하면서 `편향(bias)` 표제어가 여전히 모델 파라미터 의미로만 잡혀 있어 사회적 편향 설명과 충돌했고, `안전성(safety)`과 `투명성(transparency)` 대표 표제어도 비어 있어 윤리 절의 복습 경로가 약했다.
- 문서 반영: 사회적 위험 문맥의 `편향(bias)` 항목을 `P1-15.1` 대표 위치로 다시 정의했다. 기존 모델 파라미터 의미는 `편향 항(bias term)`으로 분리해 `P1-5.1`에 연결했다. 또한 `안전성(safety)`, `투명성(transparency)` 항목을 추가해 `P1-15.1`의 핵심 윤리 개념이 개념사전에서 직접 이어지도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `bias`와 `bias term`, `safety`, `transparency` 표제어를 구분하고 `P1-15.1` 및 `P1-5.1` 대표 연결을 같은 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-15/section-01.md`, `docs/parts/part-01/chapter-05/section-01.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-51

- 변경 이유: 개념사전이 `중심 Section`만 기록하면 대표 설명 위치는 보이지만 실제 재등장 위치를 추적하기 어려워, `등장 Section` 필드를 도입하고 예시 항목에도 바로 적용할 필요가 생겼다.
- 문서 반영: 개념사전 머리말에 `중심 Section`과 `등장 Section`의 역할 차이를 명시했다. `로그(log)` 항목에 `P1-14.3`, `P1-15.3`을, `검색(search)` 항목에 `P1-13.4`, `P1-14.2`, `P2-3.2`, `P2-9.1`, `P2-9.3`을 `등장 Section`으로 추가해 실제 추적 예시를 남겼다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `중심 Section`과 `등장 Section` 필드의 역할 구분, 그리고 `log`, `search` 항목의 재등장 Section 추적을 같은 `Section ID` 기준으로 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/reference/concept-glossary.md`, `management/authoring/concept-glossary-guidelines.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-52

- 변경 이유: `등장 Section` 규칙을 도입했지만 실제 개념사전 본문에는 예시 항목만 채워져 있어, Part 1 Chapter 13~14에서 이미 순차 검토가 끝난 핵심 서비스 개념들의 재등장 위치를 바로 추적할 필요가 생겼다.
- 문서 반영: `임베딩`, `유사도 검색`, `검색 증강 생성`, `도구 사용`, `도구 호출`, `에이전트`, `목표`, `상태`, `관찰`, `종료 조건`, `MCP`, `리소스`, `하네스`, `추적`, `평가`, `그레이더`, `재현성`, `레이트 리밋`, `재시도`, `지연 시간`, `처리량` 등 Part 1 Chapter 13~14 핵심 표제어에 `등장 Section`을 추가해 대표 설명 위치와 후속 재사용 위치를 구분해 읽을 수 있게 했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 서비스 구조 관련 표제어들의 `등장 Section`도 본문 순차 보강 범위와 함께 같은 `Section ID` 기준으로 확장해야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-13/section-01.md`, `docs/parts/part-01/chapter-13/section-02.md`, `docs/parts/part-01/chapter-13/section-03.md`, `docs/parts/part-01/chapter-14/section-02.md`, `docs/parts/part-01/chapter-14/section-03.md`, `docs/parts/part-01/chapter-14/section-04.md`, `docs/parts/part-01/chapter-14/section-05.md`, `docs/parts/part-01/chapter-14/section-06.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-53

- 변경 이유: Part 1 Chapter 17의 순차 정리를 마치면서 `전망`, `지표`, `예측`, `해석` 같은 후반부 읽기 기준 개념이 여러 절에 걸쳐 다시 쓰였지만, 개념사전에는 대표 설명 위치만 남아 있어 재등장 경로를 바로 찾기 어려웠다.
- 문서 반영: `미래 전망(forecast)`, `예측(prediction)`, `해석(interpretation)`, `지표(indicator)` 항목에 Chapter 17 내부의 `등장 Section`을 추가해 `17.1 -> 17.2 -> 17.3` 사이의 재사용 흐름을 개념사전에서 다시 따라갈 수 있게 했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 Chapter 17의 읽기 기준 개념들도 대표 설명 위치와 재등장 위치를 같은 `Section ID` 기준으로 함께 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-01/chapter-17/section-01.md`, `docs/parts/part-01/chapter-17/section-02.md`, `docs/parts/part-01/chapter-17/section-03.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-54

- 변경 이유: Part 2와 Part 3를 순차 보강한 뒤에도 `변수`, `함수`, `벡터`, `행렬`, `데이터셋`, `평가` 같은 핵심 표제어는 대표 설명 위치만 남아 있어, 후속 절에서 실제로 어떤 역할로 다시 쓰이는지 개념사전에서 곧바로 추적하기 어려웠다.
- 문서 반영: `변수(variable)`에 `P2-8.5`, `P2-10.3`을, `함수(function)`에 `P2-4.3`, `P2-6.2`, `P2-8.5`, `P2-10.3`을, `벡터(vector)`에 `P2-4.3`, `P2-11.3`을, `행렬(matrix)`에 `P2-11.3`을, `데이터셋(dataset)`에 `P3-3.2`, `P3-9.2`를, `평가(evaluation)`에 `P3-3.2`, `P3-8.2`, `P3-9.2`를 `등장 Section`으로 추가했다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 Part 2의 계산·실습 핵심 개념과 Part 3의 데이터 모델링 개념도 대표 설명 위치와 재등장 위치를 같은 `Section ID` 기준으로 함께 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-02/chapter-04/section-03.md`, `docs/parts/part-02/chapter-06/section-02.md`, `docs/parts/part-02/chapter-08/section-05.md`, `docs/parts/part-02/chapter-10/section-03.md`, `docs/parts/part-02/chapter-11/section-03.md`, `docs/parts/part-03/chapter-03/section-02.md`, `docs/parts/part-03/chapter-08/section-02.md`, `docs/parts/part-03/chapter-09/section-02.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-55

- 변경 이유: Part 3 리팩터링 이후 `데이터 모델링`, `샘플`, `기준선`, `비교 리포트`처럼 문제 설계 층위에서 새로 중심이 된 개념과, `데이터셋`, `특징`, `타깃`, `표현`, `검토`, `행`처럼 Part 3에서 다시 강하게 재정렬된 표제어를 개념사전에 반영할 필요가 생겼다.
- 문서 반영: `기준선(baseline)`, `데이터 모델링(data modeling)`, `비교 리포트(comparison report)`, `샘플(sample)` 항목을 추가했다. `데이터셋(dataset)`, `특징(feature)`, `타깃(target)`, `표현(representation)`, `검토(review)`, `행(row)` 항목에는 Part 3의 새 `중심 Section`과 `등장 Section`을 보강해 대표 설명 위치가 기존 기초 개념 층위와 데이터 모델링 층위를 함께 드러내도록 맞췄다.
- 번역 동기화 메모: 향후 다국어 개념사전이 생기면 `data modeling`, `sample`, `baseline`, `comparison report`의 표제어 선택과 Part 3의 확장된 `중심/등장 Section` 연결을 같은 `Section ID` 기준으로 함께 맞춰야 한다.
- 번역 반영 상태: 번역본 없음
- 관련 자산: `docs/parts/part-03/chapter-01/section-01.md`, `docs/parts/part-03/chapter-01/section-02.md`, `docs/parts/part-03/chapter-04/section-01.md`, `docs/parts/part-03/chapter-06/section-01.md`, `docs/parts/part-03/chapter-06/section-02.md`, `docs/parts/part-03/chapter-07/section-01.md`, `docs/parts/part-03/chapter-09/section-02.md`, `docs/parts/part-03/chapter-10/section-01.md`
- 원문 기준 버전: `v2026.07.06`
