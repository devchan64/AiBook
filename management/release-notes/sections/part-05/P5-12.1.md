# Section Release Note

- Section ID: `P5-12.1`
- Source File: `docs/parts/part-05/chapter-12/section-01.md`

### v2026.07.09
- 본문 반영: `지금 읽는 층위`, 장간 비교 표, `지금 이 절에서 읽는 것` 표를 앞쪽 범위 블록에서 사례 직전 회고 블록으로 이동했다. `사례로 보기`와 `실행 가능한 Python 예제로 상태 직관 보기` 표지도 각각 `사례 및 예시`, `연습 및 예제`로 조정했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: 상태 전달 Mermaid를 `assets/part-05/chapter-12/rnn-state-flow-ko.mmd` include 방식으로 전환했다.
- 번역 동기화 메모: Keep the English Mermaid source pair aligned with the Korean public include target. / pending

### v2026.07.12
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: 순차 상태, 문맥 누적, LSTM/GRU 필요성, 장기 의존성 장으로 이어지는 기준을 `체크리스트` 하나로 재구성했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.

### v2026.07.12
- 본문 반영: 사례 구간에 `사람이 먼저 보기 쉬운 기준 vs 순차 상태 관점` 비교 표를 추가했다. `연습 및 예제`에는 실행 전 예측 표를 넣고, 문장 예제에 `not_good` 케이스를 추가해 마지막 단어는 같아도 앞의 부정 흐름 때문에 최종 라벨이 갈라지는 장면을 직접 확인하게 만들었다. 출력 해설도 `같은 마지막 입력인데 왜 다른 결론이 나오는가`를 중심으로 다시 정리했다.
- 번역 동기화 메모: Korean source now reinforces the RNN introduction with a human-vs-sequential-view table, a prediction table, and a same-last-token/different-state comparison case. 향후 번역 시 same comparison pressure를 유지해야 한다.

### v2026.07.12
- 본문 반영: 문장 예제를 `shutdown_confirmed`, `leak_confirmed`, `restart_confirmed` 운영 메모로 교체하고, 같은 마지막 `확인` 문구라도 앞의 조치 단서에 따라 최종 판단이 달라지는 장면으로 재구성했다. 해설과 예측 표도 같은 운영 메모 기준으로 다시 정리했다.
- 번역 동기화 메모: Korean source now uses operational memo phrases instead of generic sentiment phrases for the same-last-token/state-comparison example. 향후 번역 시 the same operational contrast를 유지해야 한다.

### v2026.07.12
- 본문 반영: 문장, 음성, 시계열 사례를 `사람이 먼저 보기 쉬운 결과`와 `순차 상태 관점에서 실제로 구분해야 할 것`으로 다시 묶는 비교표를 추가했다. 예제 뒤에는 `마지막 입력`과 `누적 상태`를 분리해 읽는 해설 표를 덧붙여, 같은 마지막 단어/값 뒤에서도 상태 차이 때문에 최종 판단이 갈린다는 점을 다시 고정했다.
- 번역 동기화 메모: Korean source now ties the RNN introduction cases back to one cross-case frame, adds a post-output interpretation table separating last-input reading from accumulated-state reading, and closes each case with an explicit result sentence about preserving prior cues or trends in the current decision. 향후 번역 시 같은 cross-case and post-output comparison structure를 유지해야 한다.
- 본문 반영: 사례 1을 `누유는 확인됐지만 재가동은 승인하지 않았다` 같은 운영 메모 해석 장면으로 바꾸고, 사례 2를 설비 경보음 인식 장면으로 바꿨다. 이어서 사례 요약 문장과 비교표도 `운영 메모 해석`, `설비 경보음 인식` 기준으로 갱신해, 순차 상태의 필요가 운영 판단 흐름 안에서 직접 읽히게 맞췄다.

### v2026.07.13
- 본문 반영: `gradual_rise`와 `temporary_spike`가 모두 80으로 끝나도 누적 상태가 다르게 남는 장면을 비교하는 SVG를 `연습 및 예제` 도입부에 추가했다. 본문 설명도 `마지막 값이 같다`와 `누적 상태가 같다`를 분리해서 읽도록 보강했다.
- 번역 동기화 메모: Added a paired `-en` / `-ko` SVG that contrasts two sequences with the same final value but different accumulated states. Future translations should preserve the same comparison between raw sequence values and carried state.

### v2026.07.13-python-chart
- 번역 동기화 메모: Future translations should preserve the shared Python-generated source for the RNN state-contrast chart and localize only the derived labels.

### v2026.07.13-layout
- 번역 동기화 메모: The paired RNN state-contrast charts now keep only labels that are required to read the two sequence panels and the threshold line. Future translations should preserve this chart/body role split.

### v2026.07.14-layout
- 본문 반영: P5-12.1 연습 및 예제 도입부에서 결합 비교 차트 1개 대신 `rnn-gradual-rise-state`, `rnn-temporary-spike-state` 개별 SVG 2개를 순서대로 배치했다.
- 추가 반영: 본문 예제와 그래프 데이터가 어긋나지 않도록 `temporary_spike` 시퀀스를 본문 코드와 같은 `[80, 60, 60, 80]`으로 맞추고, 각 그래프에 센서 입력값과 누적 상태선을 함께 표시하도록 바꿨다. 그래프 내부 제목은 본문 캡션과 역할이 겹치므로 제거했다.
- 번역 동기화 메모: Future translations should preserve the split two-chart layout for the RNN state example and keep deriving both charts from the shared Python chart source.

### v2026.07.17
- 본문 반영: `사례 및 예시`를 운영 메모·경보음·시계열 3개 사례의 긴 나열에서, 운영 메모 대표 사례와 도메인 확장 비교표 구조로 다시 묶어 현재 절의 중심 질문이 `같은 마지막 입력도 누적 상태가 다르면 왜 다른 결론이 나는가`에 더 오래 머물도록 정리했다.
- 본문 반영: 사례 뒤에서 같은 판단을 반복하던 중복 비교표 1개를 걷어내고, 공통 비교표와 `사람이 먼저 보기 쉬운 기준 vs 순차 상태 관점` 표만 남겨 사례 요약 밀도를 정리했다.
- 본문 반영: `이 절의 범위`에서 게이트 수식, BPTT, attention 이후 정밀 비교 같은 생략 목록을 걷어내고, 이번 절이 `순차 상태를 왜 들고 가야 하는가`와 `기본 RNN만으로는 왜 오래 기억하기 어려웠는가`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 장기 의존성 handoff는 `P5-12.2`, attention 구조 handoff는 `P5-13.1`로 직접 연결되게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P5-12.1 closes the need for carried state first and hands long-term-dependency detail to P5-12.2.

### v2026.07.18
- 본문 반영: 앞부분의 `이 절을 읽는 순서`, 장 위치 설명 표, 장간 handoff 중심 메타 안내를 걷어내고, `왜 순차 데이터에서 누적 상태가 필요한가 -> RNN은 무엇을 도입했는가 -> 왜 basic RNN만으로는 오래 유지하기 어려웠는가` 흐름이 앞에서 바로 보이도록 재배치했다.
- 본문 반영: 순차 데이터와 feed-forward 한계를 설명하는 문단을 더 풀어 써서, 마지막 입력 하나와 누적 상태 해석이 왜 갈리는지 사례 전에 먼저 납득할 수 있게 보강했다.
- 본문 반영: 말미의 역사·다음 장 예고 중심 요약을 줄이고, 현재 절 결론을 `RNN은 상태 전달 발상`, `LSTM/GRU는 상태 유지 보강`으로 다시 닫았다.
- 번역 동기화 메모: Future translations should preserve the reduced meta-structure, the expanded explanation of sequence order vs carried state, and the tighter close that ends on RNN/LSTM/GRU rather than attention preview.

## 2026-07-18

- 영문 번역 반영: `docs/parts/part-05/chapter-12/section-01.en.md`를 추가해 순차 데이터의 순서성, RNN 상태 전달 발상, LSTM/GRU의 기억 보강 이유, 운영 메모·시계열 예제를 영어판 본문으로 동기화했다.
- 번역 자산 반영: 본문에서 `rnn-state-flow-en.mmd`, `rnn-gradual-rise-state-en.svg`, `rnn-temporary-spike-state-en.svg`를 참조하도록 맞췄다.
- 번역 동기화 메모: English section now matches the Korean v2026.07.18 structure and closes on RNN/LSTM/GRU state-management intuition rather than attention preview.
