# Section Release Note

- Section ID: `P5-12.2`
- Source File: `docs/parts/part-05/chapter-12/section-02.md`

### v2026.07.14
- 본문 반영: 예제 출력 뒤에 `state_support` 감소 그래프와 상태 기반 판정/direct reference 판정 비교 그래프를 각각 분리 PNG로 추가했다. 그래프는 본문 Python 예제의 같은 규칙 문장, gap 목록, decay 값, 판정 기준을 그대로 재계산한다.
- 추가 반영: 예제 해설의 기존 비교 코드 표현을 `간단한 비교 코드`로 정리했다.
- 번역 동기화 메모: 향후 번역 시 장기 의존성 예제의 `gap -> state_support -> state/direct decision` 산출물 순서를 유지하고, 언어별 PNG 자산을 함께 참조해야 한다.

### v2026.07.07-1
- 본문 반영: 범위와 읽기 순서 앞의 소개 문장을 제거하고 목록이 직접 이어지도록 조정했다.
- 번역 동기화 메모: wording cleanup only. / translation impact 없음

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 상태 희석 직관 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: `그런데 왜 attention이 다시 등장했나` 절에 seq2seq 병목 설명과 비교 표를 추가했다.
- 추가 반영: 장기 의존성 흐름 Mermaid와 상태 전달 대비 direct reference Mermaid를 각각 외부 자산 include 방식으로 전환했다.
- 번역 동기화 메모: Preserve the seq2seq bottleneck and direct-reference transition in translation, and keep the reusable Mermaid assets aligned. / pending

### v2026.07.12
- 본문 반영: 장기 의존성 정의, 상태 약화, LSTM/GRU의 보완, attention으로 넘어가는 발상 전환을 `체크리스트` 하나로 통합했다. `사례 및 예시`에는 `사람이 먼저 보기 쉬운 기준 / 순차 상태 관점 / 직접 참조 관점` 비교 표와 최종 결과 문장을 추가했고, `연습 및 예제`에는 `state_support`, `state_decision`, `direct_match_score`, `direct_decision`을 gap 길이 전에 먼저 예상하게 하는 표와 운영 판단 해설을 보강했다.
- 번역 동기화 메모: Korean source now closes the long-term dependency section with a human-vs-state-vs-direct-reference comparison structure and spreads the examples across document reading, long spoken instructions, and early-signal time series. Future translations should keep the sequence-type contrast instead of reverting example 2 to another text-only long-distance scene.

### v2026.07.12-asset
- 번역 동기화 메모: Chapter 12 asset-operation wording now follows the current per-language reference rule while keeping English Mermaid files as canonical originals. Future translation work should preserve the same asset split and synchronization order.

### v2026.07.12
- 본문 반영: 예제 변수 이름을 `restart_block_rule`, `restart_question`, `instruction_document` 기준으로 정리하고, 출력 해설도 `같은 재기동 금지 규칙과 같은 질문` 기준으로 다시 맞췄다.
- 번역 동기화 메모: The Korean long-term-dependency exercise now keeps operation-specific variable naming through the code and output explanation instead of generic rule/question/document labels. Future translations should preserve this aligned naming.

### v2026.07.17
- 본문 반영: `사례 및 예시`를 긴 문서·긴 음성·시계열 3개 사례의 긴 나열에서, 긴 작업 지시 대표 사례와 도메인 확장 비교표 구조로 다시 묶어 현재 절의 중심 질문이 `먼 단서를 왜 현재 시점에 다시 참고해야 하는가`에 더 오래 머물도록 정리했다.
- 본문 반영: `이 절의 범위`에서 vanishing gradient 수식, BPTT 상세 유도, attention 구현 세부 같은 생략 목록을 걷어내고, 이번 절이 `먼 단서를 현재 판단까지 안정적으로 들고 오기 어렵다`는 한계와 `직접 참조 구조가 왜 다음 질문이 되는가`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: attention handoff는 `P5-13.1`, Transformer 확장 handoff는 `P5-14.1`로 직접 연결되게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P5-12.2 closes the long-term-dependency limit first and hands direct-reference structure to P5-13.1.

### v2026.07.18
- 본문 반영: 앞부분의 `이 절을 읽는 순서`, 장 위치 설명 표, attention/seq2seq 상세 전개를 걷어내고, `장기 의존성이 무엇인가 -> 왜 basic RNN에서 오래전 단서가 흐려지는가 -> 왜 이것이 현재 판단을 흔드는가 -> LSTM/GRU는 무엇을 보강했는가` 흐름이 먼저 보이도록 재배치했다.
- 본문 반영: 장기 의존성을 단순 성능 저하가 아니라 `오래전 단서가 빠지면 현재 판단 자체가 흔들리는 문제`로 더 풀어 설명하고, 메모판 비유와 가까운 단서/먼 단서 구분 문단을 보강했다.
- 본문 반영: attention은 현재 절의 본문 축에서 물리고, `상태 보존만으로는 먼 단서를 끝까지 안정적으로 들고 오기 어렵다`는 결론 뒤의 짧은 다음 장 질문 수준으로만 남겼다.
- 번역 동기화 메모: Future translations should preserve the stronger focus on long-term dependency itself, the shorter attention handoff, and the expanded prose that distinguishes near cues from distant cues before the example.

## 2026-07-18

- 영문 번역 반영: `docs/parts/part-05/chapter-12/section-02.en.md`를 추가해 장기 의존성 정의, basic RNN의 상태 희석 한계, LSTM/GRU의 보완 방향, state 기반 판단과 direct reference 비교 예제를 영어판 본문으로 동기화했다.
- 번역 자산 반영: 본문에서 `long-term-dependency-flow-en.mmd`, `long-dependency-state-support-en.png`, `long-dependency-decision-comparison-en.png`를 참조하도록 맞췄다.
- 번역 동기화 메모: English section now matches the Korean v2026.07.18 structure and keeps the handoff from long-term dependency limits to direct-reference thinking before attention.
