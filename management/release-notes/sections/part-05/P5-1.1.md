# Section Release Note

- Section ID: `P5-1.1`
- Source File: `docs/parts/part-05/chapter-01/section-01.md`

### v2026.07.20
- 저작권 검토: Part 5 Module 1 순차 검토 범위에서 퍼셉트론의 계산 문법, 스팸 판정, 라인 재기동 승인 사례를 확인했다. 본문 사례와 표는 학습 목적에 맞춘 자체 작성 설명으로 판단했다.
- 출처 사용 점검: Rosenblatt(1958), McCulloch & Pitts(1943), Goodfellow et al., LeCun·Bengio·Hinton(2015)은 개념·역사적 배경 확인 용도로만 연결되어 있으며, 원문 표·도식·코드·문단을 재사용한 정황은 찾지 못했다.
- 관련 자산: `perceptron-flow-ko.mmd`, `restart-approval-case-flow-ko.mmd`는 본문 사례 흐름을 요약한 자체 Mermaid 자산으로 판단했다.
- 본문 반영: 없음. 기존 원문 버전 `v2026.07.19` 유지.
- 번역 동기화 메모: 영어·중국어 번역본을 v2026.07.19로 맞추고, Section 메타데이터 형식과 Rosenblatt(1958), McCulloch & Pitts(1943) 출처 URL·확인일을 원문과 동기화함.

### v2026.07.17
- 본문 반영: `깊게 다루지 않습니다` 목록을 제거하고, 이번 절은 퍼셉트론의 가장 작은 계산 문법을 먼저 붙잡고 선형 결합·XOR·다층 구조 질문은 뒤 절로 넘기는 handoff 구조가 먼저 보이도록 범위 안내를 정리했다.
- 번역 동기화 메모: `docs/parts/part-05/chapter-01/section-01.en.md`에 같은 handoff 중심 범위 안내와 사례·체크리스트 구조를 반영했다.
- 번역 동기화 메모: `docs/parts/part-05/chapter-01/section-01.zh.md`에 같은 범위 안내, 사례 흐름, 비코드 연습 구조를 반영했다. Chapter 1 공개 본문용 `perceptron-flow-zh.mmd`, `restart-approval-case-flow-zh.mmd`도 함께 추가했다.
- 번역 반영 상태: 영문 번역 반영 완료 / 중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.17`

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 순전파 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: 퍼셉트론 계산 흐름 Mermaid를 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: The perceptron Mermaid now keeps paired `-en` and `-ko` source files for translation reuse. / pending

### v2026.07.12
- 본문 반영: 퍼셉트론의 기본 계산 문법, 가중치·편향 역할, 적용 장면, 다음 절로 넘길 범위를 `체크리스트` 하나로 흡수했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.
- 본문 반영: `스팸 메일 1차 판정` 사례에 입력값·가중치·기여도 표와 `사람 기준 vs 퍼셉트론 기준` 비교 표를 추가했다. `연습 및 예제`에는 실행 전 예측 표를 넣어 `위험 신호 하나가 weighted sum을 어떻게 뒤집는가`를 먼저 예상하게 만들었다.

### v2026.07.12-asset
- 번역 동기화 메모: Chapter 1 asset-operation wording now follows the current per-language reference rule while keeping English Mermaid files as canonical originals. Future translation work should preserve the same asset split and synchronization order.

### v2026.07.13
- 본문 반영: 목표 문구에서 Python 실행 예고를 걷어내고, `라인 재기동 승인` 사례의 입력·가중치·해석을 한 표로 먼저 묶어 읽기 흐름을 단순화했다. `연습 및 예제`에서는 Python 코드와 출력 블록을 제거하고, 세 장면 비교표와 짧은 해설로 `어느 입력이 경계를 넘기거나 끌어내렸는가`를 읽게 바꾸었다.
- 번역 동기화 메모: 한국어 원고에서 code-based example을 table-based non-code exercise로 교체했고, case 1 summary table도 추가했다. 향후 번역 시 동일한 non-code exercise structure와 case summary table을 유지해야 한다.
