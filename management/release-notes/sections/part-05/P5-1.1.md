# Section Release Note

- Section ID: `P5-1.1`
- Source File: `docs/parts/part-05/chapter-01/section-01.md`

### v2026.07.09
- 변경 이유: Part 5 문단구조 정리 기준에 맞춰 사례/예제 구조 표지를 현재 원칙으로 통일할 필요가 있었다.
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 순전파 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.11
- 변경 이유: Part 5 Mermaid 도식을 외부 자산 파일로 분리해도 한국어 본문과 향후 번역용 영문 원본을 함께 유지할 수 있는지 `P5-1.1`에서 먼저 확인할 필요가 있었다.
- 본문 반영: 퍼셉트론 계산 흐름 Mermaid를 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: The perceptron Mermaid now keeps paired `-en` and `-ko` source files for translation reuse. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-01/perceptron-flow-en.mmd`, `docs/assets/part-05/chapter-01/perceptron-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.12
- 변경 이유: 말미 자기 확인 블록이 `이 절에서 기억할 관점`, `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`, `체크리스트`로 중복 분산되어 있어 기준서에 맞춘 통합이 필요했다.
- 본문 반영: 퍼셉트론의 기본 계산 문법, 가중치·편향 역할, 적용 장면, 다음 절로 넘길 범위를 `체크리스트` 하나로 흡수했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 학습 밀도 재점검 결과, 두 번째 사례와 Python 예제가 `직접 판단 장면`보다 설명 요약에 더 가까워 보여 사례-예제 역할 분리를 더 분명히 할 필요가 있었다.
- 본문 반영: `스팸 메일 1차 판정` 사례에 입력값·가중치·기여도 표와 `사람 기준 vs 퍼셉트론 기준` 비교 표를 추가했다. `연습 및 예제`에는 실행 전 예측 표를 넣어 `위험 신호 하나가 weighted sum을 어떻게 뒤집는가`를 먼저 예상하게 만들었다.
- 번역 동기화 메모: 한국어 원고 기준으로 사례 표와 예측 표가 추가되었다. 향후 번역 시 동일한 비교 구조와 수치 예측 흐름을 함께 반영해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 현재 강화 기준으로 다시 보니, `안전한 경우 vs 위험한 경우` 두 점만으로는 퍼셉트론이 `경계에서 어떻게 뒤집히는가`까지 충분히 드러나지 않았다.
- 본문 반영: `행사장 입장 승인` 사례에 `입력 하나만 바뀌었을 때 판단이 얼마나 흔들리는가` 비교 표와 `사람 기준 vs 퍼셉트론 기준` 표를 추가했다. `연습 및 예제`에는 `borderline_guest` 케이스를 더해 안전/경계/위험 세 장면을 함께 비교하도록 바꾸고, 출력 해설도 `경계에서 얼마나 멀어지는가`를 읽는 방식으로 보강했다.
- 번역 동기화 메모: 한국어 기준 원고에 경계 케이스 비교와 3케이스 예제가 추가되었다. 향후 번역 시 같은 경계 비교 구조와 예측 흐름을 함께 반영해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 추가 재검토 결과, 사례와 예제가 각각은 강해졌지만 `퍼셉트론이 결국 무엇을 판단 근거로 남기는가`를 절 전체 수준에서 다시 묶는 장면은 약했다.
- 본문 반영: 사례 뒤에 `사람이 먼저 보던 기준 vs 퍼셉트론이 실제로 남기는 판단 단서` 교차 비교표를 추가해 행사장/스팸 사례를 한 번에 다시 묶었다. 예제 출력 뒤에는 `결과만 볼 때 vs 가중합과 기여도까지 볼 때` 비교표를 덧붙여, 거절·승인 자체보다 어떤 입력이 경계를 넘기거나 끌어내렸는지를 더 직접적으로 읽게 만들었다.
- 번역 동기화 메모: Korean source now closes P5-1.1 with cross-case and post-output contribution-reading tables. 향후 번역 시 same cross-case structure and contribution-reading table should be preserved.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: Part 5 초반 기준 절인데도 사례와 예제에 행사장 입장, 스팸 메일 같은 일반 서비스 비유가 남아 있어, 뒤 절에서 강화한 운영 승인·경보 판단 흐름과 바로 이어지지 않았다.
- 본문 반영: 도입부의 단순 예시를 `라인 재기동 승인 판단`으로 교체했다. 사례 1은 `라인 재기동 승인`, 사례 2는 `경보 조치 우선순위 1차 판정`으로 바꾸고, 입력 항목·가중치·기여도 표도 같은 운영 신호 기준으로 다시 정리했다. Python 예제의 케이스 이름과 입력 변수도 `ready_restart`, `borderline_restart`, `blocked_restart` 계열로 교체해 승인 신호 부족과 강한 차단 신호를 구분해 읽게 했다.
- 번역 동기화 메모: Korean source now frames P5-1.1 around restart approval and alarm-priority scoring instead of event-entry and spam examples. Future translations should preserve the operational framing and the renamed restart cases.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12-asset
- 변경 이유: chapter 1 자산 README가 아직 예전 `한국어 본문이 -ko.mmd를 include하고 영문은 향후 번역용 원본으로 둔다`는 표현에 머물러 있어, 현재 차트 가이드라인의 `배포 언어별 본문이 자기 언어 자산을 직접 참조` 원칙을 충분히 드러내지 못했다.
- 자산 운영 반영: `docs/assets/part-05/chapter-01/README.md`에 한국어/영어 공개 본문이 각자 `-ko.mmd`, `-en.mmd` 자산을 직접 참조하고, 영문 `-en.mmd`를 기준 원본으로 먼저 수정한 뒤 한국어 파생본을 같은 conceptual flow와 node relationships로 동기화한다는 운영 원칙을 명시했다.
- 번역 동기화 메모: Chapter 1 asset-operation wording now follows the current per-language reference rule while keeping English Mermaid files as canonical originals. Future translation work should preserve the same asset split and synchronization order.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-01/README.md`
- 원문 기준 버전: `v2026.07.12`
