# Section Release Note

- Section ID: `P5-3.1`
- Source File: `docs/parts/part-05/chapter-03/section-01.md`

### v2026.07.17
- 본문 반영: `깊게 다루지 않습니다` 목록을 제거하고, 이번 절은 활성화 함수가 왜 선형 계산만 반복되는 구조를 실제 표현력으로 바꾸는지 먼저 닫고 함수 비교·손실 연결·gradient 흐름은 뒤 절로 넘기는 구조로 범위 안내를 정리했다.
- 번역 동기화 메모: `docs/parts/part-05/chapter-03/section-01.en.md`에 같은 handoff 중심 범위 안내와 공통 Mermaid, 비교표, 비코드 연습 구조를 반영했다.
- 번역 동기화 메모: `docs/parts/part-05/chapter-03/section-01.zh.md`에 같은 범위 안내, 공통 activation-survival 도식, 비코드 연습 구조를 반영했다. Chapter 3 공개 본문용 `node-activation-flow-zh.mmd`, `stacked-linear-layers-zh.mmd`, `linear-activation-linear-zh.mmd`, `activation-signal-survival-flow-zh.mmd`와 `nonlinear-activation-example-zh.svg`, `activation-case-before-after-zh.svg`, `activation-experiment-shift-zh.svg` 자산도 함께 동기화했다.
- 번역 반영 상태: 영문 번역 반영 완료 / 중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.17`

### v2026.07.16
- 본문 반영: 연습 표 앞에 해당 값들이 앞의 교육용 구간 함수 재현이 아니라 문턱 아래/위 신호 생존을 보는 단순 관찰용 규칙임을 명시했다. 사례 1과 사례 2의 개별 Mermaid 참조는 제거하고, 두 사례의 공통 신호 생존 흐름 Mermaid만 남겨 도식 역할을 압축했다.
- 번역 동기화 메모: Korean source now keeps only the shared activation-survival Mermaid in P5-3.1 and clarifies that the exercise values are a simplified threshold-passing observation, not a recomputation of the earlier educational piecewise function. The exercise now includes explicit inputs, weights, bias, calculations, small value-change experiments, and two Python-generated before/after SVG charts. Future translations should preserve this distinction and the experimentable structure.

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 활성화 전후 값 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: 선형 층 반복 도식과 활성화 포함 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: Activation-introduction Mermaid diagrams now keep paired `-en` and `-ko` source files for translation reuse. / pending

### v2026.07.12
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: 비선형성 필요, 선형 반복의 한계, 내부 표현 형성, 함수 비교로 넘어가는 기준을 `체크리스트` 하나로 흡수했다.
- 번역 동기화 메모: Korean source now uses an operational alarm/recovery framing throughout P5-3.1 and closes with explicit weak/borderline/strong comparisons plus post-output interpretation tables. 향후 번역 시 same operational framing, four-case exercise structure, and comparison tables should be preserved.

### v2026.07.13
- 본문 반영: Python 코드와 실행 출력 블록을 제거하고, 같은 네 운영 상태를 `정리된 관찰값 -> 오독 방지 -> 경계 이동 질문` 순서로 다시 구성했다. 절 목표 문구도 코드 실행이 아니라 `어떤 신호가 끊기고 살아남는지 설명할 수 있는가`로 맞췄다.
- 본문 반영: 사례 1, 사례 2, 두 사례의 공통 해설에서 설명용 표 일부를 절차형 문장으로 바꾸어 `1단계 선형 점수`, `2단계 활성화`, `3단계 다음 층 해석` 흐름이 더 직접 읽히게 정리했다. 연습 구간의 비교 표는 유지해 적용용 비교 역할만 남겼다.
- 번역 동기화 메모: Korean source now closes P5-3.1 without a Python block and instead uses a non-code comparison/exploration structure focused on threshold shifts and signal survival. Future translations should preserve that learning-flow emphasis.

### v2026.07.13-python-chart
- 번역 동기화 메모: The Korean and English nonlinear activation example chart is now generated from a shared Python source. Future translations should preserve the script-driven structure rather than editing each SVG manually.
