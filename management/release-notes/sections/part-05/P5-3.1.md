# Section Release Note

- Section ID: `P5-3.1`
- Source File: `docs/parts/part-05/chapter-03/section-01.md`

### v2026.07.07-1
- 변경 이유: 도입부 메타 진행 문구를 줄여 활성화 함수 설명으로 바로 들어가게 정리함.
- 본문 변경: `이 절은 다음 질문에 답합니다.` 문장을 제거하고 범위 목록이 직접 이어지게 조정함.
- 번역본 메모: 번역본 영향 없음.

### v2026.07.07-2
- 변경 이유: 핵심 개념 설명 뒤에 개념사전 복귀 경로가 빠져 있어 같은 Part 안 복습 동선이 약했음.
- 본문 변경: 활성화 함수 정의 바로 뒤에 개념사전 복귀 문장을 추가함.
- 번역본 메모: 번역본 영향 없음.

### v2026.07.09
- 변경 이유: Part 5 문단구조 정리 기준에 맞춰 사례/예제 구조 표지를 현재 원칙으로 통일할 필요가 있었다.
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 활성화 전후 값 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.11
- 변경 이유: Part 5 Mermaid 도식을 `docs/assets` 중심으로 정리하는 기준을 활성화 함수 도입 절에도 적용할 필요가 있었다.
- 본문 반영: 선형 층 반복 도식과 활성화 포함 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: Activation-introduction Mermaid diagrams now keep paired `-en` and `-ko` source files for translation reuse. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-03/stacked-linear-layers-en.mmd`, `docs/assets/part-05/chapter-03/stacked-linear-layers-ko.mmd`, `docs/assets/part-05/chapter-03/linear-activation-linear-en.mmd`, `docs/assets/part-05/chapter-03/linear-activation-linear-ko.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.12
- 변경 이유: 활성화 함수 도입 절의 말미 자기 확인 블록이 여러 표지로 흩어져 있어 기준서의 `체크리스트` 단일화 원칙과 맞지 않았다.
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: 비선형성 필요, 선형 반복의 한계, 내부 표현 형성, 함수 비교로 넘어가는 기준을 `체크리스트` 하나로 흡수했다.
- 본문 반영: 표지 정리와 연결 표지 제거에 더해, 사례 2를 `최근 경보 수`, `압력 복귀 지연 시간`, `재기동 실패 횟수`를 함께 읽는 운영 표 데이터 장면으로 바꾸었다. 이어서 `선형 점수만 봄 vs 활성화 뒤 값까지 봄`, `사람 기준 vs 활성화 포함 기준`, `이미지/운영 사례 교차 비교`, `세 가지 설비 상태 비교` 표를 추가해 `잘림`, `경계 바로 위 약한 양수`, `강한 경고`를 한 절 안에서 직접 비교하게 했다. `연습 및 예제`는 실행 전 예측 표와 네 케이스(`stable_with_minor_noise`, `recovery_dominant_signal`, `borderline_warning_signal`, `strong_warning_signal`) 비교 구조로 확장했고, 출력 뒤에는 `다음 층 입장에서 읽히는 상태`, `같은 양수면 다 같은 경고라고 읽는 오해`를 깨는 운영 해석 비교표를 덧붙였다.
- 번역 동기화 메모: Korean source now uses an operational alarm/recovery framing throughout P5-3.1 and closes with explicit weak/borderline/strong comparisons plus post-output interpretation tables. 향후 번역 시 same operational framing, four-case exercise structure, and comparison tables should be preserved.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 추가 재점검 결과, 사례 1에 병/컵 이미지 같은 일반 시각 비유가 남아 있어 Chapter 3 도입부의 운영 신호 해석 기준선이 끝까지 고정되지는 않았다.
- 본문 반영: 사례 1을 `설비 시각 신호 분류` 장면으로 교체하고, 병목이 있는 병/손잡이 컵 비교 대신 `경광등이 켜진 밸브`, `압력계 바늘이 위험 구간에 들어간 패널`, `정상 배관 구간`처럼 운영 시각 단서 조합으로 다시 묶었다. 교차 비교표의 핵심 신호도 `경광등 + 계기판 바늘` 같은 조합으로 바꾸어, 활성화 함수가 어떤 시각 조합을 다음 층에 더 강하게 남기는지 더 직접 읽게 했다.
- 번역 동기화 메모: Korean source now frames the first activation example around operational visual signals instead of bottle/cup objects. Future translations should preserve the equipment-camera framing and the revised visual-combination examples.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

- 변경 이유: 추가 검토 결과, 사례 1이 활성화 함수가 `왜 필요한가`는 비슷하게 보여 주지만 `활성화 함수가 무엇을 하는 규칙인가`는 직접 드러내지 못했다.
- 본문 반영: 사례 1에 `z = w_1 x_1 + w_2 x_2 + w_3 x_3 + b`, `a = f(z)` 연결을 직접 추가하고, 활성화 함수를 `중간 노드가 만든 선형 점수 z를 다음 층이 읽을 값 a로 바꾸는 규칙`으로 다시 적었다. 이어서 `선형 점수 z만 볼 때`와 `활성화 뒤 값 a까지 볼 때`를 비교하는 표를 넣어, 설비 시각 신호 사례 안에서도 활성화 함수의 역할이 직접 보이게 정리했다.
- 번역 동기화 메모: Korean source now makes the first visual-signal example explicitly show `linear score z -> activation -> next-layer value a`. Future translations should preserve this explicit bridge instead of leaving the case at the level of abstract necessity.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

- 변경 이유: 추가 검토 결과, 사례 2도 사례 1과 비슷하게 활성화 함수의 `필요성`은 보이지만 `무엇을 하는 규칙인가`는 직접 드러나지 않았다.
- 본문 반영: 사례 2에도 `z = w_1 x_1 + w_2 x_2 + w_3 x_3 + b`, `a = f(z)` 연결을 직접 추가하고, 운영 표 데이터에서 활성화 함수를 `합쳐진 위험 점수가 다음 층에서 무시될지, 약하게 남을지, 강하게 살아남을지 정하는 규칙`으로 다시 적었다. 이어서 세 가지 설비 상태를 `선형 점수 z만 볼 때`와 `활성화 뒤 값 a까지 볼 때`로 비교하는 표를 사례 앞단에 추가해, 운영 사례 안에서도 활성화 함수의 역할이 직접 보이게 정리했다.
- 번역 동기화 메모: Korean source now makes the second operational-table example also show `linear score z -> activation -> next-layer value a` explicitly. Future translations should preserve this direct bridge instead of leaving the case at the level of abstract necessity.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

- 변경 이유: 사례 1과 사례 2가 각각 표로는 충분히 설명되지만, 두 사례를 묶는 공통 판단 흐름이 한 번에 압축되어 보이지는 않았다.
- 본문 반영: 사례 2 뒤에 공통 Mermaid 도식 1개를 추가해 `약한 조합 신호 -> 거의 잘림`, `경계 조합 신호 -> 약한 양수로 겨우 남음`, `강한 조합 신호 -> 다음 층에 강하게 전달됨` 흐름을 한 번에 요약했다. 사례별 표를 다시 그림으로 반복하지 않고, 두 사례가 공통으로 보여 주는 `z -> activation -> next layer` 해석만 압축하도록 위치와 설명을 정리했다.
- 번역 동기화 메모: Added a shared activation-survival Mermaid pair so the Korean manuscript now summarizes both cases with one compact flow rather than separate per-case diagrams. Future translations should keep the same three-stage weak/borderline/strong structure.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-03/activation-signal-survival-flow-en.mmd`, `docs/assets/part-05/chapter-03/activation-signal-survival-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.12`

- 변경 이유: 연습 예제가 `x1`, `x2` 중심의 장난감 입력에 머물러 있어, 바로 앞 운영 사례와 판단 흐름이 완전히 닫히지 않았다.
- 본문 반영: `연습 및 예제`를 `최근 경보 수`, `압력 복귀 지연 시간`, `재기동 실패 횟수` 축으로 다시 맞추고, `brief_alarm_recovered`, `recovery_dominant_signal`, `borderline_warning_signal`, `strong_warning_signal` 네 상태를 비교하도록 바꾸었다. 출력 해석 표와 오독 비교표도 같은 운영 상태 이름에 맞춰 갱신해 사례와 예제가 같은 신호 축을 공유하게 정리했다.
- 번역 동기화 메모: The Korean exercise now reuses the same operational alarm/recovery signals as the preceding case instead of abstract `x1`, `x2` toy inputs. Future translations should preserve the aligned feature names and four-state comparison structure.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

- 변경 이유: 공통 흐름도만으로는 사례 1과 사례 2 각각의 판단 순서를 바로 되짚기 어려워, 사례별 해석 밀도를 조금 더 올릴 필요가 있었다.
- 본문 반영: 사례 1 아래에 `시각 단서 조합 -> 선형 점수 z -> 활성화 -> 다음 층 해석` Mermaid를, 사례 2 아래에 `운영 신호 조합 -> 선형 점수 z -> 활성화 -> 위험 상태 표현` Mermaid를 추가했다. 기존 공통 흐름도는 유지하고, 사례별 도식은 각 사례의 판단 순서만 짧게 압축하도록 분리했다.
- 번역 동기화 메모: Added paired case-specific Mermaid assets for the visual-signal example and the tabular-signal example. Future translations should preserve the same short case flow and keep the shared summary Mermaid separate.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-03/activation-visual-signal-case-flow-en.mmd`, `docs/assets/part-05/chapter-03/activation-visual-signal-case-flow-ko.mmd`, `docs/assets/part-05/chapter-03/activation-tabular-signal-case-flow-en.mmd`, `docs/assets/part-05/chapter-03/activation-tabular-signal-case-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.12`

- 변경 이유: 사례별 도식이 판단 순서를 보여 주기는 했지만, 본문 표가 강조하는 `약함`, `경계`, `강함` 비교축을 직접 시각화하지는 못했다.
- 본문 반영: 사례 1과 사례 2의 Mermaid를 모두 `약한 조합/상태 -> z 낮음 -> 거의 잘림`, `경계 조합/상태 -> 기준 바로 위 -> 작은 양수 유지`, `강한 조합/상태 -> z 높음 -> 큰 값 유지`의 3구간 비교형으로 재구성했다. 본문 설명도 각 사례 도식이 `z -> activation -> next layer` 흐름만이 아니라 `약함/경계/강함` 차이를 함께 다시 읽게 한다고 조정했다.
- 번역 동기화 메모: The case-specific Mermaid pairs now visualize weak, borderline, and strong states directly instead of only showing a single generic flow. Future translations should preserve the same three-band comparison structure in both diagrams.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-03/activation-visual-signal-case-flow-en.mmd`, `docs/assets/part-05/chapter-03/activation-visual-signal-case-flow-ko.mmd`, `docs/assets/part-05/chapter-03/activation-tabular-signal-case-flow-en.mmd`, `docs/assets/part-05/chapter-03/activation-tabular-signal-case-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.12`

### v2026.07.13
- 변경 이유: 챕터 3 재검토 결과, `연습 및 예제`의 Python 블록이 바로 위 사례와 표가 이미 닫은 판단을 다시 출력하는 검증형 예제로 남아 있어 학습 이해를 늘리지 못했다.
- 본문 반영: Python 코드와 실행 출력 블록을 제거하고, 같은 네 운영 상태를 `정리된 관찰값 -> 오독 방지 -> 경계 이동 질문` 순서로 다시 구성했다. 절 목표 문구도 코드 실행이 아니라 `어떤 신호가 끊기고 살아남는지 설명할 수 있는가`로 맞췄다.
- 변경 이유: 사례 해설의 비교 표가 많아지면서 `약함 -> 경계 -> 강함`, `선형 점수 -> 활성화 -> 다음 층 해석` 순서를 문장으로 따라가기가 오히려 어려워졌다.
- 본문 반영: 사례 1, 사례 2, 두 사례의 공통 해설에서 설명용 표 일부를 절차형 문장으로 바꾸어 `1단계 선형 점수`, `2단계 활성화`, `3단계 다음 층 해석` 흐름이 더 직접 읽히게 정리했다. 연습 구간의 비교 표는 유지해 적용용 비교 역할만 남겼다.
- 번역 동기화 메모: Korean source now closes P5-3.1 without a Python block and instead uses a non-code comparison/exploration structure focused on threshold shifts and signal survival. Future translations should preserve that learning-flow emphasis.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.13`
- 변경 이유 추가: 차트·표 형식 선택 가이드라인 재검토 결과, 연습 결과 뒤의 해석 표들이 값 비교보다 설명 반복에 가까워 읽기 흐름을 나누고 있었다.
- 본문 반영 추가: 활성화 결과 수치표와 후속 조작 표는 유지하고, `선형 점수만 볼 때 vs 활성화 뒤`, `결과만 빨리 볼 때 vs 더 나은 해석` 표는 문단 설명으로 바꿨다.
- 변경 이유 추가: `P5-3.1`은 활성화 함수 일반론 절인데 연습 구간이 특정 대표 함수 관찰처럼 닫혀, 다음 절 `P5-3.2`의 대표 함수 비교와 역할이 섞였다.
- 본문 반영 추가: 특정 대표 함수 이름과 기준 아래/위 절단 중심 표현을 걷어내고, `z`가 활성화 뒤 값 `a`로 바뀌며 다음 층에 남는 신호가 달라진다는 일반 관찰 중심으로 연습 설명을 조정했다.
- 변경 이유 추가: 도입부가 활성화 함수의 위치와 입출력보다 비선형성의 필요성을 먼저 설명해, 독자가 활성화 함수가 무엇인지 인지하기 어려웠다.
- 본문 반영 추가: 도입부에 활성화 함수를 `가중합 점수 z를 다음 층에 넘길 값 a로 바꾸는 함수`로 먼저 정의하고, 한 노드 안의 `입력값 -> z -> f -> a` 흐름을 추가했다. 뒤쪽의 `a = f(z)` 설명은 반복 정의가 아니라 식을 다시 읽는 설명으로 조정했다.
- 변경 이유 추가: `입력값 -> z -> f -> a` 흐름은 순서와 위치 관계가 핵심이라 문장만으로는 빠르게 고정되기 어렵다.
- 본문 반영 추가: 도입 정의 바로 아래에 한 노드 안의 활성화 위치를 보여 주는 Mermaid 흐름도를 추가했다.
- 관련 자산 추가: `docs/assets/part-05/chapter-03/node-activation-flow-en.mmd`, `docs/assets/part-05/chapter-03/node-activation-flow-ko.mmd`
- 변경 이유 추가: `점수에 비선형 변환을 주는 규칙`이라는 목표 문구가 있었지만, 함수식과 그래프가 없어 비선형 변환의 실제 의미가 추상적으로 남았다.
- 본문 반영 추가: 대표 함수 비교로 넘어가지 않는 교육용 \(f(z)\) 구간별 예시와 SVG 그래프를 추가해, `a = z` 선형 통과와 `a = f(z)` 비선형 변환의 차이를 시각적으로 설명했다.
- 관련 자산 추가: `docs/assets/part-05/chapter-03/nonlinear-activation-example-en.svg`, `docs/assets/part-05/chapter-03/nonlinear-activation-example-ko.svg`

### v2026.07.13-python-chart
- 변경 이유: Chapter 3의 좌표형 활성화 그래프들은 수작업 SVG보다 Python 생성형이 축, 곡선, 언어별 라벨을 더 일관되게 유지하기에 적합했다.
- 자산 반영: `nonlinear-activation-example` SVG를 Chapter 3 공통 Python 생성 스크립트에서 만들도록 정리하고, 생성 원본 `p5_3_activation_charts.py`를 추가했다.
- 번역 동기화 메모: The Korean and English nonlinear activation example chart is now generated from a shared Python source. Future translations should preserve the script-driven structure rather than editing each SVG manually.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-03/p5_3_activation_charts.py`, `docs/assets/part-05/chapter-03/nonlinear-activation-example-en.svg`, `docs/assets/part-05/chapter-03/nonlinear-activation-example-ko.svg`, `docs/assets/part-05/chapter-03/README.md`
- 원문 기준 버전: `v2026.07.13`
