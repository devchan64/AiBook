# Section Release Note

- Section ID: `P5-3.6`
- Source File: `docs/parts/part-05/chapter-03/section-06.md`

### v2026.07.07-1
- 변경 이유: 도입부 메타 문구 반복을 줄여 출력층 설명으로 바로 이어지게 정리함.
- 본문 변경: `이 절은 다음 질문에 답합니다.` 문장을 제거하고 범위 목록을 직접 연결함.
- 번역본 메모: 번역본 영향 없음.

### v2026.07.07-2
- 변경 이유: 출력층 활성화 설명 초반에 개념사전 복귀 경로가 빠져 있었음.
- 본문 변경: 출력층과 활성화의 기준선을 다시 잡을 수 있도록 개념사전 연결 문장을 추가함.
- 번역본 메모: 번역본 영향 없음.

### v2026.07.09
- 변경 이유: Part 5 문단구조 정리 기준에 맞춰 사례/예제 구조 표지를 현재 원칙으로 통일할 필요가 있었다.
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 출력 해석 차이 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.11
- 변경 이유: Part 5 Mermaid 도식을 `docs/assets` 중심으로 정리하는 기준을 출력층 활성화 절에도 적용할 필요가 있었다.
- 본문 반영: softmax 출력 흐름 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: The softmax-output Mermaid now keeps paired `-en` and `-ko` source files for translation reuse. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-03/softmax-output-flow-en.mmd`, `docs/assets/part-05/chapter-03/softmax-output-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.12
- 변경 이유: 출력층 활성화 절의 말미 구조가 `기억할 관점`, `짧은 점검`, `언제...`, `체크리스트`로 분산되어 있었다.
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: 은닉층·출력층 활성화 구분, 문제 유형별 출력 해석, 손실과의 결합, 점수와 운영 정책 분리 기준을 `체크리스트` 하나로 통합했다.
- 본문 반영: 말미 구조를 `체크리스트` 하나로 통합하고 메타형 연결 표지를 제거했다. 동시에 문제 유형 개요와 사례 축을 에너지 사용량 예측, 경보 차단 필터, 품질 상태 분류로 통일했다. 사례 본문에는 `사람 기준 vs 출력층 해석`, `숫자 크기 vs 정답과의 거리`, `sigmoid 점수 vs threshold 정책`, `1등 후보 이름 vs 후보 간 점수 차이` 비교표를 추가했고, 예제에는 실행 전 예측 표를 넣었다. 출력 뒤에는 `운영 판단 기준`, `문제 유형별 덜 나쁜 오독 vs 더 위험한 오독` 비교표를 덧붙여 회귀는 `정답과의 거리`, 이진 분류는 `threshold와 중간 분기`, 다중 분류는 `후보 간 점수 차이`를 먼저 읽게 다시 묶었다.
- 관련 자산 반영: 한국어 본문이 include하는 `softmax-output-flow-ko.mmd`의 클래스 후보 라벨을 `정상, 스크래치, 오염`으로 바꾸고, 대응 `-en.mmd`도 `normal, scratch, contamination` 기준으로 맞춰 본문 사례 축과 자산 의미 구조가 어긋나지 않게 정리했다.
- 번역 동기화 메모: Korean source now uses a unified operational case family and closes P5-3.3 with output-interpretation, policy-threshold, and misread-comparison tables for regression, binary, and multiclass outputs. 향후 번역 시 same operational framing and comparison structure should be preserved.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-03/softmax-output-flow-en.mmd`, `docs/assets/part-05/chapter-03/softmax-output-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.12`

### v2026.07.13
- 변경 이유: 출력층 절의 개념사전 복귀 링크가 존재하지 않는 `#output-layer` 앵커를 가리켜 빌드 정보성 경고를 만들고 있었다.
- 본문 반영: 개념사전 복귀 링크를 현재 실제 표제어인 `출력(output)` 앵커로 조정했다.
- 번역 동기화 메모: Updated the glossary fallback link to a valid existing anchor. Future translations should point to real glossary headings rather than inferred anchor names.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.13`
- 변경 이유: 챕터 3 재검토 결과, 출력층 절의 Python 예제도 문제 유형별 해석 단위를 더 선명하게 만들기보다 이미 본 결과를 다시 출력하는 검증형 예제로 남아 있었다.
- 본문 반영: Python 코드와 실행 출력 블록을 제거하고, 회귀/이진 분류/다중 분류 결과를 `정리된 결과 -> 읽어야 할 단위 -> 오독 방지 -> 값이 달라질 때 다시 볼 질문` 구조로 재정리했다. 절 목표 문구도 코드 실행 대신 마지막 숫자의 해석 단위를 구분하는 목표로 바꾸었다.
- 번역 동기화 메모: Korean source now closes P5-3.3 with a non-code output-interpretation exercise rather than a Python snippet. Future translations should preserve the same read-the-final-number structure.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.13`
- 변경 이유 추가: 차트·표 형식 선택 가이드라인 재검토 결과, 출력층 사례와 연습 후반의 해석 표들이 값 비교보다 같은 판단 기준을 반복 설명하는 역할에 가까웠다.
- 본문 반영 추가: 회귀·이진 분류·다중 분류의 출력값 비교표와 문제 유형별 예측 표는 유지하고, 정책 분리·출력 해석·오판 위험 표는 문단 설명으로 바꿔 읽기 흐름을 정리했다.
- 변경 이유 추가: 대표 활성화 함수 비교를 P5-3.2부터 P5-3.5까지 네 Section으로 확장하면서 기존 출력층 Section ID가 충돌하게 되었다.
- 본문 반영 추가: 출력층 Section을 `P5-3.3`에서 `P5-3.6`으로 재번호화하고, 도입부의 앞선 Section 참조를 `P5-3.2부터 P5-3.5까지`로 조정했다.
- 번역 동기화 메모 추가: Output-layer activation section moved from P5-3.3 to P5-3.6 because the representative activation functions now occupy P5-3.2 through P5-3.5.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산 추가: 없음
- 원문 기준 버전: `v2026.07.13`
- 변경 이유 추가: 말미의 `언제 출력층 해석을 먼저 분리해서 보는가` 표는 문제 유형에서 출력 해석, 손실 함수 연결로 이어지는 절차를 비교표로 눌러 담아 읽기 순서가 약했다.
- 본문 반영 추가: 해당 표를 절차형 문단과 Mermaid 흐름도로 바꾸어 `예측 대상 확인 -> 출력 의미 결정 -> 손실 연결` 순서가 바로 보이게 정리했다.
- 관련 자산 추가: `docs/assets/part-05/chapter-03/output-activation-decision-flow-en.mmd`, `docs/assets/part-05/chapter-03/output-activation-decision-flow-ko.mmd`, `docs/assets/part-05/chapter-03/README.md`
