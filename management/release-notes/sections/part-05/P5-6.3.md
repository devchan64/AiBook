# Section Release Note

- Section ID: `P5-6.3`
- Source File: `docs/parts/part-05/chapter-06/section-03.md`

### v2026.07.14
- 변경 이유: P5-6.3의 Python 예제는 `raw_outputs`, `raw_variance`, `normalized_outputs`를 출력하지만, 기존 그래프는 예제 출력값 자체와 직접 연결되지 않아 단계별 산출물을 따라 읽기 어려웠다. 이후 추가 검토에서 실제 큰 스케일 누적 실험까지 P5-6.3 안에 넣으면 `개념 정리 Section`과 `실험 Section`의 역할이 섞인다는 판단이 생겼다.
- 본문 반영: P5-6.3은 초기화, 수치 안정성, batch normalization을 한 축으로 묶어 읽는 개념 지도 역할로 정리했다. 긴 Python 예제와 그래프 해설은 제거하고, 실제 큰 초기화 스케일이 깊은 층에서 값을 키우는 장면은 새 `P5-6.4`에서 확인하도록 연결했다.
- 추가 검토: 예제 출력값과 직접 연결되지 않는 기존 `batch-normalization-scale` SVG 그래프는 분할 PNG 그래프와 역할이 겹쳐 본문과 자산 목록에서 제거했다. 이후 P5-6.3 전용 분할 PNG도 P5-6.4의 새 누적 실험 그래프로 대체되어 삭제했다.
- 추가 표현 정리: 예제의 역할을 `축약 안정화 비교`가 아니라 `단계별 안정화 비교`로 설명했던 내용을 다시 조정해, P5-6.3은 단계별 실험보다 안정화 개념 배치에 집중하게 했다.
- 번역 동기화 메모: Korean source now keeps P5-6.3 as the stabilization concept map and moves the concrete Python/charts experiment to P5-6.4. Future translations should preserve that section boundary.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-06/stabilization-bridge-flow-en.mmd`, `docs/assets/part-05/chapter-06/stabilization-bridge-flow-ko.mmd`
- 제거 자산: `docs/assets/part-05/chapter-06/p5_6_batch_norm_chart.py`, `docs/assets/part-05/chapter-06/batch-normalization-scale-en.svg`, `docs/assets/part-05/chapter-06/batch-normalization-scale-ko.svg`, `docs/assets/part-05/chapter-06/p5_6_3_initialization_bn_charts.py`, `docs/assets/part-05/chapter-06/initialization-raw-output-scale-en.png`, `docs/assets/part-05/chapter-06/initialization-raw-output-scale-ko.png`, `docs/assets/part-05/chapter-06/initialization-raw-variance-en.png`, `docs/assets/part-05/chapter-06/initialization-raw-variance-ko.png`, `docs/assets/part-05/chapter-06/batchnorm-normalized-output-scale-en.png`, `docs/assets/part-05/chapter-06/batchnorm-normalized-output-scale-ko.png`
- 원문 기준 버전: `v2026.07.14`

### v2026.07.15
- 변경 이유: 사례 재검토 결과, 사례 3이 batch normalization의 분포 안정화보다 mode 차이 재설명에 기울어 있었고, 사례 2도 초기화 스케일과 수치 안정성 연결을 더 직접적으로 닫지 못하고 있었다.
- 본문 반영: 사례 1은 `같은 gradient를 받을 가능성` 표현을 `대칭이 깨지지 않아 같은 경로로 묶이기 쉽다`는 구조 문제로 더 직접적으로 고쳤다. 사례 2는 큰 초기화 스케일이 반복 계산을 거치며 값 범위를 밀어 올리는 흐름을 보강했다. 사례 3은 mode 재설명보다 `중간 활성값 분포를 다루기 쉬운 범위로 정리하는 장치`라는 batch normalization의 첫 역할이 먼저 보이도록 다시 썼다.
- 번역 동기화 메모: Korean source now keeps the P5-6.3 cases centered on symmetry breaking, scale propagation, and activation-distribution stabilization rather than drifting back into a mode-only explanation. Future translations should preserve that stabilization-first framing.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.15`

### v2026.07.16
- 변경 이유: 추가 사례 검토 결과, 세 사례가 각각은 맞는 방향이었지만 `깊은 학습 안정화`라는 공통축으로 다시 묶이는 압력이 약해 학습 지지력이 충분하지 않았다.
- 본문 반영: 사례 3을 `배치마다 활성값 범위가 크게 흔들리는 장면`으로 더 구체화해 batch normalization의 분포 안정화 역할을 더 직접적으로 보이게 했다. 사례들 뒤에는 `출발점 -> 반복 계산 범위 -> 층 사이 분포`의 세 단계 종합 표를 추가해 initialization, numerical stability, batch normalization이 하나의 안정화 축을 어떻게 나눠 맡는지 다시 압축했다.
- 본문 반영 추가: 각 사례 끝에 `이 사례가 실제로 지지하는 질문`과 `이 절에서 남겨야 할 판단`을 직접 적어, 사례 설명이 단순 장면 소개에서 멈추지 않고 초기화, 수치 안정성, batch normalization의 역할로 바로 회수되게 다듬었다.
- 본문 반영 추가: 사례 1의 `답답함`이 추상 표현으로 남지 않도록, 여러 뉴런을 두고도 초반에는 `비슷한 계산을 반복하는 한 통로`처럼 움직여 폭이 살아나지 않는다는 해설을 덧붙였다.
- 본문 반영 추가: 사례 1에 `x = 2`, `0 -> 0.3 -> 0.6`처럼 함께 움직이는 최소 수치 장면을 넣어, 같은 초기화가 왜 뉴런들을 복제 통로처럼 묶는지 더 직접적으로 보이게 했다.
- 본문 반영 추가: 절 도입의 `깊은 네트워크가 실제로 덜 흔들리게 된 이유`라는 기준이 사례 1과 바로 이어지도록, 사례 도입을 `출발점 -> 반복 계산 범위 -> 중간 활성값 분포`의 흔들림 시작 위치로 다시 묶고 사례 1이 `출발점 문제`를 보여 준다는 문장을 더했다.
- 본문 반영 추가: 항목별 사례 3개가 다시 개념을 분해해 설명하는 구조 한계를 보여, 이를 `깊은 네트워크가 처음부터 흔들리는 장면`과 `덜 흔들리게 만드는 장면`의 2사례 구조로 대체했다. 이제 각 사례 안에서 initialization, numerical stability, batch normalization이 한 장면 안에서 함께 필요해지는 흐름을 바로 읽도록 정리했다.
- 본문 반영 추가: 언어별 6개 자산으로 세분화했던 그래프는 사례 흐름을 지나치게 끊는다고 판단해, 다시 `뉴런 경로`, `층별 활성값 범위`, `배치별 중간 활성값 범위`의 3개 비교 자산으로 정리했다.
- 번역 동기화 메모: Korean source now closes the P5-6.3 cases with a stronger shared stabilization axis and a concrete batch-scale scene. Future translations should preserve that start-range-distribution grouping.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-06/p5_6_3_stabilization_case_charts.py`, `docs/assets/part-05/chapter-06/stabilization-neuron-paths-en.png`, `docs/assets/part-05/chapter-06/stabilization-neuron-paths-ko.png`, `docs/assets/part-05/chapter-06/stabilization-layer-range-en.png`, `docs/assets/part-05/chapter-06/stabilization-layer-range-ko.png`, `docs/assets/part-05/chapter-06/stabilization-batch-spread-en.png`, `docs/assets/part-05/chapter-06/stabilization-batch-spread-ko.png`, `docs/assets/part-05/chapter-06/README.md`
- 원문 기준 버전: `v2026.07.16`

### v2026.07.11

- 변경 이유: 외부 딥러닝 입문 커리큘럼과 비교했을 때, Part 5에서 `초기화(initialization)`, `수치 안정성(numerical stability)`, `batch normalization`을 하나의 학습 안정화 축으로 묶어 주는 초심자용 회수 위치가 약했다.
- 본문 반영: Chapter 6 뒤에 새 보충학습 `P5-6.3`을 추가해 `왜 깊은 네트워크가 그냥 쌓는다고 바로 잘 학습되지 않는가`라는 질문 아래 초기화, 수치 안정성, batch normalization의 역할 차이와 연결을 한 자리에서 정리했다.
- 추가 반영: 학습 안정화 축 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: A new supplemental section was added, and the stabilization-bridge Mermaid now keeps paired `-en` and `-ko` source files for translation reuse. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-06/stabilization-bridge-flow-en.mmd`, `docs/assets/part-05/chapter-06/stabilization-bridge-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.12
- 변경 이유: 보충학습 절의 말미에도 `이 절에서 기억할 관점`과 `체크리스트`가 역할 중복을 만들고 있어 기준서에 맞춘 통합이 필요했다.
- 본문 반영: 깊은 네트워크 안정화 축, 초기화, 수치 안정성, batch normalization, optimizer·regularization과의 구분을 기존 `체크리스트`에 흡수했다.
- 본문 반영 추가: 예제 마무리의 `최소 장난감 예제` 표현을 당시에는 `축약 안정화 비교`로 바꿨으나, 이후 `v2026.07.14`에서 `단계별 안정화 비교`로 다시 정리했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: 학습 안정화 보충학습은 핵심 개념 묶음은 있었지만, 사례 구간의 `사람 기준 -> 안정화 관점` 직접 대비와 코드 실행 전 예측 장면이 약해 설명 비중이 큰 편이었다.
- 본문 반영: `사례 및 예시`에 `사람이 먼저 보기 쉬운 기준 / 초기화·수치 안정성·batch normalization 관점` 비교 표와 최종 결과 문장을 추가했다. `연습 및 예제`에는 `small_init_outputs`, `large_init_outputs`, `batch_mean`, `normalized_large_outputs`를 먼저 예상하게 하는 표를 추가했다.
- 번역 동기화 메모: Korean source now closes the stabilization supplement with a human-vs-stabilization comparison table and a pre-run prediction table for output-scale vs normalization effects. 향후 번역 시 same comparison structure를 유지해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12-2
- 변경 이유: 보충학습 예제가 출력 범위 차이는 보여 주지만, 그 숫자를 `출발 스케일 문제`와 `분포 재정리 문제`로 어떻게 나눠 읽어야 하는지 절 끝에서 다시 묶는 장면은 약했다.
- 본문 반영: `small_init_outputs`, `large_init_outputs`, `normalized_large_outputs`를 두고 `출력에서 먼저 보이는 것 / 그대로 두면 남기 쉬운 해석 / 안정화 관점에서 다시 읽는 해석` 비교표를 추가했다.
- 번역 동기화 메모: Korean source now closes P5-6.3 with a post-output stabilization reading table that separates initialization-scale effects from batch-normalization effects. Future translations should preserve the same post-output comparison structure.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.13
- 변경 이유: 보충학습 절의 개념사전 복귀 링크가 실제 MkDocs slug와 다른 앵커 문자열을 사용해 빌드 정보성 경고를 만들고 있었다.
- 본문 반영: `학습 모드`, `배치 정규화`, `초기화`, `수치 안정성` 링크를 각각 `#training-mode`, `#batch-normalization`, `#initialization`, `#numerical-stability`로 바로잡았다.
- 번역 동기화 메모: Glossary links now point to the actual generated anchors for the referenced concepts. Future translations should reuse verified glossary ids rather than inferred Korean-English mixed strings.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.13`
- 변경 이유 추가: ReLU 설명이 기존 `P5-3.2` 통합 비교 절에서 `P5-3.4` 독립 Section으로 이동해, 학습 안정화 보충학습의 참조가 예전 위치를 가리키게 되었다.
- 본문 반영 추가: ReLU 계열 참조를 `P5-3.2`에서 `P5-3.4`로 조정하고, 본문 `Version`을 `v2026.07.13`으로 갱신했다.
- 번역 동기화 메모 추가: Future translations should point ReLU-related stabilization references to P5-3.4.
- 변경 이유 추가: P5 전체 그래프 검토 결과, batch normalization 설명은 활성값 분포의 중심과 퍼짐이 핵심인데 기존 Mermaid와 표만으로는 `큰 값을 없애는 것`과 `분포를 다시 정리하는 것`의 차이가 약하게 남았다.
- 본문 반영 추가: batch normalization 설명 아래에 작은 초기화, 큰 초기화, batch normalization 뒤 활성값 위치를 한 축에서 비교하는 SVG 그래프를 추가했다.
- 번역 동기화 메모 추가: Korean source now uses a localized SVG chart for output-scale and batch-normalization intuition. Future translations should reference the matching English SVG or localized language asset.
- 관련 자산 추가: `docs/assets/part-05/chapter-06/batch-normalization-scale-en.svg`, `docs/assets/part-05/chapter-06/batch-normalization-scale-ko.svg`, `docs/assets/part-05/chapter-06/README.md`

### v2026.07.13
- 변경 이유: `P5-6.3`의 Python 예제는 초기화 스케일과 batch normalization 차이를 보여 주긴 했지만, 한 번의 `small vs large` 비교에 머물러 독자가 스케일을 바꾸며 분포 이동을 실험하는 역할은 약했다.
- 본문 반영: 예제 앞에 `weight_cases`, `inputs`, `eps`를 바꿔 볼 실험 표를 추가했다. 코드도 `small_init`, `medium_init`, `large_init` 세 경우를 연속 비교하도록 바꾸고, 각 case마다 `raw_mean`, `raw_variance`, `normalized_outputs`를 함께 출력하게 조정했다. 후속 해설 표도 `분포 퍼짐`과 `분포 재정리`를 단계적으로 읽도록 다시 정리했다.
- 번역 동기화 메모: Korean source now uses a multi-case scale experiment in P5-6.3 instead of a single small-vs-large comparison. Future translations should preserve the tunable scale cases and the raw-vs-normalized distribution reading.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.13`

### v2026.07.13-python-chart
- 변경 이유: batch normalization 전후 스케일 비교도 좌표형 자산이라 수동 SVG보다 Python 생성형으로 관리하는 편이 값 범위 조정과 언어별 재생성에 적합했다.
- 자산 반영: `batch-normalization-scale-en.svg`, `batch-normalization-scale-ko.svg`를 `p5_6_batch_norm_chart.py`에서 생성하도록 전환했다.
- 운영 메모: Chapter 6 README에 Python 그래프 원본을 기록했다.
- 번역 동기화 메모: Future translations should preserve the shared Python-generated source for the batch-normalization scale chart and localize only the derived labels.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-06/p5_6_batch_norm_chart.py`, `docs/assets/part-05/chapter-06/batch-normalization-scale-en.svg`, `docs/assets/part-05/chapter-06/batch-normalization-scale-ko.svg`, `docs/assets/part-05/chapter-06/README.md`
- 원문 기준 버전: `v2026.07.13`

### v2026.07.13-layout
- 변경 이유: 후속 자산 검토에서 batch normalization 스케일 그래프 내부에 제목과 설명문이 남아 있어, 본문과 차트의 역할이 겹쳤다.
- 자산 반영: `p5_6_batch_norm_chart.py`에서 내부 제목과 부제목을 제거하고, 축과 분포 라벨만 남기도록 정리한 뒤 영문·한글 SVG를 다시 생성했다.
- 번역 동기화 메모: The paired batch-normalization scale charts now keep only labels that are required to read the plotted distributions. Future translations should preserve this chart/body role split.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-06/p5_6_batch_norm_chart.py`, `docs/assets/part-05/chapter-06/batch-normalization-scale-en.svg`, `docs/assets/part-05/chapter-06/batch-normalization-scale-ko.svg`
- 원문 기준 버전: `v2026.07.13`
