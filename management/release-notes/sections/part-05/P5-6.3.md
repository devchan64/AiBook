# Section Release Note

- Section ID: `P5-6.3`
- Source File: `docs/parts/part-05/chapter-06/section-03.md`

### v2026.07.16
- 변경 이유: 학습 루프와 learning/inference 구분을 앞에 두면서, 학습 모드와 평가 모드 Section을 P5-6.3으로 이동했다.
- 본문 반영: Section ID를 `P5-6.3`으로 바꾸고, 도입부의 이전 절 연결을 P5-6.2 기준으로 수정했다.
- 관련 자산: mode 비교 그래프 생성 스크립트를 `p5_6_3_mode_comparison_charts.py`로 이름 변경했다.
- 번역 동기화 메모: Korean source moved training/evaluation mode from P5-6.2 to P5-6.3. Future translations should preserve the new Section ID and script name.

### v2026.07.16
- 본문 반영: `학습 중 정확도와 검증 정확도`, `실서비스 배포`, `작은 배치 평가`를 각각 긴 사례 문단과 개별 오해 표로 두던 구성을 제거했다. `연습 및 예제` 도입에 검증, 배포, 작은 배치 평가에서 mode 확인이 필요하다는 연결 문장만 남기고, 실제 학습은 Python 예제와 그래프의 단계별 산출물 확인이 맡도록 정리했다. 예제 해설도 집필 판단형 문장이 아니라 독자가 확인할 문제 장면 중심 문장으로 조정했다.
- 번역 동기화 메모: Korean source now removes the separate case section and lets the Python example and charts carry the learning role. Future translations should preserve the direct transition from mode explanation to the worked example.

### v2026.07.14
- 본문 반영: Python 예제를 `모드별 계산 규칙 비교 실험`으로 교체했다. 이후 숫자 목록을 바로 넣는 방식이 인위적으로 보이지 않도록, 작은 사용자 세션 배치에서 은닉층 활성값을 먼저 계산한 뒤 training mode에서는 dropout mask와 현재 batch mean 기준으로, evaluation mode에서는 dropout 제거와 이전 학습 배치에서 계산한 running mean 기준으로 처리하도록 보강했다.
- 추가 반영: 예제 출력 뒤에 은닉층 활성값 그래프와 mode별 centered output 비교 그래프를 먼저 추가해 코드 출력의 단계별 산출물을 직접 읽게 했다. 이어서 dropout 생존 비율 그래프와 batch normalization 기준 평균 그래프를 분리해 두고, 같은 계산 규칙을 더 긴 미니배치에 적용한 30회 forward pass 요약으로 training mode의 흔들림과 evaluation mode의 고정 기준선을 비교하게 했다.
- 번역 동기화 메모: Korean source now compares training/evaluation mode through a small session batch, hidden-layer activations, mode-specific centered outputs, repeated forward-pass dropout behavior, and batch-normalization reference selection. Future translations should preserve the separated step outputs and the chart asset sequence.

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 모드 차이 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: dropout 예제 아래에 평가를 학습처럼 다룰 때 무엇이 깨지는지 비교하는 `실패 장면` 표를 추가했다.
- 번역 동기화 메모: Failure-oriented follow-up table added for evaluation misuse cases. / pending

### v2026.07.11-2
- 본문 반영: training mode와 evaluation mode의 계산 흐름을 나란히 비교하는 Mermaid 도식을 추가하고, `업데이트 준비를 위한 흔들림 허용`과 `안정적 측정`의 차이를 한 문장으로 다시 묶었다.
- 번역 동기화 메모: Added paired Mermaid assets for the training-vs-evaluation mode flow and clarified the contrast between stochastic training behavior and stable evaluation behavior. / pending

### v2026.07.12
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: mode 차이, dropout·batch normalization 민감성, 평가 안정성, optimizer 장으로 이어지는 판단 기준을 `체크리스트` 하나로 재구성했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.

### v2026.07.17
- 본문 반영: `이 절의 범위`를 `무엇을 깊게 다루지 않는가` 중심이 아니라, 이번 절이 learning/inference 구분 다음에 `training mode와 evaluation mode가 왜 필요한가`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 본문 반영: optimizer와 regularization은 뒤 장으로 넘길 질문으로 분리하고, batch normalization 수식 유도·dropout 튜닝·분산 학습 모드 관리는 마지막에 현재 범위 밖 항목으로만 남겼다.
- 번역 동기화 메모: Future translations should preserve the scope handoff structure where this section closes mode differences after P5-6.2 and leaves optimizer/regularization detail to later sections.
