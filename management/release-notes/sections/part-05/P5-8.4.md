# Section Release Note

- Section ID: `P5-8.4`
- Source File: `docs/parts/part-05/chapter-08/section-04.md`

### v2026.07.20
- 본문 반영 내용: 한국어판 Python 예제 첫 줄에는 CSV activation 로그 기반 초기화 스케일, raw range·variance, batch normalization 범위 정리 비교 목적을, 영어·중국어 간체판 Python 예제 첫 줄에는 기존 초기화 스케일과 batch normalization 장난감 비교 목적을 설명하는 주석을 추가했다.
- 변경 이유: 기존 Python 예제는 Section 목적을 지지하지만 입력 활성값과 스케일 케이스가 코드 안에 고정되어 있어, 최근 Part 5 예제 기준처럼 데이터 자산과 실행 코드를 분리할 필요가 있었다. 또한 BN 뒤 범위 그래프의 y축이 미세 차이를 과하게 확대해 초심자에게 혼란을 줄 수 있었다.
- 본문 반영: `deep-scale-activation-log.csv`를 추가하고, Python 예제가 36행 CSV를 읽어 케이스별·층별 raw range, raw variance, after BN range를 계산하도록 바꿨다. Section 메타데이터 형식도 현재 기준에 맞춰 정리했다.
- 그래프 반영: `p5_8_4_deep_scale_charts.py`가 같은 CSV를 읽도록 바꾸고, BN range 그래프는 변화 추적용 선 그래프가 아니라 케이스별 점 비교로 바꿔 비슷한 범위로 정리된다는 점이 먼저 보이도록 재생성했다.
- 관련 자산: `docs/assets/part-05/chapter-08/deep-scale-activation-log.csv`, `docs/assets/part-05/chapter-08/p5_8_4_deep_scale_charts.py`, `docs/assets/part-05/chapter-08/deep-scale-raw-range-ko.png`, `docs/assets/part-05/chapter-08/deep-scale-raw-variance-ko.png`, `docs/assets/part-05/chapter-08/deep-scale-bn-range-ko.png`
- 번역 동기화 메모: 한국어 원문 기준 Python 예제가 CSV 기반으로 바뀌었고, 차트 생성 스크립트 재실행으로 영문·중문 PNG도 같은 CSV 로그 구조와 BN y축 기준에 맞춰 갱신되었다. 향후 번역 시 입력 파일 설명과 BN 그래프 해석 문장을 함께 동기화해야 한다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.20`

### v2026.07.20
- 저작권 검토: Part 5 Module 3 순차 검토 범위에서 큰 초기화 스케일이 깊은 층의 activation range와 variance를 흔드는 Python 예제, batch normalization 장난감 비교와 그래프를 확인했다. 예제 코드·표·그래프 해설은 자체 작성 자료로 판단했다.
- 출처 사용 점검: Dive into Deep Learning과 Goodfellow et al.은 초기화·수치 안정성·batch normalization 배경 확인 용도로만 연결되어 있으며, 원문 예제 코드·도식·표·문단 재사용 정황은 찾지 못했다.
- 관련 자산: `deep-scale-raw-range-ko.png`, `deep-scale-raw-variance-ko.png`, `deep-scale-bn-range-ko.png`는 로컬 생성 스크립트 기반 학습 보조 자산으로 판단했다.
- 본문 반영: 없음. 기존 원문 버전 `v2026.07.17` 유지.
- 번역 동기화 메모: 영어·중국어 간체 번역본은 위 v2026.07.20 CSV activation 로그 예제 갱신 항목에서 함께 최신화했다.

### v2026.07.17
- 본문 반영: 출력 예시 바로 아래에 `먼저 보이는 줄 -> 다시 물어야 할 질문 -> 붙잡아야 할 개념` 표를 추가해, `very_large_init`, `raw_variance`, `after_bn_range`를 초심자용 읽기 기준으로 다시 정리했다.
- 본문 반영: `Version`을 `v2026.07.17`로 갱신했다.
- 본문 반영: batch normalization 결과가 케이스마다 비슷하게 보이는 이유가 `스케일만 바꾸는 장난감 실험` 때문이라는 경계 문장을 앞뒤에 보강해, `batch normalization이 초기화 문제를 거의 지워 준다`는 오해를 줄이도록 정리했다.
- 본문 반영: 도입부와 범위를 다시 묶어, 이 절이 챕터 8의 마지막 단계에서 `계산 안정화 장치`를 숫자로 확인하는 자리라는 점이 더 직접 보이도록 정리했다.
- 본문 반영: Section 제목을 `깊은 층에서 값을 키우는가`보다 `계산 범위를 어떻게 흔드는가` 중심으로 바꿔, P5-8.3의 안정화 질문과 더 직접 연결되게 했다.
- 번역 동기화 메모: Future translations should keep the toy-experiment boundary explicit and preserve the chapter-8 framing where this section numerically closes the computation-stability stage.

### v2026.07.16
- 변경 이유: 큰 초기화 스케일 실험은 P5-8.3 안정화 보충학습의 실험 Section이므로, Chapter P5-8 아래로 이동했다.
- 본문 반영: Section ID를 `P5-8.4`로 바꾸고, 이전 절 연결을 P5-8.3 기준으로 수정했다. 학습 루프와 optimizer 설명으로 넘기는 참조도 P5-6.1, P5-7.1, P5-7.2 기준으로 고쳤다.
- 관련 자산: deep-scale PNG 그래프와 생성 스크립트를 `docs/assets/part-05/chapter-08/`로 옮기고, 스크립트 이름을 `p5_8_4_deep_scale_charts.py`로 변경했다.
- 번역 동기화 메모: Korean source moved the deep initialization-scale experiment from P5-6.4 to P5-8.4. Future translations should preserve the new Section ID and asset path.

### v2026.07.14
- 본문 반영: 새 `P5-6.4`를 추가해 이전 층 활성값이 3개 층을 지나는 작은 Python 실험을 제시했다. `small_init`, `medium_init`, `large_init`, `very_large_init`을 비교하며 raw activation 범위, raw variance, batch normalization 뒤 범위를 출력하게 했다.
- 번역 동기화 메모: Korean source adds P5-6.4 as the concrete deep-scale experiment following the P5-6.3 stabilization overview. Future translations should preserve the same example values and split-chart sequence.

### v2026.07.15
- 본문 반영: 스칼라 가중치를 `각 층 가중치 스케일을 단순화해 대표한 장난감 실험값`으로 설명하는 경계 문장을 추가했다. `weight_cases`, `layer` 반복 횟수, 입력 활성값, `eps`를 바꿔 볼 실험 표도 추가해 예제를 더 명시적인 실험형으로 정리했다.
- 번역 동기화 메모: Korean source now frames P5-6.4 more explicitly as a toy scale-propagation experiment with clear tweak-observe prompts, and avoids overstating batch-normalization or small-scale conclusions. Future translations should preserve that experiment boundary and caution level.

## 2026-07-18

### v2026.07.17
- 번역 동기화 메모: `docs/parts/part-05/chapter-08/section-04.en.md` was added and aligned to the current Korean source version, preserving the chapter-8 framing of this section as the numeric close of the computation-stability stage, the toy-experiment boundary around initialization scale propagation, and the paired English deep-scale charts.
- 번역 반영 상태: 영문 번역 반영 완료
- 관련 자산: `docs/assets/part-05/chapter-08/deep-scale-raw-range-en.png`, `docs/assets/part-05/chapter-08/deep-scale-raw-variance-en.png`, `docs/assets/part-05/chapter-08/deep-scale-bn-range-en.png`
- 원문 기준 버전: `v2026.07.17`
- 번역 동기화 메모: `docs/parts/part-05/chapter-08/section-04.zh.md` was added and aligned to the current Korean source version, preserving the chapter-8 framing of this section as the numeric close of the computation-stability stage, the toy-experiment boundary around initialization scale propagation, and the paired Chinese deep-scale charts.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 관련 자산: `docs/assets/part-05/chapter-08/deep-scale-raw-range-zh.png`, `docs/assets/part-05/chapter-08/deep-scale-raw-variance-zh.png`, `docs/assets/part-05/chapter-08/deep-scale-bn-range-zh.png`
