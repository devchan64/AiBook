# Section Release Note

- Section ID: `P5-13.1`
- Source File: `docs/parts/part-05/chapter-13/section-01.md`

### v2026.07.14
- 본문 반영: 예제 출력 뒤에 압력 해소 유지 시간 질문의 weight, 냉각수 유량 기준 질문의 weight, baseline/context 비교를 각각 분리 PNG 그래프로 추가했다. 그래프는 본문 Python 예제와 같은 후보 값, score table, softmax 계산을 재사용한다.
- 번역 동기화 메모: 향후 번역 시 `question -> weight distribution -> context comparison` 산출물 순서를 유지하고, 언어별 PNG 자산을 함께 참조해야 한다.

### v2026.07.07
- 본문 반영: 범위와 읽기 순서 앞의 소개 문장을 제거하고 설명 목록이 바로 이어지게 조정했다. 후반의 에이전트·RAG 문맥 관리 확장 구간도 제거하고, attention을 `요약 장치`로 오해하지 않도록 내부 계산 관점의 짧은 보강 설명으로 다시 정리했다.

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: `attention-focus-flow-ko.mmd`, `attention-direct-reference-bridge-ko.mmd` include 방식으로 전환하고, `오래 들고 가기`와 `필요할 때 다시 찾기`의 차이를 보여 주는 Mermaid 보강 도식을 추가했다.
- 번역 동기화 메모: Preserve the English Mermaid source files as the canonical diagram pair for future translation pages.

### v2026.07.12
- 본문 반영: attention 정의, 직접 참조 발상, 가중 평균 직관, self-attention 장으로 이어지는 기준을 `체크리스트` 하나로 재구성했다. 사례 구간에 `사람이 먼저 보기 쉬운 기준 vs attention 관점` 표를 추가했고, `연습 및 예제`에는 실행 전 예측 표를 넣고 질문을 `압력 해소 유지 시간은?`과 `냉각수 유량 기준은?` 두 개로 두어 같은 후보 집합에서도 질문이 달라지면 weight와 context가 달라지는 장면을 직접 비교하게 만들었다. 출력 해설도 `질문 변화 -> 참조 위치 변화`를 중심으로 다시 정리했다.
- 번역 동기화 메모: Korean source now frames P5-13.1 around operating-procedure transformation, incident-report summarization, and restart-manual QA while preserving the historical seq2seq origin note, the question-dependent weighting comparisons, and explicit case-closing result sentences about re-referencing relevant positions for the current task. Future translations should preserve the same operations-oriented framing and comparison pressure.
- 본문 반영: attention 기본 흐름도 뒤에 같은 입력 문장을 두고 `왜 보류되었나?`와 `무엇이 보류되었나?` 질문에 따라 높은 비중 위치가 달라지는 Mermaid 도식을 추가했다.

### v2026.07.12-diagram
- 번역 동기화 메모: Korean diagram labels were localized to match the deployed manuscript language while preserving the structure of the English source assets. Future translations should keep English originals as canonical and localize only the visible labels for each manuscript language.

### v2026.07.13-python-chart
- 번역 동기화 메모: Future translations should preserve the shared Python-generated source for the self-attention weight chart and localize only the derived labels.

### v2026.07.13-layout
- 번역 동기화 메모: The paired self-attention charts now keep only labels that are required to read the per-token weight contrast. Future translations should preserve this chart/body role split.

### v2026.07.17
- 본문 반영: `사례 및 예시`를 절차 문서·장애 메모·매뉴얼 QA 3개 사례의 긴 나열에서, 절차 문서 대표 사례와 도메인 확장 비교표 구조로 다시 묶어 현재 절의 중심 질문이 `질문이나 출력 목표가 바뀌면 다시 크게 볼 위치도 함께 바뀌는가`에 더 오래 머물도록 정리했다.
- 본문 반영: 사례 뒤에서 같은 판단을 반복하던 중복 비교표 1개를 걷어내고, `사람이 먼저 보기 쉬운 기준 vs attention 관점` 표만 남겨 attention의 참조 위치 변화에 더 집중되게 정리했다.
- 본문 반영: `이 절의 범위`에서 query/key/value 공식, multi-head 구현, Transformer block 구조 같은 바깥 항목 목록을 제거하고, attention 직관을 self-attention과 보충학습 `P5-13.3`으로 넘기는 handoff만 남기도록 정리했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff structure where this section closes the direct-reference intuition first and hands QKV naming and self-attention expansion to later sections.

### v2026.07.18
- 본문 반영: 앞부분의 `지금 읽는 층위`, 장간 비교 표, `이 절을 읽는 순서`를 제거하고, `왜 상태 보존 대신 필요한 위치를 다시 봐야 했는가 -> 더 강하게 본다는 말은 무엇인가` 흐름이 먼저 보이도록 재배치했다.
- 본문 반영: attention 도입부와 `더 강하게 본다` 설명 문단을 풀어 써서, 질문 변화에 따라 참조 위치가 왜 달라지는지 사례 전에 먼저 납득할 수 있게 보강했다.
- 본문 반영: 말미의 반복 handoff 표를 걷어내고, 현재 절 결론을 `attention은 direct reference 구조`라는 문장으로 먼저 닫은 뒤 self-attention 연결은 짧게만 남겼다.
- 번역 동기화 메모: Future translations should preserve the reduced meta-structure, the stronger direct-reference prose, and the shorter self-attention handoff at the end.

## 2026-07-18

- 영문 번역 반영: `docs/parts/part-05/chapter-13/section-01.en.md`를 추가해 attention의 direct-reference 직관, 질문 변화에 따른 참조 위치 변화, 절차 문서 변환 사례, 질문별 가중 평균 예제를 영어판 본문으로 동기화했다.
- 번역 자산 반영: 본문에서 `attention-direct-reference-bridge-en.mmd`, `attention-focus-flow-en.mmd`, `attention-question-shift-en.mmd`, `attention-pressure-question-weights-en.png`, `attention-flow-question-weights-en.png`, `attention-context-comparison-en.png`를 참조하도록 맞췄다.
- 번역 동기화 메모: English section now matches the Korean v2026.07.18 structure and preserves the shorter close that ends on attention as a direct-reference structure before self-attention.
