# Section Release Note

- Section ID: `P5-3.2`
- Source File: `docs/parts/part-05/chapter-03/section-02.md`

## 2026-07-07

### v2026.07.07-1
- 변경 이유: 대표 활성화 함수 비교 절에 개념사전 복귀 경로가 빠져 있었음.
- 본문 변경: ReLU, sigmoid, tanh 소개 뒤에 활성화 함수 개념사전 복귀 문장을 추가함.
- 번역본 메모: 번역본 영향 없음.

### v2026.07.07-1

- 변경 이유: 활성화 함수 비교 절 도입부에 `이 절은 다음 질문에 답합니다` 같은 메타 진행 문구가 남아 있어 설명 흐름을 무겁게 만들고 있었음.
- 본문 반영: 범위 구간의 메타 안내를 제거하고 질문 목록이 바로 이어지게 정리했다.
- 번역 동기화 메모: meta structural lead-in was removed in the activation-function comparison introduction. / wording change only. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07-1`

### v2026.07.09
- 변경 이유: Part 5 문단구조 정리 기준에 맞춰 사례/예제 구조 표지를 현재 원칙으로 통일할 필요가 있었다.
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 값 차이 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.11
- 변경 이유: Part 5 Mermaid 도식을 `docs/assets` 중심으로 정리하는 기준을 대표 활성화 함수 비교 절에도 적용할 필요가 있었다.
- 본문 반영: sigmoid, tanh, ReLU 비교 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: The activation-family Mermaid now keeps paired `-en` and `-ko` source files for translation reuse. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-03/activation-family-flow-en.mmd`, `docs/assets/part-05/chapter-03/activation-family-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.11-2
- 변경 이유: 대표 활성화 함수 비교 절에서 곡선 모양 자체를 바로 볼 수 있는 좌표 기반 차트가 없어, 함수별 반응 차이를 초심자가 눈으로 대조하기 어려웠다.
- 본문 반영: `세 함수를 그림 감각으로 비교하기` 구간에 sigmoid, tanh, ReLU 곡선을 3패널 SVG 차트로 추가하고, 기존 Mermaid는 해석 규칙 요약 도식이라는 역할이 드러나게 설명 문장을 보강했다.
- 번역 동기화 메모: Added paired SVG curve charts for activation-function comparison and clarified the distinct role of the Mermaid summary diagram. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-03/activation-function-curves-en.svg`, `docs/assets/part-05/chapter-03/activation-function-curves-ko.svg`
- 원문 기준 버전: `v2026.07.11-2`

### v2026.07.12
- 변경 이유: 말미의 회상·적용 시점·점검 표지가 `체크리스트`와 중복되어 기준서 위반 상태였다.
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: sigmoid, tanh, ReLU의 반응 방식, 출력 범위, 역사적 비교 기준, 출력층·손실 연결 흐름을 `체크리스트` 하나로 정리했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 학습 밀도 재점검 결과, 사례와 Python 예제가 함수 이름 소개는 해 주지만 `같은 z를 함수마다 어떻게 다르게 읽는가`를 독자가 먼저 예상해 볼 장면은 약했다.
- 본문 반영: `같은 점수, 다른 전달 방식` 사례에 `사람 기준 vs 함수 비교 기준` 표를 추가했다. `연습 및 예제`에는 실행 전 예측 표를 넣어 음수·작은 양수·큰 양수 입력에서 sigmoid, tanh, ReLU가 어떻게 다르게 반응할지 먼저 가늠하게 만들었고, 입력 개수 설명도 `세 입력값`에서 `네 입력값`으로 바로잡았다.
- 번역 동기화 메모: 한국어 기준 원고에 함수 비교 표와 예측 표가 추가되었다. 향후 번역 시 동일한 비교 구조와 입력별 예측 흐름을 함께 반영해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: 추가 재검토 결과, 기존 보강만으로는 `경계 근처 양수`와 `큰 양수 포화 구간`을 독자가 실제로 대비해 읽는 압력이 아직 부족했다.
- 본문 반영: 도입부에 `큰 음수 / 0 근처 / 큰 양수` 기준의 함수별 반응 비교 표를 추가했다. 사례에는 `z = 0.1`을 넣어 경계 바로 위 신호를 따로 보게 했고, 예제도 `-2.0, -0.5, 0.1, 0.5, 3.0` 다섯 입력으로 확장했다. 출력 뒤에는 `음수끼리`, `경계 근처 양수끼리`, `작은 양수와 큰 양수`를 각각 어떻게 눌리거나 유지하는지 비교 표를 덧붙였다.
- 번역 동기화 메모: 한국어 기준 원고에 saturation and near-boundary comparison structure was strengthened with the added `z = 0.1` case and post-output interpretation table. 향후 번역 시 같은 대비 구조를 유지해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 추가 재검토 결과, 비교 정보는 많았지만 `그래서 어떤 계산 감각이면 어떤 함수를 먼저 떠올려야 하는가`를 사례와 예제 끝에서 다시 묶는 장면이 약했다.
- 본문 반영: 사례 뒤에 `계산 감각 -> sigmoid/tanh/ReLU를 먼저 떠올릴 장면` 교차 비교표를 추가했다. 예제 출력 뒤에는 `이번 출력 차이에서 더 자연스럽게 떠올릴 함수` 요약 표를 덧붙여, 함수 비교가 곡선 암기가 아니라 다음 층에 남길 반응 규칙 선택이라는 점을 더 직접적으로 닫았다.
- 번역 동기화 메모: Korean source now closes P5-3.2 with function-selection tables tied to calculation intent and post-output interpretation. 향후 번역 시 same function-selection structure should be preserved.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: 대표 활성화 함수 비교가 함수 선택 감각까지는 잘 닫고 있었지만, 그 차이가 실제 운영 신호 해석에서 `경고를 눌러 볼지`, `부호를 살려 볼지`, `강한 양수만 계속 살릴지`로 어떻게 읽히는지는 아직 약했다.
- 본문 반영: 사례 뒤에 sigmoid, tanh, ReLU를 각각 `0~1 위험도`, `양/음 부호 유지`, `음수 차단과 양수 전달`의 운영 신호 해석으로 다시 묶는 설명 문단을 추가했다. 예제 출력 뒤에는 `운영 신호 해석 기준` 비교 표를 넣어 큰 음수, 경계 양수, 큰 양수 입력이 함수별로 어떤 경고 해석으로 바뀌는지 직접 대비하게 만들었다.
- 관련 자산 반영: 한국어 본문이 직접 참조하는 `activation-family-flow-ko.mmd`의 노드 라벨도 `시그모이드(sigmoid)`, `탄흐(tanh)` 기준으로 정리해, `-ko` 자산 안의 언어가 본문 언어와 더 직접 맞물리도록 보강했다.
- 번역 동기화 메모: Korean source now closes P5-3.2 with an operational-signal interpretation table, not just function-shape comparison. 향후 번역 시 same operational interpretation pressure를 유지해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-03/activation-family-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12-2
- 변경 이유: 함수 비교 예제가 여전히 `z 값 목록` 자체를 돌리는 장면에 가까워, 사례에서 말한 운영 경고 해석과 `연습 및 예제`가 같은 판단 흐름으로 닫히지 않았다.
- 본문 반영: `연습 및 예제`를 `quiet_frame`, `stable_recovery_frame`, `borderline_alarm_frame`, `confirmed_warning_frame`, `critical_shutdown_frame` 같은 설비 장면의 pre-activation score를 비교하는 구조로 다시 썼다. 출력 뒤 해설과 표도 각 장면 이름 기준으로 바꾸어, 같은 score라도 함수마다 `약한 위험도`, `안정 쪽 음수`, `강한 양수 경고`를 어떻게 다르게 남기는지 바로 읽게 했다.
- 자산 운영 반영: `docs/assets/part-05/chapter-03/README.md`에 언어별 본문이 각자 `-ko.mmd`, `-en.mmd`를 직접 참조하고, 영문 자산을 기준 원본으로 먼저 수정한 뒤 한국어 파생본을 동기화한다는 운영 원칙을 명시했다.
- 번역 동기화 메모: Korean source now uses equipment-scene pre-activation scores in the P5-3.2 practice block instead of a bare z-list, and the chapter asset README now matches the current per-language reference rule. Future translations should preserve the same scene-based comparison and asset operation rule.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-03/README.md`
- 원문 기준 버전: `v2026.07.12`

### v2026.07.13
- 변경 이유: 챕터 3 전체 재검토 결과, P5-3.2의 Python 비교 예제가 함수 차이를 직접 탐색하기보다 이미 정리된 장면별 해석을 다시 출력하는 검증형 블록에 가까웠다.
- 본문 반영: Python 코드와 실행 출력 블록을 제거하고, 다섯 장면의 함수별 반응값을 표로 직접 정리한 뒤 `함수 선택 문장`과 `경계/포화/음수 장면을 바꿔 보는 비교 질문`으로 연습을 닫도록 바꿨다.
- 번역 동기화 메모: Korean source now closes P5-3.2 with a non-code comparison table plus exploration prompts instead of a Python snippet. Future translations should preserve the same function-choice and scene-variation reading flow.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.13`
- 변경 이유 추가: 차트·표 형식 선택 가이드라인 재검토 결과, 함수 곡선 SVG 뒤의 Mermaid가 세 함수 비교를 순서형 흐름처럼 보이게 해 차트 역할과 겹쳤고, 사례·연습 해석 표 일부도 설명을 반복하고 있었다.
- 본문 반영 추가: `activation-function-curves-ko.svg` 곡선 차트는 유지하고, 중복 Mermaid include를 제거했다. 사례와 연습 뒤의 관점 비교표 일부는 문단 설명으로 바꿔 값 비교표와 후속 실험 표만 비교 자료로 남겼다.
- 자산 반영: 더 이상 참조하지 않는 `activation-family-flow-en.mmd`, `activation-family-flow-ko.mmd`를 제거하고, Chapter 3 자산 README의 언어별 Mermaid 목록도 함께 정리했다.
- 관련 자산 추가: `docs/assets/part-05/chapter-03/activation-family-flow-en.mmd`, `docs/assets/part-05/chapter-03/activation-family-flow-ko.mmd`, `docs/assets/part-05/chapter-03/README.md`
- 변경 이유 추가: 대표 활성화 함수 3개와 수식 비교를 한 Section에 묶으면 각 함수의 수식, 그래프 감각, 사용 맥락이 빠르게 지나가 초심자가 함수별 차이를 붙잡기 어렵다.
- 본문 반영 추가: 기존 통합 비교 Section을 분해해 `P5-3.2 sigmoid` Section으로 재구성했다. sigmoid의 수식, 0~1 출력 범위, 포화, threshold 해석 주의점을 독립적으로 설명하고, tanh/ReLU/수식 비교는 후속 Section으로 넘겼다.
- 번역 동기화 메모 추가: Korean source now splits the previous combined activation comparison into separate function sections. Future translations should preserve the new section boundary and section IDs.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산 추가: 없음
- 원문 기준 버전: `v2026.07.13`
- 변경 이유 추가: `sigmoid` 개별 Section이 “그래프를 어떻게 읽나”를 말하지만 실제 단일 곡선은 P5-3.5의 통합 비교 차트에만 있어, 독자가 이 Section 안에서 곡선 감각을 바로 닫기 어려웠다.
- 본문 반영 추가: `sigmoid` 단일 곡선 SVG를 추가하고, P5-3.2 본문에 삽입해 0~1 압축, \(z=0\)의 0.5, 양끝 포화 구간을 같은 Section 안에서 바로 보게 했다.
- 관련 자산 추가: `docs/assets/part-05/chapter-03/sigmoid-curve-en.svg`, `docs/assets/part-05/chapter-03/sigmoid-curve-ko.svg`, `docs/assets/part-05/chapter-03/README.md`
