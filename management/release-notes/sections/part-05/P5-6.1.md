# Section Release Note

- Section ID: `P5-6.1`
- Source File: `docs/parts/part-05/chapter-06/section-01.md`

### v2026.07.23
- 변경 이유: 장문형 Section 제목이 목차 가독성을 해치거나 본문 질문 전체를 제목에 떠안는 경우가 있어, 대표 제목과 선택적 보조제목 구조로 정리했다.
- 본문 반영 내용: H1과 nav 제목을 짧은 대표 제목으로 줄이고, 보조제목 없이도 중심축이 보이도록 제목을 정리했다. 본문 `Version`을 `v2026.07.23`으로 갱신했다.
- 번역 동기화 메모: 영어판과 중국어 간체판 제목, Version, nav를 원문 대표 제목 구조에 맞춰 동기화했다.
- 번역 반영 상태: ko/en/zh 제목 동기화 완료
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-05/chapter-06/section-01.md` `v2026.07.23`
### v2026.07.20
- 본문 반영 내용: 한국어, 영어, 중국어 간체판의 Python 예제 첫 줄에 batch 안의 샘플별 예측과 gradient를 평균한 뒤 `risk_weight`를 한 번 업데이트하는 학습 루프 목적을 설명하는 주석을 추가했다.
- 저작권 검토: Part 5 Module 3 순차 검토 범위에서 학습 루프, forward/loss/backward/optimizer step, 이미지·문장 분류 사례와 Python 예제를 확인했다. 사례·표·예제 코드는 본문 학습 목적에 맞춘 자체 작성 자료로 판단했다.
- 출처 사용 점검: Goodfellow et al., Géron 교재는 학습 루프와 딥러닝 구현 배경 확인 용도로만 연결되어 있으며, 원문 예제·도식·코드·문단 재사용 정황은 찾지 못했다.
- 관련 자산: Chapter 6 Mermaid와 `training-loop-*.png` 계열 그래프는 자체 도식과 로컬 생성 스크립트 기반 자산으로 판단했다.
- 본문 반영: 한국어, 영어, 중국어 간체판의 범위·목표 제목을 `학습 루프가 한 바퀴 도는 질문`, `forward-loss-backward-step 판단 기준` 축으로 바꾸어 진행 메타 제목을 제거했다.
- 번역 동기화 메모: 영어·중국어 번역본도 같은 제목 구조로 동기화하고 본문 Version을 `v2026.07.20`으로 맞췄다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.20`

### v2026.07.19
- 근거 보강: URL 없이 남아 있던 Géron 교재 출처에 O'Reilly 원문 페이지 링크와 확인 날짜를 추가하고, 본문 메타데이터를 현재 Section 형식으로 정리했다.
- 번역 동기화 메모: 향후 번역본 작성 시 같은 출처 링크와 확인 날짜를 반영해야 한다.

### v2026.07.16
- 변경 이유: Part 5 개편에서 학습 루프가 optimizer와 mode 구분보다 먼저 닫혀야 하므로, 기존 `P5-8.3 학습 루프를 한 번에 다시 묶기`를 Chapter P5-6의 첫 Section으로 이동했다.
- 본문 반영: Section ID를 `P5-6.1`로 바꾸고, 제목을 `학습 루프: forward, loss, backward, optimizer step`으로 조정했다. 도입부도 P5-5의 gradient 계산 뒤 바로 학습 루프를 묻는 흐름으로 바꿨다.
- 관련 자산: 학습 루프 Mermaid와 Python 그래프 자산을 `docs/assets/part-05/chapter-08/`에서 `docs/assets/part-05/chapter-06/`으로 옮기고, 생성 스크립트를 `p5_6_1_training_loop_charts.py`로 이름 변경했다.
- 번역 동기화 메모: Korean source moved the training-loop recap from P5-8.3 to P5-6.1. Future translations should follow the new Section ID and asset path.

### v2026.07.14
- 본문 반영: 예제 출력 뒤에 batch별 예측/목표, batch 평균 loss, batch 평균 gradient, optimizer step 뒤 `risk_weight`를 각각 분리 PNG 그래프로 추가했다.
- 번역 동기화 메모: 향후 번역 시 `forward -> loss -> gradient -> updated weight` 산출물 그래프 순서와 해설 구조를 유지해야 한다.

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: 학습 루프와 정규화 반복 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: The training-loop Mermaid now keeps paired `-en` and `-ko` source files for translation reuse. / pending

### v2026.07.12
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: forward-loss-backward-update 반복, mode·regularization의 위치, 구조 장 독해 기준, 계산 자원 장으로 이어지는 기준을 `체크리스트` 하나로 통합했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.

### v2026.07.12
- 본문 반영: `사례 및 예시`에 `사람이 먼저 보기 쉬운 기준 / 학습 루프 관점` 비교 표와 최종 결과 문장을 추가했다. `연습 및 예제`에는 `predictions`, `batch_loss`, `batch_gradient`, `updated_w`가 어떤 순서로 나타날지 코드 전에 예상하게 하는 표를 추가했다.
- 번역 동기화 메모: Korean source now closes the training-loop recap section with a human-vs-learning-loop comparison table and a pre-run prediction table for batch-step ordering. 향후 번역 시 same comparison structure를 유지해야 한다.

### v2026.07.12
- 본문 반영: 예제 입력, 코드, 출력 표기를 `alarm_count`, `target_block_score`, `risk_weight`, `updated_risk_weight` 기준으로 다시 적어, 학습 루프의 batch 평균 구조도 같은 운영 신호 축 안에서 읽히게 정리했다.
- 번역 동기화 메모: The Korean training-loop exercise now aligns its batch example labels with the surrounding operational batch framing instead of generic x/target/w naming. Future translations should preserve this aligned naming.

### v2026.07.13-link
- 본문 반영: 복귀 링크를 현재 실제 표제어인 `학습(training)`, `역전파(backpropagation)`, `옵티마이저(optimizer)` 조합으로 조정했다.
- 번역 동기화 메모: The loop refresher now links only to glossary entries that actually exist, replacing the absent forward-pass heading with the broader training entry.

### v2026.07.17
- 본문 반영: 도입부의 `forward -> loss -> backward -> optimizer step -> regularization/모드 제어` 표현이 한 줄에서 서로 다른 층위의 용어를 섞고 있어, `핵심 계산 4단계`와 `batch 반복 / mode 전환 / regularization`을 분리해 다시 썼다.
- 본문 반영: `가장 작은 학습 루프` 아래에 `계산 단계 / 반복 단위 / 상태 제어 / 학습 제어`를 나누는 표를 추가하고, mode와 regularization 설명도 `단계 이름`이 아니라 `계산 규칙과 학습 성격을 바꾸는 요소`로 읽게 조정했다.
- 추가 반영: 같은 자리 아래에 `forward`, `loss`, `backward`, `optimizer step`을 각각 `예측 만들기`, `오차 점수화`, `책임 되돌리기`, `모델 값 갱신`이라는 책의 기준 표현으로 다시 풀어 쓰는 연결 표와 요약 문장을 추가했다.
- 추가 반영: `왜 모드 전환이 여기에 붙는가` 절에서 dropout 이름이 갑자기 먼저 튀지 않도록, 구체 기법 이름보다 `일부 층의 학습용 계산 규칙`과 `평가 시 더 안정적인 계산 기준`이라는 수준으로 먼저 설명을 완화했다.
- 추가 반영: P5-6.1의 중심을 `학습 루프의 공통 4단계`에 더 강하게 고정하기 위해 regularization을 현재 절의 직접 설명 대상에서 한 단계 뒤로 물리고, 여기서는 `batch 반복`과 `mode 전환`만 바깥 제어 요소로 먼저 남기도록 정리했다.
- 추가 반영: 개편 수준으로 다시 정리해 P5-6.1에서 mode/regularization 중심 문단과 사례를 걷어내고, `공통 4단계 + batch 반복`이라는 학습 루프 설명에만 집중하도록 축을 좁혔다. 도식도 `정규화와 모드 제어를 포함해 반복` 대신 `다음 배치에서 다시 반복`으로 수정했다.
- 추가 반영: `이 절에서는 다음 내용을 깊게 다루지 않습니다` 범위 예고 블록을 제거하고, 필요한 안내만 `이 절의 범위` 문단 안에 흡수해 메타 안내 없이 바로 학습 루프 설명으로 이어지게 정리했다.
- 추가 반영: 학습 밀도를 높이기 위해 `아주 단순하게 그리면` 아래에 `한 번의 step`, `batch 반복`, `모델 값이 실제로 바뀌는 시점`을 분리하는 작은 표를 추가했다.
- 추가 반영: 사례 1, 2 바로 아래에 각 사례의 판단 순서를 다시 압축하는 Mermaid 흐름도를 붙였고, `사례 3. 과적합 징후` 표제는 실제 내용에 맞게 `구조 변화와 공통 루프를 함께 읽기`로 수정했다.
- 추가 반영: 예제 말미에 `batch 안쪽 / batch 끝 / step 끝` 역할 표와 요약 문단을 추가해, 샘플별 계산과 실제 update 시점을 분리해 읽게 보강했다.
- 추가 반영: `이 절의 범위` 문단을 `무엇을 다루지 않는가`의 단순 생략 안내가 아니라, 이번 절에서 닫을 질문과 다음 Section인 P5-6.2, P5-6.3, P5-6.4로 넘길 질문을 구분하는 방식으로 다시 정리했다.
- 추가 반영: Chapter 6을 4개 Section으로 재배치하면서, P5-6.1 뒤 handoff를 `P5-6.2 step/batch/epoch -> P5-6.3 learning/inference -> P5-6.4 training/evaluation mode` 순서로 다시 조정했다.
- 번역 동기화 메모: Future translations should preserve the distinction between the core four computation steps and the surrounding control/state terms, instead of listing them in one flat chain.

## 2026-07-18

### v2026.07.17
- 번역 동기화 메모: `docs/parts/part-05/chapter-06/section-01.en.md` was added and aligned to the current Korean source version, preserving the separation between the core four stages and batch repetition, the shared-loop-vs-structure distinction, and the batch-average Python example with English Mermaid/image assets.
- 번역 반영 상태: 영문 번역 반영 완료
- 관련 자산: `docs/assets/part-05/chapter-06/training-loop-regularization-flow-en.mmd`, `docs/assets/part-05/chapter-06/training-loop-predictions-en.png`, `docs/assets/part-05/chapter-06/training-loop-batch-loss-en.png`, `docs/assets/part-05/chapter-06/training-loop-batch-gradient-en.png`, `docs/assets/part-05/chapter-06/training-loop-risk-weight-update-en.png`
- 원문 기준 버전: `v2026.07.17`
- 번역 동기화 메모: `docs/parts/part-05/chapter-06/section-01.zh.md` was added and aligned to the current Korean source version, preserving the distinction between the core four computation stages and batch repetition, the structure-change cases, and the batch-average Python walkthrough with Chinese Mermaid/image assets.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 관련 자산: `docs/assets/part-05/chapter-06/training-loop-regularization-flow-zh.mmd`, `docs/assets/part-05/chapter-06/training-loop-predictions-zh.png`, `docs/assets/part-05/chapter-06/training-loop-batch-loss-zh.png`, `docs/assets/part-05/chapter-06/training-loop-batch-gradient-zh.png`, `docs/assets/part-05/chapter-06/training-loop-risk-weight-update-zh.png`
