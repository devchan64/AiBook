# Section Release Note

- Section ID: `P5-6.1`
- Source File: `docs/parts/part-05/chapter-06/section-01.md`

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: 학습과 실행의 순전파 갈림 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: The training-vs-inference Mermaid now keeps paired `-en` and `-ko` source files for translation reuse. / pending

### v2026.07.12
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: learning/inference 구분, 파라미터 변경 여부, forward 공통성과 후속 단계 차이, mode 장으로 넘기는 기준을 `체크리스트` 하나로 통합했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.

### v2026.07.12
- 본문 반영: 스팸 분류 사례에 `사람 기준 vs learning/inference 관점` 표를 추가했다. `연습 및 예제`에는 실행 전 예측 표를 넣고, inference 구간을 입력별 출력과 `weight_used`를 직접 보여 주는 형식으로 바꿨다. 출력 뒤에는 `train step`과 `inference`를 나눠 어떤 구간에서만 파라미터가 바뀌는지 해설 표를 덧붙였다.
- 번역 동기화 메모: 한국어 기준 원고에 output-change-vs-parameter-change comparison structure was added to the learning/inference section. 향후 번역 시 같은 prediction table과 post-output interpretation table을 유지해야 한다.

### v2026.07.12
- 본문 반영: 사례 1을 경보 분류 모델로, 사례 2를 검사 이미지 데모로, 사례 3을 현장 지원 채팅으로 바꿨다. 각각 `새 경보를 본다`, `검사 이미지 한 장을 올린다`, `재기동 질문을 조금 다르게 쓴다`는 입력 변화가 파라미터 학습과 별개라는 점을 같은 구조로 다시 설명했다.
- 번역 동기화 메모: Korean source now frames the learning-vs-inference cases around alarm classification, inspection-image demos, and plant-support chat instead of spam, pet images, and generic chat. 향후 번역 시 same operational case set을 유지해야 한다.
- 본문 반영: 세 사례 뒤에 `사람이 먼저 보는 결과 -> learning/inference 관점에서 실제로 구분해야 할 것 -> 파라미터 변경 여부` 교차 비교표를 추가했다. 예제 출력 뒤에는 `결과만 볼 때 vs learning/inference 관점에서 다시 읽는 해석` 비교표를 덧붙여, 같은 forward 계산이라도 update가 붙는 구간과 붙지 않는 구간을 더 직접적으로 읽게 만들었다.

### v2026.07.12
- 본문 반영: 사례 뒤에 공통 Mermaid 도식 1개를 추가해 `새 입력 -> 출력 재계산 -> 파라미터 유지`와 `학습 배치 -> 손실/gradient/update -> 파라미터 변경` 흐름을 한 번에 다시 묶었다. 사례별 표를 다시 반복하지 않고, 세 사례가 공통으로 보여 주는 `계산 결과 변화`와 `내부 파라미터 변화`의 차이만 압축하도록 위치와 설명을 정리했다.
- 번역 동기화 메모: Added a shared learning-inference Mermaid pair so the Korean manuscript now summarizes the output-change vs parameter-change contrast with one compact bridge. Future translations should preserve the same recalculation-vs-update structure.

### v2026.07.12
- 본문 반영: 예제를 `alarm_count`, `target_block_score`, `risk_weight`, `predicted_block_score` 기준으로 다시 적고, 학습 배치와 서비스 입력 구간 모두 같은 운영 신호 용어를 쓰도록 정리했다. 출력 뒤 해설 표도 `risk_weight` 변화/고정 여부를 직접 읽게 갱신했다.
- 번역 동기화 메모: The Korean exercise now uses the same operational signal names as the surrounding cases instead of abstract x/w notation. Future translations should preserve this aligned terminology across the case and exercise.

### v2026.07.13
- 본문 반영: 예제 앞에 `learning_rate`, `service_alarm_counts`, `service_shadow_sample`를 바꿔 볼 실험 표를 추가했다. 코드에는 `run_train_step(...)`를 분리하고, 같은 종류의 서비스 입력에도 update를 붙였을 때만 `shadow_risk_weight_after`가 달라지는 대비 장면을 추가했다. 출력 해설과 후속 비교표도 `같은 입력`보다 `update 경로 유무`가 파라미터 변화를 결정한다는 쪽으로 강화했다.
- 번역 동기화 메모: Korean source now treats the P5-6.1 Python block as an experiment on whether an update path is attached, not just a one-pass demonstration. Future translations should preserve the tunable knobs table and the shadow-update contrast on the same kind of service input.

### v2026.07.14
- 본문 반영: `왜 inference를 추론 하나로만 옮기면 혼동이 생기나` 구간을 다시 쓰고, 영어 `reasoning / inference / prediction`, 한국어 `추론 / 모델 실행 / 예측`, 중국어 `推理 / 推断, 模型执行 / 预测` 대응 표를 추가했다.
- 번역 동기화 메모: Korean source now separates multilingual wording for reasoning, inference, and prediction more explicitly and anchors `inference` to the role of running a trained model. Future translations should preserve that role-based distinction instead of collapsing them into one local word.
