# Section Release Note

- Section ID: `P5-8.1`
- Source File: `docs/parts/part-05/chapter-08/section-01.md`

### v2026.07.16
- 변경 이유: P5-6 내부 파일 경로와 Section ID가 바뀌면서, 학습 모드와 평가 모드 참조 위치를 새 번호로 맞춰야 했다.
- 본문 반영: 학습 모드와 평가 모드의 계산 차이를 가리키는 참조를 `P5-6.2`에서 `P5-6.3`으로 수정하고 Version을 갱신했다.
- 번역 동기화 메모: Korean source now points regularization readers to P5-6.3 for training/evaluation mode. Future translations should use the new Section ID.

### v2026.07.14
- 본문 반영: 사례를 `같은 훈련 성능을 다른 기준으로 다시 읽는 경우` 하나로 재구성하고, 훈련 손실만 보는 판단에서 가중치 크기·예측 민감도·검증 안정성을 함께 보는 판단으로 이동하게 했다. 사례 흐름을 Mermaid로 압축해 regularization과 normalization의 차이도 같은 장면 안에서 다시 확인하도록 정리했다.
- 번역 동기화 메모: Korean source now replaces the broad multi-case list with one focused regularization case: similar training fit, different weight size/sensitivity, and preference for the less aggressive solution. Future translations should preserve the case flow and paired Mermaid asset.

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: 손실 함수와의 관계 구간 아래에 regularization 역할 흐름 Mermaid를 추가하고, regularization이 덜 과격한 해를 선호하게 만드는 장치라는 설명 문장을 보강했다.
- 번역 동기화 메모: Added paired Mermaid assets for the regularization-role flow and clarified how the regularization term changes the total objective toward generalization. / pending

### v2026.07.12
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: 과적합 억제, normalization과의 차이, 벌점과 설계 철학, optimizer와의 역할 분리, dropout 장으로 이어지는 기준을 `체크리스트` 하나로 통합했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.

### v2026.07.12-asset
- 번역 동기화 메모: Korean diagram labels and chapter asset-operation wording were aligned to the current per-language reference rule. Future translation work should preserve the same node structure while keeping each deployed language page tied to its own asset file.

### v2026.07.12
- 본문 반영: 사례 2에 `사람 기준 vs regularization 관점` 표를 추가했다. `연습 및 예제`에는 실행 전 비교 표를 넣고, step 반복 뒤 `prediction_without_reg`, `prediction_with_reg`, 입력 변화에 대한 민감도 차이까지 함께 출력하도록 바꿨다. 출력 뒤에는 `without_reg / with_reg`를 `더 과격한 해 / 덜 과격한 해`로 읽는 해설 표를 덧붙였다.
- 번역 동기화 메모: 한국어 기준 원고에 weight-growth-vs-prediction-sensitivity comparison structure was added to the regularization section. 향후 번역 시 같은 pre-run table과 post-output interpretation table을 유지해야 한다.

### v2026.07.12
- 본문 반영: 네 사례를 `사람이 먼저 보기 쉬운 결과`와 `regularization 관점에서 실제로 구분해야 할 것`으로 다시 묶는 비교표를 추가했다. 예제 뒤에는 `오차만 보면 남기 쉬운 해석`과 `regularization까지 보면 바뀌는 해석`을 `without_reg / with_reg` 기준으로 다시 읽는 해설 표를 덧붙였다.
- 번역 동기화 메모: Korean source now ties the regularization cases back to one cross-case frame and adds a post-output interpretation table separating error reduction from preference for less aggressive solutions. 향후 번역 시 같은 cross-case and post-output comparison structure를 유지해야 한다.
- 본문 반영: 사례 요약 뒤에 `같은 훈련 데이터 적합도 / 다른 가중치 크기와 민감도` 비교 표를 추가하고, regularization이 더 과격한 해보다 덜 과격한 해를 선호하는 이유를 압축한 Mermaid 도식을 배치했다.

### v2026.07.13
- 본문 반영: 과적합 설명 아래에 훈련 손실은 계속 내려가지만 검증 손실이 다시 올라가는 SVG 그래프를 추가하고, regularization이 훈련 손실 하나가 아니라 새 데이터 손실과 간극을 함께 본다는 설명을 보강했다.
- 번역 동기화 메모: Korean source now uses a localized SVG chart for train-vs-validation overfitting intuition. Future translations should reference the matching English SVG or localized language asset.

### v2026.07.13-python-chart
- 번역 동기화 메모: Future translations should preserve the shared Python-generated source for the overfitting chart and localize only the derived labels.

### v2026.07.13-layout
- 번역 동기화 메모: The paired overfitting charts now keep only labels that are required to read the train/validation divergence. Future translations should preserve this chart/body role split.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 L1/L2 수식 유도, weight decay 구현 차이, data augmentation과 early stopping 튜닝 같은 바깥 항목 목록을 제거하고, regularization의 큰 관점과 `P5-8.2`, `P5-6.4` handoff만 남기도록 정리했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff structure where this section closes the regularization viewpoint first and hands dropout and mode-specific follow-up to later sections.
