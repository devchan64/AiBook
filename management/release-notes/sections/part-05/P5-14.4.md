# Section Release Note

- Section ID: `P5-14.4`
- Source File: `docs/parts/part-05/chapter-14/section-04.md`

### v2026.07.31
- 후속 보강: 소제목 정리에 그치지 않도록 사례 절 첫머리에 중심축 확인 질문 2개를 추가했다.
- 변경 이유: RNN과 Transformer 비교를 최신성 인상이 아니라 계산 의존성과 병렬화 단위 차이로 읽게 할 필요가 있었다.
- 본문 반영 내용: 한국어 원문 `Version`을 `v2026.07.31`로 갱신하고, `depends_on_previous_step`, `relation_matrix`, `parallel_unit`, `batch_axis` 필드 예시를 추가했다.
- 번역 동기화 메모: 영어판과 중국어 간체판은 이번 한국어 원문 보강 문단을 후속 동기화해야 한다.
- 번역 반영 상태: 한국어 원문만 반영.
- 관련 자산: 변경 없음.
- 원문 기준 버전: `v2026.07.31`
- 변경 이유: Part 5 보강 라운드에서 진행 메타 성격의 `사례 및 예시` 소제목을 내용형 제목으로 정리해 독자가 사례 절의 딥러닝 계산 장면과 판단 기준을 바로 읽을 수 있게 했다.
- 소제목 정리: 한국어 원문의 `사례 및 예시` 절 제목을 `RNN 상태 전달과 Transformer 병렬 계산: 확인할 판단 기준`으로 바꿔 사례 절이 맡는 딥러닝 계산 장면과 판단 기준을 바로 드러내도록 했다.
- 번역 동기화 메모: 이번 소제목 정리는 한국어 원문 기준으로 먼저 반영했으며, 영어판과 중국어 간체판은 후속 동기화가 필요하다.
- 번역 반영 상태: 한국어 원문 반영 완료, 영어판과 중국어 간체판 미반영.
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.31`
### v2026.07.26
- 정합성 점검: 한국어·영어·중국어 간체판의 본문 `Version`을 `v2026.07.26`으로 맞췄다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-05/chapter-14/section-04.md` `v2026.07.26`

### v2026.07.23
- 변경 이유: 장문형 Section 제목이 목차 가독성을 해치거나 본문 질문 전체를 제목에 떠안는 경우가 있어, 대표 제목과 선택적 보조제목 구조로 정리했다.
- 본문 반영 내용: H1과 nav 제목을 짧은 대표 제목으로 줄이고, 중심 질문 손실을 막기 위해 메타데이터 아래 보조제목을 추가했다. 본문 `Version`을 `v2026.07.23`으로 갱신했다.
- 번역 동기화 메모: 영어판과 중국어 간체판 제목, Version, nav를 원문 대표 제목 구조에 맞춰 동기화했다.
- 번역 반영 상태: ko/en/zh 제목 동기화 완료
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-05/chapter-14/section-04.md` `v2026.07.23`
### v2026.07.20
- 연습 해설 보강: `기다림`과 `묶음` 표시가 각각 앞 step 결과 의존과 행렬·텐서 계산 조직을 뜻한다는 설명을 원문·영문·중문에 풀어 추가했다.
- 본문 보강: `RNN도 텐서 계산이지만 시퀀스 축에서 앞 step의 hidden state를 기다린다`는 설명과, 행렬·텐서를 관계 점수 표와 배치로 쌓은 표 묶음으로 읽는 초심자용 해설을 원문·영문·중문에 추가했다.
- 도식 보강: RNN식 상태 전달과 Transformer식 관계 계산의 갈림점을 Mermaid 플로우차트로 추가하고, 한국어·영어·중국어 자산을 동기화했다.
- 예제 조정: 관계 score 예제의 `top related lines`에서 request 자기 자신을 제외해, 현재 질문이 앞 단서인 rule과 pressure_state를 크게 잡는다는 관찰값이 먼저 보이도록 수정했다.
- 경계 보강: Transformer의 병렬화 감각은 주로 학습 중 한 층의 관계 계산에 해당하며, 생성 단계에는 다음 토큰 순서 제약이 남는다는 문장을 본문에 추가했다.
- 문구 정리: 도입부의 `표현 계산`을 표현 갱신 계산으로 풀고, 도식 주변의 좌우 위치 의존 표현과 초심자 메타 표현을 원문·영문·중문에서 정리했다.
- 예제 안내 보강: `line_features`, `relation_kernel`, `batch`를 바꿨을 때 어떤 출력 변화를 관찰해야 하는지 원문·영문·중문에 추가했다.
- 번호 정리: 옛 P5-14.3 삭제에 따라 기존 P5-14.5를 현재 P5-14.4로 당기고, 한국어·영어·중국어 간체판의 제목, Section ID, Version, 내부 참조를 새 번호 체계에 맞췄다.
- 본문 반영 내용: 한국어, 영어, 중국어 간체판의 Python 예제 첫 줄에 RNN식 순차 trace와 Transformer식 관계 score 행렬을 비교해 step 순서 누적과 병렬 관계 계산의 차이를 확인하는 목적을 설명하는 주석을 추가했다.
- 저작권 검토: Part 5 Module 5 순차 검토 범위에서 RNN 순차 상태 전달과 Transformer 관계 계산의 병렬 처리 대비, 순차 trace와 관계 score 행렬 Python 예제를 확인했다. 사례·표·예제 코드는 자체 작성 자료로 판단했다.
- 출처 사용 점검: Transformer 논문과 Goodfellow et al.은 구조 비교와 병렬 계산 배경 확인 용도로만 연결되어 있으며, 원문 실험표·도식·코드·문단 재사용 정황은 찾지 못했다.
- 관련 자산: 본문 예제는 실행 시간 측정이나 외부 구현 재사용이 아니라 자체 toy trace와 행렬 shape 비교로 구성된 자료로 판단했다.
- 본문 반영: 번호 정리와 Python 예제 주석 보강을 반영해 원문·번역본 Version을 `v2026.07.20`으로 맞췄다.
- 번역 동기화 메모: 영어·중국어 간체 번역본의 Transformer 논문 출처 확인 날짜와 NeurIPS 원문 링크를 원문 기준 `v2026.07.20`까지 동기화했다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.20`

### v2026.07.19
- 근거 보강: Transformer 병렬 처리 대비 설명의 기준 출처에 NeurIPS 원문 페이지 링크와 확인 날짜를 추가했다.
- 번역 동기화 메모: 향후 번역본 작성 시 같은 출처 링크와 확인 날짜를 반영해야 한다.

### v2026.07.19
- 신규 분리: 기존 P5-14.2의 RNN 대비 설명을 `RNN의 상태 전달과 Transformer의 관계 계산은 어떻게 다른가` Section으로 독립시켰다.
- 본문 반영: RNN을 순차 상태 전달, Transformer를 토큰 관계 계산으로 대비하고, 작업 허가 문장의 마지막 질문 사례와 계산 흐름 분류 연습을 추가했다.
- 병합 반영: 사용자 지시에 따라 기존 `P5-14.5 관계 계산은 왜 GPU 병렬 처리와 잘 맞는가`를 현재 Section에 합쳐, 계산 흐름 비교와 GPU 병렬 처리 적합성을 하나의 중심축으로 재구성했다.
- 제목 갱신: 병합 뒤 제목을 `RNN의 상태 전달과 Transformer의 관계 계산은 병렬 처리에서 어떻게 갈라지는가`로 바꿔, 상태 전달/관계 계산 대비가 병렬 처리 설명까지 이어짐을 드러냈다.
- 제목 정리: Section 제목에서 비교 대상을 `상태 전달`과 `관계 계산`으로 직접 드러내도록 조정했다.
- 메타데이터 정리: 본문 제목 아래 `Section ID`와 `Version`을 현재 Section 메타데이터 가이드 형식에 맞춰 blockquote 표기로 정리했다.
- 도입 문장 정리: 시간순 비교가 아니라 계산 흐름 차이라는 기준이 먼저 보이도록 메타 표현을 줄였다.
- 학습 밀도 보강: 관찰 신호별로 RNN식 상태 전달과 Transformer식 관계 계산을 비교하는 표를 추가하고, 마지막 판단에 필요한 단서 경로를 찾는 연습을 보강했다.
- 사례·연습 재보강: 작업 허가 문장을 6개 줄로 분해해 마지막 질문에 필요한 규칙·상태 단서가 어떤 계산 흐름으로 연결되는지 보이게 하고, 줄 표시하기·계산 경로 완성하기·장면별 계산 감각 고르기 연습으로 실습형 학습 밀도를 높였다.
- Section 경계 정리: P5-14.4는 긴 문맥의 단서 약화가 아니라 `기다려야 하는 순차 계산`과 `묶을 수 있는 행렬 계산`을 중심축으로 닫도록 사례와 해설을 조정했다. 먼 단서의 직접 재참조 문제는 P5-14.5으로 넘긴다.
- Python 실습 보강: Python 예제 작성 가이드 기준으로 P5-14.4를 실험형 예제 후보로 판단하고, 순차 상태 trace와 관계 score 행렬·배치 score 텐서 shape를 비교하는 예제를 추가했다.
- 연습 정리: Python 실습과 중복되어 흐름을 늦추던 `마지막 질문에 필요한 줄 표시하기`, `병렬 처리 관점으로 다시 말하기` 연습을 삭제하고, 계산 trace·행렬 shape 예제와 최종 분류 연습만 남겼다.
- 사례 압축: 작업 허가 문장 사례에서 긴 문맥 단서 설명과 배치 문장 나열을 줄이고, `기다려야 하는 step 계산`과 `묶을 수 있는 관계 행렬 계산`을 구분하는 중심축만 남겼다.
- 번역 동기화 메모: 영어·중국어 번역본을 새 9개 Section 구조의 P5-14.4에 맞춰 추가했다. RNN식 순차 trace, Transformer식 관계 score 행렬, 배치 score tensor shape 예제를 원문 기준으로 반영했다.
- 번역 반영 상태: 영어·중국어 반영 완료.
- 원문 기준 버전: `v2026.07.19`
