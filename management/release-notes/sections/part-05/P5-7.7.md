# Section Release Note

- Section ID: `P5-7.7`
- Source File: `docs/parts/part-05/chapter-07/section-07.md`

### v2026.07.20
- 저작권 검토: Part 5 Module 3 순차 검토 범위에서 optimizer state, parameter-wise update, Adam의 first/second moment 상태 설명과 비교 사례를 확인했다. 본문 표·사례·연습은 자체 작성 자료로 판단했다.
- 출처 사용 점검: PyTorch `torch.optim`과 `torch.optim.Adam` API는 optimizer state와 parameter별 update 구조 확인 용도로만 연결되어 있으며, 원문 API 예제 코드·문단 재사용 정황은 찾지 못했다.
- 관련 자산: optimizer state Mermaid와 `state-update-comparison-ko.png`는 자체 도식과 로컬 생성 스크립트 기반 자산으로 판단했다.
- 본문 반영: 없음. 기존 원문 버전 `v2026.07.19` 유지.
- 번역 동기화 메모: 번역본 영향 없음.

### v2026.07.19
- 근거 보강: optimizer state, per-parameter option, Adam의 first/second moment 상태 설명을 뒷받침하도록 PyTorch `torch.optim`과 `torch.optim.Adam` API 출처를 추가하고, 본문 메타데이터를 현재 형식으로 정리했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본을 갱신할 때도 optimizer state와 parameter-wise update 근거를 같은 기준으로 반영해야 한다.

### v2026.07.17
- 변경 이유: P5-7.3에서 `최근 gradient 흐름`과 `좌표별 조절`을 설명한 뒤, optimizer state와 parameter-wise update를 별도로 풀어 주는 회수 위치가 필요해졌다.
- 본문 반영: 새 보충학습 `P5-7.7 보충학습: optimizer state와 parameter-wise update`를 추가했다. parameter, gradient, update, optimizer state를 구분하는 표를 두고, adaptive optimizer가 시간축과 좌표축 state를 어떻게 함께 쓰는지 설명했다. 같은 gradient라도 state가 다르면 다음 update가 달라질 수 있다는 점을 사례와 연습으로 정리했다.
- 본문 반영: 네 항목 구분이 표 설명에만 머물지 않도록 문단을 풀어 쓰고, optimizer state를 모델 내용과 작업 메모로 나누는 비유를 확장했다. `같은 gradient라도 update가 다를 수 있다`는 사례 해설도 현재 입력과 누적 state가 함께 입력이라는 점이 더 직접 보이도록 보강했다.
- 본문 반영: parameter와 state의 차이가 실제 저장/재시작 장면에서 왜 중요한지, 그리고 같은 gradient라도 좌표별 과거 문맥이 다르면 update가 달라질 수 있다는 아주 작은 숫자 장면을 더해 초심자 기준 학습 밀도를 높였다.
- 본문 반영: parameter/state/current gradient를 두 좌표 표로 다시 적어, `현재 gradient는 같아도 문맥은 다를 수 있다`는 점을 숫자와 상태 줄로 함께 읽게 보강했다.
- 본문 반영: `읽을 때` 중심 표현을 줄이고, 학습 코드와 설명 문장 전반에서 재사용할 수 있는 구분 기준이라는 점이 더 직접 드러나도록 핵심 해설 문장을 정리했다.
- 본문 반영: 도입에 checkpoint 저장, 학습 재시작, fine-tuning에서도 parameter/state 구분이 다시 쓰인다는 문장을 추가해, 보충학습의 활용 범위를 더 분명히 했다.
- 본문 반영: `parameter, gradient, update, state`가 모두 비슷한 숫자로 보이는 초심자 혼동을 줄이기 위해, 학습 코드 세 줄을 사건별로 분리해 읽는 표와 parameter-wise update의 직관 비유를 추가했다.
- 본문 반영: `gradient 계산 -> optimizer -> 파라미터 반영` 구간을 `optimizer-loop-flow-ko.mmd` 도식으로 다시 배치해, 현재 절의 네 항목이 학습 루프 안에서 어디에 놓이는지 눈으로 확인할 수 있게 했다.
- 본문 반영: `state-update-comparison-ko.png` 그래프를 추가해, 같은 current gradient라도 좌표별 state가 다르면 resulting update가 달라질 수 있다는 점을 수치 그림으로 다시 확인할 수 있게 했다.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 optimizer state를 모델이 배운 내용과 혼동하지 않도록 `작업 메모` 비유와 네 항목 구분표를 유지해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 원문 기준 버전: `v2026.07.17`

## 2026-07-18

### v2026.07.17
- 번역 동기화 메모: `docs/parts/part-05/chapter-07/section-07.en.md` was added and aligned to the current Korean source version, preserving the four-item distinction among parameter/gradient/update/state, the working-memory analogy for optimizer state, the parameter-wise-update framing, and the state-versus-current-gradient comparison graph.
- 번역 반영 상태: 영문 번역 반영 완료
- 관련 자산: `docs/assets/part-05/chapter-07/optimizer-loop-flow-en.mmd`, `docs/assets/part-05/chapter-07/state-update-comparison-en.png`
- 원문 기준 버전: `v2026.07.17`
- 번역 동기화 메모: `docs/parts/part-05/chapter-07/section-07.zh.md` was added and aligned to the current Korean source version, preserving the four-item distinction among parameter/gradient/update/state, the working-memory analogy for optimizer state, the parameter-wise-update framing, and the state-versus-current-gradient comparison graph.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 관련 자산: `docs/assets/part-05/chapter-07/optimizer-loop-flow-zh.mmd`, `docs/assets/part-05/chapter-07/state-update-comparison-zh.png`
