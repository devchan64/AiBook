# Section Release Note

- Section ID: `P5-3.5`
- Source File: `docs/parts/part-05/chapter-03/section-05.md`

### v2026.07.13
- 본문 반영: `P5-3.5 대표 활성화 함수 수식 비교` Section을 새로 만들고, sigmoid/tanh/ReLU의 수식, 출력 범위, 포화 여부, 같은 \(z\) 값에 대한 출력 비교, 함수 선택 기준을 한 Section으로 정리했다.
- 번역 동기화 메모: The existing activation curve SVG is now referenced from the formula-comparison section. Future translations should keep the comparison chart in this section.

### v2026.07.13-python-chart
- 번역 동기화 메모: The Korean and English activation comparison chart is now generated from the shared Chapter 3 Python chart script. Future translations should keep using the shared generated source.

### v2026.07.13-layout
- 본문 반영: 결합 그래프 1개 대신 `sigmoid`, `tanh`, `ReLU` 개별 곡선 SVG 3개를 순서대로 배치하고, 바로 아래 설명도 세 그래프를 함께 읽는 문장으로 조정했다.
- 번역 동기화 메모: The section now displays the existing per-function curve assets instead of the combined three-panel chart. Future translations should preserve the one-function-per-graph layout in this section.

### v2026.07.14-asset
- 번역 동기화 메모: P5-3.5 keeps the per-function graph layout. Future translations should not reintroduce the combined activation-function chart unless readability is re-evaluated.

### v2026.07.14
- 본문 반영: 연습을 실행 가능한 Python 예제로 교체해 `z_values`를 sigmoid, tanh, ReLU에 동시에 넣고, 음수 입력, 0 기준점, 큰 양수 입력에서 출력이 어떻게 달라지는지 행 단위로 비교하게 했다.
- 번역 동기화 메모: `docs/parts/part-05/chapter-03/section-05.en.md`에 runnable comparison exercise와 output range, negative-input handling, large-positive handling의 세 축을 반영했다.
- 번역 반영 상태: 영문 번역 반영 완료
- 원문 기준 버전: `v2026.07.14`
