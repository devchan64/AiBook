# Section Release Note

- Section ID: `P5-6.2`
- Source File: `docs/parts/part-05/chapter-06/section-02.md`

### v2026.07.09
- 변경 이유: Part 5 문단구조 정리 기준에 맞춰 사례/예제 구조 표지를 현재 원칙으로 통일할 필요가 있었다.
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 모드 차이 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.11
- 변경 이유: training/eval mode 절에 실패 유도 실험이 부족했다.
- 본문 반영: dropout 예제 아래에 평가를 학습처럼 다룰 때 무엇이 깨지는지 비교하는 `실패 장면` 표를 추가했다.
- 번역 동기화 메모: Failure-oriented follow-up table added for evaluation misuse cases. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.11`

### v2026.07.11-2
- 변경 이유: 학습 모드와 평가 모드의 차이가 본문 설명만으로는 길게 읽혀, 같은 입력이 어떤 계산 규칙으로 갈라지는지 한눈에 잡기 어려웠다.
- 본문 반영: training mode와 evaluation mode의 계산 흐름을 나란히 비교하는 Mermaid 도식을 추가하고, `업데이트 준비를 위한 흔들림 허용`과 `안정적 측정`의 차이를 한 문장으로 다시 묶었다.
- 번역 동기화 메모: Added paired Mermaid assets for the training-vs-evaluation mode flow and clarified the contrast between stochastic training behavior and stable evaluation behavior. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-06/training-eval-mode-flow-en.mmd`, `docs/assets/part-05/chapter-06/training-eval-mode-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.11-2`

### v2026.07.12
- 변경 이유: training/eval mode 절의 말미 자기 확인 구조가 여러 표지로 나뉘어 있어 기준서의 단일 `체크리스트` 원칙과 맞지 않았다.
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: mode 차이, dropout·batch normalization 민감성, 평가 안정성, optimizer 장으로 이어지는 판단 기준을 `체크리스트` 하나로 재구성했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: training/eval mode 절은 실패 장면 표는 있었지만, 사례 구간의 `사람 기준 -> mode 관점` 직접 대비와 코드 실행 전 예측 장면, 그리고 `덜 나쁜 오해`와 `더 위험한 오해`의 분리가 약해 운영 개념이 설명형으로 남을 위험이 있었다.
- 본문 반영: `사례 및 예시`에 `사람이 먼저 보기 쉬운 기준 / training-evaluation mode 관점` 비교 표와 각 사례별 `덜 나쁜 오해 / 더 위험한 오해 / 더 나은 읽기` 표를 추가했다. `연습 및 예제`에는 `train_run_1`, `train_run_2`, `eval_run`, 모드별 평균이 어떻게 달라질지 코드 전에 예상하게 하는 표를 넣고, 출력 뒤에는 모드 차이를 단순 흔들림이 아니라 해석 규칙 차이로 읽게 하는 비교 해설을 덧붙였다.
- 번역 동기화 메모: Korean source now closes P5-6.2 with a human-vs-mode comparison table, pre-run prediction table, and less-bad-vs-riskier misread comparisons for training/evaluation mode cases. Future translations should preserve the same comparison structure.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
