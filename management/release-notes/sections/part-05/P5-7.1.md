# Section Release Note

- Section ID: `P5-7.1`
- Source File: `docs/parts/part-05/chapter-07/section-01.md`

## 2026-07-07

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: optimizer 학습 루프 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: The optimizer-loop Mermaid now keeps paired `-en` and `-ko` source files for translation reuse. / pending

### v2026.07.12
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: optimizer 정의, 손실·역전파와의 역할 분리, learning rate, 대표 업데이트 규칙 비교로 넘어가는 기준을 `체크리스트` 하나로 통합했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.

### v2026.07.12
- 본문 반영: 사례 1에 `사람 기준 vs optimizer 관점` 표를 추가했다. `연습 및 예제`에는 실행 전 예측 표를 넣고, 각 learning rate에 대해 `updated_prediction`, `updated_loss`까지 함께 출력하도록 바꿨다. 출력 뒤에는 `0.01 / 0.1 / 0.5`를 각각 `너무 작음 / 비교적 적절 / 지나침`으로 읽는 해설 표를 추가했다.
- 번역 동기화 메모: 한국어 기준 원고에 less-bad-vs-worse learning-rate comparison structure was added to the optimizer section. 향후 번역 시 같은 pre-run comparison table과 post-output interpretation table을 유지해야 한다.

### v2026.07.12
- 본문 반영: 세 사례를 `사람이 먼저 보기 쉬운 결과`와 `optimizer 관점에서 실제로 구분해야 할 것`으로 다시 묶는 비교표를 추가했다. 예제 뒤에는 `gradient만 보면 남기 쉬운 해석`과 `optimizer까지 보면 바뀌는 해석`을 learning rate별로 다시 읽는 해설 표를 덧붙였다.
- 번역 동기화 메모: Korean source now ties slow progress, oscillation, and large-model update balance back to one optimizer reading frame, plus a post-output interpretation table. 향후 번역 시 같은 cross-case and post-output comparison structure를 유지해야 한다.

### v2026.07.12
- 본문 반영: `연습 및 예제`의 입력, 코드, 출력 항목을 `pressure_unrecovered`, `target_block_score`, `risk_weight`, `updated_risk_weight`, `updated_block_score` 기준으로 바꾸었다. 해설 문단에도 같은 gradient라도 learning rate에 따라 재기동 차단 점수를 `조금 더 위험하게 읽는 보정`, `거의 맞는 수준의 보정`, `과하게 차단 쪽으로 튀는 보정`으로 갈라 읽게 하는 설명을 추가했다.
- 번역 동기화 메모: Korean source now frames the optimizer learning-rate exercise around restart-block scoring instead of abstract x/target/w labels. Future translations should preserve the same operational naming and overshoot-vs-adequate-vs-too-small reading in the example outputs and interpretation tables.

### v2026.07.12
- 본문 반영: 사례 뒤에 공통 Mermaid 도식 1개를 추가해 `같은 gradient -> update rule이 보폭 결정 -> 너무 작음 / 비교적 적절 / 지나침` 흐름을 한 번에 다시 묶었다. 사례별 표를 다시 반복하지 않고, 세 사례가 공통으로 보여 주는 보폭 차이와 결과 차이만 압축하도록 위치와 설명을 정리했다.
- 번역 동기화 메모: Added a shared optimizer-step Mermaid pair so the Korean manuscript now summarizes the same-gradient but different-step-size contrast with one compact flow. Future translations should preserve the same step-size reading.

### v2026.07.12
- 본문 반영: 예제 코드와 출력의 gradient/update 표기를 `gradient_risk_weight`, `updated_risk_weight` 기준으로 정리하고, 핵심 해설도 같은 용어로 맞췄다.
- 번역 동기화 메모: The Korean optimizer exercise now keeps `risk_weight` terminology consistently through gradient and updated-weight labels instead of leaving residual generic `w` naming. Future translations should preserve this consistent naming.

### v2026.07.13
- 본문 반영: 학습률 설명 아래에 손실 곡선 위에서 너무 작은 보폭, 적절한 보폭, 너무 큰 보폭을 비교하는 SVG 그래프를 추가하고, `gradient 방향이 맞다`와 `업데이트가 적절하다`를 분리해 읽는 설명을 보강했다.
- 번역 동기화 메모: Korean source now uses a localized SVG chart for learning-rate step-size intuition. Future translations should reference the matching English SVG or localized language asset.

### v2026.07.13-flow
- 번역 동기화 메모: The Korean and English optimizer-loop Mermaid assets now show both the step order and the role split among loss, gradient, and optimizer. Future translations should preserve this layered reading.

### v2026.07.13-python-chart
- 번역 동기화 메모: Future translations should preserve the shared Python-generated source for the learning-rate chart and localize only the derived labels.

### v2026.07.13-layout
- 번역 동기화 메모: The paired learning-rate charts now keep only labels that are required to read the loss curve and update steps. Future translations should preserve this chart/body role split.

### v2026.07.14-layout
- 번역 동기화 메모: The learning-rate step-size chart now uses separated label offsets and white label backplates to keep the curve annotations readable in both languages.

### v2026.07.14
- 본문 반영: 예제 출력 뒤에 `learning rate별 업데이트 후 위험 가중치`, `업데이트 후 차단 점수`, `업데이트 후 손실` 그래프를 분리해 추가하고, 같은 gradient 방향과 적절한 update 결과가 같은 말이 아니라는 점을 단계별로 설명했다.
- 번역 동기화 메모: Korean source now uses separate generated charts to trace the formula-based transformation from gradient-scaled update to new score and new loss. Future translations should use the localized assets and preserve the three-step update reading.

### v2026.07.16
- 본문 반영: P5-7.1 사례 구간에서 큰 모델 사례를 제거하고, 느린 학습과 흔들리는 학습 두 사례를 `gradient 계산 여부`가 아니라 `optimizer가 만든 실제 update`를 확인하는 사례로 다시 정리했다. 예제와 해설도 learning rate 자체보다 `gradient -> optimizer update -> 새 예측 -> 새 손실` 흐름이 보이게 문구를 조정했다. 학습 밀도 재검토 후 예제 뒤의 중복 2열 해설 표를 제거해, 그래프와 최종 4열 해석 표가 같은 내용을 반복하지 않게 했다.
- 추가 반영: Part 5 파일 경로 개편에 맞춰 도입부의 P5-6 회수 범위를 `학습 루프, learning/inference, training/evaluation mode` 순서로 수정했다.
- 번역 동기화 메모: Korean source now keeps P5-7.1 focused on the optimizer role: turning gradients into actual parameter updates. Future translations should move the large-model optimizer discussion to the SGD/Adam comparison section.
