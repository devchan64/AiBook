# Section Release Note

- Section ID: `P5-13.3`
- Source File: `docs/parts/part-05/chapter-13/section-03.md`

## 2026-07-07

### v2026.07.07-1
- 변경 이유: 보충학습 도입부의 메타 진행 문구를 줄여 설명 흐름을 직접 연결함.
- 본문 변경: `이 보충학습은...` 형태의 반복 문장과 읽기 순서 소개 문장을 제거하고 목록과 설명이 바로 이어지게 조정함.
- 번역본 메모: 번역본 영향 없음.

### v2026.07.09
- 변경 이유: Part 5 문단구조 정리 기준에 맞춰 사례/예제 구조 표지를 현재 원칙으로 통일할 필요가 있었다.
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.11
- 변경 이유: QKV와 멀티헤드 도식을 모두 Part 5 Mermaid 자산 폴더 기준으로 정리할 필요가 있었다.
- 본문 반영: QKV 흐름 Mermaid를 `assets/part-05/chapter-13/qkv-flow-ko.mmd`로, 멀티헤드 흐름 Mermaid를 `assets/part-05/chapter-13/multihead-flow-ko.mmd`로 include 방식 전환했다.
- 번역 동기화 메모: Keep both English Mermaid source files synchronized with the Korean include targets for future translation pages. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-13/qkv-flow-en.mmd`, `docs/assets/part-05/chapter-13/qkv-flow-ko.mmd`, `docs/assets/part-05/chapter-13/multihead-flow-en.mmd`, `docs/assets/part-05/chapter-13/multihead-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.12
- 변경 이유: QKV·멀티헤드 보충학습의 말미 구조가 `이 보충학습에서 기억할 관점`, `짧은 점검`, `언제...`, `체크리스트`로 분산되어 있었다.
- 본문 반영: QKV 역할, single-head와 multi-head 차이, Transformer 절로 이어지는 이름 회수 기준을 `체크리스트` 하나로 통합했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: QKV·멀티헤드 보충학습은 이름 설명과 간단한 예제가 있었지만, 사례 구간의 `사람 기준 -> single-head -> multi-head` 직접 대비와 코드 실행 전 예측 장면이 약해 학습 밀도 기준으로는 아직 설명형에 가까웠다.
- 본문 반영: `사례 및 예시`에 `사람이 먼저 보기 쉬운 기준 / single-head 관점 / multi-head 관점` 비교 표와 최종 결과 문장을 추가했다. `연습 및 예제`에는 single-head 절충값, head1/head2 편향, `difference_from_single` 방향을 먼저 예상하게 하는 표를 추가했다.
- 본문 반영 추가: 멀티헤드 비교 표 말미의 예제 표현을 `간단한 멀티헤드 비교`로 정리해, 보충학습의 설명 톤을 앞선 운영 문맥 보강과 맞췄다.
- 번역 동기화 메모: Korean source now closes the QKV/multi-head section with a human-vs-single-vs-multi comparison table and a pre-run prediction table for context splitting. 향후 번역 시 same comparison structure를 유지해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: QKV·멀티헤드 절의 도입 예시와 사례가 여전히 환불/배송/번역 문맥에 기대고 있어, 운영 절차·정지 해제·정비 코드 해석 흐름과 직접 연결되기 어려웠다.
- 본문 반영: 도입 짧은 예시를 `정지 해제 요청`과 `압력 복귀 미완료` 문장으로 바꾸고, 사례 1~3도 절차 문서 변환, 운전 보고서 요약, 정비 코드 이해 기준으로 다시 적었다.
- 번역 동기화 메모: Korean source now frames the QKV/multi-head section around shutdown-release procedure text, operating-report summarization, and maintenance code reading instead of refund/shipping/translation examples. 향후 번역 시 same operational case set을 유지해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: QKV와 multi-head 절은 운영 문맥 사례를 갖추고 있었지만, QKV 이름 자체가 현장 문장 안에서 무엇을 가리키는지, multi-head 출력 차이가 실제 판단 분리로 어떻게 읽히는지까지는 아직 약했다.
- 본문 반영: QKV 도입부에 `정지 해제`, `압력 복귀`, 교대 인수인계, 정비 코드`를 기준으로 query/key/value를 직접 대응시키는 표를 추가했다. `연습 및 예제`에는 간단한 벡터가 각각 `정지 결정`, `압력 이상 근거`, `복귀 조건`을 뜻한다는 설명을 넣고, single-head와 multi-head 차이를 `결정 vs 조건` 판단 분리로 읽는 비교 표와 해설 문단을 보강했다.
- 본문 반영 추가: 말미 요약에서도 QKV를 `무엇을 찾는가 / 어떤 표지와 맞는가 / 무엇을 실제로 가져오는가`, multi-head를 `결정·근거·조건 같은 다른 관점을 더 오래 유지하는 구조`로 다시 닫았다.
- 본문 반영 추가 2: `회의록`, `번역`, `문서 요약`처럼 남아 있던 일반 사례 표기를 `교대 인수인계`, `절차 문서 변환`, `정비 코드 이해` 기준으로 다시 맞췄다. single-head와 multi-head 비교 표, 사람 기준 비교 표, 공통 결과 문장도 같은 운영 문서·코드 해석 축으로 정리해 보충학습 전체의 용어 기준선을 더 단단히 맞췄다.
- 본문 반영 추가 3: 세 사례 각각에 `그래서 이 사례에서 확인해야 할 결과` 문장을 추가해 절차 문서의 관계 범위, 운전 보고서의 결론/근거/조건, 코드의 정의-사용/조건-결과가 서로 다른 head 관점으로 함께 유지되는지를 명시적으로 닫았다.
- 본문 반영 추가 4: 사례 구간을 다시 다듬어 `먼 위치를 다시 본다` 일반론보다 `결정·근거·조건`과 `정의-사용·조건-결과`처럼 관계 종류를 나눠 유지하는 장면이 먼저 보이도록 정리했다. 이에 따라 사례 2는 `운전 보고서 요약`에서 `교대 판단 문장 정리`로 더 좁혔고, 사례 도입 문장과 비교 표도 `single-head 절충` 대 `multi-head 관계 분리`가 더 직접 드러나게 보강했다.
- 번역 동기화 메모: Korean source now makes the multi-head section more explicit about preserving different relation types, especially decision-ground-condition and definition-use vs condition-result splits. Future translations should keep that relation-splitting emphasis instead of collapsing back to a generic long-context rereading story.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 반복 검토 기준으로 다시 보면 본문과 예제는 좋아졌지만, single-head 절충과 multi-head 관계 분리를 같은 입력 위에 짧게 병치하는 기준선 도식이 부족했다.
- 본문 반영: multi-head 흐름도 아래에 `같은 판단 문장 -> single-head 절충 / multi-head 관계 분리` 비교 Mermaid를 추가해 결정·근거·조건 분화를 더 직접적으로 고정했다.
- 번역 동기화 메모: Korean source now includes a short single-head vs multi-head baseline figure. Future translations should preserve the same same-input / relation-splitting contrast with localized labels.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-13/single-vs-multihead-baseline-en.mmd`, `docs/assets/part-05/chapter-13/single-vs-multihead-baseline-ko.mmd`
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12-asset
- 변경 이유: chapter 13 자산 README가 아직 예전 `한국어 본문이 -ko.mmd를 include하고 영문은 향후 번역용 원본으로 둔다`는 표현에 머물러 있어, 현재 차트 가이드라인의 `배포 언어별 본문이 자기 언어 자산을 직접 참조` 원칙을 충분히 드러내지 못했다.
- 자산 운영 반영: `docs/assets/part-05/chapter-13/README.md`에 한국어/영어 공개 본문이 각자 `-ko.mmd`, `-en.mmd` 자산을 직접 참조하고, 영문 `-en.mmd`를 기준 원본으로 먼저 수정한 뒤 한국어 파생본을 같은 conceptual flow와 node relationships로 동기화한다는 운영 원칙을 명시했다.
- 번역 동기화 메모: Chapter 13 asset-operation wording now follows the current per-language reference rule while keeping English Mermaid files as canonical originals. Future translation work should preserve the same asset split and synchronization order.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-13/README.md`
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12-diagram
- 변경 이유: `P5-13.3` 한국어 본문이 참조하는 Mermaid 도식 내부에 `current token query`, `compare with all keys`, `same input tokens`, `combine heads` 같은 영문 라벨이 남아 있어, 현재 도식 한글화 원칙과 어긋났다.
- 자산 반영: `qkv-flow-ko.mmd`의 노드 라벨을 `현재 토큰의 질문`, `모든 키와 비교하기`, `더 잘 맞는 위치에 더 큰 비중`, `값들을 섞기`, `새 토큰 표현`으로 정리했다. `multihead-flow-ko.mmd`도 `같은 입력 토큰`, `한 관계 패턴`, `다른 관계 패턴`, `head 결과 합치기`, `더 풍부한 토큰 표현` 기준으로 한글화했다.
- 번역 동기화 메모: Korean diagram labels were localized to match the deployed manuscript language while preserving the same structure as the English source assets. Future translations should keep English originals as canonical and localize only the visible labels per manuscript language.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-13/qkv-flow-ko.mmd`, `docs/assets/part-05/chapter-13/multihead-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.12`

- 변경 이유: 반복 재독 기준으로 다시 보니 `P5-13.3`은 single-head 절충과 multi-head 관계 분리를 말로는 충분히 설명하지만, 사례 2를 같은 입력 위에서 다시 접어 주는 소형 도식과 잔여 한글화가 더 있으면 초심자 회수력이 좋아졌다.
- 본문 반영: 사례 2 아래에 `같은 판단 문장 -> 싱글 헤드 절충 -> 멀티헤드 관계 분리` Mermaid를 추가했다.
- 자산 반영: `multihead-flow-ko.mmd`의 `head` 표기를 `헤드`로, `single-vs-multihead-baseline-ko.mmd`의 `single-head`, `multi-head`, `head 1/2`를 각각 `싱글 헤드`, `멀티헤드`, `헤드 1/2`로, `self-attention-token-graph-ko.mmd`의 `token 1~4`를 `토큰 1~4`로 정리했다. 새 사례 도식 쌍 `multihead-decision-condition-case-flow-en/ko.mmd`도 추가했다.
- 번역 동기화 메모: Added a case-level same-input Mermaid for the decision-ground-condition split and localized the remaining Korean token/head labels. Future translations should preserve the same case-level contrast and keep visible terminology localized consistently.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-13/multihead-decision-condition-case-flow-en.mmd`, `docs/assets/part-05/chapter-13/multihead-decision-condition-case-flow-ko.mmd`, `docs/assets/part-05/chapter-13/multihead-flow-ko.mmd`, `docs/assets/part-05/chapter-13/single-vs-multihead-baseline-ko.mmd`, `docs/assets/part-05/chapter-13/self-attention-token-graph-ko.mmd`, `docs/assets/part-05/chapter-13/README.md`
- 원문 기준 버전: `v2026.07.12`

### v2026.07.14
- 변경 이유: P5-13.3의 Python 예제가 single-head와 multi-head 차이를 한 번 확인하는 검증형에 가까워, head 가중치 변화에 따른 관계 분리 민감도를 직접 실험하는 역할은 약했다.
- 본문 반영: `연습 및 예제`를 세 가지 head 시나리오(`balanced_heads`, `decision_vs_condition_split`, `condition_heavy_both_heads`) 비교 실험으로 바꾸고, `head_separation`과 `difference_from_single`을 함께 출력하도록 확장했다.
- 본문 반영 추가: 코드 뒤 해설도 `head 수가 아니라 head가 실제로 얼마나 다른 관계를 읽는가`를 읽게 하는 방향으로 다시 정리하고, 독자가 직접 바꿔 볼 값·관찰할 출력·해석할 질문 표를 추가했다.
- 본문 반영 추가 2: Python 예제 출력 뒤에 `head_separation` 비교와 single/head1/head2 문맥 위치를 각각 분리 PNG 그래프로 추가했다. 두 그래프는 본문 예제의 토큰 표현, single-head 가중치, 세 가지 head 시나리오를 그대로 사용한다.
- 추가 반영: 연습 및 예제의 벡터 표현을 `간단한 벡터`로 정리하고, 두 PNG 그래프의 내부 제목을 제거해 본문 캡션과 그래프의 역할이 겹치지 않게 했다.
- 번역 동기화 메모: Korean source now treats the QKV/multi-head Python block as an experiment-oriented scenario comparison instead of a one-shot verification. Future translations should preserve the same scenario names, separation metric, tweak-observe-interpret structure, and split chart sequence.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-13/p5_13_3_qkv_multihead_charts.py`, `docs/assets/part-05/chapter-13/qkv-head-separation-en.png`, `docs/assets/part-05/chapter-13/qkv-head-separation-ko.png`, `docs/assets/part-05/chapter-13/qkv-head-context-space-en.png`, `docs/assets/part-05/chapter-13/qkv-head-context-space-ko.png`
- 원문 기준 버전: `v2026.07.14`
