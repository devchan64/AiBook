# Section Release Note

- Section ID: `P5-9.1`
- Source File: `docs/parts/part-05/chapter-09/section-01.md`

### v2026.07.14
- 변경 이유: GPU 병렬 처리 예제는 `scores_one_by_one`, `scores_batch`, `scalar multiply count`, `if batch size doubles`를 출력하지만, 텍스트 출력만으로는 같은 결과와 증가하는 반복 계산 규모를 분리해 읽기 어려웠다.
- 본문 반영: 예제 출력 뒤에 라인별 위험 점수 비교 그래프와 batch 크기 증가에 따른 scalar multiply count 그래프를 각각 분리 PNG로 추가했다.
- 번역 동기화 메모: 향후 번역 시 `same score`와 `rising compute count` 산출물 그래프 순서와 해설 구조를 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-09/p5_9_1_gpu_parallel_charts.py`, `docs/assets/part-05/chapter-09/gpu-batch-score-comparison-en.png`, `docs/assets/part-05/chapter-09/gpu-batch-score-comparison-ko.png`, `docs/assets/part-05/chapter-09/gpu-scalar-multiply-scaling-en.png`, `docs/assets/part-05/chapter-09/gpu-scalar-multiply-scaling-ko.png`
- 원문 기준 버전: `v2026.07.14`

## 2026-07-07

### v2026.07.07-1
- 변경 이유: 설명보다 메타 진행 문구가 앞서는 도입부를 정리함.
- 본문 변경: 범위 도입의 반복 문구와 `다음 한 문장으로 기억하면 충분합니다.`를 제거해 서술이 바로 이어지게 조정함.
- 번역본 메모: 번역본 영향 없음.

### v2026.07.09
- 변경 이유: Part 5 문단구조 정리 기준에 맞춰 사례/예제 구조 표지를 현재 원칙으로 통일할 필요가 있었다.
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.11
- 변경 이유: GPU와 병렬 처리 절에서 CPU식 순차 계산과 GPU식 병렬 계산의 감각 차이를 한눈에 비교하는 도식이 없어, 본문 비유가 시각적으로 고정되지 않았다.
- 본문 반영: 병렬 처리 설명 뒤에 CPU식 순차 처리와 GPU식 병렬 처리를 대비하는 Mermaid 도식을 추가하고, GPU의 핵심을 `같은 계산을 동시에 배치하기 쉬운 구조`로 다시 짚는 문장을 보강했다.
- 번역 동기화 메모: Added paired Mermaid assets for the CPU-vs-GPU parallel-processing comparison and clarified the visual distinction between sequential and parallel execution. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-09/cpu-gpu-parallel-flow-en.mmd`, `docs/assets/part-05/chapter-09/cpu-gpu-parallel-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.12
- 변경 이유: GPU 절의 말미 자기 확인 구조가 `기억할 관점`, `짧은 점검`, `언제...`, `체크리스트`로 분산되어 기준서와 맞지 않았다.
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: GPU 병렬 처리의 의미, 계산 자원과 모델 확산의 연결, 실험 규모 판단 기준을 `체크리스트` 하나로 통합했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: 작은 사례와 연습 예제에 남아 있던 고객 점수화 표현이 Part 5의 운영 설비 사례 축과 어긋나 있었다.
- 본문 반영: 사례 1을 라인별 에너지 사용량 추정과 경보 우선순위 분류 맥락으로 다시 쓰고, 연습 예제의 배치 설명도 고객 배치에서 라인 배치와 라인별 위험 점수로 바꿨다.
- 번역 동기화 메모: The small CPU-usable example and batch-matrix exercise now use line-level operational scoring instead of customer scoring. Preserve that wording shift in future translations.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: 추가 재검토 결과, 기존 예제는 배치 행렬 계산이 같은 결과를 낸다는 점은 보여 주지만 GPU의 가치를 `실험 가능성` 차이로 읽게 하는 압력은 약했다.
- 본문 반영: 큰 이미지 모델 사례에 `사람 기준 vs GPU 관점` 표를 추가했다. `연습 및 예제`에는 실행 전 예측 표를 넣고, 출력에 `if batch size doubles, estimated scalar multiplies`를 추가해 반복 계산 규모가 바로 커지는 장면을 보강했다. 출력 뒤에는 `결과 동일성 / 반복 계산 증가 / 실험 가능성` 해설 표를 덧붙였다.
- 본문 반영 추가: 사례 1 제목과 설명을 `작은 장난감 모델`에서 `작은 업무 점수화 모델` 기준으로 바꿔, GPU 필요성 비교가 실제 운영 데이터 실험 맥락으로 읽히게 정리했다.
- 본문 반영 추가: 체크리스트의 `작은 장난감 모델 단계` 표현을 `작은 업무 실험 단계`로 바꿔, 절 전체 용어를 운영 실험 맥락으로 통일했다.
- 번역 동기화 메모: 한국어 기준 원고에 same-result-vs-rising-compute comparison structure was added to the GPU section. 향후 번역 시 같은 pre-run table, estimated compute scaling, post-output interpretation table을 유지해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: GPU 절의 사례들이 각각은 타당했지만, 작은 업무 실험, 큰 이미지 모델, LLM을 하나의 `실험 가능성` 기준으로 다시 묶는 압력이 약했다.
- 본문 반영: 세 사례를 `사람이 먼저 보기 쉬운 결과`와 `GPU 관점에서 실제로 구분해야 할 것`으로 다시 묶는 비교표를 추가했다. 예제 뒤에는 `결과만 보면 남기 쉬운 해석`과 `GPU 관점까지 보면 바뀌는 해석`을 `same result / 반복 계산 증가 / CPU vs GPU 감각` 기준으로 다시 읽는 해설 표를 덧붙였다.
- 번역 동기화 메모: Korean source now ties the GPU cases back to one cross-case frame and adds a post-output interpretation table separating same-result intuition from compute-scaling and experiment-feasibility reading. 향후 번역 시 같은 cross-case and post-output comparison structure를 유지해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: GPU 예제가 반복 계산 증가를 보여 주고 있었지만, 그 차이가 실제 실험 운영에서 무엇을 포기하게 하고 무엇을 계속 시도하게 만드는지까지는 충분히 닫히지 않았다.
- 본문 반영: `연습 및 예제`에 `CPU식 읽기만 하면 쉬운 판단 vs GPU 관점까지 읽고 바뀌는 판단` 비교 표를 추가했다. 여기서 batch 크기 조정, 하이퍼파라미터 탐색, 모델 규모 확장을 각각 `실험 설계 가능 범위` 관점으로 다시 읽게 했다.
- 본문 반영 추가: 이어지는 해설 문단에서는 GPU 절의 핵심을 단순 속도 차이가 아니라 `어떤 batch와 어떤 설정까지 실제로 반복 실험이 가능한가`를 먼저 따지게 만드는 전환으로 명시했다.
- 번역 동기화 메모: Korean source now closes the GPU exercise with an explicit experiment-feasibility interpretation, not just same-result vs rising-compute comparison. 향후 번역 시 same experiment-operations interpretation pressure를 유지해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.13-layout
- 변경 이유: CPU/GPU 병렬 처리 비교 Mermaid가 GPU 쪽 입력 샘플을 가로로 넓게 펼쳐, 본문에서 도식이 작게 렌더링되는 문제가 있었다.
- 자산 반영: `cpu-gpu-parallel-flow` 한영 Mermaid 자산에 CPU/GPU 블록의 세로 읽기 순서를 고정하고, GPU 입력 샘플 묶음을 2행 구조로 접어 전체 폭을 줄였다.
- 번역 동기화 메모: The Korean and English CPU-vs-GPU Mermaid assets now use a stacked vertical comparison and a folded sample-group layout to avoid overly wide rendering. Future translations should preserve this compact vertical reading.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-09/cpu-gpu-parallel-flow-en.mmd`, `docs/assets/part-05/chapter-09/cpu-gpu-parallel-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.13`
