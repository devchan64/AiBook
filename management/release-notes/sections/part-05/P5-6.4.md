# Section Release Note

- Section ID: `P5-6.4`
- Source File: `docs/parts/part-05/chapter-06/section-04.md`

### v2026.07.14
- 본문 반영: 새 `P5-6.4`를 추가해 이전 층 활성값이 3개 층을 지나는 작은 Python 실험을 제시했다. `small_init`, `medium_init`, `large_init`, `very_large_init`을 비교하며 raw activation 범위, raw variance, batch normalization 뒤 범위를 출력하게 했다.
- 번역 동기화 메모: Korean source adds P5-6.4 as the concrete deep-scale experiment following the P5-6.3 stabilization overview. Future translations should preserve the same example values and split-chart sequence.

### v2026.07.15
- 본문 반영: 스칼라 가중치를 `각 층 가중치 스케일을 단순화해 대표한 장난감 실험값`으로 설명하는 경계 문장을 추가했다. `weight_cases`, `layer` 반복 횟수, 입력 활성값, `eps`를 바꿔 볼 실험 표도 추가해 예제를 더 명시적인 실험형으로 정리했다.
- 번역 동기화 메모: Korean source now frames P5-6.4 more explicitly as a toy scale-propagation experiment with clear tweak-observe prompts, and avoids overstating batch-normalization or small-scale conclusions. Future translations should preserve that experiment boundary and caution level.
