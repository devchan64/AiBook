# Section Release Note

- Section ID: `P5-13.2`
- Source File: `docs/parts/part-05/chapter-13/section-02.md`

### v2026.07.20
- 변경 이유: 기존 Python 예제가 5개 토큰과 하드코딩 점수·벡터에 머물러, P5-13.1의 CSV 기반 attention 예제와 비교할 때 self-attention의 토큰별 재참조 실험 밀도가 약해 보일 수 있었다.
- 본문 반영: 한국어 원문의 메타데이터를 blockquote 형식으로 정리하고 Version을 `v2026.07.20`으로 갱신했다. Python 예제를 `self-attention-safety-memo-candidates.csv`를 읽는 구조로 바꾸고, 3개 안전 메모 시나리오·6개 현재 토큰 조건·36개 후보 토큰 행을 기반으로 `그것`과 `씌우지`의 재참조 분포와 representation shift를 비교하게 했다.
- 추가 반영: 한국어 원문의 CSV 입력 파일 링크에 `{ .csv-preview }` 표시를 붙여, 원본 링크를 유지하면서 `내용 보기` 미리보기 대상임을 명시했다.
- 관련 자산: `docs/assets/part-05/chapter-13/self-attention-safety-memo-candidates.csv`, `docs/assets/part-05/chapter-13/p5_13_attention_chart.py`, `docs/assets/part-05/chapter-13/self-attention-weight-it-ko.svg`, `docs/assets/part-05/chapter-13/self-attention-weight-cover-ko.svg`
- 번역 동기화 메모: 영어·중국어 간체 번역본을 한국어 원문 기준 `v2026.07.20`으로 갱신했다. CSV 입력 설명, `내용 보기` 미리보기 링크, 3개 안전 메모 시나리오·6개 현재 토큰 조건·36개 후보 토큰 행, `그것`/`씌우지`의 token-specific rereading 해석, 새 출력값과 결합 가중치 `0.644`/`0.55`를 번역본에 반영했다.
- 번역 자산 반영: 영어·중국어 간체 본문이 `self-attention-safety-memo-candidates.csv`와 언어별 `self-attention-weight-it-*`, `self-attention-weight-cover-*` SVG를 상대 경로로 참조하도록 맞췄다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.20`
- 추가 반영: `self-attention-target-shift-{ko,en,zh}.mmd`를 CSV 예제 수치와 맞춰 보강해, 같은 입력·같은 후보 토큰 묶음에서 현재 토큰이 `그것`인지 `씌우지`인지에 따라 다시 크게 보는 위치와 새 표현 방향이 갈라진다는 점을 도식 안에서 바로 볼 수 있게 했다.

### v2026.07.20-python-comment-audit
- 본문 반영 내용: 한국어, 영어, 중국어 간체판의 Python 예제 첫 줄에 같은 메모 안에서도 현재 토큰이 달라지면 self-attention이 다시 참고하는 단서와 새 표현이 어떻게 달라지는지 비교하는 목적을 설명하는 주석을 추가했다.
- 저작권 검토: Part 5 Module 5 순차 검토 범위에서 self-attention 흐름, 현재 토큰별 재참조 우선순위, 관계 재계산 사례와 그래프를 확인했다. 사례·표·예제 코드는 자체 작성 자료로 판단했다.
- 출처 사용 점검: Transformer 논문, Bahdanau et al., Goodfellow et al.은 self-attention과 attention 배경 확인 용도로만 연결되어 있으며, 원문 도식·표·코드·문단 재사용 정황은 찾지 못했다.
- 관련 자산: self-attention Mermaid와 `self-attention-*.svg` 계열 자산은 자체 도식과 로컬 생성 스크립트 기반 자산으로 판단했다.
- 당시 본문 반영: 없음. 이 감사 항목 시점에는 원문 본문 버전 `v2026.07.19`를 유지했다.
- 당시 번역 동기화 메모: 영어·중국어 간체 번역본은 원문 본문 기준 버전 `v2026.07.19`까지 반영했다. 이후 CSV 기반 예제 확장은 위의 `v2026.07.20` 항목에서 별도로 반영했다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.19`

### v2026.07.19
- 근거 보강: Transformer와 attention 출처에 NeurIPS/arXiv 원문 링크와 확인 날짜를 추가했다.
- 번역 동기화 메모: 향후 번역본 작성 시 같은 출처 링크와 확인 날짜를 반영해야 한다.

### v2026.07.19
- 참조 갱신: P5-14 병합 구조에 맞춰 Transformer 전체 구성 handoff를 P5-14.1부터 P5-14.6까지의 본편으로 갱신했다.

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: `self-attention-token-graph-ko.mmd`, `attention-to-self-attention-bridge-ko.mmd` include 방식으로 전환했다.
- 번역 동기화 메모: Keep the English Mermaid source pair aligned with the Korean public include target.

### v2026.07.12
- 본문 반영: self-attention 정의, 순차 전달과의 차이, 병렬 참조 장점, Transformer 장으로 이어지는 기준을 `체크리스트` 하나로 통합했다. `연습 및 예제`를 안전 점검 메모 문맥으로 교체하고, `사람이 먼저 보기 쉬운 기준 vs self-attention 관점` 표와 실행 전 예측 표를 추가했다. 현재 토큰을 `그것`과 `씌우지` 두 개로 늘려 같은 메모 안에서도 target token이 달라지면 weight와 representation shift가 달라지는 장면을 직접 비교하게 만들었고, 출력 해설도 `토큰별 재참조 분포`를 중심으로 다시 정리했다.
- 번역 동기화 메모: Korean source now reinforces the self-attention introduction with operational safety memos, shift-handoff summarization, and maintenance-code interpretation while preserving the same-token-vs-different-target-token comparison pressure. Future translations should preserve the same operations-oriented framing and token-specific decision interpretation.

### v2026.07.12
- 본문 반영: 지시어 해석, 인수인계 요약, 코드 이해 세 사례 각각에 `그래서 이 사례에서 확인해야 할 결과` 문장을 추가하고, 세 사례 공통의 `토큰마다 다시 참고할 대상이 달라진다`는 총괄 문장도 보강했다.
- 번역 동기화 메모: Korean source now closes each self-attention case with an explicit result sentence and narrows cases 2 and 3 to same-sequence token-relation reading, so future translations should preserve that token-internal relation emphasis instead of drifting back toward broader document-level rereading scenes.
- 본문 반영: self-attention 토큰 그래프 뒤에 `그것`과 `씌우지`가 같은 메모 안에서 서로 다른 재참조 분포를 만든다는 Mermaid 도식을 추가했다.

### v2026.07.13
- 본문 반영: target-token shift 도식 뒤에 `그것`과 `씌우지`의 attention weight를 나란히 비교하는 SVG 막대 그래프를 추가하고, self-attention을 문장 전체의 공통 평균이 아니라 토큰별 재읽기로 읽는 설명을 보강했다.
- 번역 동기화 메모: Korean source now uses a localized SVG chart for token-specific self-attention weight intuition. Future translations should reference the matching English SVG or localized language asset.

### v2026.07.13-python-chart
- 번역 동기화 메모: Future translations should preserve the shared Python-generated source for the self-attention chart and localize only the derived labels.

### v2026.07.14-layout
- 본문 반영: P5-13.2 본문에서 결합 비교 차트 1개 대신 `self-attention-weight-it`, `self-attention-weight-cover` 개별 SVG 2개를 순서대로 배치했다.
- 추가 반영: 예제 해설의 벡터 비교 표현을 `간단한 벡터 비교`로 정리하고, 그래프 내부 제목은 본문 캡션과 역할이 겹치므로 제거했다.
- 번역 동기화 메모: Future translations should preserve the split two-chart layout for the self-attention weight example and keep deriving both charts from the shared Python chart source.

### v2026.07.17
- 본문 반영: `사례 및 예시`를 지시어·조건 범위·코드 조건식 3개 사례의 긴 나열에서, 지시어 해석 대표 사례와 도메인 확장 비교표 구조로 다시 묶어 현재 절의 중심 질문이 `현재 토큰마다 다시 참고할 대상이 달라지는가`에 더 오래 머물도록 정리했다.
- 본문 반영: `이 절의 범위`에서 query/key/value 공식 유도, multi-head 구현, positional encoding 수식 상세 같은 바깥 항목 목록을 제거하고, Transformer 본문 `P5-14.1`, `P5-14.2`와 보충학습 `P5-13.3` handoff만 남기도록 정리했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff structure where this section closes same-sequence rereading intuition first and hands QKV naming plus Transformer block follow-up to later sections.

### v2026.07.18
- 본문 반영: 앞부분의 `지금 이 절에서 읽는 것`, `이 절을 읽는 순서`를 제거하고, `attention과 self-attention 차이 -> 현재 토큰별 재참조 -> 왜 이것이 블록 중심 계산이 되었는가` 흐름이 먼저 보이도록 재배치했다.
- 본문 반영: self-attention을 `문장 전체를 본다`가 아니라 `현재 토큰마다 다시 참고할 위치를 계산한다`는 구조로 더 분명하게 읽히도록 차이 설명 문단을 보강했다.
- 본문 반영: 말미의 반복 전환 표를 걷어내고, Transformer handoff는 `토큰 표현 재계산이 블록 중심이 되었다`는 결론 뒤의 짧은 연결만 남겼다.
- 번역 동기화 메모: Future translations should preserve the reduced meta-structure, the stronger token-specific rereading emphasis, and the shorter Transformer handoff at the end.

## 2026-07-18

- 영문 번역 반영: `docs/parts/part-05/chapter-13/section-02.en.md`를 추가해 attention과 self-attention의 차이, 토큰별 재참조 직관, 병렬 처리 장점, 안전 메모 기반 self-attention 예제를 영어판 본문으로 동기화했다.
- 번역 자산 반영: 본문에서 `attention-to-self-attention-bridge-en.mmd`, `self-attention-token-graph-en.mmd`, `self-attention-target-shift-en.mmd`, `self-attention-weight-it-en.svg`, `self-attention-weight-cover-en.svg`를 참조하도록 맞췄다.
- 번역 동기화 메모: English section now matches the Korean v2026.07.18 structure and keeps the stronger token-specific rereading emphasis before the short Transformer handoff.

### v2026.07.18-zh
- 번역 동기화 메모: `docs/parts/part-05/chapter-13/section-02.zh.md` was added and aligned to the Korean source structure, preserving the token-specific rereading emphasis, the same-sequence relationship shift, and the safety-memo self-attention example.
- 번역 반영 상태: 중국어 번역 반영 완료
- 관련 자산: `docs/assets/part-05/chapter-13/attention-to-self-attention-bridge-zh.mmd`, `docs/assets/part-05/chapter-13/self-attention-token-graph-zh.mmd`, `docs/assets/part-05/chapter-13/self-attention-target-shift-zh.mmd`, `docs/assets/part-05/chapter-13/self-attention-weight-it-zh.svg`, `docs/assets/part-05/chapter-13/self-attention-weight-cover-zh.svg`
- 원문 기준 버전: `v2026.07.18`
