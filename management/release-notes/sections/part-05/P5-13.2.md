# Section Release Note

- Section ID: `P5-13.2`
- Source File: `docs/parts/part-05/chapter-13/section-02.md`

### v2026.07.09
- 변경 이유: Part 5 문단구조 정리 기준에 맞춰 사례/예제 구조 표지를 현재 원칙으로 통일할 필요가 있었다.
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.11
- 변경 이유: self-attention 관계 도식과 attention에서 self-attention으로 참조 범위가 확장되는 전환 도식을 Part 5 공통 Mermaid 자산 구조로 정리할 필요가 있었다.
- 본문 반영: `self-attention-token-graph-ko.mmd`, `attention-to-self-attention-bridge-ko.mmd` include 방식으로 전환했다.
- 번역 동기화 메모: Keep the English Mermaid source pair aligned with the Korean public include target.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-13/self-attention-token-graph-en.mmd`, `docs/assets/part-05/chapter-13/self-attention-token-graph-ko.mmd`, `docs/assets/part-05/chapter-13/attention-to-self-attention-bridge-en.mmd`, `docs/assets/part-05/chapter-13/attention-to-self-attention-bridge-ko.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.12
- 변경 이유: self-attention 절의 말미 회상·적용 시점·점검 구조가 중복되어 기준서 위반 상태였고, 예제는 `그것` 토큰 하나의 재참조는 잘 보여 주지만 `각 토큰이 자기 입장에서 다른 비중을 만든다`는 핵심을 독자가 직접 비교할 장면은 아직 약했다.
- 본문 반영: self-attention 정의, 순차 전달과의 차이, 병렬 참조 장점, Transformer 장으로 이어지는 기준을 `체크리스트` 하나로 통합했다. `연습 및 예제`를 안전 점검 메모 문맥으로 교체하고, `사람이 먼저 보기 쉬운 기준 vs self-attention 관점` 표와 실행 전 예측 표를 추가했다. 현재 토큰을 `그것`과 `씌우지` 두 개로 늘려 같은 메모 안에서도 target token이 달라지면 weight와 representation shift가 달라지는 장면을 직접 비교하게 만들었고, 출력 해설도 `토큰별 재참조 분포`를 중심으로 다시 정리했다.
- 본문 반영 추가: 도입 짧은 예시를 `배터리팩`/`절연캡` 문장으로 바꾸고, 사례 1과 2를 안전 점검 메모와 교대 인수인계 기록 기준으로 다시 적었다. 이어서 Chapter 13 앞부분의 attention 대비 표에서도 `번역 출력 한 단어`, `문서 요약` 같은 일반 표기를 `다국어 작업 지시 문구`, `교대 인수인계 요약`, `정비 코드 해석` 기준으로 바꾸어 현재 학습 장면을 운영 문서·정비 판단 축으로 다시 고정했다.
- 번역 동기화 메모: Korean source now reinforces the self-attention introduction with operational safety memos, shift-handoff summarization, and maintenance-code interpretation while preserving the same-token-vs-different-target-token comparison pressure. Future translations should preserve the same operations-oriented framing and token-specific decision interpretation.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: self-attention 절은 사례 장면은 충분했지만, 각 사례가 `현재 토큰이 무엇을 다시 참고하는가`를 결과 문장으로 명시적으로 닫지 않아 전달력이 약해질 수 있었다.
- 본문 반영: 지시어 해석, 인수인계 요약, 코드 이해 세 사례 각각에 `그래서 이 사례에서 확인해야 할 결과` 문장을 추가하고, 세 사례 공통의 `토큰마다 다시 참고할 대상이 달라진다`는 총괄 문장도 보강했다.
- 본문 반영 추가: 후속 정리에서 사례 2와 사례 3을 `교대 인수인계 요약`, `코드 이해`에서 각각 `한 문장 안 조건 범위 해석`, `코드 한 줄 조건 해석`으로 더 좁혀, self-attention이 문서 전체 재참조보다 `같은 시퀀스 안 토큰 관계를 다시 계산하는 구조`라는 점이 먼저 보이도록 재구성했다.
- 번역 동기화 메모: Korean source now closes each self-attention case with an explicit result sentence and narrows cases 2 and 3 to same-sequence token-relation reading, so future translations should preserve that token-internal relation emphasis instead of drifting back toward broader document-level rereading scenes.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 반복 검토 기준으로 다시 보면 사례와 예제는 좋아졌지만, 같은 입력 문장 안에서 `현재 토큰이 달라지면 다시 보는 위치도 달라진다`는 핵심을 짧게 병치하는 기준선 도식이 부족했다.
- 본문 반영: self-attention 토큰 그래프 뒤에 `그것`과 `씌우지`가 같은 메모 안에서 서로 다른 재참조 분포를 만든다는 Mermaid 도식을 추가했다.
- 번역 동기화 메모: Korean source now includes a short target-token shift figure for self-attention. Future translations should preserve the same same-input / different-target-token contrast with localized labels.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-13/self-attention-target-shift-en.mmd`, `docs/assets/part-05/chapter-13/self-attention-target-shift-ko.mmd`
- 원문 기준 버전: `v2026.07.12`

### v2026.07.13
- 변경 이유: P5 전체 그래프 검토 결과, self-attention 절은 현재 토큰별 attention 비중 분포가 핵심인데 기존 Mermaid 도식과 코드 출력만으로는 `같은 문장, 다른 현재 토큰, 다른 재참조 분포`가 충분히 시각화되지 않았다.
- 본문 반영: target-token shift 도식 뒤에 `그것`과 `씌우지`의 attention weight를 나란히 비교하는 SVG 막대 그래프를 추가하고, self-attention을 문장 전체의 공통 평균이 아니라 토큰별 재읽기로 읽는 설명을 보강했다.
- 번역 동기화 메모: Korean source now uses a localized SVG chart for token-specific self-attention weight intuition. Future translations should reference the matching English SVG or localized language asset.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-13/self-attention-weights-en.svg`, `docs/assets/part-05/chapter-13/self-attention-weights-ko.svg`, `docs/assets/part-05/chapter-13/README.md`
- 원문 기준 버전: `v2026.07.13`

### v2026.07.13-python-chart
- 변경 이유: self-attention weight 비교는 축과 수치 막대가 핵심인 그래프라 수동 SVG보다 Python 생성형으로 관리하는 편이 값 조정과 다국어 파생 관리에 적합했다.
- 자산 반영: `self-attention-weights-en.svg`, `self-attention-weights-ko.svg`를 `p5_13_attention_chart.py`에서 생성하도록 전환했다.
- 운영 메모: Chapter 13 README에 Python 그래프 원본을 기록했다.
- 번역 동기화 메모: Future translations should preserve the shared Python-generated source for the self-attention chart and localize only the derived labels.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-13/p5_13_attention_chart.py`, `docs/assets/part-05/chapter-13/self-attention-weights-en.svg`, `docs/assets/part-05/chapter-13/self-attention-weights-ko.svg`, `docs/assets/part-05/chapter-13/README.md`
- 원문 기준 버전: `v2026.07.13`

### v2026.07.14-layout
- 변경 이유: self-attention 비중 비교는 두 현재 토큰의 차이를 보여 주는 데는 충분했지만, 각 토큰의 재참조 분포를 더 크게 읽기에는 패널 폭이 좁았다.
- 본문 반영: P5-13.2 본문에서 결합 비교 차트 1개 대신 `self-attention-weight-it`, `self-attention-weight-cover` 개별 SVG 2개를 순서대로 배치했다.
- 자산 반영: `p5_13_attention_chart.py`가 기존 결합 차트와 함께 두 개별 현재 토큰 차트도 생성하도록 확장했고, Chapter 13 자산 README 목록을 갱신했다.
- 추가 본문 반영: 본문 Python 예제 출력과 그래프 생성 스크립트가 서로 다른 stale self-attention weight를 사용하던 문제를 바로잡았다. 이제 그래프는 본문 예제의 `raw_scores_by_target`을 그대로 softmax 계산해 생성하며, `씌우지` target의 출력 예시도 실제 실행값인 `weights = [0.074, 0.272, 0.406, 0.165, 0.082]`, `cap_plus_not_applied_weight = 0.571`로 맞췄다.
- 추가 자산 반영: 그래프 색상은 최대 attention 비중 토큰을 진한 파란색으로, 현재 토큰을 보조색으로 구분하도록 조정해 `씌우지` 그래프에서 현재 토큰이 최대 비중처럼 보이는 오독을 줄였다.
- 추가 반영: 예제 해설의 벡터 비교 표현을 `간단한 벡터 비교`로 정리하고, 그래프 내부 제목은 본문 캡션과 역할이 겹치므로 제거했다.
- 번역 동기화 메모: Future translations should preserve the split two-chart layout for the self-attention weight example and keep deriving both charts from the shared Python chart source.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-13/p5_13_attention_chart.py`, `docs/assets/part-05/chapter-13/self-attention-weight-it-en.svg`, `docs/assets/part-05/chapter-13/self-attention-weight-it-ko.svg`, `docs/assets/part-05/chapter-13/self-attention-weight-cover-en.svg`, `docs/assets/part-05/chapter-13/self-attention-weight-cover-ko.svg`, `docs/assets/part-05/chapter-13/README.md`
- 원문 기준 버전: `v2026.07.14`
