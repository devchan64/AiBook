# Section Release Note

- Section ID: `P5-15.2`
- Source File: `docs/parts/part-05/chapter-15/section-02.md`

### v2026.07.09

- 변경 이유: Part 5 문단구조 정리 기준에 맞춰 사례/예제 구조 표지를 현재 원칙으로 통일할 필요가 있었다.
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.11

- 변경 이유: 샘플링 설정의 후속 학습 위치와 민감도 실험을 더 분명히 할 필요가 있었다.
- 본문 반영: 회수 위치를 `P6-5.2`로 구체화하고, 후보 분포 변화를 비교하는 `샘플링 민감도 실험` 표를 추가했다.
- 추가 반영: 샘플링 선택 흐름 Mermaid와 사례 묶음 Mermaid를 각각 외부 자산 include 방식으로 전환했다.
- 번역 동기화 메모: Clarified sampling follow-up location, added sensitivity experiment note, and externalized Mermaid assets for reuse. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-15/sampling-selection-flow-en.mmd`, `docs/assets/part-05/chapter-15/sampling-selection-flow-ko.mmd`, `docs/assets/part-05/chapter-15/sampling-task-flow-en.mmd`, `docs/assets/part-05/chapter-15/sampling-task-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.12

- 변경 이유: 샘플링 절의 말미 회상·적용 시점·점검 구조가 중복되어 있었고, 학습 밀도 기준으로 다시 보니 `모델이 준 점수`와 `실제 선택 절차`의 차이, 그리고 Part 5 운영 사례 축 통일이 더 필요했다.
- 본문 반영: 샘플링 정의, argmax와 sampling 차이, 다양성·안정성 균형, 다음 Part 설정 읽기 기준을 `체크리스트` 하나로 통합했다. `연습 및 예제`를 일반 자동완성 문장 대신 `배치 점검 결과` 운영 안내 문구 생성 장면으로 교체하고, 실행 전 예측 표와 `argmax_sentences / sampled_sentences / counts / average_sampled_length` 비교를 넣어 같은 후보 분포라도 선택 절차가 바뀌면 운영 메시지 경험이 달라진다는 점을 더 직접 보강했다.
- 본문 반영 추가: 절 앞쪽의 도입 예시, 상황 표, 사례 1, 비교 표도 `점검 결과 안내 문구` 운영 장면 중심으로 맞췄고, 중간에 남아 있던 일반 `챗봇 답변` 사례를 `압력 이상 정지 뒤 재기동 순서`를 설명하는 현장 지원 응답 장면으로 교체했다. 사례 3의 이미지 프롬프트도 `a small cafe in the rain`에서 `stainless mixing tank with side valve and warning beacon`으로 바꾸고, 후보 설명 역시 탱크 각도, 배관 배치, 경광등 강조, 시점 변화 기준으로 다시 적어 샘플링 절 전체가 운영 응답 설계와 설비 자산 생성 축에서 읽히게 맞췄다.
- 번역 동기화 메모: Korean source now reinforces the sampling introduction with an explicit score-vs-selection contrast and aligns the remaining examples around operational guidance, field-support responses, and equipment-scene image variation instead of generic chatbot or cafe-scene prompts. Future translations should preserve the same operations-oriented selection frame.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12-asset
- 변경 이유: 샘플링 사례 도식의 한국어 파생본이 여전히 영문 내부 라벨로 남아 있어 한국어 본문 흐름과 맞지 않았다.
- 자산 반영: `sampling-task-flow-ko.mmd`의 `same sampling question`, `autocomplete...`, `chatbot answer...`, `image generation...` 라벨을 각각 `같은 샘플링 질문`, `자동완성...`, `현장 지원 응답...`, `이미지 생성...`으로 정리했다.
- 번역 동기화 메모: Korean sampling-task diagram labels were aligned to the manuscript language while keeping the same branching structure. Future translations should preserve the same structure and language-specific asset split.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-15/sampling-task-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: 샘플링 절은 사례 수는 충분했지만, 각 사례가 `무엇이 실제로 달라지는가`를 결과 문장으로 닫지 않아 전달력이 약해질 수 있었다.
- 본문 반영: 점검 안내 문구, 현장 지원 응답, 이미지 생성 프롬프트 세 사례 각각에 `그래서 이 사례에서 확인해야 할 결과` 문장을 추가하고, 세 사례를 함께 묶는 총괄 결과 문장도 보강했다.
- 번역 동기화 메모: Korean source now closes each sampling case with an explicit result sentence and adds a shared conclusion about output selection control. Future translations should preserve the same case-closing structure.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: 샘플링 예제의 장면과 출력 해설은 이미 운영 안내 문구 축으로 정리됐지만, 코드 변수 이름이 `prefix`, `candidates`, `weights` 같은 일반 표기에 머물러 있어 절 전체 문맥과 끝까지 완전히 붙지는 않았다.
- 본문 반영: 예제 변수 이름을 `inspection_prefix`, `response_candidates`, `response_weights` 기준으로 정리하고, 후속 해설 표도 같은 이름으로 맞췄다.
- 번역 동기화 메모: The Korean sampling exercise now keeps operation-specific variable naming through the code and interpretation tables instead of generic prefix/candidates/weights labels. Future translations should preserve this aligned naming.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12-diagram
- 변경 이유: 샘플링 선택 흐름의 한국어 파생본이 여전히 영문 내부 라벨로 남아 있어 한국어 본문 도입과 바로 맞물리지 않았다.
- 자산 반영: `sampling-selection-flow-ko.mmd`의 `model scores candidate outputs`, `sampling rule chooses one candidate`, `actual generated output`을 각각 `모델이 후보 출력에 점수를 매김`, `샘플링 규칙이 한 후보를 고름`, `실제로 생성된 출력`으로 정리했다.
- 번역 동기화 메모: The Korean sampling-selection diagram now localizes its internal labels while preserving the same three-step selection structure. Future translations should keep the same structure with language-specific labels on each asset.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-15/sampling-selection-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.12`
