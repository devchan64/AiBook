# Section Release Note

- Section ID: `P5-7.2`
- Source File: `docs/parts/part-05/chapter-07/section-02.md`

## 2026-07-07

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 업데이트 감각 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: `learning_rate`, `beta`, `gradients`를 바꿔 반응 차이를 보는 `바꿔 보기` 표를 추가했다.
- 번역 동기화 메모: Sensitivity follow-up table added for optimizer setting changes. / pending

### v2026.07.11-2
- 본문 반영: SGD와 Adam의 업데이트 직관을 나란히 보는 Mermaid 도식을 추가하고, `현재 gradient 기준`과 `누적·좌표별 조절 기준`의 차이를 한 문장으로 다시 묶었다.
- 번역 동기화 메모: Added paired Mermaid assets for the SGD-vs-Adam update intuition comparison and clarified the contrast between direct and adaptive update steps. / pending

### v2026.07.12
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: SGD/Adam의 업데이트 철학, 기준점 역할, 속도·안정성·일반화 비교 축, regularization 장으로 이어지는 기준을 `체크리스트` 하나로 재구성했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.

### v2026.07.14
- 본문 반영: `gradient_risk_weight` 입력 흐름, step별 `delta` 비교, 누적 `risk_weight` 이동 경로를 각각 별도 PNG 그래프로 추가하고, 각 산출물 아래에 이 단계에서 값의 의미가 어떻게 달라지는지 설명했다.
- 번역 동기화 메모: Korean source now includes three per-stage Matplotlib charts for the SGD vs Adam-like toy example. Future translations should preserve the same gradient -> delta -> risk_weight trajectory sequence with localized visible labels.

### v2026.07.12
- 본문 반영: `사례 및 예시`에 `사람이 먼저 보기 쉬운 기준 / SGD 관점 / Adam 관점` 비교 표와 최종 결과 문장을 추가했다. `연습 및 예제`에는 첫 step 이동량, step별 delta 변화, moving average 반응을 코드 전에 예상하게 하는 표를 추가했다.
- 번역 동기화 메모: Korean source now closes the SGD-vs-Adam section with a human-vs-SGD-vs-Adam comparison table and a pre-run prediction table for direct-vs-smoothed updates. 향후 번역 시 same comparison structure를 유지해야 한다.

### v2026.07.12-asset
- 번역 동기화 메모: Chapter 7 asset-operation wording now follows the current per-language reference rule. Future translation work should preserve the same node structure while keeping each deployed language page tied to its own asset file.

### v2026.07.12
- 본문 반영: 예제 입력, 코드, 출력 표기를 `risk_weight`, `gradient_risk_weight_history`, `risk_weight_sgd`, `risk_weight_adam_like` 기준으로 다시 적어, 비교 실험도 같은 운영 신호 축 안에서 읽히게 정리했다.
- 번역 동기화 메모: The Korean SGD-vs-Adam exercise now aligns its code and output labels with the surrounding operational risk-weight framing instead of generic w/gradients naming. Future translations should preserve this aligned naming.

### v2026.07.13-link
- 본문 반영: 복귀 링크를 현재 실제 표제어인 `경사하강법(gradient descent)`과 `옵티마이저(optimizer)` 앵커로 조정했다.
- 번역 동기화 메모: The optimizer refresher links now target existing glossary entries instead of absent SGD/Adam-specific headings. Future translations should keep the fallback anchored to glossary entries that actually exist.

### v2026.07.13-layout
- 번역 동기화 메모: The Korean and English Mermaid assets now enforce a stacked top-to-bottom comparison order for the SGD-vs-Adam intuition flow. Future translations should preserve this vertical reading order.

### v2026.07.16
- 본문 반영: P5-7.2 사례 2를 `복잡한 딥러닝 모델과 큰 모델의 초기 실험`으로 확장하고, 층별 gradient 스케일 차이와 좌표별 update 균형 관점을 Adam 설명에 연결했다. 사례 비교표에도 큰 모델을 같은 보폭으로 밀 때의 한계와 적응형 update 관점을 추가했다.
- 번역 동기화 메모: Korean source now absorbs the large-model optimizer case into the SGD/Adam comparison section. Future translations should keep this as an adaptive-update example rather than a generic optimizer-role example.
