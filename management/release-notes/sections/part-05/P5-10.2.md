# Section Release Note

- Section ID: `P5-10.2`
- Source File: `docs/parts/part-05/chapter-10/section-02.md`

### v2026.07.14
- 본문 반영: 예제 출력 뒤에 배치 쌍 거리 변화 그래프와 최종 `h2` 표현 공간 그래프를 각각 추가했다. 두 그래프 모두 본문 Python 예제와 같은 `signals`, `w1`, `w2`, `h1`, `h2` 산출값을 사용해 `거리 감소`와 `관계 재서열화`를 분리해 읽도록 했다. 기존 출력 예시에 남아 있던 `batch_1-batch_4` 거리값도 실제 코드 산출값과 맞게 바로잡았다.
- 번역 동기화 메모: Korean source now adds two Python-generated charts tied directly to the P5-10.2 example: pair-distance trace across `signals/h1/h2` and final `h2` coordinate space. Future translations should preserve this two-chart sequence and localize the paired chart assets.

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: 계층적 표현 흐름 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: The hierarchical-representation Mermaid now keeps paired `-en` and `-ko` source files for translation reuse. / pending

### v2026.07.11-2
- 본문 반영: 원본 입력에서 시작해 앞층 단서를 다시 조합하며 과업 가까운 표현으로 가는 Mermaid 보강 도식을 추가했다.
- 번역 동기화 메모: Keep the English Mermaid source aligned with the Korean public include target. / pending

### v2026.07.12
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.

### v2026.07.17
- 본문 반영: `사례 및 예시`를 이미지·장면 재식별·문장·시계열 4개 사례의 나열에서, 실링 불량 대표 사례와 도메인 확장 비교표 구조로 다시 묶어 현재 절의 중심 질문이 `깊은 층이 어떤 구분 축을 더 살리는가`에 더 오래 머물도록 정리했다.
- 본문 반영: `연습 및 예제` 앞부분에 `무엇을 어떤 순서로 읽는가` 3단계 표를 추가해, `signals -> h1 -> h2`, 배치 쌍 거리 변화, 두 그래프 재해석 순서를 초심자용 읽기 기준으로 고정했다.
- 본문 반영: `이 절의 범위`를 생략 항목 목록보다 `깊은 층 표현을 어떤 직관으로 읽어야 하는가`라는 현재 절의 역할이 먼저 보이도록 다시 정리했다.
- 본문 반영: 이미지·시퀀스·attention 기반 표현 연결은 뒤 절 회수로 남기고, 표현 시각화 구현·self-supervised pretraining 최신 연구·interpretability 심화 이론 같은 바깥 항목 나열은 범위 안내에서 제거했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff structure where this section closes the layered-representation intuition first and leaves domain-specific and interpretability-heavy follow-up to later sections.

## 2026-07-18

### v2026.07.17
- 번역 동기화 메모: `docs/parts/part-05/chapter-10/section-02.en.md` was added and aligned to the current Korean source version, preserving the layered-representation intuition, the sealing-defect case, the fixed-transform caution around `signals -> h1 -> h2`, and the paired English Mermaid/PNG assets.
- 번역 반영 상태: 영문 번역 반영 완료
- 관련 자산: `docs/assets/part-05/chapter-10/layered-recomposition-flow-en.mmd`, `docs/assets/part-05/chapter-10/hierarchical-representation-flow-en.mmd`, `docs/assets/part-05/chapter-10/hierarchical-representation-distance-trace-en.png`, `docs/assets/part-05/chapter-10/hierarchical-representation-h2-space-en.png`
- 원문 기준 버전: `v2026.07.17`
- 번역 동기화 메모: `docs/parts/part-05/chapter-10/section-02.zh.md` was added and aligned to the current Korean source version, preserving the layered-representation intuition, the sealing-defect case, the fixed-transform caution around `signals -> h1 -> h2`, and the paired Chinese Mermaid/PNG assets.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 관련 자산: `docs/assets/part-05/chapter-10/layered-recomposition-flow-zh.mmd`, `docs/assets/part-05/chapter-10/hierarchical-representation-flow-zh.mmd`, `docs/assets/part-05/chapter-10/hierarchical-representation-distance-trace-zh.png`, `docs/assets/part-05/chapter-10/hierarchical-representation-h2-space-zh.png`
