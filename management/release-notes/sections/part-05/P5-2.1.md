# Section Release Note

- Section ID: `P5-2.1`
- Source File: `docs/parts/part-05/chapter-02/section-01.md`

### v2026.07.09
- 변경 이유: Part 5 문단구조 정리 기준에 맞춰 사례/예제 구조 표지를 현재 원칙으로 통일할 필요가 있었다.
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 층 흐름 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.11
- 변경 이유: Part 5 Mermaid 도식을 `docs/assets` 중심으로 정리하는 기준을 Chapter 2의 다층 구조 설명에도 적용할 필요가 있었다.
- 본문 반영: 다층 구조 흐름 도식과 단일 퍼셉트론 비교 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: Chapter 2 Mermaid diagrams now keep paired `-en` and `-ko` source files for translation reuse. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-02/multilayer-network-flow-en.mmd`, `docs/assets/part-05/chapter-02/multilayer-network-flow-ko.mmd`, `docs/assets/part-05/chapter-02/single-vs-multilayer-flow-en.mmd`, `docs/assets/part-05/chapter-02/single-vs-multilayer-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.12
- 변경 이유: 말미의 회상·적용 시점·점검 블록이 중복 분산되어 있어 기준서에 맞춘 통합이 필요했다.
- 본문 반영: 다층 구조, 은닉층 역할, 층 간 연결, 중간 표현 관점, 다음 절로 넘길 비선형성 질문을 `체크리스트` 하나로 흡수했다.
- 본문 반영: `아직 남아 있는 질문`의 마지막 항목을 `층을 더 거칠수록 표현은 어떻게 달라지는가`로 좁히고, 바로 다음 문단에 `깊이·너비의 이론 비교`가 아니라 `중간 표현 변화`를 이어서 본다는 범위 문장을 추가했다.
- 본문 반영: 도입부의 분리된 질문 문장을 일반 문단으로 흡수해 문장 흐름을 정리했다. 고객 데이터 비유에는 비교 표를 추가해 `입력값을 따로 보는 읽기`와 `중간 조합으로 다시 읽는 방식`의 차이를 더 분명히 보이도록 보강했다.
- 본문 반영: 사례 1 아래에 고객 이탈 판단 흐름 Mermaid를 추가하고, 사례 표를 넣어 `사람이 먼저 보기 쉬운 판단 -> 중간 조합으로 다시 읽는 판단 -> 더 안전한 다음 판단` 순서를 한 번 더 드러냈다.
- 본문 반영: 사례 2에도 작은 비교 표를 추가해 `작은 단서 -> 앞층 조합 -> 더 큰 라벨 단서` 흐름을 더 분명히 보이도록 했다. 사례 1 도식 바로 아래에는 `사례 판단 순서를 압축한 도식`이라는 해설 문장을 붙여 도식 역할을 고정했다.
- 본문 반영: Python 예제는 출력이 모두 같게 나오던 구성을 버리고, 은닉층 패턴과 최종 출력이 실제로 갈리는 네 샘플 예제로 다시 설계했다. 샘플 수 표기도 4개로 맞추고, `sample_2` 입력 변경 시 은닉층과 최종 출력이 어떻게 바뀌는지도 바로 아래에 적어 연습 결과를 닫았다. 또한 `이 절에서 기억할 관점` 구조 아래로 말미 연결 문단을 정리해 공통 구조를 맞췄다.
- 본문 반영: `왜 층을 더 쌓는가`에는 장난감 입력 장면 비교 표를 추가해 `퍼셉트론 하나로 바로 읽는 판단`과 `층을 나눠 읽는 판단`의 차이를 바로 확인할 수 있게 했다. `다층 구조가 딥러닝의 출발점인 이유`에는 학습 책임 비교 표를 추가해 Part 4와 Part 5의 차이를 더 압축해 보이도록 했다. `연습 및 예제` 앞에는 코드 실행 전 은닉 노드 패턴을 먼저 예상해 보는 짧은 표를 넣어 비코드 연습 밀도를 보강했다.
- 번역 동기화 메모: 한국어 기준 원고에서 사례 표, 사례 도식 해설, Python 예제 샘플 구성과 해설, 말미 구조가 함께 바뀌었다. 향후 번역 시 같은 사례 표·도식 역할 문장·예제 출력 해설을 함께 반영해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-02/production-batch-hidden-flow-en.mmd`, `docs/assets/part-05/chapter-02/production-batch-hidden-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 더 강화된 학습 밀도 기준에서 다시 보니, 사례 2와 Python 예제는 `같은 최종 출력이라도 은닉층 패턴은 다를 수 있다`는 핵심 차이를 독자가 오래 붙잡게 만드는 비교 장면이 아직 부족했다.
- 본문 반영: `이미지 인식의 초기 직관` 사례에 `사람 기준 vs 다층 구조 관점` 비교 표를 추가했다. `연습 및 예제`의 예측 표는 최종 출력 감각까지 포함하도록 확장했고, 실행 결과 아래에는 샘플별 `hidden 패턴 / output / 지금 읽어야 할 핵심` 비교 표를 추가해 같은 최종 출력 안에서도 내부 계산 차이를 직접 비교하게 만들었다.
- 본문 반영 추가: `왜 층을 더 쌓는가` 도입의 `장난감 판단 장면` 표현을 `고객 이탈 판단 장면`으로 바꿔, 다층 구조의 직관이 실제 업무 특성 조합 맥락으로 읽히게 정리했다.
- 번역 동기화 메모: 한국어 기준 원고에 이미지 사례 비교 표와 은닉층/출력 비교 표가 추가되었다. 향후 번역 시 같은 비교 구조와 예제 해석 표를 함께 반영해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: Chapter 2의 다층 구조 도입은 여전히 고객 이탈 비유가 중심이라, 현재 Part 5에서 강화 중인 운영 설비 이상 조합 예제 흐름과 직접 이어지지 않았다.
- 본문 반영: `다층 구조를 표 형식 비유로 보기`와 사례 1을 생산 라인 배치 데이터 기준으로 바꾸고, 입력값·은닉층·출력층 설명을 `온도 이탈`, `압력 흔들림`, `재작업 호출`, `개입 필요성` 같은 운영 신호로 재구성했다. `연습 및 예제`의 샘플 표기와 해설도 `batch_1` 계열로 정리해 은닉층 조합이 배치 개입 판단으로 이어지는 흐름으로 읽히게 조정했다.
- 번역 동기화 메모: Korean source now frames the multilayer-introduction section around production-batch anomaly combinations rather than customer churn, while preserving the same hidden-pattern comparison structure. 향후 번역 시 same operational framing을 유지해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 다층 구조 절의 이미지 사례 2에는 아직 동물·자동차 중심 비유가 남아 있어, 같은 절의 생산 배치·운영 이상 조합 흐름과 도메인 톤이 완전히 맞지 않았다.
- 본문 반영: 사례 2를 `설비 이미지 인식의 초기 직관`으로 바꾸고, 귀·바퀴·날개 예시를 밸브·경광등·배관 단서로 교체했다. 작은 단서, 앞층 조합, 뒤층의 더 큰 라벨 후보도 같은 설비 장면 기준으로 다시 정리했다.
- 번역 동기화 메모: Korean source aligned the remaining image-intuition case in P5-2.1 with equipment-scene labels and visual cues. Future translations should keep the equipment-oriented framing.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 본문 반영 추가: 말미에 남아 있던 `## 이 절에서 기억할 관점` 표지를 일반 문단으로 흡수해, 회상용 구조 표지도 `체크리스트` 외 별도 헤더를 두지 않는 현재 원칙과 맞췄다.

### v2026.07.13
- 변경 이유: `P5-2.1`의 Python 예제가 은닉층 패턴 차이를 잘 보여 주기는 했지만, 한 번 실행한 뒤 독자가 무엇을 더 바꿔 봐야 하는지까지는 닫아 주지 못했다.
- 본문 반영: `연습 및 예제` 뒤에 `x2`, 입력 동시 증가, 출력층 편향을 직접 바꿔 보게 하는 후속 탐색 표를 추가해, 은닉층 조합과 최종 판단 규칙이 어디서 뒤집히는지 계속 비교하게 했다.
- 변경 이유 추가: Python 가이드라인 기준으로 다시 보면, 후속 탐색이 아직 설명 표에 머물러 있어 `실제로 값을 바꾸고 출력 차이를 읽는 실험형 예제`로는 조금 약했다.
- 본문 반영 추가: 기본 예제 코드를 `run_batch(...)` 함수와 후속 실험 목록까지 포함하는 실행형 구조로 바꾸고, 출력 예시에도 `batch_2 x1 increase`, `batch_1 x2 increase`, `batch_4 both signals increase`, `looser output rule` 결과를 함께 실었다. 이어지는 해설 문단에서는 `입력을 바꿔 은닉층이 달라지는 경우`와 `출력층 규칙만 바뀌는 경우`를 분리해 읽도록 정리했다.
- 변경 이유: 설명 구간의 비교 표 몇 개가 `왜 층을 쌓는가`와 `중간 표현이 무엇인가`를 압축하기보다 읽는 순서를 끊어, 해설 문장을 따라가기가 오히려 어려웠다.
- 본문 반영: `왜 층을 더 쌓는가`, `다층 구조를 표 형식 비유로 보기`, `다층 구조가 딥러닝의 출발점인 이유`의 해설 표를 줄이고, 핵심 비교를 문장 흐름으로 다시 묶어 읽기 순서를 더 선형적으로 정리했다. 사례 구간의 비교 표는 유지해 학습용 장면 비교 역할만 남겼다.
- 번역 동기화 메모: Korean source now adds explicit follow-up parameter changes after the multilayer hidden-pattern example. Future translations should preserve the same exploratory prompts after the sample output.
- 번역 동기화 메모 추가: Korean source now turns those follow-up prompts into runnable experiments with explicit outputs. Future translations should preserve the same split between `hidden-pattern changes` and `output-rule changes`.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.13`
- 변경 이유 추가: 차트·표 형식 선택 가이드라인 재검토 결과, 설비 이미지 사례의 `사람 기준 vs 다층 구조 관점` 표가 사례 표와 Mermaid 흐름도 뒤에서 해석을 반복해 읽기 흐름을 끊는 부분이 있었다.
- 본문 반영 추가: 작은 단서·앞층 조합·뒤층 단서 표와 공통 Mermaid 흐름도는 유지하고, 사례 해석 반복 표는 문단 설명으로 바꿔 표의 역할을 비교 자료에 집중시켰다.
- 번역 동기화 메모 추가: Korean source now converts the explanatory perspective table in the equipment-image case to prose while keeping value/case comparison tables and Mermaid flow assets. Future translations should preserve this format boundary.

### v2026.07.12-asset
- 변경 이유: `P5-2.1` 본문은 이미 생산 배치 이상 조합 사례로 바뀌었는데, 연결된 Mermaid 자산 이름과 내부 라벨은 여전히 고객 이탈 흐름으로 남아 있어 본문-자산 정합성과 레포 원칙을 함께 해쳤다.
- 자산 반영: 사례 1 흐름 도식을 `production-batch-hidden-flow-en.mmd`, `production-batch-hidden-flow-ko.mmd`로 이름을 바로잡고, 내부 라벨도 `배치 신호/열적 부담/운전 불안정성/개입 우선순위` 기준으로 다시 정리했다.
- 본문 반영: `P5-2.1` 본문이 새 자산 이름을 참조하도록 include 경로를 함께 갱신했다.
- 자산 운영 반영: `docs/assets/part-05/chapter-02/README.md`에 언어별 본문이 각자 `-ko.mmd`, `-en.mmd`를 직접 참조하고, 영문 자산을 기준 원본으로 먼저 수정한 뒤 한국어 파생본을 동기화한다는 운영 원칙을 명시했다.
- 번역 동기화 메모: The Chapter 2 case-flow asset now matches the production-batch anomaly framing used in the Korean source and follows the current per-language asset-reference rule. Future translations should preserve the same production-batch flow and language-specific asset split.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-02/production-batch-hidden-flow-en.mmd`, `docs/assets/part-05/chapter-02/production-batch-hidden-flow-ko.mmd`, `docs/assets/part-05/chapter-02/README.md`
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12-diagram
- 변경 이유: `퍼셉트론 하나와 다층 구조 비교` 도식은 층 수 차이만 보이고, `한 점수에 눌러 담는 구조`와 `중간 조합을 단계적으로 만드는 구조`의 대비가 충분히 직접 읽히지 않았다.
- 자산 반영: `single-vs-multilayer-flow-ko.mmd`, `single-vs-multilayer-flow-en.mmd`를 좌우 비교형 도식으로 다시 정리하고, 단일 퍼셉트론 쪽에는 `중간 조합을 따로 만들기 어렵다`, 다층 구조 쪽에는 `작은 조합 -> 더 큰 조합 -> 최종 판단`과 `중간 표현을 여러 단계로 만든다`는 보조 문구를 추가했다.
- 본문 반영: 도식 바로 아래 해설도 `여러 신호가 한 점수에 먼저 눌러 담기기 쉽다`와 `작은 조합을 다시 더 큰 조합으로 바꾼다`는 대비가 직접 드러나도록 한 문장을 보강했다.
- 번역 동기화 메모: The single-vs-multilayer comparison diagram now emphasizes `one compressed score` versus `staged intermediate combinations` in both Korean and English source assets. Future translations should preserve this direct contrast.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-02/single-vs-multilayer-flow-en.mmd`, `docs/assets/part-05/chapter-02/single-vs-multilayer-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: 사례 1과 사례 2가 각각은 충분히 설명되지만, `작은 조합 -> 더 큰 조합 -> 최종 판단`이라는 공통 흐름이 절 전체 수준에서 한 장면으로 압축되어 보이지는 않았다.
- 본문 반영: 사례 뒤에 공통 Mermaid 도식 1개를 추가해 생산 배치 신호와 설비 이미지 단서 모두 `원래 입력 -> 작은 조합 -> 더 큰 상태 의미 -> 최종 판단` 흐름으로 다시 묶었다. 사례별 비교표를 다시 반복하지 않고, 두 사례가 공통으로 보여 주는 다층 조합 흐름만 압축하도록 위치와 설명을 정리했다.
- 번역 동기화 메모: Added a shared multilayer-combination Mermaid pair so the Korean manuscript now summarizes both cases with one compact staged-combination flow. Future translations should preserve the same small-combination to larger-pattern contrast.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-02/multilayer-combination-bridge-en.mmd`, `docs/assets/part-05/chapter-02/multilayer-combination-bridge-ko.mmd`
- 원문 기준 버전: `v2026.07.12`
