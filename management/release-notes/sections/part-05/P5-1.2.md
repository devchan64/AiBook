# Section Release Note

- Section ID: `P5-1.2`
- Source File: `docs/parts/part-05/chapter-01/section-02.md`

### v2026.07.31
- 후속 보강: 반복되어 진행 메타처럼 보이던 공통 확인 문장을 제거하고, 사례 절에는 Section별 판단 내용 한 문장만 남겼다.
- 후속 보강: 원고 본문에서 `오픈체크리스트`, `중심축` 같은 관리 표현을 제거하고, 독자가 사례 안에서 확인할 학습 판단 문장으로 다시 정리했다.
- 후속 보강: 소제목 정리에 그치지 않도록 사례 절 첫머리에 중심축 확인 질문 2개를 추가했다.
- 변경 이유: Part 5 보강 라운드에서 진행 메타 성격의 `사례 및 예시` 소제목을 내용형 제목으로 정리해 독자가 사례 절의 딥러닝 계산 장면과 판단 기준을 바로 읽을 수 있게 했다.
- 소제목 정리: 한국어 원문의 `사례 및 예시` 절 제목을 `선형 결합과 활성화: 확인할 판단 기준`으로 바꿔 사례 절이 맡는 딥러닝 계산 장면과 판단 기준을 바로 드러내도록 했다.
- 번역 동기화 메모: 이번 소제목 정리는 한국어 원문 기준으로 먼저 반영했으며, 영어판과 중국어 간체판은 후속 동기화가 필요하다.
- 번역 반영 상태: 한국어 원문 반영 완료, 영어판과 중국어 간체판 미반영.
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.31`

### v2026.07.26
- 본문 반영: 한국어, 영어, 중국어 간체판 `Version`을 `v2026.07.26`으로 맞췄다.
- 링크 정합성: 선형 결합, 가중합, 활성화 함수, 결정 경계 복귀 링크를 언어별 공개 개념사전 색인 경로로 정리했다.
- 개념사전 반영: `linear-combination.en.md`, `linear-combination.zh.md`, `weighted-sum.en.md`, `weighted-sum.zh.md`를 추가하고, 관련 한글 항목과 `decision-boundary` 항목의 등장 Section에 `P5-1.2`를 반영했다. 영어 알파벳 색인과 중국어 pinyin 색인도 함께 연결했다.
- 번역 동기화 메모: 영어판 제목 대문자 관례를 현재 번역 기준에 맞춰 정리했다. Python 예제, 그래프 자산, 출처 목록은 변경하지 않았다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.26`

### v2026.07.20
- 본문 반영 내용: 한국어, 영어, 중국어 간체판의 Python 예제 첫 줄에 퍼셉트론의 선형 결합 점수 `z`와 임계값 판단이 입력, 편향, `threshold` 변화에 따라 어디서 뒤집히는지 확인하는 목적을 설명하는 주석을 추가했다.
- 저작권 검토: Part 5 Module 1 순차 검토 범위에서 선형 결합, 활성화 임계값, XOR 한계 설명과 장학금·설비 경보 예시를 확인했다. 수식, 표, 예제 흐름은 표준 개념을 바탕으로 한 자체 작성 자료로 판단했다.
- 출처 사용 점검: Rosenblatt(1958), Minsky & Papert, Goodfellow et al.은 퍼셉트론과 선형 분리 한계의 배경 확인 용도로만 쓰였으며, 원문 목차·문제·도식·코드 재사용 정황은 찾지 못했다.
- 관련 자산: `activation-threshold-flow-ko.mmd`, `linear-boundary-flow-ko.mmd`, `xor-limit-flow-ko.mmd`, `linear-activation-*.png`, `linear-boundary-*.svg`, `xor-pattern-ko.svg`는 로컬 스크립트와 자체 도식 정의에서 생성·작성된 학습 보조 자산으로 판단했다.
- 본문 반영: 한국어, 영어, 중국어 간체판의 범위·목표 제목을 `선형 결합과 활성화가 나누는 역할`, `가중합과 출력 변환의 판단 기준` 축으로 바꾸어 진행 메타 제목을 제거했다.
- 번역 동기화 메모: 영어·중국어 번역본도 같은 제목 구조로 동기화하고 본문 Version을 `v2026.07.20`으로 맞췄다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.20`

### v2026.07.17
- 본문 반영: `깊게 다루지 않습니다` 목록을 제거하고, 이번 절은 선형 결합과 활성화가 퍼셉트론 하나의 경계를 만들고 왜 다음 장의 다층 구조 질문으로 이어지는지 먼저 닫는 방향으로 범위 안내를 정리했다.
- 번역 동기화 메모: `docs/parts/part-05/chapter-01/section-02.en.md`에 같은 handoff 중심 범위 안내와 실험형 예제 구조를 반영했다.
- 번역 동기화 메모: `docs/parts/part-05/chapter-01/section-02.zh.md`에 같은 범위 안내, 실험형 예제, 경계 이동 설명을 반영했다. Chapter 1 공개 본문용 `linear-boundary-flow-zh.mmd`, `activation-threshold-flow-zh.mmd`, `xor-limit-flow-zh.mmd`와 `linear-boundary-points-zh.svg`, `xor-pattern-zh.svg`, `linear-activation-*.zh.png` 자산도 함께 동기화했다.
- 번역 반영 상태: 영문 번역 반영 완료 / 중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.17`

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `작은 숫자 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: 선형 경계, 활성화 임계값, XOR 한계 Mermaid 3개를 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: Chapter 1 Mermaid diagrams now keep paired `-en` and `-ko` source files for translation reuse. / pending

### v2026.07.12
- 본문 반영: 장학금 심사와 임계값 판단 사례에 짧은 예시 표를 추가해 선형 결합과 활성화의 확인 결과를 더 분명히 보이도록 보강했다. 연습 예제에는 편향을 바꿔 출력이 뒤집히는 추가 확인 문장을 넣었고, 말미의 다음 단계 판단 표지는 `체크리스트`로 재구성했다.
- 본문 반영: `Part 4와 어떻게 이어지는가` 소제목을 별도 연결 표지로 두지 않고, `이 절에서 기억할 관점` 문단으로 흡수해 Part 4의 선형 모델 감각과 Part 5의 신경망 감각이 이어지는 핵심만 남겼다.
- 번역 동기화 메모: 한국어 기준 원고에서 사례 표, 연습 확인 문장, 말미 checklist 구조가 바뀌었다. 향후 번역 시 같은 예시 표와 checklist 구조를 함께 반영해야 한다.

### v2026.07.13
- 본문 반영: `연습 및 예제`를 `alarm_risk`, `bias`, `threshold`를 직접 바꿔 보며 경계 이동을 읽는 실험형 예제로 재구성했다. 고정 케이스 설명과 사후 비교표는 줄이고, `조작할 값`, `관찰할 출력`, `어디서 출력이 뒤집히는가`를 중심으로 본문 흐름을 다시 정리했다.
- 번역 동기화 메모: 한국어 원고에서 `P5-1.2`의 code block은 fixed-case verification example에서 parameter-sweep experiment로 바뀌었다. 향후 번역 시 manipulable inputs, observed outputs, and boundary-shift reading flow를 함께 유지해야 한다.

### v2026.07.13-python-chart
- 번역 동기화 메모: The Korean and English linear-boundary/XOR chart is now generated from a shared Python source so point layout and boundary geometry stay aligned across languages. Future translations should preserve the same script-driven structure.

### v2026.07.13-layout
- 본문 반영: 기존 비교형 SVG를 유지하되, 배포 본문에서는 `linear-boundary-points`와 `xor-pattern` 두 개의 독립 SVG를 나란히 읽도록 바꿨다.
- 번역 동기화 메모: The linear-boundary/XOR chart set now includes separate single-panel SVGs for the linearly separable pattern and the XOR pattern. Future translations should preserve the same split presentation in the manuscript.

### v2026.07.14
- 본문 반영: 예제 출력 뒤에 `경보 위험 입력값`, `선형 결합 점수 z`, `편향과 임계값 조합별 최종 출력` 그래프를 분리해 추가하고, 각 단계에서 값의 의미가 어떻게 바뀌는지 설명했다.
- 번역 동기화 메모: Korean source now uses separate generated charts to trace the formula-based transformation from alarm risk input to linear score and thresholded output. Future translations should use the localized assets and preserve the three-step transformation reading.
