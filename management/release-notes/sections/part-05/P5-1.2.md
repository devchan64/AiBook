# Section Release Note

- Section ID: `P5-1.2`
- Source File: `docs/parts/part-05/chapter-01/section-02.md`

### v2026.07.09
- 변경 이유: Part 5 문단구조 정리 기준에 맞춰 사례/예제 구조 표지를 현재 원칙으로 통일할 필요가 있었다.
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `작은 숫자 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.11
- 변경 이유: Part 5 Mermaid 도식을 외부 자산 파일로 분리하는 기준을 Chapter 1 전반에도 같은 방식으로 적용할 필요가 있었다.
- 본문 반영: 선형 경계, 활성화 임계값, XOR 한계 Mermaid 3개를 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: Chapter 1 Mermaid diagrams now keep paired `-en` and `-ko` source files for translation reuse. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-01/linear-boundary-flow-en.mmd`, `docs/assets/part-05/chapter-01/linear-boundary-flow-ko.mmd`, `docs/assets/part-05/chapter-01/activation-threshold-flow-en.mmd`, `docs/assets/part-05/chapter-01/activation-threshold-flow-ko.mmd`, `docs/assets/part-05/chapter-01/xor-limit-flow-en.mmd`, `docs/assets/part-05/chapter-01/xor-limit-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.12
- 변경 이유: 사례 설명이 서술 중심으로만 닫혀 있어 `사례 및 예시`, `연습 및 예제`, `체크리스트`의 역할 분리가 약했고, 말미 구조도 기준서의 `체크리스트` 단일화 원칙과 맞지 않았다.
- 본문 반영: 장학금 심사와 임계값 판단 사례에 짧은 예시 표를 추가해 선형 결합과 활성화의 확인 결과를 더 분명히 보이도록 보강했다. 연습 예제에는 편향을 바꿔 출력이 뒤집히는 추가 확인 문장을 넣었고, 말미의 다음 단계 판단 표지는 `체크리스트`로 재구성했다.
- 본문 반영: `Part 4와 어떻게 이어지는가` 소제목을 별도 연결 표지로 두지 않고, `이 절에서 기억할 관점` 문단으로 흡수해 Part 4의 선형 모델 감각과 Part 5의 신경망 감각이 이어지는 핵심만 남겼다.
- 번역 동기화 메모: 한국어 기준 원고에서 사례 표, 연습 확인 문장, 말미 checklist 구조가 바뀌었다. 향후 번역 시 같은 예시 표와 checklist 구조를 함께 반영해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 더 강화된 학습 밀도 기준으로 다시 보니, `선형 결합`과 `활성화`가 각각 무엇을 바꾸는지, 그리고 입력 변화와 편향 변화가 경계를 어떻게 움직이는지 독자가 직접 비교할 장면이 아직 약했다.
- 본문 반영: `장학금 심사 점수`, `임계값이 필요한 승인 판단` 사례에 `사람 기준 vs 선형 결합/활성화 관점` 비교 표를 추가했다. `연습 및 예제`는 단일 입력 1개 예제에서 `case_A / case_B / case_C`를 함께 비교하는 다중 케이스 예제로 바꾸어, 입력 변화와 편향 변화가 각각 경계를 어떻게 넘기는지 직접 보게 했다.
- 번역 동기화 메모: 한국어 기준 원고에 선형 결합/활성화 비교 표와 3케이스 예제가 추가되었다. 향후 번역 시 같은 비교 구조와 경계 이동 예측 흐름을 함께 반영해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 추가 재검토 결과, 사례와 예제가 각각은 강해졌지만 `조건 목록이 점수 축과 경계 판단으로 바뀐다`는 핵심이 절 전체 수준에서 다시 묶이는 장면은 약했다.
- 본문 반영: 사례 뒤에 `사람이 먼저 보기 쉬운 해석 vs 선형 결합과 활성화가 더 분명하게 남기는 것` 교차 비교표를 추가해 장학금/임계값 사례를 한 번에 다시 묶었다. 예제 출력 뒤에는 `결과만 볼 때 vs 선형 결합과 경계까지 볼 때` 비교표를 덧붙여, 편향 변화와 입력 기여 변화가 각각 경계를 어떻게 움직였는지 더 직접적으로 읽게 만들었다.
- 번역 동기화 메모: Korean source now closes P5-1.2 with cross-case and post-output boundary-reading tables. 향후 번역 시 same cross-case structure and boundary-reading table should be preserved.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 초반 기준 절 재점검 결과, `선형 결합`과 `활성화`를 설명하는 장면에 장학금 심사, 일반 입장 승인 같은 범용 비유가 남아 있어 Part 5 전체 운영 신호 해석 흐름과 바로 이어지지 않았다.
- 본문 반영: `왜 점수로 먼저 모으는가`와 `사례 및 예시`를 정비 작업 재개, 잔류 가스 경보, 자동 차단 임계값 같은 운영 장면으로 교체했다. `연습 및 예제`의 입력 이름도 `stability_score`, `alarm_risk`로 바꾸고, 출력 뒤에 항목별 기여도 표를 추가해 편향 변화와 입력 기여 변화가 각각 어떻게 경계를 움직였는지 직접 읽게 했다.
- 번역 동기화 메모: Korean source now frames P5-1.2 around maintenance restart and automatic shutdown threshold scenes, and the practice block now uses named operational signals plus a post-output contribution table. Future translations should preserve the operational framing and the explicit boundary-reading table.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 본문 반영 추가: 말미에 남아 있던 `## 이 절에서 기억할 관점` 표지를 일반 문단으로 흡수해, 회상용 구조 표지도 `체크리스트` 외 별도 헤더를 두지 않는 현재 원칙과 맞췄다.

### v2026.07.13
- 변경 이유: `P5-1.2`의 Python 블록이 고정된 세 케이스를 다시 출력하는 검증형 장난감 예제로 읽혀, 현재 가이드라인의 `실험형 우선` 원칙과 맞지 않았다.
- 본문 반영: `연습 및 예제`를 `alarm_risk`, `bias`, `threshold`를 직접 바꿔 보며 경계 이동을 읽는 실험형 예제로 재구성했다. 고정 케이스 설명과 사후 비교표는 줄이고, `조작할 값`, `관찰할 출력`, `어디서 출력이 뒤집히는가`를 중심으로 본문 흐름을 다시 정리했다.
- 본문 반영 추가: 실험 뒤에 `alarm_risk`, `stability_score`, `threshold`를 한 번 더 바꿔 보게 하는 후속 탐색 표를 추가해, 코드 실행이 단순 확인에서 끝나지 않고 `무엇이 경계를 더 민감하게 움직이는가`까지 비교하게 했다.
- 번역 동기화 메모: 한국어 원고에서 `P5-1.2`의 code block은 fixed-case verification example에서 parameter-sweep experiment로 바뀌었다. 향후 번역 시 manipulable inputs, observed outputs, and boundary-shift reading flow를 함께 유지해야 한다.
- 번역 동기화 메모 추가: the practice block now includes explicit follow-up parameter changes and interpretation questions. Future translations should preserve the same exploratory prompts after the sample output.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.13`
- 변경 이유 추가: 차트·표 형식 선택 가이드라인 재검토 결과, 설명부의 `사람 기준 vs 선형 결합/활성화 관점` 표들이 비교 자료라기보다 해석 절차를 반복해 읽기 흐름을 끊는 부분이 있었다.
- 본문 반영 추가: 수치와 실험 비교에 필요한 표는 유지하고, 초반 선형 결합 해석표, 사례별 관점 비교표, 두 사례 종합 비교표를 문단형 절차 설명으로 바꿨다.
- 번역 동기화 메모 추가: Korean source now reduces explanatory comparison tables in P5-1.2 and keeps tables mainly for values, cases, and experiment prompts. Future translations should preserve this prose-first explanation flow.
- 변경 이유 추가: Part 5 Chapter 3의 대표 활성화 함수 비교가 네 Section으로 분리되어, 선형 결합과 활성화 절의 후속 참조 범위가 달라졌다.
- 본문 반영 추가: sigmoid, tanh, ReLU 비교 참조를 `P5-3.1, P5-3.2`에서 `P5-3.1부터 P5-3.5까지`로 조정했다.
- 번역 동기화 메모 추가: Future translations should point readers to the expanded P5-3.1 through P5-3.5 activation-function range.
- 변경 이유 추가: P5 전체 그래프 검토 결과, `선형 경계`와 `XOR 한계`는 점의 좌표 배치가 핵심인데 기존 Mermaid 흐름도만으로는 직선 하나가 가능한 경우와 실패하는 경우를 공간적으로 보기 어려웠다.
- 본문 반영 추가: 선형 경계 설명 뒤에 선형 분리 가능 패턴과 XOR 패턴을 나란히 비교하는 SVG 그래프를 추가하고, 규칙 문장이 짧아도 점 배치가 직선 하나로 나뉘지 않으면 퍼셉트론 하나에는 어렵다는 설명을 보강했다.
- 번역 동기화 메모 추가: Korean source now uses a localized SVG chart for linear-boundary and XOR point-layout intuition. Future translations should reference the matching English SVG or localized language asset.
- 관련 자산 추가: `docs/assets/part-05/chapter-01/linear-boundary-xor-en.svg`, `docs/assets/part-05/chapter-01/linear-boundary-xor-ko.svg`, `docs/assets/part-05/chapter-01/README.md`
- 본문 반영 추가: 배포 페이지에서 한글 원문의 SVG 그래프가 안정적으로 보이도록 이미지 참조를 사이트 기준 절대 경로로 조정했다.

### v2026.07.13-python-chart
- 변경 이유: `P5-1.2`의 선형 경계/XOR 그림은 좌표 위치와 직선 분리 가능 여부가 핵심인데, 수작업 SVG보다 Python 기반 좌표 차트가 점 배치와 경계 관계를 더 일관되게 보여 주는 편이 적합했다.
- 자산 반영: `linear-boundary-xor` 한영 SVG를 Python/Matplotlib 생성형 자산으로 교체하고, 생성 원본 `p5_1_2_linear_boundary_xor.py`를 같은 폴더에 추가했다.
- 번역 동기화 메모: The Korean and English linear-boundary/XOR chart is now generated from a shared Python source so point layout and boundary geometry stay aligned across languages. Future translations should preserve the same script-driven structure.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-01/p5_1_2_linear_boundary_xor.py`, `docs/assets/part-05/chapter-01/linear-boundary-xor-en.svg`, `docs/assets/part-05/chapter-01/linear-boundary-xor-ko.svg`, `docs/assets/part-05/chapter-01/README.md`
- 원문 기준 버전: `v2026.07.13`

### v2026.07.13-layout
- 변경 이유: 선형 분리 가능 패턴과 XOR 패턴을 한 장 비교형으로만 두면, 각 패턴의 점 배치를 독립적으로 읽는 데 필요한 시선 고정점이 약했다.
- 본문 반영: 기존 비교형 SVG를 유지하되, 배포 본문에서는 `linear-boundary-points`와 `xor-pattern` 두 개의 독립 SVG를 나란히 읽도록 바꿨다.
- 자산 반영: `p5_1_2_linear_boundary_xor.py`가 비교형 SVG와 함께 선형 분리 단독 SVG, XOR 단독 SVG를 각각 생성하도록 확장했다.
- 번역 동기화 메모: The linear-boundary/XOR chart set now includes separate single-panel SVGs for the linearly separable pattern and the XOR pattern. Future translations should preserve the same split presentation in the manuscript.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-01/p5_1_2_linear_boundary_xor.py`, `docs/assets/part-05/chapter-01/linear-boundary-points-en.svg`, `docs/assets/part-05/chapter-01/linear-boundary-points-ko.svg`, `docs/assets/part-05/chapter-01/xor-pattern-en.svg`, `docs/assets/part-05/chapter-01/xor-pattern-ko.svg`, `docs/assets/part-05/chapter-01/README.md`
- 원문 기준 버전: `v2026.07.13`

### v2026.07.14
- 변경 이유: 수식 기반 변환 추적 원칙으로 재검토한 결과, Python 예제는 `alarm_risk`, `bias`, `threshold` 변화에 따른 출력 반전을 보여 주지만 입력값이 선형 결합 점수와 임계값 비교 결과로 바뀌는 단계가 텍스트 출력에만 묶여 있었다.
- 본문 반영: 예제 출력 뒤에 `경보 위험 입력값`, `선형 결합 점수 z`, `편향과 임계값 조합별 최종 출력` 그래프를 분리해 추가하고, 각 단계에서 값의 의미가 어떻게 바뀌는지 설명했다.
- 자산 반영 추가: 기존 선형 경계/XOR SVG 생성 스크립트에서 본문 캡션과 중복되는 내부 그래프 제목을 제거했다.
- 번역 동기화 메모: Korean source now uses separate generated charts to trace the formula-based transformation from alarm risk input to linear score and thresholded output. Future translations should use the localized assets and preserve the three-step transformation reading.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-01/p5_1_2_linear_activation_trace.py`, `docs/assets/part-05/chapter-01/linear-activation-input-ko.png`, `docs/assets/part-05/chapter-01/linear-activation-input-en.png`, `docs/assets/part-05/chapter-01/linear-activation-score-ko.png`, `docs/assets/part-05/chapter-01/linear-activation-score-en.png`, `docs/assets/part-05/chapter-01/linear-activation-output-ko.png`, `docs/assets/part-05/chapter-01/linear-activation-output-en.png`, `docs/assets/part-05/chapter-01/README.md`
- 원문 기준 버전: `v2026.07.14`
