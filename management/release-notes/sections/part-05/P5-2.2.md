# Section Release Note

- Section ID: `P5-2.2`
- Source File: `docs/parts/part-05/chapter-02/section-02.md`

### v2026.07.09
- 변경 이유: Part 5 문단구조 정리 기준에 맞춰 사례/예제 구조 표지를 현재 원칙으로 통일할 필요가 있었다.
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 중간 표현 감각 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.11
- 변경 이유: Part 5 Mermaid 도식을 `docs/assets` 중심으로 정리하는 기준을 Chapter 2의 표현 학습 설명에도 적용할 필요가 있었다.
- 본문 반영: 내부 좌표계 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: The internal-coordinate Mermaid now keeps paired `-en` and `-ko` source files for translation reuse. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-02/internal-coordinates-flow-en.mmd`, `docs/assets/part-05/chapter-02/internal-coordinates-flow-ko.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.12
- 변경 이유: 말미의 `기억할 관점`, `짧은 점검`, `언제...`가 `체크리스트`와 역할이 겹쳐 기준서 위반 상태였다.
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: 은닉층, 표현, representation learning, 내부 노드 해석 경계, 활성화 함수 장으로 이어지는 복습 기준을 `체크리스트` 하나로 재구성했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 학습 밀도 재점검 결과, `고객 상태 표현` 사례와 Python 예제가 내부 표현 비교보다 설명 요약 쪽에 더 가까워 보여, 독자가 은닉축 반응을 먼저 예측하고 비교할 장면을 보강할 필요가 있었다.
- 본문 반영: `고객 상태 표현` 사례에 `원래 입력 열 vs 내부 표현` 비교 표와 `사람 기준 vs 은닉층 표현 기준` 비교 표를 추가했다. `연습 및 예제`에는 실행 전 예측 표를 넣어 각 샘플이 어느 은닉축에서 더 크게 반응할지 먼저 가늠하게 만들었다.
- 번역 동기화 메모: 한국어 기준 원고에 사례 비교 표와 예측 표가 추가되었다. 향후 번역 시 동일한 비교 구조와 샘플 예측 흐름을 함께 반영해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 더 강화된 학습 밀도 기준에서 다시 보니, `은닉축이 다르게 켜진다`는 설명은 있었지만 `비슷한 최종 점수라도 내부 표현은 다를 수 있다`는 계산 감각을 독자가 오래 붙잡게 만드는 장면은 아직 부족했다.
- 본문 반영: `손글씨 숫자 분류` 사례에 `사람 기준 vs 표현 관점` 비교 표를 추가했다. `연습 및 예제`는 샘플을 4개로 늘리고, 실행 결과 아래에 `hidden 패턴 / score / 지금 읽어야 할 핵심` 비교 표를 추가해 sample_1과 sample_2처럼 비슷한 점수대라도 내부 표현은 다를 수 있음을 직접 읽게 만들었다.
- 번역 동기화 메모: 한국어 기준 원고에 손글씨 사례 비교 표와 4샘플 예제 해석 표가 추가되었다. 향후 번역 시 같은 비교 구조와 내부 표현 해석 표를 함께 반영해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 추가 재검토 결과, 사례와 예제가 각각은 강해졌지만 `원래 입력을 보는 기준`과 `은닉 표현을 보는 기준`이 절 전체 수준에서 다시 묶이는 장면은 약했다.
- 본문 반영: 사례 뒤에 `원래 입력만 볼 때 vs 은닉 표현까지 볼 때` 교차 비교표를 추가해 손글씨/고객 상태 사례를 한 번에 다시 묶었다. 예제 출력 뒤에는 `원래 입력 해석 vs 은닉 표현 해석` 비교표를 덧붙여, 비슷한 최종 점수라도 내부 좌표계가 다를 수 있다는 점을 더 직접적으로 닫았다.
- 번역 동기화 메모: Korean source now closes P5-2.2 with cross-case and post-output representation-reading tables. 향후 번역 시 same cross-case structure and representation-reading table should be preserved.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 초반부 재점검 결과, `고객 상태` 중심 비유가 Part 5 다른 절에서 정리한 운영·설비 신호 해석 톤과 어긋나 초심자 기준의 일관된 판단 장면을 약하게 만들고 있었다.
- 본문 반영: 도입부 특징 예시와 표현 설명의 표 데이터 장면을 `최근 경보 횟수`, `평균 진동 크기`, `온도 상승 폭`, `복구 지연 시간` 같은 설비 상태 신호로 교체했다. `고객 상태 표현` 사례는 `설비 경고 상태 표현`으로 바꾸고, 내부 축도 `정상 운전 유지`와 `고장 전조 위험`처럼 운영 판단에 직접 연결되는 표현으로 다시 정리했다.
- 번역 동기화 메모: Korean source replaced the customer-status metaphor with equipment-warning signals while keeping the same comparison structure. Future translations should preserve the operating-signal framing, not restore the older customer example.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 추가 재점검 결과, `손글씨 숫자 분류`, `얼굴의 눈` 같은 일반 시각 비유가 아직 남아 있어 초반부 표현 학습 설명의 운영·설비 신호 기준선이 끝까지 고정되지는 않았다.
- 본문 반영: 사례 1을 `설비 계기판 경고 상태 판별` 장면으로 교체하고, 중간 표현 예시도 `압력계 바늘`, `경광등`, `밸브 손잡이` 같은 운영 시각 단서로 다시 묶었다. 연습 및 예제의 샘플 이름과 입력 설명도 `minor_vibration`, `delay_dominant`, `combined_warning`, `borderline_unstable`처럼 운영 신호 축으로 바꾸고, `stability_axis`, `warning_axis` 해석으로 은닉 표현이 어떤 내부 축을 만드는지 더 직접 읽게 했다.
- 번역 동기화 메모: Korean source now frames P5-2.2 around equipment-panel warning recognition instead of handwritten digits and generic visual cues. Future translations should preserve the operational camera scene, the renamed sample cases, and the hidden-axis interpretation.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 변경 이유: 추가 재검토 결과, 연습 예제는 운영 신호 이름으로 바뀌어 있었지만 여전히 `두 입력 -> 두 은닉축` 장난감 수준에 가까워, 사례 1의 계기판 장면과 같은 판단 흐름으로 닫히는 검증 장면이 더 필요했다.
- 본문 반영: `연습 및 예제`를 `계기판 바늘 편차 + 경광등 점멸 + 밸브 손잡이 이탈` 프레임 4개를 읽는 예제로 교체했다. 은닉 표현도 `pressure_axis`, `warning_axis`, `coordination_axis` 세 축으로 확장하고, `needle_dominant_frame`, `beacon_valve_frame`, `combined_stop_frame`, `borderline_watch_frame` 비교를 통해 비슷해 보이는 이상 장면도 내부 표현과 다음 판단이 다를 수 있음을 더 직접 읽게 했다.
- 번역 동기화 메모: Korean source now closes P5-2.2 with an equipment-panel frame practice block using three raw signals and three hidden axes instead of the earlier smaller two-input exercise. Future translations should preserve the same frame-based comparison and axis-level interpretation.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.12
- 변경 이유: 사례와 예제는 좋아졌지만, `원래 입력을 그대로 읽는 방식`과 `은닉 표현으로 다시 읽는 방식`이 절 전체 수준에서 한 장면으로 압축되어 보이지는 않았다.
- 본문 반영: 사례 뒤에 공통 Mermaid 도식 1개를 추가해 시각 단서와 표 신호 모두 `원래 입력 -> 은닉축 형성 -> 최종 판단이 새 표현을 읽음` 흐름으로 다시 묶었다. 사례별 표를 다시 반복하지 않고, 두 사례가 공통으로 보여 주는 `내부 표현으로 다시 적기`만 압축하도록 위치와 설명을 정리했다.
- 번역 동기화 메모: Added a shared hidden-representation Mermaid pair so the Korean manuscript now summarizes both cases with one compact representation bridge. Future translations should preserve the same raw-input to hidden-axis contrast.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: `docs/assets/part-05/chapter-02/hidden-representation-bridge-en.mmd`, `docs/assets/part-05/chapter-02/hidden-representation-bridge-ko.mmd`
- 원문 기준 버전: `v2026.07.12`
