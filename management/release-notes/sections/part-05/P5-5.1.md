# Section Release Note

- Section ID: `P5-5.1`
- Source File: `docs/parts/part-05/chapter-05/section-01.md`

## 2026-07-07

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 gradient 감각 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: 순전파-손실-역전파 흐름 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: The backprop-introduction Mermaid now keeps paired `-en` and `-ko` source files for translation reuse. / pending

### v2026.07.11-2
- 본문 반영: 손실이 출력 책임과 가중치 책임을 거쳐 gradient로 바뀌고, 그다음 optimizer가 업데이트를 맡는다는 Mermaid 도식을 추가했다.
- 번역 동기화 메모: Added paired Mermaid assets for the loss-to-gradient role flow that separates backprop from optimizer updates. / pending

### v2026.07.12
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: 손실-그래디언트-업데이트 구분, 연쇄 법칙, 각 가중치 책임 분해, 계산 그래프로 넘기는 경계선을 `체크리스트` 하나로 재구성했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.

### v2026.07.12
- 본문 반영: 사례 1에 `사람 기준 vs 역전파 관점` 표를 추가하고, `예측이 조금 모자란 경우`와 `많이 모자란 경우`를 비교하는 문단을 넣었다. `연습 및 예제`는 `prediction_slightly_small`, `prediction_too_small`, `prediction_too_large` 세 사례로 확장해 실행 전 예측 표와 실행 후 `방향 + 수정 강도` 해설 표를 함께 보강했다.
- 번역 동기화 메모: 한국어 기준 원고에 direction-vs-magnitude comparison structure was added to the backprop intuition section. 향후 번역 시 같은 세 사례 비교와 post-output interpretation table을 유지해야 한다.
- 본문 반영: 사례 뒤에 `사람이 먼저 보기 쉬운 해석 vs 역전파가 더 분명하게 남기는 것` 교차 비교표를 추가해 단일 가중치/다층 책임 분해 사례를 한 번에 다시 묶었다. 예제 출력 뒤에는 `손실만 볼 때 vs gradient까지 볼 때` 비교표를 덧붙여, 손실이 곧바로 업데이트가 아니라 가중치별 방향·강도 신호로 다시 풀려야 한다는 점을 더 직접적으로 읽게 만들었다.

### v2026.07.12
- 본문 반영: 사례 1을 `재기동 차단 점수 하나 보정` 장면으로, 사례 2를 `재기동 판단 네트워크의 책임 분해` 장면으로 다시 썼다. `연습 및 예제`의 입력, 케이스 이름, 출력 항목, 해설 표도 `pressure_unrecovered`, `target_block_score`, `risk_weight`, `increase_risk_weight / decrease_risk_weight` 기준으로 바꾸어, 역전파가 `압력 미복귀 신호를 더 위험하게 읽어야 하는가`를 실제 수정 방향으로 다시 적는 단계임을 더 직접 드러냈다.
- 번역 동기화 메모: Korean source now frames the backprop intuition section around restart-block scoring and operational responsibility signals instead of abstract x/w/target wording. Future translations should preserve the same restart-block case framing in the examples, outputs, and interpretation tables.

### v2026.07.12
- 본문 반영: 사례 1은 `점수 하나를 맞추려면 어느 쪽으로 움직여야 하나`로 더 단순화해, 예측이 목표보다 크고 작은지에서 바로 `가중치 방향`이 읽히게 다시 썼다. 사례 2도 `재기동 판단 네트워크`보다 `마지막 점수만 고치면 왜 부족한가`라는 질문으로 바꾸고, `여러 입력 -> 중간 위험 신호 -> 최종 점수` 구조에서 책임이 앞단까지 전달된다는 직관을 먼저 보이게 정리했다. 사례 비교 표도 `가중치 하나 보정`과 `각 층 가중치별 gradient` 기준으로 단순화했다.
- 번역 동기화 메모: Korean source now simplifies the two backprop cases so that directional intuition and responsibility propagation are easier to see before operational details. Future translations should preserve this more accessible score-first framing.

### v2026.07.12
- 본문 반영: 사례 2 뒤에 공통 Mermaid 도식 1개를 추가해 `예측 점수가 너무 작음/큼 -> gradient가 증가/감소 방향을 남김`과 `최종 점수 오류 -> 마지막 층 -> 앞단 층 책임 전달` 흐름을 한 번에 요약했다. 사례별 설명을 다시 반복하지 않고, 두 사례가 공통으로 보여 주는 `방향 결정 + 뒤에서 앞으로 책임 전달`만 압축하도록 정리했다.
- 번역 동기화 메모: Added a shared backprop-direction Mermaid pair so the Korean manuscript now summarizes both cases with one compact flow. Future translations should preserve the same direction-plus-responsibility structure.

### v2026.07.12
- 본문 반영: 사례 1을 `pressure_unrecovered`, `risk_weight`, `predicted_block_score`, `target_block_score` 기준으로 다시 적어, `차단 점수 방향 판단 -> 위험 가중치 수정` 흐름이 연습 예제와 같은 용어 축으로 이어지게 정리했다.
- 번역 동기화 메모: The first backprop case now uses the same operational signal names as the exercise instead of abstract x/w/t notation. Future translations should preserve this aligned terminology so the case and exercise stay on the same axis.

### v2026.07.13
- 본문 반영: 사례 1의 방향·강도 비교표 뒤에 손실 곡선 위에서 `예측이 너무 낮음 -> 가중치 증가`, `예측이 너무 높음 -> 가중치 감소`, `큰 오차 -> 강한 신호`를 보여 주는 SVG 그래프를 추가하고, 역전파와 optimizer 역할 분리를 다시 설명했다.
- 번역 동기화 메모: Korean source now uses a localized SVG chart for gradient direction and magnitude intuition. Future translations should reference the matching English SVG or localized language asset.

### v2026.07.13
- 본문 반영: 손실 곡선 SVG를 다시 정리해 목표점 좌우 해석 박스, `현재 위치 A/B`, 가까운 오차와 먼 오차 대비, `gradient 부호` 문구를 직접 표시하도록 개선했다. 하단 요약문도 `역전파가 방향과 강도를 계산하고 optimizer가 실제 이동을 맡는다`는 구조를 더 짧게 고정하도록 다듬었다.
- 번역 동기화 메모: The gradient-direction SVG was revised to make the reading order clearer: current point, side of the target point, and stronger-vs-weaker signals are now explicit. Future translations should preserve the same reading order and label structure.

### v2026.07.13-python-chart
- 번역 동기화 메모: Future translations should preserve the shared Python-generated source for the backprop direction chart and localize only the derived labels.

### v2026.07.13-layout
- 번역 동기화 메모: The chart now follows the repository rule that figure titles and summary sentences stay outside the image. Future translations should preserve the leaner in-chart labeling and spacing.

### v2026.07.14-layout
- 본문 반영: P5-5.1 본문에서 결합 그래프 1개 대신 `backprop-gradient-signal-direction`, `backprop-gradient-signal-strength` SVG 2개를 순서대로 배치하고, 설명도 두 그래프를 함께 읽는 문장으로 조정했다.
- 번역 동기화 메모: Future translations should preserve the split two-chart layout for the first backprop case and keep deriving both assets from the shared Python chart source.

### v2026.07.14
- 본문 반영: 예제 출력 뒤에 `사례별 예측 차단 점수`, `사례별 손실`, `사례별 위험 가중치 gradient` 그래프를 분리해 추가하고, 각 단계에서 값의 의미가 어떻게 바뀌는지 설명했다.
- 번역 동기화 메모: Korean source now uses separate generated charts to trace the formula-based transformation from prediction to loss and risk-weight gradient. Future translations should use the localized assets and preserve the three-step reading.

## 2026-07-16

### v2026.07.16
- 변경 이유: P5-5.1은 `역전파의 직관`이라는 제목 아래 책임 분해 비유, gradient 부호 예제, 자동미분 보강이 섞여 있어 Chapter P5-5의 중심 질문을 닫지 못했다. Part 5 목차 개편 방향에 맞춰 `손실 숫자는 어떻게 파라미터별 gradient 신호가 되는가`로 중심축을 재설정했다.
- 본문 반영: Section 제목을 `P5-5.1 손실은 어떻게 gradient 신호가 되는가`로 바꾸고, 손실 계산, gradient 계산, optimizer update를 먼저 구분하도록 전면 재구성했다. 자동미분은 본편 밖 주제가 아니라 역전파가 코드에서 실행되는 이유를 이해하기 위한 필수 위치 감각으로 포함하고, 범위 밖 항목은 자동미분 엔진 내부 구현과 엄밀한 일반 이론으로 좁혔다.
- 추가 반영: 기존 원고는 `management/authoring/part-05-archive-2026-07-16-pre-restructure/` 아래에 개편 전 상태로 아카이브했다.
- 번역 동기화 메모: Future translations should retitle this section around loss-to-gradient conversion, not generic backprop intuition. Automatic differentiation should be presented as necessary context for understanding how backpropagation runs in code, while framework engine internals and rigorous general theory remain out of scope.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.16`
