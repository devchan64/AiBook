# Section Release Note

- Section ID: `P5-11.3`
- Source File: `docs/parts/part-05/chapter-11/section-03.md`

### v2026.07.20
- 저작권 검토: Part 5 Module 5 순차 검토 범위에서 CNN과 Vision Transformer 비교, 패치 토큰 관점, attention 연결 사례와 Python 예제를 확인했다. 사례·표·예제 코드는 자체 작성 자료로 판단했다.
- 출처 사용 점검: ViT 논문, Transformer 논문, Goodfellow et al.은 입력 단위와 구조 비교 배경 확인 용도로만 연결되어 있으며, 원문 그림·표·코드·문단 재사용 정황은 찾지 못했다.
- 관련 자산: Chapter 11 CNN/ViT Mermaid와 `cnn-vit-*.png` 계열 그래프는 자체 도식과 로컬 생성 스크립트 기반 자산으로 판단했다.
- 본문 반영: 없음. 기존 원문 버전 `v2026.07.19` 유지.
- 번역 동기화 메모: 번역본 영향 없음.

### v2026.07.18
- 본문 반영: 이 보충학습의 목적을 `CNN과 ViT 일반 비교`보다 `나중의 attention, Transformer, 생성형 AI 입력 이해를 위한 준비`가 먼저 보이도록 도입과 범위, 목표, 마무리 문장을 다시 정리했다.
- 본문 반영: 사례는 하나만 남기고, 예제 앞 예측 표와 중복 해설 표를 줄여 `지역 반응 출발`과 `패치 관계 출발`이라는 핵심 비교가 더 빠르게 드러나게 정리했다.
- 본문 반영: `생성형 AI로 이어서 보면 무엇이 준비되나` 구간을 추가해 텍스트 토큰, 이미지 패치 토큰, 멀티모달 입력 단위를 같은 질문 위에서 비교하도록 보강했다. self-attention 절과 마지막 요약에도 `이미지도 토큰처럼 읽힌다`는 연결을 더 분명히 남겼다.
- 본문 반영: patch를 왜 입력 단위로 읽어야 하는지, 그 관점이 생성형 AI의 image token·latent token·multimodal token 해석으로 어떻게 이어지는지 설명 문단과 비교 표를 더 보강해 중심축을 유지한 채 설명 밀도를 높였다.
- 번역 동기화 메모: 향후 번역 반영 필요. 한국어 원고에서 이 절의 목적 문장이 `생성형 AI 준비용 보충학습` 쪽으로 더 명시적으로 바뀌었으므로, 번역본에도 같은 목적 축과 축약된 사례/예제 구조를 함께 반영해야 한다.

### v2026.07.14
- 본문 반영: 예제 출력 뒤에 CNN 방식의 2x2 국소 후보 점수 지도와 ViT 방식의 3x3 패치 토큰 평균 지도를 각각 추가했다. 두 그래프 모두 본문 예제의 `inspection_frame`, `cnn_local_scores`, `vit_patch_tokens`와 같은 계산을 사용해 `어느 작은 위치를 다시 볼 것인가`와 `어느 구역 관계가 더 벌어지는가`를 분리해 읽도록 했다.
- 번역 동기화 메모: Korean source now adds two Python-generated charts tied directly to the P5-11.3 example: CNN local-score map and ViT patch-token mean map. Future translations should preserve this split-chart comparison with localized chart labels.

### v2026.07.09
- 본문 반영: `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: patch -> self-attention 흐름 Mermaid를 `assets/part-05/chapter-11/vit-patch-flow-ko.mmd` include 방식으로 전환했다.
- 번역 동기화 메모: Preserve the English Mermaid source pair as the canonical diagram for future translation pages. / pending

### v2026.07.12
- 본문 반영: CNN/ViT 출발 단위 차이, patch 의미, attention 절과의 연결 기준을 `체크리스트` 하나로 통합했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.
- 본문 반영: `장면으로 비교해 보면` 구간에 `사람이 먼저 보기 쉬운 기준 / CNN 관점 / ViT 관점` 비교 표와 최종 결과 문장을 추가했다. `연습 및 예제`에는 CNN 상위 후보 위치, ViT patch 평균, 구역 차이 비교를 코드 전에 예상하게 하는 표를 추가했다.
- 번역 동기화 메모: Korean source now closes the CNN-vs-ViT case with a human-vs-model comparison table and a pre-run prediction table for local-candidate vs patch-relation outputs. 향후 번역 시 same comparison structure를 유지해야 한다.
- 본문 반영: 사례 비교 끝에 `CNN은 재검사 위치`, `ViT는 구역 관계 어긋남`을 더 짧게 다시 묶는 문장을 추가했다. 예제 뒤에는 `CNN 상위 후보`, `ViT patch token`, `seal_code_gap vs blank_code_gap`을 각각 어떻게 읽어야 하는지 정리한 해설 표를 덧붙였다.
- 번역 동기화 메모: Korean source now adds a post-output interpretation table that separates local-candidate reading from patch-relation reading in the CNN-vs-ViT comparison. 향후 번역 시 같은 post-output comparison structure를 유지해야 한다.
- 본문 반영: patch token 설명 표와 장면 비교 문단을 설비 사진 기준으로 다시 정리했다. `자동차`와 `말 사진` 예시는 `탱크·밸브`, `배관·경광등` 장면으로 교체해, CNN의 지역 반응과 ViT의 patch 관계를 같은 산업 이미지 축에서 비교하도록 맞췄다.
- 번역 동기화 메모: Korean diagram labels were localized to match the deployed manuscript language while preserving the same structure as the English source assets. Future translations should keep the English originals as canonical and localize the visible labels per manuscript language.
- 본문 반영: 사례 1 아래에 `같은 배관 이미지 -> CNN의 지역 단서 질문 -> ViT의 패치 관계 질문` Mermaid를, 사례 2 아래에 `같은 설비 이미지 -> 재검사 위치 단서 vs 구역 관계 단서` Mermaid를 추가했다. 본문 안의 `patch`, `patch token`, `local window` 혼합 표기도 `패치`, `패치 토큰`, `국소 창` 중심으로 다시 맞췄다.

### v2026.07.17
- 본문 반영: `이 보충학습의 범위`를 생략 항목 나열보다 `CNN과 ViT를 어떤 계산 단위 비교로 읽을 것인가`라는 현재 절의 역할이 먼저 보이도록 다시 정리했다.
- 본문 반영: self-attention 자체는 `P5-13.2`, `P5-14.1` handoff로만 남기고, ViT 학습 레시피·세부 구조 계보·benchmark 경쟁 같은 바깥 항목 나열은 범위 안내에서 제거했다.
- 번역 동기화 메모: 확인 필요. 한국어 원고의 범위 안내와 handoff 문장이 바뀌었으므로, 향후 번역본에는 같은 범위 축약과 `P5-13.2`·`P5-14.1` 연결 구조를 함께 반영해야 한다.

## 2026-07-18

### v2026.07.18
- 번역 동기화 메모: `docs/parts/part-05/chapter-11/section-03.en.md` was added and aligned to the current Korean source version, preserving the stronger framing of this section as preparation for attention, Transformers, and generative AI, the shortened single-case comparison, the image-token bridge, and the paired English Mermaid/PNG assets.
- 번역 반영 상태: 영문 번역 반영 완료
- 관련 자산: `docs/assets/part-05/chapter-11/vit-patch-flow-en.mmd`, `docs/assets/part-05/chapter-11/cnn-vit-valve-body-case-flow-en.mmd`, `docs/assets/part-05/chapter-11/cnn-vit-cnn-local-score-map-en.png`, `docs/assets/part-05/chapter-11/cnn-vit-patch-token-mean-map-en.png`
- 원문 기준 버전: `v2026.07.18`

### v2026.07.18-zh
- 번역 동기화 메모: `docs/parts/part-05/chapter-11/section-03.zh.md` was added and aligned to the current Korean source version, preserving the stronger framing of this section as preparation for attention, Transformers, and generative AI, the shortened single-case comparison, the image-token bridge, and the localized Chinese Mermaid/PNG assets.
- 번역 반영 상태: 중국어 번역 반영 완료
- 관련 자산: `docs/assets/part-05/chapter-11/vit-patch-flow-zh.mmd`, `docs/assets/part-05/chapter-11/cnn-vit-common-input-compare-zh.mmd`, `docs/assets/part-05/chapter-11/cnn-vit-valve-body-case-flow-zh.mmd`, `docs/assets/part-05/chapter-11/cnn-vit-beacon-tank-case-flow-zh.mmd`, `docs/assets/part-05/chapter-11/cnn-vit-cnn-local-score-map-zh.png`, `docs/assets/part-05/chapter-11/cnn-vit-patch-token-mean-map-zh.png`
- 원문 기준 버전: `v2026.07.18`
