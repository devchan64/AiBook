# Section Release Note

- Section ID: `P5-4.1`
- Source File: `docs/parts/part-05/chapter-04/section-01.md`

### v2026.07.31
- 후속 보강: 원고 본문에서 `오픈체크리스트`, `중심축` 같은 관리 표현을 제거하고, 독자가 사례 안에서 확인할 학습 판단 문장으로 다시 정리했다.
- 후속 보강: 소제목 정리에 그치지 않도록 사례 절 첫머리에 중심축 확인 질문 2개를 추가했다.
- 변경 이유: Part 5 보강 라운드에서 진행 메타 성격의 `사례 및 예시` 소제목을 내용형 제목으로 정리해 독자가 사례 절의 딥러닝 계산 장면과 판단 기준을 바로 읽을 수 있게 했다.
- 소제목 정리: 한국어 원문의 `사례 및 예시` 절 제목을 `손실 함수: 확인할 판단 기준`으로 바꿔 사례 절이 맡는 딥러닝 계산 장면과 판단 기준을 바로 드러내도록 했다.
- 번역 동기화 메모: 이번 소제목 정리는 한국어 원문 기준으로 먼저 반영했으며, 영어판과 중국어 간체판은 후속 동기화가 필요하다.
- 번역 반영 상태: 한국어 원문 반영 완료, 영어판과 중국어 간체판 미반영.
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.31`

### v2026.07.26
- 본문 반영: 한국어, 영어, 중국어 간체판 본문 Version을 `v2026.07.26`으로 갱신했다.
- 링크 정리: 중국어 간체판의 `loss function` 개념사전 링크를 중국어 공개 색인 경로(`/AiBook/zh/reference/concept-glossary-pinyin/l/#loss-function`)로 바로잡았다.
- 개념사전 반영: 손실 함수 복귀 링크를 `metric`, `squared-error`, `cross-entropy`까지 확장했다. `cross-entropy` 한국어·영어·중국어 간체 항목을 추가하고, `metric`과 `squared-error` 다국어 항목의 등장 Section에 `P5-4.1`을 반영했다.
- 번역 동기화 메모: 영어판 제목 대소문자 표기를 현재 번역 가이드 기준에 맞게 정리했다. 개념 설명, 사례, 자산, 출처 내용은 변경하지 않았다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.26`

### v2026.07.20
- 본문 반영 내용: 한국어, 영어, 중국어 간체판의 Python 예제 첫 줄에 배치별 squared error를 비교해 평균 손실과 worst case가 예측 수정 우선순위를 어떻게 만드는지 확인하는 목적을 설명하는 주석을 추가했다.
- 저작권 검토: Part 5 Module 2 순차 검토 범위에서 손실 함수의 역할, metric과의 구분, 배치별 손실 예제와 Python 실험을 확인했다. 사례·표·예제 코드는 본문 학습 목적에 맞춘 자체 작성 자료로 판단했다.
- 출처 사용 점검: Goodfellow et al., Bishop 교재는 손실·목적함수·metric 구분의 배경 확인 용도로만 연결되어 있으며, 원문 문제·표·도식·코드 재사용 정황은 찾지 못했다.
- 관련 자산: `loss-role-flow-ko.mmd`, `loss-vs-metric-case-flow-ko.mmd`, `squared-error-loss-ko.svg`, `cross-entropy-loss-ko.svg`, `loss-example-batch-priority-ko.svg`는 자체 Mermaid와 로컬 생성 스크립트 기반 자산으로 판단했다.
- 본문 반영: 한국어, 영어, 중국어 간체판의 범위·목표 제목을 `손실 함수가 필요한 질문`, `오차를 숫자로 읽는 판단 기준` 축으로 바꾸어 진행 메타 제목을 제거했다.
- 번역 동기화 메모: 영어·중국어 번역본도 같은 제목 구조로 동기화하고 본문 Version을 `v2026.07.20`으로 맞췄다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.20`

### v2026.07.19
- 근거 보강: URL 없이 남아 있던 Bishop 교재 출처에 Springer 원문 페이지 링크와 확인 날짜를 추가하고, 본문 메타데이터를 현재 Section 형식으로 정리했다.
- 번역 동기화 메모: 향후 번역본 작성 시 같은 출처 링크와 확인 날짜를 반영해야 한다.

## 2026-07-07

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 손실 계산 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: 손실이 무엇을 재는가 구간 아래에 손실 역할 흐름 Mermaid를 추가하고, 손실이 다음 업데이트로 이어지는 연결 지점이라는 해설 문장을 보강했다.
- 번역 동기화 메모: Added paired Mermaid assets for the loss-to-update role flow and clarified the role of loss as the bridge from prediction-target gap to parameter updates. / pending

### v2026.07.12
- 본문 반영: 손실과 metric 구분, 손실의 학습 기준 역할, 일반화 비자동성, 다음 절과 역전파로 이어지는 경계선을 `체크리스트` 하나로 재구성했다.
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조 정리, 손실 해석 비교 표, 샘플별 예측 표, 회귀/분류 사례 비교 표, 신규 Mermaid 자산 쌍을 향후 번역에도 같은 의미 구조로 반영해야 한다.

### v2026.07.12
- 본문 반영: 회귀 사례를 배치 에너지 사용량 예측으로, 분류 사례를 검사 상태 분류로 교체하고 비교표의 용어도 실제 사용량, 미세 스크래치 확률 기준으로 다시 정리했다.
- 번역 동기화 메모: Korean source now explains loss magnitude with batch-energy regression and inspection-state classification instead of housing-price and animal examples. Future translations should preserve the same operational framing.

### v2026.07.12-2
- 본문 반영: `연습 및 예제`의 샘플 이름을 `night_shift_batch`, `stabilized_batch`, `restart_delay_batch`로 바꾸고, 출력 뒤 비교표와 운영 해석도 같은 이름 기준으로 다시 정리했다. 이를 통해 평균 손실과 worst case 해석이 실제 배치 상태 비교처럼 읽히게 보강했다.
- 번역 동기화 메모: Korean source now uses operational batch names instead of abstract sample labels in P5-4.1. Future translations should preserve the same batch-oriented naming and worst-case interpretation.

### v2026.07.13
- 본문 반영: Python 코드와 실행 출력 블록을 제거하고, 세 배치의 `target/prediction/squared error`를 표로 직접 정리한 뒤 `평균 손실`, `worst case`, `값을 바꿔 볼 때 다시 읽을 질문`으로 연습을 닫도록 바꿨다. 절 목표 문구도 코드 실행이 아니라 `어떤 오답을 먼저 크게 고칠 것인가`를 설명하는 쪽으로 맞췄다.
- 번역 동기화 메모: Korean source now closes P5-4.1 without a Python block and instead uses a non-code loss-decomposition exercise focused on worst-case prioritization. Future translations should preserve that learning-flow emphasis.

### v2026.07.13-python-chart
- 번역 동기화 메모: Future translations should keep the same Python-generated loss chart source and only localize the derived labels.

### v2026.07.14-layout
- 본문 반영: P5-4.1 본문에서 결합 손실 차트 1개 대신 `squared-error-loss`와 `cross-entropy-loss` 개별 SVG 2개를 순서대로 배치하고, 설명 문장도 두 그래프를 함께 읽는 형태로 조정했다.
- 번역 동기화 메모: Future translations should preserve the split two-chart layout for P5-4.1 and keep deriving both language assets from the shared Python chart source.

### v2026.07.14
- 본문 반영: `restart_delay_batch`를 고치는 경우와 `night_shift_batch`만 고치는 경우를 비교하는 Python 예제를 추가해, 손실이 단순 평균값이 아니라 우선 수정 대상을 드러내는 신호라는 점을 실험형으로 확인하게 했다.
- 번역 동기화 메모: Korean source now adds an experimental Python example that changes batch predictions and compares mean loss with the remaining worst case. Future translations should preserve the same experiment-oriented role rather than reverting to a fixed verification snippet.

## 2026-07-17

### v2026.07.17
- 변경 이유: 외부 자료 기준으로 P5-4.1을 다시 확인한 결과, `loss`와 `objective/cost`의 관계, 그리고 metric의 역할 범위를 조금 더 표준적으로 잡을 필요가 있었다.
- 본문 반영: 도입부에 샘플별 손실과 전체 목적함수(objective/cost)를 구분하는 짧은 설명을 추가하고, `metric`은 단순히 사람이 읽는 값이 아니라 평가와 선택에도 쓰인다는 점이 드러나도록 표와 해설을 조정했다. `손실이 왜 하나의 숫자로 모이는가` 구간에는 배치 평균/합과 정규화 항이 실제 최적화 대상으로 이어질 수 있다는 문장을 덧붙였다.
- 추가 반영: `깊게 다루지 않습니다` 목록을 제거하고, 이번 절은 손실이 예측과 목표의 어긋남을 학습 가능한 숫자로 바꾸는 역할을 먼저 붙잡고 유형별 손실과 역전파 연결은 뒤 절로 넘기는 구조로 범위 안내를 정리했다.
- 번역 동기화 메모: `docs/parts/part-05/chapter-04/section-01.en.md`에 loss와 objective/cost의 light-but-explicit distinction, 그리고 metrics를 evaluation/selection criteria로 읽는 기준을 반영했다.
- 번역 동기화 메모: `docs/parts/part-05/chapter-04/section-01.zh.md`에 같은 loss/objective/cost 구분, metric의 evaluation/selection 역할, 손실 평균과 worst case를 함께 읽는 실험 구조를 반영했다. Chapter 4 공개 본문용 `loss-role-flow-zh.mmd`, `loss-vs-metric-case-flow-zh.mmd`와 `loss-example-*-zh.svg`, `squared-error-loss-zh.svg`, `cross-entropy-loss-zh.svg` 자산도 함께 동기화했다.
- 번역 반영 상태: 영문 번역 반영 완료 / 중문 번역 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.17`
