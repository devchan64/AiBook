# Section Release Note

- Section ID: `P5-15.3`
- Source File: `docs/parts/part-05/chapter-15/section-03.md`

### v2026.07.31
- 변경 이유: 샘플링을 후보 분포에서 실제 출력을 꺼내는 절차로 읽고, Part 6의 생성 설정 설명으로 이어질 기록 기준을 보강할 필요가 있었다.
- 본문 반영 내용: 한국어 원문 `Version`을 `v2026.07.31`로 갱신하고, `candidate_distribution`, `selection_rule`, `temperature_setting`, `top_k_or_top_p`, `selected_output`, `variation_count` 필드 예시를 추가했다.
- 번역 동기화 메모: 영어판과 중국어 간체판은 이번 한국어 원문 보강 문단을 후속 동기화해야 한다.
- 번역 반영 상태: 한국어 원문만 반영.
- 관련 자산: 변경 없음.
- 원문 기준 버전: `v2026.07.31`
- 변경 이유: Part 5 보강 라운드에서 진행 메타 성격의 `사례 및 예시` 소제목을 내용형 제목으로 정리해 독자가 사례 절의 딥러닝 계산 장면과 판단 기준을 바로 읽을 수 있게 했다.
- 소제목 정리: 한국어 원문의 `사례 및 예시` 절 제목을 `샘플링은 후보 분포에서 실제 출력을 어떻게 꺼내는가: 확인할 판단 기준`으로 바꿔 사례 절이 맡는 딥러닝 계산 장면과 판단 기준을 바로 드러내도록 했다.
- 번역 동기화 메모: 이번 소제목 정리는 한국어 원문 기준으로 먼저 반영했으며, 영어판과 중국어 간체판은 후속 동기화가 필요하다.
- 번역 반영 상태: 한국어 원문 반영 완료, 영어판과 중국어 간체판 미반영.
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.31`
### v2026.07.26
- 정합성 점검: 한국어·영어·중국어 간체판의 본문 `Version`을 `v2026.07.26`으로 맞췄다.
- 링크 정리: 중국어 간체판의 `sampling` 개념사전 링크를 중국어 pinyin 색인 경로로 고쳤다.
- 표기 정리: 영어판 H1과 소제목 표기를 문장형 대소문자 기준에 맞춰 정리했다.
- 개념사전 정리: `sampling` 영문 항목의 한국어 잔여 본문을 영어로 정리하고, 중국어 간체 항목과 pinyin 색인 include를 추가했다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-05/chapter-15/section-03.md` `v2026.07.26`

### v2026.07.24
- 변경 이유: 기존 예제가 Ollama 로컬 실행을 전제로 해, 독자가 샘플링 설정의 핵심 효과를 즉시 재현하기 어려울 수 있었다.
- 본문 반영 내용: 한국어, 영어, 중국어 간체판의 `연습 및 예제` 앞부분에 고정 logits 기반 예제를 추가했다. 같은 후보 점수에서 `argmax`, `temperature_0.7`, `temperature_1.4`, `top_k_3_temperature_1.0`을 비교해 후보 확률, 40회 선택 빈도, entropy, top choice를 출력하도록 구성했다.
- 본문 반영 내용: 기존 Ollama 로컬 LLM 호출 예제는 `선택 예제`로 내려, 실제 모델 출력 변화 관찰은 유지하되 기본 학습 경로는 `.venv`의 현재 의존성만으로 실행 가능하게 조정했다.
- 관련 자산: `docs/assets/part-05/chapter-15/p5_15_3_sampling_controls.py`, `sampling-control-probabilities-*.png`, `sampling-control-counts-*.png`
- 번역 동기화 메모: 영어·중국어 간체 번역본을 원문 `v2026.07.24` 기준으로 맞추고, 언어별 후보 문구와 그래프 자산을 대응시켰다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.24`

### v2026.07.22
- 변경 이유: Part 5는 생성 모델, 후보 분포, 샘플링까지 다룬 뒤이므로 실제 로컬 LLM 실행을 받아낼 수 있는 위치라는 판단을 반영했다.
- 본문 반영 내용: 기존 `random.choices` 기반 Python 예제를 Ollama 로컬 `/api/generate` 호출 예제로 교체했다. 같은 프롬프트를 낮은 `temperature`와 높은 `temperature` 설정에서 반복 실행해 출력 안정성과 변주 폭을 비교하도록 구성했다.
- 본문 반영 내용: 예제 목표를 API 사용법 숙달이 아니라 `후보 분포 -> 샘플링 -> 출력 검토` 흐름을 실제 출력으로 확인하는 실험으로 명시했다.
- 출처 사용 점검: Ollama API Introduction과 Generate a response 공식 문서를 로컬 API 기본 주소와 `/api/generate` 동작 확인 근거로 추가했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본도 `random.choices` 기반 모의 예제를 Ollama 로컬 `/api/generate` 예제로 교체하고, 같은 프롬프트에서 낮은/높은 `temperature`를 비교하는 구조로 맞췄다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.22`

### v2026.07.21
- 진행 메타 축소: 질문 목록, 판단 기준 목록, 읽기 순서 목록을 줄이고 `후보 분포와 실제 출력 선택은 다르다`는 중심 설명으로 원문·영문·중문 도입부를 압축했다.
- Section 경계 조정: P5-15.1/P5-15.2에서 이미 다룬 `출력이 하나로 고정되지 않을 수 있음` 설명을 줄이고, P5-15.3은 같은 후보 분포에서 실제 선택 절차가 결과를 바꾸는 감각에 집중하도록 조정했다.
- 도식 정리: 사례 묶음용 `sampling-task-flow` 도식 참조를 제거하고, 점수 계산과 실제 선택 단계 구분을 보여 주는 `sampling-selection-flow` 도식만 유지했다.
- 예제 진입부 압축: Python 예제 앞의 입력·출력·문제 상황·확인 개념 목록을 줄이고, `argmax_choice`, `argmax_sentences`, `sampled_sentences`, `counts`, `avg_length` 확인표와 예측표 중심으로 정리했다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.21`

### v2026.07.20
- 구조 재배치: 기존 P5-15.2 샘플링 절을 Chapter 15의 세 번째 절 P5-15.3으로 이동하고, 앞 절 참조를 새 후보 분포 절 P5-15.2 기준으로 갱신했다.
- 본문 반영 내용: 한국어, 영어, 중국어 간체판의 Python 예제 첫 줄에 같은 대응 후보와 가중치에서 argmax와 sampling이 출력 다양성, 선택 횟수, 평균 문장 길이를 어떻게 다르게 만드는지 비교하는 목적을 설명하는 주석을 추가했다.
- 저작권 검토: Part 5 Module 6 순차 검토 범위에서 생성과 샘플링, argmax와 sampling 차이, 후보 문구 가중치 시나리오 Python 예제와 SVG 그래프를 확인했다. 사례·표·예제 코드는 자체 작성 자료로 판단했다.
- 출처 사용 점검: Goodfellow et al., Manning·Schutze 교재, Jurafsky·Martin draft는 sampling과 통계적 언어 처리 배경 확인 용도로만 연결되어 있으며, 원문 예제·도식·코드·문단 재사용 정황은 찾지 못했다.
- 관련 자산: `sampling-selection-flow-ko.mmd`, `sampling-task-flow-ko.mmd`, `sampling-candidate-weights-ko.svg`, `sampling-choice-counts-ko.svg`는 자체 Mermaid와 로컬 생성 스크립트 기반 자산으로 판단했다.
- 본문 반영: 한국어, 영어, 중국어 간체판의 `이 절의 범위`, `이 절의 목표`, `이 절을 읽는 순서` 계열 제목을 `샘플링이 생성 결과를 바꾸는 질문`, `후보 선택과 다양성의 판단 기준`, `후보 분포에서 실제 출력까지` 축으로 바꾸어 진행 메타 제목을 제거했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본도 같은 제목 구조로 동기화하고 본문 Version을 `v2026.07.20`으로 맞췄다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.20`

### v2026.07.19
- 근거 보강: 통계적 자연어 처리와 샘플링 설명의 기준 출처에 MIT Press/Stanford 원문 페이지 링크와 확인 날짜를 추가하고, 본문 메타데이터를 현재 Section 형식으로 정리했다.
- 예제 보강: `response_weights`를 `base`, `sharper`, `flatter` 세 시나리오로 바꿔 보도록 Python 예제를 재구성해, 같은 후보라도 가중치 분포가 뾰족한지 평평한지에 따라 실제 선택 폭이 달라진다는 점을 확인하게 했다.
- 번역 반영: 영어판과 중국어 간체판에도 같은 가중치 시나리오 비교 예제를 반영했다.
- 번역 동기화 메모: 향후 번역본 작성 시 같은 출처 링크와 확인 날짜를 반영해야 한다.

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: 회수 위치를 `P6-5.2`로 구체화하고, 후보 분포 변화를 비교하는 `샘플링 민감도 실험` 표를 추가했다.
- 추가 반영: 샘플링 선택 흐름 Mermaid와 사례 묶음 Mermaid를 각각 외부 자산 include 방식으로 전환했다.
- 번역 동기화 메모: Clarified sampling follow-up location, added sensitivity experiment note, and externalized Mermaid assets for reuse. / pending

### v2026.07.12
- 본문 반영: 샘플링 정의, argmax와 sampling 차이, 다양성·안정성 균형, 다음 Part 설정 읽기 기준을 `체크리스트` 하나로 통합했다. `연습 및 예제`를 일반 자동완성 문장 대신 `배치 점검 결과` 운영 안내 문구 생성 장면으로 교체하고, 실행 전 예측 표와 `argmax_sentences / sampled_sentences / counts / average_sampled_length` 비교를 넣어 같은 후보 분포라도 선택 절차가 바뀌면 운영 메시지 경험이 달라진다는 점을 더 직접 보강했다.
- 번역 동기화 메모: Korean source now reinforces the sampling introduction with an explicit score-vs-selection contrast and aligns the remaining examples around operational guidance, field-support responses, and equipment-scene image variation instead of generic chatbot or cafe-scene prompts. Future translations should preserve the same operations-oriented selection frame.

### v2026.07.12-asset
- 번역 동기화 메모: Korean sampling-task diagram labels were aligned to the manuscript language while keeping the same branching structure. Future translations should preserve the same structure and language-specific asset split.

### v2026.07.12
- 본문 반영: 점검 안내 문구, 현장 지원 응답, 이미지 생성 프롬프트 세 사례 각각에 `그래서 이 사례에서 확인해야 할 결과` 문장을 추가하고, 세 사례를 함께 묶는 총괄 결과 문장도 보강했다.
- 번역 동기화 메모: Korean source now closes each sampling case with an explicit result sentence and adds a shared conclusion about output selection control. Future translations should preserve the same case-closing structure.

### v2026.07.12
- 본문 반영: 예제 변수 이름을 `inspection_prefix`, `response_candidates`, `response_weights` 기준으로 정리하고, 후속 해설 표도 같은 이름으로 맞췄다.
- 번역 동기화 메모: The Korean sampling exercise now keeps operation-specific variable naming through the code and interpretation tables instead of generic prefix/candidates/weights labels. Future translations should preserve this aligned naming.

### v2026.07.12-diagram
- 번역 동기화 메모: The Korean sampling-selection diagram now localizes its internal labels while preserving the same three-step selection structure. Future translations should keep the same structure with language-specific labels on each asset.

### v2026.07.13
- 본문 반영: argmax/sampling/temperature 구분 뒤에 후보별 상대 비중과 20회 샘플링 빈도를 나란히 보여 주는 SVG 그래프를 추가하고, sampling을 순수 무작위가 아니라 후보 분포 기준의 가중 선택으로 읽는 설명을 보강했다.
- 번역 동기화 메모: Korean source now uses a localized SVG chart for candidate-distribution vs sampled-choice intuition. Future translations should reference the matching English SVG or localized language asset.

### v2026.07.13-python-chart
- 번역 동기화 메모: Future translations should preserve the shared Python-generated source for the sampling chart and localize only the derived labels.

### v2026.07.13-layout
- 번역 동기화 메모: The paired sampling charts now keep only labels that are required to read the candidate-distribution and sampled-choice contrast. Future translations should preserve this chart/body role split.

### v2026.07.14-layout
- 본문 반영: 현재 P5-15.3으로 이동된 샘플링 본문에서 기존 통합 그래프 대신 `후보 문구별 상대 비중`, `20회 샘플링 선택 빈도` 두 장의 SVG를 순서대로 배치해 읽기 흐름을 분리했다.
- 번역 동기화 메모: The sampling chart is now split into separate candidate-weight and sampled-count SVGs so translations can preserve the same two-step reading order with matched language assets.

### v2026.07.17
- 본문 반영: `사례 및 예시`를 점검 안내·현장 지원·이미지 생성 3개 사례의 긴 나열에서, 점검 안내 대표 사례와 출력 선택 비교표 구조로 다시 묶어 현재 절의 중심 질문이 `같은 후보 분포에서도 실제 선택 절차가 결과 변주 폭을 어떻게 바꾸는가`에 더 오래 머물도록 정리했다.
- 본문 반영: `이 절의 범위`에서 beam search, top-k/top-p 수학 비교, temperature 공식 전개, diffusion 샘플링 구현 같은 생략 목록을 걷어내고, 이번 절이 `후보 분포를 계산한 뒤 실제 출력을 어떤 감각으로 꺼내는가`와 `다양성과 안정성 균형이 왜 결과를 바꾸는가`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 생성 설정 handoff는 `P6-5.2`로 직접 연결되게 맞췄다.
- 추가 반영: 범위 표 머리글의 `범위 밖` 표현도 `뒤 Part에서 더 읽는 것`으로 바꾸어, 생략 목록처럼 읽히지 않게 정리했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P5-15.3 closes the output-selection intuition first and hands concrete generation-setting reading to Part 6.

### v2026.07.26
- 번역 동기화 메모: 중국어 간체판의 개념사전 안내 문구를 현재 링크 문체에 맞춰 `概念词汇表里的[采样（sampling）]` 형식으로 정리했다. 개념 설명, Python 예제, Ollama 예제, 자산, 출처 내용은 변경하지 않았다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 원문 기준 버전: `v2026.07.26`

## 2026-07-18

- 영문 번역 반영: `docs/parts/part-05/chapter-15/section-02.en.md`를 추가해 sampling 정의, argmax 대 sampling 비교, 다양성과 안정성 균형, 점검 결과 안내 문구 생성 예제를 영어판 본문으로 동기화했다.
- 번역 자산 반영: 본문에서 `sampling-selection-flow-en.mmd`, `sampling-task-flow-en.mmd`, `sampling-candidate-weights-en.svg`, `sampling-choice-counts-en.svg`를 참조하도록 맞췄다.
- 번역 동기화 메모: English section now matches the Korean v2026.07.17 structure and preserves the score-vs-selection contrast before the Part 6 generation-setting handoff.
- 중문 번역 반영: `docs/parts/part-05/chapter-15/section-02.zh.md`를 추가해 sampling 정의, argmax 대 sampling 비교, 다양성과 안정성 균형, 점검 결과 안내 문구 생성 예제를 중국어 간체 본문으로 동기화했다.
- 번역 자산 반영: 본문에서 `sampling-selection-flow-zh.mmd`, `sampling-task-flow-zh.mmd`, `sampling-candidate-weights-zh.svg`, `sampling-choice-counts-zh.svg`를 참조하도록 맞췄다.
- 번역 동기화 메모: Simplified Chinese section now matches the Korean v2026.07.17 structure and preserves the score-vs-selection contrast before the Part 6 generation-setting handoff.
