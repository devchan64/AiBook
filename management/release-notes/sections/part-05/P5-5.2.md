# Section Release Note

- Section ID: `P5-5.2`
- Source File: `docs/parts/part-05/chapter-05/section-02.md`

### v2026.07.19
- 근거 보강: URL 없이 남아 있던 Bishop 교재 출처에 Springer 원문 페이지 링크와 확인 날짜를 추가하고, 본문 메타데이터를 현재 Section 형식으로 정리했다.
- 번역 동기화 메모: 향후 번역본 작성 시 같은 출처 링크와 확인 날짜를 반영해야 한다.

### v2026.07.09
- 본문 반영: `사례로 보기`를 `사례 및 예시`로, `실행 가능한 Python 예제로 중간값과 gradient 흐름 보기`를 `연습 및 예제`로 조정했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 현재 `docs/parts/part-05/` 아래의 대응 `.en.md`, `.zh.md` 번역본이 없으므로, 이번 구조 조정은 한국어 원고와 공통 릴리즈노트 기준으로만 반영했다. 향후 영문·중국어 간체 번역본을 만들 때 같은 구조 표지와 문단 순서를 적용해야 한다.

### v2026.07.11
- 본문 반영: 계산 그래프 도식을 외부 `-ko.mmd` 자산으로 include하도록 바꾸고, 대응 `-en.mmd` 원본 파일을 함께 추가했다.
- 번역 동기화 메모: The computation-graph Mermaid now keeps paired `-en` and `-ko` source files for translation reuse. / pending

### v2026.07.12
- 추가 반영: 집필 순서만 예고하는 메타형 연결 표지를 제거했다.
- 본문 반영: 계산 그래프 정의, forward/backward, 중간값 재사용, 국소 미분 규칙, optimizer와 학습·추론으로 넘어가는 기준을 `체크리스트` 하나로 통합했다.
- 번역 동기화 메모: 한국어 기준 원고의 말미 구조만 정리했다. 향후 번역 시 동일한 단일 checklist 구조를 적용해야 한다.

### v2026.07.13
- 본문 반영: `연습 및 예제` 도입부에 `block_gate_open`과 `block_gate_closed`를 한 장에서 비교하는 SVG를 추가했다. 같은 계산 그래프라도 ReLU 앞값 부호에 따라 backward가 앞단 파라미터까지 이어지거나 끊기는 모습을 forward 값과 gradient 라벨로 함께 읽게 정리했다.
- 번역 동기화 메모: Added a paired `-en` / `-ko` SVG that contrasts an open and closed ReLU gate in the same computation graph. Future translations should keep the same two-panel structure and the distinction between loss magnitude and live gradient path.

### v2026.07.13-layout
- 본문 반영: ReLU 비교 SVG의 내부 제목·설명·하단 요약 박스를 제거하고, 두 패널의 핵심 노드와 gradient 경로만 남기도록 여백과 배치를 다시 조정했다.
- 번역 동기화 메모: The paired `-en` / `-ko` SVG now keeps only labels that are directly tied to nodes and gradient paths. Future translations should preserve this image/body role split.

### v2026.07.13
- 본문 반영: `연습 및 예제` 도입부에 이 코드가 `모델 학습 코드`가 아니라 `계산 그래프 노드 판독 예제`라는 역할 설명을 추가했다. 코드에는 각 노드의 forward 값, backward 신호, 읽는 관점을 묶은 `node_trace` 출력을 넣고, 출력 해설도 `손실 -> gradient` 요약보다 `노드별 경로 판독`이 중심이 되도록 조정했다.
- 번역 동기화 메모: Korean source now makes the Python example explicitly serve as a node-by-node computation-graph reading exercise. Future translations should preserve the same distinction between training code and graph-reading code, including the `node_trace` output role.

### v2026.07.14
- 본문 반영: Python 출력 뒤에 forward 노드별 값 그래프와 backward 노드별 gradient 그래프를 추가해, `block_gate_closed`의 손실은 크지만 ReLU 앞단 gradient는 끊긴다는 점을 분리해서 보게 했다.
- 번역 동기화 메모: Korean source now adds paired Matplotlib graph assets for the computation-graph Python example. Future translations should keep the forward-value graph and backward-gradient graph separated.

### v2026.07.12
- 본문 반영: 사례 1에 `사람 기준 vs 계산 그래프 관점` 표를 추가했다. `연습 및 예제`는 `relu_open`과 `relu_blocked` 두 사례를 비교하도록 바꾸고, 실행 전 예측 표와 실행 후 `gradient가 어디서 살아 있고 어디서 끊기는가` 해설 표를 덧붙였다.
- 번역 동기화 메모: 한국어 기준 원고에 open-vs-blocked ReLU comparison was added to show how forward sign information changes backward flow. 향후 번역 시 같은 two-case structure와 post-output interpretation table을 유지해야 한다.

### v2026.07.12
- 본문 반영: 사례 2를 `표면 검사 네트워크`로 바꾸고, 마지막 점수도 `고양이 0.82` 대신 `불량 0.82` 기준으로 다시 적었다. 이에 맞춰 앞단 합성곱이 만드는 특징도 표면 특징과 스크래치 판단 관점으로 정리했다.
- 번역 동기화 메모: Korean source now frames the computation-graph network case around surface inspection instead of pet classification. 향후 번역 시 same operational inspection framing을 유지해야 한다.

### v2026.07.12
- 본문 반영: 사례 절에 `사람이 먼저 보기 쉬운 결과`와 `계산 그래프가 더 분명하게 남기는 해석`을 나란히 두는 비교표를 추가했다. 예제 뒤에는 `relu_open`과 `relu_blocked`를 `손실만 보면 남기 쉬운 해석`과 `계산 그래프까지 보면 바뀌는 해석`으로 다시 읽는 해설 표를 덧붙였다.
- 번역 동기화 메모: Korean source now makes the distinction between loss magnitude and live gradient path explicit with a cross-case table and a post-output interpretation table. 향후 번역 시 같은 comparison structure를 유지해야 한다.

### v2026.07.12
- 본문 반영: 사례 2를 `재기동 차단 점수를 만드는 판단 네트워크`로 다시 쓰고, `연습 및 예제`의 입력, 케이스 이름, forward/backward 출력 항목을 `pressure_signal`, `risk_weight`, `base_block_bias`, `target_block_score`, `block_gate_open / block_gate_closed`, `weighted_pressure / block_logit / block_activation` 기준으로 바꾸었다. backward 해설도 `risk_weight`와 `base_block_bias`로 책임이 되돌아가는 흐름으로 다시 정리했다.
- 번역 동기화 메모: Korean source now frames the computation-graph section around restart-block scoring instead of abstract x/w/b/target labels. Future translations should preserve the same operational naming in the cases, forward values, backward values, and interpretation tables.

## 2026-07-16

### v2026.07.16
- 변경 이유: Part 5 목차 개편에서 Chapter P5-5의 중심축을 `손실에서 gradient로`로 바꾸면서, P5-5.2도 단순 계산 그래프 절이 아니라 자동미분이 gradient 계산을 어떻게 조직하는지 보여 주는 절로 역할을 명확히 해야 했다.
- 본문 반영: Section 제목을 `P5-5.2 계산 그래프(computation graph)와 자동미분(automatic differentiation)`으로 바꾸고, 도입부·범위·목표를 `순전파 계산 기록 -> 자동미분 -> backward gradient 경로` 중심으로 조정했다.
- 번역 동기화 메모: Future translations should pair computation graph with automatic differentiation in the title and opening frame, because this section explains the recorded structure that makes automated gradient computation readable.
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.16`

## 2026-07-17

### v2026.07.17
- 변경 이유: 범위 안내의 세 항목이 초심자 기준에서는 다소 압축적이고 기술적으로 보였다.
- 본문 반영: `깊게 다루지 않습니다` 목록을 `프레임워크가 계산 그래프를 기록·관리하는 방식의 차이`, `reverse-mode automatic differentiation의 엄밀한 수학 전개`, `자동미분 엔진의 상세 구현`처럼 더 직접 읽히는 문장으로 풀어 썼다.
- 번역 동기화 메모: Future translations should keep the scope boundary beginner-readable by expanding the excluded topics into plain descriptions instead of short technical labels.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.17`

- 추가 반영: 같은 목록이 여전히 초심자에게 불필요하게 무겁게 느껴질 수 있어, `프레임워크마다 구현이 어떻게 다른지`, `자동미분의 엄밀한 수학`, `프레임워크 내부 구현 상세`처럼 더 짧은 범위 표지로 다시 줄였다.
- 번역 동기화 메모: Future translations should prefer short scope labels over detailed technical exclusions when the audience is introductory.

- 추가 반영: 초심자에게 불필요한 범위 안내 자체를 줄이기 위해 `깊게 다루지 않습니다` 목록을 제거하고, 현재 절의 핵심 목적만 남기도록 정리했다.
- 번역 동기화 메모: Future translations should remove nonessential scope warnings when they do not help introductory readers follow the section.

- 추가 반영: 사례 1과 사례 2를 각각 더 길게 풀어 써서, `마지막 점수`와 `계산 경로`의 차이, `손실 크기`와 `앞단까지 살아 있는 gradient 경로`의 차이를 초심자가 단계적으로 따라가게 보강했다.
- 번역 동기화 메모: Future translations should preserve the step-by-step distinction between final score vs. computational path, and large loss vs. live backward path, because those are the main beginner-facing clarifications in the two cases.

- 추가 반영: 사례 1과 사례 2 바로 아래에 각각 작은 Mermaid 흐름도를 추가해, `점수 -> 경로 복원`과 `큰 손실 -> ReLU 문 -> gradient 생존 여부` 순서를 본문보다 짧게 다시 읽게 했다. 대응 `-en` / `-ko` 자산 파일을 `docs/assets/part-05/chapter-05/`에 함께 추가했다.
- 번역 동기화 메모: Future translations should keep the diagrams small and procedural, using the same node order that separates final score from path reading and large loss from live backward flow.

- 추가 반영: 자동미분 설명 아래에 `순전파 -> 손실 계산 -> 역전파` 흐름도를, 두 사례 비교표 아래에 `점수 오차 -> 책임 전달 -> 앞단까지 gradient 전파` 흐름도를 각각 추가해, 절 전체와 사례 묶음의 읽기 순서를 한 번 더 압축했다.
- 번역 동기화 메모: Future translations should preserve these two extra small diagrams as section-level reading guides, not as detailed replacements for the case-specific diagrams.

## 2026-07-18

### v2026.07.17
- 번역 동기화 메모: `docs/parts/part-05/chapter-05/section-02.en.md` was added and aligned to the current Korean source version, including the computation-graph plus automatic-differentiation framing, the English Mermaid/image assets, the step-by-step path-vs-score and live-gradient-path cases, and the `node_trace`-centered Python example.
- 번역 동기화 메모: `docs/parts/part-05/chapter-05/section-02.zh.md` was added and aligned to the current Korean source version, including the computation-graph plus automatic-differentiation framing, localized Chapter 5 Mermaid/image assets, the step-by-step path-vs-score and live-gradient-path cases, and the `node_trace`-centered Python example.
- 번역 반영 상태: 영문 번역 반영 완료 / 중문 번역 반영 완료
- 관련 자산: `docs/assets/part-05/chapter-05/computation-graph-flow-en.mmd`, `docs/assets/part-05/chapter-05/forward-loss-backward-flow-en.mmd`, `docs/assets/part-05/chapter-05/computation-graph-case1-path-vs-score-en.mmd`, `docs/assets/part-05/chapter-05/computation-graph-case2-loss-vs-gradient-en.mmd`, `docs/assets/part-05/chapter-05/backprop-direction-and-responsibility-flow-en.mmd`, `docs/assets/part-05/chapter-05/computation-graph-relu-gate-comparison-en.mmd`, `docs/assets/part-05/chapter-05/computation-graph-forward-trace-en.png`, `docs/assets/part-05/chapter-05/computation-graph-backward-trace-en.png`
- 원문 기준 버전: `v2026.07.17`
