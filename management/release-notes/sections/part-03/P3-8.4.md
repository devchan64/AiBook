# Section Release Note

- Section ID: `P3-8.4`
- Source File: `docs/parts/part-03/chapter-08/section-04.md`

### v2026.07.07

- 변경 이유: Chapter 8에는 비교표를 보수적으로 읽고 문장 강도를 조절하는 설명이 있었지만, 그 문장이 실제 운영 열과 검토 큐 기준으로 어떻게 구조화되는지는 별도 절로 닫혀 있지 않았다.
- 본문 반영: 새 Section `P3-8.4 보수적 해석 문장은 어떻게 경고 열과 검토 큐 기준으로 바뀌는가`를 추가했다. 비교 결과, 보수적 해석 문장, 구조화된 운영 출력의 세 층위를 나누고, `warning_level`, `review_needed`, `priority_score` 같은 열이 어디서 오는지 브리지 예시로 정리했다.
- 번역 동기화 메모: translation should preserve the bridge from conservative prose to structured operational outputs, especially the separation among report sentence, warning level, review flag, and queue priority. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: 기존 예시는 `warning_level`, `review_needed`, `priority_score`를 바로 출력해, 보수적 해석 문장이 운영 열로 어떻게 바뀌는지 중간 다리가 충분히 드러나지 않았다.
- 본문 반영: 같은 차이값 사례를 사람 해석 표와 운영 열 표로 나눠 출력하도록 예시를 바꿨다. `conservative_sentence`를 먼저 보여 준 뒤 `warning_level`, `review_needed`, `priority_score`로 압축되는 순서를 드러내어 `숫자 -> 문장 -> 운영 열` 흐름을 직접 읽을 수 있게 했다.
- 번역 동기화 메모: translation should preserve the staged flow from human-readable conservative sentence to structured operational columns, rather than jumping straight to warning fields. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-2

- 변경 이유: 이 절의 중심은 계산 예시보다 `비교 결과 -> 사람 문장 -> 운영 열`이라는 판단 구조에 있으므로, 코드 예시보다 도식이 더 직접적으로 설명을 지지한다.
- 본문 반영: 작은 코드 예시를 Mermaid 도식으로 교체했다. 같은 차이값 사례가 해석 강도 조절을 거쳐 `warning_level`, `review_needed`, `priority_score`로 나뉘는 흐름을 세 단계 도식으로 보여 주도록 정리했다.
- 번역 동기화 메모: translation should preserve the decision-flow diagram from comparison result to conservative sentence to operational fields, rather than reintroducing a code-shaped example. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-3

- 변경 이유: 표 예시와 도식 예시가 함께 남은 상태에서 소제목이 모두 `예시`로 보이면 역할 차이가 흐려질 수 있었다.
- 본문 반영: 앞단 표 비교는 `비교 표로 먼저 보기`, 뒤의 Mermaid는 `작은 도식으로 보기`로 소제목을 분리해, 표와 도식의 설명 역할을 제목에서도 구분했다.
- 번역 동기화 메모: preserve the distinction between the comparison-table subsection and the diagram subsection in the labels as well as the content. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`
