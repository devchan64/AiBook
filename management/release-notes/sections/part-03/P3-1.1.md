# Section Release Note

- Section ID: `P3-1.1`
- Source File: `docs/parts/part-03/chapter-01/section-01.md`

### v2026.09.19
- 변경 이유: 첫 요약 표에 지표가 한꺼번에 등장해 선택 이유와 계산 대상을 이해하기 어려웠던 설명 순서를 개선한다.
- 본문 반영: 두 동작의 원시 기록에서 차이를 관찰한 뒤 평균·표본 표준편차·마지막 구간 기울기·기준선 차이를 질문별로 도입하고 요약 표로 합쳤다. 압력 평균, 시간 간격과 단위, 반올림 규칙, 임계값 경계와 검토 결과의 의미를 설명했다. 값·기준선을 바꾸는 연습과 확인값을 추가하고 네 설계 질문을 모든 데이터셋의 필수 열로 읽지 않도록 범위를 명시했다.
- 검수: 기존 요약 표와 연습값을 재계산하고 로컬 링크·도식 include·메타데이터 대응을 확인했다. 빌드와 도식 렌더링 검수는 수행하지 않았다.
- 번역 동기화 메모: 영어·중국어 간체판을 최신 한국어 원문의 지표별 설명·단위·연습·필수 절차 오해 방지 문장과 동기화하고, 차트·캡션·대체 텍스트도 각 언어로 반영했다.
- 번역 반영 상태: 한국어·영어·중국어 간체판 `v2026.09.19` 동기화 완료.
- 관련 자산: 기존 Mermaid 2개 유지. `docs/assets/part-03/chapter-01/p3_1_1_charts.py`와 `p3-1-1-flow-mean-{ko,en,zh}.png` 추가. 원고의 가상 기록을 사용한 자체 제작 차트이며 외부 그림 복제 없음.
- 원문 기준 버전: `v2026.09.19`

- 차트 보강: 같은 축에서 A-101/A-102의 관측값·평균·마지막 구간을 비교한다. 연결선의 해석 한계와 모바일 가로 스크롤을 반영했다.
- 추가 검수: 세 언어의 설명·표·연습 구조 및 빈 줄 제외 라인 수를 대조했다. 차트 9개의 라벨·선·관측 범위를 이미지로 확인하고 생성 스크립트 재실행의 동일 산출물, 로컬 링크, 언어별 코드 실행 결과를 점검했다. 전체 사이트 빌드와 브라우저 렌더링 검수는 수행하지 않았다.

### v2026.09.15
- 변경 이유: Part 3 검수에서 개념·수치·학습 산출물과 가이드 준수를 점검했다.
- 본문 반영: 책의 데이터 모델링 범위와 일반 용어 정의를 구분했다. 원시값에서 재계산한 표준편차와 기준선 차이를 수정하고 계산 규칙을 명시했다. 요약값과 가상 검토 규칙의 산출 근거를 추가했다. 독자용 본문에 노출된 집필 메타를 재현성 설명으로 바꿨다. 자기점검을 본문 사례의 계산·판단·설명 과제로 구체화했다.
- 번역 동기화 메모: 영어·중국어 간체판의 개념 설명, 제목, 표·예제, 자기점검, 출처를 한국어 최신 원문과 동기화했다. Part 3 전체 108개 번역 문서가 원문 대비 비어 있지 않은 줄 수 98% 기준을 충족하며, 목차·탐색 제목과 학습 목표 도식도 함께 맞췄다.
- 번역 반영 상태: 한국어, 영어, 중국어 간체판 반영 완료.
- 관련 자산: 본문에서 참조한 기존 Part 3 자산 유지
- 원문 기준 버전: `v2026.09.15`

- 재검수 반영: example와 labeled example의 정의 및 출처 적용 범위 정정을 반영했다. 한국어·영어·중국어 간체판에 함께 반영했다.

### v2026.07.31
- 변경 이유: Part 3 전체 보강 라운드에서 중심 학습 산출물을 독자가 직접 확인할 수 있도록 Section 말미의 자기점검 장치를 보강했다.
- 본문 반영: 한국어 원문에 `체크리스트` 절을 추가하고, Section 질문, 중심 학습 산출물, 데이터 구조 판단 항목을 행동형 확인 문장으로 연결했다. 같은 작업에서 원문 `Version`을 `v2026.07.31`로 갱신했다.
- 번역 동기화 메모: 영어판과 중국어 간체판은 이번 한국어 원문 체크리스트 보강 내용을 기준으로 추후 동기화가 필요하다.
- 번역 반영 상태: 한국어 원문 반영 완료, 영어판과 중국어 간체판 미반영.
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.31`
- 체크리스트 보강: 한국어 원문 말미에 `체크리스트` 절을 추가해 Section 질문, 중심 학습 산출물, 데이터 구조 판단 항목을 독자가 직접 확인하도록 했다.
- 체크리스트 번역 동기화 메모: 이번 체크리스트 보강은 한국어 원문 기준으로 먼저 반영했으며, 영어판과 중국어 간체판은 후속 동기화가 필요하다.
### v2026.07.25
- 변경 이유: Part 3 초반의 대표 개념을 새 단어별 개념사전 구조와 언어별 색인에 직접 연결해, 원천데이터·데이터과학·데이터 모델링의 대표 설명 위치를 독자가 다시 찾을 수 있게 했다.
- 본문 반영: 한국어, 영어, 중국어 간체판에서 `source data`와 `data science` 첫 등장 링크를 추가하고, 한국어판의 `sample` 링크가 실제 항목 색인을 가리키도록 정리했으며, 중국어판의 기존 한국어 자음별 개념사전 링크를 중국어 병음별 개념사전 링크로 교체했다.
- 번역 동기화 메모: 내용 확장 없이 개념사전 링크 경로와 `Version`만 세 언어에서 동기화했다.
- 번역 반영 상태: 한국어, 영어, 중국어 간체판 반영 완료.
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.25`

### v2026.07.20
- 구조 정리: 한국어판, 영어판, 중국어 간체판에서 진행 메타 성격의 목적·읽는 순서·범위 제목을 데이터 모델링의 역할, 문제 구조 흐름, 닫을 경계가 드러나는 제목으로 정리했다.
- 번역 반영 상태: 영어판과 중국어 간체판 반영 완료.
- 원문 기준 버전: `v2026.07.20`
- 변경 이유: 순차 저작권 검토 과정에서 데이터 모델링 정의와 원천 로그-요약 표 예시의 근거 연결을 다시 확인했다.
- 본문 반영 내용: 한국어, 영어, 중국어 간체판 본문 `Version`을 `v2026.07.20`으로 맞추고, 세 언어 참고문헌 확인일을 2026-07-20으로 갱신했다.
- 저작권 검토: 저장 구조와 문제 구조 비교표, 시점별 로그 표, 동작 1회 요약 표, 판단 단계 비교표, Mermaid 도식은 Part 3의 자체 대표 사례를 설명하기 위한 원고 내 구성물이며, 외부 문서의 표·그림·예제 전개를 대체할 정도의 복제는 확인되지 않았다.
- 출처 확인: W3C `PROV-Overview`에서 object identification, processing steps, reproducibility, versioning, derivation 축을 확인했다. Google ML Glossary에서 labeled example, feature, feature engineering, label leakage 용어를 확인했다. BLS glossary에서 base period의 reference comparison 의미를 확인했다. Fayyad/Piatetsky-Shapiro/Smyth KDD 자료에서 data selection, preprocessing, transformation, interpretation을 포함한 지식 발견 흐름을 확인했다.
- 번역 동기화 메모: 영어판과 중국어 간체판도 한국어 원문 기준 버전과 출처 확인일을 맞췄다.
- 번역 반영 상태: 한국어, 영어, 중국어 간체판 반영 완료.
- 관련 자산: 내부 Mermaid 도식 자산 유지.
- 원문 기준 버전: `v2026.07.20`

### v2026.07.17
- 본문 반영: `통계적 검정이나 학습 알고리즘 세부는 범위 밖에 둡니다` 문장을 삭제형 범위 공지 대신, 샘플·값·비교·출력 구조를 먼저 고정하는 현재 절 질문 중심 문장으로 다시 정리했다. 같은 작업에서 `Version`을 `v2026.07.17`로 갱신했다.
- 번역 동기화 메모: 영어판과 중국어 간체판도 같은 위치의 범위 안내를 현재 절의 설계 질문 중심 문장으로 맞춰야 한다.

### v2026.07.11
- 본문 반영: 2개의 Mermaid 도식을 `docs/assets/part-03/chapter-01/` 아래 `Section ID + mermaid 순번 + 언어 약어` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 자산 정리: 중국어 간체 본문에서 영문 라벨과 동일한 도식은 별도 `-zh.mmd` 복제본 대신 공용 `-en.mmd` 자산을 직접 참조하도록 바꿔 Part 3 자산 트리의 중복을 줄였다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams, while keeping the same flow structure across language files.

### v2026.07.10
- 본문 반영: `원천 시계열 로그 -> 샘플 단위 정하기 -> 특징으로 요약하기 -> 기준선과 비교하기 -> 출력 구조 정하기` 흐름과, `시점별 로그`를 `동작 1회 샘플`로 바꾸는 비교 도식을 한국어 중심 라벨로 다시 정리했다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어 간체판은 각 언어판 흐름에 맞춰 별도 관리할 수 있다.

### v2026.07.10
- 본문 반영: 도입부를 `샘플 -> 특징 -> 기준선 -> 출력 구조`를 한 설계 판단으로 묶는 문장으로 압축하고, 원천 로그에서 검토 후보 표로 가는 흐름을 짧은 Mermaid 도식과 비교 표로 보강했다.
- 번역 동기화 메모: preserve the sample -> feature -> baseline -> output-structure design chain and the paired source-log vs modeled-table comparison. / reflected in English and Simplified Chinese on 2026-07-10

### v2026.07.08
- 본문 반영: W3C PROV, Google Machine Learning Glossary, BLS `Base period`, Fayyad 외 `KDD` 논문을 본문 하단 `출처와 참고 자료`에 추가해, 분석 단위 식별, 특징/라벨 구조, 기준선 비교, 데이터과학과의 경계 설명을 각각 대응시키도록 보강했다.
- 본문 반영: 해당 문단을 `범위 밖에 둔다`, `이미 설계된 구조 위에서 푸는 문제로 읽는다`는 현재 범위 설명으로 바꿔, Part 3의 설계 책임 안에서 문장이 닫히도록 정리했다.
- 번역 동기화 메모: preserve the question-driven representation-design frame and keep the source support tied to object identification, feature/label structure, and comparison reference. / pending

### v2026.07.07
- 본문 반영: 절의 역할 소개를 걷어내고, 샘플·특징·기준선·결과 구조를 하나의 설계 판단으로 묶는 문장으로 바로 시작하도록 정리했다.
- 본문 반영: `저장 구조를 먼저 보는 관점`과 `Part 3의 데이터 모델링 관점`을 나란히 비교하는 표와 보강 문단을 추가해, 기록 보존용 구조와 질문 응답용 비교 구조가 어떻게 다른지 직접 구분하도록 보강했다.
- 번역 동기화 메모: when translated, keep the data-modeling recall prompts separate from the short self-check bullets in the Part 3 opener. / pending

### v2026.07.06
- 본문 반영: 시점별 로그 표와 동작 1회 요약 표를 나란히 추가하고, 샘플·특징·기준선·출력 구조가 각각 어떻게 드러나는지 설명했다. 절 끝에는 초심자 점검 질문도 보강했다.
- 본문 반영: `대표 설명 위치로 사용합니다`, `다시 돌아오면 됩니다`, `먼저 고정할 문장` 표현을 정리해 3.1이 Part 3 내부의 첫 상세 개념 설명 위치라는 점이 더 직접적으로 읽히게 다듬었다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending
