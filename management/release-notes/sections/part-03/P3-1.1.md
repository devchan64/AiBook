# Section Release Note

- Section ID: `P3-1.1`
- Source File: `docs/parts/part-03/chapter-01/section-01.md`

### v2026.07.11
- 본문 반영: 2개의 Mermaid 도식을 `docs/assets/part-03/chapter-01/` 아래 `Section ID + mermaid 순번 + 언어 약어` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 자산 정리: 중국어 간체 본문에서 영문 라벨과 동일한 도식은 별도 `-zh.mmd` 복제본 대신 공용 `-en.mmd` 자산을 직접 참조하도록 바꿔 Part 3 자산 트리의 중복을 줄였다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams, while keeping the same flow structure across language files.

### v2026.07.10
- 본문 반영: `원천 시계열 로그 -> 샘플 단위 정하기 -> 특징으로 요약하기 -> 기준선과 비교하기 -> 출력 구조 정하기` 흐름과, `시점별 로그`를 `동작 1회 샘플`로 바꾸는 비교 도식을 한국어 중심 라벨로 다시 정리했다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어 간체판은 각 언어판 흐름에 맞춰 별도 관리할 수 있다.

### v2026.07.10
- 본문 반영: 도입부를 `샘플 -> 특징 -> 기준선 -> 출력 구조`를 한 설계 판단으로 묶는 문장으로 압축하고, 원천 로그에서 검토 후보 표로 가는 흐름을 짧은 Mermaid 도식과 비교 표로 보강했다.
- 번역 동기화 메모: preserve the sample -> feature -> baseline -> output-structure design chain and the paired source-log vs modeled-table comparison. / reflected in English and Simplified Chinese on 2026-07-10

### v2026.07.08
- 본문 반영: W3C PROV, Google Machine Learning Glossary, BLS `Base period`, Fayyad 외 `KDD` 논문을 본문 하단 `출처와 참고 자료`에 추가해, 분석 단위 식별, 특징/라벨 구조, 기준선 비교, 데이터과학과의 경계 설명을 각각 대응시키도록 보강했다.
- 본문 반영: 해당 문단을 `범위 밖에 둔다`, `이미 설계된 구조 위에서 푸는 문제로 읽는다`는 현재 범위 설명으로 바꿔, Part 3의 설계 책임 안에서 문장이 닫히도록 정리했다.
- 번역 동기화 메모: preserve the question-driven representation-design frame and keep the source support tied to object identification, feature/label structure, and comparison reference. / pending

### v2026.07.07
- 본문 반영: 절의 역할 소개를 걷어내고, 샘플·특징·기준선·결과 구조를 하나의 설계 판단으로 묶는 문장으로 바로 시작하도록 정리했다.
- 본문 반영: `저장 구조를 먼저 보는 관점`과 `Part 3의 데이터 모델링 관점`을 나란히 비교하는 표와 보강 문단을 추가해, 기록 보존용 구조와 질문 응답용 비교 구조가 어떻게 다른지 직접 구분하도록 보강했다.
- 번역 동기화 메모: when translated, keep the data-modeling recall prompts separate from the short self-check bullets in the Part 3 opener. / pending

### v2026.07.06
- 본문 반영: 시점별 로그 표와 동작 1회 요약 표를 나란히 추가하고, 샘플·특징·기준선·출력 구조가 각각 어떻게 드러나는지 설명했다. 절 끝에는 초심자 점검 질문도 보강했다.
- 본문 반영: `대표 설명 위치로 사용합니다`, `다시 돌아오면 됩니다`, `먼저 고정할 문장` 표현을 정리해 3.1이 Part 3 내부의 첫 상세 개념 설명 위치라는 점이 더 직접적으로 읽히게 다듬었다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending
