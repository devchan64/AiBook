# Section Release Note

- Section ID: `P3-1.1`
- Source File: `docs/parts/part-03/chapter-01/section-01.md`

### v2026.07.10

- 변경 이유: `P3-1.1`은 개념 정의와 표는 충분했지만, 도입 문단의 중복이 커서 학습 밀도가 낮고, 사례 판단 순서가 표 뒤에 흩어져 있어 독자가 `무엇을 한 건으로 세고 어떻게 비교 구조를 만드는가`를 한 번에 붙잡기 어려웠다.
- 본문 반영: 도입부를 `샘플 -> 특징 -> 기준선 -> 출력 구조`를 한 설계 판단으로 묶는 문장으로 압축하고, 원천 로그에서 검토 후보 표로 가는 흐름을 짧은 Mermaid 도식과 비교 표로 보강했다.
- 추가 정리: `사람이 로그를 바로 읽는 방식`과 `데이터 모델링을 거친 뒤의 판단 방식`을 나란히 둬, 정의보다 판단 흐름이 먼저 남도록 Section의 학습 밀도를 높였다.

- 번역 동기화 메모: 확인 필요. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.10`

### v2026.07.08

- 변경 이유: Section 본문이 데이터 모델링, 샘플, 특징, 기준선, 출력 구조의 일반 원리를 길게 설명하면서도 `출처와 참고 자료`가 빠져 있어, 근거와 출처 기준을 충족하지 못했다.
- 본문 반영: W3C PROV, Google Machine Learning Glossary, BLS `Base period`, Fayyad 외 `KDD` 논문을 본문 하단 `출처와 참고 자료`에 추가해, 분석 단위 식별, 특징/라벨 구조, 기준선 비교, 데이터과학과의 경계 설명을 각각 대응시키도록 보강했다.

- 변경 이유: 말미가 Part 3 범위 설명보다 `뒤 Part로 넘깁니다`, `Part 4의 머신러닝도 ... 이어집니다`를 더 크게 세워, 현재 절의 데이터 모델링 정의보다 handoff 메모가 앞서 보였다.
- 본문 반영: 해당 문단을 `범위 밖에 둔다`, `이미 설계된 구조 위에서 푸는 문제로 읽는다`는 현재 범위 설명으로 바꿔, Part 3의 설계 책임 안에서 문장이 닫히도록 정리했다.

- 변경 이유: `상위 프레임`을 전면 삭제하면 현재 절의 일반화된 메타 전개까지 함께 약해질 수 있어, 기준점 자체는 남기되 표 템플릿 의존만 줄일 필요가 있었다.
- 본문 반영: 반복 `상위 프레임` 표는 유지하지 않고, 현재 절의 판단을 더 넓은 개념 축으로 다시 묶는 산문 문장을 말미에 보강했다.

- 변경 이유: `상위 프레임` 재진술 표가 다른 Section들과 같은 마감 템플릿으로 반복되어, 본문이 교안형으로 굳어 보였다.
- 본문 반영: 말미의 `상위 프레임` 표를 제거하고, 현재 설명을 표 재진술 없이 직접 마무리하도록 정리했다.

- 변경 이유: `일반화된 상위 프레임으로 다시 보면` 제목이 Part 3 전반에 반복되어, 현재 절 설명도 같은 템플릿형 마감으로 굳어 보였다.
- 본문 반영: 반복 제목을 제거하고, 상위 프레임 표와 정리 문장을 현재 설명 흐름 안에 직접 이어 붙였다.

- 변경 이유: 도입부가 개념 설명보다 `표제어 안내` 문장을 먼저 세워, AGENTS가 요구하는 직접 설명 원칙보다 편집자 메모가 앞서 보였다.
- 본문 반영: 별도의 표제어 안내 문장을 제거하고, 데이터 모델링·샘플·특징·기준선·출력 구조·비교 리포트·타깃 링크를 첫 설명 문단 안으로 흡수했다.

- 변경 이유: `출력 구조(output structure)`가 Part 3 전반의 핵심 설계 축인데도 개념사전 표제어가 없어, 도입 Section의 용어 입구 블록도 해당 개념을 직접 연결하지 못하고 있었다.
- 본문 반영: 도입부의 개념사전 입구 문장에 `출력 구조(output structure)` 직접 링크를 추가해, Part 3 첫 개념 Section에서 결과 형식 설계 축도 다른 핵심 표제어와 같은 수준으로 붙잡게 했다.

- 변경 이유: Part 3 첫 핵심 개념 도입 Section이 `데이터 모델링`, `샘플`, `특징`, `기준선`, `비교 리포트`, `타깃`을 한꺼번에 소개하면서도 개념사전 직접 연결이 없어, AGENTS와 개념사전 가이드의 본문 연결 규칙을 충분히 따르지 못했다.
- 본문 반영: Section 초반에 주요 표제어를 한 번에 묶어 보여 주는 입구 문장을 추가하고, 각 용어를 개념사전 표제어 앵커로 직접 연결했다. 이후 Section에서는 정의 반복보다 현재 질문에 필요한 차이만 보강한다는 원칙도 같은 문단에서 분명히 했다.

- 변경 이유: 말미의 `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`, 본문 중간의 `이 절은 ... 문제로 다시 볼 수 있습니다`, `이 프레임을 잡아 두면` 같은 메타 템플릿이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 반복적인 메타 템플릿을 걷어내고, 중심 개념 설명과 사례 흐름만 남기도록 Section 말미와 중간 메타 문장을 정리했다. 상위 프레임 뒤의 결론 문장도 `이 프레임을 잡아 두면` 없이 바로 핵심 판단이 서도록 다듬었다.
- 변경 이유: 데이터 모델링의 목적은 충분히 설명하고 있었지만, 이를 더 일반적인 question-driven representation design 프레임으로 다시 묶고 외부 근거와 연결하는 보강이 부족했다.
- 본문 반영: `대상 식별 -> 표현 설계 -> 비교 기준 -> 결과 연결`이라는 상위 프레임을 추가하고, W3C PROV, Google ML Glossary의 `labeled example`/`label leakage`, BLS `Base period`를 `출처와 참고 자료`에 반영했다.

- 번역 동기화 메모: preserve the question-driven representation-design frame and keep the source support tied to object identification, feature/label structure, and comparison reference. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07

- 변경 이유: 도입부가 `대표 설명 위치`를 먼저 설명해 개념보다 편집자 메타 문장이 앞서는 문제가 있었다.
- 본문 반영: 절의 역할 소개를 걷어내고, 샘플·특징·기준선·결과 구조를 하나의 설계 판단으로 묶는 문장으로 바로 시작하도록 정리했다.
- 변경 이유: Part 3이 어렵게 느껴지는 핵심 원인 중 하나는 `데이터 모델링`을 저장 구조 정리와 같은 일처럼 읽는 혼동인데, 기존 본문은 이 차이를 간접적으로만 설명하고 있어 초심자가 진입 초반에 경계를 바로 잡기 어려웠다.
- 본문 반영: `저장 구조를 먼저 보는 관점`과 `Part 3의 데이터 모델링 관점`을 나란히 비교하는 표와 보강 문단을 추가해, 기록 보존용 구조와 질문 응답용 비교 구조가 어떻게 다른지 직접 구분하도록 보강했다.
- 변경 이유: Part 3 첫 개념 절도 대표 설명 위치 문장을 더 직접 설명형으로 맞출 필요가 있었다.
- 본문 반영: `이 절은 ... 대표 설명 위치입니다`를 `여기서는 ... 대표 설명 위치를 맡습니다`로 다듬어 개념 앵커 설명이 더 직접적으로 읽히게 정리했다.
- 변경 이유: Part 3 도입 절도 마지막 자기점검 구간을 데이터 모델링 관점 재호출과 확인 항목으로 분리해 Part 2와 같은 기준선으로 맞출 필요가 있었다.
- 본문 반영: 데이터 모델링 관점을 다시 떠올려야 하는 상황을 `언제 이 관점을 먼저 떠올려야 하는가`로 추가하고, 기존 `짧은 점검`은 유지했다.

- 번역 동기화 메모: when translated, keep the data-modeling recall prompts separate from the short self-check bullets in the Part 3 opener. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.07`

### v2026.07.06

- 변경 이유: Part 3 첫 개념 절이 추상 정의 중심으로만 읽혀 초심자가 `원천데이터 -> 모델링 후 표` 전환을 바로 붙잡기 어려웠다.
- 본문 반영: 시점별 로그 표와 동작 1회 요약 표를 나란히 추가하고, 샘플·특징·기준선·출력 구조가 각각 어떻게 드러나는지 설명했다. 절 끝에는 초심자 점검 질문도 보강했다.
- 변경 이유: Part 3 첫 개념 절의 대표 설명 위치 문장과 마무리 핵심 문장에 남아 있던 안내형 표현을 더 직접적인 설명형으로 맞출 필요가 있었다.
- 본문 반영: `대표 설명 위치로 사용합니다`, `다시 돌아오면 됩니다`, `먼저 고정할 문장` 표현을 정리해 3.1이 Part 3 내부의 첫 상세 개념 설명 위치라는 점이 더 직접적으로 읽히게 다듬었다.
- 변경 이유: Part 3이 `데이터 모델링`을 전제로 뒤 설명으로 바로 들어가고 있어, 독자가 먼저 붙잡아야 할 `무엇을 달성하려는가`를 독립 진입 Section으로 분리할 필요가 있었다.
- 본문 반영: 데이터 모델링을 저장 구조 정리가 아니라 샘플, 특징, 기준선, 출력 구조를 설계하는 일로 정의했다. Part 3 전체에서 만들려는 산출물과 판단 질문을 먼저 고정하는 도입 Section을 추가했다.
- 변경 이유: Part 3 진입부에서 데이터 모델링을 설명할 때, 이것이 데이터과학 전체와 어떤 관계인지 경계를 먼저 밝혀 두지 않으면 독자가 `데이터과학 전체를 배우는 중인지`, `AI 학습 전단의 문제 설계를 배우는 중인지`를 혼동할 수 있었다.
- 본문 반영: 데이터과학을 더 넓은 흐름으로 두고, Part 3의 데이터 모델링은 원천데이터를 샘플, 특징, 비교, 출력 구조로 바꾸는 앞단 설계 구간에 집중한다는 설명을 추가했다. 함께 데이터 정제, 탐색, 통계 검정, 학습 알고리즘은 필요한 범위만 연결하고 핵심 초점은 문제 구조 설계에 둔다는 경계 문단도 보강했다.
- 변경 이유: 3.1과 3.2를 연속해서 읽을 때 `데이터 모델링`, `샘플`, `특징`, `기준선` 같은 용어의 큰 정의가 두 절에서 다시 길게 반복될 여지가 있어, 3.1을 Part 3 내부의 대표 설명 위치로 더 분명히 고정할 필요가 있었다.
- 본문 반영: 3.1을 Part 3 안에서 `데이터 모델링`의 대표 설명 위치로 사용한다는 연결 문장을 추가했다. 뒤 절에서는 같은 용어를 현재 질문에 필요한 만큼만 연결하고, 큰 정의 축은 이 절과 개념사전으로 다시 돌아오게 한다는 안내를 보강했다.
- 변경 이유: Part 3 Section 제목 인덱스를 `Section ID`와 같은 `P3-1.1` 형식으로 맞추는 규칙을 적용할 필요가 있었다.
- 본문 반영: 제목 앞 인덱스를 `3.1`에서 `P3-1.1`로 바꿔 본문 제목과 `Section ID` 표기가 같은 체계를 가리키도록 맞췄다.

- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`, `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.06`
