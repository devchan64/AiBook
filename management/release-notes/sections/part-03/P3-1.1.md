# Section Release Note

- Section ID: `P3-1.1`
- Source File: `docs/parts/part-03/chapter-01/section-01.md`

### v2026.07.06-1

- 변경 이유: Part 3이 `데이터 모델링`을 전제로 뒤 설명으로 바로 들어가고 있어, 독자가 먼저 붙잡아야 할 `무엇을 달성하려는가`를 독립 진입 Section으로 분리할 필요가 있었다.
- 본문 반영: 데이터 모델링을 저장 구조 정리가 아니라 샘플, 특징, 기준선, 출력 구조를 설계하는 일로 정의했다. Part 3 전체에서 만들려는 산출물과 판단 질문을 먼저 고정하는 도입 Section을 추가했다.
- 번역 동기화 메모: translation should preserve the framing that data modeling is about designing sample, feature, comparison, and output structures before model choice. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-2

- 변경 이유: Part 3 진입부에서 데이터 모델링을 설명할 때, 이것이 데이터과학 전체와 어떤 관계인지 경계를 먼저 밝혀 두지 않으면 독자가 `데이터과학 전체를 배우는 중인지`, `AI 학습 전단의 문제 설계를 배우는 중인지`를 혼동할 수 있었다.
- 본문 반영: 데이터과학을 더 넓은 흐름으로 두고, Part 3의 데이터 모델링은 원천데이터를 샘플, 특징, 비교, 출력 구조로 바꾸는 앞단 설계 구간에 집중한다는 설명을 추가했다. 함께 데이터 정제, 탐색, 통계 검정, 학습 알고리즘은 필요한 범위만 연결하고 핵심 초점은 문제 구조 설계에 둔다는 경계 문단도 보강했다.
- 번역 동기화 메모: translation should preserve the distinction between broader data science work and Part 3's narrower focus on pre-learning problem structuring. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-3

- 변경 이유: 3.1과 3.2를 연속해서 읽을 때 `데이터 모델링`, `샘플`, `특징`, `기준선` 같은 용어의 큰 정의가 두 절에서 다시 길게 반복될 여지가 있어, 3.1을 Part 3 내부의 대표 설명 위치로 더 분명히 고정할 필요가 있었다.
- 본문 반영: 3.1을 Part 3 안에서 `데이터 모델링`의 대표 설명 위치로 사용한다는 연결 문장을 추가했다. 뒤 절에서는 같은 용어를 현재 질문에 필요한 만큼만 연결하고, 큰 정의 축은 이 절과 개념사전으로 다시 돌아오게 한다는 안내를 보강했다.
- 번역 동기화 메모: translation should preserve that 3.1 is the main Part 3 anchor for the broad definition of data modeling and that later sections should point back here instead of repeating full definitions. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-4

- 변경 이유: Part 3 Section 제목 인덱스를 `Section ID`와 같은 `P3-1.1` 형식으로 맞추는 규칙을 적용할 필요가 있었다.
- 본문 반영: 제목 앞 인덱스를 `3.1`에서 `P3-1.1`로 바꿔 본문 제목과 `Section ID` 표기가 같은 체계를 가리키도록 맞췄다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.06`
