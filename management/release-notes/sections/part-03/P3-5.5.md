# Section Release Note

- Section ID: `P3-5.5`
- Source File: `docs/parts/part-03/chapter-05/section-05.md`

### v2026.07.10

- 변경 이유: 결측 예제가 `keep_sample`, `avoid_features` 판단을 잘 보여 줬지만, 예제 앞 프리앰블이 없어 독자가 코드 블록 진입 전에 현재 점검 축을 바로 회수하기 어려웠다.
- 본문 반영: 예제 앞에 `문제 상황`, `입력(input)`, `기대 출력(output)`, `확인할 개념`을 추가하고 Section 버전을 갱신했다.
- 번역 반영 상태: 향후 번역 반영 필요

### v2026.07.08

- 변경 이유: 결측이 샘플 경계와 특징 의미를 흔드는 일반 판단을 길게 설명하면서도 `출처와 참고 자료`가 없어, 샘플 유지/제외 기준의 근거 연결이 비어 있었다.
- 본문 반영: Google `labeled example`, `feature engineering`, W3C PROV를 `출처와 참고 자료`에 추가해, 같은 비교 단위 유지, 뜻이 깨진 특징 제외, 누락 상태 추적을 각각 대응시키도록 보강했다.

- 변경 이유: 도입과 첫 소제목이 `Part 4에서 ... 배우기 전에`, `왜 이 질문이 Part 3에 필요한가`처럼 배치 메모를 먼저 세워 결측이 샘플 구조를 흔드는 현재 설명보다 편집 의도가 앞에 보였다.
- 본문 반영: 도입을 결측이 샘플 경계와 특징 의미를 흔드는 문제로 바로 열고, 첫 소제목을 개념 중심 제목으로 바꾸어 현재 판단 기준이 먼저 보이도록 정리했다.

- 변경 이유: 말미의 `인계 직전의 마지막 판정` 블록이 현재 절의 결측·샘플 구조 설명보다 배치 메모처럼 먼저 읽혔다.
- 본문 반영: 별도 handoff 소제목과 점검표를 제거하고, 같은 판단 기준을 마지막 설명 문단 안으로 흡수했다.

- 변경 이유: 핵심 판단을 백틱 독립 문장으로 따로 세워 본문 흐름보다 강조 문구가 먼저 보였다.
- 본문 반영: 결론 문장을 일반 문단 안으로 흡수해 샘플 구조 판단 설명이 자연스럽게 이어지도록 정리했다.
- 변경 이유: 말미의 `한 문장으로 묶기` 소제목이 본문을 정형 요약 템플릿처럼 보이게 했다.
- 본문 반영: 해당 소제목을 제거하고 결론 문장을 일반 설명 흐름 안에 남겼다.
- 번역 동기화 메모: remove the summary-style heading and preserve the conclusion as ordinary prose. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.08-3

- 변경 이유: `이 절의 중심`, `이 절의 핵심`, `이 절에서 붙잡아야 할 문장` 같은 메타 문장이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 해당 메타 문장을 일반 설명 문장으로 바꾸고, 같은 뜻을 더 직접적으로 설명하도록 문장 흐름을 정리했다.
- 번역 동기화 메모: preserve the section without meta labels like center/core/key sentence, and keep the explanation phrased directly in normal prose. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.08-2

- 변경 이유: 말미의 `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`, 본문 중간의 `이 절은 ... 문제로 다시 볼 수 있습니다` 같은 메타 템플릿이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 반복적인 메타 템플릿을 걷어내고, 중심 개념 설명과 사례 흐름만 남기도록 Section 말미와 중간 메타 문장을 정리했다.
- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07-3

- 변경 이유: 기존 Python 예제는 누락 위치를 표시하는 단계까지만 보여 주어, 그 구분이 실제로 `샘플 유지`와 `특징 생성 금지` 판단으로 어떻게 이어지는지는 코드 결과에서 직접 보이지 않았다.
- 본문 반영: `keep_sample`과 `avoid_features` 열을 추가해, 부분 구간 누락과 샘플 구조 붕괴가 서로 다른 데이터 모델링 판단으로 이어진다는 점을 예제 출력에서 바로 읽을 수 있게 했다.
- 번역 동기화 메모: preserve the strengthened missingness example where flags lead directly to keep/drop and feature-avoidance decisions. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-4

- 변경 이유: 이 절의 작은 예시는 값 자체보다 `누락 위치와 샘플 경계 상태에 따라 판단이 갈라진다`는 구조를 보여 주는 편이 더 중요하므로, 도식 보강이 더 적합했다.
- 본문 반영: 작은 예시를 Mermaid 도식으로 교체했다. 일부 구간 누락이면 샘플 유지와 특징 제외로, 샘플 경계 붕괴면 유지 불가로 갈라지는 판단 구조를 시각적으로 보강했다.
- 번역 동기화 메모: translation should preserve the missingness decision-flow diagram and avoid flattening it into a row-comparison table only. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-5

- 변경 이유: 예시를 도식으로 바꾼 뒤에도 소제목이 일반 예시 표지로 남아 있어, 형식 기준 정리가 덜 끝난 상태로 보일 수 있었다.
- 본문 반영: 소제목을 `작은 도식으로 보기`로 정리해, 이 절의 예시가 누락 판단 흐름을 보여 주는 도식이라는 점을 제목에서도 일치시켰다.
- 번역 동기화 메모: keep the subsection label aligned with the decision-flow diagram in this section. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-2

- 변경 이유: 누락 설명 절이 Part 4·5 연결을 독립 소제목으로 길게 전개해, 현재 절의 중심 질문인 `샘플 구조 판단`보다 뒤 Part 예고가 더 커졌다.
- 본문 반영: 두 하위 섹션을 삭제하고, 누락이 처리 기법 이전에 샘플 구조와 비교 단위를 먼저 가르는 문제라는 점만 짧은 연결 문단으로 남겼다.
- 번역 동기화 메모: keep missingness centered on sample-structure judgment and compress later-Part handoff into a short paragraph. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: Part 3 앞중반은 원시 로그, 요약 표, 특징 설계 흐름을 많이 보강했지만, 초심자가 현실 데이터에서 바로 부딪히는 `값 누락·구간 비어 있음·샘플 경계 붕괴`는 아직 독립 절로 정리되지 않았다.
- 본문 반영: 일부 값 누락, 구간 누락, 샘플 구조 붕괴를 구분하고, 무엇을 유지할지·어떤 특징을 만들지 말아야 할지·빠짐 자체를 표시 열로 남길지 먼저 정하는 Section을 추가했다. 이를 Part 4의 결측치 처리와 Part 5의 시계열 입력 구조 전제 전에 필요한 데이터 모델링 판단으로 연결했다.
- 번역 동기화 메모: preserve that the new section frames missingness first as a sample-structure and feature-validity question before later preprocessing details in Part 4. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`
