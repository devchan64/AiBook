# Section Release Note

- Section ID: `P3-5.5`
- 대응 본문 경로:
  - `docs/parts/part-03/chapter-05/section-05.md`
  - `docs/parts/part-03/chapter-05/section-05.en.md`
  - `docs/parts/part-03/chapter-05/section-05.zh.md`

### v2026.07.20
- 변경 이유: 순차 저작권 검토 과정에서 결측 위치와 샘플 구조 판단 예제, Mermaid 도식, 외부 근거의 대응을 다시 확인했다.
- 본문 반영 내용: 한국어, 영어, 중국어 간체판 본문 `Version`을 `v2026.07.20`으로 맞추고, 참고문헌 확인일을 2026-07-20으로 갱신했다. 세 언어 본문의 Python 예제를 36행 CSV 입력 자산과 표준 라이브러리 `csv` 기반 코드로 동기화해, 3행 인라인 `pandas` 예제를 대체했다. 영어판과 중국어 간체판에는 한국어 원문 기준의 `keep_partial_samples` 조작값과 scikit-learn 결측 표시 출처를 반영했다.
- 저작권 검토: 결측 위치, 샘플 유지 여부, 제외할 특징, 샘플 구조 붕괴 예제는 Part 3의 자체 샘플 데이터로 구성했다. Mermaid 도식은 자체 제작 흐름도이고, 외부 문서의 표·예제·그림을 대체할 정도의 복제는 확인되지 않았다.
- 출처 확인: Google `Machine Learning Glossary`에서 labeled example과 feature engineering의 구조를 확인했다. W3C `PROV-Overview`에서 processing steps, reproducibility, versioning, derivation을 남기는 provenance 맥락을 확인했다. scikit-learn `Imputation of missing values`에서 결측 행/열 제거의 데이터 손실 가능성과 `MissingIndicator`를 통한 결측 정보 보존을 확인했다.
- 번역 동기화 메모: 영어판과 중국어 간체판을 한국어 원문 기준의 CSV 예제, `keep_partial_samples` 조작값, scikit-learn 출처, 확인일에 맞췄다.
- 번역 반영 상태: 한국어, 영어, 중국어 간체판 반영 완료.
- 관련 자산: `docs/assets/part-03/chapter-05/p3_5_5_missing_segments.csv`, `docs/assets/part-03/chapter-05/p3-5-5-mermaid-01-ko.mmd`, `docs/assets/part-03/chapter-05/p3-5-5-mermaid-01-en.mmd`, `docs/assets/part-03/chapter-05/p3-5-5-mermaid-01-zh.mmd`
- 원문 기준 버전: `v2026.07.20`

### v2026.07.19
- 변경 이유: 결측을 표시 열로 남길 수 있다는 설명이 기존 출처만으로는 일반 feature engineering 판단에 머물러 있어, 결측 표시 자체를 직접 설명하는 근거가 필요했다.
- 본문 반영: scikit-learn `Imputation of missing values` 문서를 `출처와 참고 자료`에 추가해, 결측 데이터의 행/열 제거가 데이터 손실을 만들 수 있고 `MissingIndicator`로 결측 여부를 보존할 수 있다는 근거를 연결했다.
- 예제 보강: `keep_partial_samples` 조작값을 추가해, 부분 구간 누락 샘플을 유지할지 제외할지 정책에 따라 판단이 달라진다는 점을 코드와 해설에서 확인하게 했다.
- 번역 동기화 메모: English and Simplified Chinese translations should add the same scikit-learn missing-value/imputation source if their references are synchronized.
- 번역 반영 상태: 대기
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-03/chapter-05/section-05.md` `v2026.07.19`

### v2026.07.11
- 본문 반영: 1개의 Mermaid 도식을 `docs/assets/part-03/chapter-05/` 아래 `Section ID + mermaid 순번 + 언어 약어` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 자산 정리: 중국어 간체 본문에서 영문 라벨과 동일한 도식은 별도 `-zh.mmd` 복제본 대신 공용 `-en.mmd` 자산을 직접 참조하도록 바꿔 Part 3 자산 트리의 중복을 줄였다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams, while keeping the same flow structure across language files.

### v2026.07.10
- 본문 반영: `부분 누락 -> 샘플 경계 확인 -> 결측 표시 여부` 흐름을 한국어 중심 라벨로 다시 정리했다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어 간체판은 각 언어판 흐름에 맞춰 별도 관리할 수 있다.
- 본문 반영: 예제 앞에 `문제 상황`, `입력(input)`, `기대 출력(output)`, `확인할 개념`을 추가하고 Section 버전을 갱신했다.

### v2026.07.08
- 본문 반영: Google `labeled example`, `feature engineering`, W3C PROV를 `출처와 참고 자료`에 추가해, 같은 비교 단위 유지, 뜻이 깨진 특징 제외, 누락 상태 추적을 각각 대응시키도록 보강했다.
- 본문 반영: 도입을 결측이 샘플 경계와 특징 의미를 흔드는 문제로 바로 열고, 첫 소제목을 개념 중심 제목으로 바꾸어 현재 판단 기준이 먼저 보이도록 정리했다.
- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending

### v2026.07.07
- 본문 반영: `keep_sample`과 `avoid_features` 열을 추가해, 부분 구간 누락과 샘플 구조 붕괴가 서로 다른 데이터 모델링 판단으로 이어진다는 점을 예제 출력에서 바로 읽을 수 있게 했다.
- 본문 반영: 작은 예시를 Mermaid 도식으로 교체했다. 일부 구간 누락이면 샘플 유지와 특징 제외로, 샘플 경계 붕괴면 유지 불가로 갈라지는 판단 구조를 시각적으로 보강했다.
- 번역 동기화 메모: preserve that the new section frames missingness first as a sample-structure and feature-validity question before later preprocessing details in Part 4. / pending
