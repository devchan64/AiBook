# Section Release Note

- Section ID: `P3-5.5`
- Source File: `docs/parts/part-03/chapter-05/section-05.md`

### v2026.07.07-3

- 변경 이유: 기존 Python 예제는 누락 위치를 표시하는 단계까지만 보여 주어, 그 구분이 실제로 `샘플 유지`와 `특징 생성 금지` 판단으로 어떻게 이어지는지는 코드 결과에서 직접 보이지 않았다.
- 본문 반영: `keep_sample`과 `avoid_features` 열을 추가해, 부분 구간 누락과 샘플 구조 붕괴가 서로 다른 데이터 모델링 판단으로 이어진다는 점을 예제 출력에서 바로 읽을 수 있게 했다.
- 번역 동기화 메모: preserve the strengthened missingness example where flags lead directly to keep/drop and feature-avoidance decisions. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-4

- 변경 이유: 이 절의 작은 예시는 값 자체보다 `누락 위치와 샘플 경계 상태에 따라 판단이 갈라진다`는 구조를 보여 주는 편이 더 중요하므로, 도식 보강이 더 적합했다.
- 본문 반영: 작은 예시를 Mermaid 도식으로 교체했다. 일부 구간 누락이면 샘플 유지와 특징 제외로, 샘플 경계 붕괴면 유지 불가로 갈라지는 판단 구조를 시각적으로 보강했다.
- 번역 동기화 메모: translation should preserve the missingness decision-flow diagram and avoid flattening it into a row-comparison table only. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-5

- 변경 이유: 예시를 도식으로 바꾼 뒤에도 소제목이 일반 예시 표지로 남아 있어, 형식 기준 정리가 덜 끝난 상태로 보일 수 있었다.
- 본문 반영: 소제목을 `작은 도식으로 보기`로 정리해, 이 절의 예시가 누락 판단 흐름을 보여 주는 도식이라는 점을 제목에서도 일치시켰다.
- 번역 동기화 메모: keep the subsection label aligned with the decision-flow diagram in this section. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-2

- 변경 이유: 누락 설명 절이 Part 4·5 연결을 독립 소제목으로 길게 전개해, 현재 절의 중심 질문인 `샘플 구조 판단`보다 뒤 Part 예고가 더 커졌다.
- 본문 반영: 두 하위 섹션을 삭제하고, 누락이 처리 기법 이전에 샘플 구조와 비교 단위를 먼저 가르는 문제라는 점만 짧은 연결 문단으로 남겼다.
- 번역 동기화 메모: keep missingness centered on sample-structure judgment and compress later-Part handoff into a short paragraph. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: Part 3 앞중반은 원시 로그, 요약 표, 특징 설계 흐름을 많이 보강했지만, 초심자가 현실 데이터에서 바로 부딪히는 `값 누락·구간 비어 있음·샘플 경계 붕괴`는 아직 독립 절로 정리되지 않았다.
- 본문 반영: 일부 값 누락, 구간 누락, 샘플 구조 붕괴를 구분하고, 무엇을 유지할지·어떤 특징을 만들지 말아야 할지·빠짐 자체를 표시 열로 남길지 먼저 정하는 Section을 추가했다. 이를 Part 4의 결측치 처리와 Part 5의 시계열 입력 구조 전제 전에 필요한 데이터 모델링 판단으로 연결했다.
- 번역 동기화 메모: preserve that the new section frames missingness first as a sample-structure and feature-validity question before later preprocessing details in Part 4. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`
