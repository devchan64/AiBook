# Section Release Note

- Section ID: `P3-5.1`
- 대응 본문 경로:
  - `docs/parts/part-03/chapter-05/section-01.md`
  - `docs/parts/part-03/chapter-05/section-01.en.md`
  - `docs/parts/part-03/chapter-05/section-01.zh.md`

### v2026.07.20
- 본문 반영 내용: 한국어, 영어, 중국어 간체판의 Python 예제 첫 줄에 원시 로그 행을 이벤트별 요약표와 구간별 집계표로 바꾸는 목적을 설명하는 주석을 추가했다.
- 변경 이유: 순차 저작권 검토 과정에서 원시 로그를 요약 표와 집계 표로 바꾸는 예제, Mermaid 도식, 외부 근거의 대응을 다시 확인했다.
- 본문 반영 내용: 한국어, 영어, 중국어 간체판 본문 `Version`을 `v2026.07.20`으로 맞추고, 참고문헌 확인일을 2026-07-20으로 갱신했다. 세 언어 본문의 Python 예제를 36행 CSV 입력 자산과 표준 라이브러리 `csv` 기반 코드로 동기화해, 하드코딩된 18행 `pandas` 예제를 대체했다.
- 저작권 검토: 원시 로그, 동작별 요약, 최근/기준선 집계 예제는 Part 3의 자체 운영 조건 샘플 데이터로 구성했다. Mermaid 도식은 자체 제작 흐름도이고, 외부 문서의 표·예제·그림을 대체할 정도의 복제는 확인되지 않았다.
- 출처 확인: W3C `PROV-Overview`에서 처리 단계, 재현 가능성, 버전, 파생 관계를 표현하는 provenance 맥락을 확인했다. Google `Machine Learning Glossary`에서 example/labeled example의 feature·label 구조를 확인했다. U.S. Bureau of Labor Statistics `Base period`에서 비교 기준 시점의 의미를 확인했다.
- 번역 동기화 메모: 영어판과 중국어 간체판을 한국어 원문 기준의 CSV 예제, 출처 구성, 확인일에 맞췄다.
- 번역 반영 상태: 한국어, 영어, 중국어 간체판 반영 완료.
- 관련 자산: `docs/assets/part-03/chapter-05/p3_5_1_raw_log_segments.csv`, `docs/assets/part-03/chapter-05/p3-5-1-mermaid-01-ko.mmd`, `docs/assets/part-03/chapter-05/p3-5-1-mermaid-01-en.mmd`, `docs/assets/part-03/chapter-05/p3-5-1-mermaid-01-zh.mmd`
- 원문 기준 버전: `v2026.07.20`

### v2026.07.11
- 본문 반영: 1개의 Mermaid 도식을 `docs/assets/part-03/chapter-05/` 아래 `Section ID + mermaid 순번 + 언어 약어` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 자산 정리: 중국어 간체 본문에서 영문 라벨과 동일한 도식은 별도 `-zh.mmd` 복제본 대신 공용 `-en.mmd` 자산을 직접 참조하도록 바꿔 Part 3 자산 트리의 중복을 줄였다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams, while keeping the same flow structure across language files.

### v2026.07.10
- 본문 반영: 진행률 기준 구간화와 동작별 요약, 동작 간 집계를 보여 주는 도식을 한국어 중심 라벨로 다시 정리했다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어 간체판은 각 언어판 흐름에 맞춰 별도 관리할 수 있다.
- 본문 반영: 예제 앞에 `문제 상황`, `입력(input)`, `기대 출력(output)`, `확인할 개념`을 추가하고 Section 버전을 갱신했다.

### v2026.07.08
- 본문 반영: `이전 Chapter에서 남긴 것 / 이번 Chapter에서 추가로 정하는 것 / 다음 Chapter로 넘길 구조` 표를 도입부에 추가해, 샘플 단위와 대표성 범위를 바탕으로 원시 로그를 요약 표와 집계 표로 바꾸는 현재 장의 역할이 더 직접적으로 보이도록 정리했다.
- 본문 반영: 도입을 `원시 로그 -> 요약 표 -> 집계 표`가 같은 시계열을 서로 다른 질문에 맞게 다시 표현하는 순서라는 직접 설명으로 바꾸고, 말미의 메타 재프레이밍 문장은 제거한 뒤 현재 절의 핵심을 기록 수준·샘플 수준·상태 수준 표현 차이로 직접 마무리했다.
- 번역 동기화 메모: preserve the multi-level time-series representation frame, keep the section free of repeated meta-study blocks, and tie the source support to record/sample/state-level tables. / pending

### v2026.07.07
- 본문 반영: 예제와 예상 출력의 단계 이름을 목적 중심으로 다시 붙여, 원시 로그가 비교 전 출발점이고 요약 표가 직접 비교용이며 집계 표가 최근 대 기준선 판단용이라는 점이 출력 제목에서 바로 드러나게 정리했다.
- 본문 반영: 이벤트 B와 C를 추가하고, 요약 표에 `window`를 부여한 뒤 최근/기준선 집계 표까지 이어지는 3단계 출력으로 예제를 확장했다. 해설도 `요약 표가 다시 집계 표의 재료가 된다`는 점을 더 직접 설명하도록 조정했다.
- 번역 동기화 메모: when translated, keep the raw-summary-aggregate recall prompts separate from the short self-check bullets. / pending

### v2026.07.06
- 본문 반영: 개별 동작 비교, 최근 상태 비교, 세부 시점 확인에 각각 원시 로그·요약 표·집계 표 중 무엇이 더 직접적인지 정리한 보강 표를 추가하고, 절 끝에는 표 변환 흐름을 점검하는 짧은 질문을 넣었다.
- 본문 반영: 3.9의 표 변환 큰 흐름과 개념사전 연결을 기준 위치 설명으로 바꾸어, 뒤 절들이 참조하는 대표 설명 위치가 더 직접적으로 읽히게 다듬었다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending

### v2026.07.05
- 본문 반영: Section 메타데이터 추가. / 각 표를 손에 들었을 때 먼저 해야 할 일과 아직 어려운 일을 나누는 비교 표, 예제 뒤의 3단계 자가 점검 질문을 추가했다.
- 번역 동기화 메모: 메타데이터 추가와 비교 표/자가 점검 질문 보강이 포함되어 번역 시 표 역할 구분과 질문 순서를 유지해야 한다. / pending
