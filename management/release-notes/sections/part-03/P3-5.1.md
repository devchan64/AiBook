# Section Release Note

- Section ID: `P3-5.1`
- Source File: `docs/parts/part-03/chapter-05/section-01.md`

### v2026.07.08

- 변경 이유: Chapter 5가 표 변환 절차는 설명하고 있었지만, 기준 계획서가 요구한 `수치형 탐색`과 `범주형 탐색`의 장 관점이 이 대표 Section에서 바로 읽히지는 않았다.
- 본문 반영: 원시 로그를 요약 표와 집계 표로 바꾸는 절차가 왜 수치형 값과 범주형 상태를 함께 탐색 가능한 구조를 만드는 일인지 보강하고, 두 탐색 관점이 표에서 먼저 무엇을 남겨야 하는지도 표로 추가했다.
- 번역 동기화 메모: preserve P3-5.1 as the anchor that turns raw logs into tables for both numerical exploration and categorical exploration, not only as a table-conversion procedure. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

- 변경 이유: 메타 템플릿 정리와 상위 프레임 보강 뒤에도, 결론 문장이 `이 프레임을 잡아 두면`이라는 반복 표현을 유지해 문체가 다시 고정형으로 보였다.
- 본문 반영: `기록 수준 표현 -> 샘플 수준 표현 -> 상태 수준 표현` 상위 프레임과 `출처와 참고 자료`는 유지하되, 뒤의 정리 문장을 직접 진술형으로 다듬었다.
- 번역 동기화 메모: preserve the multi-level time-series representation frame, keep the section free of repeated meta-study blocks, and tie the source support to record/sample/state-level tables. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07-4

- 변경 이유: 3단계 Python 예제는 구조상 맞았지만, 출력 제목이 아직 일반적이라 각 표가 `비교 전 원시 로그`, `동작 단위 비교 표`, `최근 대 기준선 집계 표` 중 무엇인지 결과만으로는 덜 분명했다.
- 본문 반영: 예제와 예상 출력의 단계 이름을 목적 중심으로 다시 붙여, 원시 로그가 비교 전 출발점이고 요약 표가 직접 비교용이며 집계 표가 최근 대 기준선 판단용이라는 점이 출력 제목에서 바로 드러나게 정리했다.
- 번역 동기화 메모: keep the output labels tied to their role in the raw-summary-aggregate pipeline rather than generic table names. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-3

- 변경 이유: 본문은 `원시 로그 -> 요약 표 -> 집계 표` 변환을 설명했지만, 기존 Python 예제는 실제로는 `원시 로그 -> 요약 표`까지만 보여 주어 절의 중심 흐름을 끝까지 받치지 못했다.
- 본문 반영: 이벤트 B와 C를 추가하고, 요약 표에 `window`를 부여한 뒤 최근/기준선 집계 표까지 이어지는 3단계 출력으로 예제를 확장했다. 해설도 `요약 표가 다시 집계 표의 재료가 된다`는 점을 더 직접 설명하도록 조정했다.
- 번역 동기화 메모: preserve the full raw-to-summary-to-aggregate walkthrough, including the final grouped comparison table built from per-event summaries. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-2

- 변경 이유: 표 변환 대표 Section 도입에 남아 있던 `Part 3 안에서 ... 대표 설명 위치` 문구를 직접 설명형 기준으로 맞출 필요가 있었다.
- 본문 반영: 원시 로그에서 요약 표와 집계 표로 넘어가는 큰 흐름을 `여기서는 ... 가장 먼저 고정합니다`로 바꿔, 메타 안내 없이도 이 Section이 표 변환 기준점으로 읽히게 정리했다.
- 번역 동기화 메모: when translated, keep the opening declarative and preserve P3-5.1 as the anchor for the raw-summary-aggregate transformation. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: 원시 로그 변환 절도 마지막 자기점검 구간을 표 변환 관점 재호출과 확인 항목으로 분리해 Part 3 기준선을 맞출 필요가 있었다.
- 본문 반영: 원시 로그, 요약 표, 집계 표를 다시 구분해야 하는 상황을 `언제 이 관점을 먼저 떠올려야 하는가`로 추가하고, 기존 `짧은 점검`은 유지했다.
- 번역 동기화 메모: when translated, keep the raw-summary-aggregate recall prompts separate from the short self-check bullets. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.07`

### v2026.07.06-15

- 변경 이유: 표 변환 절차는 설명하고 있었지만, 어떤 판단에 어떤 표가 더 직접적인 출발점인지와 절 끝 점검 장치가 초심자 기준에서 더 필요했다.
- 본문 반영: 개별 동작 비교, 최근 상태 비교, 세부 시점 확인에 각각 원시 로그·요약 표·집계 표 중 무엇이 더 직접적인지 정리한 보강 표를 추가하고, 절 끝에는 표 변환 흐름을 점검하는 짧은 질문을 넣었다.
- 번역 동기화 메모: preserve the table-to-question routing aid and the short check questions for the raw-summary-aggregate flow. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-15`

### v2026.07.06-14

- 변경 이유: 표 변환 대표 설명 위치 문장에 남아 있던 `다시 돌아오면 됩니다` 표현을 직접 설명형으로 정리할 필요가 있었다.
- 본문 반영: 3.9의 표 변환 큰 흐름과 개념사전 연결을 기준 위치 설명으로 바꾸어, 뒤 절들이 참조하는 대표 설명 위치가 더 직접적으로 읽히게 다듬었다.
- 번역 동기화 메모: keep P3-5.1 declarative and preserve it as the Part 3 anchor for raw-to-summary-to-aggregate table transformation. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`, Mermaid 도식 1건.
- 원문 기준 버전: `v2026.07.06-14`

### v2026.07.05

- 변경 이유: 메타데이터 누락 보완. / 원시 로그, 요약 표, 집계 표의 역할 차이를 초심자 기준에서 더 직접적으로 구분할 필요가 있었음.
- 본문 반영: Section 메타데이터 추가. / 각 표를 손에 들었을 때 먼저 해야 할 일과 아직 어려운 일을 나누는 비교 표, 예제 뒤의 3단계 자가 점검 질문을 추가했다.
- 번역 동기화 메모: 메타데이터 추가와 비교 표/자가 점검 질문 보강이 포함되어 번역 시 표 역할 구분과 질문 순서를 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.05`

### v2026.07.06-2

- 변경 이유: 원시 로그/요약 표/집계 표 구분 절에서도 `초심자가 자주 놓치는 점` 같은 메타 문장을 줄이고, 표 역할 구분이 작업 출발점 판단으로 바로 이어지게 정리.
- 본문 반영: 세 표 중 출발점을 확인하는 문장과 예제 뒤 자가 점검 질문 도입을 일반 서술로 바꾸어, 축약이 아니라 표현 전환이라는 점을 더 직접적으로 읽히게 했다. 도식, 비교 표, 예제는 유지했다.
- 번역 동기화 메모: learner-label framing이 줄고 representation-shift emphasis가 강해졌으므로, 번역 시에도 raw/summary/aggregate distinction을 direct framing으로 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-2`

### v2026.07.06-3

- 변경 이유: 원시 로그/요약 표/집계 표 설명의 도입과 다음 절 연결에 남아 있던 `예로 들어 보겠습니다`, `살펴봅니다` 같은 유도형 표현을 직접 설명형으로 정리할 필요가 있었다.
- 본문 반영: 세 표의 역할 차이를 여는 도입 문장을 `예로 들겠습니다`로 조정하고, 다음 절 연결 문장을 `다룹니다`로 정리했다. 원시 로그→요약 표→집계 표의 표현 전환 구조와 비교 표, 예제, 도식은 유지했다.
- 번역 동기화 메모: 번역본이 생기면 section opening과 next-section handoff를 guide-like phrasing보다 direct explanatory prose로 맞춘다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-10

- 변경 이유: Part 3 앞단에 데이터 모델링 진입 챕터를 추가하면서 기존 Section의 번호 체계와 파일 경로를 한 칸씩 뒤로 재배치할 필요가 있었다.
- 본문 반영: Section 제목 번호, Section ID, 본문 경로, 목차 대응 위치를 새 Part 3 번호 체계에 맞춰 갱신했다. 본문 설명 구조와 사례 흐름은 유지했다.
- 번역 동기화 메모: translation should follow the renumbered Part 3 section IDs and updated source paths while preserving the same content structure. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-11

- 변경 이유: 원시 로그/요약 표/집계 표 절이 구분 설명에 머무르기보다 `원시 로그를 비교 가능한 표로 어떻게 바꾸는가`라는 절차 중심 Section로 읽히도록 리팩터링 축에 맞출 필요가 있었다.
- 본문 반영: Section 제목을 `원시 로그를 비교 가능한 표로 어떻게 바꾸는가`로 조정하고, 샘플 단위를 정한 뒤 원시 로그를 요약 표와 집계 표로 바꾸는 절차라는 설명을 도입에 보강했다. 마지막 핵심 문장도 `비교 가능한 표는 다시 만들어야 한다`는 방향으로 조정했다.
- 번역 동기화 메모: translation should preserve the procedural framing around transforming raw logs into comparable tables rather than merely distinguishing table types. / pending
- 번역 반영 상태: not-started
- 관련 자산: Mermaid 도식 1건.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-12

- 변경 이유: 3.9와 3.10을 연속으로 읽을 때 3.9가 표 변환의 대표 설명 위치라는 점이 분명해야, 뒤 절이 같은 절차를 다시 반복하지 않고 요약 표 안에 무엇을 남길지로 더 좁혀질 수 있었다.
- 본문 반영: 3.9를 Part 3 안에서 `원시 로그 -> 요약 표 -> 집계 표` 변환 절차의 대표 설명 위치라고 명시했다. 또한 요약을 단순 축소가 아니라, 이미 정한 샘플 단위 위에 비교 가능한 표를 만드는 절차라는 점을 보강했다.
- 번역 동기화 메모: translation should preserve 3.9 as the main Part 3 anchor for table transformation and keep the emphasis that summarization follows an already chosen sample unit. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`, Mermaid 도식 1건.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-13

- 변경 이유: Part 3 Section 제목 인덱스를 `Section ID`와 같은 `P3-5.1` 형식으로 맞추는 규칙을 적용할 필요가 있었다.
- 본문 반영: 제목 앞 인덱스를 `3.9`에서 `P3-5.1`로 바꿔 본문 제목과 `Section ID` 표기가 같은 체계를 가리키도록 맞췄다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.06`
