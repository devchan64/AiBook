# Section Release Note

- Section ID: `P3-6.1`
- Source File: `docs/parts/part-03/chapter-06/section-01.md`

### v2026.07.10
- 본문 반영: 장간 연결 표를 제거하고, 특징 설계 안에서 변수변환과 특징 선택이 어떻게 갈리는지 첫 문단에서 바로 설명하도록 재배치했다. `Version`도 `v2026.07.10`으로 갱신했다.
- 번역 동기화 메모: English and Simplified Chinese translations now reflect the reordered opening that foregrounds variable transformation versus feature selection, and preserve the level/change/stability feature frame and the practical question-to-feature mapping. / reflected in en, zh

### v2026.07.08
- 본문 반영: `이전 Chapter에서 남긴 것 / 이번 Chapter에서 추가로 정하는 것 / 다음 Chapter로 넘길 구조` 표를 도입부에 추가해, 요약 표에서 특징 표로 넘어가는 현재 장의 역할이 더 분명하게 보이도록 정리했다.
- 본문 반영: 말미의 `상위 프레임` 표를 제거하고, 현재 설명을 표 재진술 없이 직접 마무리하도록 정리했다.
- 번역 동기화 메모: preserve the numeric-representation-of-structure frame, keep the section free of repeated meta-study labels, and tie the source support to level/change/stability feature roles. / pending

### v2026.07.07
- 본문 반영: 특징 설계를 `요약 표 숫자 사용`이 아니라 `비교 구조를 숫자 표현으로 다시 고르는 일`로 바로 설명하도록 첫 문단을 정리했다.
- 본문 반영: `현장에서 먼저 나오는 질문 -> 우선 만들 특징 -> 그 특징이 먼저 필요한 이유` 대응표와 보강 문단을 추가해, 전체 수준, 후반 붕괴, 흔들림, 타이밍 차이가 각각 어떤 특징으로 옮겨지는지 직접 연결했다.
- 번역 동기화 메모: when translated, keep the feature-design recall prompts separate from the short self-check bullets. / pending

### v2026.07.06
- 본문 반영: 전체 수준, 구간 차이, 흔들림, 급격한 변화에 대응하는 특징 선택 기준표를 추가하고, 절 끝에는 특징 설계 기준을 확인하는 짧은 질문을 넣었다.
- 본문 반영: 3.11의 숫자 특징 설계 큰 축과 개념사전 연결을 기준 위치 설명으로 바꾸어, 뒤 절들이 참조하는 대표 설명 위치가 더 직접적으로 읽히게 다듬었다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending

### v2026.07.05
- 본문 반영: Section 메타데이터 추가.
- 번역 동기화 메모: 본문 내용 변화 없이 메타데이터만 추가되어 번역본 영향은 없다. / 없음
