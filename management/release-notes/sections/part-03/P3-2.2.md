# Section Release Note

- Section ID: `P3-2.2`
- Source File: `docs/parts/part-03/chapter-02/section-02.md`

### v2026.07.08-2

- 변경 이유: 샘플, 특징, 기준선, 출력 구조를 한 표 안의 역할 구분으로 설명하는 대표 Section인데도, 현재 본문을 받치는 `출처와 참고 자료`가 빠져 있어 역할 구분의 일반화 근거가 끝에서 바로 회수되지 않았다.
- 본문 반영: Google ML Glossary의 `example`/`feature`/`label`, BLS `Base period`, W3C PROV를 `출처와 참고 자료`에 추가해, 샘플-특징-기준선-출력 구조를 하나의 example 설계 흐름으로 읽는 근거를 보강했다.

- 변경 이유: `상위 프레임`을 전면 삭제하면 현재 절의 일반화된 메타 전개까지 함께 약해질 수 있어, 기준점 자체는 남기되 표 템플릿 의존만 줄일 필요가 있었다.
- 본문 반영: 반복 `상위 프레임` 표는 유지하지 않고, 현재 절의 판단을 더 넓은 개념 축으로 다시 묶는 산문 문장을 말미에 보강했다.

- 변경 이유: `상위 프레임` 재진술 표가 다른 Section들과 같은 마감 템플릿으로 반복되어, 본문이 교안형으로 굳어 보였다.
- 본문 반영: 말미의 `상위 프레임` 표를 제거하고, 현재 설명을 표 재진술 없이 직접 마무리하도록 정리했다.

- 변경 이유: `일반화된 상위 프레임으로 다시 보면` 제목이 Part 3 전반에 반복되어, 현재 절 설명도 같은 템플릿형 마감으로 굳어 보였다.
- 본문 반영: 반복 제목을 제거하고, 상위 프레임 표와 정리 문장을 현재 설명 흐름 안에 직접 이어 붙였다.

- 변경 이유: 도입부가 데이터셋 후보의 네 구조 설명보다 `표제어 안내` 문장을 먼저 세워, 편집자 메모가 개념 설명보다 앞서 보였다.
- 본문 반영: 별도 표제어 안내 문장을 제거하고, 샘플·특징·기준선·출력 구조·타깃 링크를 첫 두 문단 안에 자연스럽게 흡수했다.

- 변경 이유: `출력 구조(output structure)`가 현재 Section의 중심 설명 요소인데도 개념사전 표제어가 없어, 이 절의 입구 문장에서는 다른 핵심 개념과 달리 직접 복귀 경로를 만들지 못했다.
- 본문 반영: `출력 구조(output structure)`를 개념사전 표제어 앵커로 직접 연결하고, 이 절에서는 이를 검토용 결과와 예측용 목표 후보를 가르는 문제 설계 축으로 읽는다는 문장을 추가했다.
- 번역 동기화 메모: preserve the direct glossary link for output structure and keep P3-2.2 framed as the anchor section that explains how outputs split into review-facing results and target candidates. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.08`

- 변경 이유: 샘플, 특징, 기준선, 출력 구조를 함께 소개하는 대표 Section인데도 개념사전 직접 연결이 없어, 같은 Part 안의 핵심 개념 복습 동선이 약했다.
- 본문 반영: Section 초반에 `샘플`, `특징`, `기준선`, `타깃`을 개념사전 표제어 앵커로 직접 연결하는 입구 문장을 추가했다. `출력 구조`는 아직 Part 3 본문에서 더 자세히 풀어야 하는 축이라는 점도 함께 분리해 적었다.
- 번역 동기화 메모: preserve the opening glossary-entry block and keep the direct anchor links for sample, feature, baseline, and target while leaving output structure explained in the local Part 3 context. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.08`

- 변경 이유: 말미의 `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`, 본문 중간의 `이 절은 ... 문제로 다시 볼 수 있습니다`, `이 프레임을 잡아 두면` 같은 메타 템플릿이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 반복적인 메타 템플릿을 걷어내고, 중심 개념 설명과 사례 흐름만 남기도록 Section 말미와 중간 메타 문장을 정리했다. 상위 프레임 뒤의 결론 문장도 직접 설명형으로 다듬었다.
- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.08

- 변경 이유: 샘플, 특징, 기준선, 출력 구조의 연결은 충분히 설명하고 있었지만, 이를 더 일반적인 learnable-example construction 프레임으로 다시 묶고 외부 근거와 연결하는 보강이 부족했다.
- 본문 반영: `example 단위 -> 설명 변수 -> 비교 기준 -> 결과 형식`이라는 상위 프레임을 추가하고, Google ML Glossary의 `labeled example`/`label leakage`와 BLS `Base period`를 `출처와 참고 자료`에 반영했다.
- 번역 동기화 메모: preserve the learnable-example construction frame and keep the source support tied to feature/label roles and baseline comparison structure. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07-5

- 변경 이유: 보강된 Python 예제도 최종 표만 한 번에 보여 주고 있어, 본문이 설명하는 `샘플 -> 특징 -> 기준선 비교 -> 출력` 순서를 실행 결과에서 직접 따라가기 어려웠다.
- 본문 반영: 예제 앞에 문제 상황, 입력, 기대 출력, 확인할 개념을 명시하고, 코드와 예상 출력도 단계별 표 확장 흐름이 드러나도록 다시 구성했다.
- 번역 동기화 메모: keep the staged Python walkthrough that shows sample rows growing into feature, baseline-comparison, and output columns. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-4

- 변경 이유: `네 요소가 같은 흐름 안에 있다`는 주장을 받치던 표와 코드가 열 이름 나열 수준에 머물러, 왜 출력 구조가 앞선 샘플·특징·기준선 결정의 결과인지 충분히 보여 주지 못했다.
- 본문 반영: 추상 표를 `현재 특징값`, `기준선 값`, `기준선 차이`, `운영 출력`이 함께 들어간 더 구체적인 예시로 교체하고, Python 예제도 기준선 차이 계산 뒤 출력 열이 만들어지는 흐름이 드러나도록 다시 작성했다.
- 번역 동기화 메모: preserve the concrete row-level flow from sample and features through baseline values, gap calculation, and final operational output. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-3

- 변경 이유: 도입부가 네 구조의 연결 설명보다 `대표 설명 위치`를 먼저 말해 독자가 개념 구조보다 편집 메타를 먼저 읽게 했다.
- 본문 반영: 샘플, 특징, 기준선, 출력 구조가 함께 정해져야 데이터셋 후보의 뜻이 생긴다는 설명으로 바로 들어가도록 첫 문단을 정리했다.
- 번역 동기화 메모: begin with the coupled four-part dataset structure itself, not with anchor-position commentary. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-2

- 변경 이유: 네 구조 연결 절도 대표 설명 위치 문장을 더 직접 설명형으로 맞출 필요가 있었다.
- 본문 반영: `이 절은 ... 대표 설명 위치입니다`를 `여기서는 ... 대표 설명 위치를 맡습니다`로 다듬어 샘플·특징·기준선·출력 구조의 연결 앵커가 더 직접적으로 읽히게 정리했다.
- 번역 동기화 메모: keep the four-structure anchor statement direct and aligned with the shared explanatory tone. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: 데이터셋 후보의 네 구조 연결 절도 마지막 점검 구간을 역할 재호출과 확인 항목으로 분리해 Part 3 기준선을 맞출 필요가 있었다.
- 본문 반영: 샘플·특징·기준선·출력 구조를 다시 떠올려야 하는 상황을 `언제 이 관점을 먼저 떠올려야 하는가`로 추가하고, 기존 `짧은 점검`은 유지했다.
- 번역 동기화 메모: when translated, keep the sample-feature-baseline-output recall prompts separate from the short checklist items. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.07`

### v2026.07.06-15

- 변경 이유: 네 구조를 한 표로 묶어 설명하고 있었지만, 각 열이 실제로 어떤 역할을 맡는지와 절 끝 점검 장치가 더 필요했다.
- 본문 반영: `sample_id`, 특징 열, 기준선 비교 열, 출력 열을 역할별로 해부한 보강 표를 추가하고, 절 끝에는 네 구조 연결을 확인하는 짧은 점검 질문을 넣었다.
- 번역 동기화 메모: preserve the column-role breakdown table and the short check questions that reinforce the four-structure linkage. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-15`

### v2026.07.06-14

- 변경 이유: 네 구조 대표 설명 위치 문장에 남아 있던 `다시 돌아오면 됩니다` 표현을 직접 설명형으로 정리할 필요가 있었다.
- 본문 반영: 3.4의 네 구조 전체 관계를 뒤 Chapter가 참조하는 기준 축으로 다시 적어, 개념사전 연결도 안내형보다 기준 위치 설명으로 읽히게 다듬었다.
- 번역 동기화 메모: keep P3-2.2 declarative and preserve it as the Part 3 anchor for the four-structure relationship. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.06-14`

### v2026.07.05

- 변경 이유: 메타데이터 누락 및 메타 표현 정리. / 핵심 용어 설명에서 영어 기능어가 먼저 보이는 문제 완화. / `출력 구조`가 곧바로 최종 라벨로 오해되지 않도록 초심자용 전개를 보강.
- 본문 반영: Section 메타데이터 추가, 표 머리글 일반화. / `sample_id` 설명과 출력 구조 설명을 한국어 본문 흐름에 맞게 다듬었다. / 샘플, 특징, 기준선 비교, 출력 구조를 읽는 4단계 질문 순서를 추가했고, 운영 판단 문구와 이후 지도학습용 목표 라벨 후보를 구분하는 비교 표를 보강했다.
- 번역 동기화 메모: 메타데이터 추가와 용어 설명 보강이 포함되어 번역 시 질문 순서와 운영 판단/목표 라벨 후보의 구분을 같이 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.05`

### v2026.07.06-2

- 변경 이유: Part 3 용어 소개 절에서도 `초심자 기준에서는`, `...기억하면 충분합니다` 같은 메타 안내를 줄이고, 샘플-특징-기준선-출력 구조의 연결 자체가 앞에 오도록 정리.
- 본문 반영: 네 요소를 읽는 4단계 질문 순서를 `설계 순서` 중심 서술로 바꾸고, 마지막 요약 문장도 관계 설명으로 직접 연결되게 다듬었다. 핵심 표, 예제 코드, 운영 판단과 목표 라벨 후보의 구분은 유지했다.
- 번역 동기화 메모: 메타 안내 대신 design sequence 강조가 강해졌으므로, 번역 시에도 term list가 아니라 sequential framing이 앞에 오도록 맞춰야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-2`

### v2026.07.06-3

- 변경 이유: 핵심 용어 소개 절에서 남아 있던 `초심자` 직접 호명 표현을 더 줄이고, 용어 연결과 출력 구조/라벨 구분을 일반 서술로 정리.
- 본문 반영: 용어 도입 문장에서 `초심자는` 표현을 제거하고, 출력 구조와 지도학습 라벨의 차이 설명도 독자 호명 없이 읽히도록 다듬었다. 핵심 표와 예제는 유지했다.
- 번역 동기화 메모: learner-addressed phrasing이 추가로 줄었으므로, 번역 시에도 concept linkage와 output-vs-label distinction을 neutral narration으로 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-3`

### v2026.07.06-4

- 변경 이유: 용어 연결 절에 남아 있던 `읽는 편이 더 정확합니다`, `정리해 보겠습니다`, `보게 해 줍니다`, `붙잡으면` 같은 안내형 표현을 직접 설명형 문장으로 정리할 필요가 있었다.
- 본문 반영: 샘플-특징-기준선-출력 구조 도입을 `하나의 연결 구조로 읽어야 더 정확하다`는 진술로 바꾸고, 관계 표와 예제 출력 해설을 직접 설명형 문장으로 조정했다. 마지막 핵심 관계 문장도 `먼저 정리해야 할 관계`로 다듬었다.
- 번역 동기화 메모: 번역본이 생기면 term-sequence explanation과 example-output commentary를 reader guidance보다 direct explanatory prose로 맞춘다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-10

- 변경 이유: Part 3 앞단에 데이터 모델링 진입 챕터를 추가하면서 기존 Section의 번호 체계와 파일 경로를 한 칸씩 뒤로 재배치할 필요가 있었다.
- 본문 반영: Section 제목 번호, Section ID, 본문 경로, 목차 대응 위치를 새 Part 3 번호 체계에 맞춰 갱신했다. 본문 설명 구조와 사례 흐름은 유지했다.
- 번역 동기화 메모: translation should follow the renumbered Part 3 section IDs and updated source paths while preserving the same content structure. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-11

- 변경 이유: 샘플, 특징, 기준선, 출력 구조 절이 용어 소개에 머무르기보다 `데이터셋 후보 안에 들어가는 설계 요소`로 읽히도록 Chapter 리팩터링 축에 맞춰야 했다.
- 본문 반영: Section 제목을 `데이터셋 후보 안에는 어떤 구조가 들어가는가`로 바꾸고, 도입 문단과 핵심 관계 문장을 데이터셋 설계 관점으로 다시 정리했다. 네 요소의 연결 구조와 예제 표, 코드, 운영 출력과 라벨 후보의 구분은 유지했다.
- 번역 동기화 메모: translation should keep the shift from term glossary framing to dataset-design framing while preserving the four-structure sequence. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-12

- 변경 이유: 3.4는 Part 3 안에서 `샘플`, `특징`, `기준선`, `출력 구조`의 전체 관계를 대표로 잡는 절인데, 뒤 Chapter에서 무엇을 다시 정의하지 않고 무엇만 이어받아야 하는지 연결 문장이 약했다.
- 본문 반영: 3.4를 네 구조의 대표 설명 위치로 사용한다는 문장을 추가하고, 뒤 Chapter에서는 현재 질문에 필요한 역할만 보강하며 전체 관계는 이 절과 개념사전으로 다시 돌아오게 한다는 안내를 넣었다. 또한 네 요소 설명 문단은 정의 목록 반복보다 질문 순서 고정이 중심이라는 방향으로 정리했다.
- 번역 동기화 메모: translation should preserve 3.4 as the main Part 3 anchor for the four-structure relationship and keep later sections as role-specific follow-ups rather than repeated full definitions. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-13

- 변경 이유: Part 3 Section 제목 인덱스를 `Section ID`와 같은 `P3-2.2` 형식으로 맞추는 규칙을 적용할 필요가 있었다.
- 본문 반영: 제목 앞 인덱스를 `3.4`에서 `P3-2.2`로 바꿔 본문 제목과 `Section ID` 표기가 같은 체계를 가리키도록 맞췄다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.06`
