# Section Release Note

- Section ID: `P3-2.2`
- Source File: `docs/parts/part-03/chapter-02/section-02.md`

### v2026.07.19
- 예제 보강: 미리 만들어 둔 특징/출력 표를 재출력하는 구성을 피하고, `event_log` 원시 입력 CSV에서 샘플 행과 `mean_flow`, `late_drop_rate`를 계산한 뒤 기준선과 비교해 운영 출력과 `threshold sensitivity`를 생성하도록 재구성했다.
- 입력 자산 분리: 본문에 긴 원시 로그를 인라인 배열로 복제하지 않고 `p3_2_2_event_flow_log.csv`를 읽는 구조로 바꾸어, 원고 본문은 입력 파일 설명과 실행 코드의 역할을 분리했다.
- 메타데이터 정합성: 본문 `Version`을 `v2026.07.19`로 갱신했다.
- 검증 메모: 로컬 `.venv`에는 `pandas`가 설치되어 있지 않아 Python 예제 실행은 완료하지 못했다.
- 번역 동기화 메모: English and Simplified Chinese translations should reflect the threshold-control example if the localized sections are synchronized.
- 번역 반영 상태: 대기
- 관련 자산: `docs/assets/part-03/chapter-02/p3_2_2_event_flow_log.csv`
- 원문 기준 버전: `docs/parts/part-03/chapter-02/section-02.md` `v2026.07.19`

### v2026.07.17
- 본문 반영: `샘플 -> 특징 -> 기준선 비교 -> 출력 구조` 설계 순서를 한 번에 따라 읽을 수 있도록 `작은 도식으로 보기` Mermaid 흐름도를 추가하고, 도식 도입 문장을 보강했다. `Version`을 `v2026.07.17`로 갱신했다.
- 번역 동기화 메모: English and Simplified Chinese translations now include localized Mermaid flows and synchronized lead-in sentences for the four-part dataset-candidate design sequence. / reflected in `docs/parts/part-03/chapter-02/section-02.en.md`, `docs/parts/part-03/chapter-02/section-02.zh.md`
- 관련 자산: `docs/assets/part-03/chapter-02/README.md`, `docs/assets/part-03/chapter-02/p3-2-2-mermaid-01-en.mmd`, `docs/assets/part-03/chapter-02/p3-2-2-mermaid-01-ko.mmd`, `docs/assets/part-03/chapter-02/p3-2-2-mermaid-01-zh.mmd`
- 번역 반영 상태: ko/en/zh 최신화 완료

### v2026.07.10
- 본문 반영: 출력 구조 설명과 데이터셋 후보 정의를 도입부에서 한 번에 묶고, 말미에서도 네 구조의 관계를 하나의 닫힌 문단으로 정리해 반복 설명을 줄였다.
- 번역 동기화 메모: preserved the single through-line from sample to feature to baseline to output structure without reopening the same definition multiple times in the English and Simplified Chinese translations. / reflected in `docs/parts/part-03/chapter-02/section-02.en.md`, `docs/parts/part-03/chapter-02/section-02.zh.md`

### v2026.07.08
- 본문 반영: Google ML Glossary의 `example`/`feature`/`label`, BLS `Base period`, W3C PROV를 `출처와 참고 자료`에 추가해, 샘플-특징-기준선-출력 구조를 하나의 example 설계 흐름으로 읽는 근거를 보강했다.
- 본문 반영: 반복 `상위 프레임` 표는 유지하지 않고, 현재 절의 판단을 더 넓은 개념 축으로 다시 묶는 산문 문장을 말미에 보강했다.
- 번역 동기화 메모: preserve the learnable-example construction frame and keep the source support tied to feature/label roles and baseline comparison structure. / pending

### v2026.07.07
- 본문 반영: 예제 앞에 문제 상황, 입력, 기대 출력, 확인할 개념을 명시하고, 코드와 예상 출력도 단계별 표 확장 흐름이 드러나도록 다시 구성했다.
- 본문 반영: 추상 표를 `현재 특징값`, `기준선 값`, `기준선 차이`, `운영 출력`이 함께 들어간 더 구체적인 예시로 교체하고, Python 예제도 기준선 차이 계산 뒤 출력 열이 만들어지는 흐름이 드러나도록 다시 작성했다.
- 번역 동기화 메모: when translated, keep the sample-feature-baseline-output recall prompts separate from the short checklist items. / pending

### v2026.07.06
- 본문 반영: `sample_id`, 특징 열, 기준선 비교 열, 출력 열을 역할별로 해부한 보강 표를 추가하고, 절 끝에는 네 구조 연결을 확인하는 짧은 점검 질문을 넣었다.
- 본문 반영: 3.4의 네 구조 전체 관계를 뒤 Chapter가 참조하는 기준 축으로 다시 적어, 개념사전 연결도 안내형보다 기준 위치 설명으로 읽히게 다듬었다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending

### v2026.07.05
- 본문 반영: Section 메타데이터 추가, 표 머리글 일반화. / `sample_id` 설명과 출력 구조 설명을 한국어 본문 흐름에 맞게 다듬었다. / 샘플, 특징, 기준선 비교, 출력 구조를 읽는 4단계 질문 순서를 추가했고, 운영 판단 문구와 이후 지도학습용 목표 라벨 후보를 구분하는 비교 표를 보강했다.
- 번역 동기화 메모: 메타데이터 추가와 용어 설명 보강이 포함되어 번역 시 질문 순서와 운영 판단/목표 라벨 후보의 구분을 같이 유지해야 한다. / pending
