# Section Release Note

- Section ID: `P3-7.3`
- Source File: `docs/parts/part-03/chapter-07/section-03.md`

### v2026.07.08

- 변경 이유: `상위 프레임` 재진술 표가 다른 Section들과 같은 마감 템플릿으로 반복되어, 본문이 교안형으로 굳어 보였다.
- 본문 반영: 말미의 `상위 프레임` 표를 제거하고, 현재 설명을 표 재진술 없이 직접 마무리하도록 정리했다.

- 변경 이유: `이 프레임을 쓰면` 식의 결론 문장이 다른 Section들과 같은 어조로 반복되어, 마감 문체가 다시 템플릿형으로 굳어 보였다.
- 본문 반영: 상위 프레임 표는 유지하고, 마지막 문장을 직접 진술형 결론으로 바꿨다.

- 변경 이유: `일반화된 상위 프레임으로 다시 보면` 제목이 반복되면서, 기준선의 현재 뜻을 설명하는 마감도 다른 절과 같은 템플릿처럼 보였다.
- 본문 반영: 반복 제목을 제거하고, `참조 측정` 프레임을 기준선 설명의 연장선으로 바로 읽히게 정리했다.

- 변경 이유: 같은 날 여러 차례 수정하면서 이 절의 경계를 다시 Part 3의 기준선 설명으로 좁혔지만, 최종 형태에 맞는 상위 프레임과 외부 근거 연결을 한 항목으로 정리할 필요가 있었다.
- 본문 반영: Part 4의 기준 모델 확장을 제거하고, 기준선을 `현재 측정-참조 측정-변화 읽기`의 상위 프레임으로 다시 정리했다. 또한 메타 문장을 걷어낸 상태를 유지하면서 NCI의 `baseline`과 BLS의 `base period`를 참고 자료로 붙여, 이 절의 중심을 `상태 비교용 참조 측정`에 맞추어 보강했다.
- 번역 동기화 메모: keep the section narrowly focused on baseline as a reference measurement for change, and preserve the source-backed boundary from later model-evaluation concepts. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07-2

- 변경 이유: 기준선과 기준 모델을 비교하는 표에서 `모델 선택`, `분류기 성능 비교` 같은 표현이 남아 있어, 이 절의 핵심인 `평가 기준 층위 구분`보다 특정 모델 선택 문맥이 먼저 읽힐 수 있었다.
- 본문 반영: 메타 문장 `이 절의 중심`, `이 절의 핵심`, `이 절에서 붙잡아야 할 문장` 같은 표현을 일반 설명 문장으로 다시 풀어 썼다. 표 표현을 `방법 비교`, `학습 성능 비교`로 낮춰, Part 3의 데이터 비교 기준과 Part 4의 평가 기준을 구분하는 데 초점이 남도록 조정했다.
- 번역 동기화 메모: keep the distinction at the level of evaluation roles, not named model-selection language. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: 기준선과 기준 모델의 차이 설명이 Part 4 평가 구조를 별도 하위 섹션처럼 길게 펼쳐, 현재 Section의 용어 경계 설명보다 뒤 Part 예고가 더 크게 보였다.
- 본문 반영: 메타 문장 `이 절의 중심`, `이 절의 핵심`, `이 절에서 붙잡아야 할 문장` 같은 표현을 일반 설명 문장으로 다시 풀어 썼다. Part 4 설명을 짧은 구분 표와 연결 문단으로 압축하고, 현재 Section의 중심을 `데이터 비교 기준`과 `모델 평가 기준`의 층위 차이에 다시 맞췄다.
- 번역 동기화 메모: compress the Part 4 preview and keep the section focused on the boundary between data-comparison baseline and model-evaluation baseline. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07

- 변경 이유: 기준선 비교와 기준 모델의 차이는 Chapter 7 도입과 Chapter 10 인계 점검에서 짧게 언급되고 있었지만, Part 4 전환 전에 한 Section으로 묶여 있지 않아 초심자 혼동이 남아 있었다.
- 본문 반영: 메타 문장 `이 절의 중심`, `이 절의 핵심`, `이 절에서 붙잡아야 할 문장` 같은 표현을 일반 설명 문장으로 다시 풀어 썼다. 새 Section `P3-7.3 기준선 비교와 기준 모델은 왜 다른데 둘 다 필요한가`를 추가했다. Part 3의 데이터 비교 기준과 Part 4의 모델 평가 기준을 표로 나누고, 두 기준이 어떤 순서로 이어지는지 정리했다. 같은 `baseline` 단어가 데이터 비교 층위와 모델 평가 층위에서 다르게 쓰인다는 점도 오해 표와 함께 보강했다.
- 번역 동기화 메모: translation should preserve the explicit distinction between baseline comparison in Part 3 and baseline models in Part 4, especially the difference between data-comparison questions and model-evaluation questions. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.07`
