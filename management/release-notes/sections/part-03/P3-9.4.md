# Section Release Note

- Section ID: `P3-9.4`
- Source File: `docs/parts/part-03/chapter-09/section-04.md`

### v2026.07.08-5

- 변경 이유: `이 프레임을 잡아 두면`이 target 생성 과정보다 템플릿형 요약처럼 읽혔다.
- 본문 반영: 메타 문구를 제거하고, target이 검토 기록을 구조화한 결과라는 점을 직접 설명하는 문장으로 정리했다.
- 번역 동기화 메모: replace the frame phrase with a direct explanation of structured targets. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.08-4

- 변경 이유: `왜 이 절이 Part 4 직전 이해에 중요한가` 같은 소제목이 현재 절의 라벨 후보 형성 설명보다 뒤 Part 예고를 더 크게 드러내는 문제가 남아 있었다.
- 본문 반영: 뒤 Part 중요성 소제목과 `한 문장으로 묶기` 표지를 제거하고, 운영 메모가 구조화된 target 후보로 바뀌는 현재 절 설명 안에서 Part 4 연결이 자연스럽게 읽히도록 정리했다.
- 번역 동기화 메모: keep the Part 4 connection folded into the explanation of how review notes become target candidates, not as a separate preview block. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.08-3

- 변경 이유: `이 절의 중심`, `이 절의 핵심`, `이 절에서 붙잡아야 할 문장` 같은 메타 문장이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 해당 메타 문장을 일반 설명 문장으로 바꾸고, 같은 뜻을 더 직접적으로 설명하도록 문장 흐름을 정리했다.
- 번역 동기화 메모: preserve the section without meta labels like center/core/key sentence, and keep the explanation phrased directly in normal prose. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.08-2

- 변경 이유: 말미의 `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`, 본문 중간의 `이 절은 ... 문제로 다시 볼 수 있습니다` 같은 메타 템플릿이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 반복적인 메타 템플릿을 걷어내고, 중심 개념 설명과 사례 흐름만 남기도록 Section 말미와 중간 메타 문장을 정리했다.
- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.08

- 변경 이유: 운영 메모가 target 후보로 바뀌는 과정은 설명하고 있었지만, 이를 자유 판단의 구조화 과정이라는 상위 프레임으로 다시 묶고 외부 근거와 연결하는 장치가 부족했다.
- 본문 반영: `자유로운 운영 판단 -> 반복 의미 정규화 -> 구조화된 target 후보`라는 상위 프레임을 추가하고, Google ML Glossary의 `label`, `proxy labels`, `rater` 설명을 `출처와 참고 자료`에 붙여 Part 4 target과의 연결 근거를 보강했다.
- 번역 동기화 메모: preserve the free-form-to-structured-target frame and keep the source support focused on why notes need normalization before target use. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07

- 변경 이유: Chapter 9에는 비교 리포트, 검토 후보 큐, 목표 라벨 후보 표의 구분은 있었지만, 실제 운영 검토 결과가 어떻게 목표 라벨 후보로 축적되는지는 별도 절로 닫혀 있지 않아 Part 4의 `target`이 갑자기 나타나는 것처럼 느껴질 수 있었다.
- 본문 반영: 새 Section `P3-9.4 검토 결과는 어떻게 운영 메모에서 목표 라벨 후보로 바뀌는가`를 추가했다. 자유로운 운영 메모와 안정된 라벨 후보의 차이, 반복되는 검토 판단을 공통 열로 옮기는 과정, `review_needed`류의 운영 판단이 왜 먼저 target 후보가 되기 쉬운지를 표와 예시로 정리했다.
- 번역 동기화 메모: translation should preserve that many targets are accumulated from repeated review outcomes rather than being clean labels from the start, especially the distinction between free-form operator notes and stable target-candidate columns. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: 기존 예시는 운영 메모에서 라벨 후보 열이 생기는 과정보다 문자열 포함 여부 계산이 더 앞에 보여, `메모 -> 반복 의미 -> 목표 라벨 후보 열`이라는 Section 핵심 흐름이 약하게 드러났다.
- 본문 반영: `raw review log`와 `target-candidate columns`를 나눠 출력하고, 중간에 `note_pattern`을 두어 자유 메모가 공통 의미 묶음으로 정리된 뒤 라벨 후보 열로 바뀌는 단계를 보강했다. `note_source`도 함께 남겨 근거 추적 흐름을 연결했다.
- 번역 동기화 메모: translation should preserve the staged transformation from raw operator notes to normalized note patterns and then to target-candidate columns, including retained note-source evidence. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-2

- 변경 이유: 이 절의 핵심은 메모 처리 기술이 아니라 `검토 메모가 공통 의미를 거쳐 라벨 후보 열로 바뀌는 흐름`이므로, 판단 전환을 보여 주는 도식이 더 적합하다.
- 본문 반영: 작은 코드 예시를 Mermaid 도식으로 교체했다. 검토 큐 결과와 자유 메모가 쌓인 뒤, 반복 의미 묶음을 거쳐 `late_drop_repeated`, `needs_manual_review`, `note_source` 같은 열로 정리되는 단계를 시각적으로 보강했다.
- 번역 동기화 메모: translation should preserve the note-to-pattern-to-target-candidate diagram and avoid falling back to an implementation-oriented code example. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`
