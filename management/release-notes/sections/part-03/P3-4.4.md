# Section Release Note

- Section ID: `P3-4.4`
- Source File: `docs/parts/part-03/chapter-04/section-04.md`

### v2026.07.10

- 변경 이유: 샘플 단위 오판 신호를 보여 주는 작은 코드 예시가 `예상 출력`만 두고 바로 시작해, 무엇을 경고 신호로 읽어야 하는지 코드 바깥에서 즉시 회수되기 어려웠다.
- 본문 반영: 예제 앞에 `문제 상황`, `입력(input)`, `기대 출력(output)`, `확인할 개념`을 추가하고 Section 버전을 갱신했다.

- 번역 동기화 메모: reflected the Python-example preamble and kept the warning-sign section title and output labels explicit in the English and Simplified Chinese translations. / reflected in `docs/parts/part-03/chapter-04/section-04.en.md`, `docs/parts/part-03/chapter-04/section-04.zh.md`
- 번역 반영 상태: reflected in English and Simplified Chinese on 2026-07-10
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.10`

### v2026.07.08

- 변경 이유: 샘플 단위 오판 신호를 설명하는 대표 Section인데도 `출처와 참고 자료`가 빠져 있어, 반복 라벨·설명되지 않는 특징·행/샘플 혼동을 어떤 일반 원리로 읽는지 본문 끝에서 바로 회수되지 않았다.
- 본문 반영: Google ML Glossary, W3C PROV, Wickham의 `Tidy Data`를 `출처와 참고 자료`에 추가해, example 단위, derivation 추적, 관측치/표 구조 구분과 현재 경고 신호를 직접 연결했다.

- 변경 이유: 도입과 마무리에 `Chapter 4 앞 절까지`, `Chapter 5부터는` 같은 독서 흐름 메모가 크게 남아, 샘플 단위 오판 신호 자체보다 배치 설명이 먼저 보였다.
- 본문 반영: 첫 문단을 `샘플 단위를 잘못 잡으면 그 문제는 나중 단계에서 이상한 모양으로 다시 드러난다`는 설명으로 다시 열고, 말미도 `다음 단계 예고`보다 현재 표에서 반복 라벨, 설명되지 않는 특징, 어색한 비교 문장으로 오판을 먼저 알아차리는 일로 초점을 모았다.

- 변경 이유: `짧은 점검` 같은 정형 소제목뿐 아니라 `왜 이 절이 Chapter 5 앞에 필요한가` 같은 메타 제목도 설명 흐름보다 먼저 보여 본문이 설계 메모처럼 읽히는 문제가 남아 있었다.
- 본문 반영: 점검 소제목을 직접 설명형 제목으로 바꾸고, 질문 목록이 현재 절의 판단 흐름 안에 붙도록 정리했다. `왜 이 절이 Chapter 5 앞에 필요한가` 소제목은 일반 설명 문단으로 흡수해 Chapter 5 연결이 본문 흐름 안에서 읽히도록 다듬었다.
- 변경 이유: 말미의 `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`, 본문 중간의 `이 절은 ... 문제로 다시 볼 수 있습니다` 같은 메타 템플릿이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 반복적인 메타 템플릿을 걷어내고, 중심 개념 설명과 사례 흐름만 남기도록 Section 말미와 중간 메타 문장을 정리했다.
- 변경 이유: 샘플 단위 오판 신호 예제는 방향이 맞았지만, 소제목과 첫 출력 제목이 아직 일반적이라 `라벨 반복 자체가 경고 신호`라는 핵심이 코드 결과에 바로 걸리지 않았다.
- 본문 반영: 소제목을 `작은 코드 예시`로 바꾸고, 첫 출력과 마지막 출력 제목을 `labels repeat inside one event`, `warning signs`가 드러나도록 조정해 경고 신호 예제의 목적을 더 직접적으로 읽히게 했다.

- 번역 동기화 메모: keep the subsection title and output labels explicit that the example is surfacing warning signs of a wrong sample unit. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07

- 변경 이유: 기존 Python 예제는 샘플 단위 오판의 경고 신호를 계산해 보여 주기보다 경고 문장을 그대로 출력하는 비중이 커서, 이 절의 핵심 주장인 `무엇을 보면 오판을 의심할 수 있는가`를 코드 결과가 직접 뒷받침하지 못했다.
- 본문 반영: 예제에 이벤트 B를 추가하고, `event_id`별 반복 행 수와 라벨 합, 재묶은 뒤에만 나타나는 동작 단위 특징 요약, 마지막 경고 체크 표를 출력하도록 바꾸었다. 해설도 `앞 출력에서 이미 보인 신호를 다시 묶어 읽는다`는 방향으로 정리했다.
- 변경 이유: Chapter 4에는 샘플 단위의 뜻과 파급효과는 있었지만, 초심자가 실제로 샘플 단위를 잘못 잡았는지 어떻게 알아차릴지는 별도 Section으로 고정돼 있지 않았다.
- 본문 반영: 같은 라벨 반복, 한 줄로 설명되지 않는 특징, 문장과 단위의 어긋남, 잘못된 분할 단위 같은 샘플 단위 오판 신호를 모은 새 Section을 추가했다. 또한 Chapter 5의 요약 표 재구성이 왜 필요한지 이해하게 하는 경고등 역할을 명시했다.

- 번역 동기화 메모: preserve the diagnostic section for detecting a wrong sample unit; keep the warning-sign format explicit. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`
