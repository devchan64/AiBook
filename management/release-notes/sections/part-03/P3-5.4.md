# Section Release Note

- Section ID: `P3-5.4`
- Source File: `docs/parts/part-03/chapter-05/section-04.md`

### v2026.07.11
- 본문 반영: 1개의 Mermaid 도식을 `docs/assets/part-03/chapter-05/` 아래 `Section ID + mermaid 순번 + 언어 약어` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 자산 정리: 중국어 간체 본문에서 영문 라벨과 동일한 도식은 별도 `-zh.mmd` 복제본 대신 공용 `-en.mmd` 자산을 직접 참조하도록 바꿔 Part 3 자산 트리의 중복을 줄였다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams, while keeping the same flow structure across language files.

### v2026.07.10
- 본문 반영: 창을 적용한 동작 순서에서 `한 줄 특징 입력`과 `순서형 입력 후보`로 갈라지는 도식을 한국어 중심 라벨로 다시 정리했다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어 간체판은 각 언어판 흐름에 맞춰 별도 관리할 수 있다.
- 본문 반영: 도입과 후반 문단에 같은 중심 연결을 직접 보강해, 입력 창이 먼저이고 그 뒤에 요약 입력과 순서가 남은 입력이 갈라진다는 흐름을 더 빠르게 읽히게 정리했다. `Version`도 `v2026.07.10`으로 갱신했다.

### v2026.07.08
- 본문 반영: `Windowed event sequence -> summary features / ordered segments`의 작은 Mermaid 도식을 추가해, 같은 입력 창 결정이 한 줄 특징 입력과 순서가 남은 입력 후보를 함께 만든다는 점을 더 빠르게 읽히게 정리했다.
- 본문 반영: 도입부에 앞 절까지의 요약 특징 흐름과 현재 절의 시간 순서 입력 구조 흐름을 직접 잇는 한 문장을 추가해, Chapter 5 내부의 설명 축 전환이 더 선명하게 보이도록 정리했다.
- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending

### v2026.07.07
- 본문 반영: 새 Section `P3-5.4 입력 창(window)은 어디서 자르고 길이는 어떻게 맞추는가`를 추가했다. 시작점·끝점·길이 기준·정렬 기준을 먼저 정하는 이유, 절대 시간 기준과 진행률 기준의 차이, 같은 창 결정이 Part 4 요약 특징과 Part 5 시계열 입력으로 갈라지는 구조를 표와 문장으로 정리했다.
- 번역 동기화 메모: translation should preserve that input windows are a data-modeling decision before they are a sequence-model input shape, especially the distinction between absolute-time alignment and progress-based alignment. / pending
