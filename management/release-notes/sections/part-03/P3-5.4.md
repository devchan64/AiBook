# Section Release Note

- Section ID: `P3-5.4`
- Source File: `docs/parts/part-03/chapter-05/section-04.md`

### v2026.07.11

- 변경 이유: Part 3의 Mermaid 도식을 본문 인라인 코드로만 두지 않고, P5-1.1과 같은 `mmd` 자산 관리 방식으로 정리할 필요가 있었다.
- 본문 반영: 1개의 Mermaid 도식을 `docs/assets/part-03/chapter-05/` 아래 `Section ID + mermaid 순번 + 언어 약어` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 자산 정리: 중국어 간체 본문에서 영문 라벨과 동일한 도식은 별도 `-zh.mmd` 복제본 대신 공용 `-en.mmd` 자산을 직접 참조하도록 바꿔 Part 3 자산 트리의 중복을 줄였다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams, while keeping the same flow structure across language files.
- 번역 반영 상태: 영문·중국어 간체 반영 완료
- 관련 자산: `docs/assets/part-03/chapter-05/`
- 원문 기준 버전: `v2026.07.11`


### v2026.07.10

- 변경 이유: 입력 구조의 두 갈래를 보여 주는 Mermaid 도식이 영어 중심 라벨로 남아 있어, 요약 입력과 순서형 입력 후보의 갈림이 한국어 본문 흐름과 바로 맞물리지 않았다.
- 본문 반영: 창을 적용한 동작 순서에서 `한 줄 특징 입력`과 `순서형 입력 후보`로 갈라지는 도식을 한국어 중심 라벨로 다시 정리했다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어 간체판은 각 언어판 흐름에 맞춰 별도 관리할 수 있다.

- 변경 이유: 입력 창 결정 뒤에 이어지는 두 갈래 설명이 표와 문단으로 나뉘어 있어, `창을 먼저 닫아야 이후 입력 구조도 일관되게 갈린다`는 중심 연결이 한 번 더 또렷하게 드러나지 않았다.
- 본문 반영: 도입과 후반 문단에 같은 중심 연결을 직접 보강해, 입력 창이 먼저이고 그 뒤에 요약 입력과 순서가 남은 입력이 갈라진다는 흐름을 더 빠르게 읽히게 정리했다. `Version`도 `v2026.07.10`으로 갱신했다.

- 번역 동기화 메모: English and Simplified Chinese translations now reflect that the input window decision comes first and then branches into summary input versus order-preserving input, including the reinforced bridge in the introduction and later explanation. / reflected in en, zh
- 번역 반영 상태: 영문·중국어 간체 반영 완료
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.10`

### v2026.07.08

- 변경 이유: Chapter 5 후반부가 `요약 특징 경로`와 `순서가 남는 입력 경로`로 갈라지는 구조를 본문만으로 따라가야 해, 리포트가 지적한 두 갈래 분기가 아직 시각적으로는 약했다.
- 본문 반영: `Windowed event sequence -> summary features / ordered segments`의 작은 Mermaid 도식을 추가해, 같은 입력 창 결정이 한 줄 특징 입력과 순서가 남은 입력 후보를 함께 만든다는 점을 더 빠르게 읽히게 정리했다.

- 변경 이유: Chapter 5 후반부가 `한 줄 특징 표`에서 `시간 순서가 남는 입력 구조`로 넘어가는 전환을 독자가 바로 붙잡기 어렵다는 리포트 지적이 있었다.
- 본문 반영: 도입부에 앞 절까지의 요약 특징 흐름과 현재 절의 시간 순서 입력 구조 흐름을 직접 잇는 한 문장을 추가해, Chapter 5 내부의 설명 축 전환이 더 선명하게 보이도록 정리했다.

- 변경 이유: 입력 창의 시작점·끝점·정렬 기준을 일반 원리로 설명하면서도 `출처와 참고 자료`가 비어 있어, 입력 구조 결정의 근거 연결이 빠져 있었다.
- 본문 반영: Google `labeled example`, W3C PROV, BLS `Base period`를 `출처와 참고 자료`에 추가해, 입력 단위 정의, 창 규칙 추적, 비교 기준 선택을 각각 대응시키도록 보강했다.

- 변경 이유: 도입 첫 문단이 `Part 5에서 ... 읽기 전에`라는 handoff 문장을 먼저 세워, 입력 창의 시작점·끝점·길이 기준 설명보다 배치 메모가 앞에 보였다.
- 본문 반영: 도입을 입력 창이 무엇에 답하는 데이터 모델링 결정인지로 바로 열고, 뒤 Part 호출 없이 현재 질문 자체가 먼저 보이도록 정리했다.
- 변경 이유: 핵심 판단을 백틱 독립 문장으로 따로 세워 본문 흐름보다 강조 문구가 먼저 보였다.
- 본문 반영: 입력 창 판단 문장을 앞뒤 문단에 흡수해 설명이 일반 본문 안에서 자연스럽게 이어지도록 정리했다.
- 변경 이유: `한 문장으로 묶기` 같은 정형 소제목이 설명보다 템플릿 구조를 먼저 드러냈다.
- 본문 반영: 소제목을 제거하고 핵심 결론 문장을 일반 본문 흐름 안에 남겼다.
- 변경 이유: 입력 창 설명 뒤에 `Part 4 입력`, `Part 5 입력`, 역할 비교표가 길게 붙어 현재 절의 창 설계 판단보다 handoff 설명이 더 크게 읽혔다.
- 본문 반영: 표와 문장을 `한 줄 요약 입력`과 `시간 순서가 남은 입력 묶음` 수준으로 일반화하고, 뒤 Part 명시를 줄여 현재 절의 경계를 설명하는 문장만 남겼다.
- 변경 이유: 뒤 Part 연결이 별도 소제목으로 커져, 현재 절의 입력 창 설명보다 handoff 예고가 더 크게 읽히는 문제가 남아 있었다.
- 본문 반영: `Part 4와 Part 5로 이어지는 갈림길`, `무엇이 아직 모델링 범위이고, 무엇이 뒤 Part 범위인가`를 일반 본문 흐름 안으로 접고, 뒤 Part 연결은 현재 절 경계를 설명하는 짧은 문단 수준으로 줄였다.
- 변경 이유: `이 절의 중심`, `이 절의 핵심`, `이 절에서 붙잡아야 할 문장` 같은 메타 문장이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 해당 메타 문장을 일반 설명 문장으로 바꾸고, 같은 뜻을 더 직접적으로 설명하도록 문장 흐름을 정리했다.
- 변경 이유: 말미의 `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`, 본문 중간의 `이 절은 ... 문제로 다시 볼 수 있습니다` 같은 메타 템플릿이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 반복적인 메타 템플릿을 걷어내고, 중심 개념 설명과 사례 흐름만 남기도록 Section 말미와 중간 메타 문장을 정리했다.

- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07

- 변경 이유: 원시 시계열이 곧 학습 입력이 아니라는 설명 다음에, 실제로 입력 창(window)을 어디서 자르고 길이를 어떻게 맞출지에 대한 데이터 모델링 판단을 별도 Section으로 닫아 Part 5 준비 공백을 메울 필요가 있었다.
- 본문 반영: 새 Section `P3-5.4 입력 창(window)은 어디서 자르고 길이는 어떻게 맞추는가`를 추가했다. 시작점·끝점·길이 기준·정렬 기준을 먼저 정하는 이유, 절대 시간 기준과 진행률 기준의 차이, 같은 창 결정이 Part 4 요약 특징과 Part 5 시계열 입력으로 갈라지는 구조를 표와 문장으로 정리했다.

- 번역 동기화 메모: translation should preserve that input windows are a data-modeling decision before they are a sequence-model input shape, especially the distinction between absolute-time alignment and progress-based alignment. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.07`
