# Section Release Note

- Section ID: `P3-2.1`
- Source File: `docs/parts/part-03/chapter-02/section-01.md`

### v2026.07.08-2

- 변경 이유: 말미의 `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`, 본문 중간의 `이 절은 ... 문제로 다시 볼 수 있습니다`, `이 프레임을 잡아 두면` 같은 메타 템플릿이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 반복적인 메타 템플릿을 걷어내고, 중심 개념 설명과 사례 흐름만 남기도록 Section 말미와 중간 메타 문장을 정리했다. 상위 프레임 뒤의 결론 문장도 바로 뜻이 서도록 고쳤다.
- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.08

- 변경 이유: 저장 구조와 데이터셋 후보의 차이는 충분히 설명하고 있었지만, 이를 더 일반적인 stored-record reframing 프레임으로 다시 묶고 외부 근거와 연결하는 보강이 부족했다.
- 본문 반영: `원천 기록 보존 -> 분석 단위 재설정 -> 파생 표현 생성`이라는 상위 프레임을 추가하고, W3C PROV와 Google ML Glossary의 `labeled example`/`label leakage`를 `출처와 참고 자료`에 반영했다.
- 번역 동기화 메모: preserve the stored-record reframing frame and keep the source support tied to derivation, example-unit redesign, and feature generation. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07-4

- 변경 이유: 예제 자체는 적절했지만 출력 제목이 `storage table`, `dataset candidate` 수준에 머물러, 이 절에서 무엇을 확인해야 하는지 코드 결과만으로는 즉시 드러나지 않았다.
- 본문 반영: 출력 단계를 `stored time-step records`, `event-level dataset candidate for comparison`으로 바꿔, 첫 표는 아직 기록 보존 구조이고 둘째 표는 비교 가능한 데이터셋 후보라는 목적 차이가 결과 제목에서 바로 읽히게 정리했다.
- 번역 동기화 메모: keep the output labels purpose-driven so the example explicitly contrasts stored records with an event-level comparison candidate. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-2

- 변경 이유: 저장 구조와 데이터셋 후보 구분 절도 역할 소개 문장을 더 직접 설명형으로 맞출 필요가 있었다.
- 본문 반영: `이 절의 역할은 ...`을 `여기서는 ... 집중합니다`로 다듬어 저장 구조와 데이터셋 후보 구분의 초점이 더 곧바로 읽히게 정리했다.
- 번역 동기화 메모: keep the storage-vs-dataset-candidate focus statement direct and aligned with the shared tone. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-3

- 변경 이유: 기존 Python 예제는 저장 구조와 문제 표현 구조의 `행 수`와 `열 이름` 차이만 보여 주어, 왜 저장된 기록이 곧바로 데이터셋이 아닌지까지는 충분히 밀어 주지 못했다.
- 본문 반영: 예제를 `원시 시점 기록`과 `질문에 맞게 다시 묶은 데이터셋 후보`를 실제로 나란히 출력하는 방향으로 바꿨다. `event_id`별 groupby와 요약 열 생성을 통해 동작 1회가 한 행이 되는 순간과, 그때 비로소 평균 유량과 후반 하강률 같은 비교 열이 생긴다는 점을 직접 보여 주도록 정리했다.
- 번역 동기화 메모: preserve the stronger example that transforms raw time-step records into an event-level dataset candidate, rather than only contrasting row counts and column names. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: 저장 구조와 데이터셋 후보 구분 절도 마지막 자기점검 구간을 구조 구분 재호출과 확인 항목으로 분리할 필요가 있었다.
- 본문 반영: 저장 구조와 문제 표현 구조를 다시 떠올려야 하는 상황을 `언제 이 관점을 먼저 떠올려야 하는가`로 추가하고, 기존 `짧은 점검`은 유지했다.
- 번역 동기화 메모: when translated, keep the storage-vs-dataset-candidate recall prompts separate from the short self-check bullets. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.06-14

- 변경 이유: 저장 구조와 데이터셋 후보의 차이를 말로는 설명하고 있었지만, 저장 구조를 그대로 쓸 때 어느 질문에서 막히는지 초심자 기준으로 더 직접 보여 줄 필요가 있었다.
- 본문 반영: `이번 동작 1회 길이`, `후반 하강 비교`, `최근 대 과거 비교`가 왜 저장 구조만으로 바로 안 되는지 정리한 보강 표를 추가하고, 절 끝에는 짧은 점검 질문을 넣었다.
- 번역 동기화 메모: preserve the concrete “where storage tables fail as datasets” comparison table and the short check questions for beginners. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-14`

### v2026.07.05

- 변경 이유: 메타데이터 누락 보완. / 저장 구조와 문제 표현 구조의 차이를 초심자가 더 빨리 붙잡도록 도입 질문을 보강.
- 본문 반영: Section 메타데이터 추가. / 저장 중심 표와 문제 해결 중심 표를 본 뒤 바로 확인할 수 있는 `처음 표를 받았을 때 먼저 묻는 질문` 표를 추가해, 행의 의미와 비교 가능성, 원시 데이터로 되돌아가는 지점을 더 직접적으로 읽히게 정리했다.
- 번역 동기화 메모: 메타데이터 추가와 짧은 질문 표 보강이 포함되어 번역 시 같은 질문 구조를 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.05`

### v2026.07.06-2

- 변경 이유: Part 3 초입에서도 반복되는 읽기 가이드 문장을 줄이고, 표를 해석하는 본문 흐름 자체로 중심 질문에 들어가도록 정리.
- 본문 반영: `처음 ... 충분합니다` 형태의 안내 문장을 제거하고, 세 질문 표를 `행의 의미와 비교 가능성을 확인하는 질문`으로 바로 연결되게 다듬었다. 질문 표와 저장 구조/문제 표현 구조의 대비 내용은 유지했다.
- 번역 동기화 메모: 읽기 가이드 문장이 줄어든 대신 질문 표의 기능이 더 직접적으로 드러나므로, 번역 시에도 `three checks before reframing the table` 같은 구조를 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-2`

### v2026.07.06-3

- 변경 이유: 저장 구조와 문제 표현 구조 절에서 남아 있던 `초심자` 직접 호명 한 문장을 줄이고, 질문 차이 설명을 일반 서술로 정리.
- 본문 반영: 저장 구조와 문제 표현 구조 비교의 핵심 문단에서 독자 호명 없이 `질문 차이` 자체가 중심이 되도록 다듬었다. 질문 표와 예제 구조는 유지했다.
- 번역 동기화 메모: reader-addressed wording이 더 줄었으므로, 번역 시에도 question-capability contrast를 neutral narration으로 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-3`

### v2026.07.06-4

- 변경 이유: 도입 사례와 질문 표 설명에 `있다고 해 봅시다`, `판단하게 해 줍니다`, `읽힙니다` 같은 완화형 표현이 남아 있어 저장 구조와 문제 표현 구조의 차이를 더 직접적인 본문형으로 정리할 필요가 있었다.
- 본문 반영: 도입 사례 문장을 `있다고 하겠습니다`, `바로 담기기 좋습니다`로 조정하고, 세 질문 표 설명을 `빠르게 가르는 기준`과 `표를 다시 설계하는 일`이라는 직접 진술형 문장으로 교체했다. 마지막 정리 문장도 `더 직접적으로 읽을 수 있습니다`로 다듬었다.
- 번역 동기화 메모: 번역본이 생기면 도입 사례와 질문 표 해설을 reader-coaching phrasing보다 direct explanatory prose로 맞춘다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-10

- 변경 이유: Part 3 앞단에 데이터 모델링 진입 챕터를 추가하면서 기존 Section의 번호 체계와 파일 경로를 한 칸씩 뒤로 재배치할 필요가 있었다.
- 본문 반영: Section 제목 번호, Section ID, 본문 경로, 목차 대응 위치를 새 Part 3 번호 체계에 맞춰 갱신했다. 본문 설명 구조와 사례 흐름은 유지했다.
- 번역 동기화 메모: translation should follow the renumbered Part 3 section IDs and updated source paths while preserving the same content structure. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-11

- 변경 이유: Part 3 리팩터링 기준에 맞추려면 이 Section도 `저장 구조 설명` 자체보다 `저장된 기록이 왜 아직 데이터셋이 아닌가`를 먼저 보여 주는 역할로 읽혀야 했다.
- 본문 반영: Section 제목을 `저장된 기록은 왜 곧바로 데이터셋이 아닌가`로 조정하고, 저장 구조와 문제 표현 구조의 대비를 `데이터셋 후보` 관점으로 다시 묶었다. 마지막 정리 문장도 샘플, 특징, 기준선, 출력 구조가 모두 데이터셋 후보를 만드는 흐름에 속한다는 방향으로 다듬었다.
- 번역 동기화 메모: translation should preserve the reframing from storage-vs-representation contrast to the stronger question of why stored records are not yet a dataset. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-12

- 변경 이유: 3.3과 3.4를 연속으로 읽을 때 3.3이 `샘플`, `특징`, `기준선`, `출력 구조`의 큰 정의까지 다시 끌어안기보다, 저장된 기록과 데이터셋 후보의 구분을 먼저 고정하는 연결 절이라는 점을 더 분명히 할 필요가 있었다.
- 본문 반영: 이 절의 역할이 저장 구조와 데이터셋 후보의 구분을 먼저 고정하는 데 있다는 문장을 추가했다. 뒤 절에서 네 구조를 한 번에 펼치되, 여기서는 왜 저장 구조만으로 그 단계에 바로 들어갈 수 없는지를 먼저 본다는 경계도 함께 보강했다.
- 번역 동기화 메모: translation should preserve 3.3 as a bridge section that fixes the stored-records-vs-dataset-candidate distinction before the four-structure definition pass in 3.4. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-13

- 변경 이유: Part 3 Section 제목 인덱스를 `Section ID`와 같은 `P3-2.1` 형식으로 맞추는 규칙을 적용할 필요가 있었다.
- 본문 반영: 제목 앞 인덱스를 `3.3`에서 `P3-2.1`로 바꿔 본문 제목과 `Section ID` 표기가 같은 체계를 가리키도록 맞췄다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.06`
