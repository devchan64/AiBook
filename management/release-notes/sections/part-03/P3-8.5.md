# Section Release Note

- Section ID: `P3-8.5`
- 대응 본문 경로
  - `docs/parts/part-03/chapter-08/section-05.md`
  - `docs/parts/part-03/chapter-08/section-05.en.md`
  - `docs/parts/part-03/chapter-08/section-05.zh.md`

### v2026.07.20
- 구조 정리: 한국어판, 영어판, 중국어 간체판에서 진행 메타 성격의 목적·읽는 순서·범위 제목을 데이터 모델링의 역할, 문제 구조 흐름, 닫을 경계가 드러나는 제목으로 정리했다.
- 번역 반영 상태: 영어판과 중국어 간체판 반영 완료.
- 원문 기준 버전: `v2026.07.20`
- 변경 이유: 세 언어 본문은 같은 설명 구조를 유지하고 있었지만, 출처 확인일과 Section 버전이 오래된 상태였고 릴리즈노트도 원문 단일 경로 형식으로 남아 있어 다국어 본문 추적과 저작권 검토 기록을 보강했다.
- 본문 반영: 한·영·중 본문 버전을 `v2026.07.20`으로 맞추고, Google for Developers의 thresholding 및 ROC/AUC 자료 확인일을 세 언어 모두 `2026-07-20`으로 갱신했다.
- 저작권 검토: 본문 표, event_id 예시, 판단 축 분류, Mermaid 도식은 자체 구성한 교육용 예시이며 외부 표·도식·이미지·문제·코드 예제를 재사용하지 않았다. Google 자료는 raw score와 threshold·비용 구조의 분리, AUC의 순위화 관점이라는 일반 개념 근거로만 요약·재서술했다.
- 출처 확인: Google for Developers `Thresholds and the confusion matrix`는 raw numerical output이 classification threshold와 비용 맥락을 거쳐 분류로 연결된다는 설명을 확인했다. `Classification: ROC and AUC`는 AUC가 양성 예시를 음성 예시보다 높게 순위화할 확률로 해석된다는 설명과 threshold 선택의 비용 맥락을 확인했다.
- 번역 동기화 메모: English and Simplified Chinese translations remain content-aligned with the Korean source; only version and source access dates were refreshed. / reflected in `docs/parts/part-03/chapter-08/section-05.en.md`, `docs/parts/part-03/chapter-08/section-05.zh.md`
- 번역 반영 상태: 한국어 원문, 영어 번역, 중국어 간체 번역 반영 완료.
- 관련 자산: `docs/assets/part-03/chapter-08/p3-8-5-mermaid-01-ko.mmd`, `docs/assets/part-03/chapter-08/p3-8-5-mermaid-01-en.mmd`, `docs/assets/part-03/chapter-08/p3-8-5-mermaid-01-zh.mmd`

### v2026.07.11
- 본문 반영: 1개의 Mermaid 도식을 `docs/assets/part-03/chapter-08/` 아래 `Section ID + mermaid 순번 + 언어 약어` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 자산 정리: 중국어 간체 본문에서 영문 라벨과 동일한 도식은 별도 `-zh.mmd` 복제본 대신 공용 `-en.mmd` 자산을 직접 참조하도록 바꿔 Part 3 자산 트리의 중복을 줄였다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams, while keeping the same flow structure across language files.

### v2026.07.10
- 본문 반영: 변화 크기, 반복성, 해석 확신도, 운영 중요도를 `우선순위 후보`와 `검토 큐 또는 구조화된 출력`으로 모으는 도식을 한국어 중심 라벨로 다시 정리했다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어 간체판은 각 언어판 흐름에 맞춰 별도 관리할 수 있다.
- 본문 반영: 도식 뒤 반복 목록을 제거하고, `비교 열 -> 판단 축 -> 검토 후보` 구조를 한 문단 결론으로 직접 묶었다. `Version`을 `v2026.07.10`으로 갱신했다.

### v2026.07.08
- 본문 반영: 첫 문단을 `여러 비교 열을 몇 개의 판단 축으로 다시 묶어야 하는 이유`로 바로 시작하게 고쳤고, 말미도 `Chapter 9로 이어 주는 다리` 표현 대신 `비교 열이 어떤 질문 묶음으로 압축되는가`라는 현재 절의 핵심으로 닫았다.
- 본문 반영: 말미의 `상위 프레임` 표를 제거하고, 현재 설명을 표 재진술 없이 직접 마무리하도록 정리했다.
- 번역 동기화 메모: preserve the multi-criteria judgment frame and keep the source support attached to grouped decision axes rather than a fixed scoring recipe. / pending

### v2026.07.07
- 본문 반영: 평균 차이, 반복성, 표본 수, 운영 조건 같은 비교 열을 `변화 크기`, `반복성`, `해석 신뢰도`, `운영 중요도` 네 축으로 먼저 묶는 절을 추가했다. 이를 통해 Chapter 9의 검토 후보 큐가 갑자기 나온 점수표가 아니라, 비교표와 보수적 문장을 압축한 산출물이라는 점을 예시 표와 코드로 보강했다.
- 본문 반영: 아주 단순한 코드 예시를 Mermaid 도식으로 교체했다. 여러 비교 열이 변화 크기, 반복성, 해석 신뢰도, 운영 중요도 네 축으로 묶인 뒤 검토 후보로 이어지는 흐름을 시각적으로 보강했다.
- 번역 동기화 메모: preserve the subsection-label split between the comparison table and the diagram in this section. / pending
