# Section Release Note

- Section ID: `P3-2.3`
- Source File: `docs/parts/part-03/chapter-02/section-03.md`

### v2026.07.07-5

- 변경 이유: 기존 Python 예제는 다섯 줄 메모 문장을 그대로 출력하는 비중이 커서, 왜 그 메모가 필요한지보다 메모 내용을 따라 읽는 느낌이 더 강했다.
- 본문 반영: 예제를 `구조 점검 -> event_id 반복 확인 -> 행 단위 오독 -> event_id 재묶음` 순서로 다시 구성했다. `row_count`와 `event_id_count`, `rows per event`를 먼저 보여 주도록 바꾸어, 표의 정체를 판별하는 신호가 코드 결과에서 먼저 드러나게 정리했다.
- 번역 동기화 메모: preserve the stronger structural-read example where row count, unique event count, and repeated rows per event reveal why regrouping is needed before comparison. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-4

- 변경 이유: 표 읽기 메모의 표현 수위와 예제는 정리됐지만, 이 메모가 어떤 상위 원리에서 나온 것인지 본문에 외부 근거 연결과 참고 자료가 남아 있지 않았다.
- 본문 반영: `observation` 단위, 시간 데이터의 `key/index`, provenance/traceability 원리와 연결되는 설명 문단을 추가하고, Wickham의 `Tidy Data`, Google ML Glossary, Wang et al.의 temporal tidy data 논문, W3C PROV를 `출처와 참고 자료`로 정리했다.
- 번역 동기화 메모: preserve the bridge from the five-line memo to broader principles around observation units, temporal keys/indexes, and provenance. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-3

- 변경 이유: 기존 Python 예제는 열 이름과 후보 해석만 출력해, 왜 `행 의미`, `묶음 기준`, `시간/순서 열`을 먼저 읽어야 하는지 본문의 핵심 주장을 충분히 뒷받침하지 못했다.
- 본문 반영: 원시 로그 표를 바로 행 단위로 비교했을 때와 `event_id`로 다시 묶어 동작 1회 요약 표를 만든 뒤를 나란히 보여 주도록 예제를 교체하고, 이 절의 메모가 왜 `정체 확인`을 먼저 요구하는지 해설도 보강했다.
- 번역 동기화 메모: preserve the staged example showing that row-level reading is insufficient until repeated time-step rows are regrouped into one row per event. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-2

- 변경 이유: `다섯 가지를 먼저 적어 둔다`는 표현이 일반화된 표준 방법론이나 보편 절차처럼 읽힐 수 있어, 이 절의 의도보다 강한 주장으로 오해될 여지가 있었다.
- 본문 반영: 다섯 항목을 `표준 절차`가 아니라 `행 단위, 묶음 기준, 시간 구조, 비교 가능성, 원시 근거`를 놓치지 않기 위한 Part 3의 작업용 메모 축으로 다시 설명하고, 관련 문장도 보편 규칙처럼 들리지 않도록 조정했다.
- 번역 동기화 메모: frame the five-item list as a Part 3 working memo derived from broader principles, not as a universal method. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07

- 변경 이유: Chapter 2에는 저장 구조와 데이터셋 후보의 차이는 설명되어 있었지만, 초심자가 새 표를 처음 받았을 때 실제로 무엇부터 적어야 하는지는 별도 Section으로 고정돼 있지 않았다.
- 본문 반영: 행 의미, 식별자, 시간/순서 열, 비교 가능성, 원시 근거 위치를 먼저 적는 다섯 가지 점검 항목을 새 Section으로 추가했다. 특징 계산이나 모델 선택보다 `표의 정체 확인`이 먼저라는 점을 보강하고, Chapter 3의 데이터셋 재설계 앞에 필요한 실전 브리지 역할을 명시했다.
- 번역 동기화 메모: preserve the beginner checklist for first-contact table reading; keep the five memo items explicit and positioned as a bridge into dataset redesign. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`
