# Section Release Note

- Section ID: `P3-9.3`
- Source File: `docs/parts/part-03/chapter-09/section-03.md`

### v2026.07.11
- 본문 반영: 2개의 Mermaid 도식을 `docs/assets/part-03/chapter-09/` 아래 `Section ID + mermaid 순번 + 언어 약어` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 자산 정리: 중국어 간체 본문은 첫 번째 도식처럼 영문 라벨과 동일한 경우 공용 `-en.mmd`를 직접 참조하고, 두 번째 도식처럼 실제 중국어 라벨이 있는 경우에만 `-zh.mmd` 파생본을 유지하도록 정리했다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams, while keeping the same flow structure across language files.

### v2026.07.10
- 본문 반영: 기준선 차이와 반복성, 운영 문맥을 세 산출물로 나누는 도식을 한국어 중심 라벨로 다시 정리했다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어 간체판은 각 언어판 흐름에 맞춰 별도 관리할 수 있다.
- 본문 반영: 도입을 한 문단으로 압축해 목표 라벨 후보의 의미와 세 산출물의 차이를 함께 세웠다. 이어지는 질문 비교 표는 일반 문장으로 흡수하고, 도식 뒤 중복 정리 문장도 한 문단으로 합쳐 현재 산출물 구분이 더 빨리 읽히도록 재배치했다.

### v2026.07.08
- 본문 반영: 첫 문단을 `같은 사건 목록도 목적에 따라 세 표로 달라진다`는 현재 구조 차이 설명으로 다시 열고, 각 표가 무엇을 앞세우는지만 곧바로 대비하도록 정리했다.
- 본문 반영: 첫 문단을 `비교 리포트`, `검토 후보 큐`, `목표 라벨 후보`라는 현재 산출물 이름과 표 구조 차이 중심으로 다시 열고, `예측 문제로 넘긴다`는 표현은 입력 열과 결과 후보 열이 갈린 표 설명으로 바꿨다.
- 번역 동기화 메모: preserve the direct prose, the three-output frame, and the source-backed explanation that the same event list is reorganized for different purposes. / pending

### v2026.07.07
- 본문 반영: 새 Section `P3-9.3 비교 리포트, 검토 후보 큐, 목표 라벨 후보 표는 어떻게 다른가`를 추가했다. 같은 사건 목록이 세 산출물로 갈라지는 표 비교, 세 단계 절차, 열 구성 차이를 보여 주는 작은 코드 예시, `언제 무엇으로 멈춰야 하는가` 판정 표를 넣어 Chapter 9와 Chapter 10 사이의 연결을 보강했다.
- 본문 반영: 하나의 `events` 표에서 `report_table`, `queue_table`, `target_candidate_table`을 각각 만들어 세 표를 모두 출력하도록 예시를 교체했다. 변화 설명 중심 표, 우선순위 중심 표, Part 4 인계용 표가 어떻게 달라지는지 실제 표 단위로 읽을 수 있게 보강했다.
- 번역 동기화 메모: translation should preserve the branching diagram from one event list into report, review-queue, and target-candidate outputs, with emphasis on each output's decision purpose. / pending
