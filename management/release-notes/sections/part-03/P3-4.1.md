# Section Release Note

- Section ID: `P3-4.1`
- Source File: `docs/parts/part-03/chapter-04/section-01.md`

### v2026.07.20
- 본문 반영 내용: 한국어, 영어, 중국어 간체판의 Python 예제 첫 줄에 원천 측정 로그를 질문에 맞는 샘플 단위로 다시 묶고 비교 가능성을 확인하는 목적을 설명하는 주석을 추가했다.
- 변경 이유: 순차 저작권 검토 과정에서 비교 가능한 샘플 단위 설명, CSV 기반 Python 예제, Mermaid 도식, 외부 근거의 대응을 다시 확인했다.
- 본문 반영 내용: 한국어, 영어, 중국어 간체판 본문 `Version`을 `v2026.07.20`으로 맞추고, 참고문헌 확인일을 2026-07-20으로 갱신했다. 영어판과 중국어 간체판은 한국어 원문 기준의 CSV 입력, `question_focus_options`, `selected_question_focus`, `expected_rows_per_event` 조작값, 이벤트 단위 검토 결과 결합 예제로 동기화했다.
- 저작권 검토: 행 의미 비교표, 샘플 단위 후보 표, Python 예제, CSV 입력 자산, Mermaid 도식은 Part 3의 자체 자동 동작 로그 사례와 자체 샘플 데이터로 확인했다. 외부 문서의 표·예제·그림을 대체할 정도의 복제는 확인되지 않았다.
- 출처 확인: W3C `PROV-Overview`에서 object 식별과 derivation 표현 축을 확인했다. Google `Machine Learning Glossary`에서 labeled example이 features와 label을 포함한다는 설명을 확인했다. BLS `Base period`에서 기준 시점/기간이 다른 기간과 비교하기 위한 reference라는 설명을 확인했다.
- 번역 동기화 메모: 영어판과 중국어 간체판을 한국어 원문 기준 예제·출처 구성에 맞췄다.
- 번역 반영 상태: 한국어, 영어, 중국어 간체판 반영 완료.
- 관련 자산: `docs/assets/part-03/chapter-04/p3_4_1_measurement_log.csv`, `docs/assets/part-03/chapter-04/p3_4_1_review_decisions.csv`, 내부 Mermaid 도식 자산 유지.
- 원문 기준 버전: `v2026.07.20`

### v2026.07.19
- 예제 보강: `question_focus` 조작값과 추천 샘플 단위 출력을 추가해, 비교 가능한 샘플 1건이 고정 정답이 아니라 질문과 특징·라벨 연결성에 따라 달라진다는 점을 코드와 해설에서 확인하게 했다.
- 예제 보강: 원시 시점 로그에 `review_needed`를 반복 주입하지 않고, `event_id` 단위 검토 결과 표를 별도로 둔 뒤 동작 1회 요약 표에 결합하도록 수정했다. 이를 통해 시점 행, 동작 1회, 최근 구간 중 어느 단위에 특징과 라벨이 자연스럽게 붙는지를 더 직접적으로 비교하게 했다.
- 입력 자산 분리: 원시 시점 로그와 이벤트 단위 검토 결과를 CSV 자산으로 분리하고, 본문 코드가 `pd.read_csv(...)`로 읽어 완전성 점검, 라벨 결합, 구간 집계, 질문별 추천 단위를 생성하도록 복잡도를 높였다.
- 예제 보강: 측정 로그와 검토 결과 CSV를 각각 36행으로 확장하고, 출력은 측정 이벤트와 실제로 결합되는 검토 결과만 보여 주도록 조정했다.
- 메타데이터 정합성: 본문 `Version`을 `v2026.07.19`로 갱신했다.
- 검증 메모: `.venv`의 `pandas==3.0.3`으로 P3-4.1 Python 예제를 실행해 본문 예상 출력과 일치함을 확인했다.
- 번역 동기화 메모: English and Simplified Chinese translations should reflect the question-focus example if the localized sections are synchronized.
- 번역 반영 상태: 대기
- 관련 자산: `docs/assets/part-03/chapter-04/p3_4_1_measurement_log.csv`, `docs/assets/part-03/chapter-04/p3_4_1_review_decisions.csv`
- 원문 기준 버전: `docs/parts/part-03/chapter-04/section-01.md` `v2026.07.19`

### v2026.07.17
- 본문 반영: `지금 답할 질문 -> 맞는 단위 선택 -> 동작 1회가 비교 가능한 샘플로 이어짐` 흐름을 압축한 `작은 도식으로 보기` 블록과 Mermaid include를 한국어 본문에 추가했다.
- 번역 동기화 메모: added the matching small-diagram section and localized Mermaid include to the English and Simplified Chinese translations. / reflected in `docs/parts/part-03/chapter-04/section-01.en.md`, `docs/parts/part-03/chapter-04/section-01.zh.md`
- 관련 자산: added `docs/assets/part-03/chapter-04/p3-4-1-mermaid-01-en.mmd`, `docs/assets/part-03/chapter-04/p3-4-1-mermaid-01-ko.mmd`, `docs/assets/part-03/chapter-04/p3-4-1-mermaid-01-zh.mmd`
- 번역 반영 상태: 영어/중국어 반영 완료

### v2026.07.10
- 본문 반영: `시점`, `동작 1회`, `최근 구간` 중 어떤 단위를 샘플 1건으로 볼지 확인하는 예제 앞에 `문제 상황`, `입력(input)`, `기대 출력(output)`, `확인할 개념`을 추가했다.
- 본문 반영: 행과 샘플의 차이를 설명하는 첫 문단들을 압축하고, `비교 가능한 샘플`의 세 기준과 표 판별 순서가 더 빨리 나오도록 재배치했다. 데이터 생애주기 설명은 빠른 판별표 근처로 옮겨 이해 순서를 정리했다.
- 번역 동기화 메모: reflected the Python-example preamble, the compressed direct opening around row meaning versus comparable sample choice, and the quick table-diagnosis ordering in the English and Simplified Chinese translations. / reflected in `docs/parts/part-03/chapter-04/section-01.en.md`, `docs/parts/part-03/chapter-04/section-01.zh.md`

### v2026.07.09
- 본문 반영: 내용 추가 없이 기존 문단 블록만 재배치해 `행과 샘플의 차이 -> 비교 가능한 샘플의 세 기준 -> Chapter 3 연결과 읽기 순서 -> 작은 표 예시 -> 질문별 샘플 단위 선택 -> 코드 예제와 출력 해설 -> 빠른 판별표 -> 결론` 흐름으로 다시 묶었다.
- 번역 동기화 메모: align future English and Chinese versions to the reordered Korean structure without adding or deleting content; as of 2026-07-09, corresponding `docs/en` and `docs/zh` files for `P3-4.1` do not exist yet, so translation follow-up remains pending. / pending

### v2026.07.08
- 본문 반영: `이전 Chapter에서 남긴 것 / 이번 Chapter에서 추가로 정하는 것 / 다음 Chapter로 넘길 구조` 표를 추가해, 학습 문제 승격 보류 판단이 실제 샘플 단위 결정으로 이어지는 현재 장의 역할을 더 직접적으로 읽히게 정리했다.
- 본문 반영: 뒤 장 연결 메모와 독립 선언문을 일반 본문으로 흡수하고, `비교 가능한 샘플`과 `분석 단위` 판단을 한 흐름에서 직접 설명하도록 정리했다.
- 번역 동기화 메모: preserve the analytical-unit-choice frame, keep the section free of repeated meta-study blocks, and tie the source support to observation units, analysis units, and aggregation units. / pending

### v2026.07.07
- 본문 반영: Python 예제와 예상 출력의 단계 이름을 목적 중심으로 다시 붙여, 각 출력이 `후보 단위별 행 수`, `시점 기록`, `동작 단위 샘플`, `구간 집계`, `비교 가능한 샘플 적합성 점검` 중 무엇을 보여 주는지 더 직접적으로 드러나게 정리했다.
- 본문 반영: 예제 입력에 `review_needed`를 추가하고, `event_summary`에 결과 열을 함께 남기도록 바꿨다. 또 `measurement_row`, `event`, `window`를 같은 기준으로 비교하는 `unit check` 출력 표를 추가해, 특징 가능 여부와 결과 열 부착 가능성을 코드 결과에서 바로 읽을 수 있게 보강했다.
- 번역 동기화 메모: when translated, keep the sample-unit recall prompts separate from the short self-check bullets. / pending

### v2026.07.06
- 본문 반영: `event_id` 반복, 요약 열, 최근 구간 비교 열을 기준으로 표의 행 의미를 판별하는 보강 표를 추가하고, 절 끝에는 샘플 판별 기준을 확인하는 짧은 점검 질문을 넣었다.
- 본문 반영: 3.7의 큰 구분 축과 개념사전 연결을 기준 위치 설명으로 바꾸어, 뒤 절들이 참조하는 대표 설명 위치가 더 직접적으로 읽히게 다듬었다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending

### v2026.07.05
- 본문 반영: Section 메타데이터 추가. / `feature`, `label`, `baseline`, `evaluation`을 한국어 설명 우선 표현으로 정리했다.
- 번역 동기화 메모: 본문 내용 변화 없이 메타데이터만 추가되어 번역본 영향은 없다. / 없음
