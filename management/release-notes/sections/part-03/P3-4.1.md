# Section Release Note

- Section ID: `P3-4.1`
- Source File: `docs/parts/part-03/chapter-04/section-01.md`

### v2026.07.08-2

- 변경 이유: 말미의 `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`, 본문 중간의 `이 절은 ... 문제로 다시 볼 수 있습니다` 같은 메타 템플릿이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 반복적인 메타 템플릿을 걷어내고, 중심 개념 설명과 사례 흐름만 남기도록 Section 말미와 중간 메타 문장을 정리했다.
- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07-9

- 변경 이유: 예제 내용은 충분했지만 출력 제목이 너무 짧아, 어떤 단계가 `시점 기록`, `동작 단위 샘플`, `구간 집계`, `적합성 점검`을 가리키는지 한눈에 구분하기는 아직 약했다.
- 본문 반영: Python 예제와 예상 출력의 단계 이름을 목적 중심으로 다시 붙여, 각 출력이 `후보 단위별 행 수`, `시점 기록`, `동작 단위 샘플`, `구간 집계`, `비교 가능한 샘플 적합성 점검` 중 무엇을 보여 주는지 더 직접적으로 드러나게 정리했다.
- 번역 동기화 메모: keep the stage labels explicit so the output reads as a sample-unit decision walkthrough, not just a sequence of tables. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-8

- 변경 이유: 앞선 보강으로 샘플 단위별 차이는 설명됐지만, Python 예제 실행 결과만 보고도 어떤 단위가 비교 가능한 샘플에 더 가까운지 곧바로 읽히는 정도는 아직 약했다.
- 본문 반영: 예제 입력에 `review_needed`를 추가하고, `event_summary`에 결과 열을 함께 남기도록 바꿨다. 또 `measurement_row`, `event`, `window`를 같은 기준으로 비교하는 `unit check` 출력 표를 추가해, 특징 가능 여부와 결과 열 부착 가능성을 코드 결과에서 바로 읽을 수 있게 보강했다.
- 번역 동기화 메모: preserve the strengthened Python output that includes `review_needed` at the event level and the `unit check` table comparing measurement rows, events, and windows. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-7

- 변경 이유: 비교 가능한 샘플의 조건과 예제 해설은 보강됐지만, 독자가 실제로 `시점`, `동작 1회`, `최근 구간` 중 무엇을 먼저 샘플로 잡아야 하는지 판단 절차까지는 아직 본문 안에서 바로 회수하기 어려웠다.
- 본문 반영: 질문 유형별로 먼저 잡을 샘플 단위를 정리한 선택 표를 추가하고, 절 마지막에는 `질문이 비교 단위와 특징·라벨 구조를 함께 정한다`는 한 줄 요약을 보강했다.
- 번역 동기화 메모: preserve the question-to-sample-unit choice table and the concluding line that sample choice is determined by the question plus feature/label structure, not by the raw table alone. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-6

- 변경 이유: 예제의 행 수와 단위별 열 차이는 보였지만, 독자가 왜 그 결과를 보고 `동작 1회`를 비교 가능한 샘플로 판단해야 하는지 일반 기준으로 회수하기에는 아직 설명이 부족했다.
- 본문 반영: `비교 가능한 샘플`의 최소 조건 세 가지를 본문에 명시하고, 예제 뒤에는 `측정 시점`, `동작 1회`, `최근 구간`을 그 기준으로 다시 평가하는 보강 표와 해설 문단을 추가했다.
- 번역 동기화 메모: preserve the three criteria for a comparable sample and the follow-up table that re-evaluates row, event, and window units against those criteria. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-5

- 변경 이유: 샘플 수 차이만으로는 왜 `동작 1회`가 비교 가능한 샘플 단위이고 `최근 구간`은 그 위의 해석 단위인지, 또 어떤 열이 어느 단위에서만 자연스러운지까지는 충분히 드러나지 않았다.
- 본문 반영: 예제 출력에 `event_id`별 측정 행 수와 `pressure_rise` 같은 동작 단위 요약 열을 추가하고, 뒤 문단에는 값별로 어떤 단위에서 자연스러운지 정리한 보강 표를 덧붙였다.
- 번역 동기화 메모: preserve the distinction between row counts and unit-specific columns, especially the per-event `pressure_rise` example and the value-to-unit mapping table. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-4

- 변경 이유: 기존 Python 예제는 시점 행을 `event_id` 기준으로 묶는 1단계만 보여 주어, 같은 원천데이터에서 샘플 단위 후보가 `시점`, `동작 1회`, `최근 구간`으로 달라질 수 있다는 이 절의 핵심을 충분히 드러내지 못했다.
- 본문 반영: 예제 입력에 최근 구간 구분 열을 추가하고, 출력에서 `측정 시점 수`, `동작 1회 샘플 수`, `구간 집계 수`를 함께 비교하도록 보강했다. 해설도 `몇 건으로 세고 있는가`를 먼저 읽도록 조정했다.
- 번역 동기화 메모: preserve the strengthened example that contrasts measurement rows, per-event samples, and window-level aggregates from the same raw data. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-3

- 변경 이유: 도입부가 행과 샘플의 차이 자체보다 대표 설명 위치를 먼저 말해 핵심 개념 진입이 늦어졌다.
- 본문 반영: 뒤의 특징, 라벨, 비교 기준이 모두 샘플 단위 위에 놓인다는 이유를 먼저 제시하고, `행이 보인다고 곧 샘플은 아니다`라는 판단으로 바로 연결했다.
- 번역 동기화 메모: foreground why row-vs-sample distinction matters before any section-role framing. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-2

- 변경 이유: 샘플 단위 도입 절도 대표 설명 위치 문장을 더 직접 설명형으로 맞출 필요가 있었다.
- 본문 반영: `이 절은 ... 대표 설명 위치입니다`를 `여기서는 ... 대표 설명 위치를 맡습니다`로 다듬어 행과 샘플 구분 앵커가 더 직접적으로 읽히게 정리했다.
- 번역 동기화 메모: keep the row-vs-sample anchor statement direct and aligned with the shared tone. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: 샘플 단위 도입 절도 마지막 자기점검 구간을 샘플 판별 관점 재호출과 확인 항목으로 분리해 Part 3 기준선을 맞출 필요가 있었다.
- 본문 반영: 한 행과 샘플 1건을 다시 구분해야 하는 상황을 `언제 이 관점을 먼저 떠올려야 하는가`로 추가하고, 기존 `짧은 점검`은 유지했다.
- 번역 동기화 메모: when translated, keep the sample-unit recall prompts separate from the short self-check bullets. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.07`

### v2026.07.06-15

- 변경 이유: 샘플 판별 절이 행 의미와 샘플 단위를 설명하고 있었지만, 처음 표를 받을 때 어떤 신호로 표 종류를 가를지 초심자용 점검 장치가 더 필요했다.
- 본문 반영: `event_id` 반복, 요약 열, 최근 구간 비교 열을 기준으로 표의 행 의미를 판별하는 보강 표를 추가하고, 절 끝에는 샘플 판별 기준을 확인하는 짧은 점검 질문을 넣었다.
- 번역 동기화 메모: preserve the quick table-type diagnosis cues and the short check questions for beginners. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-15`

### v2026.07.06-14

- 변경 이유: 행과 샘플 차이 대표 설명 위치 문장에 남아 있던 `다시 돌아오면 됩니다` 표현을 직접 설명형으로 정리할 필요가 있었다.
- 본문 반영: 3.7의 큰 구분 축과 개념사전 연결을 기준 위치 설명으로 바꾸어, 뒤 절들이 참조하는 대표 설명 위치가 더 직접적으로 읽히게 다듬었다.
- 번역 동기화 메모: keep P3-4.1 declarative and preserve it as the Part 3 anchor for distinguishing rows from sample units. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.06-14`

### v2026.07.05

- 변경 이유: 메타데이터 누락 보완. / Chapter 3 시작 구간의 영어 기능어 노출 완화.
- 본문 반영: Section 메타데이터 추가. / `feature`, `label`, `baseline`, `evaluation`을 한국어 설명 우선 표현으로 정리했다.
- 번역 동기화 메모: 본문 내용 변화 없이 메타데이터만 추가되어 번역본 영향은 없다. / 없음
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.05`

### v2026.07.06-2

- 변경 이유: 샘플 단위 도입 절에서도 `도움이 된다`, `초심자` 같은 메타 안내를 줄이고, 행 의미와 샘플 의미의 차이가 본문 질문 구조에서 바로 드러나게 정리.
- 본문 반영: 표를 읽는 순서 도입과 샘플 1건 설명에서 독자 지시형 표현을 일반 서술로 바꾸고, `행이 있다고 해서 곧 샘플은 아니다`는 논리를 직접 연결했다. 표와 예제 구조는 유지했다.
- 번역 동기화 메모: learner-directed phrasing이 줄고 row-vs-sample distinction이 직접화되었으므로, 번역 시에도 same framing을 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-2`

### v2026.07.06-3

- 변경 이유: Chapter 3 시작 절의 도입과 마무리에 남아 있던 `해 봅시다`, `살펴보겠습니다` 같은 유도형 표현을 직접 설명형 문장으로 정리할 필요가 있었다.
- 본문 반영: 원천 시계열 예시 도입을 `해 봅시다`에서 `하겠습니다`로 바꾸고, 다음 절 연결 문장을 `더 직접적으로 다룹니다`로 조정했다. 행 의미와 샘플 단위의 핵심 논리, 표와 예제 구조는 유지했다.
- 번역 동기화 메모: 번역본이 생기면 scenario setup과 next-section handoff 문장을 guide-like phrasing보다 direct explanatory prose로 맞춘다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-10

- 변경 이유: Part 3 앞단에 데이터 모델링 진입 챕터를 추가하면서 기존 Section의 번호 체계와 파일 경로를 한 칸씩 뒤로 재배치할 필요가 있었다.
- 본문 반영: Section 제목 번호, Section ID, 본문 경로, 목차 대응 위치를 새 Part 3 번호 체계에 맞춰 갱신했다. 본문 설명 구조와 사례 흐름은 유지했다.
- 번역 동기화 메모: translation should follow the renumbered Part 3 section IDs and updated source paths while preserving the same content structure. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-11

- 변경 이유: Chapter 4 시작 Section이 단순히 `행 의미`를 묻는 절에 머무르기보다, `비교 가능한 샘플 한 건을 어떻게 정하는가`라는 리팩터링 흐름의 출발점으로 읽혀야 했다.
- 본문 반영: Section 제목을 `비교 가능한 샘플 한 건은 어떻게 정하는가`로 조정하고, 도입과 마무리 문장에 샘플 결정이 Part 3 전체의 후속 구조를 여는 역할이라는 설명을 보강했다. 표와 예제 구조는 유지했다.
- 번역 동기화 메모: translation should preserve the shift from generic row-meaning framing to the more explicit question of defining a comparable sample unit. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-12

- 변경 이유: 3.7과 3.8을 연속으로 읽을 때 3.7이 `행과 샘플의 차이`를 고정하는 대표 설명 위치라는 점이 더 분명해야, 뒤 절이 같은 정의를 다시 반복하지 않고 파급효과 설명으로 좁혀질 수 있었다.
- 본문 반영: 3.7을 Part 3 안에서 `한 행`과 `샘플 1건`의 차이를 가장 먼저 고정하는 대표 설명 위치라고 명시했다. 또한 핵심 문단에서 샘플 뜻 자체의 재정의보다 `어떤 표가 어떤 질문에 답하려고 만들어졌는지`를 먼저 구분하는 일이 중심이라는 점을 보강했다.
- 번역 동기화 메모: translation should preserve 3.7 as the main Part 3 anchor for distinguishing rows from sample units and keep the focus on question-appropriate table roles. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-13

- 변경 이유: Part 3 Section 제목 인덱스를 `Section ID`와 같은 `P3-4.1` 형식으로 맞추는 규칙을 적용할 필요가 있었다.
- 본문 반영: 제목 앞 인덱스를 `3.7`에서 `P3-4.1`로 바꿔 본문 제목과 `Section ID` 표기가 같은 체계를 가리키도록 맞췄다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.06`
