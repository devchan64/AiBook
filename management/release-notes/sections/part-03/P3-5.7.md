# Section Release Note

- Section ID: `P3-5.7`
- Source File: `docs/parts/part-03/chapter-05/section-07.md`

### v2026.07.10
- 본문 반영: 예제 앞에 `문제 상황`, `입력(input)`, `기대 출력(output)`, `확인할 개념`을 추가하고 Section 버전을 갱신했다.
- 번역 동기화 메모: English and Simplified Chinese translations now reflect the example preamble and preserve the aggregation-rule distinction among `any`, `first`, `worst`, and `count`, including the report-vs-prediction note. / reflected in en, zh

### v2026.07.08
- 본문 반영: Google `label`, `labeled example`, `label leakage`, W3C PROV를 `출처와 참고 자료`에 추가해, 대표 결과 열 정의, 보고용/예측용 결과 구분, 접기 규칙 추적을 각각 대응시키도록 보강했다.
- 본문 반영: 정리 문장을 `대표 결과를 어떤 규칙으로 접어 표에 남길지`라는 현재 데이터 모델링 판단으로 바꿔, 결과 열 설계 설명 안에서 문장이 닫히도록 정리했다.
- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending

### v2026.07.07
- 본문 반영: 예제를 결과 DataFrame 출력으로 바꾸고, `worst_event` 열까지 추가해 같은 샘플이 접기 규칙에 따라 어떻게 다른 결과 구조로 바뀌는지 코드 결과에서 직접 비교할 수 있게 했다.
- 본문 반영: 새 Section `P3-5.7 같은 샘플 뒤의 여러 후속 사건은 표 구조에서 어떻게 접어야 하는가`를 추가했다. `any`, `first`, `worst`, `count` 같은 접기 규칙이 결과 표의 뜻을 어떻게 바꾸는지 정리했다.
- 번역 동기화 메모: preserve the aggregation-rule distinction among any/first/worst/count when translated. / pending
