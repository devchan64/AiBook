# Section Release Note

- Section ID: `P3-6.4`
- Source File: `docs/parts/part-03/chapter-06/section-04.md`

### v2026.07.08

- 변경 이유: 작업 표 예제를 보강한 뒤에도 각 열의 역할을 일반화된 기준으로 묶는 상위 프레임과 공식 근거가 부족해, `숫자 열이면 다 feature`라는 오해를 구조적으로 끊어 주는 설명이 더 필요했다.
- 본문 반영: 메타 템플릿 제거와 예제 보강 상태를 유지하면서, 작업 표 안의 열을 입력 설명 열, 비교 열, 결과 열 후보, 식별·문맥 열로 나누는 상위 프레임 표를 추가했다. 또한 Google의 `labeled example`, `label leakage`와 W3C PROV 자료를 참고 자료로 붙여, feature·label 분리와 문맥 열 분리가 왜 필요한지 일반화된 근거와 함께 설명했다.
- 번역 동기화 메모: preserve the column-role-separation frame, the strengthened example, and the source-backed distinction among feature, comparison, target-candidate, and context columns. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07-2

- 변경 이유: 기존 코드 예시는 열 이름을 역할별 리스트로만 나눠 출력해, `한 작업 표 안에 여러 역할의 열이 함께 있을 수 있다`는 이 절의 핵심 장면을 코드 결과가 직접 보여 주는 정도는 약했다.
- 본문 반영: 예제를 `mixed working table -> column roles` 2단계 출력으로 바꾸고, `used_as_model_input`까지 함께 적어 같은 숫자 열도 특징, 비교 열, 결과 후보로 다르게 읽힐 수 있음을 표 결과에서 바로 확인하게 했다.
- 번역 동기화 메모: preserve the strengthened example where one mixed working table is followed by an explicit column-role table distinguishing features, comparison columns, target candidates, and context. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07

- 변경 이유: Chapter 5와 Chapter 6을 읽은 뒤에도 초심자가 `요약 표의 모든 열이 곧 feature인가`에서 자주 멈추며, 이 혼동이 뒤의 기준선 비교 열과 target 후보 열 구분까지 함께 흔들고 있었다.
- 본문 반영: 새 Section `P3-6.4 요약 표의 모든 열이 왜 특징은 아닌가`를 추가했다. 요약 표 안의 열을 특징 열, 비교 열, 결과 열 후보, 식별·문맥 열로 먼저 나누는 기준을 제시했고, 이 구분이 Chapter 7과 Chapter 10으로 어떻게 이어지는지 브리지 문장으로 정리했다.
- 번역 동기화 메모: translation should preserve the four-way separation among feature columns, comparison columns, target-candidate columns, and context columns as an early bridge before baseline and handoff sections. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.07`
