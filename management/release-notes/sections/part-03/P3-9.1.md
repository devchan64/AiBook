# Section Release Note

- Section ID: `P3-9.1`
- Source File: `docs/parts/part-03/chapter-09/section-01.md`

### v2026.07.11
- 변경 이유: Part 3의 Mermaid 도식을 본문 인라인 코드로만 두지 않고, P5-1.1과 같은 `mmd` 자산 관리 방식으로 정리할 필요가 있었다.
- 본문 반영: 1개의 Mermaid 도식을 `docs/assets/part-03/chapter-09/` 아래 `Section ID + mermaid 순번 + 언어 약어` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 자산 정리: 중국어 간체 본문에서 영문 라벨과 동일한 도식은 별도 `-zh.mmd` 복제본 대신 공용 `-en.mmd` 자산을 직접 참조하도록 바꿔 Part 3 자산 트리의 중복을 줄였다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams, while keeping the same flow structure across language files.
- 번역 반영 상태: 영문·중국어 간체 반영 완료
- 관련 자산: `docs/assets/part-03/chapter-09/`
- 원문 기준 버전: `v2026.07.11`

- 변경 이유: 도입과 예시 표에 `센서 로그`, `운영 조건`, `운영 문맥` 같은 표현이 남아 있어, 현재 절의 문제 유형 판단보다 특정 업무 맥락이 먼저 떠오를 여지가 있었다.
- 본문 반영: 도입을 `사건 기록`과 `결과 메모` 중심으로 일반화하고, `운영` 표현을 `판단 맥락`, `우선순위 기준`, `사건 단위 특징 표` 같은 문제 유형 설명으로 바꾸어 현재 절이 스스로 닫히도록 정리했다.
- 변경 이유: 후반 판단 순서와 도식 해설에 `운영 라벨`, `운영 우선순위` 같은 표현이 남아 있어, 라벨 예측 판단 자체보다 적용 현장이 먼저 보일 수 있었다.
- 본문 반영: 해당 구간을 `판단 라벨`, `검토 우선순위` 중심으로 다시 묶고, `현재 데이터가 어디까지 정직하게 지지하는가`라는 절의 중심 질문이 끝까지 유지되도록 재배치했다.
- 변경 이유: 문제 유형 분기 설명에 `다음 단계`, `더 높은 단계의 학습 문제` 같은 표현이 남아 있어, 현재 절의 핵심인 문제 유형 정의보다 단계 handoff 뉘앙스가 조금 남아 있었다.
- 본문 반영: `더 복잡한 문제로 정의`, `문제 유형을 더 무겁게 정한다`, `실제 운영 판단`처럼 현재 절 안에서 문제 유형을 고르는 표현으로 바꾸고 버전을 갱신했다.
- 변경 이유: 같은 날짜 항목 안에서도 `문제 승격` 표현이 남아 있어, 현재 절의 `지금 어디서 멈추는가`라는 분기보다 위계 상승 뉘앙스가 다시 살아날 여지가 있었다.
- 본문 반영: 해당 표현을 `문제 유형 상향`으로 바꿔, 현재 데이터가 허용하는 범위를 넘겨 올리는 판단이라는 뜻이 직접 보이도록 정리했다.
- 번역 동기화 메모: English and Simplified Chinese translations now reflect the current-problem-typing wording, including the generalized event-record framing and the revised evidence ladder for alert, review candidate, and label prediction. / reflected in `docs/parts/part-03/chapter-09/section-01.en.md`, `docs/parts/part-03/chapter-09/section-01.zh.md`
- 번역 반영 상태: 영문·중국어 간체 반영 완료
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.11`

### v2026.07.10

- 변경 이유: 문제 승격 분기 Mermaid가 영어 중심 라벨로 남아 있어, 경고-검토 후보-라벨 예측을 가르는 현재 절의 판단 구조가 한국어 본문 흐름과 바로 맞물리지 않았다.
- 본문 반영: 현재 데이터 상태에서 `비교 신호`, `검토 규칙`, `안정된 목표 라벨과 평가 구조`를 차례로 묻는 도식을 한국어 중심 라벨로 다시 정리했다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어 간체판은 각 언어판 흐름에 맞춰 별도 관리할 수 있다.

- 변경 이유: Chapter 9 도입 절이 같은 구분을 `이전 Chapter 비교표`, `문제 형태 표`, `단계 표`, `운영 흐름`으로 여러 번 다시 말해, 핵심 판단인 `지금 데이터가 어디까지 정직하게 지지하는가`가 상대적으로 늦게 드러났다.
- 본문 반영: 도입에서 메타 성격의 연결 표를 걷어내고 핵심 판단 문장을 앞세웠다. 이어지는 비교도 `입력 예시-출력 예시-필요 근거-아직 올리지 않는 것`을 한 표로 묶어, 문제 승격 기준이 한 번에 읽히도록 재배치했다.

- 번역 동기화 메모: English and Simplified Chinese translations now preserve the direct escalation frame from alert to review prioritization to supervised prediction, including the stop-here decision table tied to current evidence strength. / reflected in `docs/parts/part-03/chapter-09/section-01.en.md`, `docs/parts/part-03/chapter-09/section-01.zh.md`
- 번역 반영 상태: 영문·중국어 간체 반영 완료
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.10`

### v2026.07.08

- 변경 이유: 해석 경계까지 세운 비교표가 Chapter 9에서 어떤 산출물 구분으로 이어지는지 도입에서 바로 회수되지 않아, 경고·검토 후보·라벨 예측이 새 구분처럼 보일 여지가 있었다.
- 본문 반영: `이전 Chapter에서 남긴 것 / 이번 Chapter에서 추가로 정하는 것 / 이 장 끝에 남는 구조` 표를 추가해, 비교표가 문제 승격 경계를 만나 산출물 구분으로 갈라지는 현재 장의 역할을 더 직접적으로 읽히게 정리했다.

- 변경 이유: 도입 중간에 `이 절에서는` 메타 안내가 다시 들어와, 경고·검토 후보·라벨 예측의 차이를 바로 설명하는 흐름이 잠시 끊겼다.
- 본문 반영: 질문 목록 도입을 `지금 단계에서 바로 확인해야 하는 세 질문`으로 바꾸고, 문제 단계 비교도 `사다리` 비유보다 `근거 강도` 차이를 직접 말하는 쪽으로 정리했다.

- 변경 이유: 말미가 `모델링 사다리`와 다음 절 예고에 기대어 닫혀, 현재 절의 `경고/검토 후보/라벨 예측` 구분보다 handoff 비중이 더 크게 남았다.
- 본문 반영: 마지막 문단들을 현재 데이터가 어디까지 정직하게 지지하는가라는 판단으로 다시 묶고, 예측 문제는 앞 단계 근거가 충분할 때만 검토한다는 현재 절 결론으로 정리했다.

- 변경 이유: `상위 프레임` 재진술 표가 다른 Section들과 같은 마감 템플릿으로 반복되어, 본문이 교안형으로 굳어 보였다.
- 본문 반영: 말미의 `상위 프레임` 표를 제거하고, 현재 설명을 표 재진술 없이 직접 마무리하도록 정리했다.

- 변경 이유: `일반화된 상위 프레임으로 다시 보면` 제목이 Part 3 전반에 반복되어, 현재 절 설명도 같은 템플릿형 마감으로 굳어 보였다.
- 본문 반영: 반복 제목을 제거하고, 상위 프레임 표와 정리 문장을 현재 설명 흐름 안에 직접 이어 붙였다.

- 변경 이유: 문제 승격 설명에 메타 문구가 반복되고, 경고·검토 후보·라벨 예측의 구분을 일반화된 상위 프레임과 외부 근거로 다시 받쳐 주는 보강이 더 필요했다.
- 본문 반영: 메타 안내 문장을 직접 설명형으로 정리하고, `signal detection -> review prioritization -> supervised prediction` 프레임과 출처 묶음을 추가해 단계 상승이 아니라 전제 강화의 문제라는 점을 더 분명히 했다.

- 번역 동기화 메모: preserve the direct prose, the escalation frame, and the source-backed distinction between alert, review prioritization, and supervised prediction. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07

- 변경 이유: `분류기부터 만든다`, `모델이 단순한가 복잡한가`, `모델을 더 쓰고 싶다` 같은 표현이 문제 승격 판단보다 이름이나 도구 선택의 뉘앙스를 먼저 만들 수 있었다.
- 본문 반영: 해당 문장들을 `분류 문제로 올린다`, `문제가 단순한가 복잡한가`, `더 높은 단계의 학습 문제로 올린다`처럼 더 일반화된 문제 구조 언어로 조정했다.
- 변경 이유: 이 절은 라벨 예시보다 `지금 어느 단계에서 멈춰야 하는가`를 가르는 문제 승격 분기가 중심이므로, 코드 예시보다 도식이 더 적합했다.
- 본문 반영: 작은 코드 예시를 Mermaid 도식으로 교체했다. 비교 신호만 있는지, 검토 규칙까지 있는지, 안정된 목표 라벨과 평가 구조가 있는지에 따라 경고, 검토 후보, 라벨 예측으로 갈라지는 판단 흐름을 시각적으로 보강했다.
- 변경 이유: 문제 승격 판단 절도 마지막 자기점검 구간을 경고·검토 후보·라벨 예측 구분 재호출과 확인 항목으로 분리할 필요가 있었다.
- 본문 반영: 지금 문제를 어디까지 올릴지 다시 떠올려야 하는 상황을 `언제 이 관점을 먼저 떠올려야 하는가`로 추가하고, 기존 `짧은 점검`은 유지했다.

- 번역 동기화 메모: when translated, keep the alert-review-label-escalation recall prompts separate from the short self-check bullets. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.05

- 변경 이유: 메타데이터 누락 및 영어 기능어 밀도 완화. / 경고, 검토 후보, 라벨 예측의 문제 사다리를 더 직접적으로 보이게 할 필요가 있었음.
- 본문 반영: Section 메타데이터 추가, 용어 설명 정리, Part 4 연결 문장 보정. / 세 문제 형태를 단계별로 비교하는 사다리 표와 예제 뒤의 3단계 확인 질문을 추가했다.

- 번역 동기화 메모: 메타데이터 추가와 사다리 표/확인 질문 보강이 포함되어 번역 시 문제 단계 구분을 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.05`

### v2026.07.06

- 변경 이유: 경고·검토 후보·라벨 예측 절에서도 독자 반응을 직접 호출하는 메타 문장을 줄이고, 문제 사다리 구조가 본문 논리로 바로 드러나게 정리.
- 본문 반영: 도입의 `초심자는 ... 종종` 표현을 일반 서술로 바꾸고, 사다리 표 도입과 예제 확인 순서도 현재 데이터 상태에서 가능한 문제 형태를 직접 읽는 문장으로 다듬었다. 세 문제 구분과 예제 구조는 유지했다.
- 변경 이유: 경고·검토 후보·라벨 예측 절의 도입, 문제 사다리 해설, 다음 절 연결에 남아 있던 `생각하기 쉽습니다`, `이해하는 편이 안전합니다`, `정리하겠습니다` 같은 유도형 표현을 직접 설명형 문장으로 정리할 필요가 있었다.
- 본문 반영: 분류기부터 만들자는 발상을 `받아들이기 쉽습니다`로 조정하고, 문제를 위로 올릴수록 더 강한 근거가 필요하다는 문장을 직접 진술형으로 바꾸었다. 다음 절 연결도 `정리합니다`로 정리해 Part 4 인계 전제가 더 직접적으로 드러나게 했다.
- 변경 이유: Part 3 앞단에 데이터 모델링 진입 챕터를 추가하면서 기존 Section의 번호 체계와 파일 경로를 한 칸씩 뒤로 재배치할 필요가 있었다.
- 본문 반영: Section 제목 번호, Section ID, 본문 경로, 목차 대응 위치를 새 Part 3 번호 체계에 맞춰 갱신했다. 본문 설명 구조와 사례 흐름은 유지했다.
- 변경 이유: 문제 유형 구분 Section이 용어 비교보다 `지금 문제를 경고, 검토 후보, 라벨 예측 중 어디까지 올릴 것인가`를 판단하는 절로 읽히도록 리팩터링 축에 맞출 필요가 있었다.
- 본문 반영: Section 제목을 `지금 문제를 어디까지 올려야 하는가`로 조정하고, 도입에 해석 경계를 세운 뒤 문제 수준을 정해야 한다는 연결 문장을 추가했다. 문제 사다리 표와 예제 구조는 유지했다.
- 변경 이유: Part 3 Section 제목 인덱스를 `Section ID`와 같은 `P3-9.1` 형식으로 맞추는 규칙을 적용할 필요가 있었다.
- 본문 반영: 제목 앞 인덱스를 `3.17`에서 `P3-9.1`로 바꿔 본문 제목과 `Section ID` 표기가 같은 체계를 가리키도록 맞췄다.
- 변경 이유: 문제 수준 판단 절의 사다리 해설과 예제 마무리에 남아 있던 `...처럼 읽으면`, `기억해야 할 핵심은 다음과 같습니다` 같은 메타 안내를 직접 설명형으로 더 정리할 필요가 있었다.
- 본문 반영: 세 문제를 `사다리를 이룬다`는 진술로 바꾸고, 예제 해설도 `다음 순서로 보여 준다`로 정리했다. 마지막 핵심 문장 역시 `핵심은 ... 점입니다` 형식으로 바꾸어 문제 설정 강도의 차이가 본문 진술 자체로 읽히게 했다.
- 변경 이유: 문제를 어디까지 올릴지 판단하는 절에서, 독자가 현재 근거 수준에 따라 어느 단계에서 멈춰야 하는지 더 직접적으로 점검할 장치가 필요했다.
- 본문 반영: `지금 확인한 상태 -> 이 단계의 산출물 -> 아직 올리지 않는 단계` 표를 추가해 경고, 검토 후보, 라벨 예측의 승격 기준을 한눈에 비교할 수 있게 했다. 끝에는 짧은 점검 질문을 넣어 라벨 공백 상태와 문제 단계 선택을 다시 확인하게 했다.

- 번역 동기화 메모: translation should preserve the added stop-here decision table and short self-check prompts about when not to escalate beyond alert or review-candidate framing. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.06`
