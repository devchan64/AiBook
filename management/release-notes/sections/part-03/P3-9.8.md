# Section Release Note

- Section ID: `P3-9.8`
- Source File: `docs/parts/part-03/chapter-09/section-08.md`

### v2026.07.10

- 변경 이유: 점수와 정책을 구분하는 마지막 설명이 핵심 판단, 일반화 문장, 결론으로 나뉘어 짧은 절 안에서 다시 반복됐다.
- 본문 반영: 후반 설명을 하나의 문단으로 묶어, 예측 1회가 `모델 출력 -> 판정 규칙 -> 실제 행동`의 결정 구조라는 점이 한 번에 읽히도록 정리했다.

- 번역 동기화 메모: English and Simplified Chinese translations now preserve the compact explanation that one prediction is a decision structure of model output, policy rule, and real action, not just a number. / reflected in `docs/en/parts/part-03/chapter-09/section-08.md`, `docs/zh/parts/part-03/chapter-09/section-08.md`
- 번역 반영 상태: 영문·중국어 간체 반영 완료
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.10`

### v2026.07.08

- 변경 이유: `상위 프레임`을 전면 삭제하면 현재 절의 일반화된 메타 전개까지 함께 약해질 수 있어, 기준점 자체는 남기되 표 템플릿 의존만 줄일 필요가 있었다.
- 본문 반영: 반복 `상위 프레임` 표는 유지하지 않고, 현재 절의 판단을 더 넓은 개념 축으로 다시 묶는 산문 문장을 말미에 보강했다.

- 변경 이유: `상위 프레임` 재진술 표가 다른 Section들과 같은 마감 템플릿으로 반복되어, 본문이 교안형으로 굳어 보였다.
- 본문 반영: 말미의 `상위 프레임` 표를 제거하고, 현재 설명을 표 재진술 없이 직접 마무리하도록 정리했다.

- 변경 이유: 핵심 판단을 백틱 독립 문장으로 따로 세워 본문 흐름보다 강조 문구가 먼저 보였다.
- 본문 반영: 예측값, 점수, 정책 구분 결론을 일반 문단 안으로 흡수해 현재 설명 안에서 자연스럽게 이어지도록 정리했다.
- 변경 이유: `일반화된 상위 프레임으로 다시 보면` 제목이 Part 3 전반에 반복되어, 현재 절 설명도 같은 템플릿형 마감으로 굳어 보였다.
- 본문 반영: 반복 제목을 제거하고, 상위 프레임 표와 정리 문장을 현재 설명 흐름 안에 직접 이어 붙였다.

- 변경 이유: 예측 1회의 의미를 설명하는 절에 메타 템플릿이 남아 있었고, 점수와 정책의 분리를 일반화된 `score -> decision rule -> action` 프레임과 외부 근거로 다시 받쳐 줄 필요가 있었다.
- 본문 반영: 메타 문구를 직접 설명형으로 정리하고, `모델 점수 -> 판정 규칙 -> 실제 행동` 프레임과 출처를 추가해 숫자 하나가 아니라 결정 구조 전체를 읽어야 한다는 점을 더 분명히 했다.

- 번역 동기화 메모: preserve the direct prose, the score/decision-rule/action frame, and the source-backed distinction between model score, policy, and action. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07

- 변경 이유: Chapter 10의 `실행 단위`, `점수와 정책 구분`, `점수 해석`을 Chapter 9의 문제 정의 구간으로 흡수할 필요가 있었다.
- 본문 반영: 새 Section `P3-9.8 예측 1회는 실제로 무엇을 결정하며 점수와 정책은 왜 다른가`를 추가했다. 예측 단위, 모델 출력, 정책 규칙, 실제 행동을 분리하는 구조를 정리했다.

- 번역 동기화 메모: preserve the separation between prediction unit, model output, policy rule, and action when translated. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/book/table-of-contents.md`, `mkdocs.yml`
- 원문 기준 버전: `v2026.07.07`
