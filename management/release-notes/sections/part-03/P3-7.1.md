# Section Release Note

- Section ID: `P3-7.1`
- 대응 본문 경로:
  - `docs/parts/part-03/chapter-07/section-01.md`
  - `docs/parts/part-03/chapter-07/section-01.en.md`
  - `docs/parts/part-03/chapter-07/section-01.zh.md`

### v2026.07.31
- 변경 이유: Part 3 전체 보강 라운드에서 중심 학습 산출물을 독자가 직접 확인할 수 있도록 Section 말미의 자기점검 장치를 보강했다.
- 본문 반영: 한국어 원문에 `체크리스트` 절을 추가하고, Section 질문, 중심 학습 산출물, 데이터 구조 판단 항목을 행동형 확인 문장으로 연결했다. 같은 작업에서 원문 `Version`을 `v2026.07.31`로 갱신했다.
- 번역 동기화 메모: 영어판과 중국어 간체판은 이번 한국어 원문 체크리스트 보강 내용을 기준으로 추후 동기화가 필요하다.
- 번역 반영 상태: 한국어 원문 반영 완료, 영어판과 중국어 간체판 미반영.
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.31`
- 체크리스트 보강: 한국어 원문 말미에 `체크리스트` 절을 추가해 Section 질문, 중심 학습 산출물, 데이터 구조 판단 항목을 독자가 직접 확인하도록 했다.
- 체크리스트 번역 동기화 메모: 이번 체크리스트 보강은 한국어 원문 기준으로 먼저 반영했으며, 영어판과 중국어 간체판은 후속 동기화가 필요하다.
### v2026.07.25
- 본문 반영: 한국어·영어·중국어 본문의 `Version`을 `v2026.07.25`로 맞추고, 기준선(baseline), 기준 모델(baseline model), 기준선 구간(baseline window) 개념사전 링크를 추가했다.
- 개념사전 정합성: `baseline-window` 항목에 `glossary-baseline-window` 앵커를 추가하고, `baseline-model`과 `baseline-window`의 등장 Section에 `P3-7.1`을 반영했다.
- 보조 인덱스 반영: 한국어·영어·중국어 개념사전 보조 인덱스에 `baseline`, `baseline model`, `baseline window` 행을 추가해 본문 링크 대상과 검색 기준을 맞췄다.
- 번역 반영 상태: 영어판과 중국어 간체판 반영 완료.
- 코드 예제: 이 Section은 Python 예제를 포함하지 않으므로 코드 변경 없음.

### v2026.07.20
- 구조 정리: 한국어판, 영어판, 중국어 간체판에서 진행 메타 성격의 목적·읽는 순서·범위 제목을 데이터 모델링의 역할, 문제 구조 흐름, 닫을 경계가 드러나는 제목으로 정리했다.
- 번역 반영 상태: 영어판과 중국어 간체판 반영 완료.
- 원문 기준 버전: `v2026.07.20`
- 변경 이유: 본문에 사용한 외부 정의 출처의 접근 가능성과 주장 연결성을 다시 확인하고, 다국어 본문의 확인일과 Section 메타데이터를 현재 검토일 기준으로 맞출 필요가 있었다.
- 본문 반영: 한국어·영어·중국어 본문의 `Version`을 `v2026.07.20`으로 맞추고, BLS `Base period`, NCI `baseline` 출처 확인일을 `2026-07-20`으로 갱신했다.
- 저작권 검토: 비교표의 값과 Mermaid 흐름도는 Section 설명을 위해 만든 자체 예시이며 외부 표, 도표, 이미지, 문제를 재사용하지 않았다. 외부 자료는 정의 근거로만 짧게 재서술했다.
- 출처 확인: U.S. Bureau of Labor Statistics `Base period`, National Cancer Institute `baseline` 정의가 현재 구간과 기준선 구간을 함께 두어 변화를 읽는다는 본문 주장과 연결됨을 확인했다.
- 번역 동기화 메모: English and Simplified Chinese translations remain content-aligned with the Korean source; only version and source access dates were refreshed. / reflected in `docs/parts/part-03/chapter-07/section-01.en.md`, `docs/parts/part-03/chapter-07/section-01.zh.md`
- 관련 자산: `docs/assets/part-03/chapter-07/p3-7-1-mermaid-01-ko.mmd`, `docs/assets/part-03/chapter-07/p3-7-1-mermaid-01-en.mmd`, `docs/assets/part-03/chapter-07/p3-7-1-mermaid-01-zh.mmd`
- 원문 기준 버전: `v2026.07.20`
- 번역 반영 상태: 영어/중국어 반영 완료

### v2026.07.11
- 본문 반영: 1개의 Mermaid 도식을 `docs/assets/part-03/chapter-07/` 아래 `Section ID + mermaid 순번 + 언어 약어` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 자산 정리: 중국어 간체 본문에서 영문 라벨과 동일한 도식은 별도 `-zh.mmd` 복제본 대신 공용 `-en.mmd` 자산을 직접 참조하도록 바꿔 Part 3 자산 트리의 중복을 줄였다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams, while keeping the same flow structure across language files.

### v2026.07.10
- 본문 반영: `최근 구간 -> 비교 표 <- 기준선 구간 -> 차이 열 -> 사람 검토 문장` 흐름을 한국어 중심 라벨로 다시 정리했다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어 간체판은 각 언어판 흐름에 맞춰 별도 관리할 수 있다.
- 본문 반영: 연결 표를 제거하고, 특징 표가 어떤 평소 구간과 만나야 비교표가 되는지를 도입 문장 안으로 흡수했다. 말미도 기준선 용어 소개보다 `참조 구간을 함께 두는 문제`라는 현재 절 결론으로 더 직접 묶었다. `Version`을 `v2026.07.10`으로 갱신했다.

### v2026.07.08
- 본문 반영: `이전 Chapter에서 남긴 것 / 이번 Chapter에서 추가로 정하는 것 / 다음 Chapter로 넘길 구조` 표를 추가해, 특징 표가 참조 구간을 만나 비교표로 바뀌는 현재 장의 역할을 더 직접적으로 읽히게 정리했다.
- 본문 반영: Python 예제를 유지하지 않고 제거했으며, 본문에 남아 있던 `이 예제는` 문장도 `이 비교표는`으로 바꿔 현재 설명 단위가 표와 문장 해설이라는 점이 자연스럽게 이어지도록 정리했다.
- 번역 동기화 메모: preserve the comparison-reference framing, the explicit output title, and the source-backed explanation that a baseline window is a reference window rather than an extra numeric field. / pending

### v2026.07.07
- 본문 반영: 기준선을 `현재 구조가 어떤 평소 구조와 만나야 변화가 보이는지 정하는 일`로 바로 설명하고, 비교 구조를 먼저 설계해야 한다는 문장으로 정리했다.
- 본문 반영: 기준선 비교 관점을 다시 떠올려야 하는 상황을 `언제 이 관점을 먼저 떠올려야 하는가`로 추가하고, 기존 `짧은 점검`은 유지했다.
- 번역 동기화 메모: when translated, keep the baseline-comparison recall prompts separate from the short self-check bullets. / pending

### v2026.07.06
- 본문 반영: 최근 평균, 변동성, 공정 유형 비교에 각각 왜 기준선이 필요한지 정리한 보강 표를 추가하고, 절 끝에는 기준선 설계 기준을 확인하는 짧은 질문을 넣었다.
- 본문 반영: 3.13의 비교 구조 큰 축과 개념사전 연결을 기준 위치 설명으로 바꾸어, 뒤 절들이 참조하는 대표 설명 위치가 더 직접적으로 읽히게 다듬었다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending

### v2026.07.05
- 본문 반영: Section 메타데이터 추가. / `baseline model`을 `기준 모델(baseline model)`로 바꿔 한국어 설명을 먼저 읽히게 정리했다. / 기준선과 기준 모델의 차이를 나누는 비교 표, 비교표 예제 뒤의 3단계 확인 질문을 추가했다.
- 번역 동기화 메모: 메타데이터 추가와 비교 표/확인 질문 보강이 포함되어 번역 시 용어 경계와 읽기 순서를 유지해야 한다. / pending
