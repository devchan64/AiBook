# Section Release Note

- Section ID: `P3-1.2`
- Source File: `docs/parts/part-03/chapter-01/section-02.md`

### v2026.07.11
- 본문 반영: 1개의 Mermaid 도식을 `docs/assets/part-03/chapter-01/` 아래 `Section ID + mermaid 순번 + 언어 약어` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 자산 정리: 중국어 간체 본문에서 영문 라벨과 동일한 도식은 별도 `-zh.mmd` 복제본 대신 공용 `-en.mmd` 자산을 직접 참조하도록 바꿔 Part 3 자산 트리의 중복을 줄였다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams, while keeping the same flow structure across language files.

### v2026.07.10
- 본문 반영: `질문 -> 샘플 단위 -> 요약 표 -> 특징과 기준선 -> 출력 구조 -> 보수적 해석` 흐름도를 한국어 중심 라벨로 정리했다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어 간체판은 각 언어판 흐름에 맞춰 별도 관리할 수 있다.
- 본문 반영: 도입과 마무리를 각각 한 흐름으로 묶어, `질문 -> 샘플 -> 표 -> 특징과 기준선 -> 출력 구조 -> 해석 경계`의 순서 기준이 더 직접적으로 읽히도록 정리했다.

### v2026.07.08
- 본문 반영: Google ML Glossary, W3C PROV, Fayyad의 KDD 개관 글을 `출처와 참고 자료`에 추가해, 질문-샘플-표-특징-출력 구조 순서를 example, derivation, broader discovery workflow와 연결해 보강했다.
- 본문 반영: 뒤 장 예고 bullet을 제거하고, `질문 -> 샘플 -> 표 -> 특징과 기준선 -> 출력 구조 -> 해석 경계`의 맞물림이 왜 먼저 필요한지 현재 절의 순서 기준으로 직접 마무리했다.
- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending

### v2026.07.07
- 본문 반영: 도입, 잘못된 시작 비교표, 핵심 경고 문장, 점검 질문에서 `모델 이름/유형` 표현을 줄이고 `예측 문제`, `분류 문제`, `이상 징후 판별 문제`처럼 더 일반화된 학습 문제 틀 언어로 바꿨다.
- 본문 반영: 상위 프레임 문장은 유지하되, 여섯 항목을 `모델을 붙이기 전에 먼저 닫아야 할 판단`으로 다시 세웠다. 방어적인 `표준 목록` 문장은 걷어내고, 여섯 항목의 연결을 보여 주는 Mermaid 도식을 다시 넣었으며, 마무리와 재호출 문장도 `이 설명 순서`보다 `여섯 항목` 자체를 다시 떠올리게 고쳤다.
- 번역 동기화 메모: when translated, keep the modeling-sequence recall prompts separate from the short checklist items. / pending

### v2026.07.06
- 본문 반영: 동작 안정성 예시를 6단계 표에 다시 매핑해 단계별 결정 내용을 구체화하고, 절 끝에는 순서 감각을 확인하는 짧은 점검 질문을 추가했다.
- 본문 반영: 3.2가 Part 3 전체를 읽는 지도라는 점과 문제 설계 관점을 현재형 설명 문장으로 바꾸고, 마무리 핵심 문장도 직접 진술형으로 다듬었다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending
