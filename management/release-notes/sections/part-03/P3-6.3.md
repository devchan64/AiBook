# Section Release Note

- Section ID: `P3-6.3`
- Source File: `docs/parts/part-03/chapter-06/section-03.md`

### v2026.07.10
- 본문 반영: 도입 첫 문단을 더 직접적으로 묶고, 중간 표현 절에도 같은 경계를 다시 붙여 `입력 재구성`과 `표현 학습`의 차이가 더 빨리 읽히도록 정리했다. `Version`도 `v2026.07.10`으로 갱신했다.
- 본문 반영: 마지막 세 문장을 하나의 결론 흐름으로 다시 묶어 `입력 명세와 표현 학습의 경계`가 Section 끝에서도 끊기지 않도록 정리했다.
- 번역 동기화 메모: English and Simplified Chinese translations now reflect the strengthened boundary between human-built input specification and model-learned representation, including the tighter opening and ending conclusion flow. / reflected in en, zh

### v2026.07.08
- 본문 반영: 제목을 `사람이 만든 특징과 모델이 학습하는 표현은 어떻게 구분되는가`로 바꾸고, 도입도 입력 구조를 정하는 일과 그 입력 안에서 표현을 학습하는 일을 구분하는 현재 질문으로 다시 열었다.
- 본문 반영: 해당 표를 `지금 더 잘 남는 것` 중심으로 다시 써서, 요약 벡터·구간별 특징·토큰 시퀀스가 각각 어떤 정보 구조를 더 직접 보존하는지 비교하게 바꾸었다.
- 번역 동기화 메모: preserve the input-specification versus representation-learning frame and the source-backed distinction between pre-model input design and later learned representations. / pending

### v2026.07.07
- 본문 반영: 새 Section `P3-6.3 사람이 만든 특징과 뒤에서 학습될 표현은 어떻게 이어지는가`를 추가했다. 사람이 만든 특징, 중간 표현, 뒤에서 학습될 표현의 역할 차이를 표로 나누고, Part 3의 입력 정의 책임과 Part 4·5의 학습 책임을 구분했다. 또한 `딥러닝이면 특징 설계가 필요 없다`, `특징을 만들었으니 표현 학습은 불필요하다` 같은 오해를 정리했다.
- 번역 동기화 메모: translation should preserve the distinction between human-designed features/intermediate representations and later learned representations, especially the boundary between Part 3 input design and Part 4/5 learning. / pending
