# Section Release Note

- Section ID: `P3-4.5`
- Source File: `docs/parts/part-03/chapter-04/section-05.md`

### v2026.07.11
- 본문 반영: 1개의 Mermaid 도식을 `docs/assets/part-03/chapter-04/` 아래 `Section ID + mermaid 순번 + 언어 약어` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 자산 정리: 중국어 간체 본문에서 영문 라벨과 동일한 도식은 별도 `-zh.mmd` 복제본 대신 공용 `-en.mmd` 자산을 직접 참조하도록 바꿔 Part 3 자산 트리의 중복을 줄였다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams, while keeping the same flow structure across language files.

### v2026.07.10
- 본문 반영: 근무조, 장비, 고부하 사례, 정비 직후 사례의 치우침을 보여 주는 도식을 한국어 중심 라벨로 다시 정리했다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어 간체판은 각 언어판 흐름에 맞춰 별도 관리할 수 있다.
- 본문 반영: 예제 앞에 `문제 상황`, `입력(input)`, `기대 출력(output)`, `확인할 개념`을 추가하고 Section 버전을 갱신했다.

### v2026.07.08
- 본문 반영: Google ML Glossary, W3C PROV, NIST control chart 설명을 `출처와 참고 자료`에 추가해, example 집합, 운영 맥락 추적, 같은 본질 조건 아래의 표본 비교라는 세 축으로 대표성 점검을 보강했다.
- 본문 반영: 첫 문단과 첫 소제목 아래 문장을 전체 운영 장면을 고르게 설명하지 못할 수 있다는 현재 대표성 설명으로 바꾸고, 샘플 묶음이 어떤 운영 범위를 실제로 덮는지 다시 묻는 문제로 직접 정리했다.
- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending

### v2026.07.07
- 본문 반영: `maintenance_phase`를 포함한 원본 샘플 표와, 각 범위에서 `most_seen`과 `least_seen`을 함께 보여 주는 coverage summary 표를 출력하도록 예제를 보강했다. 해설도 `샘플 수`보다 `대표 범위와 공백`을 읽는 쪽으로 더 직접 연결했다.
- 본문 반영: 작은 예시를 Mermaid 도식으로 교체했다. day shift, machine M1, high-load scarcity, after-maintenance scarcity가 함께 `coverage is uneven`으로 모이는 구조를 시각적으로 보강했다.
- 번역 동기화 메모: preserve that the new section treats representativeness as a pre-evaluation and pre-generalization concern in Part 3, not only as a later statistical topic. / pending
