# Section Release Note

- Section ID: `P3-5.2`
- Source File: `docs/parts/part-03/chapter-05/section-02.md`

### v2026.07.10

- 변경 이유: 평균은 같지만 패턴은 다른 경우를 보여 주는 예제가 `예상 출력`은 제공했지만, Python 예제 프리앰블이 없어 현재 절의 관찰 포인트가 코드 앞에서 즉시 드러나지 않았다.
- 본문 반영: 예제 앞에 `문제 상황`, `입력(input)`, `기대 출력(output)`, `확인할 개념`을 추가하고 Section 버전을 갱신했다.
- 번역 반영 상태: 향후 번역 반영 필요

### v2026.07.08-2

- 변경 이유: 평균 밖 패턴을 남겨야 한다는 일반 원리를 길게 설명하면서도 `출처와 참고 자료`가 없어, 패턴 보존과 파생 특징 설계의 근거 연결이 비어 있었다.
- 본문 반영: NIST 관리도 설명, Google `feature engineering`, W3C PROV를 `출처와 참고 자료`에 추가해, 평균 외 구간 패턴과 파생 요약값을 함께 남겨야 한다는 판단을 각각 시간 패턴 해석, 입력 표현 설계, 파생 과정 추적 관점으로 보강했다.

- 변경 이유: 말미의 `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`, 본문 중간의 `이 절은 ... 문제로 다시 볼 수 있습니다` 같은 메타 템플릿이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 반복적인 메타 템플릿을 걷어내고, 중심 개념 설명과 사례 흐름만 남기도록 Section 말미와 중간 메타 문장을 정리했다.
- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07-5

- 변경 이유: 기존 예제도 평균과 패턴 차이를 설명했지만, 출력 제목이 짧아 `평균만으로는 부족하다`는 경고와 `구간 차이가 남는다`는 확인 포인트가 코드 결과에서 조금 약하게 읽혔다.
- 본문 반영: Python 예제와 예상 출력의 단계 이름을 `same overall mean is not enough`, `segment-level differences remain`, `one-line pattern interpretation`으로 조정해 절의 핵심 주장과 출력 단계가 더 직접적으로 연결되게 정리했다.
- 번역 동기화 메모: preserve the stronger stage labels so the example reads as a pattern-vs-average argument, not just a neutral table dump. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-4

- 변경 이유: 기존 Python 예제는 같은 평균을 보여 주는 데는 충분했지만, `그래서 어떤 패턴 차이가 남았는가`를 코드 결과가 직접 계산해 보여 주지 않아 절의 핵심 주장보다 해설 문단 의존이 컸다.
- 본문 반영: 예제를 `same overall mean -> segment pattern columns -> one-line interpretation` 3단계 출력으로 바꾸고, `mid_minus_early`, `late_minus_mid`, `pattern_note`를 추가해 평균은 같지만 구조는 다르다는 점이 코드 결과에서 바로 드러나게 했다.
- 번역 동기화 메모: preserve the strengthened example where identical overall means are followed by explicit segment-difference columns and a short pattern interpretation. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-3

- 변경 이유: 커리큘럼 계획에서 아직 본문에 직접 흡수되지 않은 `이상치와 분포 치우침` 항목을 요약 표 해석 절 안에서 실제로 반영할 필요가 있었다.
- 본문 반영: 평균이 일부 극단값과 비대칭 분포를 가릴 수 있다는 설명을 추가하고, 평균만으로는 잘 안 보이는 분포 구조와 함께 남겨야 할 값(최소값, 최대값, 분위수, 중간값, 이상치 메모)을 표로 정리했다.
- 번역 동기화 메모: preserve the new distinction that averages can hide outliers and skewed distributions, plus the checklist of extra summary values to retain. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-2

- 변경 이유: 평균 밖 패턴 절 도입에 남아 있던 `이 절의 역할은` 문구를 직접 설명형 기준으로 맞출 필요가 있었다.
- 본문 반영: 앞 절의 요약 표가 무엇을 더 남겨야 하는지 설명하는 도입을 `여기서는 ... 다시 설명하지 않습니다`로 바꾸어, 표 변환 재설명과 패턴 보존 논점을 더 분명히 갈랐다.
- 번역 동기화 메모: when translated, keep the opening focused on retained pattern information rather than role-framing language. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: 평균 밖 패턴 읽기 절도 마지막 점검 구간을 패턴 재호출과 확인 항목으로 분리할 필요가 있었다.
- 본문 반영: 같은 평균, 다른 패턴을 다시 떠올려야 하는 상황을 `언제 이 관점을 먼저 떠올려야 하는가`로 추가하고, 기존 `짧은 점검`은 유지했다.
- 번역 동기화 메모: when translated, keep the same-mean-different-pattern recall prompts separate from the short checklist items. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.06-14

- 변경 이유: 같은 평균과 다른 패턴의 대비는 설명하고 있었지만, 평균 밖에 무엇을 더 남겨야 하는지와 절 끝 점검 장치가 더 필요했다.
- 본문 반영: 구간 평균, 구간 기울기, 최대값 시점, 하강 시작 시점이 왜 필요한지 정리한 보강 표를 추가하고, 절 끝에는 평균 밖 패턴 읽기를 확인하는 짧은 질문을 넣었다.
- 번역 동기화 메모: preserve the beyond-average feature checklist and the short check questions that reinforce pattern-aware summary design. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-14`

### v2026.07.05

- 변경 이유: 메타데이터 누락 보완. / `같은 평균, 다른 패턴`을 초심자가 해석 순서로 따라가도록 보강할 필요가 있었음.
- 본문 반영: Section 메타데이터 추가. / 평균만 보면 놓치는 정보와 함께 남겨야 할 값을 정리한 표, 예제 출력 뒤의 3단계 해석 순서와 한 문장 요약 예시를 추가했다.
- 번역 동기화 메모: 메타데이터 추가와 해석 순서 보강이 포함되어 번역 시 비교 표와 한 문장 요약 예시를 함께 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.05`

### v2026.07.06-2

- 변경 이유: 평균과 패턴 차이를 설명하는 절에서도 `초심자 기준에서는` 같은 메타 진행문을 줄이고, 평균이 가리는 정보가 본문 표와 해석 순서로 직접 드러나게 정리.
- 본문 반영: `평균만 보면 놓치는 것` 비교 표의 도입 문장을 메타 안내에서 요약 표 설계의 필요로 바꾸고, 예제 읽기 순서도 평균과 패턴 차이를 확인하는 직접 설명으로 다듬었다. 표와 예제 구조는 유지했다.
- 번역 동기화 메모: reassurance tone이 줄고 summary-table design emphasis가 강해졌으므로, 번역 시에도 `what average hides`와 reading order를 직접 연결해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-2`

### v2026.07.06-3

- 변경 이유: 같은 평균/다른 패턴 절의 도입과 다음 장 연결에 남아 있던 `해 봅시다`, `다루겠습니다` 같은 유도형 표현을 직접 설명형 문장으로 정리할 필요가 있었다.
- 본문 반영: 평균 유량 2.4 사례 도입을 `하겠습니다`로 바꾸고, 다음 장 연결 문장을 `더 본격적으로 다룹니다`로 조정했다. 평균과 패턴의 대비, 비교 표, 예제 출력 해석 순서는 유지했다.
- 번역 동기화 메모: 번역본이 생기면 opening example setup과 next-chapter handoff를 guide-like phrasing보다 direct explanatory prose로 맞춘다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-10

- 변경 이유: Part 3 앞단에 데이터 모델링 진입 챕터를 추가하면서 기존 Section의 번호 체계와 파일 경로를 한 칸씩 뒤로 재배치할 필요가 있었다.
- 본문 반영: Section 제목 번호, Section ID, 본문 경로, 목차 대응 위치를 새 Part 3 번호 체계에 맞춰 갱신했다. 본문 설명 구조와 사례 흐름은 유지했다.
- 번역 동기화 메모: translation should follow the renumbered Part 3 section IDs and updated source paths while preserving the same content structure. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-11

- 변경 이유: 요약 표의 역할을 더 분명히 하려면 이 Section도 `같은 평균/다른 패턴` 사례 설명보다 `요약 표는 평균 밖의 패턴을 어떻게 남기는가`라는 질문 중심으로 읽히게 조정할 필요가 있었다.
- 본문 반영: Section 제목을 `요약 표는 평균 밖의 패턴을 어떻게 남기는가`로 바꾸고, 도입 문장에서 평균 밖의 패턴 차이를 남기는 설계 문제라는 점을 더 직접적으로 밝혔다. 표와 예제, 해석 순서는 유지했다.
- 번역 동기화 메모: translation should preserve the reframing toward keeping pattern information beyond averages in the summary table design. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-12

- 변경 이유: 3.10이 원시 로그에서 요약 표로 바꾸는 절차 자체를 다시 설명하는 절처럼 퍼지지 않도록, 이 절이 앞 절에서 만든 요약 표가 어떤 패턴 정보를 남겨야 하는지에 집중한다는 점을 더 분명히 할 필요가 있었다.
- 본문 반영: 이 절의 역할은 요약 표 변환 절차의 재설명이 아니라, 앞 절에서 만든 요약 표가 평균만이 아니라 패턴 차이까지 남겨야 한다는 점을 따라가는 데 있다는 문장을 추가했다.
- 번역 동기화 메모: translation should preserve 3.10 as a follow-up focused on what a summary table must retain beyond averages, not as another full pass on raw-to-summary transformation. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-13

- 변경 이유: Part 3 Section 제목 인덱스를 `Section ID`와 같은 `P3-5.2` 형식으로 맞추는 규칙을 적용할 필요가 있었다.
- 본문 반영: 제목 앞 인덱스를 `3.10`에서 `P3-5.2`로 바꿔 본문 제목과 `Section ID` 표기가 같은 체계를 가리키도록 맞췄다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.06`
