# Section Release Note

- Section ID: `P3-3.1`
- Source File: `docs/parts/part-03/chapter-03/section-01.md`

### v2026.07.08

- 변경 이유: Chapter 2의 데이터셋 후보 관점이 Chapter 3에서 어떻게 `학습 문제 승격 보류` 판단으로 좁혀지는지 도입에서 한 줄로 회수되지 않아, 현재 장의 경고가 새 이야기처럼 읽힐 여지가 있었다.
- 본문 반영: `이전 Chapter에서 남긴 것 / 이번 Chapter에서 추가로 정하는 것 / 다음 Chapter로 넘길 구조` 표를 추가해, 데이터셋 후보 관점에서 샘플 단위와 표 구조를 실제로 정하는 단계로 넘어가기 전의 현재 장 역할을 더 직접적으로 보이도록 정리했다.

- 변경 이유: 도입부가 `바로 다음 절에서 ...`라는 예고 문장을 먼저 세워, 현재 절의 경고 설명보다 다음 절 handoff가 먼저 보였다.
- 본문 반영: 해당 예고 문장을 제거하고, 아직 학습 문제의 틀을 고르는 단계가 아니라는 현재 절의 경고만 직접 남기도록 도입을 정리했다.

- 변경 이유: `상위 프레임` 재진술 표가 다른 Section들과 같은 마감 템플릿으로 반복되어, 본문이 교안형으로 굳어 보였다.
- 본문 반영: 말미의 `상위 프레임` 표를 제거하고, 현재 설명을 표 재진술 없이 직접 마무리하도록 정리했다.

- 변경 이유: `일반화된 상위 프레임으로 다시 보면` 제목이 Part 3 전반에 반복되어, 현재 절 설명도 같은 템플릿형 마감으로 굳어 보였다.
- 본문 반영: 반복 제목을 제거하고, 상위 프레임 표와 정리 문장을 현재 설명 흐름 안에 직접 이어 붙였다.

- 변경 이유: 반복 메타 템플릿이 설명보다 먼저 보이는 문제를 줄이는 작업은 있었지만, 상위 프레임 뒤 결론 문장이 여전히 `이 프레임을 잡아 두면`이라는 고정형 표현을 유지하고 있었다.
- 본문 반영: `기록 구조 확인 -> 샘플/라벨 공백 확인 -> 학습 문제 승격 보류` 상위 프레임은 유지하되, 뒤의 정리 문장을 직접 진술형으로 바꿔 메타 템플릿 인상을 줄였다. 기존의 `출처와 참고 자료` 보강은 유지했다.
- 번역 동기화 메모: preserve the problem-escalation frame, keep the section free of repeated meta-study blocks, and tie the source support to sample/label readiness before promotion to a learning problem. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07-7

- 변경 이유: 기존 Python 예제는 `row meaning`, `sample meaning`을 말로만 출력해, 이 절의 핵심 주장인 `원천데이터를 너무 빨리 학습 문제로 읽으면 빈 질문이 남는다`는 점을 충분히 증명하지 못했다.
- 본문 반영: 원시 로그를 곧바로 분류 문제처럼 읽는 너무 빠른 해석과, 먼저 샘플 1건·비교 표·라벨 후보를 정하는 읽기를 단계별로 대비하는 예제로 교체했다.
- 번역 동기화 메모: preserve the contrast between the too-early learning-problem read and the later event-level table that appears only after sample and label questions are clarified. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-6

- 변경 이유: 제목을 바꾼 뒤에도 본문에는 `모델 이름`, `분류기`, `회귀`, `딥러닝` 같은 표현이 많이 남아 있어, 독자의 주의가 다시 이름이나 알고리즘 쪽으로 쏠리며 이 절의 핵심인 `원천데이터를 너무 빨리 학습 문제로 읽는 습관`이 흐려질 수 있었다.
- 본문 반영: 도입, 비교 표, 예시 해설, 점검 질문, 재호출 문장에서 `모델 이름/종류` 표현을 줄이고 `분류 문제`, `예측 문제`, `시계열 학습 문제`, `학습 문제의 틀` 같은 더 일반화된 표현으로 정리했다.
- 번역 동기화 메모: avoid foregrounding model names or algorithm labels; frame the warning around choosing the learning-problem frame too early. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-5

- 변경 이유: `원천데이터 앞에서 왜 문제 구조부터 정해야 하는가`는 앞선 `P3-2.3`의 표 읽기/선확인 축과 겹쳐 보여, 이 절이 새로 맡는 경고가 무엇인지 충분히 분리되지 않았다.
- 본문 반영: Section 제목을 `원천데이터를 왜 곧바로 학습 문제로 읽으면 안 되는가`로 바꾸고, 배포 목차와 Part 3 계획 문서의 대응 표기도 같은 제목으로 맞췄다.
- 번역 동기화 메모: retitle this section around the mistake of reading raw records as an already-formed learning problem, to separate it from the earlier table-reading checklist section. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`, `management/authoring/2026-07-07-part-03-curriculum-recomposition-plan.md`
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-4

- 변경 이유: `문제 구조는 왜 모델 이름보다 먼저 정해야 하는가`도 여전히 `모델 이름`을 제목 전면에 남겨, 이 절의 의도보다 `이름 자체`에 주의를 모으게 만들었다.
- 본문 반영: Section 제목을 `원천데이터 앞에서 왜 문제 구조부터 정해야 하는가`로 다시 조정하고, 배포 목차와 Part 3 계획 문서의 대응 표기도 함께 갱신했다.
- 번역 동기화 메모: retitle around problem-structure-first reading of raw data, avoiding a title that still foregrounds model names. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`, `management/authoring/2026-07-07-part-03-curriculum-recomposition-plan.md`
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-3

- 변경 이유: 현재 본문은 `모델 이름이 먼저 뜨는 반사적 습관` 자체보다 `문제 구조를 먼저 정해야 한다`는 기준을 중심 주장으로 삼고 있어, 기존 제목이 핵심 초점을 충분히 정확하게 드러내지 못했다.
- 본문 반영: Section 제목을 `문제 구조는 왜 모델 이름보다 먼저 정해야 하는가`로 바꾸고, 배포 목차와 Part 3 계획 문서의 대응 표기도 같은 제목으로 정리했다.
- 번역 동기화 메모: retitle this section around problem-structure-first framing rather than the weaker model-name-first warning. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`, `management/authoring/2026-07-07-part-03-curriculum-recomposition-plan.md`
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-2

- 변경 이유: 모델 우선 사고 경고 절도 역할 소개 문장을 더 직접 설명형으로 맞출 필요가 있었다.
- 본문 반영: `이 절의 역할은 ...`을 `여기서는 ... 고정합니다`로 다듬어 문제 구조 우선 원칙이 더 곧바로 읽히게 정리했다.
- 번역 동기화 메모: keep the model-first warning focus statement direct and aligned with the shared tone. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: 모델 이름 경계 절도 마지막 자기점검 구간을 문제 구조 재호출과 확인 항목으로 분리해 Part 3 기준선에 맞출 필요가 있었다.
- 본문 반영: 모델 이름보다 문제 구조를 먼저 떠올려야 하는 상황을 `언제 이 관점을 먼저 떠올려야 하는가`로 추가하고, 기존 `짧은 점검`은 유지했다.
- 번역 동기화 메모: when translated, keep the model-name warning recall prompts separate from the short self-check bullets. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.06-16

- 변경 이유: 모델 우선 사고를 경고하는 논지는 있었지만, 실제로 어떤 질문이 비어 있는지와 절 끝 점검 장치가 초심자 기준에서 더 필요했다.
- 본문 반영: `이상 탐지 모델`, `분류 모델`, `시계열 딥러닝` 같은 말 앞에 남아 있는 빈 질문을 보여 주는 보강 표를 추가하고, 절 끝에는 짧은 점검 질문을 넣었다.
- 번역 동기화 메모: preserve the “model name first, missing question second” contrast table and the short check questions for beginners. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-16`

### v2026.07.06-15

- 변경 이유: 모델 우선 사고 경고 절의 마무리 핵심 문장 도입에 남아 있던 `먼저 고정할 문장` 표현을 직접 설명형으로 정리할 필요가 있었다.
- 본문 반영: 3.5의 결론 문장을 `핵심 문장`으로 바꾸어, 경고 절의 핵심 요지가 안내형보다 직접 진술형으로 읽히게 다듬었다.
- 번역 동기화 메모: keep P3-3.1 declarative and preserve its warning conclusion as a direct core statement. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-15`

### v2026.07.05

- 변경 이유: 메타데이터 누락 보완. / 영어 기능어가 먼저 드러나는 설명 완화.
- 본문 반영: Section 메타데이터 추가. / `feature`, `baseline`, `target`을 한국어 설명 우선 표현으로 정리했다.
- 번역 동기화 메모: 본문 내용 변화 없이 메타데이터만 추가되어 번역본 영향은 없다. / 없음
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.05`

### v2026.07.06-2

- 변경 이유: 원천데이터와 모델 연상 절에서도 `초심자`를 직접 호출하는 메타 표현을 줄이고, 모델 이름이 먼저 튀어나오는 문제를 본문 논리 안에서 바로 드러나게 정리.
- 본문 반영: 동작 시계열 표를 처음 볼 때의 반응을 독자 호명 없이 일반 서술로 바꾸어, 기록 구조를 학습 구조로 착각하는 문제를 더 직접적으로 읽히게 했다. 질문 비교 표와 예제 구조는 유지했다.
- 번역 동기화 메모: reader-label phrasing이 줄고 model-first reflex explanation이 직접화되었으므로, 번역 시에도 same framing을 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-2`

### v2026.07.06-3

- 변경 이유: 도입 사례와 원인 설명 문장에 남아 있던 `떠올려 봅시다`, `떠오르기 쉽습니다`, `왜 이런 일이 자주 생길까요` 같은 유도형 표현을 직접 설명형으로 더 정리할 필요가 있었다.
- 본문 반영: 원천 시계열 상황 소개를 `보겠습니다`와 `먼저 나오기 쉽습니다`로 조정하고, 원인 설명 문장을 `이런 일이 자주 생기는 이유는 분명합니다`로 바꾸어 모델 우선 연상 문제를 더 직접적인 서술로 정리했다.
- 번역 동기화 메모: 번역본이 생기면 scenario setup과 cause explanation을 question-led guidance보다 direct explanatory prose로 맞춘다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-10

- 변경 이유: Part 3 앞단에 데이터 모델링 진입 챕터를 추가하면서 기존 Section의 번호 체계와 파일 경로를 한 칸씩 뒤로 재배치할 필요가 있었다.
- 본문 반영: Section 제목 번호, Section ID, 본문 경로, 목차 대응 위치를 새 Part 3 번호 체계에 맞춰 갱신했다. 본문 설명 구조와 사례 흐름은 유지했다.
- 번역 동기화 메모: translation should follow the renumbered Part 3 section IDs and updated source paths while preserving the same content structure. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-11

- 변경 이유: Chapter 3 앞단이 `원천데이터를 보고 곧바로 모델을 떠올리는 습관`을 더 직접적으로 비판하는 역할을 맡도록, 제목과 핵심 문장을 리팩터링 구조에 맞게 조정할 필요가 있었다.
- 본문 반영: Section 제목을 `왜 모델 이름이 먼저 떠오르면 안 되는가`로 조정하고, 원천 기록이 많아야 데이터셋 후보일 뿐이라는 점과 모델 우선 사고가 샘플 단위·표 구조 재작업으로 이어진다는 문장을 보강했다.
- 번역 동기화 메모: translation should preserve the stronger caution against model-first framing and the explicit mention that raw records may only be dataset candidates. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-12

- 변경 이유: 3.5와 3.6을 연속으로 읽을 때 3.5가 데이터셋 재설계 절까지 대신 설명하는 것처럼 퍼지지 않게, 이 절이 먼저 맡는 역할을 더 분명히 적어 둘 필요가 있었다.
- 본문 반영: 3.5는 `모델 이름`보다 `문제 구조`가 먼저라는 점을 고정하는 경고 절이라는 문장을 추가했다. 데이터셋 재설계의 구체 절차는 다음 절에서 다루고, 이 절에서는 아직 모델 종류를 고르는 단계가 아니라는 점을 분명히 했다.
- 번역 동기화 메모: translation should preserve 3.5 as a warning section that delays model selection until the problem structure is defined, with concrete dataset redesign deferred to 3.6. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-13

- 변경 이유: Part 3 Section 제목 인덱스를 `중심 대상 + 질문` 형식으로 통일하는 기준과 비교했을 때, 3.5만 `왜`가 먼저 오는 의문사 선행형으로 남아 있어 목차 호흡이 어긋났다.
- 본문 반영: Section 제목을 `왜 모델 이름이 먼저 떠오르면 안 되는가`에서 `모델 이름은 왜 먼저 떠오르면 안 되는가`로 조정해, 다른 Part 3 제목들과 같은 `중심 대상 + 질문` 패턴으로 맞췄다.
- 번역 동기화 메모: translation should preserve the title change as a structural consistency fix so the section lead noun appears before the question phrase, matching the surrounding Part 3 title pattern. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-14

- 변경 이유: Part 3 Section 제목 인덱스를 `Section ID`와 같은 `P3-3.1` 형식으로 맞추는 규칙을 적용할 필요가 있었다.
- 본문 반영: 제목 앞 인덱스를 `3.5`에서 `P3-3.1`로 바꿔 본문 제목과 `Section ID` 표기가 같은 체계를 가리키도록 맞췄다.
- 번역 동기화 메모: translation should keep the title index identical to the Section ID rather than using a shortened numeric prefix. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.06`
