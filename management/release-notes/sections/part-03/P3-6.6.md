# Section Release Note

- Section ID: `P3-6.6`
- Source File: `docs/parts/part-03/chapter-06/section-06.md`

### v2026.07.08-2

- 변경 이유: 말미의 `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`, 본문 중간의 `이 절은 ... 문제로 다시 볼 수 있습니다` 같은 메타 템플릿이 설명보다 먼저 보이는 문제가 남아 있었다.
- 본문 반영: 반복적인 메타 템플릿을 걷어내고, 중심 개념 설명과 사례 흐름만 남기도록 Section 말미와 중간 메타 문장을 정리했다.
- 번역 동기화 메모: preserve the section without the repeated meta-study template blocks and keep the explanation centered on the concept and example flow itself. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.08

- 변경 이유: 기존 예제는 `same_definition_group` 문자열을 만드는 데서 멈춰, 실제로 어떤 행끼리만 같은 특징 정의로 묶이고 어떤 행은 비교 전부터 분리해야 하는지가 코드 결과에서 충분히 직접 드러나지 않았다.
- 본문 반영: 앞단 작은 예시는 `ops_definition`까지 포함한 도식 중심 예시로 바꾸고, 코드 예제는 A-D 네 행 전체를 다루도록 확장했다. 출력도 `정의 메모가 다른 행 목록 -> 실제 정의 그룹별 event 묶음` 2단계로 바꿔, A/B만 같은 정의 그룹으로 남고 C/D는 따로 분리된다는 점이 곧바로 보이게 정리했다.
- 번역 동기화 메모: preserve the diagram and the two-step example that separates same-name rows into actual feature-definition groups, including the added operations-definition field. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07-2

- 변경 이유: 특징 정의 일치 절이 Part 4·5 설명을 별도 하위 섹션으로 펼쳐, 현재 Section의 중심 질문보다 뒤 Part 예고가 과하게 앞섰다.
- 본문 반영: `Part 4와는 어떻게 이어지는가`, `Part 5와는 어떻게 이어지는가`를 걷어내고, 특징 정의 메모가 왜 뒤 분할·평가·시계열 입력 해석에서도 먼저 필요한지만 한 문단으로 압축했다.
- 번역 동기화 메모: compress later-Part implications into one short handoff paragraph and keep feature-definition consistency as the section core. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-3

- 변경 이유: 코드 예제가 남더라도 이 절에서는 `정의 그룹이 실제로 몇 개로 갈라지는가`가 먼저 보여야 하므로, 예제 목적을 설명에서 더 직접적으로 드러낼 필요가 있었다.
- 본문 반영: `same_definition_group` 출력이 왜 중요한지 설명을 보강했다. 같은 `feature_name` 아래에서도 단위, 버전, 규칙 조합이 다르면 여러 정의 그룹으로 갈라진다는 점이 예제 목적임을 분명히 적었다.
- 번역 동기화 메모: translation should preserve that the code example's purpose is to expose multiple feature-definition groups under one shared column name. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: Part 3은 특징의 역할, 단위, 비교 구조를 많이 보강했지만, 같은 열 이름 아래에서 센서 버전·단위·계산 규칙이 바뀌면 왜 더 이상 같은 특징이 아닐 수 있는지는 독립 절로 정리되지 않았다.
- 본문 반영: 특징 이름이 같아도 단위, 센서 버전, 구간 계산 규칙, 운영 정의가 바뀌면 정의가 달라질 수 있다는 Section을 추가했다. 이를 Chapter 7의 기준선 비교와 Part 4·5 인계 전제, 즉 같은 정의끼리만 비교하고 넘겨야 한다는 문제로 연결했다.
- 번역 동기화 메모: preserve that the new section treats feature-definition consistency as a pre-comparison and pre-handoff concern, not only as a later modeling-performance issue. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`
