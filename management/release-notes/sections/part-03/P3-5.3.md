# Section Release Note

- Section ID: `P3-5.3`
- Source File: `docs/parts/part-03/chapter-05/section-03.md`

### v2026.07.08

- 변경 이유: 뒤 Part 연결 블록과 메타 템플릿 정리만으로는 부족했고, 이 절을 더 일반적인 입력 구조 명세(input-structure specification) 프레임으로 다시 묶고 외부 근거와 연결하는 보강이 필요했다.
- 본문 반영: 독립적인 뒤 Part 연결 블록과 반복 메타 문장을 정리한 뒤 `샘플 경계 -> 길이/구간 규칙 -> 목표 구조`라는 상위 프레임과 `출처와 참고 자료`를 추가했다. Google ML Glossary의 `labeled example`, `label leakage`와 W3C PROV를 연결해 원시 시계열이 곧바로 학습 입력은 아니라는 일반 근거를 보강했다.
- 번역 동기화 메모: preserve the input-structure-specification frame, keep the section free of repeated meta-study blocks and oversized later-part handoff, and tie the source support to sample boundaries, input segmentation, and target structure. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07-2

- 변경 이유: `딥러닝 모델`, `모델 이름` 같은 표현이 남아 있어, 이 절의 핵심인 `입력 구조가 먼저`보다 구조 이름 자체에 주의가 쏠릴 여지가 있었다.
- 본문 반영: 도입과 재호출 문장을 `순차 학습 입력`, `구조 이름` 중심 표현으로 바꾸고, `그냥 모델에 넣는다`는 표현도 `그냥 학습 입력으로 넘긴다`로 조정했다.
- 번역 동기화 메모: avoid foregrounding model names; keep the focus on input-structure decisions before later sequence-learning choices. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07

- 변경 이유: 원시 로그와 요약 표의 차이는 설명되어 있었지만, 원시 시계열이 있어도 왜 바로 학습 입력이라고 말할 수 없는지는 한 Section으로 닫혀 있지 않아 뒤 Part의 딥러닝 입력 구조와 연결할 때 공백이 남아 있었다.
- 본문 반영: 새 Section `P3-5.3 원시 시계열이 있어도 왜 바로 학습 입력이라고 말할 수 없는가`를 추가했다. 원시 시계열과 학습 입력의 차이, 샘플 경계와 입력 길이 결정의 필요, 요약 표와 중간 표현이 여전히 필요한 이유를 표와 문장으로 정리했다.
- 번역 동기화 메모: translation should preserve the distinction between having raw time-series data and having a task-ready model input, especially the need to define sample boundaries and input windows before later sequence-model chapters. / pending
- 번역 반영 상태: not-started
- 관련 자산: `mkdocs.yml`, `docs/book/table-of-contents.md`
- 원문 기준 버전: `v2026.07.07`
