# Section Release Note

- Section ID: `P3-4.3`
- Source File: `docs/parts/part-03/chapter-04/section-03.md`

### v2026.07.08

- 변경 이유: 세 층위 비교 예제의 내용은 맞았지만, 소제목과 출력 제목이 아직 일반적이라 `행`, `샘플`, `구간`이 각각 무엇을 뜻하는지 코드 결과만으로는 덜 직접적으로 보였다.
- 본문 반영: 소제목을 `한눈에 비교하는 작은 코드 예시`로 바꾸고, 출력 단계 이름도 `time-step record`, `whole event`, `multiple samples regrouped`가 드러나도록 다시 붙였다.
- 번역 동기화 메모: preserve the explicit stage labels so the code walkthrough reads as a row-vs-sample-vs-window distinction, not just a count comparison. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07-4

- 변경 이유: 직전 보강본은 `window`를 원시 행 단계에 직접 두고 시작해, 이 절의 핵심인 `행 -> 샘플 -> 구간` 구분보다 구간이 행 속성처럼 읽힐 여지가 있었다. 또한 짧은 점검의 행 수가 본문 예제와 맞지 않았다.
- 본문 반영: Python 예제의 `raw`를 순수한 시점 행 표로 되돌리고, `window`는 `per_event`를 만든 뒤 샘플 표에 부여되도록 정리했다. 해설도 `window summary`가 샘플 여러 건을 다시 묶은 결과라는 점을 더 직접 설명하도록 다듬었고, 짧은 점검의 `6행`을 `9행`으로 바로잡았다.
- 번역 동기화 메모: preserve the clearer row-to-sample-to-window progression, where raw rows stay row-level and window grouping is introduced only after per-event samples are created. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-3

- 변경 이유: 기존 예시와 Python 예제는 행, 샘플, 구간의 개수 차이는 보여 줬지만, 같은 장면이 세 층위에서 각각 어떤 모양으로 보이는지와 질문이 바뀔 때 무엇을 읽게 되는지까지는 충분히 체감시키지 못했다.
- 본문 반영: 운영 질문을 `행`, `샘플`, `구간`에 다시 대응시키는 보강 문단을 추가했다. Python 예제에는 이벤트 C와 `window` 구분을 넣고, `one row example`, `one sample example`, `window summary` 출력을 추가해 각 층위의 대표 형태가 코드 결과에서 직접 드러나도록 바꿨다.
- 번역 동기화 메모: preserve the strengthened three-level example with explicit row, sample, and window outputs, plus the question-to-level bridge paragraph. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-2

- 변경 이유: 도입부가 세 층위의 개념 차이보다 `보강 위치`라는 절 역할을 먼저 말해 직접 설명 원칙과 어긋났다.
- 본문 반영: 행, 샘플, 구간을 구분해야 하는 이유를 특징·기준선·검토 문장이 붙는 층위 차이로 바로 설명하고, 층위를 섞을 때 생기는 문제를 첫 문단에서 드러내도록 정리했다.
- 번역 동기화 메모: open with why row/sample/window confusion breaks downstream structures rather than describing the section as a bridge. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`

### v2026.07.07-1

- 변경 이유: Part 3가 어렵게 느껴지는 핵심 공백 중 하나는 `한 행`, `샘플 1건`, `최근 구간 1개`가 서로 다른 층위라는 점을 한 화면에서 직접 비교하는 브리지 설명이 부족하다는 데 있었다.
- 본문 반영: 새 Section `P3-4.3 한 행, 샘플 1건, 최근 구간 1개는 어떻게 다른가`를 추가하고, 시점 기록/동작 1회/최근 구간 집계를 한 장면에서 다시 나누어 보는 표, 짧은 코드 예시, 빠른 판별 질문을 넣어 Part 3 중반 난도를 낮췄다.
- 번역 동기화 메모: preserve the three-level bridge between row, sample, and recent-window units because it resolves one of the main structural confusions in Part 3. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`
