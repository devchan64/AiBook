# Section Release Note

- Section ID: `P3-9.6`
- Source File: `docs/parts/part-03/chapter-09/section-06.md`

### v2026.07.10

- 변경 이유: 라벨 불일치 탐지 예제가 `예상 출력`은 있었지만, Python 예제 프리앰블이 없어 `검토 수`, `라벨 종류 수`, `불일치 사건 목록`을 왜 함께 보는지 코드 앞에서 바로 드러나지 않았다.
- 본문 반영: 예제 앞에 `문제 상황`, `입력(input)`, `기대 출력(output)`, `확인할 개념`을 추가하고 Section 버전을 갱신했다.

- 변경 이유: 말미의 예제 해설과 최종 요약이 같은 결론을 다시 나눠 말해, `라벨 의미 안정성`이라는 핵심보다 반복 정리가 먼저 느껴질 수 있었다.
- 본문 반영: 코드 예시 뒤의 해설과 마지막 점검 문장을 한 문단 흐름으로 묶어, `같은 뜻이 반복되는가`라는 판단이 한 번에 닫히도록 재배치했다.

- 번역 동기화 메모: English and Simplified Chinese translations now preserve the label-stability check, including review-count, label-variety, and disagreement-event framing before target promotion. / reflected in `docs/en/parts/part-03/chapter-09/section-06.md`, `docs/zh/parts/part-03/chapter-09/section-06.md`
- 번역 반영 상태: 영문·중국어 간체 반영 완료
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.10`

### v2026.07.08

- 변경 이유: 도입과 말미에 같은 정의가 반복되고, `Part 3에서 계속 다뤄 온 것` 같은 배치 메모가 현재 절의 라벨 의미 안정성 설명보다 먼저 보였다.
- 본문 반영: 중복된 도입 정의를 제거하고, 첫 소제목 아래 설명을 `현재 라벨 후보를 바로 목표 라벨로 올릴 수 있는가`라는 현재 판단 기준으로 다시 묶었다. 말미의 반복 결론도 하나의 정리 문단으로 압축해 라벨 의미 안정성 판단이 현재 절 안에서 직접 닫히도록 정리했다.

- 변경 이유: 본문이 `인계 메모`, `뒤 단계` 같은 표현을 직접 남겨, 현재 절의 라벨 안정성 설명보다 handoff 메모가 먼저 보였다.
- 본문 반영: 해당 문장들을 `현재 판단 기록`, `현재 목표 라벨 후보 보류 판단` 중심 표현으로 바꿔, 라벨 의미 안정성 점검이 현재 절 안에서 닫히도록 정리했다.

- 변경 이유: 도입부에서 `목표 라벨 후보` 개념사전 직접 링크가 같은 Section 안에서 반복되어, `한 Section 1회 직접 링크` 원칙과 어긋났다.
- 본문 반영: 도입 첫 문단의 직접 링크만 남기고, 바로 뒤 핵심 판단 문장에서는 일반 표기로 바꿔 개념사전 직접 연결을 한 번으로 줄였다.

- 변경 이유: 도입부와 첫 소제목이 `한 걸음 더 들어간다`, `왜 이 질문이 Part 3에 필요한가` 같은 메타 문장으로 열려, 현재 절의 중심인 라벨 일관성 문제보다 독서 흐름 설명이 먼저 보였다.
- 본문 반영: 첫 문단을 `라벨 후보 열이 있어도 같은 사건과 비슷한 조건에서 같은 뜻의 판단이 반복되는가를 함께 봐야 한다`는 개념 설명으로 다시 열고, 첫 소제목도 `라벨 일관성은 왜 함께 점검해야 하는가`로 바꾸었다.

- 변경 이유: 후속 Section 도입부가 `목표 라벨 후보`를 반복 핵심 개념으로 다시 세우면서도 개념사전 직접 앵커로 되돌리지 않아, 후속 Section 축약 규칙이 느슨하게 보였다.
- 본문 반영: 도입 첫 문장과 핵심 판단 문장에서 `목표 라벨 후보(target candidate)`를 개념사전 직접 앵커로 연결했다.

- 변경 이유: `상위 프레임` 재진술 표가 다른 Section들과 같은 마감 템플릿으로 반복되어, 본문이 교안형으로 굳어 보였다.
- 본문 반영: 말미의 `상위 프레임` 표를 제거하고, 현재 설명을 표 재진술 없이 직접 마무리하도록 정리했다.

- 변경 이유: `일반화된 상위 프레임으로 다시 보면` 제목이 Part 3 전반에 반복되어, 현재 절 설명도 같은 템플릿형 마감으로 굳어 보였다.
- 본문 반영: 반복 제목을 제거하고, 상위 프레임 표와 정리 문장을 현재 설명 흐름 안에 직접 이어 붙였다.

- 변경 이유: 라벨 의미 안정성을 설명하는 절에서 메타 템플릿이 반복되었고, 말미의 `인계 직전의 마지막 판정`과 독립 결론 문장도 본문을 교안형으로 보이게 만들고 있었다.
- 본문 반영: 메타 문장을 직접 설명형으로 정리하고, `라벨 존재 여부 -> 라벨 일관성 -> 라벨 승격 가능성` 프레임과 출처를 유지하면서, 말미의 판정 표와 독립 결론 문장을 일반 본문 문단으로 흡수했다. 라벨 열의 존재보다 반복 판단의 안정성이 더 중요하다는 점은 그대로 남겼다.
- 변경 이유: 일부 문장이 여전히 Part 4·5 학습 문맥을 먼저 호출해, 현재 절의 중심인 라벨 일관성 판단보다 뒤 Part 인계 메모가 앞서 보였다.
- 본문 반영: 도입, 비교 표 설명, 말미 문단에서 뒤 Part 직접 호출을 줄이고, 현재 목표 라벨 후보를 바로 학습 문제로 올릴 수 있는지 판단하는 설명으로 초점을 다시 모았다.

- 번역 동기화 메모: preserve the direct prose, the label-stability frame, and the source-backed emphasis on judgment consistency beyond just having a label column. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07

- 변경 이유: 라벨 일관성 절이 Part 4·5 설명을 별도 하위 섹션으로 길게 펼쳐, 현재 Section의 중심 질문인 `목표 라벨 후보의 의미 안정성`보다 뒤 학습 예고가 더 크게 보였다.
- 본문 반영: 두 하위 섹션을 제거하고, 라벨 의미 메모가 왜 뒤 학습 입력과 긴 시퀀스 해석에서도 먼저 필요한지만 짧은 연결 문단으로 압축했다.
- 변경 이유: 코드 예제가 남더라도 이 절에서는 `무엇을 계산하나`보다 `무엇을 먼저 확인하나`가 더 중요하므로, 예제 목적이 출력 단계에서 바로 보이도록 보강할 필요가 있었다.
- 본문 반영: 사건별 검토 수, 사건별 라벨 다양성, 실제 불일치 사건 목록을 순서대로 출력하도록 예제를 손봤다. 같은 사건의 중복 검토와 라벨 갈림을 먼저 확인하는 목적이 예제 구조에서 바로 드러나도록 설명도 보강했다.
- 변경 이유: 이 절은 앞단에 비교 표와 뒤쪽에 코드 예시가 함께 있으므로, 두 부분이 같은 `작은 예시` 표지 아래 있으면 역할 차이가 약하게 보일 수 있었다.
- 본문 반영: 앞단 표는 `비교 표로 먼저 보기`, 뒤는 `작은 코드 예시`로 소제목을 정리해, 라벨 갈림 상황 확인과 실제 점검 코드의 역할을 제목에서도 구분했다.
- 변경 이유: Chapter 9에는 목표 라벨 후보가 어디서 오는지와 산출물 간 추적 기준은 있었지만, 라벨 후보 열이 생긴 뒤에도 사람별·시기별 판단 차이로 의미가 흔들릴 수 있다는 전제가 별도 절로 닫혀 있지 않아 Part 4·5 인계 전에 필요한 데이터 과학 점검이 비어 있었다.
- 본문 반영: 새 Section `P3-9.6 같은 사건도 사람이나 시기에 따라 다른 라벨이 붙으면 어떻게 해야 하는가`를 추가했다. 같은 사건의 중복 검토에서 라벨이 갈리는 장면, 시기별 기준 변경, 바로 target으로 올리기 어려운 신호, Part 4 평가와 Part 5 시계열 학습에 미치는 영향을 표와 코드 예시로 정리했다.

- 번역 동기화 메모: translation should preserve that label availability is not enough; the text must keep the idea that target candidates also need meaning stability across reviewers, time, and repeated cases before handoff. / pending
- 번역 반영 상태: not-started
- 관련 자산: `docs/book/table-of-contents.md`, `mkdocs.yml`
- 원문 기준 버전: `v2026.07.07`
