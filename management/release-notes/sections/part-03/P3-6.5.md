# Section Release Note

- Section ID: `P3-6.5`
- Source File: `docs/parts/part-03/chapter-06/section-05.md`

### v2026.07.10

- 변경 이유: 서로 다른 특징 단위를 비교하는 예제가 구조상은 충분했지만, Python 예제 프리앰블이 없어 현재 절의 핵심인 `같은 역할끼리 비교`를 코드 앞에서 즉시 회수하기 어려웠다.
- 본문 반영: 예제 앞에 `문제 상황`, `입력(input)`, `기대 출력(output)`, `확인할 개념`을 추가하고 Section 버전을 갱신했다.

- 번역 동기화 메모: 확인 필요. / pending
- 번역 반영 상태: 향후 번역 반영 필요
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.10`

### v2026.07.08

- 변경 이유: 도입과 마무리에 `Part 4에서는 ...` handoff가 반복되어, 현재 절의 열 역할 설명보다 뒤 Part 예고가 먼저 보였다.
- 본문 반영: 도입을 단위·범위·변동 폭·기준선 대비 변화라는 현재 판독 기준으로 바로 열고, 결론도 뒤 Part 호출 없이 같은 역할의 같은 열 비교 원리로 직접 닫히게 정리했다.

- 변경 이유: `상위 프레임` 재진술 표가 다른 Section들과 같은 마감 템플릿으로 반복되어, 본문이 교안형으로 굳어 보였다.
- 본문 반영: 말미의 `상위 프레임` 표를 제거하고, 현재 설명을 표 재진술 없이 직접 마무리하도록 정리했다.

- 변경 이유: 핵심 판단을 백틱 독립 문장으로 따로 세워 본문 흐름보다 강조 문구가 먼저 보였다.
- 본문 반영: 단위와 역할을 먼저 적어야 한다는 결론을 일반 문단 안으로 흡수해 상위 프레임 설명과 자연스럽게 이어지게 했다.
- 변경 이유: `이 프레임을 쓰면` 식의 결론 문장이 다른 Section들과 같은 어조로 반복되어, 마감 문체가 다시 템플릿형으로 굳어 보였다.
- 본문 반영: 상위 프레임 표는 유지하고, 마지막 문장을 직접 진술형 결론으로 바꿨다.

- 변경 이유: `일반화된 상위 프레임으로 다시 보면` 제목이 Part 3 전반에 반복되어, 현재 절 설명도 같은 템플릿형 마감으로 굳어 보였다.
- 본문 반영: 반복 제목을 제거하고, 상위 프레임 표와 정리 문장을 현재 설명 흐름 안에 직접 이어 붙였다.

- 변경 이유: 같은 날 여러 차례 손보면서 메타 문장과 과도한 Part 4 예고는 줄였지만, 최종 형태에 맞는 상위 프레임과 외부 근거가 아직 빠져 있었다.
- 본문 반영: 기준선 대비 delta 예제와 역할 표를 유지하면서, `측정 축 구분-역할별 비교-해석 분리`의 상위 프레임 표를 추가했다. 또한 Google의 `feature`, `feature engineering`과 BLS의 `base period`를 참고 자료로 붙여, 숫자 크기 경쟁이 아니라 같은 역할의 같은 열을 기준선 대비로 읽어야 한다는 설명을 일반화된 근거와 함께 정리했다.

- 번역 동기화 메모: preserve the role-aware reading frame, the baseline-delta example, and the source-backed explanation that heterogeneous feature scales should be read by role rather than raw magnitude. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

### v2026.07.07

- 변경 이유: 기존 코드 예시는 특징 값과 역할 이름만 보여 주어, 절의 핵심인 `절대 크기보다 같은 열의 기준선 대비 변화로 읽어야 한다`는 점을 코드 결과가 직접 뒷받침하지 못했다.
- 본문 반영: 기준선 값을 추가하고 `duration_delta`, `pressure_delta`, `flow_std_delta`, `late_drop_delta`를 계산하도록 예제를 보강했다. 또 역할 표를 별도로 출력해, 단위와 구조 역할이 다른 특징은 같은 역할의 같은 열끼리 비교해야 한다는 점이 수치와 표 둘 다에서 드러나게 했다.
- 변경 이유: Part 3의 특징 설계 구간에는 평균, 기울기, 변동성 같은 특징 후보는 설명되어 있었지만, 서로 단위와 범위가 다른 특징을 한 표에서 어떻게 읽고 남길지에 대한 초심자용 다리가 부족했다.
- 본문 반영: 특징 표의 숫자를 절대 크기만으로 읽지 않도록, `단위(unit)`, `구조 역할`, `비교 방식`을 함께 적는 기준을 새 절로 추가했다. `수준`, `변화`, `변동성`, `지속 시간`처럼 역할이 다른 특징을 구분해 읽는 이유와, 같은 열의 기준선 대비 변화가 더 중요할 수 있다는 점을 예시 표와 코드로 보강했다.

- 번역 동기화 메모: preserve that this section is a pre-Part-4 bridge about unit/scale awareness in feature tables, not a full preprocessing recipe. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.07`
