- Section ID: P1-2.1
- Source File: docs/parts/part-01/chapter-02/section-01.md

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 논리 체계 수식, 전문가 시스템 구현, 정량 성능 비교 목록을 제거하고, 기호 기반 AI의 기본 질문과 `P1-2.2`, Chapter 3 handoff만 남기도록 정리했다. 같은 작업에서 `Version`을 `v2026.07.17`로 갱신했다.
- 본문 반영: 규칙 기반 접근 설명 뒤의 전문가 시스템 문장을 `깊게 다루지 않는다`는 표현 없이 현재 절의 사고방식 정리와 `P1-3.1` handoff 중심으로 다시 묶었다.
- 번역 동기화 메모: 영어판과 중국어 간체판도 같은 범위 안내를 삭제형 목록 대신 현재 절 질문과 후속 Section 연결 중심으로 맞춰야 한다.

### v2026.07.12
- 핵심 변경: 한국어 원문 말미의 `이 절에서 기억할 관점`과 `언제 이 관점을 먼저 떠올려야 하는가`를 제거하고 `체크리스트` 하나로 통합했다.
- 핵심 변경: 기호 기반 AI의 정의, 강점과 한계, 규칙 기반 접근과 학습 기반 접근의 문제 성격 차이를 자기 점검 질문으로 다시 정리하고 한국어 원문 `Version`을 `v2026.07.12`로 갱신했다.
- 번역 반영: 영어판 `docs/parts/part-01/chapter-02/section-01.en.md`, 중국어 간체판 `docs/parts/part-01/chapter-02/section-01.zh.md`의 `Version`을 원문 기준 `v2026.07.12`로 동기화했다.

### v2026.07.07
- 핵심 변경: 중국어 간체판 `docs/parts/part-01/chapter-02/section-01.zh.md`를 같은 Section ID와 버전 기준으로 추가했다.

### v2026.07.09
- 핵심 변경: 한국어판의 `사례로 보기`를 `사례 및 예시`로, `짧은 점검`을 `체크리스트`로 정리했다.
- 핵심 변경: 영어판 `Cases`, 중국어 간체판 `案例` 표지를 각각 `Cases and Examples`, `案例与示例`로 통일했다.

### v2026.07.11
- 핵심 변경: 한국어판, 영어판, 중국어 간체판 본문의 인라인 Mermaid 도식을 각각 `rule-based-decision-flow-ko.mmd`, `rule-based-decision-flow-en.mmd`, `rule-based-decision-flow-zh.mmd` include 방식으로 분리했다.
- 핵심 변경: `docs/assets/part-01/chapter-02/README.md`를 다국어 자산 운용 기준에 맞게 갱신하고, 영문 기준 원본과 한국어·중국어 파생본의 대응 관계를 함께 정리했다.
