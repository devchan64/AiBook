# Section Release Note

- Section ID: `P6-3.3`
- Source File: `docs/parts/part-06/chapter-03/section-03.md`

### v2026.07.17
- 본문 반영: 구현 세부를 열거하는 `깊게 다루지 않습니다` 블록을 제거하고, 이번 절은 이름 구분을 먼저 닫고 `context window`와 `운영 제약`은 각각 `P6-3.2`, `P6-16.1`로 다시 읽게 하는 구조로 범위 안내를 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 범위 안내 축소와 handoff 문장을 함께 반영해야 한다.

### v2026.07.18
- 추가 반영: 제목을 `...을 처음 읽는 법`에서 `...은 문맥 읽기에 무엇을 더하는가`로 바꿔, 집필 메타 표현보다 `관계 읽기`와 `순서 정보 공급`이 각각 무엇을 보강하는가라는 실제 학습 질문이 먼저 보이게 정리했다.
- 본문 반영: 절이 너무 많은 이름을 한 번에 얇게 다뤄 압축적으로 읽히던 문제를 줄이기 위해, 기존 P6-3.3을 `위치 표현과 multi-head attention` 중심 보충학습으로 다시 좁혔다.
- 추가 반영: KV cache, sparse attention, long-context는 새 `P6-3.4`로 분리하고, 현재 절은 `관계 읽기`와 `순서 정보 공급`을 구분하는 데 집중하도록 사례와 연습 구조를 다시 썼다.
- 추가 반영: 같은 날짜 재수정에서 `P6-3.4`도 다시 둘로 나누면서, 현재 절의 후속 연결도 `P6-3.4 KV cache`, `P6-3.5 sparse attention/long-context`로 더 세밀하게 조정했다.
- 추가 반영: 초심자가 두 이름을 모두 `Transformer 내부 부품 이름`으로만 외우며 다시 압축적으로 읽는 문제를 줄이기 위해, `지금 막힌 장면 -> 먼저 던질 질문 -> 더 직접 연결되는 것`으로 가르는 `바로 적용해 보면` 표를 보강했다.
- 추가 반영: `둘 다 중요하다`에서 설명이 다시 뭉개지지 않도록, `multi-head attention` 또는 `위치 표현` 감각이 약할 때 각각 어떤 빈칸과 오해가 남는지 비교하는 표를 덧붙였다.
- 추가 반영: 제목 아래 `Section ID`, `Version` 표기를 저장소 규칙 형식으로 바로잡았다.
- 번역 동기화 메모: 대응 번역본이 생기면 기존 단일 보충학습을 두 개의 보충학습으로 나눈 구조와 현재 절의 새 범위 축을 함께 반영해야 한다.

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 종결부는 `언제 구현 인접 개념을 따로 떠올려야 하는가` 하나로 닫히도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
