# P6-10.1 릴리즈 노트

- Section ID: `P6-10.1`
- Source File: `docs/parts/part-06/chapter-10/section-01.md`

## v2026.07.31

- 후속 보강: 반복되어 진행 메타처럼 보이던 공통 확인 문장을 제거하고, 사례 절에는 Section별 판단 내용 한 문장만 남겼다.
- 후속 보강: 원고 본문에서 `오픈체크리스트`, `중심축` 같은 관리 표현을 제거하고, 독자가 사례 안에서 확인할 학습 판단 문장으로 다시 정리했다.
- 후속 보강: 소제목 정리에 그치지 않도록 사례 절 첫머리에 중심축 확인 질문 2개를 추가했다.
- 변경 이유: Section별 학습 평가 리포트의 Part 6 보강 라운드에서 진행 메타 소제목 정리가 필요한 항목으로 분류되었기 때문이다.
- 본문 반영: 한국어 원문의 `사례 및 예시` 소제목을 `입력 지시·맥락·예시를 조정하는 프롬프트 엔지니어링: 확인할 판단 기준`으로 바꾸어 사례 절이 Section 중심축과 직접 연결되게 했다.
- 번역 동기화 메모: 이번 변경은 한국어 원문의 소제목 정리이며, 영어판과 중국어 간체판에는 별도 반영하지 않았다.
- 번역 반영 상태: 한국어 원문만 반영 / 영문·중문 미반영
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-06/chapter-10/section-01.md` `v2026.07.31`

- 변경 이유: 프롬프트로 가능한 일과 프롬프트 밖 구조로 넘겨야 하는 일을 기록 필드 단위로 분리할 필요가 있었다.
- 본문 반영: 한국어 원문 `Version`을 `v2026.07.31`로 갱신하고, `user_goal`, `instruction`, `context`, `example`, `output_format`, `observed_response`, `remaining_limit` 필드 예시를 추가했다.
- 번역 동기화 메모: 영어판과 중국어 간체판은 이번 한국어 원문 보강 문단을 후속 동기화해야 한다.
- 번역 반영 상태: 한국어 원문만 반영.
- 관련 자산: 변경 없음.
- 원문 기준 버전: `v2026.07.31`

## v2026.07.26

- 정합성 점검: 한국어·영어·중국어 간체판의 본문 `Version`을 `v2026.07.26`으로 맞췄다.
- 표기 정리: 영어판 소제목의 대소문자를 현재 번역 표기 기준에 맞춰 정리했다.
- 번역 반영 상태: 영문/중문 번역 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-06/chapter-10/section-01.md` `v2026.07.26`

## v2026.07.25-zh

- 변경 이유: Part 6 중국어판 순차 번역에서 P6-10.1을 최신 한국어 원문과 같은 Section ID·Version 기준으로 제공하기 위함.
- 본문 반영: `docs/parts/part-06/chapter-10/section-01.zh.md`를 추가하고, prompt engineering을 지시·맥락·예시를 조정하는 입력 설계와 반복 관찰 실험으로 읽는 설명 흐름을 중국어 간체로 옮겼다.
- 예제 반영: 저장 응답 로그 CSV를 읽는 Python 예제와 실행 결과를 중국어판에 반영하고, `.venv` Python으로 CSV 집계 출력이 본문과 일치함을 확인했다. Ollama 직접 호출 코드는 선택 실행 예시로 유지했다.
- 자산 반영: `p6-c10-s01-prompt-loop-zh.mmd`, `p6-c10-s01-prompt-cases-zh.mmd`, `prompt-structure-check-zh.png`를 추가했다.
- CSV 반영: `p6-10-1-prompt-response-log.csv`는 원문 릴리즈 기준에서 이미 영어 프롬프트와 영어 슬롯 이름으로 생성된 공용 실행 스냅샷이므로 중국어판도 같은 CSV를 참조한다.
- 검수 반영: 한국어 원문과 중국어판의 빈 줄 제외 줄 수를 대조하고, 중국어판에서 한국어 전용 자산을 참조하지 않음을 확인했다.
- 번역 동기화 메모: 중국어판은 한국어 원문 `v2026.07.24`를 기준으로 최신 반영했다.
- 번역 반영 상태: 중국어 번역 완료.
- 원문 기준 버전: `v2026.07.24`

## v2026.07.24-en

- 변경 이유: Part 6 영어판 순차 번역에서 P6-10.1을 최신 한국어 원문과 같은 Section ID·Version 기준으로 제공하기 위함.
- 본문 반영: `docs/parts/part-06/chapter-10/section-01.en.md`를 추가하고, prompt engineering을 지시·맥락·예시를 조정하는 입력 설계와 반복 관찰 실험으로 읽는 설명 흐름을 영어로 옮겼다.
- 예제 반영: 저장 응답 로그 CSV를 읽는 Python 예제와 실행 결과를 영어판에 반영하고, `.venv` Python으로 CSV 집계 출력이 본문과 일치함을 확인했다. Ollama 직접 호출 코드는 선택 실행 예시로 유지했다.
- 자산 반영: 영어판 Mermaid 도식은 `p6-c10-s01-prompt-loop-en.mmd`, `p6-c10-s01-prompt-cases-en.mmd`를 참조하고, 차트는 `prompt-structure-check-en.png`를 참조하도록 정리했다.
- CSV 반영: `p6-10-1-prompt-response-log.csv`는 원문 릴리즈 기준에서 이미 영어 프롬프트와 영어 슬롯 이름으로 생성된 공용 실행 스냅샷이므로 영어판도 같은 CSV를 참조한다.
- 검수 반영: 한국어 원문과 영어판의 빈 줄 제외 줄 수를 대조하고, 영어판에서 한국어 전용 자산을 참조하지 않음을 확인했다.
- 번역 동기화 메모: 영어판은 한국어 원문 `v2026.07.24`를 기준으로 최신 반영했다.
- 번역 반영 상태: 영어 번역 완료.
- 원문 기준 버전: `v2026.07.24`

## v2026.07.24

- 변경 이유: Part 6 Python 예제 개선 방안에 따라 프롬프트 엔지니어링 예제를 실시간 Ollama 호출 의존 중심에서 저장 응답 로그 우선 구조로 바꾸기 위함.
- 본문 반영: `simple`, `instruction_context`, `instruction_context_example`, `instruction_context_example_check`의 80행 저장 응답 로그 CSV를 읽어 `format_ok_count`, `slot_ok_count`, `full_keyword_keep_count`, `average_keyword_ratio`를 집계하는 Python 예제로 교체했다. Ollama 직접 호출은 새 로그를 만들 수 있는 선택 실행 전제로 분리했다.
- 관련 자산: `docs/assets/part-06/chapter-10/p6-10-1-prompt-response-log.csv`를 추가하고, `p6_10_1_prompt_structure_chart.py`, `prompt-structure-check-ko.png`, `prompt-structure-check-en.png`를 CSV 집계 기준으로 갱신했다.
- 예제 보강: 저장 CSV를 `작업만 적은 프롬프트 -> 지시+맥락 프롬프트 -> 지시+맥락+예시 프롬프트`의 3단 비교로 재구성하고, Ollama 로컬 모델로 같은 형식의 로그를 다시 만들 수 있는 `p6_10_1_generate_prompt_response_log.py`를 같은 프롬프트 유형 기준으로 맞췄다.
- 예제 보강: 본문 Ollama 호출 예시도 네 프롬프트 유형을 순회하는 구조로 바꾸어, 실제 모델 호출 흐름이 저장 CSV 집계의 중심선과 끊기지 않게 했다.
- 정합성 보강: 저장 로그 CSV에 `log_source`, `model_name`, `temperature`, `slot_language` 메타 열을 추가하고, 실제 Ollama 호출 로그의 생성 기준, 영어 슬롯 기준, 재집계·차트 재생성 흐름을 본문에 명시했다.
- 정합성 보강: 저장 응답 로그를 `llama3.2:latest`와 `temperature=0.2`로 실제 실행해 만든 CSV 스냅샷으로 갱신하고, 차트 y축 분모를 CSV의 반복 수에서 동적으로 표시하게 했다.
- 예제 보강: 운영 메모를 3개에서 4개로, 프롬프트 유형을 3개에서 4개로, 반복 수를 프롬프트당 12회에서 20회로 늘려 저장 로그 규모를 약 두 배로 확대했다.
- 검증 흐름 보강: Ollama 로그 생성, CSV 관찰 열 저장, 집계와 차트 재생성의 세 단계를 표로 추가해 프롬프트 실험을 단일 응답 감상이 아니라 반복 가능한 관찰 절차로 읽게 했다.
- 모델 호출 보강: 본문 예제 앞에 Ollama API 호출부를 별도 Python 블록으로 추가해, 저장 CSV 집계가 실제 로컬 모델 응답 관찰에서 시작될 수 있음을 코드에서 직접 확인하게 했다.
- 원문 기준 버전: `v2026.07.24`

## v2026.07.23

- 변경 이유: Part 6 목차에서 질문형 제목이 반복되어 Section 중심축이 한눈에 구분되기 어려웠기 때문.
- 본문 반영: 제목을 `입력 지시·맥락·예시를 조정하는 프롬프트 엔지니어링`으로 바꾸어 프롬프트가 조정하는 입력 요소가 목차에서 바로 보이게 했다.
- 목차 반영: `mkdocs.yml`, 독자용 목차, Part 6 개편안의 제목을 같은 표현으로 맞추었다.
- 번역 동기화 메모: 향후 번역본도 질문형 반복보다 입력 요소의 조정 축이 먼저 보이게 제목을 맞춘다.
- 번역 반영 상태: 한국어 원문과 목차만 수정했다.
- 원문 기준 버전: `v2026.07.23`

## v2026.07.22

- 변경 이유: Part 6 본문에서 진행 메타형 소제목을 구체 개념, 비교 대상, 판단 기준이 드러나는 제목으로 정리하기 위함.
- 본문 반영: `여기서 남겨야 할 구분`, `여기까지를 한 줄로 묶으면`, `보는 순서`, `이 예제를 ... 다시 보면` 계열 제목을 현재 절의 실제 구분 축과 설명 흐름이 보이는 소제목으로 바꾸었다.
- 본문 반영: Python 예제를 사람이 만든 응답 함수 비교에서 Ollama 로컬 API를 호출해 실제 모델 응답을 검사하는 실험형 예제로 전면 교체하고, 실행 결과가 모델과 버전에 따라 달라질 수 있음을 명시했다.
- 본문 반영: 고정 요약 차트 해설을 제거하고, 실행 로그의 형식 통과 수, 슬롯 통과 수, 핵심 키워드 보존율을 직접 비교하는 흐름으로 바꾸었다.
- 번역 동기화 메모: 향후 번역본도 진행 안내형 제목보다 현재 절의 구체 개념, 비교 대상, 판단 기준이 보이는 소제목으로 맞춘다.
- 번역 반영 상태: 한국어 원문만 수정했다.
- 원문 기준 버전: `v2026.07.22`

## v2026.07.21

- Part 6 개편 기준에 따라 Section ID, 파일 위치, 제목을 새 목차 구조에 맞춰 재정렬했다.
- 생성형 AI 산출물에서 시작해 LLM을 대표 사례로 읽는 흐름 안에서 이 Section의 위치를 다시 잡았다.
- 프롬프트 엔지니어링을 좋은 문장 요령이 아니라 지시, 맥락, 예시 중 비어 있는 칸을 찾아 채우는 입력 설계로 읽도록 도입부를 정리했다.
- 프롬프트 입력 조정, RAG 근거 연결, tool use/agent 실행 구조의 층위를 번호 나열이 아니라 역할 구분으로 보강했다.
- P6-10.1 전용 도식과 차트 자산을 chapter-10 경로로 분리하고, 독자 진행 안내식 표현을 개념 경계 중심 문장으로 정리했다.
- 개편 전 릴리즈노트 원문은 `management/authoring/archive/part-06-before-restructure-2026-07-21/management/release-notes/sections/part-06/`에 보존했다.
- `바로 적용해 보면` 제목을 입력 설계로 먼저 줄일 수 있는 장면으로 바꾸어 프롬프트가 맡는 조정 범위를 더 분명히 드러냈다.
- 요약, 분류, 문서 기반 질의응답 사례에서 막연한 기대와 입력 설계 기준 전환을 문단으로 나누어 프롬프트가 조정하는 범위가 더 선명하게 읽히도록 했다.
- 문서 기반 질의응답 사례의 근거 문단 제공과 기준 전환 해설을 추가로 분리해 질문 작성과 문서 범위 고정의 차이를 더 또렷하게 했다.
- 제목을 `프롬프트 엔지니어링은 무엇을 입력에서 조정하는가`로 바꾸고, 도입부와 사례 요약의 반복 표를 문단 설명으로 압축해 입력 설계라는 중심축을 더 선명하게 했다.
