# Section Release Note

- Section ID: `P6-8.4`
- Source File: `docs/parts/part-06/chapter-08/section-04.md`

### v2026.07.18
- 추가 반영: 도입부에서 `보충학습의 역할`, `이 보충학습을 읽는 순서` 같은 메타 안내를 더 줄이고, `먼저 보인 제약 장면 -> 먼저 떠올릴 이름 -> 왜 이렇게 갈라지는가` 표를 앞쪽에 넣어 초심자가 adapter, LoRA, QLoRA를 이름 암기보다 `구조 차이`, `변화분 규모`, `메모리 제약` 선택 기준으로 먼저 읽게 정리했다.
- 변경 이유: `adapter`, `LoRA`, `QLoRA` 이름 구분은 Chapter 7의 본선 질문을 닫기 전에 앞질러 들어가면 `파인튜닝이 필요한가 -> 전체를 다 건드려야 하는가 -> 이제 무엇을 가르칠 것인가` 흐름을 끊는다는 문제가 드러났다.
- 본문 반영: 기존 `P6-7.3` 주제를 Chapter 8 뒤쪽 `P6-8.4`로 옮기고, `P6-8.3`의 선택 지도에서 `파인튜닝 축을 먼저 고른 뒤 읽는 보충절`이라는 위치로 다시 정리했다.
- 추가 반영: 제목을 `처음 구분하는 법`에서 `어떤 제약에서 갈리는가`로 바꾸고, 도입부도 `이름 암기`보다 `비용·구조·메모리 제약` 기준이 먼저 보이도록 다시 썼다.
- 추가 반영: `P6-7.2`의 handoff 문장도 바로 `P6-8.1`로 이어지게 정리하고, low-rank·adapter·QLoRA 세부 구분은 `P6-8.4`에서 회수하도록 앞뒤 연결을 다시 맞췄다.
- 번역 동기화 메모: 대응 번역본이 생기면 `P6-8.4`의 새 위치와 제목, 그리고 `P6-8.3 -> P6-8.4` handoff를 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `깊게 다루지 않습니다` 목록을 제거하고, 이번 보충학습은 adapter, LoRA, QLoRA 이름 구분을 먼저 닫고 지시 튜닝·운영 제약으로 handoff하는 구조가 먼저 보이도록 범위 안내를 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 범위 안내 단순화와 handoff 문장을 반영해야 한다.

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 사례 뒤에 `이름만 외웠을 때의 오해 vs 먼저 붙잡아야 할 판단 기준` 표를 보강했고, 예제 앞 예상 표와 제약 장면 기반 연습 문제를 추가해 adapter, LoRA, QLoRA 구분이 실제 자원 제약 판단으로 이어지게 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 예제 뒤에 자원 제약·사본 수·실험 회전 수 기준으로 선택지를 가르는 비교표를 추가했고, 종결 표지를 `체크리스트`로 통일함. 참고 자료도 실제 논문 링크로 구체화함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 종결부에서는 `언제 보충학습 구분표를 먼저 떠올려야 하는가`만 남기도록 일반형 `언제 이 관점을 먼저 떠올려야 하는가`를 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
