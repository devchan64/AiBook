# Section Release Note

- Section ID: `P6-9.1`
- Source File: `docs/parts/part-06/chapter-09/section-01.md`

### v2026.07.18
- 추가 반영: 제목 아래 `Section ID`, `Version` 표기를 저장소 공통 형식에 맞춰 blockquote 대신 일반 메타데이터 줄로 정리했다.
- 본문 반영: 세 사례 요약 뒤에 `바로 적용해 보면` 블록을 추가해, 초심자가 요약·분류·문서 질의응답 장면에서 무엇을 먼저 명시해야 하는지 `작업 지시`, `예시`, `문맥` 기준으로 바로 대응해 읽도록 보강했다.
- 추가 반영: `바로 적용해 보면`과 점검 질문에 `사실관계보다 형식과 길이 쪽이 먼저 흔들리는 장면`을 덧붙여, `새 구조 추가`와 `입력 지시 보강`을 초심자 첫 질문에서 먼저 가르도록 보강했다.
- 추가 반영: 도입부에서 `이 절은 ... 답합니다`, `손잡이` 같은 메타 설명을 줄이고, 프롬프트를 `입력 설계`로 읽는 핵심 전환이 실제 설명 문장으로 먼저 드러나도록 정리했다.
- 추가 반영: 본문 중간의 `첫 손잡이`, `실무 손잡이` 같은 표현도 `첫 제어 지점`, `실무 제어 지점`으로 바꿔, 프롬프트를 설명 도구보다 실제 입력 조정 위치로 읽게 다듬었다.
- 추가 반영: `이 절을 읽는 순서` 안내를 걷고, `형식 흔들림`, `독자 수준/말투 불일치`, `최신성·근거 불안`, `실행 구조 필요`를 먼저 가르는 장면별 분기 표로 바꿔 초심자 첫 질문이 프롬프트 층 판단으로 바로 이어지게 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 직접 적용 표와 질문 표를 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절에서는 다음 내용을 깊게 다루지 않습니다` 목록을 제거하고, 이번 절이 닫는 질문인 프롬프트를 `입력 설계`라는 첫 실무 손잡이로 읽는 기준과 `P6-9.2`, `P6-9.3`, `P6-12.*`, `P6-13.1` handoff가 먼저 보이도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 범위 안내 구조로 반영이 필요하다.

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: `프롬프트로 먼저 해 볼 질문 / 프롬프트만으로는 닫히지 않는 질문` 요약 표와 `capability를 끌어내는 첫 손잡이`라는 정리 문장을 추가해, P6-9.2와 P6-10으로 넘어가는 전환을 더 선명하게 보강함. 추가로 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: `커리큘럼 관점` 문단을 목표 뒤로 옮기고, `언제 ... 떠올려야 하는가` 표지를 종결부 하나로 정리했으며, 마무리 구조 표지를 원칙 기준에 맞춰 통일함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
