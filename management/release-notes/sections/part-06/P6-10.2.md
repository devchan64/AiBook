# Section Release Note

- Section ID: `P6-10.2`
- Source File: `docs/parts/part-06/chapter-10/section-02.md`

### v2026.07.19

- 입력 데이터 분리: RAG 문서 목록과 실험 조건을 `docs/assets/part-06/chapter-10/p6-10-rag-documents.csv`, `docs/assets/part-06/chapter-10/p6-10-rag-experiments.csv`로 추가하고, Python 예제가 실제 CSV를 읽도록 바꿨다.
- 근거 정합성: RAG 논문, OpenAI Retrieval, OpenAI File search 자료의 확인 날짜를 현재 검토일로 갱신했다.
- Python 예제 재설계: 고정된 payload 묶음을 검사하던 예제를 `retrieval_terms`와 `generation_style` 조작값에 따라 검색 문서와 생성 결과가 달라지는 실험형 예제로 바꿨다.
- 가이드라인 반영: 검색 결과, 후보 목록, 생성 결과를 미리 주입하지 않고 입력 문서와 조작 변수에서 실패가 드러나도록 수정했다.
- 메타데이터 반영: 본문 Version을 `v2026.07.19`로 갱신했다.
- 본문 정리: 연습 및 예제의 `문제 상황` 블록이 같은 검색 실패·생성 실패 구분을 두 번 반복하던 부분을 줄였다.
- Python 예제 검토: 예제 실행 결과가 본문 출력 예시와 일치함을 확인했다.
- 학습 밀도 보강: 검색 실패와 생성 실패가 따로 감지되는 수를 비교하는 PNG 차트 `rag-failure-split-en.png` / `rag-failure-split-ko.png`를 추가했다.
- 차트 정리: 한국어 차트 y축 라벨의 영문 잔여 표현을 한국어 설명으로 맞췄다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 예제 도입 중복 축소와 차트 연결 문단을 반영해야 한다.

### v2026.07.18

- 체크리스트 정리: 체크리스트 안에 섞여 있던 요약 표, 전환 문단, 중복 요약 항목을 걷고 현재 Section 중심 질문을 닫는 자기 확인 항목만 남겼다.
- 정리 반영: 도입 질문 앞의 `이 절에서는 다음 질문을 다룹니다` 문장을 내용 중심 표현으로 낮췄다.
- 본문 반영: `이 절이 맡는 역할` 같은 설명 밖 문장을 줄이고, 검색 실패와 생성 실패를 왜 따로 판정해야 하는지가 본문 문장 자체로 보이게 다듬었다.; `이 절은 Part 6에서 ...` 같은 위치 설명형 메타 문장을 걷고, `검색 실패`와 `생성 실패`를 따로 읽는 기준이 바로 보이도록 도입부를 더 직접적으로 정리했다.; `커리큘럼 관점에서 이 절이 중요한 이유` 표지를 `이 절이 맡는 역할`로 낮춰, 검색-생성 결합 설명이 안내 문구보다 실패 분리 기준 중심으로 읽히게 정리했다.; `## 이 절의 범위`, `## 이 절의 목표`, `## 이 절을 읽는 순서` 같은 메타 제목을 `검색-생성 결합이 다루는 질문`, `여기서 남겨야 할 구분`, `결합 흐름을 보는 순서`로 바꿔, 장 진행 안내보다 실제 결합 구조가 먼저 보이게 정리했다.
- 추가 반영: 제목 아래 `Section ID`, `Version` 표기를 저장소 공통 형식에 맞춰 blockquote 대신 일반 메타데이터 줄로 정리했다.; 세 사례 요약 뒤에 `바로 적용해 보면` 블록을 추가해, 초심자가 제품 지원·법률 문서·개발 문서 장면에서 오답 원인을 `검색 실패`, `생성 실패`, `검색 오염의 전염`으로 바로 가르고 어떤 기록을 먼저 다시 봐야 하는지 짧게 점검하도록 보강했다.; 도입부의 `이 절은 ...`, `손잡이` 중심 문장을 줄이고, `검색 실패`와 `생성 실패`를 따로 보는 현재 절 핵심이 먼저 드러나도록 정리했다.; 사례와 `바로 적용해 보면`에 남아 있던 `초심자가 ...` 독자 표지를 `여기서 바로잡아야 할 오해`, `자주 생기는 오해` 중심 표현으로 낮춰, 검색-생성 결합 설명이 메타 독자 안내보다 실패 분리 기준에 더 직접 붙게 다듬었다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 직접 적용 표와 질문 표를 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절에서는 다음 내용을 깊게 다루지 않습니다` 목록을 제거하고, 이번 절이 닫는 질문인 검색 결과와 생성의 결합, 검색 실패와 생성 실패 구분, `P6-11.*` handoff가 먼저 보이도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 범위 안내 구조로 반영이 필요하다.

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 검색 전에 필요한 `분할`, `최신 버전 구분`, `중복 제거`, `메타데이터 정리`를 짧게 설명하는 문단을 추가했고, 이어서 `문서 준비 단계`, `검색 단계`, `생성 단계`를 나누는 비교표를 더해 P6-11의 인덱스·저장 구조로 넘어가는 전환을 보강함. 종결 표지는 저장소 공통 기준에 맞춰 `체크리스트`로 통일함. 추가로 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 종결부에서는 `언제 검색-생성 결합 관점을 먼저 떠올려야 하는가`만 남기도록 일반형 `언제 이 관점을 먼저 떠올려야 하는가`를 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
