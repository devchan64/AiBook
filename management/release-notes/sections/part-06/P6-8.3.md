# Section Release Note

- Section ID: `P6-8.3`
- Source File: `docs/parts/part-06/chapter-08/section-03.md`

### v2026.07.18
- 추가 반영: `바로 적용해 보면` 표의 `초심자가 먼저 하기 쉬운 오해` 머리글을 `먼저 떠올리기 쉬운 오해`로 낮춰, 실패 원인 분기가 독자 라벨보다 형식·근거·실행·지속 적응의 차이로 먼저 읽히게 다듬었다.
- 추가 반영: `## 이 절의 범위`, `## 이 절의 목표` 같은 메타 제목을 `실패 원인 선택이 다루는 질문`, `여기서 남겨야 할 구분`으로 바꿔, 기술 목록 안내보다 `무엇을 어디서 먼저 고칠까`라는 선택 지도가 먼저 보이게 정리했다.
- 추가 반영: 이 절이 뒤의 Module 4~6을 `기술 목록`이 아니라 `실패 원인별 보강 경로`로 읽게 만드는 입구라는 점을 앞부분 handoff 문장에 더 직접 반영했다.
- 추가 반영: 메타적인 `이 절을 읽는 순서` 안내를 제거하고, 선택 지도의 역할이 본문 전환 문장 자체에서 먼저 드러나도록 정리했다.
- 추가 반영: 제목 아래 `Section ID`, `Version` 표기를 저장소 공통 형식에 맞춰 blockquote 대신 일반 메타데이터 줄로 정리했다.
- 추가 반영: 도입부를 다시 써서 `왜 지시 튜닝과 정렬 다음에 바로 수단 선택 지도를 읽는가`, `좋은 답 기준을 안다는 것과 실패 원인을 올바르게 가르는 것이 왜 다른가`, `이 절의 닫힘이 기술 이름 암기가 아니라 문제 증상을 보고 수단을 덜 잘못 고르는 기준이라는 점`이 먼저 보이도록 Chapter 8의 handoff를 더 분명하게 세웠다.
- 본문 반영: 선택 지도 요약 뒤에 `바로 적용해 보면` 표를 추가해, 초심자가 `형식 흔들림`, `최신 규정 오류`, `계산·조회 실패`, `반복 문체 불안정` 장면을 볼 때 무엇을 `프롬프트`, `RAG`, `도구 사용`, `파인튜닝` 문제로 먼저 읽어야 하는지 바로 점검하게 했다.
- 추가 반영: 선택 기준을 `어떤 기술이 더 좋아 보이는가`가 아니라 `형식`, `근거`, `실행`, `지속 적응` 중 무엇이 먼저 부족한가를 묻는 직접 적용 닫힘으로 다시 붙잡았다.
- 추가 반영: 도입부의 `이 절은 ...` 문장을 줄이고, `무엇을 바꿔야 하는가`보다 먼저 `실패 원인을 갈라 본다`는 판단 흐름이 드러나도록 정리했다.
- 추가 반영: 파인튜닝 축을 더 파고들 때는 `P6-8.4`에서 adapter·LoRA·QLoRA 구분을 이어 읽도록 handoff 문장을 추가해, 선택 지도 다음의 세부 보충 위치를 더 분명하게 남겼다.
- 추가 반영: `이 절은 Part 6에서 ...`와 `초심자가 여기서 ...` 같은 해설 문장을 줄이고, Module 4~6 진입이 `기술 이름 나열`이 아니라 `실패 원인별 수단 선택`이라는 흐름으로 바로 읽히게 다듬었다.
- 번역 동기화 메모: 대응 번역본이 생기면 적용 표도 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: 비용 비교표·파이프라인·운영 프로세스를 열거하던 `깊게 다루지 않습니다` 블록을 제거하고, 이번 절은 선택 기준만 남긴 뒤 프롬프트·RAG·도구 사용·운영 절로 handoff하는 구조로 범위 안내를 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 handoff 중심 범위 안내와 표 표현 수정을 반영해야 한다.

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 종결부에서는 `언제 선택 지도 관점을 먼저 떠올려야 하는가`만 남기도록 일반형 `언제 이 관점을 먼저 떠올려야 하는가`를 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
