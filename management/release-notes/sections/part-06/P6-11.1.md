# Section Release Note

- Section ID: `P6-11.1`
- Source File: `docs/parts/part-06/chapter-11/section-01.md`

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 생략 항목 목록을 걷어내고, 이번 절이 `임베딩, 원문, 메타데이터를 함께 저장하는 검색 저장 구조`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 속도와 근사 검색의 균형은 `P6-11.2`, 검색을 넘어 실제 조회·실행으로 가는 확장은 `P6-12.1`, `P6-12.2`로 이어지게 handoff를 분명히 했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P6-11.1 closes the storage role first and hands search-speed tradeoffs to P6-11.2.

### v2026.07.18
- 본문 반영: Chapter 11~14의 후반 실행 구조 전환을 더 선명하게 읽게 하려고, `지금 부족한 것 -> 이번 장이 붙이는 구조 -> 아직 남는 문제 -> 바로 이어지는 위치` 비교 블록을 추가했다.
- 본문 반영: `이 절은 다음 질문에 답합니다` 같은 메타 도입 문장을 `이 절에서 먼저 붙잡을 질문` 형식으로 낮춰, 공개 본문 호흡을 더 자연스럽게 정리했다.
- 추가 반영: `이 절은 Part 6에서 ...` 같은 한 겹 감싸는 메타 문장을 더 직접적인 설명 문장으로 바꿔, 저장 구조 설명이 바로 본론으로 읽히게 정리했다.
- 추가 반영: 사례 요약 뒤에 `바로 적용해 보면` 블록을 추가해, 초심자가 벡터 데이터베이스를 `벡터만 찾는 곳`이 아니라 `원문`과 `메타데이터`까지 함께 돌려주는 검색 저장 구조로 바로 점검해 읽도록 보강했다.
- 번역 동기화 메모: Future translations should preserve the new handoff block that distinguishes storage role from later execution layers.

### v2026.07.11
- 본문 반영: 내용 전개는 유지하고 종결 표지를 저장소 공통 기준에 맞춰 `체크리스트`로 통일했으며, 본문 메타데이터 버전도 함께 갱신함. 추가로 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 종결부에서는 `언제 벡터 데이터베이스 관점을 먼저 떠올려야 하는가`만 남기도록 일반형 `언제 이 관점을 먼저 떠올려야 하는가`를 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
