# Section Release Note

- Section ID: `P6-3.1`
- Source File: `docs/parts/part-06/chapter-03/section-01.md`

### v2026.07.17
- 본문 반영: `이 절에서는 다음 내용을 깊게 다루지 않습니다` 목록을 제거하고, Transformer 구조는 현재 절에서 닫고 구현 감각이 더 필요한 이름은 `P6-3.3`, 운영 제약은 `P6-16.1`로 넘기는 handoff 문장으로 다시 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 범위 안내도 같은 handoff 중심 구조로 맞춰야 한다.

### v2026.07.18
- 본문 반영: Chapter 3 보충학습을 둘로 나누면서, 현재 절의 후속 참조도 `P6-3.3`의 문맥 읽기 보강과 `P6-3.4`의 긴 문맥/반복 생성 보강으로 분리해 다시 적었다.
- 추가 반영: 같은 날짜 재수정에서 `P6-3.4`를 KV cache 단독 절로, `P6-3.5`를 sparse attention/long-context 절로 다시 나누고 현재 절의 handoff 문장도 그 구조에 맞춰 조정했다.
- 번역 동기화 메모: 대응 번역본이 생기면 단일 보충학습 참조를 두 갈래 handoff 구조로 함께 반영해야 한다.

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, `커리큘럼 관점` 문단을 목표 뒤로 옮겼으며, 종결부는 `언제 Transformer 구조 관점을 먼저 떠올려야 하는가` 하나로 닫히도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
