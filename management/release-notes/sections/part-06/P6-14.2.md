# Section Release Note

- Section ID: `P6-14.2`
- Source File: `docs/parts/part-06/chapter-14/section-02.md`

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 테스트 프레임워크, 관측성 스택, 배포 파이프라인 같은 생략 목록을 앞세우지 않고, 이번 절이 `실행을 어떤 trace, replay, approval 기록으로 감싸고 남길 것인가`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 하네스 설명의 후속 handoff를 `P6-15.1`, `P6-15.2` 평가와 `P6-16.1`, `P6-16.2` 운영 제약·실패 대응으로 직접 연결했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P6-14.2 closes execution-management records first and hands evaluation/operations to the next chapters.

### v2026.07.18
- 본문 반영: 연결과 루프만으로는 왜 같은 실패를 다시 설명하기 어려운지 먼저 보이게 하려고, `지금 부족한 것 -> 이번 장이 붙이는 구조 -> 아직 남는 문제 -> 바로 이어지는 위치` 비교 블록을 추가했다.
- 본문 반영: `이 절은 다음 질문에 답합니다` 문장을 `이 절에서 먼저 붙잡을 질문`으로 바꿔, 하네스 절의 도입이 작업 메모처럼 읽히지 않게 정리했다.
- 추가 반영: `이 절은 Part 6에서 ...`와 `이 절에서는 ...로 설명합니다` 같은 메타 서술을 더 직접적인 설명 문장으로 바꿔, 실행 관리 장치의 역할이 바로 읽히게 정리했다.
- 추가 반영: `지금 읽는 층위는`으로 절의 역할을 따로 설명하던 문장을 줄이고, 연결 구조에서 평가 입력이 되는 실행 기록으로 넘어가는 질문이 바로 이어지도록 앞부분을 다듬었다.
- 추가 반영: 세 사례 요약 뒤에 `바로 적용해 보면` 블록을 추가해, 초심자가 `trace`, `approval`, `replay` 기록을 기준으로 원인 분리와 운영 후속 조치를 바로 읽도록 보강했다.
- 번역 동기화 메모: Future translations should preserve the new comparison block that separates execution records from later evaluation and operations decisions.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 종결부에서는 `언제 하네스 관점을 먼저 떠올려야 하는가`만 남기도록 일반형 `언제 이 관점을 먼저 떠올려야 하는가`를 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.11
- 본문 반영: `먼저 보인 신호 -> 하네스 층에서 바로 남겨야 하는 것 -> 평가/운영에서 이어지는 판단` 비교표를 추가했고, 종결 표지를 `체크리스트`로 통일함. 이어서 독자용 표 설명에 남아 있던 `run record` 표현은 `요청 실행 기록`으로 일반화해 공용 서술 톤을 맞춤. 추가로 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
