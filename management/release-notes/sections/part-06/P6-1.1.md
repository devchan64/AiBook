# Section Release Note

- Section ID: `P6-1.1`
- Source File: `docs/parts/part-06/chapter-01/section-01.md`

### v2026.07.12
- 본문 반영: `P6-1.1`을 `왜 토큰이 필요한가`라는 출발 절로 재작성했다. 사람의 길이 감각과 모델의 계산 단위를 구분하고, 길이 제한·비용·문맥 보존 문제 때문에 토큰이 등장한다는 흐름만 남기도록 본문을 크게 줄였다.
- 추가 반영: 재구성 뒤 사례층이 너무 얇아진 문제를 보완하기 위해, 짧은 한 줄 문장, 긴 문서 한 파일, 검색용 청크 분할의 세 장면을 다시 보강하고 비교표를 복원했다.
- 번역 동기화 메모: 대응 번역본이 생기면 `P6-1.1`은 정의 소개가 아니라 필요성부터 여는 절로 유지해야 한다.

### v2026.07.18
- 본문 반영: 사례 뒤에 `바로 적용해 보면` 표를 추가해, 초심자가 `사람 기준 질문`을 `모델 기준 질문`으로 바꾸는 연습을 바로 해 볼 수 있게 했다.
- 추가 반영: 종결 문장도 `지금 단계에서 무엇을 붙잡으면 충분한가`를 더 직접 적어, 다음 절로 넘어가기 전 이해 닫힘을 보강했다.
- 추가 반영: 같은 날짜 재수정에서 길이·비용·문맥 보존 셋 중 무엇이 걸린 장면인지 바로 고르게 하는 짧은 적용 표와 마무리 점검 표를 덧붙여, 이 절의 닫힘을 `토큰이라는 이름 알기`보다 `문제를 토큰 기준으로 다시 읽기` 쪽에 더 강하게 고정했다.
- 추가 반영: 같은 작업이라도 토큰 수가 달라질 때 `비용`, `잘림`, `문맥 보존` 중 무엇이 먼저 달라지는지 장면별로 고르게 하는 작은 비교표를 덧붙여, 초심자가 실패 유형을 더 짧게 분류해 보도록 보강했다.
- 추가 반영: `초심자 기준에서는`처럼 집필 메타가 앞으로 나오던 닫힘 문장을 줄이고, 현재 단계에서 바로 남겨야 할 토큰 판단 기준이 직접 보이게 다듬었다.
- 번역 동기화 메모: 대응 번역본이 생기면 사례 뒤의 적용 표와 마지막 닫힘 문장도 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리했다.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 종결부 일반형 제목을 `언제 토큰 관점을 먼저 떠올려야 하는가`로 구체화했다.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
