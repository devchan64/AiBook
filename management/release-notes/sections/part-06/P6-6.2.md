# Section Release Note

- Section ID: `P6-6.2`
- Source File: `docs/parts/part-06/chapter-06/section-02.md`

### v2026.07.18

- 체크리스트 정리: 체크리스트 안에 섞여 있던 요약 표, 전환 문단, 중복 요약 항목을 걷고 현재 Section 중심 질문을 닫는 자기 확인 항목만 남겼다.
- 정리 반영: 도입 질문 앞의 `이 절에서는 다음 질문을 다룹니다` 문장을 내용 중심 표현으로 낮췄다.
- 본문 반영: 적용 표 머리글의 `초심자가 먼저 하기 쉬운 오해`를 `먼저 떠올리기 쉬운 오해`로 낮춰, 스케일 판단이 독자 표지보다 능력·비용·검증 부담 구분에 바로 붙게 다듬었다.; `## 이 절의 범위`, `## 이 절의 목표`, `## 이 절을 읽는 순서` 같은 메타 제목을 `규모 확대 판단이 다루는 질문`, `여기서 남겨야 할 구분`, `능력과 부담을 보는 순서`로 바꿔, 큰 모델 일반론보다 `무엇이 함께 커지고 무엇이 함께 부담이 되는가`가 먼저 보이게 정리했다.; 도입부를 다시 써서 `사전학습 다음에 왜 바로 스케일을 읽는가`, `넓은 기반을 만든다는 말과 큰 모델이 중요하다는 말을 왜 분리해야 하는가`, `이 절이 큰 모델 예찬이 아니라 능력과 비용을 함께 읽는 자리라는 점`이 먼저 보이도록 Chapter 6의 handoff를 더 분명하게 다듬었다.; 사례 표 뒤에 `바로 적용해 보면` 표를 추가해, 초심자가 `긴 계약서와 긴 로그를 함께 읽는 요청 실패`, `답 품질은 괜찮아졌지만 비용과 지연이 급증한 장면`, `데이터는 늘었지만 검증 묶음도 크게 늘어난 장면`을 볼 때 무엇을 `더 큰 능력 필요`, `운영 한도 문제`, `검증 책임 증가`로 먼저 읽어야 하는지 바로 점검하게 했다.
- 추가 반영: 예제 뒤에도 `왜 더 큰 모델이 어떤 요청에서는 필요해 보이는가`, `왜 성능이 좋아져도 바로 운영 최적해가 되지 않는가`, `왜 데이터가 많아질수록 검증 부담도 같이 커지는가`를 다시 묻는 정리 표를 덧붙여, 이 절의 닫힘을 `성능 가능성`과 `비용·검증 부담`의 동시 판단 쪽에 더 강하게 고정했다.; 도입부의 `이 절은 ...`, `서비스 손잡이` 같은 메타 표현을 줄이고, `규모가 커질수록 능력과 부담이 함께 바뀐다`는 현재 절 핵심이 바로 보이도록 정리했다.; Chapter 6 형식 정합성 점검에 맞춰 현재 절의 제목 아래 `Section ID`, `Version` 표기도 저장소 규칙 형식으로 다시 확인했다.
- 번역 동기화 메모: 대응 번역본이 생기면 적용 표와 예제 뒤 정리 표도 함께 반영해야 한다.

### v2026.07.14
- 본문 반영: 예제를 `스케일 단계별 trade-off 추적`으로 교체했다. `small -> medium -> large` 단계에서 데이터 양, 파라미터 수, 학습 계산량, context window, 처리 가능한 요청 수, 총 추론 비용, 지연 시간, 데이터 검증 묶음이 어떻게 함께 달라지는지 출력하도록 바꿨다.
- 추가 반영: 예제 출력 뒤에 처리 가능한 요청 수, 총 추론 비용, 데이터 검증 부담 그래프를 분리해 배치했다. 통합 그래프 대신 축별 PNG를 나누어 스케일의 이득과 부담을 따로 읽게 했다.
- 번역 동기화 메모: Korean source now replaces the arbitrary quality-score example with a scale-step trade-off trace and three separated chart assets. Future translations should preserve the split between context coverage, inference cost, and data review burden.

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 스케일의 양면성에 대한 커리큘럼 문단을 목표 뒤로 옮겼으며, 종결부는 `언제 스케일 관점을 먼저 떠올려야 하는가` 하나로 닫히도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.17
- 본문 반영: `이 절의 범위`를 생략 항목 목록보다 `스케일을 성능 가능성과 비용 부담이 함께 커지는 구조로 읽는 일`이라는 현재 절의 역할이 먼저 보이도록 다시 정리했다.
- 본문 반영: 다음 토큰 예측 목표와 운영 제약은 뒤 절 회수로 남기고, scaling law 수식·GPU 클러스터 설계·최신 상용 모델 수치 비교는 현재 본편 범위 밖 항목으로만 분리했다.
- 추가 반영: `현재 본편 범위 밖` 문장을 삭제하고, 스케일을 기회와 제약이 함께 있는 구조로 읽는 현재 절의 역할이 바로 이어지도록 범위 안내를 더 짧게 다듬었다.
