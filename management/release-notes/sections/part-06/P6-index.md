# Section Release Note

- Section ID: `P6-index`
- Source File: `docs/parts/part-06/index.md`

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: Chapter 1 재구성에 맞춰 Part 6 개요의 초반 설명도 다시 정리했다. `P6-1.1`은 필요성, `P6-1.2`는 토큰 정의라는 점을 드러내고, Chapter 1 전체 흐름을 `필요성 -> 정의 -> 사용 -> 토큰화 정의 -> 토큰화 영향 -> 종류 소개 -> 차이 관찰 -> 활용` 기준으로 다시 설명했다.
- 번역 동기화 메모: 영어판과 중국어 간체판에 같은 메타 표지 제거와 현재 Part 6 시작 페이지의 주 흐름을 반영했다. / reflected in English and Simplified Chinese on 2026-07-12

### v2026.07.17
- 본문 반영: Part 6 시작 페이지의 범위 안내에서 `깊게 다루지 않는 목록`을 걷어내고, 이 Part가 실제로 회수하는 `모델 구조 -> 생성 메커니즘 -> 서비스 연결 -> 평가와 운영` 축이 먼저 보이도록 다시 정리했다.
- 추가 반영: 최신 제품 경쟁이나 구현 세부를 앞세우지 않고, 각 장에서 실제로 닫히는 LLM 학습 흐름과 서비스 연결 흐름이 시작 페이지에서 바로 보이게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the Part 6 overview as a map of actual LLM/service recovery axes rather than omitted-product-detail notes.

### v2026.07.18
- 추가 반영: 시작 페이지 본문에 `전반부 / 후반부 / 배경 축` 표를 덧붙여, Part 6가 `모델 내부 이해 -> 서비스 연결 -> 배경 축`으로 읽히는 구조라는 점을 더 직접 드러냈다.
- 추가 반영: 시작 페이지에 `지금 익숙한 사용 경험 -> 실제로 먼저 바꿔 읽어야 할 질문 -> 먼저 들어갈 Chapter 묶음` 표를 덧붙여, 초심자가 기능 이름 친숙함 때문에 Part 6의 필요성을 놓치지 않고 현재 막힘을 구조 질문으로 먼저 가를 수 있게 했다.
- 추가 반영: 한국어 시작 페이지와 영어·중국어 번역본 모두 제목 아래 메타데이터를 저장소 공통 형식인 `Section ID`, `Version` 평문 두 줄로 바로잡아 공개 진입 문서의 형식 기준을 맞췄다.
- 본문 반영: 시작 페이지에서 `목적`, `범위`, `회수 책임`, `필요성`을 여러 번 반복하던 구간을 압축해, 본류와 배경 축, 그리고 이 Part가 실제로 닫아야 할 질문이 먼저 보이게 정리했다.
- 추가 반영: 같은 구조 설명을 다시 늘어놓던 `무엇을 설명하는가`, `이 파트를 마치면 생기는 이해` 계열 문단을 걷어내고, `이 Part가 왜 필요한가`를 모델 내부 원리와 서비스 연결을 구분하는 문제로 더 직접 묶었다.
- 추가 반영: `지금 읽는 층위는`처럼 층위 안내를 메타 문장으로 먼저 세우기보다, 바로 아래 표를 읽기 기준으로 삼도록 도입 문장을 더 직접적으로 다듬었다.
- 추가 반영: `이 파트를 읽는 순서` 안내를 걷고, `생성 구조 혼선`, `RAG/tool/agent 경계 혼선`, `품질과 운영 혼선`, `본류와 배경 축 혼선`을 먼저 가르는 진입 분기 표로 바꿔 시작 페이지가 초심자 구조 질문의 입구로 바로 작동하게 정리했다.
- 추가 반영: 같은 날짜 재검토에서 Part 6 시작 페이지가 마무리 페이지보다 후반 본류를 더 느슨하게 요약하고 있던 차이를 줄이기 위해, `evaluation/operations` 설명과 요청 본류 흐름 표, `Part 6에서 함께 점검해야 하는 대표 판단 기준` 표에 `답변 상태`, `사람 검토 필요 여부`, `요청 기록` 관점을 더 직접적으로 반영했다.
- 추가 반영: `이 파트`, `초심자` 같은 진행 해설 표지를 줄이고, 시작 페이지가 `구조 안내`보다 `본류와 배경 축을 어떤 질문으로 읽을 것인가`를 먼저 보여 주도록 문장을 더 직접적으로 다듬었다.
- 추가 반영: `바로 들어가기 전에 먼저 가를 질문`처럼 읽기 진행을 드러내는 제목을 `먼저 가를 구조 질문`으로 바꾸고, 도입 문장도 `지금 막힌 장면을 어떤 구조 질문으로 바꿔 읽을 것인가`에 바로 붙도록 정리했다.
- 번역 동기화 메모: 영어판과 중국어 간체판에도 같은 메타데이터 형식 정리와 공개 진입 문서 기준을 반영했다. / reflected metadata-format normalization and public-entry conventions in English and Simplified Chinese.
