# Section Release Note

- Section ID: `P6-13.2`
- Source File: `docs/parts/part-06/chapter-13/section-02.md`

### v2026.07.17
- 본문 반영: `이 절에서는 다음 내용을 깊게 다루지 않습니다` 목록을 제거하고, 이번 절이 단일 에이전트 루프를 `계획-행동-관찰 반복`으로 먼저 닫는다는 현재 역할이 먼저 보이도록 범위 문단을 다시 정리했다.
- 본문 반영: `P6-14.1`, `P6-14.2`로 이어지는 도구 연결 규칙과 실행 환경 handoff만 남기고, 탐색 전략 수식·다중 agent 패턴·장기 메모리 설계 같은 바깥 항목 나열은 제거했다. 본문 메타데이터 `Version`도 `v2026.07.17`로 갱신했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff structure where this section closes the plan-action-observation loop first and hands connection and runtime management to later sections.

### v2026.07.18
- 본문 반영: `## 이 절의 범위`, `## 이 절의 목표` 같은 메타 소제목을 `반복 루프가 맡는 일`, `여기서 남겨야 할 구분`으로 바꾸고, `초심자 기준에서는` 문장을 직접 설명형으로 정리했다.; 도입부에서 `기본 정의를 다시 길게 반복하지 않습니다`, `커리큘럼 관점`, `읽는 순서` 같은 메타 안내를 더 줄이고, `관찰 결과 -> 다음 결정 -> 왜 이렇게 갈라지는가` 표를 앞쪽에 넣어 초심자가 loop를 용어 목록보다 분기 구조로 먼저 읽게 정리했다.; 제목 아래 `Section ID`, `Version` 표기를 저장소 공통 형식에 맞춰 blockquote 대신 일반 메타데이터 줄로 정리했다.; `목표 흐름을 안다`와 `실제로 언제 계속하고 멈출지 안다`를 분리해 읽게 하려고, `지금 부족한 것 -> 이번 장이 붙이는 구조 -> 아직 남는 문제 -> 바로 이어지는 위치` 비교 블록을 추가했다.
- 추가 반영: `이 절은 다음 질문에 답합니다` 문장을 `이 절에서 먼저 붙잡을 질문`으로 바꿔, 반복 루프 설명의 도입 톤을 더 자연스럽게 낮췄다.; `이 절은 Part 6에서 ...`, `여기서 먼저 남겨야 할 것은 ...` 같은 메타 서술을 더 직접적인 설명 문장으로 바꿔, 반복 루프와 종료 기록이 바로 읽히게 정리했다.; `지금 읽는 층위는`으로 절의 역할을 따로 설명하던 문장을 줄이고, 목표 흐름에서 반복 루프로 넘어가는 전환이 더 바로 보이도록 앞부분 연결을 다듬었다.; 세 사례 요약 뒤에 `바로 적용해 보면` 블록을 추가해, 초심자가 `계속 진행`, `종료`, `사람 검토 전환`을 가르는 관찰 기준과 stop condition을 실제 점검 질문으로 읽도록 보강했다.
- 번역 동기화 메모: Future translations should preserve the new comparison block that separates loop control from later runtime/evaluation layers.

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.09
- 본문 반영: `커리큘럼 관점` 문단을 목표 뒤로 옮기고, `여기까지를 한 줄로 묶으면`을 `다음 절과의 연결` 앞쪽으로 이동했으며, `언제 ... 떠올려야 하는가` 표지를 종결부 하나로 합쳐 마무리 순서를 정리했고, 구조 표지를 `사례 및 예시`, `연습 및 예제` 기준으로 맞춤.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.11
- 본문 반영: 루프가 직접 다루는 것과 MCP/하네스 쪽으로 넘겨야 하는 것을 나누는 비교표를 추가했고, 종결 표지를 `체크리스트`로 통일함. 추가로 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
