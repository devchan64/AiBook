# Section Release Note

- Section ID: `P6-6.1`
- Source File: `docs/parts/part-06/chapter-06/section-01.md`

### v2026.07.18
- 추가 반영: 도입부를 다시 써서 `생성 구조를 이해한 뒤에도 왜 학습 축을 다시 읽어야 하는가`, `왜 같은 생성 구조라도 기본 반응 범위가 달라지는가`, `왜 바로 다음 절에서 스케일을 읽게 되는가`가 먼저 보이도록 Chapter 6의 진입 흐름을 더 강하게 세웠다.
- 추가 반영: 사례 요약 뒤와 예제 앞에 `지금 먼저 보이는 신호 -> 먼저 의심할 층위 -> 바로 다음에 붙여 볼 질문` 표를 더해, 초심자가 `넓은 기반`, `과업 조정`, `요청 형식 조정`을 더 짧게 가르도록 보강했다.
- 본문 반영: 사례 요약 뒤에 `바로 적용해 보면` 표를 추가해, 초심자가 `요약 데이터는 조금 있는데 문장 연결이 흔들리는 장면`, `표현이 다른 같은 문의를 자주 놓치는 분류 장면`, `기본 설명은 되는데 요청 형식을 자주 어기는 장면`을 볼 때 무엇을 `넓은 기반 학습`, `기반 위 과업 조정`, `지시 조정층 문제`로 먼저 읽어야 하는지 바로 점검하게 했다.
- 추가 반영: 예제 뒤에도 `왜 요약 예시가 조금 있어도 문장 연결 자체가 흔들리는가`, `왜 라벨 정의는 분명한데 표현이 다른 같은 문의를 놓치는가`, `왜 기본 설명은 되는데 요청 형식을 자주 어기는가`를 다시 묻는 정리 표를 덧붙여, 이 절의 닫힘을 `사전학습`, `파인튜닝`, `지시 튜닝` 구분 쪽에 더 강하게 고정했다.
- 추가 반영: 도입부의 `이 절은 ...`, `학습과 조정의 출발점`, `assistant-like` 같은 메타 표현을 줄이고, `먼저 넓은 언어 기반을 배우고 나중에 목적별 조정을 얹는 구조`가 더 직접 보이도록 다듬었다.
- 추가 반영: 제목 아래 `Section ID`, `Version` 표기를 저장소 규칙 형식으로 바로잡았다.
- 번역 동기화 메모: 대응 번역본이 생기면 적용 표와 예제 뒤 정리 표도 함께 반영해야 한다.

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, `커리큘럼 관점` 문단을 목표 뒤로 옮겼으며, 종결부는 `언제 사전학습 관점을 먼저 떠올려야 하는가` 하나로 닫히도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.17
- 본문 반영: `이 절의 범위`를 `깊게 다루지 않습니다` 중심 생략 공지보다, 이번 절이 사전학습을 기반 학습 단계로 먼저 닫고 다음 절 `P6-6.2`와 뒤 장들로 질문을 넘기는 handoff 구조가 먼저 보이도록 다시 정리했다.
- 본문 반영: 스케일은 `다음 절`, 지시 튜닝과 정렬은 `뒤 장`, 손실 함수 수식·분산 학습 인프라·RLHF 세부 절차는 `현재 범위 밖`으로 분리했다.
- 추가 반영: `현재 본편 범위 밖` 문장을 삭제하고, 사전학습을 먼저 닫는 역할과 `P6-6.2`, `P6-8.*` handoff만 남기도록 범위 문장을 더 단순화했다.
