# Section Release Note

- Section ID: `P6-6.1`
- Source File: `docs/parts/part-06/chapter-06/section-01.md`

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, `커리큘럼 관점` 문단을 목표 뒤로 옮겼으며, 종결부는 `언제 사전학습 관점을 먼저 떠올려야 하는가` 하나로 닫히도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.17
- 본문 반영: `이 절의 범위`를 `깊게 다루지 않습니다` 중심 생략 공지보다, 이번 절이 사전학습을 기반 학습 단계로 먼저 닫고 다음 절 `P6-6.2`와 뒤 장들로 질문을 넘기는 handoff 구조가 먼저 보이도록 다시 정리했다.
- 본문 반영: 스케일은 `다음 절`, 지시 튜닝과 정렬은 `뒤 장`, 손실 함수 수식·분산 학습 인프라·RLHF 세부 절차는 `현재 범위 밖`으로 분리했다.
- 추가 반영: `현재 본편 범위 밖` 문장을 삭제하고, 사전학습을 먼저 닫는 역할과 `P6-6.2`, `P6-8.*` handoff만 남기도록 범위 문장을 더 단순화했다.
