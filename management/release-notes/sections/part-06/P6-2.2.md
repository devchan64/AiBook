# Section Release Note

- Section ID: `P6-2.2`
- Source File: `docs/parts/part-06/chapter-02/section-02.md`

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, `커리큘럼 관점` 문단을 목표 뒤로 옮겼으며, 종결부는 `언제 의미와 거리 비교 관점을 먼저 떠올려야 하는가` 하나로 닫히도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.17
- 본문 반영: `이 절의 범위`를 생략 항목 목록보다 `임베딩 벡터를 어떤 비교 기준으로 읽을 것인가`라는 현재 절의 역할이 먼저 보이도록 다시 정리했다.
- 본문 반영: P6-2.3은 거리 직관 보강 handoff로, P6-11은 검색 사용 맥락 회수로 남기고, 유사도 수식 전개·nearest neighbor 구현·ANN index 구조는 현재 본편 범위 밖 항목으로만 분리했다.
- 본문 반영: 범위 마무리 문장에서 `현재 본편 범위 밖` 선언을 줄이고, 이번 절이 `후보 비교 손잡이`를 먼저 닫는다는 현재 역할이 더 직접 보이도록 문장을 다듬었다.

### v2026.07.18
- 본문 반영: 사례 뒤에 `바로 적용해 보면` 표를 추가해, 초심자가 `distance 1등`, `top-k 후보`, `질문과 비슷한 문서`를 볼 때 각각 무엇을 `1차 후보 선정`, `후보 재검토`, `관련성 확인`으로 읽어야 하는지 바로 점검하게 했다.
- 추가 반영: 예제 뒤에도 `가까운 후보`, `더 최신인 2등 후보`, `질문과 비슷한 문서`를 각각 어떤 질문으로 다시 읽어야 하는지 정리 표를 덧붙여, 이 절의 닫힘을 `가까움` 자체보다 `후보 선정 -> 후보 검토 -> 최종 근거 확정` 단계 구분 쪽에 더 강하게 고정했다.
- 번역 동기화 메모: 대응 번역본이 생기면 적용 표와 예제 뒤 마무리 표도 함께 반영해야 한다.
