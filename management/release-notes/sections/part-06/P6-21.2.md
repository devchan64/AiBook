# P6-21.2 릴리즈 노트

- Section ID: `P6-21.2`
- Source File: `docs/parts/part-06/chapter-21/section-02.md`

## v2026.08.28

- 변경 이유: 공개 디퓨전 모델의 실제 비교를 Part 6 안에서 완료하도록 P6-21.3을 신설해, 기존 Part 7 연결을 현재 학습 경로에 맞출 필요가 있었다.
- 본문 반영: 다음 실행 연결을 P6-21.3의 한 변수 비교로 바꾸고, 체크리스트도 실행 조건·품질 검수를 그 기록에 남기는 기준으로 수정했다.
- 번역 동기화 메모: 영어·중국어 간체판은 원문 변경을 후속 동기화해야 한다.
- 번역 반영 상태: 한국어 원문 반영 완료, 영어판·중국어 간체판 반영 예정.
- 관련 자산: 없음.
- 원문 기준 버전: `docs/parts/part-06/chapter-21/section-02.md` `v2026.08.28`

## v2026.08.11

- 변경 이유: 메모리 배치 설명 뒤에 오류·속도·품질을 실제 다음 실행의 선택으로 연결해, 실행 개념의 학습 밀도를 독자 판단으로 전환할 필요가 있었다. CPU offloading의 사용 조건과 `empty_cache()` 설명을 공식 문서 기준으로 더 정확히 다듬을 필요가 있었다.
- 본문 반영: GPU OOM, 과도한 지연, 품질 미달을 구분하고 각 신호에 대해 한 번에 하나의 변경 축을 고르는 표와 체크리스트 항목을 추가했다.
- 번역 동기화 메모: 한국어 원문 `v2026.08.11` 기준으로 영문·중문 본문에 같은 판단 표와 체크리스트를 반영했다.
- 번역 반영 상태: 한국어 원문 / 영문 / 중문 번역 반영 완료
- 외부 근거 반영: Diffusers와 Accelerate의 CPU offloading·`device_map` 문서, PyTorch의 `empty_cache()` API 문서를 재확인하고 확인일을 갱신했다.
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-06/chapter-21/section-02.md` `v2026.08.11`

## v2026.08.07

- 변경 이유: CPU offloading이 P7 이미지 생성 실험에서 반복되는 실행 개념이어서, Part 6의 오픈웨이트 모델 장에서 사전 개념으로 분리했다.
- 본문 반영: GPU VRAM, CPU RAM, dtype, 양자화, CPU offloading, 입력 규모 축소를 구분하고, 실행 가능성 gate와 품질 gate를 분리해 기록하는 기준을 정리했다.
- 목차 반영: `mkdocs.yml`과 `docs/table-of-contents.md`, 영어·중국어 독자용 목차에 `P6-21.2 로컬 실행 환경과 메모리 배치` 항목을 추가했다.
- 개념사전 반영: `CPU offloading(CPU 오프로딩)`의 중심 Section을 `P6-21.2`로 두고, P7 이미지 생성 실험 절을 등장 Section으로 연결했다.
- 외부 근거 반영: Hugging Face Diffusers의 메모리 최적화와 pipeline offloading 설명, Hugging Face Accelerate의 big model offload 설명을 참고 자료로 연결했다.
- 번역 동기화 메모: 한국어 원문 `v2026.08.07` 기준으로 영어·중국어 본문 번역을 추가했다. 개념사전 3개 언어 항목과 다국어 목차도 함께 연결했다.
- 번역 반영 상태: 한국어 원문 / 영문 / 중문 번역 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-06/chapter-21/section-02.md` `v2026.08.07`

## v2026.08.10

- 변경 이유: P7-5.1~P7-5.4의 실제 순차 CPU offload 사용 경로와 대조해, 독자가 실행 시 빠뜨릴 수 있는 설정 순서와 메모리 운용 구분을 보강했다.
- 본문 반영: 추가 구성요소 연결 뒤 `enable_sequential_cpu_offload()`를 한 번 설정하는 순서, `pipe.to("cuda")`와의 비호환적 사용, `device_map` 해제 후 선택, `torch.cuda.empty_cache()`와 offload의 차이를 추가했다. 실행 기록 양식과 체크리스트에도 관련 항목을 넣었다.
- 외부 근거 반영: Hugging Face Diffusers의 최신 메모리 절감 문서와 PyTorch CUDA 캐시 문서를 확인해 순차 offload 및 빈 캐시 반환의 역할을 대조했다.
- 번역 동기화 메모: 한국어 원문 `v2026.08.10` 기준으로 영어·중국어 본문에 같은 실행 순서와 기록 기준을 반영했다.
- 번역 반영 상태: 한국어 원문 / 영문 / 중문 번역 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-06/chapter-21/section-02.md` `v2026.08.10`
