# Section Release Note

- Section ID: `P6-18.2`
- Source File: `docs/parts/part-06/chapter-18/section-02.md`

### v2026.07.19
- 입력 데이터 분리: 계보 판정 항목을 `docs/assets/part-06/chapter-18/p6-18-lineage-items.csv`로 추가하고, Python 예제가 실제 CSV를 읽어 분류하도록 바꿨다.
- Python 예제 보강: direct lineage 판정 기준을 `lineage_rules` 조작값으로 분리하고, 각 항목의 `domain_ok`, `target_ok`, `connection_ok` 점검값이 출력되도록 바꿔 고정 분류표처럼 읽히지 않게 했다.
- 가이드라인 반영: `direct_targets`, `requires_transformer_connection`, 입력 항목을 바꾸며 분류 경계가 어떻게 달라지는지 확인할 수 있는 조정 안내를 추가했다.
- 차트 보강: Python 예제의 `domain_ok`, `target_ok`, `connection_ok` 판정값을 행렬 차트로 추가해, 직접 계보와 주변 근거가 이름이 아니라 기준 통과 여부로 갈린다는 점을 시각화했다.
- 검토 결과: 직접 계보와 주변 근거를 구분하는 현재 본문 구조를 유지했다.
- 메타데이터: 본문 `Version`을 `v2026.07.19`로 갱신했다.
- 예제 검증: Python 예제 실행 결과가 문서의 출력 예시와 일치함을 확인했다.
- 번역 동기화 메모: Future translations should preserve the distinction between direct lineage and surrounding evidence.

### v2026.07.18

- 체크리스트 정리: 체크리스트 안에 섞여 있던 요약 표, 전환 문단, 중복 요약 항목을 걷고 현재 Section 중심 질문을 닫는 자기 확인 항목만 남겼다.
- 본문 반영: `## 이 절의 범위`, `## 이 절의 목표` 같은 메타 제목을 `직접 계보 구분이 다루는 질문`, `여기서 남겨야 할 구분`으로 바꾸고, `초심자에게 여기서 더 필요한 것은` 같은 진행형 문장을 줄여 direct lineage 구분 기준이 먼저 보이게 정리했다.; 도입부의 `정리 단계`, `배경 장` 같은 위치 설명을 줄이고, `무엇이 direct lineage이고 무엇이 surrounding evidence인가`를 먼저 읽게 하는 방향으로 문장 중심축을 더 직접적으로 정리했다.; 중간 소제목 `이 절을 어디까지 읽으면 충분한가`를 `무엇을 먼저 구분하면 되나`로 바꾸고, 종결부의 배경 장 설명도 `Transformer·GPT·BERT를 읽을 때 무엇을 섞지 말아야 하는가`라는 실제 판단 기준으로 다시 고정했다.; 세 사례 요약 뒤에 `바로 적용해 보면` 블록을 추가해, 초심자가 `유명한 사건`, `계산 조건`, `동시 유행`을 곧바로 direct lineage로 묶지 않고 `직접 구조 계보`와 `주변 근거`를 먼저 가르도록 보강했다.
- 추가 반영: 도입부의 `이 절은 ...`, `배경 층위` 같은 메타 문장을 줄이고, `현재 LLM 구조와 직접 이어지는 흐름이 무엇인가`가 먼저 보이도록 정리했다.; `이 절에서는 다음 질문을 다룹니다`, `그렇다면` 같은 진행형 문장을 걷고, direct lineage와 surrounding evidence를 먼저 가르는 기준이 바로 앞에 오도록 다듬었다.; 제목 아래 메타데이터를 저장소 공통 형식인 `Section ID`, `Version` 평문 두 줄로 바로잡아 배경 축 문서도 같은 형식 기준을 따르게 했다.; 사례와 `바로 적용해 보면`에 남아 있던 `초심자가 ...` 독자 표지를 `여기서 바로잡아야 할 오해`, `자주 생기는 오해` 중심 표현으로 낮춰, 배경 비교 설명이 메타 독자 안내보다 direct lineage 판단 기준에 더 직접 붙게 다듬었다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 직접 적용 표와 질문 표를 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절에서는 다음 내용을 깊게 다루지 않습니다` 목록을 제거하고, 이번 절이 직접 계보와 주변 근거를 가르는 기준을 먼저 닫는다는 현재 역할이 먼저 보이도록 범위 문단을 다시 정리했다.
- 본문 반영: `P6-3.1`, `P6-4.1`, `P6-19.1`로 다시 연결되는 실제 handoff만 남기고, 주변 분야 논문 열거·도메인별 상세 발전사·최신 계보 전수조사 같은 바깥 항목 나열은 제거했다. 본문 메타데이터 `Version`도 `v2026.07.17`로 갱신했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff structure where this section closes the direct-lineage distinction first and reuses it in later structure comparisons.

### v2026.07.12
- 추가 반영: `본류와의 연결`을 `직접 계보 구분이 본류 해석을 어떻게 바꾸는가`로 바꿔 비교 배경 장의 설명 초점을 더 직접 드러내게 했다.
- 본문 반영: `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 종결부에서는 `언제 직접 계보와 주변 근거 구분을 먼저 떠올려야 하는가`만 남기도록 일반형 `언제 이 관점을 먼저 떠올려야 하는가`를 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
