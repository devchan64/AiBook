# Section Release Note

- Section ID: `P6-16.2`
- Source File: `docs/parts/part-06/chapter-16/section-02.md`

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 온콜 조직, 장애 등급, 관측성 스택 같은 생략 목록을 걷어내고, 이번 절이 `어디를 추적해야 실패 원인을 구분할 것인가`와 `retry/fallback/stop/approval 중 어디로 보낼 것인가`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 운영 판단의 후속 handoff를 `P6-17.1`, `P6-17.2`의 요청 흐름과 회고 기록으로 직접 연결했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P6-16.2 closes failure-routing judgment first and hands the resulting record structure to P6-17.

### v2026.07.18
- 추가 반영: 제목 아래 `Section ID`, `Version` 표기를 저장소 공통 형식에 맞춰 blockquote 대신 일반 메타데이터 줄로 정리했다.
- 본문 반영: 평가와 운영 한도를 알아도 왜 실패 경로 판단이 별도로 더 필요한지 먼저 보이게 하려고, `지금 부족한 것 -> 이번 장이 붙이는 구조 -> 아직 남는 문제 -> 바로 이어지는 위치` 비교 블록을 추가했다.
- 본문 반영: `이 절은 다음 질문에 답합니다` 문장을 `이 절에서 먼저 붙잡을 질문`으로 바꿔, 실패 대응 장의 도입이 작업 메모처럼 읽히지 않게 정리했다.
- 추가 반영: `이 절은 Part 6에서 ...`와 `이 절에서는 ...로 설명합니다` 같은 메타 서술을 더 직접적인 설명 문장으로 바꿔, 실패 경로 판단이 바로 읽히게 정리했다.
- 추가 반영: `지금 읽는 층위는`, `이 절의 핵심은`처럼 절의 역할을 따로 표지하던 문장을 줄이고, 좋은 답 판단에서 실패 경로 결정으로 넘어가는 전환이 더 직접적으로 보이도록 앞부분과 종결부를 다듬었다.
- 추가 반영: 세 사례 요약 뒤에 `바로 적용해 보면` 블록을 추가해, 초심자가 `retry`, `fallback`, `stop`, `approval`을 실제 분기 질문으로 읽고 실패 대응 경로를 먼저 가르도록 보강했다.
- 추가 반영: 예제 실행 결과 바로 아래에 `failure_case`별 운영 경로 대응표를 덧붙여, `retry`, `fallback`, `stop`, 사람 검토, 모델 수정 중 어느 갈래로 보내야 하는지 각 실행 이름만 보고도 바로 읽히게 정리했다.
- 번역 동기화 메모: Future translations should preserve the new comparison block that separates failure routing from later request-flow and retrospective records.

### v2026.07.11
- 본문 반영: `실패 유형 -> retry/fallback/stop/approval` 1차 triage 표와 정리 문장을 추가했고, 이어서 `먼저 보인 신호 -> 1차 triage` 운영 표를 더해 일시 오류, 검색 실패, 권한 실패, 모델 실패를 서로 다른 운영 경로로 읽는 기준을 더 빠르게 닫히게 보강함. 종결 표지는 저장소 공통 기준에 맞춰 `체크리스트`로 통일함. 추가로 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 종결부에서는 `언제 실패 대응 관점을 먼저 떠올려야 하는가`만 남기도록 일반형 `언제 이 관점을 먼저 떠올려야 하는가`를 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
