# P6-16.2 릴리즈 노트

- Section ID: `P6-16.2`
- Source File: `docs/parts/part-06/chapter-16/section-02.md`

## v2026.07.24

- 변경 이유: P6-16.2가 자동 평가와 사람 평가의 분업을 다루므로, 출처 블록도 최신 OpenAI Datasets의 annotation/graders 설명, Graders의 자동 평가기 유형 설명, agent trace/eval 설명을 직접 가리키는 편이 더 적절하다고 판단했다.
- 본문 반영 내용: Section 버전을 `v2026.07.24`로 올리고, 참고 자료에서 `Working with evals` 대신 `Getting started with datasets`를 주 근거로 반영했다. 자동 채점기(automatic grader)의 직접 근거로 `Graders` 문서도 추가했다. 예제는 코드 채점기와 선택적 LLM 판정기(LLM-as-a-judge grader)를 함께 기록하고, LLM 판정기의 이유가 비거나 약하면 코드 채점기 관측값으로 보강한 `reason_source`를 출력하도록 수정했다. 확인 날짜도 2026-07-24로 갱신했다.
- 추가 반영: 에이전트 실행 사례에서 비용·지연 최적화 설명을 줄이고, P6-16.2의 범위인 자동 신호와 사람 해석의 분업에 맞췄다. 예제 코드에는 `GRADER_AXIS_MAP`을 추가해 자동 채점기 항목이 P6-16.1의 근거성·유용성·형식 적합성·안전성 축과 어떻게 이어지는지 드러내고, 실제 출력의 수정 메모에도 평가 축을 함께 표시했다.
- 근거 확인: OpenAI Datasets 문서의 annotation, subject matter expert annotation, graders at scale 설명이 사람 평가와 자동 평가 분업을 직접 지지한다. OpenAI Graders 문서는 grader가 reference answer와 model-generated answer를 비교해 0~1 grade를 반환하고, string check, text similarity, score model, label model, Python code execution 같은 유형이 있음을 설명하므로 자동 채점기의 직접 근거가 된다. OpenAI Agent evals 문서는 traces, graders, datasets, eval runs가 agent quality 개선에 쓰이고 trace가 model calls, tool calls, guardrails, handoffs 기록을 담는다는 점에서 하네스/기록 연결 문장을 지지한다. Chang et al.의 LLM evaluation survey는 LLM 평가가 task, risk, benchmark, method 차원을 포함하는 넓은 평가 분야라는 배경 근거로 유지한다.
- 번역 동기화 메모: 향후 번역본도 P6-16.2 출처는 Datasets annotation/graders, Graders 유형, Agent eval traces, LLM evaluation survey 순서로 맞춘다.
- 번역 반영 상태: 한국어 원문과 공용 Python/차트 자산만 수정했다.
- 원문 기준 버전: `v2026.07.24`

## v2026.07.23

- 변경 이유: Python 예제가 코드 안의 네 개 답변 후보를 정해진 규칙으로 다시 분류하는 구조라 자동 평가와 사람 평가의 실제 분업보다 정답 확인형 예제로 보였기 때문.
- 본문 반영 내용: 연습 및 예제를 CSV 기반 LLM 출력 후보 36개로 바꾸고, 자동 채점기는 형식·근거 신호·필수 행동·금지 표현을 계산하며 사람 평가는 코드가 대신 채점하지 않고 검토 패킷과 큐로 남기도록 재구성했다. CSV에서 사전 위험 라벨과 사람 검토 라벨을 빼 정답 확인형 라우팅처럼 보이는 문제를 줄였다.
- 자산 반영: `p6_16_2_eval_routing_cases.csv`, `p6_16_2_eval_routing_cases.py`를 추가하고, `p6_16_2_auto_human_eval_chart.py`가 CSV 기반 summary로 차트를 생성하도록 수정했다. 차트는 자동 채점기와 검토 준비를 두 행의 누적 막대로 나누어 자동 통과가 승인이 아니라 사람 검토 큐로 넘어가는 중간 상태임을 드러내게 했다.
- 번역 동기화 메모: 향후 번역본도 CSV와 차트 데이터 흐름은 공용으로 유지하되, 본문 해설과 차트 라벨은 언어별로 맞춘다.
- 번역 반영 상태: 한국어 원문과 공용 자산만 수정했다.
- 원문 기준 버전: `v2026.07.23`

- 변경 이유: 장문형 Section 제목이 목차 가독성을 해치거나 본문 질문 전체를 제목에 떠안는 경우가 있어, 대표 제목과 선택적 보조제목 구조로 정리했다.
- 본문 반영 내용: H1과 nav 제목을 짧은 대표 제목으로 줄이고, 중심 질문 손실을 막기 위해 메타데이터 아래 보조제목을 추가했다. 본문 `Version`을 `v2026.07.23`으로 갱신했다.
- 번역 동기화 메모: 현재 대응 영어판·중국어 간체판 파일이 없어 원문 제목만 반영했다.
- 번역 반영 상태: ko 원문 반영, en/zh 파일 없음
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-06/chapter-16/section-02.md` `v2026.07.23`

## v2026.07.22

- 변경 이유: Part 6 본문에서 진행 메타형 소제목을 구체 개념, 비교 대상, 판단 기준이 드러나는 제목으로 정리하기 위함.
- 본문 반영: `여기서 남겨야 할 구분`, `여기까지를 한 줄로 묶으면`, `보는 순서`, `이 예제를 ... 다시 보면` 계열 제목을 현재 절의 실제 구분 축과 설명 흐름이 보이는 소제목으로 바꾸었다.
- 번역 동기화 메모: 향후 번역본도 진행 안내형 제목보다 현재 절의 구체 개념, 비교 대상, 판단 기준이 보이는 소제목으로 맞춘다.
- 번역 반영 상태: 한국어 원문만 수정했다.
- 원문 기준 버전: `v2026.07.22`

## v2026.07.21

- Part 6 개편 기준에 따라 Section ID, 파일 위치, 제목을 새 목차 구조에 맞춰 재정렬했다.
- 생성형 AI 산출물에서 시작해 LLM을 대표 사례로 읽는 흐름 안에서 이 Section의 위치를 다시 잡았다.
- 자동 평가와 사람 평가를 우열 비교가 아니라 같은 평가 축을 자동 채점기, 사람 검토, 검토 패킷으로 나누는 분업 구조로 다시 정리했다.
- 진행 메타 중심 문장을 줄이고, 형식 적합성·근거성·유용성·안전성별로 자동 평가가 맡기 쉬운 부분과 사람이 끝까지 봐야 하는 부분을 표로 보강했다.
- P6-16 전용 자동 평가·사람 평가 도식과 평가 경로 그래프 참조로 바꿔, 이전 장 자산을 빌려 쓰던 혼선을 줄였다.
- 개편 전 릴리즈노트 원문은 `management/authoring/archive/part-06-before-restructure-2026-07-21/management/release-notes/sections/part-06/`에 보존했다.
- `바로 적용해 보면` 제목을 자동 평가와 사람 평가를 나눌 장면으로 바꾸어 반복 가능한 표면 기준과 맥락 판단의 분업을 더 직접 드러냈다.
- RAG 답변, 에이전트 실행, 고객 지원 답변 사례에서 자동 점검으로 잡을 수 있는 신호와 사람 검토가 끝까지 봐야 하는 판단을 문단으로 분리했다.
- 제목을 질문형 중심축으로 바꾸고, 배포 전후 분업 설명과 적용 장면의 반복 표를 줄여 자동 채점기와 사람 검토의 역할 차이를 더 직접 읽게 정리했다.
