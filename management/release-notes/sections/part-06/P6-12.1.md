# Section Release Note

- Section ID: `P6-12.1`
- Source File: `docs/parts/part-06/chapter-12/section-01.md`

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 내용 전개는 유지하고 종결 표지를 저장소 공통 기준에 맞춰 `체크리스트`로 통일했으며, 본문 메타데이터 버전도 함께 갱신함. 추가로 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, 종결부에서는 `언제 도구 사용 관점을 먼저 떠올려야 하는가`만 남기도록 일반형 `언제 이 관점을 먼저 떠올려야 하는가`를 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.17
- 본문 반영: `이 절의 범위`를 `깊게 다루지 않습니다` 중심 생략 공지에서, 이번 절이 도구 사용의 실행 연결 구조를 먼저 닫고 다음 절 `P6-12.2`와 뒤 장들로 질문을 넘기는 handoff 구조가 먼저 보이도록 다시 정리했다.
- 본문 반영: 함수 호출, agent loop, 권한/운영 문제를 각각 `다음 절`, `뒤 장`, `현재 범위 밖`으로 분리해 한 문단 안에서 섞이지 않게 정리했다.
- 추가 반영: `agent loop 전체 구조, 권한 관리 시스템 세부, 특정 SDK의 구현 방식은 현재 본편 범위 밖`이라는 잔여 문장을 제거하고, 실제 회수 위치인 `P6-12.2`, `P6-13.*`, `P6-14.2`, `P6-16.2` handoff만 남기도록 범위 문단을 더 압축했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff structure where this section closes tool-use connection first and hands structured calls and multi-step loops to later sections.

### v2026.07.18
- 추가 반영: 제목 아래 `Section ID`, `Version` 표기를 저장소 공통 형식에 맞춰 blockquote 대신 일반 메타데이터 줄로 정리했다.
- 본문 반영: `문서를 읽는 것만으로는 무엇이 부족한가`를 먼저 보이게 하려고, `지금 부족한 것 -> 이번 장이 붙이는 구조 -> 아직 남는 문제 -> 바로 이어지는 위치` 비교 블록을 추가했다.
- 본문 반영: `이 절은 다음 질문에 답합니다` 문장을 `이 절에서 먼저 붙잡을 질문`으로 바꿔, 실행 구조 장의 도입 호흡을 공개 본문 기준으로 더 자연스럽게 정리했다.
- 추가 반영: `이 절은 Part 6에서 ...`, `여기서 먼저 남겨야 할 것은 ...` 같은 메타 서술을 더 직접적인 공개 본문 문장으로 바꿔, 도구 사용 설명과 기록 기준이 바로 읽히게 정리했다.
- 추가 반영: `지금 읽는 층위는`으로 절의 역할을 다시 말하던 문장을 줄이고, RAG에서 실행 연결로 넘어가는 질문이 바로 보이도록 앞부분 연결을 더 직접적으로 다듬었다.
- 추가 반영: `이 절을 읽는 순서` 안내를 걷고, `실시간 조회 필요`, `계산 정확도 문제`, `실제 실행 필요`, `읽기와 실행 경계 혼선`을 먼저 가르는 장면별 분기 표로 바꿔 초심자 첫 질문이 도구 사용 필요 판단으로 바로 이어지게 정리했다.
- 추가 반영: 세 사례 요약 뒤에 `바로 적용해 보면` 블록을 추가해, 초심자가 `문서 읽기`, `실시간 상태 조회`, `실제 실행`을 먼저 가르고 도구 사용 필요를 판단하도록 보강했다.
- 추가 반영: 같은 날짜 후속 재검토에서 본문이 세운 `조회`, `계산`, `실행` 축 가운데 `계산`이 직접 적용 질문에서는 약하게 보인다는 점을 보완하려고, `숫자 정확도가 핵심인 장면에서 계산 도구 결과를 먼저 가져와야 하는가`를 묻게 하는 행과 실무 질문을 추가했다.
- 번역 동기화 메모: Future translations should preserve the new comparison block that separates tool-use connection from later structured calling and agent flow.
