# Section Release Note

- Section ID: `P6-7.2`
- Source File: `docs/parts/part-06/chapter-07/section-02.md`

### v2026.07.19
- 메타데이터 정합성: 현재 순차 검토 결과 본문 구조는 유지하고, 본문 `Version`을 `v2026.07.19`로 갱신했다.
- 근거 정합성: 효율적 조정과 LoRA 설명의 참고 자료를 Houlsby adapter 논문, LoRA 논문, Hugging Face PEFT 공식 문서 링크로 구체화했다.
- 예제 정리: 연습 및 예제 도입부의 `입력`, `출력`, `문제 상황`, `입력(input)`, `확인할 개념` 표지를 줄이고, 업무 수가 늘어날 때 전체 미세조정과 PEFT/LoRA의 관리 비용이 갈라지는 흐름을 본문 설명으로 연결했다.
- Python 예제 검토: 로컬 `.venv`의 Python으로 예제를 실행해 본문 출력과 일치함을 확인했다.
- 검토 메모: LoRA를 작은 모델이 아니라 큰 기반 모델 위의 작은 조정분 운영 방식으로 읽게 하는 중심축은 유지한다.
- 학습 밀도 보강: Python 예제의 업무 수별 추가 저장 크기 비교를 PNG 차트 `lora-storage-growth-en.png` / `lora-storage-growth-ko.png`로 추가했다.
- 번역 동기화 메모: 대응 번역본이 생기면 본문 버전 갱신과 차트 연결 문단을 함께 반영해야 한다.

### v2026.07.18

- 체크리스트 정리: 체크리스트 안에 섞여 있던 요약 표, 전환 문단, 중복 요약 항목을 걷고 현재 Section 중심 질문을 닫는 자기 확인 항목만 남겼다.
- 추가 정리: 도입 질문 앞의 `이 절에서는 다음 질문을 다룹니다` 문장을 내용 중심 표현으로 낮췄다.
- 본문 반영: `바로 적용해 보면` 표의 `초심자가 먼저 하기 쉬운 오해` 머리글을 `먼저 떠올리기 쉬운 오해`로 낮춰, 지식 추가와 지시 형식 조정의 경계가 독자 라벨보다 현상 구분으로 먼저 읽히게 다듬었다.; `Part 6에서 ... 첫 상세 설명은 이 절에서 잡습니다`, `이 절은 Part 6에서 ...` 같은 위치 설명형 메타 문장을 걷고, `같은 큰 기반 모델을 더 적은 조정 비용으로 적응시킨다`는 핵심이 바로 보이도록 도입부를 더 직접적으로 정리했다.; `## 이 절의 범위`, `## 이 절의 목표`, `## 이 절을 읽는 순서` 같은 메타 제목을 `효율적 조정 비용이 다루는 질문`, `여기서 남겨야 할 구분`, `조정 비용 판단을 보는 순서`로 바꿔, 위치 설명보다 `전체를 다 건드리지 않고 무엇을 줄이는가`가 먼저 보이게 정리했다.; `adapter / LoRA / QLoRA` 세부 구분이 삭제된 것이 아니라 `P6-8.4`로 재배치된 상태라는 문장을 본문 앞쪽에 직접 덧붙여, Chapter 7만 읽는 독자도 보충절 위치를 놓치지 않게 정리했다.
- 추가 반영: 사례 요약 뒤에 `바로 적용해 보면` 표를 추가해, 초심자가 `같은 기반 모델로 여러 업무용 조정본을 만들어야 하는 장면`, `GPU 예산이 빠듯해 첫 실험이 무거운 장면`, `짧은 기간 안에 여러 조정 가설을 비교해야 하는 장면`을 볼 때 무엇을 `조정본 분리 관리`, `실험 진입 비용`, `가설 회전 속도` 문제로 먼저 읽어야 하는지 바로 점검하게 했다.; LoRA를 `작은 모델`이 아니라 `큰 기반 모델을 더 가볍게 적응시키는 운영 방식`으로 다시 붙잡는 직접 적용 닫힘을 보강해, 이 절의 초심자용 선택 기준을 더 분명하게 만들었다.; 도입부의 `이 절은 ...`, `조정 비용 절감 축` 같은 메타 문장을 줄이고, `같은 큰 기반 모델을 더 적은 조정 비용으로 적응시킨다`는 핵심이 먼저 보이도록 정리했다.
- 정리 반영: Chapter 7의 본선 handoff가 `P6-7.1 -> P6-7.2 -> P6-8.1`로 바로 이어지도록 문장을 다시 정리하고, low-rank·adapter·QLoRA 세부 구분은 뒤의 `P6-8.4` 보충절에서 회수하도록 연결 위치를 옮겼다.; 제목 아래 `Section ID`, `Version` 표기를 저장소 규칙 형식으로 바로잡았다.; `이 절이 맡는 역할`, `초심자가 여기서 ...` 같은 해설 표지를 줄이고, 비용 병목과 품질 병목을 어떻게 가를지 본문 문장 자체로 보이게 다듬었다.
- 번역 동기화 메모: 대응 번역본이 생기면 적용 표도 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절에서는 다음 내용을 깊게 다루지 않습니다` 목록과 메모리 계산식 범위 밖 문장을 제거하고, 이번 절이 닫는 질문인 효율적 조정의 비용 절감 발상과 `P6-7.3`, `P6-8.1` handoff가 먼저 보이도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 범위 안내 구조로 반영이 필요하다.

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, `커리큘럼 관점` 문단을 목표 뒤로 옮겼으며, 종결부는 `언제 효율적 조정 관점을 먼저 떠올려야 하는가` 하나로 닫히도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
