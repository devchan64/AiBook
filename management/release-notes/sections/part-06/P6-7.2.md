# Section Release Note

- Section ID: `P6-7.2`
- Source File: `docs/parts/part-06/chapter-07/section-02.md`

### v2026.07.18
- 본문 반영: 사례 요약 뒤에 `바로 적용해 보면` 표를 추가해, 초심자가 `같은 기반 모델로 여러 업무용 조정본을 만들어야 하는 장면`, `GPU 예산이 빠듯해 첫 실험이 무거운 장면`, `짧은 기간 안에 여러 조정 가설을 비교해야 하는 장면`을 볼 때 무엇을 `조정본 분리 관리`, `실험 진입 비용`, `가설 회전 속도` 문제로 먼저 읽어야 하는지 바로 점검하게 했다.
- 추가 반영: LoRA를 `작은 모델`이 아니라 `큰 기반 모델을 더 가볍게 적응시키는 운영 방식`으로 다시 붙잡는 직접 적용 닫힘을 보강해, 이 절의 초심자용 선택 기준을 더 분명하게 만들었다.
- 추가 반영: 도입부의 `이 절은 ...`, `조정 비용 절감 축` 같은 메타 문장을 줄이고, `같은 큰 기반 모델을 더 적은 조정 비용으로 적응시킨다`는 핵심이 먼저 보이도록 정리했다.
- 추가 반영: 제목 아래 `Section ID`, `Version` 표기를 저장소 규칙 형식으로 바로잡았다.
- 번역 동기화 메모: 대응 번역본이 생기면 적용 표도 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절에서는 다음 내용을 깊게 다루지 않습니다` 목록과 메모리 계산식 범위 밖 문장을 제거하고, 이번 절이 닫는 질문인 효율적 조정의 비용 절감 발상과 `P6-7.3`, `P6-8.1` handoff가 먼저 보이도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 범위 안내 구조로 반영이 필요하다.

### v2026.07.12
- 본문 반영: 집필 순서만 예고하는 `다음 ... 연결` 계열 표지를 제거했다; `이 절에서 기억할 관점`, `짧은 점검`, `언제 ... 먼저 떠올려야 하는가`를 `체크리스트` 하나 아래로 통합했다. 본문 메타데이터 버전도 함께 갱신했다.
- 추가 반영: 한국어 본문이 참조하는 Mermaid 도식의 내부 라벨을 한글화하고, 대응 영문 원본(`-en.mmd`)은 별도 유지하도록 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.11
- 본문 반영: 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 분리하고, 본문은 snippet include로 참조하도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.

### v2026.07.09
- 본문 반영: 사례/실습 표지를 통일하고, `커리큘럼 관점` 문단을 목표 뒤로 옮겼으며, 종결부는 `언제 효율적 조정 관점을 먼저 떠올려야 하는가` 하나로 닫히도록 정리함.
- 번역 동기화 메모: 대응 번역본 없음. 향후 번역 시 반영 필요.
