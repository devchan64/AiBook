# Section Release Note

- Section ID: `P4-19.4`
- Source File: `docs/parts/part-04/chapter-19/section-04.md`

### v2026.07.20
- 구조 정리: 한국어판, 영어판, 중국어 간체판에서 진행 메타 성격의 범위·목표·읽는 순서 제목을 각 Section의 개념, 판단 기준, 해석 흐름이 드러나는 구체 소제목으로 정리했다.
- 번역 반영 상태: 영어판과 중국어 간체판 반영 완료.
- 원문 기준 버전: `v2026.07.20`
- 저작권 검토: Part 4 Module 6 순차 검토 범위에서 DQN, PPO, TRPO, A2C/A3C, safe/offline RL, RLHF 비교와 병목별 분기 사례를 확인했다. 비교표와 사례는 본문 학습 흐름에 맞춰 재구성한 자체 설명 자료로 판단했다.
- 출처 사용 점검: Sutton & Barto, Mnih et al., Schulman et al., Ouyang et al. 자료는 대표 계열의 위치와 LLM 정렬 연결 근거로만 사용했고, 원문 표·그림·알고리즘 문구·코드를 재사용한 흔적은 발견하지 않았다.
- 관련 자산: `p4-19-4-mermaid-01-ko.mmd`는 본문 사례 흐름을 압축한 자체 도식으로 확인했다.
- 본문 반영: 저작권 기준에서 수정이 필요한 문장이나 자산은 없어 본문과 `Version`은 변경하지 않았다. 원문 기준 버전은 `v2026.07.12`이다.
- 번역 동기화 메모: 번역본 영향 없음.

### v2026.07.12
- 본문 반영: 말미 구조를 `체크리스트` 하나로 통합하고, DQN/PPO 분기, 현실 제약 분기, RLHF의 위치를 자기 점검 질문으로 다시 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 추가 반영: `연습 및 예제`를 새로 추가해 `상태 공간 확대`, `정책 흔들림`, `탐험 제약`, `사람 선호 반영`이라는 병목을 각각 DQN, PPO 계열, safe/offline RL, RLHF에 대응시키는 표와 `직접 판단해 보기` 해석표를 넣었다.
- 번역 동기화 메모: 영어판과 중국어판에도 새 `Practice And Example` / `练习与示例`, `Try Judging Directly` / `直接判断一下`, 정리된 `Checklist` / `检查清单` 구조를 같은 위치에 반영했다.

### v2026.07.11
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-19/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 현재는 한국어 자산만 운영하도록 README와 본문 참조를 맞췄고, 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어판 `docs/parts/part-04/chapter-19/section-04.en.md`와 중국어판 `docs/parts/part-04/chapter-19/section-04.zh.md`를 열고, 같은 폴더에 공용 `-en.mmd` 기준 원본을 추가했다. 두 번역본 모두 DQN, PPO / actor-critic, safe RL, offline RL, domain randomization, RLHF 분기 지도를 한국어 원문 구조에 맞춰 반영했다.

### v2026.07.07
- 본문 반영: DQN을 가치 기반 강화학습의 확장 사례로 직접 설명하도록 문장을 교체했고, 핵심 정의도 별도 표지 없이 본문 흐름 안에 통합했다.
- 번역 동기화 메모: 향후 번역본에서도 DQN 위치를 조언형이 아니라 정의형 서술로 유지해야 한다.

### v2026.07.08
- 본문 반영: 사례 1 아래에 `큰 상태 공간 -> DQN`, `정책 업데이트 불안정 -> PPO`, `사람 선호 반영 필요 -> RLHF`로 갈라지는 Mermaid 흐름도를 추가하고 본문 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 알고리즘 이름보다 병목 질문을 먼저 고르게 만드는 사례 흐름을 유지해야 한다.

### v2026.07.10
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 추가 반영: DQN, 정책 안정화 계열, safe RL과 offline RL, domain randomization, RLHF를 각각 `무슨 병목에서 나왔고 무엇을 직접 바꾸려는가` 기준으로 더 촘촘히 읽을 수 있도록 비교 표와 해석 문장을 추가했다. 새 알고리즘을 늘리기보다 기존 분기 설명의 학습 밀도를 높이는 방향으로 보강했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다. 추가된 분기 비교 표와 해석 문장도 함께 동기화해야 한다.
