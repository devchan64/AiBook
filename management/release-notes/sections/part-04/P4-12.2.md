# Section Release Note

- Section ID: `P4-12.2`
- Source File: `docs/parts/part-04/chapter-12/section-02.md`

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 거리 함수 전면 비교, 전처리 전체 체계, 고차원 거리 이론 목록을 제거하고, k-NN에서 거리와 스케일이 판단을 바꾸는 현재 절 질문에 집중하는 문장으로 정리했다. 같은 작업에서 Section 버전을 `v2026.07.17`로 갱신했다.
- 번역 동기화 메모: 영어·중국어 번역본도 같은 범위 안내를 삭제형 목록 대신 현재 절 질문과 기준 Section 연결 중심으로 맞춰야 한다.

### v2026.07.12
- 본문 반영: 해당 소제목을 제거하고, k-NN 비교 실습이 `표현과 입력 변화에 따라 어떤 이웃이 들어오고 빠지는가`를 읽는 설명 문장으로 바로 이어지게 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 번역본에서도 같은 위치의 메타형 소제목을 제거하고, 반복 변화 실습 설명을 본문 문단으로 접은 뒤 말미를 `Checklist` 하나로 통일했다.
- 추가 반영: 말미의 `이 절에서 기억할 관점`과 `짧은 점검`을 `체크리스트` 하나로 정리했다. 번역본 반영은 후속 확인이 필요하다.

### v2026.07.11
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-12/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 이어서 영문 기준 `p4-12-2-mermaid-01-en.mmd`부터 `p4-12-2-mermaid-03-en.mmd`까지 추가하고, `docs/parts/part-04/chapter-12/section-02.en.md`와 `docs/parts/part-04/chapter-12/section-02.zh.md`를 열었다.
- 자산 정리: 한국어 Mermaid `p4-12-2-mermaid-01-ko.mmd`에서 `같은 질의 / 같은 새 입력`, `거리 기준 선택 / 거리 규칙 선택`, `이웃 순서 계산 / 이웃 순서 계산`, `예측값이 달라질 수 있음 / 예측도 달라질 수 있음`처럼 같은 뜻을 반복한 라벨을 `같은 새 입력(query) -> 거리 규칙 선택 -> 이웃 순서 계산 -> 예측이 달라질 수 있음`으로 단일화했다.
- 번역 동기화 메모: 영어·중국어 번역 경로를 열었고, 중국어판은 현재 Chapter 12에서 공용 `-en.mmd` 자산을 직접 참조한다.

### v2026.07.08
- 본문 반영: 대출 위험 분류 사례 아래에 raw distance, income-axis domination, scaled distance, late-payment signal recovery, neighbor-list change를 보여 주는 Mermaid 도식을 추가하고 Section 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 스케일 조정 전후의 사례 흐름 대비를 유지해야 한다.

### v2026.07.09
- 본문 반영: 다음 장 연결과 비교 기록 운영 문단을 줄이고, 거리 규칙 변화가 이웃 순서와 예측을 어떻게 바꾸는지, 스케일 왜곡과 표준화가 무엇을 바꾸는지 중심으로 재작성했다. Python 예제에는 스케일 조정 전후의 이웃 순위 변화를 직접 출력하도록 보강했다. 적용/점검 가이드는 새 `P4-12.3`으로 분리했다.
- 추가 반영: Python 예제가 `이웃 순위 변화`뿐 아니라 `k=3` 최종 예측이 `risky`에서 `safe`로 실제 뒤집히는 지점까지 직접 출력하게 바꿔, 표준화가 판단 규칙을 어떻게 바꾸는지 더 분명히 닫았다.
- 번역 동기화 메모: 향후 번역본에서도 거리 함수, 스케일, 표준화의 역할을 현재 절의 중심 질문으로 유지하고 적용/점검 가이드는 별도 Section으로 분리해야 한다.

### v2026.07.10
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
