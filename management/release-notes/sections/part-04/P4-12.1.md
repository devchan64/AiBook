# Section Release Note

- Section ID: `P4-12.1`
- Source File: `docs/parts/part-04/chapter-12/section-01.md`

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 거리 함수, 스케일, 적용 가이드 목록을 제거하고, k-NN의 기본 판단 발상과 `P4-12.2`, `P4-12.3`로 이어지는 handoff 중심 문장으로 정리했다. 같은 작업에서 Section 버전을 `v2026.07.17`로 갱신했다.
- 번역 동기화 메모: 영어·중국어 번역본도 같은 범위 안내를 삭제형 목록 대신 현재 절 질문과 후속 Section 연결 중심으로 맞춰야 한다.

### v2026.07.12
- 본문 반영: 해당 말미 블록을 `체크리스트` 하나로 정리하고 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 번역본의 같은 위치 말미 `Perspectives` 블록을 제거하고, 한국어 원문과 같은 `Checklist` 단일 구조로 맞췄다. 같은 작업에서 EN/ZH 버전도 `v2026.07.12`로 올렸다.

### v2026.07.11
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-12/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 이어서 영문 기준 `p4-12-1-mermaid-01-en.mmd`, `p4-12-1-mermaid-02-en.mmd`를 추가하고, `docs/parts/part-04/chapter-12/section-01.en.md`와 `docs/parts/part-04/chapter-12/section-01.zh.md`를 열어 다국어 본문을 연결했다.
- 자산 정리: 한국어 Mermaid `p4-12-1-mermaid-01-ko.mmd`에서 `새 질의 / 새 입력`, `가장 가까운 이웃 찾기 / 가까운 사례 찾기`, `주변 레이블 모으기 / 주변 레이블 모으기`, `투표 또는 평균 / 판단 만들기`처럼 같은 뜻을 겹쳐 적은 라벨을 정리해 `새 입력(query) -> 가까운 사례 찾기 -> 주변 레이블 모으기 -> 투표 또는 평균으로 판단` 흐름만 남겼다.
- 번역 동기화 메모: 영어·중국어 번역 경로를 열었고, 중국어판은 현재 Chapter 12에서 공용 `-en.mmd` 자산을 직접 참조한다.

### v2026.07.08
- 본문 반영: 새 고객 사례 아래에 query, nearest customers, neighbor labels, vote, prediction, mixed-neighbor review를 묶는 Mermaid 도식을 추가하고 Section 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 이웃 기반 판단과 review 분기 구조를 유지해야 한다.

### v2026.07.09
- 본문 반영: 커리큘럼 안내, 비교 기록 운영 문단, 역사 블록을 걷어내고 `query`, `neighbor`, `label`, `k`의 역할, `k` 변화 예시, 예측 시 비용 설명을 보강했다. 적용/점검 가이드는 새 `P4-12.3`으로 분리했다.
- 번역 동기화 메모: 향후 번역본에서도 `직관`, `k의 의미`, `학습의 의미`를 중심 질문으로 유지하고 적용/점검 가이드는 별도 Section으로 분리해야 한다.

### v2026.07.10
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
