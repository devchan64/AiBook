# Section Release Note

- Section ID: `P4-19.2`
- Source File: `docs/parts/part-04/chapter-19/section-02.md`

### v2026.07.20
- 저작권 검토: Part 4 Module 6 순차 검토 범위에서 정책 기반 강화학습, policy gradient, actor-critic 설명과 정책 확률 조정 Python 예제를 확인했다. 표·사례·코드는 본문 학습 목적에 맞춘 자체 작성 장난감 예제로 판단했다.
- 출처 사용 점검: Sutton & Barto 교재, Williams REINFORCE 논문, Sutton et al. policy gradient 논문, Konda & Tsitsiklis actor-critic 논문은 정책 기반 강화학습 근거로만 사용했고, 원문 표·그림·알고리즘 문구·코드를 재사용한 흔적은 발견하지 않았다.
- 관련 자산: `p4-19-2-mermaid-01-ko.mmd`부터 `p4-19-2-mermaid-06-ko.mmd`까지는 본문 사례 흐름을 압축한 자체 도식으로 확인했다.
- 본문 반영: 저작권 기준에서 수정이 필요한 문장이나 자산은 없어 본문과 `Version`은 변경하지 않았다. 원문 기준 버전은 `v2026.07.17`이다.
- 번역 동기화 메모: 번역본 영향 없음.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 policy gradient 정리, likelihood ratio trick, PPO/TRPO/A2C/A3C, 연속 제어 수식 목록을 걷어내고, 정책 기반 강화학습의 중심 질문과 `P4-19.3`, `P4-19.4`, `P4-19.6` handoff만 남기도록 정리했다. 같은 작업에서 Section 버전을 `v2026.07.17`로 갱신했다.
- 번역 동기화 메모: 영어·중국어 번역본도 같은 범위 안내를 삭제형 목록 대신 현재 절 질문과 후속 연결 중심으로 맞춰야 한다.

### v2026.07.12
- 본문 반영: 해당 소제목을 제거하고, 정책 기반 비교가 `보상 변화 -> 행동 분포 변화`를 읽는 설명 문장으로 바로 이어지게 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 추가 반영: `연습 및 예제` 아래에 `직접 판단해 보기` 표를 추가해, 확률이 올라간 행동을 곧바로 `계속 고를 정답`으로 읽지 않고 `현재 정책이 더 자주 시도하도록 기울었다`는 해석으로 다시 읽게 보강했다.
- 번역 동기화 메모: 영어·중국어 번역본에서도 같은 위치의 메타형 소제목을 제거하고, `직접 판단해 보기` 표를 추가해 policy probability 상승을 곧바로 현실 최선으로 단정하지 않게 맞췄다. 말미 구조도 `Checklist` 하나로 통일했다.

### v2026.07.11
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-19/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 현재는 한국어 자산만 운영하도록 README와 본문 참조를 맞췄고, 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어판 `docs/parts/part-04/chapter-19/section-02.en.md`와 중국어판 `docs/parts/part-04/chapter-19/section-02.zh.md`를 열고, 같은 폴더에 공용 `-en.mmd` 기준 원본을 추가했다. 두 번역본 모두 policy 직접 조정 관점, policy gradient / actor-critic, 로봇 팔·배너 비율 사례, 확률 변화 예제를 한국어 원문 구조에 맞춰 반영했다.

### v2026.07.08
- 본문 반영: 사례 1 아래에 `상태 -> 정책 출력 -> 제어 행동 -> 성공/실패 -> 확률 조정` 흐름의 Mermaid 도식을 추가하고 본문 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 정책 업데이트를 점수 비교가 아니라 행동 성향 조정 흐름으로 읽게 하는 도식을 유지해야 한다.

### v2026.07.09
- 본문 반영: 음수 보상 비교, Part 4 목표 회수 문단, `보인 구조 / 해석 경계 / 다음 질문` 공통 기록 표를 추가해 `좋은 보상 -> 확률 상승`, `나쁜 보상 -> 확률 하락`을 한 묶음으로 읽게 했다.
- 번역 동기화 메모: 향후 번역본에서도 보상 부호 변화에 따른 정책 분포 비교와 해석 문단을 함께 유지해야 한다.

### v2026.07.10
- 본문 반영: 정책 출력 도식의 라벨을 짧게 다듬고, 가치 기반/정책 기반 비교를 두 개의 짧은 Mermaid 도식으로 분리했으며, policy gradient와 actor-critic 도식의 문구와 피드백 화살표를 더 명확하게 정리했다. 또한 연속 행동 장면 비교 표, 정책 기반 해석 질문 표, actor/critic 역할 오해 정리 표, 가치 기반과 정책 기반 적용 장면 비교 표, 광고 노출 비율 사례를 추가해 학습 밀도를 보강했다. 여기에 상태별 행동 확률 표, REINFORCE 직관 예시, 광고 배너 노출 확률 예시를 더해 예시도 구체화했다.
- 추가 반영: `이 절은 다음 내용은 깊게 다루지 않습니다` 목록 가운데 policy gradient 정리와 likelihood ratio trick의 회수 위치가 부족하던 문제를 `P4-19.6 보충학습` 추가로 보완했다. 이제 적용 제약은 P4-19.3, PPO/TRPO/A2C/A3C와 연속 제어 확장 흐름은 P4-19.4, policy gradient와 likelihood ratio trick의 최소 수학 감각은 P4-19.6으로 각각 대응되도록 정리했다.
- 번역 동기화 메모: 향후 번역본에서도 분리된 비교 도식 구조와 actor-critic 피드백 흐름, 정책 분포 해석 표, 추가 사례와 상태별 확률 예시를 같은 방식으로 유지해야 한다. 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
