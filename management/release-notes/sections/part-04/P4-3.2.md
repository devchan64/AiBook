# Section Release Note

- Section ID: `P4-3.2`
- Source File: `docs/parts/part-04/chapter-03/section-02.md`

### v2026.07.12

- 변경 이유: 모델 선택 절 말미의 기억·점검·적용 시점 표지가 역할상 중복되어, 현재 절의 판단 기준이 하나의 자기 확인 블록으로 모이지 않았다.
- 본문 반영: 말미 구조를 `체크리스트` 하나로 통합하고, 후보 집합 축소, 기준 모델, 데이터와 라벨 재점검, 운영 제약을 자기 점검 질문으로 다시 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본도 말미의 기억/점검/적용 시점 블록을 제거하고 한국어 원문과 같은 `Checklist` 단일 구조로 정렬했다. 중국어 간체 번역본의 개념사전 링크도 상대 경로로 바로잡았고, 번역본 버전을 `v2026.07.12`로 맞췄다.
- 번역 반영 상태: 영어·중국어 간체 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.11

- 변경 이유: Part 4의 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 관리해 차트 가이드라인의 Mermaid 자산 운영 방식과 맞출 필요가 있었다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-03/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 영문·중국어 간체 본문도 같은 자산 패턴으로 동기화했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams in this section.
- 번역 반영 상태: 영어·중국어 간체 반영 완료
- 관련 자산: `docs/assets/part-04/chapter-03/`
- 원문 기준 버전: `v2026.07.11`

- 변경 이유: 모델 선택 Section의 Mermaid 도식 세 개가 영문 라벨로 남아 있어 한글 본문에서 후보 축소와 비교 흐름을 따라갈 때 일관성이 떨어지고 있었다.
- 본문 반영: 고객 이탈 사례 도식, 모델 선택 기본 흐름 도식, 문의 분류 사례 도식의 보이는 텍스트를 한국어로 바꾸고 Section 버전을 갱신했다. 도식 구조와 판단 순서는 바꾸지 않았다.
- 본문 반영: 같은 세 Mermaid 도식을 `docs/assets/part-04/chapter-03/` 아래 `-en`/`-ko`/`-zh` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다.
- 번역 동기화 메모: 영문 번역본은 기존 영어 라벨 구조를 유지한 채 `v2026.07.11` 버전을 맞췄다. 중국어 간체 번역본은 세 Mermaid 도식의 보이는 라벨을 중국어 본문 기준으로 정리했고, `문제 유형 -> 제약 -> 기준 모델 -> 후보 비교 -> 기록`과 `규칙 기반 출발 -> 기준 모델 -> 후보 비교 -> 클래스별 점검`의 구조는 그대로 유지했다.
- 번역 반영 상태: 영어·중국어 반영 완료
- 관련 자산: `docs/assets/part-04/chapter-03/churn-model-selection-flow-en.mmd`, `docs/assets/part-04/chapter-03/churn-model-selection-flow-ko.mmd`, `docs/assets/part-04/chapter-03/churn-model-selection-flow-zh.mmd`, `docs/assets/part-04/chapter-03/model-selection-basic-flow-en.mmd`, `docs/assets/part-04/chapter-03/model-selection-basic-flow-ko.mmd`, `docs/assets/part-04/chapter-03/model-selection-basic-flow-zh.mmd`, `docs/assets/part-04/chapter-03/inquiry-classification-model-selection-flow-en.mmd`, `docs/assets/part-04/chapter-03/inquiry-classification-model-selection-flow-ko.mmd`, `docs/assets/part-04/chapter-03/inquiry-classification-model-selection-flow-zh.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.08

- 변경 이유: 모델 선택 흐름도는 있었지만, 고객 이탈 예측 사례에서 실제로 어떤 이유로 후보 모델 집합을 줄이는지 한눈에 보여 주는 예시 도식이 부족했다.
- 본문 반영: 첫 사례 뒤에 `churn problem -> task type -> constraints -> baseline / tree / ensemble candidates -> validation comparison -> keep or expand set` 흐름의 Mermaid 도식을 추가해, 모델 선택 휴리스틱이 후보 축소와 비교 절차라는 점을 사례 수준에서 보강했다.
- 번역 동기화 메모: preserve the churn-model-selection funnel diagram and keep the candidate shortlist tied to task type, constraints, and validation comparison. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`
- 변경 이유: 사례 문단 바로 아래에도 규칙 기반 출발점에서 기준 모델과 후보 비교로 이어지는 흐름을 더 직접 붙여 둘 필요가 있었다.
- 본문 반영: 사례 1 아래에 `rule-based start -> simple baseline -> candidate comparison -> per-class check` 흐름의 Mermaid 도식을 추가했다.

### v2026.07.07

- 변경 이유: 모델 선택 휴리스틱 도입부의 권고형 표현을 설명형 문장으로 정리했다.
- 본문 반영: 모델 후보를 먼저 줄이는 판단 문장을 `편이 좋습니다`가 아닌 직접 서술형으로 교체했다.
- 번역 동기화 메모: 향후 번역본에서도 휴리스틱 판단 도입을 설명형 문장으로 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.07`

### v2026.07.10

- 변경 이유: 구조 표지가 현재 원칙의 `사례 및 예시` 축과 어긋난 부분을 정리할 필요가 있었다.
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 2026-07-11에 `management/authoring/translation-backups/part-04-invalid-2026-07-10/`의 아카이브 번역본을 활성 경로로 복구했다. 영문판과 중국어판 모두 원문 `v2026.07.10` 기준을 유지하며 반영했다.
- 번역 반영 상태: 영어·중국어 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.10`
