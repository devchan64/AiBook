# Section Release Note

- Section ID: `P4-15.4`
- Source File: `docs/parts/part-04/chapter-15/section-04.md`

### v2026.07.11

- 변경 이유: Part 4 영어·중국어 번역 경로를 Chapter 15의 Extra Trees 보충학습까지 열고, 같은 절의 Mermaid 자산 참조도 다국어 경로에 맞게 정리할 필요가 있었다.
- 본문 반영: `docs/parts/part-04/chapter-15/section-04.en.md`와 `docs/parts/part-04/chapter-15/section-04.zh.md`를 새로 열어 random forest 대비 Extra Trees의 차이, bootstrap/OOB 기본값, churn/defect 사례, 비교 Python 예제를 같은 순서로 옮겼다. 또한 `docs/assets/part-04/chapter-15/` 아래에 `p4-15-4-mermaid-01-en.mmd`부터 `p4-15-4-mermaid-03-en.mmd`까지 추가하고, 영어·중국어 본문이 공용 `-en.mmd` 자산을 참조하도록 정리했다.
- 번역 동기화 메모: 영어·중국어 번역 경로를 열었고, 현재 Chapter 15에서는 P4-15.1부터 P4-15.4까지 공용 `-en.mmd` 자산을 순차적으로 사용한다. 번역본에서도 Extra Trees를 random forest 옆의 가까운 비교 후보로 두고, `best split` 대 `random threshold`, `bootstrap` 대 `OOB`의 차이를 같은 흐름으로 유지한다.
- 번역 반영 상태: 영어/중국어 반영
- 관련 자산: `docs/assets/part-04/chapter-15/`
- 원문 기준 버전: `v2026.07.11`

- 변경 이유: Part 4의 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 관리해 차트 가이드라인의 Mermaid 자산 운영 방식과 맞출 필요가 있었다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-15/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 현재는 한국어 자산만 운영하도록 README와 본문 참조를 맞췄고, 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 현재 이 Section의 번역 경로는 열려 있지 않아 한국어 Mermaid 자산만 운영한다. 번역본이 열리면 같은 폴더에 `-en.mmd` 기준 원본과 필요한 파생본을 추가해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: `docs/assets/part-04/chapter-15/`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.09

- 변경 이유: `P4-15.1`에서 Extra Trees와의 세부 비교를 생략하면서도 회수 위치가 비어 있었고, 랜덤포레스트를 배운 독자가 같은 숲 계열 안의 가장 가까운 비교 후보를 어디서 이해해야 하는지 공백이 남아 있었다.
- 본문 반영: `P4-15.4 보충학습: Extra Trees와 랜덤포레스트를 처음 비교하는 법`을 새로 추가해 split threshold 무작위화, bootstrap 기본값, OOB 가능 조건, bias-variance 해석, 간단한 비교 예제를 한 절로 묶어 보강했다. 이어서 사례로 보기도 고객 이탈과 불량 탐지 두 장면으로 확장해, 왜 같은 숲 계열 안에서 두 모델을 나란히 시험하는지 판단 순서가 더 직접 보이게 했다.
- 번역 동기화 메모: 향후 번역본에서도 Extra Trees를 범위 밖으로 처리하지 말고, 랜덤포레스트와의 비교 기준을 같은 보충학습 구조로 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.10

- 변경 이유: 구조 표지가 현재 원칙의 `사례 및 예시` 축과 어긋난 부분을 정리할 필요가 있었다.
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.10`
