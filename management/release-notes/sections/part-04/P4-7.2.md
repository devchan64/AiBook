# Section Release Note

- Section ID: `P4-7.2`
- Source File: `docs/parts/part-04/chapter-07/section-02.md`

### v2026.07.06-2

- 변경 이유: Chapter 7 두 번째 절의 릴리즈노트가 없었고, 전처리 절의 현재 기준본과 후속 번역 동기화 기준을 기록할 필요가 있었음.
- 본문 반영: 본문 구조는 유지하며, 전처리를 `입력 표현을 모델이 다룰 수 있는 형태로 바꾸는 단계`로 설명하고, 결측치 처리, 스케일 조정, 범주형 인코딩, `fit/transform` 분리, 파이프라인 연결을 현재 기준본으로 삼는다.
- 번역 동기화 메모: release note 신규 생성. / preprocessing as representation transformation and assumption matching, plus train-only fit / test-only transform order를 번역에서도 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-2`

### v2026.07.06-3

- 변경 이유: 전처리 전략 표와 도구 소개 주변에 남아 있던 `이해하면 충분합니다`, `입문 단계에서는` 같은 메타 안내를 줄일 필요가 있었음.
- 본문 반영: 결측치 처리 표와 `Pipeline`/`ColumnTransformer` 설명은 유지하면서 연결 문장을 직접 설명형으로 다듬었다. 전처리 도구 이름 목록도 `범주를 알아두는 이유`가 바로 드러나게 정리했다.
- 번역 동기화 메모: remove learner-addressed meta guidance while preserving the preprocessing strategy tables and tool-category framing. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-3`

### v2026.07.06-4

- 변경 이유: 본문 수정 이력과 달리 Section 메타데이터가 `v2026.07.05`로 남아 있었고, 전처리의 실무적 출발점과 마무리 정의에 남아 있던 `더 자연스럽습니다`, `볼 수 있습니다` 같은 표현을 직접 설명형으로 정리할 필요가 있었다.
- 본문 반영: Section `Version`을 `v2026.07.06`으로 갱신했고, 전처리의 현장 질문 도입과 최종 정의 문장을 직접 서술형으로 조정했다. 결측치/스케일/인코딩, fit/transform, pipeline 연결 구조는 유지했다.
- 번역 동기화 메모: translation에서도 version sync를 반영하고, preprocessing-as-representation-transformation framing을 advisory phrasing보다 direct prose로 유지한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-5

- 변경 이유: 전처리의 학술적 위치와 실무 출발점 설명에 남아 있던 `더 정확합니다`, `더 자연스럽습니다` 같은 완충 표현을 더 직접적인 설명형 문장으로 맞출 필요가 있었다.
- 본문 반영: 전처리를 `학습 가능한 표현을 만드는 단계`로 직접 고정했고, 현장 질문에서 출발하는 전처리 흐름 도입도 직접 서술형으로 조정했다. 전처리 범주, fit/transform, pipeline 구조는 유지했다.
- 번역 동기화 메모: translation에서도 the academic positioning of preprocessing and the field-first framing should remain in direct prose. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`
