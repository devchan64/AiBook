# Section Release Note

- Section ID: `P4-3.1`
- Source File: `docs/parts/part-04/chapter-03/section-01.md`

### v2026.07.12

- 변경 이유: 휴리스틱 절 말미의 회상·점검·적용 시점 표지가 중복되어, 무엇을 이해해야 하는지보다 템플릿 표지가 먼저 보였다.
- 본문 반영: 말미 구조를 `체크리스트` 하나로 통합하고, 후보 축소, 검증 가능성, 기준 모델과의 결합, 기록 원칙을 자기 점검 질문으로 다시 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본도 같은 `Checklist` 단일 구조로 정리하고, 중문 번역본의 개념사전 링크를 현재 상대 경로로 바로잡았다. 기존 `Perspective / Short Check / When` 메타형 말미 블록은 제거했다.
- 번역 반영 상태: 영어·중국어 간체 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.11

- 변경 이유: Part 4의 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 관리해 차트 가이드라인의 Mermaid 자산 운영 방식과 맞출 필요가 있었다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-03/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 영문·중국어 간체 본문도 같은 자산 패턴으로 동기화했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams in this section.
- 번역 반영 상태: 영어·중국어 간체 반영 완료
- 관련 자산: `docs/assets/part-04/chapter-03/`
- 원문 기준 버전: `v2026.07.11`

- 변경 이유: 휴리스틱 Section의 Mermaid 도식 라벨이 영문으로 남아 있어 한글 본문 기준의 설명 흐름과 시각 언어가 어긋나고 있었다.
- 본문 반영: `휴리스틱은 후보를 줄이는 방법이다` 도식과 사례 1 아래 도식의 보이는 텍스트를 한국어로 바꾸고 Section 버전을 갱신했다. 도식 구조와 검증 흐름은 유지했다.
- 본문 반영: 두 Mermaid 도식을 `docs/assets/part-04/chapter-03/` 아래 `-en`/`-ko`/`-zh` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다.
- 번역 동기화 메모: 영문 번역본은 기존 영어 라벨 구조를 유지한 채 `v2026.07.11` 버전을 맞췄다. 중국어 간체 번역본은 두 Mermaid 도식의 보이는 라벨을 중국어 본문 기준으로 정리했고, `후보 과다 -> 휴리스틱 -> 검증 -> 수정`과 `주문 표 -> 특징 축소 -> 기준 모델 -> 비교 검증`의 흐름 구조는 그대로 유지했다.
- 번역 반영 상태: 영어·중국어 반영 완료
- 관련 자산: `docs/assets/part-04/chapter-03/heuristic-candidate-reduction-flow-en.mmd`, `docs/assets/part-04/chapter-03/heuristic-candidate-reduction-flow-ko.mmd`, `docs/assets/part-04/chapter-03/heuristic-candidate-reduction-flow-zh.mmd`, `docs/assets/part-04/chapter-03/return-risk-heuristic-flow-en.mmd`, `docs/assets/part-04/chapter-03/return-risk-heuristic-flow-ko.mmd`, `docs/assets/part-04/chapter-03/return-risk-heuristic-flow-zh.mmd`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.07

- 변경 이유: 휴리스틱 사용 판단 도입부의 권고형 표현을 설명형 문장으로 정리했다.
- 본문 반영: 휴리스틱 사용 가능 여부와 경계 대상을 먼저 판정하는 문장을 직접 서술형으로 바꿨다.
- 번역 동기화 메모: 향후 번역본에서도 휴리스틱 경계 설명을 직접 진술형으로 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.07`

### v2026.07.08

- 변경 이유: 휴리스틱이 `모든 특징 비교`를 줄여 `작은 기준 모델`과 검증으로 이어지는 흐름을 사례 수준에서 더 빠르게 보이게 할 필요가 있었다.
- 본문 반영: 사례 1 아래에 `전체 특징 나열 -> 후보 과다 -> 휴리스틱 부분집합 -> 기준 모델 -> 비교 검증` 흐름의 Mermaid 도식을 추가하고 본문 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 휴리스틱을 임의 추측이 아니라 후보 축소와 검증 흐름으로 읽게 하는 같은 도식을 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.08`

### v2026.07.10

- 변경 이유: 구조 표지가 현재 원칙의 `사례 및 예시` 축과 어긋난 부분을 정리할 필요가 있었다.
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 2026-07-11에 `management/authoring/translation-backups/part-04-invalid-2026-07-10/`의 아카이브 번역본을 활성 경로로 복구했다. 영문판과 중국어판 모두 원문 `v2026.07.10` 기준을 유지하며 반영했다.
- 번역 반영 상태: 영어·중국어 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.10`
