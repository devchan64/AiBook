# Section Release Note

- Section ID: `P4-10.1`
- Source File: `docs/parts/part-04/chapter-10/section-01.md`

### v2026.07.11

- 변경 이유: Part 4의 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 관리해 차트 가이드라인의 Mermaid 자산 운영 방식과 맞출 필요가 있었다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-10/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 현재는 한국어 자산만 운영하도록 README와 본문 참조를 맞췄고, 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어 번역본 `docs/parts/part-04/chapter-10/section-01.en.md`와 중국어 번역본 `docs/parts/part-04/chapter-10/section-01.zh.md`를 열었고, 같은 날짜 후속 정리에서 누락돼 있던 후반 가정·사례·실습·정리 블록도 번역본에 반영해 원문과 같은 구조로 맞췄다. 같은 작업에서 `docs/assets/part-04/chapter-10/` 아래에 `p4-10-1-mermaid-01-en.mmd`부터 `p4-10-1-mermaid-04-en.mmd`까지 영문 기준 자산을 추가했고, 중국어 본문은 현재 이 공용 `-en.mmd` 자산을 직접 참조한다.
- 번역 반영 상태: 영어/중국어 반영
- 관련 자산: `docs/assets/part-04/chapter-10/`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.12

- 변경 이유: 선형회귀 첫 절에서 `기울기/절편 해석`이 대표 사례 뒤로 밀려 있어, 독자가 사례를 읽기 전에 필요한 해석 손잡이를 먼저 잡기 어려웠다.
- 본문 반영: `기울기(coefficient)와 절편(intercept)은 어떻게 읽는가` 블록을 사례 앞쪽으로 옮겨 설명 순서를 정리했고, 중복으로 끊기던 `사례 및 예시` 표지를 하나의 흐름으로 합쳤다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 한국어 원문은 `p4-10-1-regression-line-intuition-ko.svg`, 중국어 번역본은 `p4-10-1-regression-line-intuition-zh.svg`를 참조하도록 맞췄다. 영어 번역본도 같은 후반 구조 재배치와 말미 `Checklist` 단일 구조로 정렬했다.
- 번역 반영 상태: 영어·중국어 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`
- 추가 반영: 기준서에 따라 `이 연습이 Part 4 목표를 어떻게 회수하는가` 메타형 소제목을 제거하고, 바로 뒤 문단도 현재 절의 해석만 남는 설명형 문장으로 정리했다. 영어·중국어 번역본도 같은 흐름으로 맞췄다.
- 추가 반영: 말미의 `이 절에서 기억할 관점`, `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`, `다음 절과의 연결`을 걷어내고 `체크리스트` 하나로 정리했다. 영어·중국어 번역본도 같은 구조로 정렬했다.

- 변경 이유: 선형회귀 첫 절은 `직선`, `기울기`, `절편`을 설명하지만 좌표형 그림이 없어, 독자가 산점도 위 회귀선의 의미를 바로 붙잡기 어려웠다.
- 본문 반영: `선형회귀는 왜 직선으로 관계를 표현하는가` 단락 아래에 `p4-10-1-regression-line-intuition-en.svg`를 추가해 산점도, 회귀선, 기울기, 절편의 관계를 좌표형 차트로 보강했다.
- 번역 동기화 메모: 한국어 원문에는 같은 좌표형 자산의 한국어 파생 SVG 연결을, 중국어 번역본에는 중국어 파생 SVG 연결을 반영했다. 영어 번역본의 동일 위치 연결 여부는 별도 확인이 남아 있다.
- 번역 반영 상태: 중국어 반영 완료, 영어 확인 필요
- 관련 자산: `docs/assets/part-04/chapter-10/p4-10-1-regression-line-intuition-en.svg`, `docs/assets/part-04/chapter-10/p4-10-1-regression-line-intuition-ko.svg`, `docs/assets/part-04/chapter-10/p4-10-1-regression-line-intuition-zh.svg`
- 원문 기준 버전: `v2026.07.12`

### v2026.07.08

- 변경 이유: 선형회귀를 `직선으로 먼저 설명해 볼 수 있는가`라는 질문으로 읽게 하는 사례 흐름을 본문 바로 아래에서 더 직접 보강할 필요가 있었다.
- 본문 반영: 사례 1 아래에 `광고비/매출 데이터 -> 직선 적합 -> 기울기 해석 -> 예측-실제 차이 확인` 흐름의 Mermaid 도식을 추가하고 본문 버전을 갱신했다. 추가 정리에서는 현재 MkDocs 수식 렌더링 형식에 맞게 이중 이스케이프된 블록 수식 구문을 바로잡았다.
- 번역 동기화 메모: 향후 번역본에서도 선형회귀를 완전한 현실 복제가 아니라 첫 설명 직선으로 읽게 하는 같은 도식을 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.08`

### v2026.07.09

- 변경 이유: 리포트의 우선 작업 기준에 따라 선형회귀 첫 절에도 `값 하나를 바꿔 반복 관찰하는 실습`과 Part 4 목표 회수 문장을 직접 드러낼 필요가 있었다.
- 본문 반영: 다변수 예제 뒤에 `study_hours`만 바꾸는 통제된 변화 실습, `무엇이 유지되고 무엇이 달라지는가`, `이 연습이 Part 4 목표를 어떻게 회수하는가`, `보인 구조 / 해석 경계 / 다음 질문` 기록 표를 추가하고 Section 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 선형회귀 입문 실습을 `입력 하나 변화 -> 예측 변화 -> 해석 경계` 순서로 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.10

- 변경 이유: 구조 표지가 현재 원칙의 `사례 및 예시` 축과 어긋난 부분을 정리할 필요가 있었다.
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.10`
