# Section Release Note

- Section ID: `P4-8.1`
- Source File: `docs/parts/part-04/chapter-08/section-01.md`

### v2026.07.11

- 변경 이유: Part 4의 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 관리해 차트 가이드라인의 Mermaid 자산 운영 방식과 맞출 필요가 있었다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-08/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 영문·중국어 간체 본문도 같은 자산 패턴으로 동기화했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams in this section.
- 번역 반영 상태: 영어·중국어 간체 반영 완료
- 관련 자산: `docs/assets/part-04/chapter-08/`
- 원문 기준 버전: `v2026.07.11`

- 변경 이유: 한국어 본문의 모델 선택 Mermaid 도식 라벨이 영문으로 남아 있어 본문 언어 흐름과 어긋났다.
- 본문 반영: 모델 선택 큰 흐름 도식, shortlist 구성 도식, 고객 이탈 사례 도식의 보이는 라벨을 한국어로 바꾸고 Section 버전을 갱신했다.
- 변경 이유: 아카이브해 둔 영어·중국어 간체 번역본을 활성 경로에 반영하고 현재 공개 내비게이션과 연결할 필요가 있었다.
- 본문 반영: `docs/parts/part-04/chapter-08/section-01.en.md`와 `docs/parts/part-04/chapter-08/section-01.zh.md`를 활성 경로에 복원하고 번역본 버전을 `v2026.07.11`로 맞췄다.
- 번역 동기화 메모: 영문·중국어 간체 번역본을 현재 공개 경로에 반영했고, 한국어 도식 라벨 한글화에 맞춰 원문 기준 버전도 함께 정렬했다.
- 번역 반영 상태: 영문·중국어 간체 번역본 반영 완료 (2026-07-11)
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.11`

### v2026.07.08

- 변경 이유: baseline의 필요성은 보이지만, baseline을 만들려면 어떤 선행 판단이 먼저 고정되어야 하는지가 현재 본문에서는 충분히 직접적이지 않았다.
- 본문 반영: `baseline은 무엇을 알아야 만들 수 있는가`에 답하는 연결 문단과 준비 지식 표를 추가해, 문제 유형, 샘플 단위, 평가 지표, 데이터 분포, 운영상 중요한 실패가 baseline 설계의 선행 조건이라는 점을 보강했다.
- 변경 이유: 모델 선택의 일반 흐름과 추상 도식은 있었지만, 구독 서비스 이탈 예측 사례에서 설명 가능성·배치 운영 같은 제약이 후보군을 어떻게 갈라놓는지 사례 도식이 부족했다.
- 본문 반영: 사례로 보기 절에 `churn prediction task -> tabular data / need explanation / daily batch inference -> linear / tree / random-forest candidates -> compare shortlist` 흐름의 Mermaid 도식을 추가해, 문제와 제약이 후보군 설계로 번역되는 과정을 보강했다.
- 번역 동기화 메모: 향후 번역본에서도 baseline 준비 지식 표와 churn 후보군 도식을 함께 유지해, 모델 선택과 baseline 설계의 연결이 끊기지 않게 해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.08`

### v2026.07.09

- 변경 이유: Chapter 8 baseline 확장에 따라 P4-8.1이 `baseline을 실제로 어떻게 세우는가`를 직접 설명하지 않더라도, 그 준비 지식이 다음 절과 새 보충학습으로 자연스럽게 이어져야 했다.
- 본문 반영: baseline 준비 지식이 다음 절 P4-8.2와 `P4-8.3 보충학습`으로 이어진다는 연결을 유지하도록 전체 흐름을 재검토했다.
- 번역 동기화 메모: 향후 번역본에서도 모델 선택이 baseline 준비 지식과 새 보충학습 절로 이어지는 연결을 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.10

- 변경 이유: 구조 표지가 현재 원칙의 `사례 및 예시` 축과 어긋난 부분을 정리할 필요가 있었다.
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.10`
