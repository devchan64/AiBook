# Section Release Note

- Section ID: `P4-2.2`
- Source File: `docs/parts/part-04/chapter-02/section-02.md`

### v2026.07.11

- 변경 이유: Part 4의 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 관리해 차트 가이드라인의 Mermaid 자산 운영 방식과 맞출 필요가 있었다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-02/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 영문·중국어 간체 본문도 같은 자산 패턴으로 동기화했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams in this section.
- 번역 반영 상태: 영어·중국어 간체 반영 완료
- 관련 자산: `docs/assets/part-04/chapter-02/`
- 원문 기준 버전: `v2026.07.11`

- 변경 이유: 비지도학습 Section의 Mermaid 도식 세 개가 영문 라벨로 남아 있어 한글 본문에서 구조 후보 해석 흐름을 따라갈 때 일관성이 떨어지고 있었다.
- 본문 반영: 고객 표 예시 도식, 비지도학습 기본 흐름 도식, 사례 1 아래 도식의 보이는 텍스트를 한국어로 바꾸고 Section 버전을 갱신했다. 도식의 분기 구조와 설명 순서는 바꾸지 않았다.
- 번역 동기화 메모: 2026-07-11에 `management/authoring/translation-backups/part-04-invalid-2026-07-10/`의 아카이브 번역본을 활성 경로로 복구했다. 영문판은 기존 영문 도식 라벨을 유지했고, 중국어판도 번역 가이드에 따라 도식은 영문 라벨을 유지한 채 본문 설명과 메타데이터를 원문 `v2026.07.11`에 맞췄다.
- 번역 반영 상태: 영어·중국어 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.11`

### v2026.07.08

- 변경 이유: 비지도학습 Section은 추상 흐름도는 있었지만, 독자가 같은 입력 표가 군집 후보, 축 요약, 이상치 후보처럼 어떻게 다른 구조 읽기로 갈라지는지 사례 수준에서 바로 붙잡기 어려웠다.
- 본문 반영: 고객 데이터 예시 아래에 `customer table -> cluster candidates / reduced view / outlier candidate -> human interpretation -> next task` 흐름의 Mermaid 도식을 추가해, 비지도학습 결과가 정답 라벨이 아니라 구조 후보라는 점을 사례로 시각화했다.
- 번역 동기화 메모: preserve the concrete customer-data diagram that branches one unlabeled table into cluster candidates, a reduced-view map, and an outlier candidate before human interpretation. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

- 변경 이유: 사례 본문 바로 아래에 같은 비지도학습 흐름을 다시 붙여, 라벨 없는 고객 표에서 구조 후보가 어떻게 나오고 해석이 뒤에 따라오는지 더 직접 보강할 필요가 있었다.
- 본문 반영: 사례 1 아래에 `customer behavior table -> no labels -> similar-pattern search -> group/outlier hints -> human interpretation` 흐름의 Mermaid 도식을 추가했다.

### v2026.07.07

- 변경 이유: 비지도학습 판단 도입부의 권고형 표현을 설명형 문장으로 정리했다.
- 본문 반영: 비지도학습 여부를 먼저 판정하는 문장에서 `편이 맞는가`, `편이 좋습니다`류 표현을 제거했다.
- 번역 동기화 메모: 향후 번역본에서도 직접 서술형 판단 문장을 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.07`

### v2026.07.10

- 변경 이유: 구조 표지가 현재 원칙의 `사례 및 예시` 축과 어긋난 부분을 정리할 필요가 있었다.
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.10`
