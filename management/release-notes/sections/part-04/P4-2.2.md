# Section Release Note

- Section ID: `P4-2.2`
- Source File: `docs/parts/part-04/chapter-02/section-02.md`

### v2026.07.06

- 변경 이유: 비지도학습 결과를 `정답 라벨`로 오해하지 않도록, 군집 ID와 구조 후보의 차이를 더 직접적으로 설명할 필요가 있었음.
- 본문 반영: 군집 ID, 2차원 그림의 가까움, 이상치 후보가 각각 무엇을 뜻하고 무엇이 아닌지를 나누는 표를 추가했고, 비지도학습 결과를 `답`이 아니라 `구조 후보`로 읽는 관점과 3단계 해석 순서를 보강했다.
- 번역 동기화 메모: 대비 표와 해석 순서가 새로 들어가므로 번역 시 `구조 후보` 관점과 순서를 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-2

- 변경 이유: 비지도학습 절에서도 `초심자 기준에서는` 같은 메타 안내를 줄이고, 구조 후보를 해석하는 순서가 본문 전개 안에서 바로 읽히게 정리.
- 본문 반영: 결과 해석 3단계 도입 문장을 일반 서술로 바꾸어, 군집·축·이상치 후보를 정답이 아니라 구조 후보로 읽는 흐름을 더 직접적으로 드러냈다. 비교 표와 사례는 유지했다.
- 번역 동기화 메모: learner-addressed phrasing이 줄었으므로, 번역 시에도 structure-candidate interpretation order를 neutral narration으로 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-2`

### v2026.07.06-3

- 변경 이유: 비지도학습 문제 유형 표 앞에 남아 있던 `입문 단계에서는` 안내를 직접 설명형으로 정리할 필요가 있었음.
- 본문 반영: 군집화, 차원 축소, 이상치 탐지를 `먼저 구분해야 하는 세 문제`로 바로 제시하도록 문장을 다듬었다.
- 번역 동기화 메모: preserve the direct framing of the three unsupervised-problem types without learner-addressed staging. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-3`

### v2026.07.06-4

- 변경 이유: 본문 수정 이력과 달리 Section 메타데이터가 `v2026.07.05`로 남아 있었고, 비지도학습 정의와 해석 순서 문장에 `더 안전하게 말하면`, `읽는 편이 안전합니다` 같은 완충 표현이 남아 있었다.
- 본문 반영: Section `Version`을 `v2026.07.06`으로 갱신했고, 비지도학습의 첫 정의, 세 문제 소개, 구조 후보 해석, 해석 순서 문장을 직접 설명형으로 조정했다. 군집 ID/구조 후보 대비 표와 사례 구조는 유지했다.
- 번역 동기화 메모: translation에서도 version sync를 반영하고, structure-candidate framing과 interpretation order를 hedging-style guidance보다 direct prose로 유지한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`
