# Section Release Note

- Section ID: `P4-16.3`
- Source File: `docs/parts/part-04/chapter-16/section-03.md`

### v2026.07.12
- 본문 반영: 부스팅 라이브러리 비교 절의 핵심 관점을 `체크리스트` 안으로 흡수해 말미 자기 확인 구조를 하나로 정리하고 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 번역본도 같은 날짜에 핵심 관점을 `Checklist` 안으로 흡수했고, XGBoost·LightGBM·CatBoost 비교축과 운영 병목 질문을 같은 구조로 맞췄다.

### v2026.07.11
- 본문 반영: `docs/parts/part-04/chapter-16/section-03.en.md`와 `docs/parts/part-04/chapter-16/section-03.zh.md`를 새로 열어 XGBoost·LightGBM·CatBoost 비교, histogram binning, GPU·distributed training, automation과 early stopping, gradient·hessian 읽기, 운영 사례를 같은 구조로 옮겼다. 또한 `docs/assets/part-04/chapter-16/` 아래에 `p4-16-3-mermaid-01-en.mmd`를 추가하고, 영어·중국어 본문이 해당 공용 자산을 참조하도록 정리했다.
- 번역 동기화 메모: 영어·중국어 번역 경로를 열었고, 현재 Chapter 16에서는 P4-16.1부터 P4-16.3까지 공용 `-en.mmd` 자산을 참조한다. 번역본에서도 boosting library를 `새 이름 암기`가 아니라 `무엇을 더 빠르게 하고 무엇을 더 안전하게 다루려 하는가`라는 비교축으로 유지한다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-16/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 현재는 한국어 자산만 운영하도록 README와 본문 참조를 맞췄고, 같은 작업에서 Section 버전을 갱신했다.

### v2026.07.10
- 본문 반영: `P4-16.3 보충학습: 부스팅 라이브러리와 운영 감각`을 새로 추가해 XGBoost, LightGBM, CatBoost의 구현 감각 차이, histogram binning, GPU·distributed training, 교차검증 자동화와 early stopping의 연결, gradient와 hessian의 입문적 의미를 한 절로 묶어 회수했다. 고객 이탈과 사기 탐지 장면도 사례로 덧붙여 구현 차이와 운영 감각이 실제 문제 장면에서 어떻게 읽히는지 연결했다.
- 추가 반영: 구현 차이 설명의 학습 밀도를 더 올렸다. 세 라이브러리를 `어떤 병목을 먼저 줄이려 했는가`와 `어떤 데이터 장면에서 먼저 떠오르는가`로 다시 비교하는 표를 추가했고, LightGBM의 leaf-wise 성장, CatBoost의 범주형 장면, histogram binning의 장난감 숫자 예시, GPU·분산·자동화의 병목 구분 표, gradient와 hessian의 방향·민감도 표, 실무 판단 요약 문장을 덧붙여 이름 나열에서 실제 선택 기준으로 이어지게 보강했다.
- 번역 동기화 메모: 향후 번역본에서도 구현 차이와 운영 감각을 `새 알고리즘 나열`이 아니라 `무엇을 더 빠르게 하고 무엇을 더 안전하게 다루려는가`라는 비교축으로 유지해야 한다.
