# Section Release Note

- Section ID: `P4-13.2`
- Source File: `docs/parts/part-04/chapter-13/section-02.md`

### v2026.07.08

- 변경 이유: 커널 사례가 표현 공간 변경의 의미를 설명하지만, 원래 공간과 새 표현 공간을 대비하는 사례 도식이 부족했다.
- 본문 반영: 불량 패턴 사례 아래에 original feature space, awkward linear boundary, interaction view, new feature space, clearer linear separation을 보여 주는 Mermaid 도식을 추가하고 Section 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 원래 공간과 새 표현 공간의 대비 구조를 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.08`

### v2026.07.09

- 변경 이유: Module 4가 개별 모델 설명은 충분해도, 같은 문제 장면에서 어떤 질문 구조로 모델을 다시 비교해야 하는지 한눈에 묶는 통합 비교 장면이 약했다.
- 본문 반영: `같은 작은 데이터 장면으로 Module 4를 다시 비교하면` 절을 추가해 선형회귀, 로지스틱 회귀, k-NN, 선형 SVM, 커널 SVM을 `먼저 보는 비교축`과 `다음 질문` 기준으로 다시 묶고 본문 버전을 갱신했다.
- 본문 반영: 같은 날짜 후속 보강으로 `학습 배경` 앞부분의 운영형 기록 표와 kernel 후보 판단 표를 뒤쪽 사례 블록으로 옮기고, `P4-11.2의 선형 경계`, `P4-12.2의 표현과 계산 기준`, `P4-13.1의 같은 공간 안의 경계 개선`과 현재 절의 차이를 앞에서 직접 연결했다. 또한 `직접 특징 추가`와 `kernel의 우회 계산` 관계, `좋은 선형 경계 조정`과 `표현 공간 변경`의 차이를 별도 문단과 표로 보강해, 재배치 뒤에도 남아 있던 학습 공백을 메우도록 수정했다.
- 번역 동기화 메모: 향후 번역본에서도 모델 목록이 아니라 문제 구조 비교표로 읽히도록 같은 통합 비교 절을 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.10

- 변경 이유: 구조 표지가 현재 원칙의 `사례 및 예시` 축과 어긋난 부분을 정리할 필요가 있었다.
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.10`

### v2026.07.11

- 변경 이유: Part 4의 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 관리해 차트 가이드라인의 Mermaid 자산 운영 방식과 맞출 필요가 있었다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-13/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 이어서 영문 기준 `p4-13-2-mermaid-01-en.mmd`부터 `p4-13-2-mermaid-03-en.mmd`까지 추가하고, `docs/parts/part-04/chapter-13/section-02.en.md`와 `docs/parts/part-04/chapter-13/section-02.zh.md`를 열었다.
- 추가 반영: 영어·중국어 번역본에 `13.1 선형 경계 -> 13.2 표현 공간` 연결 문단, 커널 후보 판단 설명, Module 4 통합 비교표의 정리 문단을 보강해 원문 후반 구조와 비교 질문을 더 가깝게 맞췄다.
- 번역 동기화 메모: 영어·중국어 번역 경로를 열었고, 중국어판은 현재 Chapter 13에서 공용 `-en.mmd` 자산을 직접 참조한다. 이번 보강으로 두 번역본 모두 커널을 `표현 공간 재설계` 관점으로 읽는 원문 흐름을 더 직접적으로 따르도록 정리했다.
- 번역 반영 상태: 영어/중국어 반영
- 관련 자산: `docs/assets/part-04/chapter-13/`
- 원문 기준 버전: `v2026.07.11`

- 변경 이유: 학습 밀도 검토 결과 `P4-13.2`는 커널의 직관, 직접 특징 추가와의 관계, 대표 커널 비교, Module 4 통합 비교가 한 절 안에 빠르게 몰려 있어 초심자 기준의 절 내부 속도를 늦출 필요가 있었다.
- 본문 반영: 절 초반에 `이 절을 읽는 순서`를 추가해 `선형 한계 -> 표현 변경 -> 직접 특징 추가와 kernel의 관계 -> 대표 커널 비교`의 읽기 경로를 먼저 고정했다. 또한 `직접 특징 추가`, `kernel이라는 이름`, `왜 새 특징을 다 만들지 않아도 되는가` 구간의 중복 질문을 줄이고 표 중심으로 재배치해 같은 개념을 두 번 설명하는 흐름을 완화했다.
- 본문 반영: 중복된 `사례 및 예시` 표지를 한 흐름으로 정리하고, `같은 작은 데이터 장면으로 Module 4를 다시 비교하면` 절은 `고급 모델 순서`가 아니라 `같은 장면에서 먼저 보는 질문 차이`를 재정리하는 표라는 점이 더 직접적으로 드러나게 다듬었다.
- 번역 동기화 메모: 향후 번역본에서도 `이 절을 읽는 순서`와 `직접 특징 추가 vs kernel` 비교 표를 유지해 절 내부 속도를 먼저 낮추는 구조를 보존해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.11`
