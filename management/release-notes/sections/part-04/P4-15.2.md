# Section Release Note

- Section ID: `P4-15.2`
- Source File: `docs/parts/part-04/chapter-15/section-02.md`

### v2026.07.11
- 본문 반영: `docs/parts/part-04/chapter-15/section-02.en.md`와 `docs/parts/part-04/chapter-15/section-02.zh.md`를 새로 열어 중요도 해석 경계, PDP/SHAP 비교, 인과 추론 경계, correlated feature 주의점, Python 예제를 같은 구조로 옮겼다. 또한 `docs/assets/part-04/chapter-15/` 아래에 `p4-15-2-mermaid-01-en.mmd`부터 `p4-15-2-mermaid-03-en.mmd`까지 추가하고, 영어·중국어 본문이 해당 공용 자산을 참조하도록 정리했다.
- 번역 동기화 메모: 영어·중국어 번역 경로를 열었고, 현재 Chapter 15에서는 P4-15.1과 P4-15.2가 공용 `-en.mmd` 자산을 참조한다. 번역본에서도 `importance는 사용량 요약`이라는 경계와 MDI / permutation / causal reading의 구분을 같은 흐름으로 유지한다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-15/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 현재는 한국어 자산만 운영하도록 README와 본문 참조를 맞췄고, 같은 작업에서 Section 버전을 갱신했다.

### v2026.07.12
- 본문 반영: permutation importance 예제에 `baseline accuracy` 출력을 추가해 `기준 성능 -> 특징 섞기 -> 성능 흔들림 해석` 흐름이 더 직접 보이게 정리했다.
- 추가 반영: `직접 판단해 보기` 표를 `연습 및 예제` 안에 추가해, `MDI가 높다`, `permutation 값이 낮다`, `두 중요도 값이 다르다`는 관찰을 각각 어떤 문장으로 읽어야 안전한지 독자가 바로 점검하게 보강했다.
- 번역 동기화 메모: 영어판과 중국어 간체판에도 permutation example의 `baseline accuracy` 출력, `직접 판단해 보기` 연습표, `체크리스트` 통합 구조를 같은 위치에 반영했다. / reflected in English and Simplified Chinese on 2026-07-12

### v2026.07.10
- 본문 반영: `이 절의 범위` 아래 구조를 다시 정리해 `PDP`, `SHAP`, 인과 추론 관점, 강한 상관 특성의 해석 전략을 현재 절 안에서 어떻게 회수할지 명시했다.
- 본문 반영: 상관 특성이 importance를 어떻게 갈라 보이게 하는지 작은 표와 설명을 추가하고, `중요도 숫자를 보고 바로 무엇을 해야 하나`, `중요도가 높거나 낮을 때 바로 내리기 쉬운 잘못된 결론` 구간으로 해석 순서를 보강했다.
- 번역 동기화 메모: 향후 번역본에서도 `PDP/SHAP와 중요도의 질문 차이`, `중요도와 인과 해석의 경계`, `강한 상관 특성 데이터의 보수적 해석 전략`을 함께 유지해야 한다. 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.

### v2026.07.08
- 본문 반영: 마케팅 사례 아래에 high importance observation, MDI와 permutation 점검, correlated/high-cardinality review, cautious interpretation 흐름의 Mermaid 도식을 추가하고 Section 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 중요도 관찰과 해석 경계의 분리 구조를 유지해야 한다.

### v2026.07.07
- 본문 반영: `입문 수준에서는`과 `이 절에서는 다음 순서로 읽습니다`를 각각 개념 설명과 계산 절차 설명 문장으로 교체했다.
- 번역 동기화 메모: 향후 번역본에서도 조언형 도입 대신 직접 설명하는 구조를 유지해야 한다.
