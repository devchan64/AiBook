# Section Release Note

- Section ID: `P4-14.2`
- Source File: `docs/parts/part-04/chapter-14/section-02.md`

### v2026.07.20
- 저작권 검토: Part 4 Module 5 순차 검토 범위에서 트리 과적합, 깊이·가지치기·일반화 설명, iris 기반 Python 예제를 확인했다. 표·사례·코드는 본문 학습 목적에 맞춘 자체 작성 또는 scikit-learn 내장 데이터 호출 예제로 판단했다.
- 출처 사용 점검: scikit-learn Decision Trees, `DecisionTreeClassifier`, CART 문헌은 과적합·pruning 개념 근거로만 사용했고, 원문 표·그림·코드·데이터셋 설명을 재사용한 흔적은 발견하지 않았다.
- 관련 자산: `p4-14-2-mermaid-01-ko.mmd`부터 `p4-14-2-mermaid-04-ko.mmd`까지는 본문 사례 흐름을 압축한 자체 도식으로 확인했다.
- 본문 반영: 저작권 기준에서 수정이 필요한 문장이나 자산은 없어 본문과 `Version`은 변경하지 않았다. 원문 기준 버전은 `v2026.07.19`이다.
- 번역 동기화 메모: 번역본 영향 없음.

### v2026.07.19
- 근거 보강: Breiman et al.의 *Classification and Regression Trees* 출처에 Routledge DOI 링크와 확인 날짜를 추가하고 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본을 갱신할 때도 트리 과적합과 가지치기 보충 문헌 출처 링크를 같은 기준으로 맞춰야 한다.

### v2026.07.17
- 본문 반영: `깊게 다루지 않습니다` 목록을 제거하고, 이번 절은 트리 질문 흐름이 어디서부터 예외 암기로 바뀌는지 먼저 붙잡고 랜덤포레스트·부스팅·튜닝은 뒤 장으로 넘기는 구조로 범위 안내를 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 handoff 중심 범위 안내를 반영해야 한다.

### v2026.07.12
- 본문 반영: 메타형 `Part 4 목표 회수` 블록을 제거하고, 말미 구조를 `체크리스트` 하나로 통합했다. train/test 분리, 과적합 신호, 복잡도 손잡이 해석을 자기 점검 질문으로 다시 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어판과 중국어판에서도 말미의 `Part 4 목표 회수`, 회상 블록, 적용 시점 블록을 제거하고 `Checklist` 단일 구조로 맞췄다.

### v2026.07.11
- 본문 반영: `docs/assets/part-04/chapter-14/` 아래 `p4-14-2-mermaid-01-en.mmd`부터 `p4-14-2-mermaid-04-en.mmd`까지 추가하고, `docs/parts/part-04/chapter-14/section-02.en.md`와 `docs/parts/part-04/chapter-14/section-02.zh.md`를 열었다. 같은 작업에서 Chapter 14 자산 README와 다국어 `mkdocs.yml` nav도 갱신했다.
- 번역 동기화 메모: 영어·중국어 번역 경로를 열었고, 중국어판은 현재 Chapter 14에서 공용 `-en.mmd` 자산을 직접 참조한다.

### v2026.07.08
- 본문 반영: 공장 불량 탐지 사례 아래에 more splits, tiny leaves, train fit rise, test stability drop, depth or pruning control 흐름의 Mermaid 도식을 추가하고 Section 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 과적합 흐름과 제어 손잡이 연결 구조를 유지해야 한다.

### v2026.07.09
- 본문 반영: 고객 이탈 예측 트리가 review 기준보다 소수 고객 조합을 더 자세히 외우기 시작하는 사례를 `사례 2`로 추가했다. 사람이 먼저 보던 기준과 지나치게 깊어진 트리가 붙잡기 쉬운 조합을 비교 표로 붙였고, 과적합을 `잔질문이 새 고객에서도 반복될 만한가`라는 질문으로 다시 읽게 하는 해설을 보강했다. 추가 보강에서는 고객 A-D의 작은 입력 장면 표, `얕은 읽기 vs 지나치게 깊은 읽기` 비교 표, `review rule becomes fragile`로 끝나는 짧은 Mermaid 흐름도를 더해 review 기준이 어떻게 흔들리는지 더 직접적으로 보이게 했다. 같은 날 제조 불량 탐지 `사례 1`도 제품 A-D의 센서 장면 표, `비교적 단순한 읽기 vs 지나치게 깊은 읽기` 비교 표, `큰 기준 vs 드문 센서 조합` 대비 해설을 더해 공장 데이터의 예외 leaf가 어떤 식으로 만들어지는지 더 직접 보이게 보강했다. 이어서 `pruning은 무엇을 하는가` 단락에는 `pre-pruning vs pruning` 비교 표, pruning 전후 가지 상태 표, 좁은 leaf를 다시 볼 때의 해석 문장, `ccp_alpha` 방향성 설명을 추가해 초심자가 `무엇이 잘리고 무엇이 남는가`를 더 직접 읽게 보강했다. 추가로 `과적합을 데이터 흐름으로 보기` 단락에는 앞쪽 분기, 중간 분기, 뒤쪽 분기, 마지막 leaf를 나누어 보는 흐름 표와 `큰 패턴 설명 -> 소수 사례 외우기` 전환 해설을 더해, 과적합이 데이터 흐름 안에서 어디서 시작되는지 더 직접 읽게 보강했다. `Python 예제로 깊이에 따른 과적합 보기`에는 입력 비교 표, `train-test gap` 출력, 깊이 구간별 읽기 표, 값을 바꿔 다시 볼 질문을 추가해 예제에서 무엇을 바로 비교해야 하는지 더 직접 보이게 했다. Section 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 제조 불량 탐지와 고객 이탈 예측의 두 사례가 `예외 leaf`와 `소수 고객 조합 외우기`를 대비해 보여 주는 역할을 유지해야 한다.

### v2026.07.10
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.

### v2026.07.11
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-14/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 현재는 한국어 자산만 운영하도록 README와 본문 참조를 맞췄고, 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 현재 이 Section의 번역 경로는 열려 있지 않아 한국어 Mermaid 자산만 운영한다. 번역본이 열리면 같은 폴더에 `-en.mmd` 기준 원본과 필요한 파생본을 추가해야 한다.
