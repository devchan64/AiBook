# Section Release Note

- Section ID: `P4-11.4`
- Source File: `docs/parts/part-04/chapter-11/section-04.md`

### v2026.07.11

- 변경 이유: Part 4의 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 관리해 차트 가이드라인의 Mermaid 자산 운영 방식과 맞출 필요가 있었다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-11/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 현재는 한국어 자산만 운영하도록 README와 본문 참조를 맞췄고, 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 현재 이 Section의 번역 경로는 열려 있지 않아 한국어 Mermaid 자산만 운영한다. 번역본이 열리면 같은 폴더에 `-en.mmd` 기준 원본과 필요한 파생본을 추가해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: `docs/assets/part-04/chapter-11/`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.09

- 변경 이유: 기존 `P4-11.3`이 log-odds, MLE, 다중 클래스, solver와 regularization을 한 절에 함께 담아 중심 질문이 분산되어 있어, 다중 클래스(multinomial) 확장을 별도 보충학습으로 분리할 필요가 생겼다.
- 본문 반영: 새 `P4-11.4 보충학습: 다중 클래스(multinomial) 로지스틱 회귀를 읽는 법`을 추가했다. 클래스별 점수와 softmax 확률식, one-vs-rest와 multinomial 비교, 다중 클래스 로그우도의 최소 합 형태, 고객 문의 분류 사례, argmax 중심 읽기 예제와 Python 확률표 예제를 구성했다. 이어서 레포 원칙 재검토에 따라 `다루지 않는다` 문장 바로 뒤에 후속 회수 위치와 범위 밖 판단을 명시했고, 절 끝에 `출처와 참고 자료`를 추가했다.
- 번역 동기화 메모: 향후 번역본에서도 `threshold` 중심의 이진 분류 감각에서 `argmax` 중심의 다중 클래스 감각으로 옮겨 가는 설명 순서를 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`
