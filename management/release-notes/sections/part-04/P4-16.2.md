# Section Release Note

- Section ID: `P4-16.2`
- Source File: `docs/parts/part-04/chapter-16/section-02.md`

### v2026.07.19
- 근거 보강: Friedman의 `Greedy Function Approximation`과 `Stochastic Gradient Boosting` 출처에 DOI 링크와 확인 날짜를 추가하고 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본을 갱신할 때도 부스팅 성능·위험 설명의 고전 논문 출처 링크를 같은 기준으로 맞춰야 한다.

### v2026.07.12
- 본문 반영: 말미 구조를 `체크리스트` 하나로 통합하고, validation/test 확인, 세 손잡이의 결합 해석, shrinkage·subsampling·early stopping의 역할을 자기 점검 질문으로 다시 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 번역본도 같은 날짜에 `Checklist` 단일 구조로 맞췄고, validation/test 확인과 세 손잡이 해석을 같은 질문 밀도로 다시 정리했다.

### v2026.07.11
- 본문 반영: `docs/parts/part-04/chapter-16/section-02.en.md`와 `docs/parts/part-04/chapter-16/section-02.zh.md`를 새로 열어 boosting의 강점과 과적합 위험, `learning_rate`·`n_estimators`·tree size 조합, fraud/churn 사례, toy residual 예제와 실무 점검표를 같은 구조로 옮겼다. 또한 `docs/assets/part-04/chapter-16/` 아래에 `p4-16-2-mermaid-01-en.mmd`부터 `p4-16-2-mermaid-03-en.mmd`까지 추가하고, 영어·중국어 본문이 공용 `-en.mmd` 자산을 참조하도록 정리했다.
- 번역 동기화 메모: 영어·중국어 번역 경로를 열었고, 현재 Chapter 16에서는 P4-16.1과 P4-16.2가 공용 `-en.mmd` 자산을 참조한다. 번역본에서도 `강한 성능 신호`와 `과한 보정 위험`을 동시에 읽는 흐름, early stopping 판단, fraud/churn 운영 사례를 같은 밀도로 유지한다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-16/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 현재는 한국어 자산만 운영하도록 README와 본문 참조를 맞췄고, 같은 작업에서 Section 버전을 갱신했다.

### v2026.07.07
- 본문 반영: `입문 수준에서는 다음 비교가 실용적입니다`를 두 방식의 차이를 직접 드러내는 문장으로 교체했다. subsampling 설명 앞의 `독자 관점에서` 표현도 제거했다.
- 번역 동기화 메모: 향후 번역본에서도 비교 도입을 조언형이 아니라 설명형으로 유지해야 한다.

### v2026.07.08
- 본문 반영: 사기 탐지 사례 아래에 `train 성능 과열 -> validation 흔들림 -> learning rate, 단계 수, 깊이, early stopping 재점검` 흐름을 보여 주는 Mermaid 도식을 추가하고 본문 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 과적합 신호를 개념 설명이 아니라 사례 흐름으로 따라가도록 같은 도식을 반영해야 한다.

### v2026.07.09
- 본문 반영: `연습 및 예제` 절로 재구성하고 추가 correction stage 비교, Part 4 목표 회수 문단, `보인 구조 / 해석 경계 / 다음 질문` 공통 기록 표, 결정트리·랜덤포레스트·부스팅을 같은 표 데이터 장면으로 다시 읽는 비교 표를 추가했다.
- 번역 동기화 메모: 향후 번역본에서도 보정 단계 추가 비교와 멈춤 판단 문단, 공통 기록 표를 함께 유지해야 한다.

### v2026.07.10
- 본문 반영: `다루지 않는 내용` 아래 회수 위치 표를 다시 정리해 XGBoost·LightGBM·CatBoost, GPU·histogram binning·distributed training, 교차검증 자동화, 손실 함수 gradient 흐름을 현재 절 안에서 어떻게 이어 읽는지 명시했다. 또한 고객 이탈 입력 표, 설정별 train/validation 비교 표, 사기 탐지 점검 표, 고객 이탈 운영 사례와 Mermaid 도식을 추가해 예시와 사례를 더 구체화했다.
- 추가 반영: XGBoost·LightGBM·CatBoost의 구현 감각 비교, histogram binning·GPU·distributed training이 함께 등장하는 이유, 교차검증 자동화와 early stopping의 연결, 제곱오차와 로그 손실을 예로 든 negative gradient 흐름을 현재 절 안에서 직접 회수했다.
- 번역 동기화 메모: 향후 번역본에서도 구현 비교 표, 계산 구조 요약, 교차검증 자동화와 멈춤 판단의 연결, 손실 함수 gradient 설명, 고객 이탈·사기 탐지 사례의 입력 표와 운영 판단 흐름을 함께 유지해야 한다. 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
