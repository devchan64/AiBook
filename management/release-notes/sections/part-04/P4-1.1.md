# Section Release Note

- Section ID: `P4-1.1`
- Source File: `docs/parts/part-04/chapter-01/section-01.md`

### v2026.07.12

- 변경 이유: 말미의 `이 절에서 기억할 관점`과 `언제 이 관점을 먼저 떠올려야 하는가`가 `체크리스트`와 역할이 겹쳐 자기 확인 구조가 분산되어 있었다.
- 본문 반영: 말미 구조를 `체크리스트` 하나로 통합하고, 용어 구분·Part 2 연결·서비스 구성 설명을 자기 점검 질문으로 다시 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본도 한국어 원문과 같은 단일 `Checklist` 구조로 맞추고, `Perspective`·`When...` 계열 말미 블록을 정리했다.
- 번역 반영 상태: 영어·중국어 간체 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.08

- 변경 이유: 챗봇 경험에서 곧바로 `AI = LLM`으로 뛰는 오해를 사례 수준에서 더 빠르게 끊어 주는 시각적 흐름이 필요했다.
- 본문 반영: 사례 1 아래에 `도구 경험 -> LLM 동일시`와 `서비스 구조 분해 -> LLM은 한 구성요소`를 대비하는 Mermaid 도식을 추가하고 본문 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 LLM 경험을 AI 전체와 동일시하지 않도록 서비스 구성요소 분해 흐름을 같은 도식으로 유지해야 한다.
- 번역 반영 상태: 영어·중국어 간체 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.08`

### v2026.07.10

- 변경 이유: 구조 표지가 현재 원칙의 `사례 및 예시` 축과 어긋난 부분을 정리할 필요가 있었다.
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 본문 반영: 한국어 본문에 남아 있던 Mermaid 도식 3개를 한국어 파생 SVG 자산으로 분리하고, 한국어판은 한글 도식을 직접 참조하도록 바꿨다.
- 번역 동기화 메모: English and Simplified Chinese translations now preserve the `Cases and Examples` heading and keep the LLM-versus-service-structure diagrams aligned with the Korean source. / 영문·중국어 간체 번역본에 `사례 및 예시` 구조 표지와 서비스 구성요소 분해 도식을 함께 반영했다.
- 번역 동기화 메모: 영문 기준 원본 SVG 자산도 함께 추가했지만, 현재 영어판과 중국어간체판 본문은 기존 Mermaid를 유지한다. 이번 변경은 한국어판 도식 한글화 적용이 중심이다.
- 번역 반영 상태: 영어·중국어 간체 반영 완료
- 원문 기준 버전: `v2026.07.10`

### v2026.07.11

- 변경 이유: Part 4의 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 관리해 차트 가이드라인의 Mermaid 자산 운영 방식과 맞출 필요가 있었다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-01/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 영문·중국어 간체 본문도 같은 자산 패턴으로 동기화했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams in this section.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: `docs/assets/part-04/chapter-01/`
- 원문 기준 버전: `v2026.07.11`

- 변경 이유: P4-1.1의 관계도와 서비스 흐름도는 좌표 정밀도보다 개념 관계가 중심이라, 외부 SVG 자산보다 본문 Mermaid 코드블록으로 유지하는 편이 수정과 검토에 더 유리했다.
- 본문 반영: `AI-머신러닝-딥러닝-생성형 AI-LLM` 관계도, 서비스 구성도, `AI = LLM` 오해 대비 도식을 모두 Mermaid로 다시 넣고 SVG 참조를 제거했다. / 같은 작업에서 말미 표지를 `체크리스트`로 통일하고 Section 버전을 갱신했다. / 이어서 한국어 본문에서 학습 흐름을 더 직접 읽을 수 있도록 Mermaid 노드 라벨도 한글 중심으로 다시 정리했다.
- 본문 반영: 같은 세 Mermaid 도식을 `docs/assets/part-04/chapter-01/` 아래 `-en`/`-ko`/`-zh` `.mmd` 자산으로 분리하고, 한·영·중 본문은 외부 include 방식으로 참조하도록 정리했다.
- 번역 동기화 메모: 영문 번역본은 Mermaid 기준 구조를 유지한 채 `Checklist` 표지와 `v2026.07.11` 버전을 맞췄다. 중국어 간체 번역본은 같은 Mermaid 구조를 유지하면서 도식 라벨을 중국어 본문 기준으로 정리하고 `检查清单` 표지를 반영했다.
- 번역 반영 상태: 영어·중국어 간체 반영 완료
- 관련 자산: `docs/assets/part-04/chapter-01/ai-scope-map-en.mmd`, `docs/assets/part-04/chapter-01/ai-scope-map-ko.mmd`, `docs/assets/part-04/chapter-01/ai-scope-map-zh.mmd`, `docs/assets/part-04/chapter-01/ai-service-composition-map-en.mmd`, `docs/assets/part-04/chapter-01/ai-service-composition-map-ko.mmd`, `docs/assets/part-04/chapter-01/ai-service-composition-map-zh.mmd`, `docs/assets/part-04/chapter-01/ai-llm-scope-misconception-flow-en.mmd`, `docs/assets/part-04/chapter-01/ai-llm-scope-misconception-flow-ko.mmd`, `docs/assets/part-04/chapter-01/ai-llm-scope-misconception-flow-zh.mmd`
- 원문 기준 버전: `v2026.07.11`
