# Section Release Note

- Section ID: `P4-11.1`
- Source File: `docs/parts/part-04/chapter-11/section-01.md`

### v2026.07.20
- 저작권 검토: Part 4 Module 4 순차 검토 범위에서 로지스틱 회귀 직관, sigmoid와 threshold 설명, 합격 예측 Python 예제를 확인했다. 표·사례·코드는 자체 작성 학습 예제로 판단했다.
- 출처 사용 점검: scikit-learn Logistic regression User Guide와 `LogisticRegression` API 문서는 개념·도구 근거로만 사용했고, 원문 예제 코드·표·그림·데이터셋을 재사용한 흔적은 발견하지 않았다.
- 관련 자산: `p4-11-1-sigmoid-score-map-ko.png`, `p4-11-2-threshold-shift-ko.png`, `p4-11-1-mermaid-01-ko.mmd`, `p4-11-1-mermaid-02-ko.mmd`는 본문 설명을 위해 만든 자체 도식으로 확인했다.
- 본문 반영: 저작권 기준에서 수정이 필요한 문장이나 자산은 없어 본문과 `Version`은 변경하지 않았다. 원문 기준 버전은 `v2026.07.17`이다.
- 번역 동기화 메모: 번역본 영향 없음.

### v2026.07.17
- 본문 반영: `이 절은 다음 내용은 깊게 다루지 않습니다` 목록을 제거하고, 이번 절이 로지스틱 회귀를 `선형 계산의 출력을 분류용 확률처럼 읽는 모델`로 먼저 닫는다는 현재 역할이 먼저 보이도록 범위 문단을 다시 정리했다.
- 본문 반영: `P4-11.2`, `P4-11.3`, `P4-11.4`, `P4-11.5`, `P4-9.1`, `P4-9.2`, `P5-8.1`로 이어지는 실제 handoff만 남기고, log-odds 엄밀 유도·MLE 수식 전개·다중 클래스 세부 수식·solver 차이 같은 바깥 항목 나열은 제거했다. 본문 메타데이터 `Version`도 `v2026.07.17`로 갱신했다.
- 번역 동기화 메모: 향후 번역본도 범위 문장을 같은 handoff 구조로 다시 맞출 필요가 있다.

### v2026.07.11
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-11/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 이어서 영문 기준 `p4-11-1-mermaid-01-en.mmd`, `p4-11-1-mermaid-02-en.mmd`를 추가하고, `docs/parts/part-04/chapter-11/section-01.en.md`와 `docs/parts/part-04/chapter-11/section-01.zh.md`를 열어 다국어 본문이 같은 자산 체계를 참조하도록 맞췄다.
- 추가 반영: 영어·중국어 번역본에 원문 후반의 논의점, 사례 비교 프레임, 의료/대출 사례, 장난감 Python 예제를 보강해 한국어 원문과 비슷한 설명 밀도로 맞췄다.
- 번역 동기화 메모: 영어·중국어 번역 경로를 열었고, 중국어판은 현재 Chapter 11에서 공용 `-en.mmd` 자산을 직접 참조한다. 이번 보강으로 두 번역본 모두 원문 구조의 후반 사례·예제 축을 따라가도록 정리했다.

### v2026.07.13
- 본문 반영: 같은 날짜의 초기 반영에서는 영어 번역본 `section-01.en.md`의 `p4-11-1-sigmoid-score-map-en.svg`, `p4-11-2-threshold-shift-en.svg` 참조를 사이트 절대 경로로 정규화했다.
- 번역 동기화 메모: 한국어·영어·중국어 본문 모두 같은 Python 스크립트에서 생성한 언어별 PNG를 참조하도록 동기화했다.

### v2026.07.19
- Python 자산 점검: 로지스틱 회귀 sigmoid 차트 생성 스크립트의 한국어·중국어 폰트 후보에 Noto Sans CJK 계열을 추가해, Linux 검증 환경에서 한글/CJK 글리프 누락 경고 없이 차트를 재생성할 수 있게 했다.

### v2026.07.12
- 본문 반영: sigmoid 직관 단락 아래에 `p4-11-1-sigmoid-score-map-en.svg`를 추가해 `z = 0`, `p = 0.5`, class 0/1 쪽의 감각을 좌표형 차트로 보강했고, threshold 설명 구간에는 `p4-11-2-threshold-shift-en.svg`를 추가해 같은 점수도 정책 경계에 따라 다른 행동으로 이어진다는 점을 시각적으로 보강했다. 같은 작업에서 Section 버전을 갱신했다.
- 추가 반영: `predict_proba`와 threshold 설명 사이에 `11.1은 출력 점수 축을 읽는 절`이라는 연결문을 추가하고, threshold 설명도 입력 공간 경계가 아니라 `점수 축 위 cutoff`를 먼저 읽는 방식으로 정리해 다음 절 `P4-11.2`와의 역할 경계를 더 분명하게 다듬었다.
- 번역 동기화 메모: 한국어 원문에는 `p4-11-1-sigmoid-score-map-ko.svg`, 영어 번역본에는 `p4-11-1-sigmoid-score-map-en.svg`, 중국어 번역본에는 `p4-11-1-sigmoid-score-map-zh.svg`를 연결했다. threshold 시각화도 영어·중국어 번역본에 각각 연결하고, 빠져 있던 `학술적 배경과 역사` 보충 단락을 영어·중국어에 함께 반영했다. 이번 정리에서 영어·중국어 번역본의 말미 메타 소제목을 제거하고 `Checklist` 하나로 맞췄으며, 중국어판 개념사전 링크도 상대 경로로 바로잡았다.

### v2026.07.08
- 본문 반영: 고객 이탈 예측 사례 아래에 features, linear score, sigmoid output, threshold policy, action으로 이어지는 Mermaid 도식을 추가하고 Section 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 점수 생성과 정책 행동의 분리 구조를 유지해야 한다.

### v2026.07.09
- 본문 반영: 생략 항목을 P4-11.2와 보충학습 축으로 다시 배치하고, Chapter 11 내부 연결을 `P4-11.3 log-odds와 MLE`, `P4-11.4 다중 클래스`, `P4-11.5 solver와 regularization`의 세 갈래 구조에 맞게 정리했다. 같은 날 사례·예시·예제 관점의 재검토로 사례 비교 프레임 표, Python 예제 입력 표, 값을 바꿔 다시 볼 관찰 포인트도 보강했다. 이어서 도식 관점 재검토로 sigmoid 흐름도와 고객 이탈 사례 도식의 박스 문구를 더 짧게 정리해 모바일 폭에서도 압축 역할이 유지되게 다듬었다. 추가 보강에서는 고객 이탈, 스팸, 의료 위험, 대출·사기 탐지 사례에 작은 입력-점수-행동 표를 붙여 사례 장면의 추상도를 낮추고, 장난감 Python 예제의 훈련 데이터를 표로 다시 풀어 읽게 했다. 이후 리포트 기준 흡수 작업으로 경계 근처 정답 하나를 바꾸는 반복 변화 실습과 `무엇이 유지되고 무엇이 달라지는가`, `이 연습이 Part 4 목표를 어떻게 회수하는가`, `보인 구조 / 해석 경계 / 다음 질문` 기록 표를 더해, 점수 흔들림과 데이터 경계 해석을 직접 회수했다. 추가 정리에서는 현재 MkDocs 수식 렌더링 형식에 맞게 이중 이스케이프된 블록 수식 구문을 바로잡았다. Section 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 `직관 절 -> 결정 경계 -> 세 갈래 보충학습`의 연결 순서를 유지해야 한다.

### v2026.07.10
- 본문 반영: `실증 예시로 보면 더 분명해진다` 보조 소제목을 일반 도입 문단으로 정리해, 같은 블록 안에서는 사례 번호 체계와 공통 비교 프레임이 바로 이어지도록 구조를 단순화했다.
- 번역 동기화 메모: 향후 번역본에서도 로지스틱 회귀 사례 블록은 메타 안내 표지보다 사례 번호 체계를 우선 유지해야 한다.
