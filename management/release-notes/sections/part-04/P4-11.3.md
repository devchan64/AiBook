# Section Release Note

- Section ID: `P4-11.3`
- Source File: `docs/parts/part-04/chapter-11/section-03.md`

### v2026.07.11

- 변경 이유: Part 4의 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 관리해 차트 가이드라인의 Mermaid 자산 운영 방식과 맞출 필요가 있었다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-11/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 이어서 영문 기준 `p4-11-3-mermaid-01-en.mmd`, `p4-11-3-mermaid-02-en.mmd`를 추가하고, `docs/en/parts/part-04/chapter-11/section-03.md`와 `docs/zh/parts/part-04/chapter-11/section-03.md`를 열었다.
- 번역 동기화 메모: 영어·중국어 번역 경로를 열었고, 중국어판은 현재 Chapter 11에서 공용 `-en.mmd` 자산을 직접 참조한다.
- 번역 반영 상태: 영어/중국어 반영
- 관련 자산: `docs/assets/part-04/chapter-11/`
- 원문 기준 버전: `v2026.07.11`

### v2026.07.09

- 변경 이유: P4-11.1에서 언급된 log-odds, 최대우도추정(MLE), 다중 클래스, solver와 regularization이 후속 회수 없이 남아 있어, 초심자 기준 학습 경로를 닫는 보충학습이 필요했다.
- 본문 반영: 초안 단계에서 한 절에 모아 두었던 `log-odds`, `MLE`, `다중 클래스`, `solver / regularization` 내용을 세 갈래 보충학습으로 재구성했다. `P4-11.3`은 `log-odds와 MLE를 처음 읽는 법`으로 범위를 좁혀, sigmoid 식의 역방향 읽기, odds와 log-odds 표, 이진 우도와 로그우도 식, accuracy와 log loss의 차이를 보여 주는 사례와 Python 예제로 다시 구성했다. 같은 날 현재 MkDocs 수식 렌더링 형식에 맞게 블록 수식 구문도 유지 점검했고, 레포 원칙 재검토에 따라 `다루지 않는다` 문장 바로 뒤에 후속 회수 위치를 명시했으며, 절 끝에 `출처와 참고 자료`를 추가했다.
- 번역 동기화 메모: 향후 번역본에서도 `P4-11.3`은 log-odds와 MLE에만 집중하고, 다중 클래스와 설정 설명은 별도 Section으로 분리된 구조를 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`
