# Section Release Note

- Section ID: `P4-4.1`
- Source File: `docs/parts/part-04/chapter-04/section-01.md`

### v2026.07.20
- 저작권 검토: Part 4 Module 2 순차 검토 범위에서 학습/평가 분리 설명, 고객 이탈 예시 표, `train_test_split` Python 예제, 희소 라벨 예제를 확인했다. 예시 데이터와 코드 흐름은 본문 학습 목적에 맞춘 자체 작성 예제로 판단했다.
- 출처 사용 점검: scikit-learn Cross-validation, `train_test_split`, ISL 자료는 개념 근거와 도구 문서 위치로만 사용했고, 원문 표·그림·코드·데이터셋을 재사용한 흔적은 발견하지 않았다.
- 관련 자산: `docs/assets/part-04/chapter-04/p4-4-1-mermaid-01-ko.mmd`, `docs/assets/part-04/chapter-04/p4-4-1-mermaid-02-ko.mmd`는 본문 사례 흐름을 압축한 자체 도식으로 확인했다.
- 본문 반영: 저작권 기준에서 수정이 필요한 문장이나 자산은 없어 본문과 `Version`은 변경하지 않았다. 원문 기준 버전은 `v2026.07.19`이다.
- 번역 동기화 메모: 번역본 영향 없음.

### v2026.07.19
- Python 예제 검토: 연습 및 예제 도입부에서 반복되던 `문제 상황`, `입력(input)`, `기대 출력(output)`, `확인할 개념` 표지를 줄이고, 데이터 분리 후 샘플 수와 라벨 비율을 확인하는 흐름을 본문 설명으로 연결했다.
- 실행 가능성 보정: DataFrame 예제에서 본문 밖의 `df`를 암묵적으로 참조하던 문제를 없애고, 코드 블록 안에서 `pd.DataFrame`으로 입력 표를 직접 만들도록 수정했다.
- 출력 예시 정합성: pandas 3 출력 형식과 `train_test_split` 실제 분할 결과에 맞춰 DataFrame 예제, `test_size`, `random_state`, 희소 라벨 실습의 출력 예시를 갱신했다.
- 의존성 보강: Part 4의 scikit-learn 기반 Python 예제가 로컬 환경에서 실행될 수 있도록 `requirements.txt`에 `scikit-learn==1.9.0`을 추가했다.
- 출처 확인: scikit-learn Cross-validation, `train_test_split`, ISL 공식 웹사이트 위치를 다시 확인하고 참고 자료 확인 날짜를 최신 검토일 기준으로 갱신했다.
- 메타데이터: 본문 `Version`을 `v2026.07.19`로 갱신했다.
- 예제 검증: 로컬 `.venv`에 `scikit-learn==1.9.0`을 설치한 뒤 P4-4.1의 Python 코드 블록 5개가 모두 실행되고 본문 출력 예시와 일치함을 확인했다.

### v2026.07.12
- 본문 반영: 말미 구조를 `체크리스트` 하나로 통합하고, 학습/평가 분리, 시간 순 데이터, 점수 해석, 작은 데이터셋의 흔들림을 자기 점검 질문으로 다시 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본의 말미 `Perspective/When...` 구조를 제거하고, 한국어 원문과 같은 `Checklist` 단일 구조로 맞췄다. 같은 작업에서 EN/ZH 출처 목록도 한국어 원문 기준으로 다시 정렬했고, 두 번역본의 버전도 `v2026.07.12`로 올렸다.

### v2026.07.11
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-04/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 영문·중국어 간체 본문도 같은 자산 패턴으로 동기화했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams in this section.
- 본문 반영: 데이터 분리 개요 도식과 쿠폰 반응 사례 도식의 보이는 라벨을 한국어로 바꾸고 Section 버전을 갱신했다.

### v2026.07.07
- 본문 반영: 어떤 분리 관점을 먼저 써야 하는지 판정하는 문장을 직접 서술형으로 교체했다.
- 번역 동기화 메모: 향후 번역본에서도 데이터 분리 판단 문장을 설명형으로 맞춰야 한다.

### v2026.07.08
- 본문 반영: 사례 1 아래에 `whole-table 평가`와 `holdout 평가`를 대비하는 Mermaid 도식을 추가하고 본문 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 같은 쿠폰 반응 예측 사례에서 전체 표 재사용과 분리 평가의 차이를 같은 흐름으로 유지해야 한다.

### v2026.07.10
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 추가 반영: `체크리스트` 기준 원칙과 문단 재배치 리포트에 맞춰 자기 확인 질문을 절 후반 `이 절에서 기억할 관점` 뒤로 옮겼다. 점수 간격을 읽는 설명과 예시는 앞 본문에 남기고, 체크리스트는 절 전체 회수 블록으로 다시 정리했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
