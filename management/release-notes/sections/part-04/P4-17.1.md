# Section Release Note

- Section ID: `P4-17.1`
- Source File: `docs/parts/part-04/chapter-17/section-01.md`

### v2026.07.31
- 후속 보강: 소제목 정리에 그치지 않도록 사례 절 첫머리에 중심축 확인 질문 2개를 추가했다.
- 변경 이유: Part 4 보강 라운드에서 진행 메타 성격의 `사례 및 예시` 소제목을 내용형 제목으로 정리해 독자가 사례 절의 모델 학습 장면과 판단 기준을 바로 읽을 수 있게 했다.
- 소제목 정리: 한국어 원문의 `사례 및 예시` 절 제목을 `클러스터링의 직관: 확인할 판단 기준` 중심의 내용형 제목으로 바꿔 사례 절이 맡는 모델 학습 장면과 판단 기준을 바로 드러내도록 했다.
- 번역 동기화 메모: 이번 소제목 정리는 한국어 원문 기준으로 먼저 반영했으며, 영어판과 중국어 간체판은 후속 동기화가 필요하다.
- 번역 반영 상태: 한국어 원문 반영 완료, 영어판과 중국어 간체판 미반영.
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.31`

### v2026.07.26
- 본문 반영: 한국어판, 영어판, 중국어 간체판의 Section 버전을 갱신하고, 클러스터링·군집·라벨·비지도학습·유사도·k-means·DBSCAN·중심점·밀도 등 핵심 개념을 각 언어별 개념사전 직접 링크로 연결했다.
- 번역 반영 상태: 영어판의 진행 메타형 소제목을 `Questions Closed By Clustering`, `Judgments To Keep From Clustering`로 정리하고, 중국어판도 `聚类先收束的问题`, `聚类要留下的判断标准`로 맞췄다.
- 예제 보강: k-means와 DBSCAN 비교 Python 예제 앞에 `n_clusters`, `eps`, 외곽점 좌표를 바꿔 보며 결과 해석을 확인하는 값을 추가했다.
- 개념사전 반영: `cluster`, `k-means`, `DBSCAN`, `centroid`, `density`, `similarity`의 누락 언어 항목을 추가하거나 보강하고, 관련 색인 include와 P4-17.1 중심 Section 정보를 정리했다.
- 출처 확인: scikit-learn Clustering, `KMeans`, `DBSCAN` 문서를 다시 확인하고 확인 날짜를 `2026-07-26`으로 갱신했다.
- 관련 자산: 중국어 Mermaid 자산이 실제로 존재하므로 `docs/assets/part-04/chapter-17/README.md`의 P4-17.1 자산 목록에 `-zh.mmd` 파일을 추가했다.

### v2026.07.20
- 구조 정리: 한국어판, 영어판, 중국어 간체판에서 진행 메타 성격의 범위·목표·읽는 순서 제목을 각 Section의 개념, 판단 기준, 해석 흐름이 드러나는 구체 소제목으로 정리했다.
- 번역 반영 상태: 영어판과 중국어 간체판 반영 완료.
- 원문 기준 버전: `v2026.07.20`
- 본문 반영 내용: 한국어, 영어, 중국어 간체판의 Python 예제 첫 줄에 같은 점 집합에서 k-means와 DBSCAN이 외곽점 G를 어떻게 다르게 배정하는지 비교하는 목적을 설명하는 주석을 추가했다.
- 저작권 검토: Part 4 Module 5 순차 검토 범위에서 클러스터링 직관, k-means·DBSCAN 비교, 군집 사례와 Python 예제를 확인했다. 표·사례·코드는 본문 학습 목적에 맞춘 자체 작성 또는 scikit-learn API 호출 예제로 판단했다.
- 출처 사용 점검: scikit-learn Clustering, `KMeans`, `DBSCAN` 문서는 군집화 개념과 API 근거로만 사용했고, 원문 예제 코드·표·그림·데이터셋 설명을 재사용한 흔적은 발견하지 않았다.
- 관련 자산: `p4-17-1-mermaid-01-ko.mmd`부터 `p4-17-1-mermaid-07-ko.mmd`까지는 본문 사례 흐름을 압축한 자체 도식으로 확인했다.
- 본문 반영: 저작권 기준에서 수정이 필요한 문장이나 자산은 없어 본문과 `Version`은 변경하지 않았다. 원문 기준 버전은 `v2026.07.17`이다.
- 번역 동기화 메모: 영어·중국어 번역본을 `v2026.07.17`로 맞추고, 절 범위 안내와 영어판 체크리스트 구조를 원문과 동기화함.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 k-means 수식, DBSCAN 파라미터, 계층/스펙트럴 구현, silhouette 지표를 나열하던 목록을 걷어내고, 클러스터링의 중심 질문과 `P4-17.2`, `P4-17.3`, `P4-18.*`, `P4-6.4` handoff만 남기도록 정리했다. 같은 작업에서 Section 버전을 `v2026.07.17`로 갱신했다.
- 번역 동기화 메모: 영어·중국어 번역본도 같은 범위 안내를 삭제형 목록 대신 현재 절 질문과 후속 연결 중심으로 맞춰야 한다.

### v2026.07.12
- 본문 반영: 한국어 원문 `P4-17.1`의 `연습 및 예제`를 Python 코드와 출력 재현에서 `좌표 표를 먼저 읽고 묶음 가설과 경계 사례를 스스로 판단한 뒤 해설과 비교하는 표 기반 검증형 실습`으로 바꿨다. 두 번째 장면에서는 가운데 점 G를 추가해 `정답 번호`보다 `어디서 애매해지는가`를 먼저 기록하게 흐름을 재구성했다.
- 추가 반영: 같은 절 안에서 `k-means`와 `DBSCAN`의 차이를 출력 수준에서 직접 확인할 수 있도록, 두 개의 조밀한 묶음과 하나의 멀리 떨어진 점을 가진 장난감 데이터에 두 알고리즘을 나란히 적용하는 Python 비교 예제를 추가했다. `k-means`는 모든 점을 미리 정한 두 군집 안에 배정하고, `DBSCAN`은 멀리 떨어진 점을 노이즈로 남길 수 있다는 차이를 바로 읽게 정리했다.
- 번역 동기화 메모: 영어판과 중국어판도 같은 구조로 수정해, 좌표 표 기반 비교 실습 뒤에 `k-means vs DBSCAN` Python 비교 예제를 같은 위치에 반영했다.

### v2026.07.11
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-17/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 현재는 한국어 자산만 운영하도록 README와 본문 참조를 맞췄고, 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어판 `docs/parts/part-04/chapter-17/section-01.en.md`와 중국어판 `docs/parts/part-04/chapter-17/section-01.zh.md`를 열고, 같은 폴더에 공용 `-en.mmd` 기준 원본을 추가했다. 두 번역본 모두 라벨 없는 구조 탐색이라는 중심 질문, k-means/DBSCAN 비교, 고객·기사 사례, 간단 예제를 한국어 원문 구조에 맞춰 반영했다.

### v2026.07.08
- 본문 반영: 사례 1 아래에 `구매액 순위 -> 패턴 혼합 노출 -> 방문/금액/최근성 기반 군집 -> 후속 해석` 흐름을 보여 주는 Mermaid 도식을 추가하고 본문 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 군집을 바로 의미화하지 않고 패턴 제안에서 해석 검토로 넘어가는 흐름을 같은 도식으로 유지해야 한다.

### v2026.07.09
- 본문 반영: `연습 및 예제` 절로 재구성하고, 가운데 점 추가 비교, Part 4 목표 회수 문단, `보인 구조 / 해석 경계 / 다음 질문` 공통 기록 표를 넣어 `기본 실행 -> 값 변경 -> 해석 -> 다음 질문` 흐름을 명시했다.
- 번역 동기화 메모: 향후 번역본에서도 예제 실행 결과만 번역하지 말고 군집 가설이 흔들리는 비교와 목표 회수 문단을 함께 유지해야 한다.

### v2026.07.10
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 추가 반영: 차트 가이드라인 기준으로 `지도학습 vs 클러스터링` 비교 도식과 `k-means vs DBSCAN` 비교 도식을 다시 정리했다. 좌우 비교를 한 장에 몰아 넣던 구조를 더 짧은 흐름으로 분리하고, `군집 번호 -> 사람 검토 -> 의미 부여` 도식도 해석 단계가 더 직접 보이도록 다듬었다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
