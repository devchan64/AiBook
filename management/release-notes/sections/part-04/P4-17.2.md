# Section Release Note

- Section ID: `P4-17.2`
- Source File: `docs/parts/part-04/chapter-17/section-02.md`

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 군집 평가 지표, 고급 시각화, 반지도학습 구현 절차를 나열하던 목록을 걷어내고, 군집 해석의 주의점과 `P4-6.4`, `P4-18.2`, `P4-17.4` handoff만 남기도록 정리했다. 같은 작업에서 Section 버전을 `v2026.07.17`로 갱신했다.
- 번역 동기화 메모: 영어·중국어 번역본도 같은 범위 안내를 삭제형 목록 대신 현재 절 질문과 후속 연결 중심으로 맞춰야 한다.

### v2026.07.12
- 본문 반영: 한국어 원문 `P4-17.2`의 `연습 및 예제`를 Python 코드와 출력 재현에서 `고객 표를 먼저 읽고 번호만으로 무엇을 말할 수 있는지 판단한 뒤 해설과 비교하는 표 기반 검증형 실습`으로 바꿨다. 두 번째 장면에서는 번호 뒤집기 비교를 코드 없이 `바뀐 것 / 바뀌지 않은 것` 표로 다시 정리해, `번호 -> 의미` 오독을 더 직접 끊도록 조정했다.
- 번역 동기화 메모: 영어판과 중국어판도 같은 구조로 수정해, Python 재출력 블록을 제거하고 고객 표 기반 비교 실습으로 맞췄다.
- 추가 반영: 기준서에 따라 말미의 중복 설명 성격 문장을 `체크리스트` 중심으로 정리해 자기 확인 구조를 하나로 맞췄다.

### v2026.07.11
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-17/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 현재는 한국어 자산만 운영하도록 README와 본문 참조를 맞췄고, 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어판 `docs/parts/part-04/chapter-17/section-02.en.md`와 중국어판 `docs/parts/part-04/chapter-17/section-02.zh.md`를 열고, 같은 폴더에 공용 `-en.mmd` 기준 원본을 추가했다. 두 번역본 모두 군집 번호 해석 경계, 파라미터·표현 의존성, 정책 연결 위험, 기사/고객 사례, 작은 검토 메모 흐름을 한국어 원문 구조에 맞춰 반영했다.

### v2026.07.08
- 본문 반영: 사례 1 아래에 `cluster 2 즉시 정책화`와 `특징 요약·민감도·후속 지표 검토`를 갈라 보여 주는 Mermaid 도식을 추가하고 본문 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 군집 번호를 등급처럼 읽는 오해와 검토 경로를 같은 흐름도로 유지해야 한다.

### v2026.07.09
- 본문 반영: `연습 및 예제` 절로 재구성하고 cluster 번호 뒤집기 비교, Part 4 목표 회수 문단, `보인 구조 / 해석 경계 / 다음 질문` 공통 기록 표를 추가해 `번호 -> 의미` 오독을 끊는 연습 흐름을 넣었다.
- 번역 동기화 메모: 향후 번역본에서도 군집 번호 뒤집기 비교와 해석 경계 문단을 같은 위치에 유지해야 한다.

### v2026.07.10
- 본문 반영: `그렇다면 무엇으로 극복해야 하는가`, `군집 결과를 본 뒤 바로 무엇을 해야 하는가` 절을 추가해 군집 요약, 민감도 점검, 대표 사례 읽기, 라벨·성과 대조의 순서를 명시하고, 짧은 검토 메모 표와 보완 Mermaid 흐름도를 함께 넣었다.
- 추가 반영: `반지도학습과의 연결은 본편 범위 밖`이라는 표현을 현재 원칙에 맞게 걷어내고, 군집이 왜 라벨 가설의 보조 신호는 될 수 있지만 정답 라벨을 자동으로 대신하지는 못하는지까지는 현재 절 안에서 직접 회수했다.
- 번역 동기화 메모: 향후 번역본에서도 군집 해석의 브레이크뿐 아니라 후속 검토 절차와 짧은 메모 구조를 함께 유지해야 한다. 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
