# Section Release Note

- Section ID: `P4-4.2`
- Source File: `docs/parts/part-04/chapter-04/section-02.md`

### v2026.07.12

- 변경 이유: 검증/테스트 절 말미에 `이 절에서 기억할 관점`, `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`가 함께 남아 자기 확인 구조가 분산되어 있었다.
- 본문 반영: 말미 구조를 `체크리스트` 하나로 통합하고, 검증과 테스트의 역할 분리, 테스트 오염, 작은 데이터셋의 제약을 자기 점검 질문으로 다시 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본도 한국어 원문과 같은 단일 `Checklist` 구조로 맞추고, `Perspective`·`Short Check`·`When...` 계열 말미 블록을 정리했다. 중국어 간체 번역본의 개념사전 링크도 상대경로로 바로잡았다.
- 번역 반영 상태: 영어·중국어 간체 반영 완료
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.12`

### v2026.07.11

- 변경 이유: Part 4의 인라인 Mermaid 도식을 외부 `.mmd` 자산으로 관리해 차트 가이드라인의 Mermaid 자산 운영 방식과 맞출 필요가 있었다.
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-04/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 영문·중국어 간체 본문도 같은 자산 패턴으로 동기화했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams in this section.
- 번역 반영 상태: 영어·중국어 간체 반영 완료
- 관련 자산: `docs/assets/part-04/chapter-04/`
- 원문 기준 버전: `v2026.07.11`

- 변경 이유: 한국어 본문의 검증·테스트 역할 도식 세 개가 영문 라벨로 남아 있어 현재 원고 언어 흐름과 맞지 않았다.
- 본문 반영: 후보 비교 도식, 조기 테스트 오염 도식, 고객 이탈 분할 도식의 보이는 라벨을 한국어로 바꾸고 Section 버전을 갱신했다.
- 변경 이유: 아카이브해 둔 영어·중국어 간체 번역본을 활성 경로에 반영하고 현재 공개 내비게이션과 연결할 필요가 있었다.
- 본문 반영: `docs/parts/part-04/chapter-04/section-02.en.md`와 `docs/parts/part-04/chapter-04/section-02.zh.md`를 활성 경로에 복원하고 번역본 버전을 `v2026.07.11`로 맞췄다.
- 번역 동기화 메모: 영문·중국어 간체 번역본을 현재 공개 경로에 반영했고, 한국어 도식 라벨 한글화에 맞춰 원문 기준 버전도 함께 정렬했다.
- 번역 반영 상태: 영문·중국어 간체 번역본 반영 완료 (2026-07-11)
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.11`

### v2026.07.08

- 변경 이유: 검증과 테스트의 역할 구분은 설명되어 있었지만, 같은 고객 이탈 표가 `후보 선택`과 `마지막 확인` 단계에서 어떻게 다른 역할로 나뉘는지 사례 수준의 도식이 없어 구조를 한눈에 붙잡기 어려웠다.
- 본문 반영: 사례 및 예시 절에 `customer churn table -> train split / validation split / final candidate -> test split -> reported final result` 흐름의 Mermaid 도식을 추가해, 검증과 테스트가 같은 질문에 쓰이지 않는다는 점을 시각적으로 보강했다.
- 번역 동기화 메모: preserve the churn-data split diagram that separates model-choice use from final test use, especially the one-way flow into the test split at the end. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`
- 변경 이유: 테스트 점수를 너무 일찍 여는 사례 자체에서 `선택 과정 오염`과 `마지막 한 번 확인`의 대비를 바로 따라가게 할 필요가 있었다.
- 본문 반영: 사례 1 아래에 `early test peek -> repeated changes -> test contamination`와 `validation choice -> final test once`를 대비하는 Mermaid 도식을 추가했다.

### v2026.07.09

- 변경 이유: 문단 구조 검토 결과, `짧은 점검`이 사례와 연습보다 먼저 와서 검증·테스트 역할 구분을 실제 장면으로 붙잡기 전에 점검 문항부터 읽히는 순서를 정리할 필요가 있었다.
- 본문 반영: 스팸 분류 사례와 역할 구분 표를 `사례 및 예시` 절 아래로 모으고, `짧은 점검`은 `이 절에서 기억할 관점` 뒤로 옮겨 사례 이해와 연습이 먼저 이어지도록 문단 순서를 재배치했다. 같은 정리에서 상위 사례 헤더도 `사례로 보기` 대신 `사례 및 예시` 기준으로 맞췄다.
- 번역 동기화 메모: Part 4 영어·중국어 본문은 아직 없어 이번 구조 조정은 한국어 원고 기준으로만 반영했다. 향후 번역본 작성 시에도 사례와 연습을 점검보다 먼저 두는 순서를 유지한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`

### v2026.07.07

- 변경 이유: 검증/테스트 판단표 도입이 메타 진행 문구로 먼저 열리는 문제를 줄이기 위해 수정했다.
- 본문 반영: 검증 데이터와 테스트 데이터 구분을 후보 선택용 질문과 마지막 확인용 질문의 차이로 직접 설명하는 문장으로 교체했다.
- 번역 동기화 메모: 향후 번역본에서도 데이터 역할 구분을 안내형보다 설명형 문장으로 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.07`
