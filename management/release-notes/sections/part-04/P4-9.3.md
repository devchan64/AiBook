# Section Release Note

- Section ID: `P4-9.3`
- Source File: `docs/parts/part-04/chapter-09/section-03.md`

### v2026.07.12
- 본문 반영: 말미 구조를 `체크리스트` 하나로 통합하고, AIC/BIC, 탐색 기법, nested cross-validation, AutoML, tracking의 층위 차이를 자기 점검 질문으로 다시 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본도 말미의 기억/점검/적용 시점 블록을 제거하고 한국어 원문과 같은 `Checklist` 단일 구조로 정렬했다. 중국어 간체 번역본의 개념사전 링크도 상대 경로로 바로잡았고, 번역본 버전을 `v2026.07.12`로 맞췄다.

### v2026.07.11
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-09/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 영문·중국어 간체 본문도 같은 자산 패턴으로 동기화했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams in this section.
- 본문 반영: experiment tracking 사례 도식의 보이는 라벨을 한국어로 바꾸고 Section 버전을 갱신했다.

### v2026.07.07
- 본문 반영: AutoML이 자동화하는 묶음을 소개하는 문장을 직접 설명형으로 교체했다.
- 번역 동기화 메모: 향후 번역본에서도 AutoML 도입을 조언형 대신 설명형으로 유지해야 한다.

### v2026.07.08
- 본문 반영: 사례 1 아래에 `many runs -> score only`와 `tracking metadata -> explainable comparison`을 대비하는 Mermaid 도식을 추가하고 본문 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 experiment tracking을 재현성의 일부로 읽게 하는 같은 사례 도식을 유지해야 한다.

### v2026.07.10
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
