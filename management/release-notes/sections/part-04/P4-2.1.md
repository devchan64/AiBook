# Section Release Note

- Section ID: `P4-2.1`
- Source File: `docs/parts/part-04/chapter-02/section-01.md`

### v2026.07.12
- 본문 반영: 말미 구조를 `체크리스트` 하나로 통합하고, 라벨 판정, `X`/`y` 구분, 학습과 평가 분리, 예측과 최종 결정 구분을 자기 점검 질문으로 다시 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본도 한국어 원문과 같은 단일 `Checklist` 구조로 맞추고, `Perspective`·`Short Check`·`When...` 계열 말미 블록을 정리했다.

### v2026.07.11
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-02/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 영문·중국어 간체 본문도 같은 자산 패턴으로 동기화했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: English and Simplified Chinese translations now use the same external `.mmd` asset pattern for the corresponding Mermaid diagrams in this section.
- 본문 반영: `지도학습의 흐름` 도식과 사례 1 아래 Mermaid 도식의 보이는 텍스트를 한국어로 바꾸고 Section 버전을 갱신했다. 도식 구조와 설명 순서는 유지했다.

### v2026.07.07
- 본문 반영: `편이 좋습니다` 표현을 제거하고 지도학습 여부를 먼저 판정해야 한다는 직접 서술형으로 교체했다.
- 번역 동기화 메모: 향후 번역본에서도 판단 도입 문장을 설명형으로 유지해야 한다.

### v2026.07.08
- 본문 반영: 사례 1 아래에 `past inquiries -> labels -> X/y 설정 -> 지도학습 -> 새 문의 평가` 흐름의 Mermaid 도식을 추가하고 본문 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 라벨 존재와 입력/출력 구조 설정이 지도학습으로 이어지는 사례 흐름을 같은 도식으로 유지해야 한다.

### v2026.07.10
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
