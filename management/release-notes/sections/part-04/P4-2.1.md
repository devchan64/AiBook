# Section Release Note

- Section ID: `P4-2.1`
- Source File: `docs/parts/part-04/chapter-02/section-01.md`

### v2026.07.06

- 변경 이유: 라벨이 보인다고 곧바로 지도학습 문제가 성립하는 것으로 오해하지 않도록, 라벨 품질 점검 단계를 더 직접적으로 보강할 필요가 있었음.
- 본문 반영: `라벨이 있다`와 `바로 지도학습 문제로 쓸 수 있다`를 구분하는 비교 표를 추가했고, 라벨 일관성, 규칙 변경, 사람 판정 차이를 다시 확인하는 질문을 보강했다.
- 번역 동기화 메모: 비교 표와 점검 질문이 새로 들어가므로 번역 시 라벨 존재와 라벨 품질의 구분을 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-2

- 변경 이유: 지도학습 도입 절에서도 `처음 읽을 때는`, `초심자에게는` 같은 읽기 가이드를 줄이고, `X`/`y`와 라벨 품질 점검이 본문 논리로 직접 이어지게 정리.
- 본문 반영: 지도학습의 첫 정의, `X`와 `y` 표기 설명, 라벨 존재와 문제 성립의 구분 문장을 일반 서술로 다듬었다. 비교 표와 도식, 사례는 유지했다.
- 번역 동기화 메모: onboarding phrasing이 줄고 label-quality framing이 직접화되었으므로, 번역 시에도 neutral narration과 `label exists vs usable target` 구분을 유지해야 한다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-2`

### v2026.07.06-3

- 변경 이유: 데이터 분리 설명에 남아 있던 `입문 단계에서는` 문장을 더 직접적인 설명형으로 정리할 필요가 있었음.
- 본문 반영: 학습 데이터와 테스트 데이터 구분의 핵심을 일반 진술형으로 바꾸어, `본 것`과 `안 본 것`의 차이가 바로 읽히게 다듬었다.
- 번역 동기화 메모: keep the direct contrast between seen-vs-unseen data without learner-staging phrasing. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-3`

### v2026.07.06-4

- 변경 이유: 본문 수정 이력과 달리 Section 메타데이터가 `v2026.07.05`로 남아 있었고, 도입과 라벨 점검 문장에 `잡아 두면 됩니다`, `보는 습관이 중요합니다` 같은 안내형 표현이 남아 있었다.
- 본문 반영: Section `Version`을 `v2026.07.06`으로 갱신했고, 지도학습의 첫 정의, Part 3 연결, 라벨 존재와 문제 성립의 구분, 데이터 분리 도입 문장을 직접 설명형으로 조정했다. `X`/`y`, 라벨 품질 표, 사례 구조는 유지했다.
- 번역 동기화 메모: translation에서도 version sync를 반영하고, introductory framing과 label-quality checks를 guide-like phrasing보다 direct explanatory prose로 맞춘다. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`
