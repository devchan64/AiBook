# Section Release Note

- Section ID: `P4-13.1`
- Source File: `docs/parts/part-04/chapter-13/section-01.md`

### v2026.07.20
- 저작권 검토: Part 4 Module 4 순차 검토 범위에서 SVM 직관, margin, support vector, 오류 허용 설명과 margin 사례를 확인했다. 표·사례·Python 예제는 본문 학습 목적에 맞춘 자체 작성 자료로 판단했다.
- 출처 사용 점검: scikit-learn SVM 문서와 Cortes·Vapnik 논문은 SVM 개념과 역사 근거로만 사용했고, 원문 표·그림·코드·데이터셋을 재사용한 흔적은 발견하지 않았다.
- 관련 자산: `p4-13-1-mermaid-01-ko.mmd`부터 `p4-13-1-mermaid-06-ko.mmd`까지는 본문 사례 흐름을 압축한 자체 도식으로 확인했다.
- 본문 반영: 저작권 기준에서 수정이 필요한 문장이나 자산은 없어 본문과 `Version`은 변경하지 않았다. 원문 기준 버전은 `v2026.07.19`이다.
- 번역 동기화 메모: 번역본 영향 없음.

### v2026.07.19
- 근거 보강: Cortes와 Vapnik의 *Support-Vector Networks* 출처를 DOI 링크로 바꾸고 확인 날짜를 갱신했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 간체 번역본을 갱신할 때도 SVM 고전 논문 출처 링크를 같은 기준으로 맞춰야 한다.

### v2026.07.17
- 본문 반영: `현재 본편 범위 밖` 문장을 제거하고, 이번 절은 margin과 support vector로 `좋은 경계`를 먼저 붙잡고 kernel과 검증 기준은 뒤 절·뒤 장으로 넘기는 handoff 구조가 먼저 보이도록 범위 안내를 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 handoff 중심 범위 안내를 반영해야 한다.

### v2026.07.12
- 본문 반영: 해당 소제목을 제거하고, 예외 점 변화가 margin과 경계 품질 해석을 어떻게 흔드는지 현재 절의 설명 문장으로 바로 이어지게 정리했다. 같은 작업에서 Section 버전을 갱신했다.
- 번역 동기화 메모: 영어·중국어 번역본에서도 같은 위치의 메타 소제목을 제거하고 설명 문장으로 직접 잇도록 정리했다. 말미의 `Perspective/Short Check/When...` 계열 구조도 한국어 원문과 같은 `Checklist` 단일 구조로 맞췄고, 중국어 간체 번역본의 개념사전 링크도 상대 경로로 바로잡았다.
- 추가 반영: 말미의 `이 절에서 기억할 관점`과 `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`를 `체크리스트` 하나로 정리했다. 번역본 반영은 후속 확인이 필요하다.

### v2026.07.11
- 본문 반영: 이 Section의 Mermaid 도식을 `docs/assets/part-04/chapter-13/` 아래 외부 `.mmd` 자산으로 분리하고, 한국어 본문은 include 방식으로 참조하도록 정리했다. 이어서 영문 기준 `p4-13-1-mermaid-01-en.mmd`부터 `p4-13-1-mermaid-06-en.mmd`까지 추가하고, `docs/parts/part-04/chapter-13/section-01.en.md`와 `docs/parts/part-04/chapter-13/section-01.zh.md`를 열었다.
- 추가 반영: 영어·중국어 번역본에 SVM 후보 판단 표, `완벽 분리`가 깨지는 반복 실습, margin 근처 예외점 해석을 보강해 원문 후반의 연습·정리 흐름을 더 가깝게 맞췄다.
- 번역 동기화 메모: 영어·중국어 번역 경로를 열었고, 중국어판은 현재 Chapter 13에서 공용 `-en.mmd` 자산을 직접 참조한다. 이번 보강으로 두 번역본 모두 SVM의 후보 판단과 margin 변화 실습을 원문 흐름에 더 가깝게 따라가도록 정리했다.

### v2026.07.08
- 본문 반영: 사기 거래 탐지 사례 아래에 candidate boundary에서 small margin, large margin, review 증가, stable separation으로 이어지는 Mermaid 도식을 추가하고 Section 버전을 갱신했다.
- 번역 동기화 메모: 향후 번역본에서도 margin 차이에 따른 review와 안정성 분기 구조를 유지해야 한다.

### v2026.07.09
- 본문 반영: `완벽 분리` 예제 뒤에 경계 근처 예외 점을 더 움직이는 통제된 변화 실습과 `무엇이 유지되고 무엇이 달라지는가`, `이 연습이 Part 4 목표를 어떻게 회수하는가`, `보인 구조 / 해석 경계 / 다음 질문` 기록 표를 추가했다. 또한 `왜 margin을 따로 보아야 하는가` 도식을 `여러 경계 후보 -> 가장 가까운 점 비교 -> 작은 흔들림/review 증가 vs 더 안정적 해석` 흐름으로 다시 그려, margin을 별도 기준으로 읽는 이유를 더 직접적으로 드러냈다. 이어서 `support vector` 도식을 `멀리 있는 점 vs 가까운 점` 분기로 바꾸고, `soft margin`, `채용 서류 분류`, 절 말미 요약 흐름에도 Mermaid 도식을 보강해 섹션 전체의 시각 압축을 맞췄다.
- 본문 반영: 같은 날짜 후속 정리로 `학습 배경`의 과밀한 비교·기록 표를 뒤쪽 세부 학습 블록으로 옮기고, `왜 margin을 따로 보아야 하는가 -> 큰 margin은 왜 좋은가 -> support vector -> Python 실습` 순서가 앞에서 바로 이어지도록 문단을 재배치했다. 실습 뒤에는 `데이터가 완벽히 나뉘지 않으면 어떻게 되는가`, 모델 비교, 적용 판단, 사례, 학술적 배경과 역사를 차례로 다시 배치해 핵심 직관과 반복 실습이 먼저 읽히도록 조정했다. 또한 역사 절에는 학술적 배경이 본론을 대체하지 않고 학습 보조 맥락으로만 쓰인다는 기준을 명시했다.
- 번역 동기화 메모: 향후 번역본에서도 SVM 입문 실습을 `예외 점 변화 -> margin 흔들림 -> 경계 품질 해석` 순서로 유지해야 한다.

### v2026.07.10
- 본문 반영: `사례로 다시 읽기` 보조 소제목을 일반 도입 문단으로 정리해, 같은 블록 안에서는 `사례 1`, `사례 2` 번호 체계만 남도록 구조를 단순화했다.
- 추가 반영: 문단 재배치 리포트 기준으로 `이 절의 범위` 도입을 압축해 `좋은 경계란 무엇인가`와 margin·support vector·soft margin 직관을 먼저 보이게 정리했다. 생략 항목과 회수 위치 설명은 유지하되, 범위 통제 문단은 뒤에서 한 번에 닫히도록 배치를 조정했다.
- 번역 동기화 메모: 향후 번역본에서도 SVM 사례 블록은 별도 메타 소제목보다 실제 사례 번호 체계를 우선 유지해야 한다.
