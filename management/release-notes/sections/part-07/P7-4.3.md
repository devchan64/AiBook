# Section Release Note

- Section ID: `P7-4.3`
- Source File: `docs/parts/part-07/chapter-04/section-03.md`

### v2026.07.19
- 변경 이유: 원고 가이드의 메타 진행 표지 축소 기준에 맞춰 연습 도입부 제목을 점검했다.
- 본문 반영: `이 절의 범위`, `이 절의 목표` 표지를 `표현 정규화가 바꾸는 출력`, `연습 뒤 남겨야 할 판단`으로 바꿔 현재 Section의 중심 질문과 기록 기준이 바로 보이도록 정리했다.
- 추가 반영: `새 이론을 늘리는 자리가 아니라` 식의 방어형 도입 문장을 줄이고, 정규화 전후 실행이 운영 표현 처리 판단을 어떻게 바꾸는지 바로 보이게 했다.
- 번역 동기화 메모: 향후 번역본도 generic scope/goal headings보다 section-specific practice judgment headings를 우선한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 원문 기준 버전: `v2026.07.19`

### v2026.07.18
- 추가 반영: 말미 체크리스트를 `비교 조건`, `coverage`, `오류 표현`, `남은 위험`, `다음 반복` 축의 확인 표로 압축해 정규화 전후 비교 기록이 더 일관되게 남도록 정리했다.
- 구조 정리: 동작 단위 센서 표현 선택 확장 연습을 `P7-4.4 같은 평균, 다른 패턴 비교 연습`으로 분리하고, 이 절은 텍스트 표현 정규화 전후 비교라는 중심 질문에 집중하도록 정리했다.
- 추가 반영: 본문의 두 Mermaid 도식을 `docs/assets/part-07/chapter-04/*.mmd` 자산으로 분리하고, Chapter 4 자산 폴더 README에 표현 정규화 도식 운영 범위를 기록했다.
- 추가 반영: 정규화 후 정확도 상승을 곧바로 `사전만 늘리면 된다`로 읽지 않도록, 오답 유발 표현·low-coverage 정답·잔여 OOV를 먼저 보게 하는 사례 문단과 Mermaid 흐름도를 보강했다.
- 추가 반영: 범위 문단의 진행형 메타 표현을 줄이고, 표현 하나의 차이가 coverage와 예측 해석을 어떻게 바꾸는지 현재 절 질문이 먼저 보이게 다듬었다.
- 추가 반영: `직접 더 바꿔 볼 것` 표지를 `직접 바꿔 보며 확인할 것`으로 통일하고, 문의 데이터 파일과 자체 예시 사용 여부를 `출처와 참고 자료`에 보강했다.
- 추가 반영: 도입 문장의 `연습 절` 표기를 다듬어, 표현 정규화 전후를 실행해 보는 현재 절의 역할이 더 직접 보이게 정리했다.
- 추가 반영: 범위와 입력 파일 설명에서 다른 절 이름을 직접 호출하는 표현을 더 줄이고, 정규화 규칙 하나가 coverage와 예측 해석을 어떻게 바꾸는지가 현재 절 안에서 먼저 읽히게 정리했다.
- 추가 반영: 도입에서 `P7-4.1`, `P7-4.2`를 계단형으로 되짚는 문장을 줄이고, 정규화 규칙이 예측·coverage·회고 우선순위를 함께 바꾸는 현재 절 질문이 앞에 오도록 다듬었다.
- 추가 반영: 입력 파일 안내의 CSV 자산 경로를 링크로 노출해, 정규화 전후 비교 전에 실습 데이터 파일을 바로 열어볼 수 있게 했다.
- 신규 작성: P7-4 장에 `표현 정규화 연습` 절을 추가해, 설명 절과 별도로 `정규화 전후 비교 실습`을 수행할 수 있게 구성했다.
- 신규 반영: `캔슬 -> 취소`, `스케줄 -> 일정`, `하자 -> 불량` 같은 표현 치환을 중심으로 coverage, OOV, 예측 변화, 우선순위 기록을 함께 읽는 Python 예제를 추가했다.
- 신규 반영: `이 절에서 직접 할 일`, `관찰 포인트`, `기록 템플릿`, `직접 더 바꿔 볼 것`을 별도 소절로 두어 Part 7의 실습 중심 구조를 강화했다.
- 번역 동기화 메모: Future translations should preserve the before/after normalization comparison, especially the distinction between expressions that directly cause wrong routing and expressions that only lower coverage.
