# Section Release Note

- Section ID: `P7-2.3`
- Source File: `docs/parts/part-07/chapter-02/section-03.md`

### v2026.07.18
- 추가 반영: 도입 문장의 `연습 절` 표기를 다듬어, 여러 비교 실험을 한 화면에서 읽어 실패 종류를 가르는 현재 절의 역할이 더 직접 보이게 정리했다.
- 추가 반영: 입력 파일 설명에서 `앞 절과 같은 학습 데이터` 같은 순차 안내를 줄이고, 같은 실험판 안에서 무엇이 전처리 문제로 풀리고 무엇이 경계 사례로 남는가가 먼저 보이게 정리했다.
- 추가 반영: 도입과 범위 안내에서 `P7-2.1`, `P7-2.2`를 순차적으로 되짚는 문장을 줄이고, 여러 비교 실험을 같은 평가 셋에서 읽는 현재 절의 질문이 더 앞에 오도록 다듬었다.
- 신규 작성: P7-2 장에 `비교 실험 연습` 절을 추가해, baseline과 여러 1-NN 변형을 같은 평가 셋에서 나란히 읽는 실습을 별도 절로 분리했다.
- 신규 반영: `p7-2-stress-test.csv`를 새로 추가해, 전처리로 해결되는 실패와 경계 데이터 부족으로 남는 실패를 함께 해석할 수 있는 스트레스 평가 사례를 보강했다.
- 신규 반영: Python 예제는 설정별 정확도뿐 아니라 샘플별 실패 진단을 `전처리로 해결됨`, `경계 사례 추가 또는 특징 보강 필요`, `설정에 따라 갈리는 경계 사례`로 나눠 출력하도록 구성했다.
- 번역 동기화 메모: Future translations should preserve the distinction between preprocessing-fixable failures and failures that remain because the boundary data or features are still insufficient.
