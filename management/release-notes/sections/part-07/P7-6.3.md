# Section Release Note

- Section ID: `P7-6.3`
- Source File: `docs/parts/part-07/chapter-06/section-03.md`

### v2026.07.18
- 추가 반영: 본문의 두 Mermaid 도식을 `docs/assets/part-07/chapter-06/*.mmd` 자산으로 분리하고, Chapter 6 자산 폴더 README에 권한 판정 도식 운영 범위를 기록했다.
- 추가 반영: `캐시 삭제 실행` 같은 겉보기의 가벼운 변경을 자동 실행으로 오해하지 않도록, 행동 종류·영향 범위·승인 필요성을 먼저 보게 하는 사례 문단과 Mermaid 흐름도를 보강했다.
- 추가 반영: 범위 문단의 진행형 메타 표현을 줄이고, 자동 진행과 즉시 보류를 가르는 실행 경계가 먼저 보이도록 현재 절 질문을 더 직접 정리했다.
- 추가 반영: `직접 더 바꿔 볼 것` 표지를 `직접 바꿔 보며 확인할 것`으로 통일하고, 승인 정책 연습 파일과 자체 예시 사용 여부를 `출처와 참고 자료`에 보강했다.
- 추가 반영: 도입 문장의 `연습 절` 표기를 다듬어, 승인 정책과 보류 상태를 단계별 판단 기록으로 다시 나누는 현재 절의 역할을 더 직접 드러냈다.
- 추가 반영: 입력 파일과 Python 예제 설명에서 `P7-6.2의 기록을 유지한 채` 같은 순차 안내를 줄이고, 기존 실행 기록과 새 승인 정책 판단이 같은 운영 시나리오 안에서 함께 읽히도록 정리했다.
- 추가 반영: 도입과 범위 안내에서 `P7-6.1`, `P7-6.2`를 순차적으로 되짚는 표현을 줄이고, 승인 정책을 단계별 판단 기록으로 읽는 현재 절의 질문이 앞에 오도록 다듬었다.
- 신규 작성: P7-6 장에 `승인 정책 연습` 절을 추가해, 실행 루프와 권한 기록 뒤에서 자동 실행 가능 단계와 보류 단계를 직접 다시 분류할 수 있게 구성했다.
- 신규 반영: 기존 권한 기록 CSV와 함께 `p7-6-approval-cases.csv`를 새로 추가해, 조회 단계, 기록 단계, 프로덕션 변경 단계를 같은 운영 시나리오 안에서 비교하도록 만들었다.
- 신규 반영: Python 예제는 단계별 정책 판정, 즉시 보류 이유, 더 안전한 다음 행동을 함께 출력해 `보류`를 운영 상태로 읽게 구성했다.
- 번역 동기화 메모: Future translations should preserve the distinction between executable actions and currently permitted actions, especially the comparison across different scopes of change.
