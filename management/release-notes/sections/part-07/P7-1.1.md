# Section Release Note

- Section ID: `P7-1.1`
- Source File: `docs/parts/part-07/chapter-01/section-01.md`

### v2026.07.20
- 저작권 검토: Part 7 Chapter 1 모듈 순차 검토 범위에서 프로젝트 질문 정의, 입력 단위 확인, CSV 발췌, Python 예제 출력, Mermaid 흐름도, 체크리스트를 확인했다. 사례 데이터와 도식은 Part 7 실습을 위해 구성한 자체 예시로 판단했다.
- 출처 사용 점검: Python `csv`, `statistics` 모듈 설명은 Python Software Foundation 공식 문서를 참고 자료로 명시했고, 외부 문장을 직접 인용하지 않았다. 외부 도표·표·데이터 재사용은 확인하지 않았다.
- 관련 자산: `docs/assets/part-07/chapter-01/p7-1-traffic-log.csv`, `docs/assets/part-07/chapter-01/p7-1-1-case-reading-flow-ko.mmd`, `docs/assets/part-07/chapter-01/p7-1-1-project-start-flow-ko.mmd`
- 본문 반영: 제목 아래 메타데이터를 저장소 공통 형식인 평문 `Section ID`, `Version` 두 줄로 바로잡고, 본문 `Version`을 `v2026.07.20`으로 갱신했다.
- 번역 동기화 메모: 대응 번역본이 생기면 메타데이터 형식과 한국어 Mermaid 자산의 언어별 파생본 생성 여부를 함께 확인해야 한다.

### v2026.07.19
- 변경 이유: Part 7 연습 Section의 학습밀도 보강 이력을 원고 가이드 기준에 맞춰 압축 정리했다.
- 본문 반영: 범위·목표·연습 표지와 본문 안의 절 진행 지시 표현을 내용형 기록 기준으로 바꾸고, 실행 기록·결과 해석·프로젝트 기록 예시가 설명과 해설 안에서 읽히도록 정리했다.
- 번역 동기화 메모: 향후 번역본도 진행 안내 제목보다 Section별 질문, 판단 기준, 실행 결과 해석, 프로젝트 기록 예시가 먼저 보이게 맞춘다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 원문 기준 버전: `v2026.07.19`

### v2026.07.07
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: `metric`, `recent_5_avg`, `baseline_20_avg`, `delta`, `interpretation` 표 헤더를 `지표`, `최근 5개 평균`, `기준선 20개 평균`, `차이`, `해석`으로 바꾸었다. / Part 초반부의 기준 용어 흐름을 맞추기 위해 `baseline 문서`, `질문과 baseline` 같은 표현을 `기준점(baseline)` 기준으로 정리했다. / P7-1.1의 연결 표를 `공통 기록 구조` 중심으로 다시 쓰고, 설명 문장에서는 `해석`을 먼저 두어 회고 구조의 한국어 흐름이 먼저 읽히게 정리했다.
- 번역 동기화 메모: 향후 번역본도 Part 초반 회고 구조 설명에서 한국어 구조명을 먼저 두고 영어 병기는 보조로 유지해야 한다.

### v2026.07.12
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 분산된 회상·적용 시점 표지를 하나의 `체크리스트` 아래로 통합했다. / 질문 중심 출발, 요약값의 기준점 역할, 다음 절 연결, 실제 적용 질문을 `체크리스트` 하나로 재구성했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: `이 절의 범위`에서 통계 검정, 시계열 예측, 대시보드, 대규모 파이프라인 같은 생략 목록을 걷어내고, 이번 절이 `질문 -> 표 확인 -> 요약 -> 관찰 기록`으로 프로젝트 문서를 시작하는 기준을 닫는 자리라는 점이 먼저 보이도록 다시 정리했다. / 회고 handoff는 `P7-1.2`, 후속 프로젝트 공통 기록 구조 handoff는 Part 7 뒤 장들로 이어지게 분명히 했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-1.1 closes the project-start record structure first and hands retrospective writing to P7-1.2.

### v2026.07.18
- 변경 이유: 기존 P7-1.1 예제가 7일치 총합 CSV를 읽는 수준에 머물러, 사용자가 요구한 `실제로 동작시켜 보며 AI 동작을 이해하는 실습` 기준에서는 너무 장난감처럼 읽혔다.
- 본문 반영: 말미 체크리스트의 반복 문항을 `질문`, `입력 단위`, `비교 단위`, `기준점`, `다음 질문` 다섯 칸으로 압축해, 실행 뒤 확인해야 할 학습 포인트가 더 직접 보이게 정리했다. / 본문의 두 Mermaid 도식을 `docs/assets/part-07/chapter-01/*.mmd` 자산으로 분리하고, 자산 폴더 README에 현재 한국어 운영 범위와 향후 번역 동기화 기준을 기록했다. / 전체 전환율 하락을 곧바로 `서비스 전체 문제`로 읽지 않도록, 입력 단위 확인과 채널별 비교를 먼저 거치게 하는 사례 문단과 Mermaid 흐름도를 보강했다. 등으로 압축 정리했다.
- 번역 동기화 메모: Future translations should preserve the stronger P7-1.1 example where the unit-of-analysis shift from daily totals to channel-day rows changes the project conclusion.
