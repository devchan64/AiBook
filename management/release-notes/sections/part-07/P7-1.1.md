# Section Release Note

- Section ID: `P7-1.1`
- Source File: `docs/parts/part-07/chapter-01/section-01.md`

### v2026.07.07
- 본문 반영: `metric`, `recent_5_avg`, `baseline_20_avg`, `delta`, `interpretation` 표 헤더를 `지표`, `최근 5개 평균`, `기준선 20개 평균`, `차이`, `해석`으로 바꾸었다.
- 본문 반영: Part 초반부의 기준 용어 흐름을 맞추기 위해 `baseline 문서`, `질문과 baseline` 같은 표현을 `기준점(baseline)` 기준으로 정리했다.
- 번역 동기화 메모: 향후 번역본도 공개 독자용 표 헤더를 유지하고 내부 스키마식 칼럼명은 피해야 한다.

### v2026.07.07
- 본문 반영: P7-1.1의 연결 표를 `공통 기록 구조` 중심으로 다시 쓰고, 설명 문장에서는 `해석`을 먼저 두어 회고 구조의 한국어 흐름이 먼저 읽히게 정리했다.
- 번역 동기화 메모: 향후 번역본도 Part 초반 회고 구조 설명에서 한국어 구조명을 먼저 두고 영어 병기는 보조로 유지해야 한다.

### v2026.07.12
- 추가 반영: 분산된 회상·적용 시점 표지를 하나의 `체크리스트` 아래로 통합했다.
- 본문 반영: 질문 중심 출발, 요약값의 기준점 역할, 다음 절 연결, 실제 적용 질문을 `체크리스트` 하나로 재구성했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 통계 검정, 시계열 예측, 대시보드, 대규모 파이프라인 같은 생략 목록을 걷어내고, 이번 절이 `질문 -> 표 확인 -> 요약 -> 관찰 기록`으로 프로젝트 문서를 시작하는 기준을 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 회고 handoff는 `P7-1.2`, 후속 프로젝트 공통 기록 구조 handoff는 Part 7 뒤 장들로 이어지게 분명히 했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-1.1 closes the project-start record structure first and hands retrospective writing to P7-1.2.

### v2026.07.18
- 추가 반영: 다음 절 연결 문장에서 예전 제목 `결과와 회고`를 현재 제목 `baseline과 첫 비교`로 바꿔, Part 7 초반 교차 참조가 실제 목차와 같은 이름을 가리키도록 정리했다.
- 추가 반영: Section 제목을 `데이터 분석 미니 프로젝트 목표`에서 `프로젝트 질문과 입력 정의`로 바꾸고, 도입 문장도 질문과 입력 단위가 먼저 보이도록 정리했다.
- 추가 반영: Python 예제에서 `프로젝트_메모` 딕셔너리를 제거하고, 코드는 `전체 요약`, `전환율 차이`, `채널별 비교` 같은 계산 결과만 출력하도록 정리했다.
- 본문 반영: 해석과 다음 질문은 코드 안 하드코딩 메모가 아니라, 바로 아래 결과 해석 표와 문단에서 사람이 다시 정리하는 구조로 바꿨다.
- 추가 반영: 입력 파일 안내의 CSV 자산 경로를 상대 링크로 바꿔, 본문에서 실습 파일을 바로 열어볼 수 있게 했다.
- 변경 이유: 기존 P7-1.1 예제가 7일치 총합 CSV를 읽는 수준에 머물러, 사용자가 요구한 `실제로 동작시켜 보며 AI 동작을 이해하는 실습` 기준에서는 너무 장난감처럼 읽혔다.
- 본문 반영: 예제 데이터를 `하루 전체 합계`가 아니라 `채널-일자(channel-day)` 운영 로그 발췌로 바꾸고, 한 행이 무엇을 뜻하는지와 샘플 단위가 왜 중요한지를 더 직접적으로 설명했다.
- 본문 반영: Python 예제도 `평균과 최대값 요약` 중심에서 `기준선 7일 vs 최근 7일`, `전체 합계 vs 채널별 비교`, `전환율 변화 vs 오류율 변화`를 함께 읽는 비교 구조로 다시 썼다.
- 추가 반영: 결과 해석 표를 새로 넣어 전체 합계만 보면 흐릿한 하락처럼 보이지만 ads 채널만 크게 무너진다는 점이 눈에 보이도록 보강했다.
- 추가 반영: 실습용 운영 로그를 `docs/assets/part-07/chapter-01/p7-1-traffic-log.csv`로 분리하고, Python 코드는 인라인 CSV 문자열 대신 실제 파일을 읽는 형태로 바꿨다.
- 추가 반영: `입력 파일` 소절을 따로 세워 CSV 경로, 한 행의 의미, 핵심 열을 먼저 읽고 그다음 Python 예제로 넘어가도록 구조를 분리했다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 비교 구간과 ads 오류 값을 바꿨을 때 프로젝트 메모 해석이 어떻게 달라지는지 직접 실험하도록 보강했다.
- 추가 반영: `직접 바꿔 보며 확인할 것`과 결과 해석 문장에서도 `프로젝트 메모` 표현을 걷어내고, 본문 해석과 다음 질문이 코드 출력 위에 얹히는 구조로 다시 정리했다.
- 번역 동기화 메모: Future translations should preserve the stronger P7-1.1 example where the unit-of-analysis shift from daily totals to channel-day rows changes the project conclusion.
