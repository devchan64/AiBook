# Section Release Note

- Section ID: `P7-2.1`
- Source File: `docs/parts/part-07/chapter-02/section-01.md`

### v2026.07.22
- 변경 이유: Part 7 예제·연습 코드 순차 개선 작업에서 P7-2.1의 실행 출력과 스케일 실험 연결을 바로잡았다.
- 본문 반영: 실습 CSV 링크에 `csv-preview` 표시를 붙이고, Python 예제에 `feature_columns`와 `feature_scales` 조작 변수를 추가해 사용 시간 축을 바꿔 보는 실험 위치를 명확히 했다. 현재 CSV 기준 `평가-02`의 최근접 학습 샘플을 실제 출력인 `학습-02`로 정정했으며, 직접 바꿔 볼 실험 문장도 `feature_scales` 기준으로 맞췄다.
- 번역 동기화 메모: 향후 번역본도 `feature_scales` 조작 변수와 `평가-02 -> 학습-02` 출력 예시를 기준으로 맞춰야 한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 관련 자산: `docs/assets/part-07/chapter-02/p7-2-churn-dataset.csv`
- 원문 기준 버전: `v2026.07.22`

### v2026.07.20
- Python 예제 주석 보강: 구독 이탈 데이터에서 다수 라벨 baseline과 1-NN 예측 오류를 비교하는 목적을 코드 블록 첫 줄 주석으로 명시했다.
- 저작권 검토: Part 7 Chapter 2 모듈 순차 검토 범위에서 비교표, 오류 사례, NumPy 기반 1-NN 예제, synthetic 이탈 위험 데이터, Mermaid 흐름도, 체크리스트를 확인했다. 데이터와 도식은 Part 7 실습을 위해 구성한 자체 예시로 판단했다.
- 출처 사용 점검: NumPy 공식 문서는 참고 자료로 명시했고, 외부 문장을 직접 인용하지 않았다. 외부 도표·표·데이터 재사용은 확인하지 않았다.
- 관련 자산: `docs/assets/part-07/chapter-02/p7-2-churn-dataset.csv`, `docs/assets/part-07/chapter-02/p7-2-1-project-compare-flow-ko.mmd`, `docs/assets/part-07/chapter-02/p7-2-1-baseline-review-flow-ko.mmd`
- 본문 반영: 제목 아래 메타데이터를 저장소 공통 형식인 평문 `Section ID`, `Version` 두 줄로 바로잡고, 본문 `Version`을 `v2026.07.20`으로 갱신했다.
- 번역 동기화 메모: 대응 번역본이 생기면 메타데이터 형식과 한국어 Mermaid 자산의 언어별 파생본 생성 여부를 함께 확인해야 한다.

### v2026.07.19
- 변경 이유: Part 7 연습 Section의 학습밀도 보강 이력을 원고 가이드 기준에 맞춰 압축 정리했다.
- 본문 반영: 범위·목표·연습 표지와 본문 안의 절 진행 지시 표현을 내용형 기록 기준으로 바꾸고, 실행 기록·결과 해석·프로젝트 기록 예시가 설명과 해설 안에서 읽히도록 정리했다.
- 번역 동기화 메모: 향후 번역본도 진행 안내 제목보다 Section별 질문, 판단 기준, 실행 결과 해석, 프로젝트 기록 예시가 먼저 보이게 맞춘다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 원문 기준 버전: `v2026.07.19`

### v2026.07.07
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 도입 문장의 첫 핵심 비교 표현을 `기준점(baseline)과 모델`로 맞추어 같은 문장 안의 용어 흐름을 정리했다. / 비교 요약과 샘플별 출력의 공개 키를 `평가 샘플`, `기준점 예측`, `가장 가까운 학습 샘플` 같은 설명형 표현으로 바꾸어 문서용 기록 예시로 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-2.1 코드 예제의 공개 출력 키를 독자용 설명형 레이블로 유지해야 한다.

### v2026.07.12
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 분산된 회상·적용 시점 표지를 하나의 `체크리스트` 아래로 통합했다. / baseline, train/test 분리, 점수 해석, 샘플별 예측 비교 항목을 `체크리스트` 하나로 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: `이 절의 범위`에서 scikit-learn API, 교차검증, 자동 탐색, 대규모 성능 비교 같은 생략 목록을 걷어내고, 이번 절이 `문제 -> 데이터 분리 -> baseline -> 모델 -> 비교`로 분류 프로젝트 문서를 시작하는 기준을 닫는 자리라는 점이 먼저 보이도록 다시 정리했다. / 개선 비교 handoff는 `P7-2.2`, 후속 프로젝트 공통 비교 기록 handoff는 Part 7 뒤 장들로 이어지게 분명히 했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-2.1 closes the classification-project start structure first and hands improvement writing to P7-2.2.

### v2026.07.18
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 말미 체크리스트를 `문제`, `분리`, `baseline`, `샘플별 비교`, `실패 목록` 다섯 칸으로 압축해, 모델 실행 뒤 같은 기준으로 남겨야 할 기록 단위가 더 직접 보이도록 정리했다. / 본문의 두 Mermaid 도식을 `docs/assets/part-07/chapter-02/*.mmd` 자산으로 분리하고, 자산 폴더 README에 현재 한국어 운영 범위와 비교 흐름 동기화 기준을 기록했다. / `baseline 0.500 vs 1-NN 0.667`를 점수 한 줄로 닫지 않도록, 회수 사례·새 오답·최근접 샘플 확인을 먼저 거치게 하는 사례 문단과 Mermaid 흐름도를 보강했다. 등으로 압축 정리했다.
- 번역 동기화 메모: Future translations should preserve the churn-risk scenario and the explicit handoff from raw distance errors to preprocessing comparison.
