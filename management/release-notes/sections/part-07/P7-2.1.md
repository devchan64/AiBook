# Section Release Note

- Section ID: `P7-2.1`
- Source File: `docs/parts/part-07/chapter-02/section-01.md`

### v2026.07.07
- 본문 반영: 도입 문장의 첫 핵심 비교 표현을 `기준점(baseline)과 모델`로 맞추어 같은 문장 안의 용어 흐름을 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-2.1 첫 도입 문장의 baseline 비교 표현을 한국어 기준 용어와 함께 유지해야 한다.

### v2026.07.07
- 본문 반영: 비교 요약과 샘플별 출력의 공개 키를 `평가 샘플`, `기준점 예측`, `가장 가까운 학습 샘플` 같은 설명형 표현으로 바꾸어 문서용 기록 예시로 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-2.1 코드 예제의 공개 출력 키를 독자용 설명형 레이블로 유지해야 한다.

### v2026.07.12
- 추가 반영: 분산된 회상·적용 시점 표지를 하나의 `체크리스트` 아래로 통합했다.
- 본문 반영: baseline, train/test 분리, 점수 해석, 샘플별 예측 비교 항목을 `체크리스트` 하나로 정리했다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 scikit-learn API, 교차검증, 자동 탐색, 대규모 성능 비교 같은 생략 목록을 걷어내고, 이번 절이 `문제 -> 데이터 분리 -> baseline -> 모델 -> 비교`로 분류 프로젝트 문서를 시작하는 기준을 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 개선 비교 handoff는 `P7-2.2`, 후속 프로젝트 공통 비교 기록 handoff는 Part 7 뒤 장들로 이어지게 분명히 했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-2.1 closes the classification-project start structure first and hands improvement writing to P7-2.2.

### v2026.07.18
- 추가 반영: 말미 정리 문장의 `읽는 편이 더 정확합니다` 같은 독법 안내를 줄이고, baseline 문서화와 다음 비교 질문이 이 절의 핵심 역할이라는 설명으로 더 직접 정리했다.
- 추가 반영: 체크리스트 문장에서 `다음 전처리 검토 질문으로 넘긴다`는 표현을 걷어내고, 샘플별 실패 목록이 현재 절 안에서 바로 전처리 검토 기준으로 남도록 정리했다.
- 추가 반영: 다음 절 연결 문장에서 예전 제목 `기준 모델과 개선`을 현재 제목 `회고와 다음 질문 만들기`로 바꿔, Chapter 2 안의 비교 흐름이 실제 목차 이름과 같은 형태로 이어지게 정리했다.
- 추가 반영: Section 제목을 `전통 머신러닝 예측 모델 목표`에서 `비교표와 오류 사례 읽기`로 바꾸고, 예측 모델을 붙인 뒤 무엇을 읽어야 하는지가 제목에서 바로 드러나게 맞췄다.
- 추가 반영: 입력 파일 안내의 CSV 자산 경로를 상대 링크로 바꿔, 데이터 파일과 본문 설명 사이를 바로 오갈 수 있게 했다.
- 본문 반영: `공부 시간과 출석률` 장난감 분류 예제를 `구독 고객 이탈 위험` 예측 사례로 교체하고, 문의 수·미접속 일수·사용 시간처럼 실제 운영형 특징을 함께 읽게 다시 구성했다.
- 추가 반영: baseline 0.500과 raw 1-NN 0.667 비교, 그리고 `평가-02`, `평가-04` 실패 사례를 통해 다음 절의 전처리 비교 필요성이 바로 보이도록 샘플별 기록을 강화했다.
- 추가 반영: 실습용 분류 데이터를 `docs/assets/part-07/chapter-02/p7-2-churn-dataset.csv`로 분리하고, Python 코드를 인라인 배열 대신 `split` 열 기반 파일 읽기 구조로 바꿨다.
- 추가 반영: `입력 파일` 소절을 따로 세워 CSV 경로, 한 행의 의미, 핵심 열을 먼저 읽고 그다음 Python 예제로 넘어가도록 구조를 분리했다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 사용 시간 스케일과 평가 샘플 구성을 직접 바꿔 보며 raw 거리 기반 오류가 어떻게 달라지는지 실험하도록 보강했다.
- 추가 반영: 도입과 범위 문장에서 `P7-1 다음 프로젝트` 같은 계단형 안내를 줄이고, 현재 절이 분류 프로젝트의 비교표와 오류 사례 읽기 자체로 서게 정리했다.
- 추가 반영: 본문 표와 해설에서도 `뒤 절`, `다음 절` 중심 표현을 줄이고, 현재 비교표와 오류 사례가 바로 재실험 판단 기준이 되도록 문장을 다듬었다.
- 번역 동기화 메모: Future translations should preserve the churn-risk scenario and the explicit handoff from raw distance errors to preprocessing comparison.
