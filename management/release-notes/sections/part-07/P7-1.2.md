# Section Release Note

- Section ID: `P7-1.2`
- Source File: `docs/parts/part-07/chapter-01/section-02.md`

### v2026.07.07
- 본문 반영: 다음 프로젝트 연결 문장과 점검 문장에 `기준점(baseline)` 병기를 보강해 회고 구조와 후속 비교 프로젝트의 연결을 더 분명히 했다.
- 번역 동기화 메모: 향후 번역본도 Part 초반부에서 baseline first mention을 한국어 기준 용어와 함께 유지해야 한다.

### v2026.07.07
- 본문 반영: 회고 구조 설명을 `사실 -> 해석 -> 다음 질문` 중심으로 다시 세우고, `추가 검토` 같은 한국어 설명을 먼저 둔 뒤 영어 병기는 보조로 남기도록 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-1.2 공통 회고 구조 설명에서 한국어 구조명을 먼저 두고 영어 병기는 보조 표기로 유지해야 한다.

### v2026.07.07
- 본문 반영: 회고 예시의 공개 키와 출력 제목을 `사실`, `해석`, `다음 질문`, `회고 메모`로 바꾸고, 바로 쓰는 템플릿 헤더도 같은 한국어 구조명으로 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-1.2 회고 예시와 템플릿의 공개 헤더는 한국어 구조명을 먼저 두고 영어 키는 노출하지 않는 편이 좋다.

### v2026.07.12
- 추가 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다.
- 본문 반영: 회고 문서의 핵심 원칙, 기준점 비교 연결, 실제 적용 확인 항목을 `체크리스트` 하나로 통합했다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 보고서 자동화, 인과 추론, 실험 설계 비교 같은 생략 목록을 걷어내고, 이번 절이 `계산 기록을 다음 반복의 의사결정 기록으로 바꾸는 최소 회고 구조`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 회고 습관의 후속 handoff를 `P7-5.2`, `P7-7.2`로 직접 연결해 Part 7 전체의 공통 기록 구조가 다시 보이게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-1.2 closes the retrospective record structure first and hands reuse of that structure to later project sections.

### v2026.07.18
- 추가 반영: 실습 입력으로 쓰는 CSV 자산 경로를 본문 링크로 바꿔, 검토 기준 실험 전에 파일 자체를 바로 열어볼 수 있게 했다.
- 추가 반영: `작은 프로젝트` 중심 표현을 줄이고, 회고 절이 프로젝트 규모와 무관하게 다음 반복을 여는 공통 기록 구조로 읽히도록 문장 톤을 정리했다.
- 추가 반영: 회고 예제를 단순 문장 정리 구조에서 `검토 기준을 바꾸면 어떤 날짜와 채널이 회고 후보로 올라오는가`를 직접 비교하는 실험형 실습으로 강화했다.
- 추가 반영: `엄격 모드`와 `탐색 모드`를 비교해 공통 검토 후보를 추리는 구조를 넣어, 회고가 감상문이 아니라 우선순위 문서가 되는 장면을 더 직접 보이게 했다.
- 추가 반영: P7-1.1과 같은 실습용 CSV 파일을 읽어 기준선과 최근 구간을 다시 계산하도록 바꿔, 인라인 데이터 배열 없이 `데이터 파일 -> 코드 -> 회고` 순서가 보이게 정리했다.
- 추가 반영: 다른 Part 7 절과 같은 읽기 리듬이 보이도록 `## Python 예제`, `## 결과를 어떻게 읽는가` 표지를 명시해 회고 실습 구조를 더 또렷하게 정리했다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 검토 기준과 채널 이상 행을 직접 바꾸며 회고 우선순위가 어떻게 달라지는지 실험하도록 보강했다.
- 번역 동기화 메모: Future translations should avoid shrinking P7-1.2 into only a small-project retrospective and should preserve the stronger experiment where review criteria change the candidate rows and the final retrospective priority.
