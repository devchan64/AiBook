# Section Release Note

- Section ID: `P7-5.3`
- Source File: `docs/parts/part-07/chapter-05/section-03.md`

### v2026.09.08

- 원고·코드·유지되는 결과 JSON의 참조 관계를 확인해 5.3 미사용 자산 41개를 삭제했다. 교체된 회전·앨리웁과 미채택 결과 40개(PNG·JSON), 미사용 OpenPose 추출 스크립트 1개가 대상이다. 다른 절의 입력으로 사용하는 이전 착장·회전 자료는 유지했다.

- 3단계 재킷 착장을 입력으로 재생성한 회전 15방향(960×1440, Lightning 4스텝)과 앨리웁(1024×1536, 20스텝)을 원고의 이미지·결과 JSON에 반영했다. 입력 역할표를 세 단계 경로로 맞추고 회전 전체 실행 목록을 연결했다. 앨리웁의 재킷 누락·바지 길이 변화를 명시했다. 16개 결과의 입력·출력 해시와 원고 링크를 확인했다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.08`.

- 착장 이미지에서 신발을 추출하는 실험을 폐기하고 해당 본문·생성 스크립트·PNG·결과 JSON과 모델 인벤토리 사용 소스 등록을 제거했다. Qwen-Image-2512의 흰색 스니커즈 생성과 착장 2단계 신발 추가는 유지했다. 번역 동기화: 한국어 원문만 반영.

- 재킷 추가 3단계의 로컬 GPU 10스텝 PNG·결과 JSON을 원고에 반영했다. 2단계 전신과 정면 머리의 입력 역할, 열린 앞판·접힌 칼라·양손과 기존 착장 유지, 크림색 배경 변화를 기록했다. 기존 회전·앨리웁과 새 착장 결과의 관계를 구분하고 입력·출력 해시와 원고 링크를 검증했다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.08`.

- 신발 추가 2단계의 로컬 GPU 10스텝 결과와 JSON을 원고에 반영했다. 흰색 스니커즈 적용과 함께 배경색 변화·음영 단순화를 기록하고, 3단계의 첫 입력으로 연결했다. 재현 명령은 새 실행 이름의 전체 3단계 실행으로 정리했다. 번역 동기화: 한국어 원문만 반영.

- 착장 생성기를 1단계 맨발 기본 의상, 2단계 신발 참조 적용, 3단계 재킷 추가로 개편했다. 이전 단계 자동 연결과 단일 단계 입력 지정 옵션을 추가하고, 맨발 1단계를 로컬 GPU에서 10스텝 생성해 원고에 반영했다. 1단계 반영 당시 새 2·3단계는 미실행이었으며 기존 두 단계 결과와 구분했다. 입력·출력 해시와 코드 구문을 확인했다. 번역 동기화: 한국어 원문만 반영.

- Qwen-Image-2512로 흰색 스니커즈 한 쌍을 텍스트만으로 10스텝 생성했다. 로컬 BF16 생성기·PNG·결과 JSON·재현 명령을 추가하고 모델 인벤토리에 사용 소스를 등록했다. 기존 착장 신발 추출과 새 디자인 생성을 구분하며 밑창의 검은 줄·신발 겹침을 한계로 기록했다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.08`.
- 신발 추출 v1을 로컬 GPU에서 30스텝으로 생성하고 PNG·결과 JSON을 원고에 연결했다. 인물·바지가 제거된 신발 한 쌍은 생성됐으나 남색 패널·두꺼운 밑창이 추가되어 원본 디자인 보존에 실패했음을 기록했다. 입력·출력·생성 코드의 SHA-256을 검증했다. 번역 동기화: 한국어 원문만 반영.
- 긴 바지·접힌 칼라 v3 착장 한 장에서 신발 한 쌍을 흰 배경에 분리 생성하는 로컬 Qwen Edit 2511 스크립트를 추가했다. 입력 참조·프롬프트·30스텝 기본값·드라이런·재현 옵션을 원고에 연결하고, 픽셀 추출과 생성 방식의 차이 및 신발 형태 검수 기준을 설명했다. 실제 생성은 실행하지 않았으며 출력 성능을 단정하지 않았다.
- 관련 자산: `p7_5_3_qwen_edit_2511_extract_shoes.py`, 기존 P7-5.3 착장 v3 PNG. Qwen 2511 모델 인벤토리에 사용 소스를 등록했다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.08`.

### v2026.09.05

- P7-5.2의 정면 identity·상반신 멀티뷰 설명을 반복하지 않도록 경계를 분명히 하고, P7-5.3 고유의 단계별 입력 역할·편집 모델·LoRA·OpenPose renderer·메모리 배치 역할을 다시 정리했다.
- 1·2단계 착장, 관계 맵, 15방향 회전, 앨리웁 생성기의 실제 옵션·입력 순서·조작값·각 `result.json` 기록 항목을 원고의 각 코드 링크 바로 뒤에 보강했다.
- 이전 2509 4방향 yaw 실험 표와 실행 기록을 `Qwen/Qwen-Image-Edit-2511`, Multiple-Angles LoRA, Lightning 4-step으로 생성한 960×1440 15방향 결과로 대체했다. 저각·아이레벨·엘리베이티드와 yaw −90°·−45°·0°·+45°·+90°의 각 결과 PNG를 원고에 배치하고, 개별 `result.json` 15개를 lazy-source 링크로 연결했다.
- 회전 생성기의 기본 실행 조건을 960×1440, 순차 CPU offload, 4-step Lightning으로 본문 설명과 맞췄다. 단일 입력은 2단계 정면 착장이고, OpenPose와 별도 얼굴 참조는 회전 단계에서 사용하지 않는다.
- 앨리웁 생성기를 2509 Nunchaku FP4 경로에서 `Qwen/Qwen-Image-Edit-2511` 공식 BF16 `DiffusionPipeline`과 순차 CPU offload 경로로 전환했다. 원고에서 2509·Nunchaku를 현재 앨리웁 실행 조건으로 설명하거나 연결한 이전 산출물은 분리했다.
- 새 2511 앨리웁 결과(1024×1536, 20-step)와 개별 `result.json`을 동적 장면 문단에 반영했다.
- 1단계 기준 이미지를 새 OpenPose 맵을 사용한 10-step v9 결과로 교체했다. 기존 압축 prompt에는 양팔·양손이 보인다는 긍정 지시만 추가해, 팔과 손을 끝까지 생성한 결과를 원고에 반영했다.
- 착장 생성기의 공통 fallback과 1·2단계 기본 추론 스텝을 10으로 낮췄다.
- 2단계 생성기의 첫 입력을 원고 반영 1단계 v9으로 교체하고, 새 10-step 자켓 결과를 2단계 기준 이미지와 실행 기록으로 반영했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.05`.

### v2026.09.04

- 1·2단계 복장 생성기의 머리 참조를 이전 Q4 Comfy 출력에서 P7-5.2 BF16 정면 Mira 머리 결과(1280×1280)로 교체하고, 입력 역할의 해상도 표기를 함께 정정했다.
- 원고의 1·2단계 입력 설명도 같은 정면 머리 결과와 1280×1280 기준으로 맞췄다.
- 1·2단계 생성기를 Nunchaku FP4 transformer 대신 공식 Qwen-Image-Edit-2511 BF16 pipeline과 순차 CPU offload 경로로 전환하고, 원고의 런타임 설명을 갱신했다.
- 1단계는 OpenPose를 첫 입력, P7-5.2 정면 Mira 머리를 두 번째 입력으로 둔 채, 회색 마이크로 크롭티·딥틸 와이드 팬츠·흰 로우탑 스니커즈만 한 문장으로 지시하는 압축 착장 프롬프트로 정리했다.
- 전신 BODY_18 OpenPose 맵을 960×1440으로 다시 렌더링하고, 전체 키를 10% 축소했다. 좌표는 유지한 채 선 반폭과 관절 반지름도 각각 7px로 키워 전신 생성 입력에서 읽기 쉽게 했다.
- 같은 OpenPose 비례 템플릿에서 다리 비중의 10%를 상체·허리 구간으로 옮겨, 전체 키와 목·얼굴 위치는 유지하면서 허리를 길게 하고 다리를 짧게 조정했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.04`.

### v2026.09.03

- 참조 역할표를 각 생성 `result.json`의 실제 `inputs`·`output`·prompt 기준으로 다시 작성했다. 정면 머리, 정면 토르소, 1·2단계 착장, OpenPose, 방향 착장의 역할과 연결 범위를 분리했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.03`.

### v2026.09.02

- 기본 1·2단계 착장 설명, 현재 생성 코드, Part 7 오픈체크리스트에서 가방·스트랩을 제거하고, 착장·전신·body-only OpenPose의 역할만 남겼다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.02`.

### 이전 이력

- 정면 1·2단계 착장, body-only OpenPose, 앨리웁 전신, 방향별 착장의 입력 역할을 분리하고 각 실행 코드와 `result.json`을 해당 결과 바로 뒤에 배치했다.
- 회전과 앨리웁은 2511 경로로 정리했으며, 현재 기본 경로에서 제외한 과거 실행 기록은 Git 이력으로 남긴다.
- 헤드리스·추가 단계·전신 회전·OpenPose 결합 후보를 비교했으며, 현재 기본 경로에서 제외한 자산과 세부 조건은 Git 이력으로 남긴다.
