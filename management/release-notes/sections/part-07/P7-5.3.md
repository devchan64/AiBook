# Section Release Note

- Section ID: `P7-5.3`
- Source File: `docs/parts/part-07/chapter-05/section-03.md`

### v2026.07.18
- 추가 반영: 본문의 두 Mermaid 도식을 `docs/assets/part-07/chapter-05/*.mmd` 자산으로 분리하고, 자산 폴더 README에 현재 한국어 운영 범위와 질문 경계 도식 동기화 기준을 기록했다.
- 추가 반영: 질문 재작성 성공을 곧바로 `질문만 잘 쓰면 해결된다`로 읽지 않도록, 범위 밖 질문·과장 질문·재작성 뒤에도 남는 근거 부족 질문을 먼저 가르게 하는 사례 문단과 Mermaid 흐름도를 보강했다.
- 추가 반영: 범위 문단의 진행형 메타 표현을 줄이고, 질문 재작성과 멈춤 기준이 답변 상태를 어떻게 바꾸는지 현재 절의 판단 축이 먼저 보이게 다듬었다.
- 추가 반영: 말미 실습 표제를 `직접 바꿔 보며 확인할 것`으로 통일하고, 문서 집합과 경계 사례 질문 파일을 `출처와 참고 자료`에 명시해 절 마감 구조를 가이드와 맞췄다.
- 추가 반영: 도입 문장에서 현재 절을 독립된 순차 연습이 아니라 같은 RAG 프로젝트 기록 안에서 `질문 경계`가 품질 판정을 어떻게 바꾸는지 확인하는 자리로 더 분명히 적었다.
- 추가 반영: 도입과 범위 설명에서 `P7-5.1`, `P7-5.2`를 계단식으로 요약하는 문장을 줄이고, 질문 문구가 답변 상태를 가르는 현재 절의 연습 축이 먼저 보이게 다듬었다.
- 신규 작성: P7-5 장에 `질문-근거 경계 연습` 절을 추가해, 설명 절과 검증 절 뒤에서 질문 재작성과 멈춤 규칙을 직접 연습할 수 있게 구성했다.
- 신규 반영: 기존 RAG 문서 CSV와 함께 `p7-5-boundary-cases.csv`를 새로 추가해, 문서 범위 밖 질문, 과장 질문, 보수적 재작성 질문을 같은 지식베이스 위에서 비교하도록 만들었다.
- 신규 반영: Python 예제는 질문별 상태 판정만 보여 주지 않고, 재작성 전후 상태 변화와 다음 수정 방향까지 한 번에 읽도록 구성했다.
- 번역 동기화 메모: Future translations should preserve the distinction between out-of-scope questions and over-strong questions, plus the before/after rewrite comparison.
