# Section Release Note

- Section ID: `P7-7.3`
- Source File: `docs/parts/part-07/chapter-07/section-03.md`

### v2026.07.18
- 구조 정리: 동작 단위 운영 신호를 세 행동 구간으로 분류하던 확장 연습을 `P7-7.4 경고 등급 설계 연습`으로 분리하고, 이 절은 일반 운영 시나리오를 `즉시 수정`, `재현 확인`, `다음 반복 개선`으로 나누는 중심 질문에 집중하도록 정리했다.
- 추가 반영: 본문의 두 Mermaid 도식을 `docs/assets/part-07/chapter-07/*.mmd` 자산으로 분리하고, Chapter 7 자산 폴더 README에 운영 회고 도식 운영 범위를 기록했다.
- 추가 반영: `404 링크`와 `모바일 차트 넘침`을 같은 속도의 문제로 묶지 않도록, 독자 영향·재현 가능성·즉시 수정 가능성을 먼저 보게 하는 사례 문단과 Mermaid 흐름도를 보강했다.
- 추가 반영: 범위 문단의 진행형 메타 표현을 줄이고, 운영 신호를 `즉시 수정`, `재현 확인`, `다음 반복 개선`으로 나누는 현재 절 판단 축이 먼저 보이게 다듬었다.
- 추가 반영: 중복으로 남아 있던 `직접 더 바꿔 볼 것` 표지를 제거해 실습 말미 구조를 가이드 표지 하나로 정리했다.
- 추가 반영: 번호 목록 앞에 `직접 바꿔 보며 확인할 것` 표지를 새로 세우고, 운영 시나리오 파일과 자체 예시 사용 여부를 `출처와 참고 자료`에 보강해 마감 구조를 가이드와 맞췄다.
- 추가 반영: 운영 판단 표와 설명에서 `다음 반복으로 넘긴다`는 표현을 `다음 반복 개선으로 둔다`로 다듬고, `앞 절의 실패 원장` 같은 순차 안내를 걷어내 현재 절의 행동 구간 분류가 먼저 보이게 정리했다.
- 추가 반영: 도입과 범위 설명에서 `P7-7.1`, `P7-7.2`를 계단식으로 되짚는 표현을 줄이고, 운영 신호를 행동 구간으로 나누는 현재 절의 판단 질문이 먼저 보이게 정리했다.
- 신규 작성: P7-7 장에 `운영 점검 시뮬레이션` 절을 추가해, 배포 뒤 관찰 신호를 `즉시 수정`, `재현 확인`, `다음 반복 개선`으로 직접 분류하는 실습을 별도 절로 분리했다.
- 신규 반영: `p7-7-ops-scenarios.csv`를 새로 추가해, 404, 반영 지연, 모바일 차트 넘침, 번역 링크 오류 같은 운영 장면을 행동 구간 중심으로 비교하도록 만들었다.
- 신규 반영: Python 예제는 사건별 행동 구간, 첫 점검 위치, 회고 문장 초안을 함께 출력해 운영 판단이 바로 다음 행동으로 이어지게 구성했다.
- 번역 동기화 메모: Future translations should preserve the separation between immediate fixes, reproduction checks, and next-iteration improvements, especially where reader impact and reproducibility differ.
