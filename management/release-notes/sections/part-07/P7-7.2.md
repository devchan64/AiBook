# Section Release Note

- Section ID: `P7-7.2`
- Source File: `docs/parts/part-07/chapter-07/section-02.md`

### v2026.07.07
- 본문 반영: 공통 회고 구조 표를 `사실`, `해석`, `다음 질문` 중심으로 다시 쓰고, 실패 예시 범주와 조치 설명도 `배포/본문 반영`, `실행 중`, `목차 경로 설정`, `배포 브랜치` 같은 한국어 중심 표현으로 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-7.2 공통 회고 구조와 실패 예시 표에서는 한국어 구조명과 운영 설명을 먼저 두고 영어 병기는 보조로 유지하는 편이 좋다.

### v2026.07.12
- 추가 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다.
- 본문 반영: 실패 유형 분리, 가능한 원인, 우선순위, 다음 조치, 운영 회고 기준을 `체크리스트` 하나로 통합했다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 본격 장애 관리 체계, 장기 비용 분석, 팀 단위 SLO 설계 같은 생략 목록을 걷어내고, 이번 절이 `실패를 원인 추정과 다음 조치로 남기는 마지막 회고 구조`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: Part 7 전체에서 쌓은 검토 기록이 마지막 실패 회고 문서로 다시 묶인다는 점을 범위 문장에 더 직접 드러냈다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-7.2 closes the final failure-retrospective structure for the whole project part.

### v2026.07.18
- 추가 반영: `가능한 원인` 설명에서 `읽는 편이 맞습니다` 같은 독법 안내를 줄이고, 현재 기록 시점의 가장 가까운 설명 후보라는 운영 기록 원칙으로 더 직접 정리했다.
- 추가 반영: Section 제목을 `실패 기록과 개선 계획`에서 `장애 기록과 다음 반복 계획`으로 바꾸고, 운영 사건을 기록하고 다음 반복으로 넘기는 역할이 현재 목차와 같은 이름으로 보이게 맞췄다.
- 추가 반영: 배포 사건 CSV 자산 경로를 본문 링크로 바꿔, 운영 회고 원장과 실패 기록 해설을 바로 연결해 읽을 수 있게 했다.
- 추가 반영: `작은 프로젝트` 중심 표현을 줄이고, 실패 기록 절이 프로젝트 규모와 무관하게 운영 회고의 출발점이 되는 구조로 읽히도록 문장 톤을 정리했다.
- 변경 이유: 기존 P7-7.2는 실패 기록 형식은 있었지만, AiBook 저장소의 실제 배포 흐름에서 어떤 단계가 실패하는지보다 일반적인 운영 회고 템플릿 설명이 앞서 실습성이 약했다.
- 본문 반영: 실패 범주를 `브랜치 반영 실패`, `빌드 실패`, `Pages 반영 실패`, `공개 확인 실패`로 다시 나누고, `dev`와 `main`, strict build, 공개 URL, 링크 404 같은 현재 저장소 기준 문제 장면으로 예시를 교체했다.
- 추가 반영: Python 예제도 일반 문제 목록이 아니라 사건 ID, 관찰 신호, 확인 위치, 즉시 확인 명령, 우선순위를 함께 다루는 운영 회고 구조로 강화했다.
- 추가 반영: 운영 사건 예시를 `docs/assets/part-07/chapter-07/p7-7-deployment-incidents.csv`로 분리하고, Python 코드는 CSV를 읽어 실패 기록과 개선 계획을 만드는 구조로 정리했다.
- 추가 반영: 다른 Part 7 절과 같은 읽기 리듬이 보이도록 실행 결과 해설 표지를 `## 결과를 어떻게 읽는가`로 통일했다.
- 추가 반영: `입력 파일` 소절을 따로 세워 사건 파일의 핵심 열과 회고 원장 역할을 먼저 설명하고, 그다음 실패 기록 재구성 코드로 넘어가도록 구조를 분리했다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 우선순위와 관찰 신호 강도를 바꾸며 실패 회고와 개선 계획이 어떻게 달라지는지 직접 실험하도록 보강했다.
- 번역 동기화 메모: Future translations should avoid framing P7-7.2 as only a small-project retrospective and should preserve the AiBook-specific failure categories and the stronger operational review structure with observable signals and inspection locations.
