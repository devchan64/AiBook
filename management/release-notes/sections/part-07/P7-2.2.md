# Section Release Note

- Section ID: `P7-2.2`
- Source File: `docs/parts/part-07/chapter-02/section-02.md`

### v2026.07.20
- 저작권 검토: Part 7 Chapter 2 모듈 순차 검토 범위에서 회고와 다음 질문 구조, 정규화 전후 비교, NumPy 예제 출력, synthetic 이탈 위험 데이터, Mermaid 흐름도, 체크리스트를 확인했다. 데이터와 비교 예시는 Part 7 실습을 위해 구성한 자체 예시로 판단했다.
- 출처 사용 점검: NumPy 공식 문서는 참고 자료로 명시했고, 외부 문장을 직접 인용하지 않았다. 외부 도표·표·데이터 재사용은 확인하지 않았다.
- 관련 자산: `docs/assets/part-07/chapter-02/p7-2-churn-dataset.csv`, `docs/assets/part-07/chapter-02/p7-2-2-raw-distance-risk-flow-ko.mmd`
- 본문 반영: 제목 아래 메타데이터를 저장소 공통 형식인 평문 `Section ID`, `Version` 두 줄로 바로잡고, 본문 `Version`을 `v2026.07.20`으로 갱신했다.
- 번역 동기화 메모: 대응 번역본이 생기면 메타데이터 형식과 한국어 Mermaid 자산의 언어별 파생본 생성 여부를 함께 확인해야 한다.

### v2026.07.19
- 변경 이유: Part 7 연습 Section의 학습밀도 보강 이력을 원고 가이드 기준에 맞춰 압축 정리했다.
- 본문 반영: 범위·목표·연습 표지와 본문 안의 절 진행 지시 표현을 내용형 기록 기준으로 바꾸고, 실행 기록·결과 해석·프로젝트 기록 예시가 설명과 해설 안에서 읽히도록 정리했다.
- 번역 동기화 메모: 향후 번역본도 진행 안내 제목보다 Section별 질문, 판단 기준, 실행 결과 해석, 프로젝트 기록 예시가 먼저 보이게 맞춘다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 원문 기준 버전: `v2026.07.19`

### v2026.07.07
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 도입부, 범위, 목표, 기준 표의 첫 핵심 언급을 `기준점(baseline)`으로 정리해 후속 절에서도 공개 독자용 설명을 유지했다. / 비교 요약과 샘플별 출력 키를 `정규화 전 예측`, `정규화 후 최근접 학습 샘플`, `예측 변화 샘플 수`처럼 독자용 설명형 표현으로 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-2.2 공개 출력 키를 정규화 전후 비교가 바로 읽히는 설명형 레이블로 유지해야 한다.

### v2026.07.12
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다. / 기준점 대비 차이, 예측 사례, 변화 없음 기록, 성과와 한계 요약 항목을 `체크리스트` 하나로 통합했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: `이 절의 범위`에서 precision/recall/F1 심화 비교, 교차검증, 하이퍼파라미터 탐색 자동화 같은 생략 목록을 걷어내고, 이번 절이 `같은 기준에서 무엇이 달라졌는가`를 비교 가능한 기록으로 바꾸는 기준을 닫는 자리라는 점이 먼저 보이도록 다시 정리했다. / 평가 축 handoff를 `P7-4.2`, `P7-5.2`로 직접 연결해 Part 7 뒤 프로젝트의 공통 비교 기록 구조가 다시 보이게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-2.2 closes the improvement-record structure first and hands richer evaluation framing to later project sections.

### v2026.07.18
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 말미 체크리스트를 `같은 평가 셋`, `달라진 사례`, `예측 경로`, `한계`, `다음 질문` 다섯 칸으로 압축해, 개선 주장을 점수 상승이 아니라 비교 가능한 기록으로 남기게 정리했다. / 본문의 Mermaid 도식을 `docs/assets/part-07/chapter-02/p7-2-2-raw-distance-risk-flow-ko.mmd` 자산으로 분리하고, Chapter 2 자산 폴더 README에 현재 한국어 운영 범위를 기록했다. / `평가-02`, `평가-04`를 중심으로 `빠른 판단 -> 더 안전한 다음 판단`이 어떻게 갈리는지 사례 문단으로 풀어 쓰고, 바로 아래 Mermaid 흐름도로 비교 판단 순서를 다시 읽게 보강했다. 등으로 압축 정리했다.
- 번역 동기화 메모: Future translations should preserve the normalization-caused nearest-neighbor changes and the stronger improvement-record framing.
