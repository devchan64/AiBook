# Section Release Note

- Section ID: `P7-2.2`
- Source File: `docs/parts/part-07/chapter-02/section-02.md`

### v2026.08.01
- 변경 이유: P7-2.2에서 정규화 후 정확도만으로 개선을 읽지 않고, 회수된 오류와 새 오답을 같은 평가 셋에서 함께 기록하게 보강했다.
- 본문 반영: raw 1-NN과 정규화 1-NN 사이의 샘플 결과 전환을 세는 차트를 추가했다. `평가-02`, `평가-04`의 회수 2건과 새 오답 0건을 해설로 연결하고, 사실·해석·다음 실험의 세 단계 학습 점검을 추가했다.
- 번역 동기화 메모: 향후 번역본은 raw 1-NN 대비 정규화 1-NN의 회수 2건, 새 오답 0건과 사실·해석·다음 실험 점검을 함께 반영한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 관련 자산: `docs/assets/part-07/chapter-02/p7-2-churn-dataset.csv`, `docs/assets/part-07/chapter-02/p7_2_2_normalization_outcome_chart.py`, `docs/assets/part-07/chapter-02/p7-2-2-normalization-outcome-chart-ko.png`
- 원문 기준 버전: `v2026.08.01`

### v2026.07.31
- 변경 이유: Part 7 Section 메타데이터가 현재 가이드의 blockquote 형식과 어긋나 있어 규칙 준수 정리를 진행했다.
- 본문 반영: 제목 아래 `Section ID`, `Version` 두 줄을 blockquote 형식으로 정리하고, 본문 `Version`을 `v2026.07.31`로 갱신했다. 원고 학습 흐름과 실습 내용은 변경하지 않았다.
- 추가 반영: 회고 기록을 사실, 해석, 남은 오류, 다음 질문, 추가 데이터 필요, 결정 기록으로 분리하도록 보강했다.
- 번역 동기화 메모: 향후 번역본도 같은 Section ID와 blockquote 메타데이터 형식을 사용해야 한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.31`


### v2026.07.26
- 정합성 점검: 본문 `Version`을 `v2026.07.26`으로 맞췄다.
- 개념사전 연결: 포괄 개념사전 링크를 관련 한국어 항목 직접 링크로 좁혔다.
- 번역 반영 상태: 기존 공개 번역 구조 유지
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-07/chapter-02/section-02.md` `v2026.07.26`


### v2026.07.23
- 변경 이유: Part 7에서 실제 AI 모델·라이브러리 사용으로 학습밀도를 높일 수 있는 후보로 P7-2.2를 선정했다.
- 본문 반영: NumPy 직접 거리 계산 예제를 scikit-learn `KNeighborsClassifier`, `StandardScaler`, `Pipeline` 기반 예제로 교체했다.
- 학습 품질 보강: 전처리와 모델을 하나의 실행 묶음으로 연결하면서도 기존 출력 구조인 정규화 전후 정확도, 최근접 학습 샘플, 예측 변화 샘플을 유지해 개선 기록 기준을 더 실무형으로 읽게 했다.
- 번역 동기화 메모: 향후 번역본도 scikit-learn `Pipeline`이 전처리와 모델을 묶어 같은 평가 셋에 같은 기준을 적용한다는 설명을 유지해야 한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 관련 자산: `docs/assets/part-07/chapter-02/p7-2-churn-dataset.csv`
- 원문 기준 버전: `v2026.07.23`

### v2026.07.22
- 변경 이유: Part 7 예제·연습 코드 순차 개선 작업에서 P7-2.2의 실제 실행 출력과 본문 예시를 맞추고, 정규화 실험의 방어 조건을 보강했다.
- 본문 반영: CSV 링크에 `csv-preview` 표시를 붙이고, `feature_columns`를 분리해 입력 특징 순서를 명확히 했다. 표준편차가 0인 특징에 대한 방어 오류를 추가했으며, 학습 평균·표준편차와 최근접 학습 샘플 출력 예시 및 결과 해석 표를 현재 CSV 기준 실제 실행값으로 정정했다.
- 번역 동기화 메모: 향후 번역본도 학습 평균 `[3.83, 12.83, 3158.33]`, 표준편차 `[2.34, 8.52, 570.76]`, `평가-02 -> 학습-02`, `평가-06 정규화 후 -> 학습-06` 출력 예시를 기준으로 맞춰야 한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 관련 자산: `docs/assets/part-07/chapter-02/p7-2-churn-dataset.csv`
- 원문 기준 버전: `v2026.07.22`

### v2026.07.20
- Python 예제 주석 보강: 구독 이탈 1-NN 모델의 정규화 전후 최근접 샘플과 예측 변화 비교 목적을 코드 블록 첫 줄 주석으로 명시했다.
- 저작권 검토: Part 7 Chapter 2 모듈 순차 검토 범위에서 회고와 다음 질문 구조, 정규화 전후 비교, NumPy 예제 출력, synthetic 이탈 위험 데이터, Mermaid 흐름도, 체크리스트를 확인했다. 데이터와 비교 예시는 Part 7 실습을 위해 구성한 자체 예시로 판단했다.
- 출처 사용 점검: NumPy 공식 문서는 참고 자료로 명시했고, 외부 문장을 직접 인용하지 않았다. 외부 도표·표·데이터 재사용은 확인하지 않았다.
- 관련 자산: `docs/assets/part-07/chapter-02/p7-2-churn-dataset.csv`, `docs/assets/part-07/chapter-02/p7-2-2-raw-distance-risk-flow-ko.mmd`
- 본문 반영: 제목 아래 메타데이터를 저장소 공통 형식인 평문 `Section ID`, `Version` 두 줄로 바로잡고, 본문 `Version`을 `v2026.07.20`으로 갱신했다.
- 번역 동기화 메모: 대응 번역본이 생기면 메타데이터 형식과 한국어 Mermaid 자산의 언어별 파생본 생성 여부를 함께 확인해야 한다.

### v2026.07.19
- 변경 이유: Part 7 연습 Section의 학습밀도 보강 이력을 원고 가이드 기준에 맞춰 압축 정리했다.
- 본문 반영: 범위·목표·연습 표지와 본문 안의 절 진행 지시 표현을 내용형 기록 기준으로 바꾸고, 실행 기록·결과 해석·프로젝트 기록 예시가 설명과 해설 안에서 읽히도록 정리했다.
- 번역 동기화 메모: 향후 번역본도 진행 안내 제목보다 Section별 질문, 판단 기준, 실행 결과 해석, 프로젝트 기록 예시가 먼저 보이게 맞춘다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 원문 기준 버전: `v2026.07.19`

### v2026.07.07
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 도입부, 범위, 목표, 기준 표의 첫 핵심 언급을 `기준점(baseline)`으로 정리해 후속 절에서도 공개 독자용 설명을 유지했다. / 비교 요약과 샘플별 출력 키를 `정규화 전 예측`, `정규화 후 최근접 학습 샘플`, `예측 변화 샘플 수`처럼 독자용 설명형 표현으로 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-2.2 공개 출력 키를 정규화 전후 비교가 바로 읽히는 설명형 레이블로 유지해야 한다.

### v2026.07.12
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다. / 기준점 대비 차이, 예측 사례, 변화 없음 기록, 성과와 한계 요약 항목을 `체크리스트` 하나로 통합했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: `이 절의 범위`에서 precision/recall/F1 심화 비교, 교차검증, 하이퍼파라미터 탐색 자동화 같은 생략 목록을 걷어내고, 이번 절이 `같은 기준에서 무엇이 달라졌는가`를 비교 가능한 기록으로 바꾸는 기준을 닫는 자리라는 점이 먼저 보이도록 다시 정리했다. / 평가 축 handoff를 `P7-4.2`, `P7-6.2`로 직접 연결해 Part 7 뒤 프로젝트의 공통 비교 기록 구조가 다시 보이게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-2.2 closes the improvement-record structure first and hands richer evaluation framing to later project sections.

### v2026.07.18
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 말미 체크리스트를 `같은 평가 셋`, `달라진 사례`, `예측 경로`, `한계`, `다음 질문` 다섯 칸으로 압축해, 개선 주장을 점수 상승이 아니라 비교 가능한 기록으로 남기게 정리했다. / 본문의 Mermaid 도식을 `docs/assets/part-07/chapter-02/p7-2-2-raw-distance-risk-flow-ko.mmd` 자산으로 분리하고, Chapter 2 자산 폴더 README에 현재 한국어 운영 범위를 기록했다. / `평가-02`, `평가-04`를 중심으로 `빠른 판단 -> 더 안전한 다음 판단`이 어떻게 갈리는지 사례 문단으로 풀어 쓰고, 바로 아래 Mermaid 흐름도로 비교 판단 순서를 다시 읽게 보강했다. 등으로 압축 정리했다.
- 번역 동기화 메모: Future translations should preserve the normalization-caused nearest-neighbor changes and the stronger improvement-record framing.
