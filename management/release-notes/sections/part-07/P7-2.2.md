# Section Release Note

- Section ID: `P7-2.2`
- Source File: `docs/parts/part-07/chapter-02/section-02.md`

### v2026.07.19
- 변경 이유: 원고 가이드의 메타 진행 표지 축소 기준에 맞춰 연습 도입부 제목을 점검했다.
- 본문 반영: `이 절의 범위`, `이 절의 목표` 표지를 `개선을 주장하는 기준`, `남겨야 할 판단`으로 바꿔 현재 Section의 중심 질문과 기록 기준이 바로 보이도록 정리했다.
- 추가 반영: `이 절에서 직접 할 일`, `관찰 포인트`, `기록 템플릿`을 추가해 정규화 전후 정확도, 예측 변화 샘플, 최근접 이웃 변화, 개선 한계를 함께 기록하도록 보강했다.
- 번역 동기화 메모: 향후 번역본도 generic scope/goal headings보다 section-specific practice judgment headings를 우선한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 원문 기준 버전: `v2026.07.19`

### v2026.07.07
- 본문 반영: 도입부, 범위, 목표, 기준 표의 첫 핵심 언급을 `기준점(baseline)`으로 정리해 후속 절에서도 공개 독자용 설명을 유지했다.
- 번역 동기화 메모: 향후 번역본도 P7-2.2 도입부와 목표 문장에서 baseline을 한국어 기준 용어와 함께 유지해야 한다.

### v2026.07.07
- 본문 반영: 비교 요약과 샘플별 출력 키를 `정규화 전 예측`, `정규화 후 최근접 학습 샘플`, `예측 변화 샘플 수`처럼 독자용 설명형 표현으로 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-2.2 공개 출력 키를 정규화 전후 비교가 바로 읽히는 설명형 레이블로 유지해야 한다.

### v2026.07.12
- 추가 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다.
- 본문 반영: 기준점 대비 차이, 예측 사례, 변화 없음 기록, 성과와 한계 요약 항목을 `체크리스트` 하나로 통합했다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 precision/recall/F1 심화 비교, 교차검증, 하이퍼파라미터 탐색 자동화 같은 생략 목록을 걷어내고, 이번 절이 `같은 기준에서 무엇이 달라졌는가`를 비교 가능한 기록으로 바꾸는 기준을 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 평가 축 handoff를 `P7-4.2`, `P7-5.2`로 직접 연결해 Part 7 뒤 프로젝트의 공통 비교 기록 구조가 다시 보이게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-2.2 closes the improvement-record structure first and hands richer evaluation framing to later project sections.

### v2026.07.18
- 추가 반영: 말미 체크리스트를 `같은 평가 셋`, `달라진 사례`, `예측 경로`, `한계`, `다음 질문` 다섯 칸으로 압축해, 개선 주장을 점수 상승이 아니라 비교 가능한 기록으로 남기게 정리했다.
- 추가 반영: 본문의 Mermaid 도식을 `docs/assets/part-07/chapter-02/p7-2-2-raw-distance-risk-flow-ko.mmd` 자산으로 분리하고, Chapter 2 자산 폴더 README에 현재 한국어 운영 범위를 기록했다.
- 추가 반영: `평가-02`, `평가-04`를 중심으로 `빠른 판단 -> 더 안전한 다음 판단`이 어떻게 갈리는지 사례 문단으로 풀어 쓰고, 바로 아래 Mermaid 흐름도로 비교 판단 순서를 다시 읽게 보강했다.
- 추가 반영: 범위 문단의 진행형 메타 표현을 줄이고, 기준점 대비 차이를 비교 가능한 기록으로 바꾸는 현재 절 역할이 더 직접 읽히게 다듬었다.
- 추가 반영: 도입과 소제목의 `앞 절 결과` 중심 표지를 줄이고, 같은 비교 결과를 회고와 다음 질문으로 바꾸는 현재 절의 역할이 먼저 보이게 정리했다.
- 추가 반영: 후속 절 연결 문장에서 예전 제목 `토큰화와 평가`, `검색 품질과 답변 검증`을 현재 제목 `실패한 결과를 다시 분해하기`, `검색 실패와 근거 누락 기록`으로 바꿔, Part 7 중반 교차 참조가 실제 목차와 어긋나지 않게 정리했다.
- 추가 반영: Section 제목을 `기준 모델과 개선`에서 `회고와 다음 질문 만들기`로 바꾸고, 기준점 대비 비교를 다음 질문으로 넘기는 문서 역할이 제목에 드러나게 정리했다.
- 추가 반영: P7-2.1과 공유하는 CSV 자산 경로를 링크로 노출해, 같은 데이터 파일 위에서 비교 실험이 이어진다는 점이 더 직접 보이게 했다.
- 본문 반영: 개선 예제를 `정규화 전후 정확도는 같음` 구조에서 `raw 1-NN 0.667 -> 정규화 후 1.000`으로 바꾸고, `평가-02`, `평가-04`가 왜 뒤집혔는지를 최근접 이웃 변화와 함께 읽게 다시 구성했다.
- 추가 반영: 전처리를 단순 부가 절차가 아니라 거리 계산과 최근접 이웃 선택을 실제로 바꾸는 단계로 설명하고, 회고 문장도 같은 기준으로 강화했다.
- 추가 반영: 한계 표와 체크리스트에 남아 있던 `작은 데이터`, `작은 프로젝트` 표현을 `제한된 데이터`, `프로젝트 회고`로 조정해 실습 강도를 약하게 만드는 축소 표현을 줄였다.
- 추가 반영: P7-2.1과 같은 `docs/assets/part-07/chapter-02/p7-2-churn-dataset.csv`를 읽도록 예제를 정리해, 분류 데이터와 전처리 코드를 분리하고 같은 데이터 파일 위에서 개선 비교가 이어지게 맞췄다.
- 추가 반영: 다른 Part 7 절과 같은 읽기 리듬이 보이도록 `## Python 예제`, `## 결과를 어떻게 읽는가` 표지를 명시해 개선 실습 구조를 더 또렷하게 정리했다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 부분 스케일 조정과 애매한 고객 추가를 통해 개선이 어떤 샘플에서는 남고 어떤 샘플에서는 남지 않는지 직접 실험하도록 보강했다.
- 번역 동기화 메모: Future translations should preserve the normalization-caused nearest-neighbor changes and the stronger improvement-record framing.
