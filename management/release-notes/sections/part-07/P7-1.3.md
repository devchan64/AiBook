# Section Release Note

- Section ID: `P7-1.3`
- Source File: `docs/parts/part-07/chapter-01/section-03.md`

### v2026.07.18
- 추가 반영: 동작 단위 합성 원시 로그와 동작 요약 CSV를 추가하고, 최근 구간 시작점을 바꾸며 중반 유량 평균, 후반 하강률, 검토 비율을 비교하는 기준선 확장 연습을 본문에 넣었다.
- 관련 자산: `docs/assets/part-07/chapter-01/p7-action-unit-sensor-log.csv`, `docs/assets/part-07/chapter-01/p7-action-unit-summary.csv`
- 추가 반영: 본문의 두 Mermaid 도식을 `docs/assets/part-07/chapter-01/*.mmd` 자산으로 분리하고, Chapter 1 자산 폴더 README에 기준선 도식 운영 범위를 기록했다.
- 추가 반영: `최근 4일 집중` 설정이 더 민감해 보여도 곧바로 최적 기준선으로 단정하지 않도록, 질문 종류와 표본 수를 함께 보게 하는 사례 문단과 Mermaid 흐름도를 보강했다.
- 추가 반영: 범위 문단의 진행형 메타 표현을 줄이고, 같은 로그라도 기준선 구간과 샘플 단위가 바뀌면 회고 첫 문장이 달라진다는 현재 절 질문이 더 직접 보이게 다듬었다.
- 추가 반영: `직접 더 바꿔 볼 것` 표지를 가이드 기준인 `직접 바꿔 보며 확인할 것`으로 맞추고, 실습 로그 파일과 자체 예시 사용 여부를 `출처와 참고 자료`에 함께 보강했다.
- 추가 반영: 도입 문장의 `연습 절` 표기를 다듬어, 현재 절이 기준선 설계 차이를 직접 확인하는 자리라는 설명이 더 먼저 보이게 정리했다.
- 추가 반영: 입력 파일 설명에서 `앞 절과 같은 파일` 같은 순차 안내를 걷어내고, 같은 로그라도 기준선 경계와 비교 단위가 달라지면 해석의 앞줄이 달라진다는 현재 절 질문이 먼저 보이게 정리했다.
- 추가 반영: 도입과 범위 안내에서 `P7-1.1`, `P7-1.2`를 순서대로 되짚는 설명을 줄이고, 현재 절이 `기준선 구간`과 `샘플 단위` 재설계 자체로 읽히게 정리했다.
- 신규 작성: P7-1 장에 `기준선 재설계 연습` 절을 추가해, 같은 운영 로그를 서로 다른 기준선 경계일과 비교 단위로 다시 읽는 실습을 별도 절로 분리했다.
- 신규 반영: 기존 `p7-1-traffic-log.csv`를 그대로 사용하면서 `전체 합계`, `채널-일자`, `최근 4일 집중` 비교를 한 번에 읽는 Python 예제를 추가했다.
- 신규 반영: `이 절에서 직접 할 일`, `관찰 포인트`, `기록 템플릿`, `직접 더 바꿔 볼 것`을 넣어 baseline 설계가 회고 문장을 어떻게 바꾸는지 직접 쓰게 구성했다.
- 번역 동기화 메모: Future translations should preserve the distinction between whole-service summaries and channel-day baselines, especially the shift from weak overall decline to strong channel-specific signals.
