# Section Release Note

- Section ID: `P7-5.4`
- Source File: `docs/parts/part-07/chapter-05/section-04.md`

### v2026.08.19

- FLUX 승인 동작 36장과 각 review JSON, 이를 참조하던 54컷 character-LoRA manifest를 제거했다. 원고는 해당 데이터셋을 현재 LoRA 근거로 사용하지 않으며, 새 학습셋은 이미지·검수 기록·manifest를 함께 새로 승인해야 한다고 정리했다.
- P7-5.1의 `local-gpu-v5` 아트리움 자산 삭제에 맞춰, P7-5.4의 재실행 소스와 검수 기록이 가리키는 화풍 원본을 현재 승인 manifest의 Qwen Image v3 아트리움 원본으로 일괄 교체했다. 본문 서술과 승인 기준은 변경하지 않았다.

### v2026.08.18

- 원고의 Qwen 2입력 착장 누락 PNG와 역할 분리 고각도 seed `62295` PNG에 공통 생성 코드를 연결했다. 새 Python 자산은 두 입력/세 입력 역할, 고정 seed·40 step, Nunchaku FP4 r128 offload, prompt, 입력·출력 SHA-256과 새 사람 검수 경계를 함께 기록한다.
- 번역 동기화 메모: 공용 Python 자산과 한국어 원문의 코드 연결을 추가했다. 번역 원고에도 같은 코드 링크와 실행 경계 반영이 필요하다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.18`.
- 관련 자산: `p7_5_4_qwen_edit_high_angle_role_comparison.py`, `p7-5-4-qwen-edit-two-input-outfit-loss.png`, `p7-5-4-qwen-edit-high-angle-seed-62295-reference.png`.

### v2026.08.17

- 삭제한 `p7-5-4-lora-style-data/`의 11개 legacy 복사본과 `p7-5-4-lora-style-dataset-manifest.json`은 학습 입력이나 원고 근거가 아니었다. P7-5.4 style LoRA의 승인 원본은 `p7-5-1-approved-style-reference-pack.json`에 보존된다.
- 원고·데이터셋·재실행 코드에서 사용하지 않는 `p7-5-4-styled-identity-*` PNG 및 검수 JSON 36개를 제거했다. 과거 SDXL 실행 기록의 입력 경로는 실험 이력으로 남기되, 현재 재실행 입력으로 사용하지 않는다.
- 번역 동기화 메모: 공용 실험 자산·manifest 정합성만 변경했으며, 한국어 원문 본문과 번역 본문에는 반영할 서술 변경이 없다.
- 번역 반영 상태: 본문 변경 없음.
- 관련 자산: `p7-5-1-approved-style-reference-pack.json`.

### v2026.08.16

- OpenPose 비교 설명에서 승인 캐릭터에 없는 고양이 귀를 오류 항목으로 적은 표현을 제거했다. 실제 관찰한 머리 길이와 복장 이탈만 남겼다. 또한 Animagine을 guide 생성기로 일반화한 표현을 고쳐, 이 실험에서 고각도 스토리보드를 만든 뒤 그 결과를 구조용 guide로 사용한 제한된 역할만 설명했다.
- 본문에서 단순 링크로 제공하던 실행·검수 JSON 11개를 lazy-source 패널로 바꿨다. 54컷 데이터셋 manifest·준비 코드도 다른 P7-5 절과 같은 `/AiBook/assets/...` 사이트 절대 경로로 통일했다.
- OpenPose 입력 map만 나열하지 않도록 정리했다. 이 절에서 실제로 비교한 우측 쿼터 map과 ControlNet off/on 산출물을 한 검수 시트로 함께 보여 주어, 입력 구조와 결과의 연결을 바로 확인할 수 있게 했다.
- SDXL 얼굴·전신 조건, 54컷 LoRA, OpenPose, 익명 고각도 guide 전이, depth/Canny, 국소 보정, Qwen 입력 역할 분리의 각 비교 뒤에 관찰 단서·해석 범위·다음 선택을 잇는 해설을 보강했다. 부분 통과가 전체 통과나 일반화의 증거로 읽히지 않게 했다.
- 보조 조건 파이프라인 도식을 Qwen 절 앞의 보조 실험 문맥으로 옮겼다. OpenPose·depth/Canny·FaceID·IP-Adapter·LoRA·mask/VTON의 결합은 SDXL·Animagine 보조 실험 안에만 해당하며, Qwen은 고각도 guide·얼굴·완성 착장의 세 이미지 입력을 역할별로 분리한 독립 경로임을 명시했다.
- P7-5.4의 중심 학습 산출물을 실제 실패 판정에 맞췄다. Part 7 체크포인트에서 ControlNet·mask inpaint를 통과 컷의 기본 보정 도구로 예정하지 않고, 고각도 전신에서 structure·identity·outfit·style 네 계약을 함께 통과시키지 못한 비교 결과와 다음 역할 분리 경로의 선택 근거로 기록했다.
- 54컷 character LoRA 데이터셋의 stage1 동작 PNG·review JSON 72개를 제거했다. 학습셋의 기준은 stage2 사람 승인 PNG·review JSON과 manifest이며, 데이터 준비 코드는 stage1 기록 대신 그 manifest의 caption·view·pose family를 검증해 재실행한다. stage1 생성·화풍 변환 코드는 함께 제거한다.
- Part 7 단계 개요와 독자용 목차도 같은 실패 해석으로 갱신했다. P7-5.4를 통과 컷의 국소 보정 단계로 소개하지 않고, ControlNet·mask inpaint·VTON의 고각도 전신 미통과와 구조 guide·얼굴·완성 착장 역할 분리의 제한된 후속 검증으로 설명한다.
- 원고의 `주요 서사`·`보조 실험` 진행 메타 소제목을 결과와 판단이 드러나는 제목으로 교체했다. 네 계약의 쉬운 뜻과 이미지 비교를 실제 판정으로 바꾸는 세 항목 체크리스트를 추가했다.
- 54컷 LoRA lazy-source summary에 실제 파일명과 JSON·Python 유형을 표시했다.
- SDXL·Animagine XL·OpenPose·LoRA·InstantID·DiffEdit·FitDiT·CatVTON의 1차 문서·논문 출처를 보강했다. 기존 Qwen·Nunchaku·ControlNet·IP-Adapter·Diffusers 출처와 함께, 실험 도구의 기본 역할과 로컬 실행 범위를 확인할 수 있게 했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.16`.
- 54컷 character LoRA 데이터 구성에서 1단계 동작 검수 시트 이미지를 원고와 자산에서 제거했다. 데이터셋 manifest와 로컬 학습 준비 코드는 유지한다.
- styled identity 검수 시트는 원고·데이터셋·실행 경로에서 사용하지 않아 자산에서 제거했다.
- 원고에 사용하지 않는 SD 1.5 OpenPose·SDXL Canny·SDXL IP-Adapter/OpenPose 검수 시트 네 개를 자산에서 제거했다. 과거 실험 이력과 재생성 코드는 보존한다.
- 공개 원고에 쓰지 않는 재킷 operator mask 다섯 개를 제거했다. 이 mask를 입력으로 기록한 과거 review JSON은 실험 이력으로 보존한다.
- 원고·코드에서 사용하지 않는 manual-mask 및 IP-Adapter probe 시트·review JSON 22개를 제거했다.
- 원고에 쓰지 않는 오래된 FaceID 비교 시트를 자산에서 제거했다. FaceID·FullFace와 FacePlus·FaceID의 현재 비교 자산은 유지한다.
- 원고에 직접 표시하는 SDXL·FaceID·OpenPose·FitDiT 비교 이미지 11개는 `chapter-05` 최상위로 이동하고, 실행 기록·입력·manifest·LoRA 학습셋만 하위 폴더에 남겼다.
- 삭제 검토에서 유지한 P7-5.4 실행 기록·입력·검수 자산도 `chapter-05` 최상위로 이동해 하위 폴더를 해소했다. LoRA style 데이터와 54컷 character LoRA 데이터셋만 참고 자료 묶음으로 하위 폴더에 유지했다. 미사용 FaceID report는 제거했다.
- `p7-5-4-character-lora-54/actions/`의 승인 학습셋과 manifest가 참조하지 않는 루트 후보·review 자산 118개를 제거했다. 이전 후보 PNG와 그 판정 JSON은 데이터셋과 원고의 근거가 아니므로 유지하지 않는다.
- P7-5.4의 해설과 도식을 보강했다. 새 Mermaid `p7-5-4-experiment-decision-flow-ko.mmd`는 수평 기준의 계약 붕괴가 얼굴·전신 분리, 동작·camera 분리, 국소 보정 한계 확인, Qwen 세 입력 역할 분리로 이어진 판단 순서를 보여 준다. depth·Canny 뒤에는 관찰 결과·피한 해석·다음 선택을 대조하는 표를 추가해, 보조 실험의 실패를 도구 일반의 무효가 아니라 입력 역할 재배치의 근거로 읽도록 설명했다.
- 원고의 제목 계층과 문단 배치를 조정해 `주요 서사`와 `보조 실험`을 구분했다. 상단에는 수평 기준의 붕괴, 고각도 guide의 역할, Qwen 세 입력 역할 분리라는 세 주요 전환을 두고, SDXL 얼굴·전신 조건, 54컷 LoRA, OpenPose, SDXL 구조 전이, depth·Canny, mask·VTON은 각 전환의 근거를 검증하는 보조 실험으로 표지와 요약 문장을 추가했다. 보조 조건의 노드 도식도 Qwen 결과 뒤의 보조 실험 연결로 이동했다.
- 원고의 character LoRA 비교에서 `p7-5-4-faceid-contact-sheet.png`를 제외했다. FaceID 단독 probe의 오래된 시트는 서사에 사용하지 않고, 별도 FaceID·FullFace A/B와 FacePlus·FaceID 결합 비교만 유지했다.
- P7-5.4 원고를 도구 목록 중심에서 실험 서사 중심으로 재배치했다. 수평 FLUX 기준에서 고각도 계약이 깨진 문제를 출발점으로, SDXL Base 얼굴 기준선과 전신 조건의 차이, 54컷 LoRA 데이터 구성, OpenPose의 2D 동작 범위, 익명 고각도 guide의 SDXL 전이 실패, mask·VTON의 3D 가림 관계 한계, Qwen 세 입력 역할 분리의 통과 경로 순서로 정리했다. 기존 비교 시트·실행 기록·lazy source는 유지하고, 결과가 다음 실험 선택에 준 의미를 중심으로 중복 설명을 줄였다.

### v2026.08.15

- 익명 Animagine 고각도 guide를 SDXL Mira 조건으로 전이한 구조 분리 실험을 추가했다. 익명 인물 RGB·얼굴·복장을 제외하고 OpenPose와 인물 제외 배경 Canny만 전달한 `768×1152`, 50 step, seed `62431`, SDXL Base 1.0 + character LoRA `0.6` 비교에서, 조건 없음은 camera 미통과, OpenPose만은 달리기 동작 미통과, 배경 Canny만은 인체 실루엣 중복·pose 미통과로 판정했다. 두 ControlNet 결합은 현 8 GB sequential-offload Diffusers 경로에서 `768×1152`·`512×768` 모두 실행을 완료하지 못했다. 사람 외곽을 뺀 background Canny와 pose/camera 입력 분리의 체크포인트는 보존하되, 제작 도구로 승격하지 않는 결론과 시트·검수 기록을 `p7-5-4-sdxl-anonymous-high-angle-transfer/`에 정리했다.
- SDXL Base 얼굴 기준선과 full-body OpenPose probe를 원고에 추가했다. reference·ControlNet·LoRA 없는 SDXL Base 1.0의 `1024×1024`, 50 step, CFG `5.0`, seed `62295` 정면 얼굴 probe는 모델이 정상 얼굴을 형성하는지 분리한 기준선이며, Mira identity 승인 자산은 아니다. 이어 FaceID·전신 착장 image adapter를 제외하고 Plus Face `0.15`, character LoRA `0.30`, seed `62295`, CFG `5.0`, `960×1440`, 50 step을 고정한 OpenPose off/on 비교에서는 승인된 identity·재킷·바지·가방 계약을 보장하지 못했다. OpenPose on은 2D 몸통·다리 배치를 더 따르지만 머리 길이·복장 이탈도 남았다. 저해상도 `512×768`의 50/100 step 비교를 함께 보존해 step 증가는 identity·outfit 고정의 해법이 아니라는 경계를 명시했다. Base-face 자산은 `p7-5-4-sdxl-base-face-50step/`, 전신 결과와 off/on 실행 기록은 `p7-5-4-sdxl-safe-face-openpose-fullbody/`에 정리했다.
- P7-5.4 원고를 83행의 중심 서사로 정리했다. LoRA·OpenPose·depth/Canny·FaceID/IP-Adapter·mask/VTON의 세부 실행 조건과 반복 sweep은 본문에서 제거하고, 각 도구가 확인한 역할과 넘지 못한 경계, FLUX 고각도 문제, Qwen 역할 분리의 통과 경로, 최종 네 계약 검수 기준만 남겼다.
- 54컷 character-LoRA 데이터 구성 실험을 본문에 연결했다. P7-5.2 사람 승인 identity anchor 18장과 P7-5.4의 2단계 화풍 적용·사람 승인 동작 36장을 구분해 설명하고, `p7-5-4-character-lora-54/` 아래에 동작 36장의 1·2단계 PNG·review JSON, 54개 입력의 hash·caption·분류를 담은 `dataset-manifest.json`, 1단계 동작 검수 시트를 정리했다. manifest와 로컬 학습 디렉터리 준비 코드는 lazy source로 연결했다.
- OpenPose·Canny·DiffEdit 대표 비교를 최근 실험 산출물로 갱신했다. OpenPose는 character 조건과 함께 쓴 경우 관절 map을 따르면서 Mira의 핵심 외형을 남긴 부분 통과 사례가 있고, Canny는 사선 보행 frame과 일부 외형은 남기지만 흰 재킷 레이어를 잃은 사례로 정리했다. DiffEdit의 자동 mask 전신 확산 실패는 비교군으로 유지했다.
- 재사용 전신 OpenPose map 다섯 장(정면·우측 쿼터·좌우 측면·후면)을 원고에서 직접 연결하고, 그중 우측 쿼터 map으로 한 최신 54컷 LoRA 재검증을 결과로 추가했다. Animagine XL `960×1440`, 30 step, seed `62296`, LoRA scale `0.6`에서 ControlNet `0.0/1.0`을 비교해 `1.0`이 다리·발의 2D 배치와 Mira의 핵심 외형을 함께 유지한 부분 통과 사례를 확인했다. 다섯 방향 전체·3D 회전·가림 순서의 통과로 확대하지 않는 범위도 명시했다.
- 54컷 LoRA + OpenPose의 네 비교를 하나의 자산 묶음 `p7-5-4-openpose-lora-experiments/`으로 정리하고 원고에 보강했다. 저장 우측 쿼터 map의 ControlNet `0.0/1.0`, 선언형 오른팔 올리기 map의 ControlNet `0.0/1.0`, 선언형 동작의 LoRA scale `0.6/0.8`, eye-level/high-angle camera 문구 비교 시트와 실행 기록을 연결했다. 결론은 OpenPose가 2D 동작 배치를 보조하고 LoRA가 일부 외형·복장 계약을 보조하지만, scale 상승은 색 이탈을 만들며 camera 문구만으로 고각도 원근을 고정하지 못한다는 것이다.
- 표의 나머지 보조 실험에는 character LoRA on/off, FaceID probe, FacePlus+FaceID, CatVTON 전면 재킷 비교 시트를 유지했다. LoRA의 부분 보조, FaceID 계열의 일러스트 embedding 실패, VTON의 수평 재킷 레이어 부분 통과를 이미지로 확인할 수 있다. `.tmp`에만 남은 LoRA·FaceID·FacePlus 비교는 `p7-5-4-character-lora-on-off-contact-sheet.png`, `p7-5-4-faceid-contact-sheet.png`, `p7-5-4-faceplus-faceid-contact-sheet.png`로 자산화했다. 다중 reference IP-Adapter 비교 시트는 본문과 자산에서 제거했다.
- ComfyUI FaceID·FullFace A/B를 별도 비교로 소개했다. FaceID 단독은 전신 frame은 남아도 검은 장발·다른 착장으로 identity·복장 계약을 잃었고, FullFace 결합 후보는 청록 단발·호박색 눈 단서는 더 많이 남겼지만 흉상 구도로 수렴해 전신·복장 계약을 잃었다. 두 후보와 FaceID 단독 실행 기록을 `p7-5-4-faceid-fullface-experiment/`에 보존했다.
- FitDiT 고각도 상반신 좁은 mask 착장 교체를 소개했다. 고각도 원본의 카메라·하체를 보존한 채 완성 착장을 전달했지만, 결과는 재킷·어깨 레이어와 가방 strap·가방 본체를 복원하지 못했다. `768×1024`, 30 step, guidance `2.5`, seed `62431`, 약 `33.55초`의 입력·mask·착장 reference·후보·실행 기록을 `p7-5-4-fitdit-high-angle-upperbody-complete-outfit-tight-mask/`에 보존하고, 고각도 전신 착장 보정 미통과 사례로 정리했다.
- SDXL Base 1.0의 depth + 역할 분리 adapter 실험을 추가했다. depth ControlNet `0.8`, global IP-Adapter `0.30`, face IP-Adapter `0.35`, character LoRA `0.6`, outfit LoRA `0.3`을 `768×1152`, 50 step, seed `62431`에 결합했다. 고각도 타일 바닥 원근과 머리·눈 단서는 일부 남았지만 흰 크롭 재킷이 짧은 흰 상의로 바뀌고 가방·strap이 사라져, 고각도 복장·가림 계약은 미통과로 판정했다. 결과 시트·후보·실행 기록은 `p7-5-4-sdxl-depth-role-separated/`에 보존했다.
- 보조 실험의 노드 구성을 보여 주는 한국어 Mermaid 구조 다이어그램 `p7-5-4-supporting-pipeline-ko.mmd`를 추가했다. OpenPose·depth/Canny 구조 조건, FaceID/FacePlus·IP-Adapter·LoRA 캐릭터 조건, mask·VTON 국소 복장 보정의 입력·노드·출력 연결만 분리해 보이며, 실험 서사나 Qwen 전환은 도식에 넣지 않았다.
- 보조 실험 표에 character LoRA 학습, FaceID·FacePlus, 일반 IP-Adapter, CatVTON을 구분해 소개했다. LoRA는 54장 승인 데이터에서 화풍·수평 복장 보조를 확인했지만 고각도 일반화에 실패했고, FaceID는 일러스트 얼굴 embedding 차이로 탈락했으며 FacePlus·IP-Adapter·VTON도 각자 부분 역할만 확인했다. 파이프라인 차트에도 이 역할 분리를 반영했다.
- Qwen-Image-Edit-2509 3입력 역할 분리 고각도 경로를 승인 실험 자산으로 반영했다. `image 1=고각도 구도·배경·보행`, `image 2=정면 얼굴 identity`, `image 3=완성 착장`으로 역할을 분리하고, Nunchaku FP4 r128·per-layer CPU offload에서 `768×1152`, 40 step, seed `62294/62295`를 비교했다. 두 후보 모두 약 `16분 32초`, GPU 약 `3.5–3.7 GiB`로 8 GB에서 완료했고, 고각도·청록 턱선 단발·양쪽 호박색 눈·흰 크롭 재킷·청록 바지·흰 운동화·남색 가방과 재킷 바깥 strap 계약을 함께 통과했다. 고정 보행 guide 범위에서는 컷신 구성과 캐릭터 재현의 기본 경로가 8 GB Qwen에서 확인됐다.
- 원고를 실험 서사순으로 재배치했다. FLUX가 수평 시점에서는 얼굴·복장을 비교적 재현했으나 고각도에서 흔들린 것이 mask inpaint·VTON 검토의 출발점이었음을 명시했다. 고각도 스토리보드 자체가 아니라, FLUX·SDXL·Animagine이 그 스토리보드 안에서 얼굴·복장·소품을 함께 재현하지 못한 것이 병목이었다. depth·Canny edge로 카메라 시점·원근을 만들 수 있는 가능성은 확인했지만, SDXL·ControlNet·IP-Adapter 결합이 얼굴·복장·소품을 신뢰할 수준으로 재현한 범위는 수평 시점에 한정됐다는 결론도 추가했다. OpenPose는 복잡한 신체 동작을 전달하는 guide로는 통과했지만 캐릭터 재현 조건은 아니었다. 이어 mask inpaint·VTON이 새 신체 구성에 맞춰 의상 객체를 재구성하지 못한 한계, LoRA가 화풍·정면 복장에는 의미 있는 산출물을 냈지만 고각도 복장에는 미통과인 경계, OpenPose·FaceID가 복장 재현성과 경쟁할 수 있었던 SDXL·ControlNet 결합 결과를 정리한다. 마지막으로 익명 Animagine guide와 Qwen 역할 분리로 넘어간다.
- 원고에는 2입력에서 재킷·가방·strap이 빠진 결과, 3입력에서 분홍색 신발·좁아진 바지가 남은 결과, 역할 분리 뒤 두 seed 통과라는 실험 서사와 결과 4장을 추가했다. SDXL·ControlNet·mask·VTON의 미통과 경로는 실행 코드와 검수 JSON 패널을 본문에서 제거하고 이미지와 계약 판정만 남겼다. `p7-5-4-qwen-edit-high-angle-seed-62294-reference.png`, `...-seed-62295-reference.png`, `p7-5-4-qwen-edit-high-angle-reference-review.json`을 승인 실험 자산으로 등록했다. 이 결과는 고정 보행 guide 범위의 P7-5.4 기준 자산이며, P7-5.3 storyboard 대체·LoRA 학습 데이터 자동 승격·다른 pose 일반화의 근거는 아니다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.15`.

### v2026.08.10

- SDXL Depth Control-LoRA feasibility: 공식 rank-128 Depth Control-LoRA와 같은 SDXL Base 54장 character LoRA scale `0.6`에 승인된 고각도 storyboard depth scaffold를 연결해 `768×768`, 50 step, seed `62305`, condition scale `0.0/1.0`을 비교했다. sequential CPU offload에서 peak `1,445 MiB`, `151.3초`로 완료됐고, depth `1.0`은 공중 동작 실루엣과 청록 머리·흰 재킷·청록 바지를 더 강하게 따랐다. 그러나 전신 투영은 거의 정면이어서 하이앵글 camera는 만들지 못했다. 8GB 구조 보조는 통과했지만 camera adapter로는 미통과이며, 동작 실루엣 보조 후보로만 유지한다.
- PreciseCam same-base pitch sweep: SDXL Base 1.0 54장 LoRA scale `0.6`, prompt·seed `62304`·`1024×1024`·30 step·FOV `50°`·`xi=0.2`를 고정하고 PF-US pitch `25°/40°/55°/70°`을 비교했다. four-condition 실행은 peak `2,560 MiB`, `220.1초`였으며 화풍·복장은 대체로 유지됐다. 그러나 pitch가 커져도 인물 위치·머리 기울기만 조금 달라졌고 하이앵글 전신 원근과 지정 동작은 형성되지 않았다. 현 full-body text 조건에서 pitch 단독 camera gate는 미통과로 닫고 FOV sweep은 진행하지 않는다.
- PreciseCam + same-SDXL-base character-LoRA 비교: 기존에 학습해 둔 SDXL Base 1.0 54장 LoRA(`768/1152`, 1,200 step)를 찾아 PF-US pitch `55°`·`1024×1024`·30 step·seed `62304`에서 off/on(scale `0.6`)으로 다시 비교했다. sequential CPU offload에서 peak `2,560 MiB`, `90.8초`로 완료됐고, on은 절제된 화풍·청록 단발·흰 크롭 재킷·청록 바지를 더 일관되게 만들었다. 다만 하이앵글 원근은 약하고 얼굴·가방 세부도 제작 기준에 못 미쳤다. base를 맞춘 뒤 LoRA의 화풍·복장 보조는 부분 통과, camera·identity 제작 경로는 미통과로 기록했다.
- PreciseCam + character-LoRA 결합 무효 처리: PF-US pitch `55°`·`1024×1024`·30 step·seed `62304`을 고정하고 54장 Animagine XL character LoRA off/on(scale `0.6`)을 비교했다. sequential CPU offload는 peak `2,560 MiB`, `90.7초`로 끝났지만, on 후보는 모자·과장 재킷·새 소품·신체 붕괴를 만들었다. Animagine XL 기반 LoRA를 SDXL Base 기반 PreciseCam에 결합한 base model 불일치이므로 LoRA의 하이앵글 성능 판정에서 제외한다.
- PreciseCam text-only 하이앵글 feasibility: Blender·SMPL·RGB reference 없이 prompt·seed `62303`·`1024×1024`·30 step을 고정하고 PF-US pitch `0°/55°`만 비교했다. 공식 model CPU offload는 UNet 이동에서 20 MiB 부족 OOM이었지만, sequential CPU offload는 peak `2,560 MiB`, `86.7초`로 완료됐다. `55°`는 시점·건축 원근을 바꿔 camera condition은 부분 통과했으나, 공개 모델 단독은 전신·Mira 얼굴·승인 복장·가방 계약을 유지하지 못했다. 캐릭터 LoRA와의 결합 전까지 제작 자산으로 승인하지 않는다.
- 선언형 OpenPose 하이앵글 비교: 오른팔 올리기 map·Animagine XL `960×1440`·30 step·seed `62302`·54장 LoRA scale `0.6`·ControlNet `1.0`을 고정하고 eye-level/high-angle camera 문구만 비교했다. 두 후보 모두 복장 계약은 유지했으나 high-angle 후보도 거의 정면 전신 구도에 머물러, OpenPose와 camera 문구만으로는 위에서 내려다보는 원근을 고정하지 못했다. camera-specific 구조 조건이 별도 gate임을 기록하고 Canny는 마지막 보정 후보로 유지한다.
- 원고 결과 요약 갱신: 54장 고해상도 Animagine character-LoRA의 학습 조건과 LoRA off/on, 저장 OpenPose map의 ControlNet `0.0/1.0`, 선언형 오른팔 올리기 map의 LoRA off/on, LoRA scale `0.6/0.8`을 한 표로 정리했다. LoRA는 얼굴 동일성의 단독 고정에는 약하지만 화풍·재킷·상의·바지·가방의 복장 계약을 보조한다는 판정을 명시했다. 3D 회전·가림·색 계약의 남은 gate와 Canny·inpaint·VTON을 마지막 보정 후보로만 두는 경계를 본문에 반영했다.
- 선언형 동작 LoRA scale 비교: 오른팔 올리기 OpenPose map·ControlNet `1.0`·seed `62301`·`960×1440`·30 step을 고정해 LoRA scale `0.6/0.8`을 비교했다. 두 후보 모두 동작·재킷·가방은 유지했으나 `0.8`은 와이드 바지를 거의 흰색으로 과도하게 끌어가 색 계약이 더 악화됐다. ControlNet 결합의 기준은 `0.6`으로 유지하고 scale 상승 sweep은 중단한다.
- 선언형 OpenPose 동작 map 비교: 기존 인물 PNG를 쓰지 않고 “오른팔을 위로 뻗고 양발로 선 자세”의 18개 관절 좌표를 `960×1440` map으로 고정했다. 새 54장 LoRA scale `0.6`, 30 step, seed `62301`에서 ControlNet `0.0/1.0`을 비교한 결과, `1.0`은 팔을 위로 올린 2D 동작과 청록 단발·호박색 눈·흰 크롭 재킷·가방·두 다리를 함께 유지했다. `0.0`은 팔 방향과 재킷 레이어가 함께 흔들렸다. 바지 색은 옅어졌으므로 색·소품 세부와 3D camera는 별도 gate로 남긴다. image reference·Canny·Inpaint·VTON은 사용하지 않았다.
- 새 character-LoRA + 정적 OpenPose 재검증: 새 54장 LoRA와 저장된 우측 쿼터 OpenPose map만 결합하고 image reference·Canny·Inpaint·VTON을 제외했다. Animagine XL `960×1440`, 30 step, seed `62296`, LoRA scale `0.6`에서 ControlNet scale `0.0/1.0`을 비교한 결과, `1.0`은 다리·발의 2D 배치를 더 강하게 따르면서 청록 단발·호박색 눈·흰 재킷·와이드 바지·가방을 유지했다(peak `3,371 MiB`). 머리·흉곽의 정확한 회전과 가림 순서는 고정하지 못하므로 2D pose 부분 통과로만 기록한다.
- 54장 고해상도 Animagine XL character-LoRA 성능시험: 승인 화풍 적용 54장(18 identity anchor·36 action), 고유 caption을 square `640×640`·full-body `640×960` bucket, rank/alpha `8`, bf16, learning rate `1e-4`, 1,200 step으로 학습했다. `768/1152`와 일반 `640/960` 역전파는 8 GB에서 OOM이었으나 UNet gradient checkpointing을 추가한 `640/960` 경로는 peak `5,314 MiB`에서 완료했다. 같은 Animagine XL, `960×1440`, 30 step, seed `62295/62296`, scale `0.6`의 LoRA off/on 비교에서 on은 청록 단발·호박색 눈·흰 크롭 재킷·와이드 바지·네이비 가방을 함께 유지했다. 보행 변형은 identity·복장에는 부분 통과했지만 동작은 정적인 쿼터 포즈로 수렴해 별도 pose gate가 남는다. Canny·Inpaint·VTON은 사용하지 않았다.
- LoRA 학습 기반 일치 재검증: 앞선 일부 비교는 SDXL Base 1.0으로 학습한 character LoRA를 Animagine XL 4.0 추론에 연결했으므로 LoRA 자체 성능의 확정 근거에서 제외했다. 같은 Animagine XL 4.0 기반의 tagged 12장 LoRA와 P7-5.2 turnaround/full-body LoRA를 image reference·ControlNet·Inpaint·VTON 없이 `960×1440`, 30 step, scale `0.6`, seed `62295/62296`에서 비교했다. 정면에서는 청록 단발·호박색 눈·기본 복장이 식별됐지만, 우측 3/4에서 바지색 변형과 신발 겹침이 남아 제작 gate는 미통과다. 학습 기록 재검수 결과 turnaround LoRA는 11장·300 step·`384×512`와 동일 caption의 feasibility pilot, tagged LoRA는 12장·300 step·`320×448` identity pilot, 54장 action LoRA는 `320×448`·600 step(이미지당 약 11회 노출)이다. 따라서 현재 Animagine XL LoRA에는 제작 기준선이 될 만큼 충분히 학습된 adapter가 없으며, 방향·복장·동작을 구분한 caption과 고해상도 bucket 학습을 새로 설계한다. Canny·Inpaint·VTON은 이 학습 검증 경로에서 제외한다.
- 캐릭터 LoRA 후보 생성 이관·확대: P7-5.2의 승인 방향 얼굴 6장·기본 전신 6장·리파인 전신 6장은 재생성하지 않고 직접 입력으로 유지한다. 당시 P7-5.4 후보 생성 경로는 방향 얼굴의 identity, 기본 전신의 비례·복장, P7-5.1 승인 배경의 절제된 웹툰·투명 수채화 화풍을 함께 참조해 스포츠 후보를 36장으로 확대했다. 사람 검수를 통과한 32–40장을 1차 학습 세트로 고른다. 기본 canvas는 `1024×1024`, seed `62294`, seed 증분 `0`, 기본 6 step이며, 팔·손·다리·발이 각각 정확히 둘이 아니거나 화풍 계약이 어긋나면 즉시 탈락한다. 후보 PNG와 review JSON은 사람 승인 전까지 학습 입력이 아니다.
- 전신 비율 공용 계약: 동작 후보가 얼굴·몸통·골반·다리 비율을 임의로 바꾸지 않도록 P7-5.2 공용 JSON에 약 여덟 등신, 작은 머리·짧은 몸통·높은 허리·균형 잡힌 골반·긴 다리·자연스러운 관절 크기 계약을 추가했다. P7-5.4 후보 생성기는 이 JSON 파일명을 review 기록에 남기고 모든 prompt에 적용한다.
- 배경 분리: 캐릭터 LoRA 후보의 장소 이름은 pose 분류와 화풍 참조 선택에만 남기고, 모든 출력은 균일한 오프화이트 배경의 고립된 인물로 생성하도록 바꿨다. 코트·바닥·건물·수평선 환경은 prompt에서 금지한다. FLUX RGB 출력에는 투명 알파를 만들지 않는다.
- LoRA 본시험·베이스 비교: 사람 승인한 P7-5.1 배경 16장과 held-out 4장을 caption·SHA-256·승인 기록으로 고정했다. Animagine XL 4.0의 300-step 본시험은 `217.19초`, peak `6,043 MiB`로 실행됐지만, 자연어 caption·표준 bf16 파이프라인 비교가 해당 모델의 tag prompt·fp16·Euler Ancestral 권장 조건을 따르지 않아 checkpoint 선택 근거에서 제외했다. 권장 조건의 100-step 재파일럿은 장면은 유지했으나 승인 화풍의 뚜렷한 이득이 없어 미통과로 기록했다. SDXL base 1.0에서는 rank/alpha `4`, learning rate `5e-5`, 100 step, bf16 학습·fp16 추론을 실행해 `74.41초`, peak `5,997 MiB`로 완료했다. held-out 네 장소와 두 seed의 scale `0.5` 비교에서 장소 범주와 수채화 질감 강화는 확인했지만 일부 구도 변화가 있어 사람 검수 대기이며, adapter를 제작 자산·VTON 입력으로 승인하지 않았다. 관련 자산: `p7-5-4-lora-style-trial-plan.json`.
- LoRA 시험 수립·환경 pilot: 의상·pose·Mira identity를 넣지 않는 P7-5.1 배경 화풍 전용 LoRA를 독립 실험으로 정했다. 데이터 분리 전의 승인 배경 9장 반복 pilot에서 Animagine XL 4.0, `384 x 512`, bf16, UNet attention rank/alpha `8`, batch 1을 사용했다. UNet과 text encoder 동시 상주는 OOM이었지만, frozen text/VAE 출력을 CPU에 사전계산한 재시도는 50 step·`12.54초`, peak `6,021 MiB`에서 adapter 저장까지 통과했다. 이 기록은 메모리·실행 경로만 뜻하며, 이후 16/4 분리 데이터 본시험과 분리한다.
- 비교 구조 정리: 의상 전달과 장면 재현을 하나의 성능으로 일반화하지 않도록, 수동 inpaint + 일반 IP-Adapter, ControlNet + 일반 IP-Adapter, VTON, LoRA를 입력·현재 고정 시험 결과·판단 경계로 나란히 정리했다. CatVTON은 의상 레이어 전달의 사람 검수 후보, FASHN VTON v1.5는 8 GB 첫 실행 통과지만 open-front·안쪽 crop-top 레이어 미통과, generic adapter 조합은 현 조건의 제작 gate 미통과로 기록했다. LoRA는 위의 독립 화풍 시험 결과만 반영하며, 의상·pose·Mira identity 문제의 해법으로 일반화하지 않았다.
- 실험 순서 보완: 8 GB 표에 person·garment 계약, mask 또는 parser, 의상·canvas gate를 기록하는 VTON 실행 계약을 LoRA 최소 학습 전에 추가했다. VTON과 LoRA는 단독 gate 통과 전 결합하지 않는다는 경계를 본문과 출처에 반영했다.
- DiffEdit 첫 8 GB 실행 결과: P7-5.2 승인 전신 정면을 제작 컷이 아닌 고정 시험 입력으로 사용해, cropped white jacket 편집의 자동 mask를 실제 실행했다. SD 1.5 base만으로 `512 x 768`, 20 step, mask map 4개, seed `5404`에서 `20.6초`, peak `2,723 MiB`로 실행은 완료했다. 그러나 mask가 머리·얼굴·바지·신발·바닥까지 번지고 출력도 요청한 재킷과 인물·신발 보존에 실패해 자동 mask baseline은 미통과로 기록했다. contact sheet, 검수 JSON, 재실행 Python을 추가했으며 제작 자산에는 승인하지 않았다.
- DiffEdit 반복 종료: 공통 prompt 압축, mask encode strength `0.2`, mask threshold ratio `8.0`·`5.0`, 단일 의상 목표를 차례로 비교했다. mask 확산을 줄인 설정은 재킷까지 놓쳤고, 완화한 설정은 얼굴·하체·신발로 다시 퍼지며 상의·허리띠 artifact를 만들었다. 세 설정 모두 미통과여서 prompt·step sweep을 중단하고, 반복 검수 JSON과 마지막 contact sheet를 추가했다.
- 다음 실험 준비: 사람의 black/white mask와 승인 full-frame을 필수로 받는 SDXL inpaint 대조 스크립트를 추가했다. checkpoint 자동 다운로드를 막는 offline 기본값과 명시적 `--allow-download`를 두었고, 준비 시점에는 checkpoint와 ComfyUI가 없어 Diffusers 단일 경로만 먼저 실행하도록 정했다.
- 수동 mask SDXL 첫 preflight 정정: 첫 실행은 `width`·`height`를 호출에 전달하지 않아 SDXL 기본 canvas가 섞였으므로, 잘린 출력은 mask 품질 실패가 아니라 출력 canvas 계약 누락으로 판정했다. 첫 다운로드·로딩 포함 `276.4초`, peak `3,292 MiB`의 8 GB 실행 기록은 유지한다.
- 수동 mask SDXL 수정 canvas 비교: 목표 `512 x 768`과 mask 밖 source 복원을 명시해 재실행했다. `strength 0.4`/20 step은 full-frame 보존에 통과했으나 재킷 편집이 거의 없었고, `strength 0.8`/30 step은 회색 긴소매 상의로 바뀌어 open cropped white denim jacket 요청에 실패했다. 두 결과 모두 제작 경로 미통과로 기록하고 contact sheet·mask·검수 JSON을 추가했다.
- 수동 mask SDXL 개선 10회 반복 종료: wide/narrow mask, strength `0.55`~`0.85`, CFG `10`·`12`·`15`, seed `5501`·`62294`·`62382`, prompt 길이를 한 변수씩 바꿔 10개 조건을 비교했다. 전신·mask 밖 보존은 모두 통과했고 peak VRAM은 `1,785`~`2,185 MiB`, 실행 시간은 `13.4`~`19.3초`였지만 흰 open-front cropped jacket 세 조건을 함께 충족한 후보는 없었다. prompt·CFG·strength·seed·coarse mask sweep은 중단하고, 의상 reference conditioning 또는 다른 inpaint 모델 경로를 다음 가설로 남겼다.
- 의상 reference IP-Adapter 첫 실행: 승인한 전면 jacket-crop-top 참조와 일반 `ip-adapter_sdxl.bin`을 수동 mask SDXL inpaint에 추가했다. Plus adapter와 attention slicing의 processor 충돌을 분리해 slicing 없이 실행했고, `512 x 768`, 30 step, scale `0.55`, seed `62294`에서 `20.1초`, peak `1,885 MiB`로 완료했다. 흰 포켓 flap·소매 끝은 부분 전달됐지만 재킷 몸판이 회색으로 남아 제작 후보는 미통과다. contact sheet·검수 JSON·재실행 Python을 추가했다.
- 의상 reference IP-Adapter scale 비교 종료: 같은 입력·seed에서 scale을 `0.85`로 올려 `19.7초`, peak `2,085 MiB`로 실행했다. 포켓 flap·소매 끝만 흰색인 부분 전달 패턴이 유지됐고 몸판 색·open-front 계약은 개선되지 않았다. scale `0.55`→`0.85` sweep을 중단하고, 새 conditioning 또는 다른 inpaint 모델 경로를 다음 가설로 남겼다.
- 정밀 jacket-shell mask 비교: 중앙 charcoal crop top을 black 보존 영역으로 남기고 칼라·좌우 패널·긴소매만 white로 둔 목표 실루엣 mask를 적용했다. 같은 일반 IP-Adapter scale `0.55`, seed `62294`에서 `20.1초`, peak `1,646 MiB`로 open-front·크롭 레이어·긴소매 구조는 회복했다. 재킷 몸판은 여전히 회색이므로 mask 구조는 부분 통과, 흰 fabric color 계약과 제작 후보는 미통과로 기록했다.
- fitted-shell mask 비교 종료: 원본의 어깨·팔·손목·크롭탑 opening·높은 밑단을 더 촘촘히 따른 raster mask로 다시 실행했다. `18.6초`, peak `1,991 MiB`에서 경계 부자연스러움은 줄었으나 재킷 패널은 계속 회색이었다. 다각형·경계 정밀화의 반복은 중단하고, 남은 색·재질 conditioning을 다음 가설로 분리했다.
- fitted-shell border 확장 비교: 같은 mask의 white 편집 영역을 원본 해상도 기준 `16px` 바깥으로 확장했다. `25.8초`, peak `1,608 MiB`에서 버튼·연속 몸판은 더 잘 형성됐지만 회색의 닫힌 크롭 재킷이 되어 흰 fabric color·open-front 계약은 모두 미통과였다. 경계 여유만 더 키우는 반복을 중단하고 색 conditioning 문제로 분리했다.
- 분리된 흰 재킷 참조 비교: 레이어 참조에서 회색 crop top을 제거하고 흰 재킷만 있는 승인 소품을 일반 IP-Adapter 입력으로 사용했다. `19.2초`, peak `1,814 MiB`에서 흰 포켓·소매 끝은 남았지만 몸판은 계속 회색이었다. 회색 top이 참조에 포함된 것이 원인이라는 가설을 기각하고, generic IP-Adapter의 white detail 전달과 white body 색 강제 한계를 기록했다.
- padding-mask-crop 국소 해상도 비교: SDXL inpaint의 `1024 x 1024` 학습 해상도와 Diffusers의 국소 crop 안내를 근거로 fitted-shell에 padding `64`를 적용했다. `26.1초`, peak `2,206 MiB`로 실행됐지만 회색 몸판은 유지되고 소매는 흰 짧은 소매가 되며 open-front도 사라졌다. 국소 해상도 부족만이 원인이라는 가설을 기각하고 padding crop sweep을 종료했다.
- generic IP-Adapter 10조건 탐색 종료: fitted-shell mask·분리된 흰 재킷 참조·seed `62294`를 고정하고 crop padding `32`·`96`, mask border `4px`·`8px`, adapter scale `0.30`·`1.00`, strength `0.60`·`0.85`, CFG `7`을 각각 한 변수씩 비교했다. `16.1`~`23.7초`, peak `1,661`~`2,323 MiB`에서 full-frame 보존은 모두 통과했지만, 열 후보 모두 회색 몸판 또는 짧은 소매에 머물러 흰 몸판·긴 cuffed 소매·open-front 계약을 만족하지 못했다. 이 경로의 미세 sweep을 종료하고 다른 model 또는 conditioning 비교로 전환한다.
- CatVTON virtual try-on 비교: 범용 image prompt 대신 person·garment·operator mask를 받는 CatVTON을 `768 x 1024`, bf16, 50 step, seed `62294`에서 실행했다. 전면 jacket-crop-top 레이어 참조와 jacket-shell mask를 결합하고 border를 `16px` 확장한 guidance `3.5` 조건은 `46.7초`, peak `5,443 MiB`에서 흰 cropped jacket 몸판·긴 소매·open-front·보존된 charcoal crop top을 함께 만들었다. `8px` 확장은 검은 칼라 잔존으로 탈락했다. 최선 출력은 사람 검수 후보이며 제작 자산으로 자동 승인하지 않았다.
- CatVTON 조건 분리: jacket-shell `16px`에서 guidance `2.5`·`3.5`는 기본 jacket gate를 함께 통과했지만 목선·대칭·원단 질감에 뚜렷한 차이가 없어 sweep을 종료했다. 재킷 단독 참조는 회갈색 몸판과 깨진 open front로 탈락했다. 전면 jacket-crop-top 레이어 참조를 유지한 fitted-shell `0px`은 어깨·소매가 어둡게 남아 탈락했으며, fitted-shell `16px`은 `46.2초`, peak `5,451 MiB`에서 흰 cropped jacket·긴소매·open front를 함께 유지했다. 이 출력은 사람 검수 후보로만 기록했다.
- CatVTON 출력 종횡비 계약 확인: `768 x 1152`를 요청하면 source·mask·garment는 2:3으로 준비되지만 candidate가 `768 x 1024`로 반환됐다. `67.9초`, peak `5,447 MiB`의 이 출력은 2:3 비율 후보가 아니므로 탈락 처리했다. 이후 실행기는 요청·반환 해상도가 다르면 저장 전에 오류를 낸다.
- 8 GB 실험 순서 추가: 제작 컷의 승인을 앞당기지 않는 고정 시험 이미지에서 실행 환경→SDXL inpaint 실행기→저정밀 추론→SD 1.5 최소 LoRA→LoRA와 수동·DiffEdit mask 결합→승인 제작 컷 적용 순으로 검증하도록 본문을 추가했다. 각 단계의 OOM, VRAM·RAM·SSD, 시간, mask 경계, identity·style gate를 다음 단계 조건으로 분리했다.
- 대안 경로 정리: ComfyUI Dynamic VRAM, Diffusers FP8/4-bit, CUDA Unified Memory의 역할과 한계를 추가했다. SD 3.5 Medium, T2I-Adapter, StyleAligned는 현 8 GB 기준선이 아니라 후속 비교 후보로 보류했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.10`.
- LoRA 전환 조건을 P7-5.3에서 이동: FLUX.2 Klein 4B Base와 distilled 추론 adapter의 관계, 공식 예제의 15–40장 스타일 데이터·약 24 GB VRAM 조건, 8 GB 환경에서의 보류 판단, edit LoRA의 입력/목표 쌍 계약을 후속 identity·style 보정 절인 P7-5.4로 옮겼다. 관련 공식 출처도 함께 이동했다.
- P7-5.3 승인 상태 동기화: 구조·캐릭터 정보 수용을 승인한 A/B/C 결과는 존재하지만 공간·조명·그림자까지 통과한 완성 컷은 없다는 현재 경계를 도입부와 입력 계약에 반영했다.

### v2026.08.07

- P7-5.3 경로 폐기 동기화: Animagine 스토리보드와 이를 입력으로 쓴 캐릭터 컷의 승인 경로가 폐기되어, 현재 P7-5.3에는 승인 장면·guide·전체 컷이 없음을 반영했다. P7-5.4의 ControlNet·inpaint는 새 FLUX 스토리보드가 인체·가림·접지 검수를 통과한 뒤에만 시작한다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.07`.

### v2026.08.06

- 생성 probe·review JSON은 로컬 실행 기록으로 전환하고 저장소에서 제거했다. 원고에는 검증 결론과 재실행 가능한 코드만 남긴다.
- 링크 정리: 삭제된 P7-5.3 manifest 검사기·출력 링크를 제거하고, 현재의 텍스트 스토리보드와 구조 guide 생성 코드 및 사람 검수 gate로 연결했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.06`.

### v2026.08.03

- 구조 재배치: 이 Section을 `화풍·연속성 보정` 단계로 명시했다. 컷신 생성의 실패를 inpaint로 숨기지 않으며, full-frame gate를 통과한 컷에만 구조 조건과 국소 보정을 적용한다.
- 변경 이유: identity 참조가 pose·camera·장면 변화를 막는지 판단하려면, 먼저 구조 제어만 독립적으로 실제 검증해야 했다.
- 실행 결과: 캐시된 `control_v11p_sd15_openpose`와 표준 SD 1.5로 네 held-out 장면의 OpenPose map을 만들고, 같은 prompt·seed에서 ControlNet scale `0.0`·`1.0`을 비교했다. scale `1.0`은 팔·몸통·다리 방향과 주방·난간·영화관·작업대의 큰 구조를 off보다 더 따랐고 peak VRAM 약 `3,211 MiB`로 실행됐다.
- 판정: identity 입력을 넣지 않았으므로 얼굴·머리·의상·가방은 Mira와 일치하지 않는다. 이 결과는 structure 부분 통과일 뿐 최종 웹툰 컷이나 character consistency 통과가 아니다.
- 호환성 확인: WD 1.5 base는 text context `1024`, 이 OpenPose ControlNet은 `768`을 사용해 직접 결합할 수 없었다. WD와 이 ControlNet의 결합 경로를 제외했다.
- 실행 결과: 캐시된 Animagine XL, SDXL IP-Adapter, SDXL OpenPose ControlNet을 새 Mira 기준과 네 pose map으로 결합했다. `512 x 768`은 model CPU offload에서 OOM이었지만 sequential CPU offload로 15 step을 완료했다. IP-Adapter on은 큰 pose·장면 구조를 유지한 채 청록 단발·흰 재킷·청록 바지·가방을 더 자주 남겼다. 얼굴·가방 geometry·손·일부 camera와 장소는 미통과이므로 combined-condition 부분 통과로만 기록한다.
- 검수 결과: 결합 출력의 IP-Adapter on 네 panel을 identity·structure·style·local detail로 분리해 검토했다. 네 panel 모두 full-frame identity 또는 structure가 `partial`/`fail`이라 inpaint 가능 panel은 `0`개다. 얼굴·가방·손 보정은 전체 frame 통과 뒤에만 허용하도록 review ledger와 checker를 추가했다.
- 실행 결과: 정면·3/4·얼굴·가방 detail의 다섯 승인 reference를 한 IP-Adapter 입력 묶음으로 비교했지만 얼굴·가방 geometry는 안정화되지 않았고 일부 출력은 옅어졌다. reference 수 증가는 단일 reference 실패의 해결책이 아니므로 이 경로도 품질 미통과로 기록했다.
- 교체 검증: SDXL Plus와 Plus Face를 ViT-H 인코더와 별도 adapter slot으로 실제 실행했다. 두 조합 모두 Animagine XL·SDXL OpenPose·`512 x 768`·15 step·sequential CPU offload에서 생성돼 모델 계열과 8 GB 실행 경로는 호환됐다. 그러나 Plus 단독은 가방·색 일관성을 충분히 개선하지 못했고 Plus Face는 잘못된 옆얼굴 세부와 배경 약화를 만들었다. PNG는 품질 미통과로 제거하고 실행 코드와 로컬 판정 기록으로만 남겼다.
- 카메라 구조 검증: 캐시된 SDXL Canny ControlNet과 기존 범용 IP-Adapter를 저각도 cinema 컷에 결합했다. Canny scale `0.75`는 몸의 굽힘 방향과 foyer 원근을 off보다 더 따랐지만 얼굴·가방·손·전신 비례는 모두 품질 미통과였다. Canny는 camera·silhouette 보조 조건으로만 부분 통과이며 최종 웹툰 컷 후보가 아니다.
- 구조 단독 검증: IP-Adapter, LoRA, inpaint, source RGB를 모두 제외하고 옆면 turnaround의 Canny edge만 SDXL Canny ControlNet에 전달했다. 같은 seed에서 scale `0.75`는 `0.0`보다 side profile, head-neck-shoulder 방향, 전신 frame, 가방과 손의 큰 상대 위치를 더 따랐다. `512 x 768`, 15 step, sequential CPU offload에서 `33.5초`, peak VRAM `1,733MiB`였다. 이는 camera·silhouette structure 통과만 뜻하며 identity·style 통과가 아니다.
- 관련 자산 추가: `p7_5_4_sdxl_canny_structure_only_probe.py`, `p7-5-4-sdxl-canny-structure-only-contact-sheet.png`.
- identity-anchor 결합 검증: 같은 Canny structure 조건에 IP-Adapter scale `0.35`를 추가했다. 정면/3-4 reference와 angle-matched side reference 모두 side profile·전신 frame은 남겼지만, hair가 옅은 흰색으로 변하고 face·가방 geometry를 복원하지 못했다. 두 결과 모두 full-frame identity 실패이므로 inpaint, scale sweep, reference 추가, 두 번째 ControlNet으로 확장하지 않는다. PNG와 임시 script는 제거하고 판정은 로컬 기록으로만 보관한다.
- 관련 자산: `p7_5_4_sd15_openpose_structure_probe.py`, `p7-5-4-sd15-openpose-controlnet-on-off-contact-sheet.png`.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 원문 기준 버전: `v2026.08.03`

### v2026.08.02

- 변경 이유: 직접 LoRA 학습 소개를 웹툰 컷의 최종 조합 검증과 분리했다.
- 본문 반영: SD 1.5 character LoRA, ControlNet 하나, mask inpaint를 네 컷의 identity·structure·style·local detail 판정으로 재구성하고, 승인 자산을 검사하는 Python 실행 경로를 연결했다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 관련 자산: `p7_5_3_controlnet_pipeline_check.py`, `p7-5-3-controlnet-pipeline-manifest.json`.
- 원문 기준 버전: `v2026.08.02`

### v2026.07.31

- 변경 이유: Stable Diffusion과 LoRA 조합 실습이 한 Section 안에 과도하게 압축되어 있어, kohya_ss로 직접 LoRA를 학습하는 확장 실습을 독립 Section으로 분리했다.
- 본문 반영: 직접 LoRA 학습 전 정해야 할 학습 목표, 이미지 수, caption 규칙, 반복 횟수, sample prompt, 검토 기준을 표로 정리하고, 실패 신호를 다음 데이터셋 수정으로 연결하는 기록 양식을 추가했다.
- 가방·스트랩 conditioning 비교: source-aligned 가방+스트랩 mask, `768×1024`, CatVTON 30 step/guidance `2.5`, seed `62294`를 고정하고 승인 가방 단독 참조와 전면 착장 참조를 비교했다. 두 조건 모두 갈색 둥근 덩어리와 스트랩 소실, 얼굴 보존 실패가 발생해 탈락했다. 동일 CatVTON mask/seed 반복은 중단하고 가방 전용 inpaint 또는 다른 conditioning 경로를 다음 실험으로 분리한다. 상세 review JSON은 로컬 `.tmp/p7-5-4-bag-conditioning-comparison-review.json`에 보관한다.
- SDXL 국소 inpaint 대안: 같은 source-aligned mask와 seed `62294`로 SDXL inpainting `768×1024`, 30 step, strength `0.65`, guidance `7.0`을 실행했다. 얼굴·복장·대각선 스트랩은 보존됐지만 가방 본체가 승인 네이비 플랩형과 달라 조건부 통과로 기록했다. CatVTON보다 유망한 체크포인트이지만 PNG는 제작 승인·학습 입력으로 승격하지 않는다. 상세 판정은 `.tmp/p7-5-4-sdxl-bag-inpaint-review.json`에 보관한다.
- 실험 순서 정정: Inpaint와 VTON은 기본 생성·reference·LoRA·pose/camera 구조 제어를 먼저 검증한 뒤에도 남는 국소 결함에만 적용하는 마지막 보정 수단으로 둔다. 이번 CatVTON/SDXL 결과는 이 순서를 결정하기 위한 체크포인트이며 기본 제작 경로로 승격하지 않는다.
- LoRA 단독 기준 실험: 이미지 reference·ControlNet·Inpaint·VTON 없이 Animagine XL 4.0과 캐릭터 LoRA만 `512×768`, 30 step, scale `0.6`, seed `62295/62296`으로 실행했다. 정면은 identity·색상 경향이 부분 보존됐으나 3/4에서 화풍·복장·가방이 흔들렸다. LoRA는 identity/style 보조 조건으로만 기록하고 다음은 pose/camera 구조 제어와의 분리 비교로 진행한다.
- LoRA 해상도 체크포인트: 동일 조건에서 출력만 `960×1440`으로 올렸다. 저해상도보다 전신 구조·옷 경계·가방/스트랩 배치가 안정됐지만 3/4 머리색과 화풍·복장 편차는 남았다. 이후 실험의 기본 해상도를 `960×1440`으로 정하고 identity/style conditioning을 먼저 분리 검증한다. 상세 판정은 `.tmp/p7-5-4-lora-only-960x1440-review.json`에 보관한다.
- LoRA step 체크포인트: 같은 `960×1440`, LoRA scale `0.6`, seed `62295/62296`에서 30 step과 60 step을 비교했다. 60 step은 선·복장 경계를 약간 개선했지만 3/4의 머리색·화풍·복장 편차는 유지됐다. step 증가는 detail 개선이지 identity 고정 해결책이 아니므로, 다음은 identity/style conditioning을 먼저 검증한다. 상세 판정은 `.tmp/p7-5-4-lora-only-960x1440-steps60-review.json`에 보관한다.
- Face Adapter + LoRA 체크포인트: `960×1440`, 30 step, Face Adapter scale `0.35`, LoRA scale `0.6`, seed `62295/62296`으로 정면 얼굴 reference를 추가했다. 단발·호박색 눈·얼굴 identity는 LoRA 단독보다 안정됐지만 재킷·바지 실루엣이 바뀌어 복장 gate에서 탈락했다. identity와 outfit/reference 역할을 분리해야 하며, 후보는 승인·학습 입력으로 승격하지 않는다. 상세 판정은 `.tmp/p7-5-4-face-adapter-lora-960x1440-review.json`에 보관한다.
- Face + outfit reference + LoRA 체크포인트: dual IP-Adapter에 승인 전면 착장 reference를 추가했다. 정면의 재킷·가방·스트랩은 개선됐지만 바지 길이가 짧아졌고, 우측 3/4에서는 바지·다리 형태가 붕괴했다. 복장 reference는 방향 일반화에 실패했으므로 다음 pose/camera 구조 제어와 분리 비교한다. 상세 판정은 `.tmp/p7-5-4-face-outfit-adapter-lora-960x1440-review.json`에 보관한다.
- OpenPose 구조 제어 정정·체크포인트: 첫 비교는 skeleton map 대신 인물 RGB를 ControlNet 입력으로 넘긴 오류가 있어 무효 처리했다. 실제 OpenPose skeleton map으로 같은 `960×1440` dual reference+LoRA 조건에서 scale `0.8/1.0`을 비교한 결과, `1.0`은 다리·몸통의 2D 배치를 강화했지만 목표 우측 3/4 카메라 회전은 고정하지 못했다. OpenPose는 2D body layout 보조 조건으로만 부분 통과이며 camera/rotation 조건의 대체물이 아니다. 상세 판정은 `.tmp/p7-5-4-true-openpose-scale-ab-review.json`에 보관한다.
- 우측 3/4 얼굴 reference 비교: 얼굴 reference만 우측 3/4 자산으로 교체하자 카메라는 여전히 정면에 가깝고, 가방 이탈과 스트랩 단절이 발생했다. 방향별 얼굴 reference는 구조 제어의 대체물이 아니므로, 다음은 얼굴 reference를 고정한 OpenPose scale 분리 비교로 진행한다. 상세 판정은 `.tmp/p7-5-4-face-right-quarter-outfit-lora-openpose-960x1440-review.json`에 보관한다.
- 우측 3/4 Canny camera 체크포인트: 전신 기준 이미지의 Canny edge는 우측 3/4 걷기 방향과 전신 비율을 가장 잘 전달했다. 반면 흰 재킷이 사라지고 한쪽 눈이 무너져 appearance·identity는 실패했다. Canny는 camera/pose 조건으로만 통과하며 appearance 조건과 경쟁한다. 상세 판정은 `.tmp/p7-5-4-face-outfit-lora-canny-960x1440-review.json`에 보관한다.
- Canny 적용 범위 정정: Canny는 기준 이미지에 이미 존재하는 외곽·가림 관계를 전달할 뿐, 다양한 새 pose를 지정하는 조건이 아니다. 팔·다리·가방처럼 신체가 가려지는 동작에서는 edge 부족·충돌로 형태와 가림 순서를 고정하지 못했으므로, Inpaint·VTON과 같이 기본 생성·reference·LoRA·구조 조건 검증 뒤에만 쓰는 camera·silhouette 최후 보정 후보로 제한한다.
- Canny Face Adapter scale 비교: Face Adapter만 `0.35→0.50`으로 올렸지만 한쪽 눈 붕괴·흰 재킷 소실은 회복되지 않았고 camera·걷기 구조만 유지됐다. 이 결합에서 Face Adapter scale은 identity·복장 실패의 해결 변수가 아니므로 추가 sweep은 중단한다. 상세 판정은 `.tmp/p7-5-4-face-outfit-lora-canny-960x1440-face50-review.json`에 보관한다.
- 재사용 OpenPose map 자산: 승인 전신 기준의 정면·우측 3/4·좌우 측면·후면 skeleton map을 `p7-5-4-openpose-...-reference.png` 이름으로 저장하고 생성기를 추가했다. 이후 OpenPose 실험은 detector를 다시 실행하지 않고 이 정적 자산을 입력으로 사용한다.
- 추가 반영: 직접 LoRA 학습의 기준 경로는 `kohya_ss`로 유지하되, 로컬 GPU 환경이 어려운 독자를 위한 보조 경로로 `hollowstrawberry/kohya-colab`을 언급했다.
- 추가 반영: 2026-07-31 기준 GitHub stars/forks 규모와 `kohya_ss` README의 LoRA, DreamBooth, fine-tuning, SDXL training 지원 설명을 반영해 기준 경로 판단 근거를 보강했다.
- 번역 동기화 메모: 향후 번역본도 trigger token, caption, sample prompt, checkpoint, overfitting 등 학습 기록 용어의 의미 대응을 유지해야 한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.31`
