# Section Release Note

- Section ID: `P7-5.4`
- Source File: `docs/parts/part-07/chapter-05/section-04.md`

이 파일은 본문 구조와 채택·제외 판단을 날짜별로 요약한다. 개별 실행의 입력 순서, 해시, seed, step, 출력 파일은 본문에서 연결한 각 `result.json`과 Git 이력으로 확인한다.

### v2026.09.02

- 현재 경로를 `카메라판 → 마스크·컷아웃 → 그림자 포함 포즈 → Stage 2 착장 다중 참조 이식 → DeLight → 45도 얼굴·헤어 이식 → 배경판 → DeLight 통합 → Relight`로 정리했다. Scene A·B·C의 1280×1280 대표 결과와 코드·`result.json`을 본문에 연결했다.
- C의 거리 토큰을 제거하고, BFS Head V5 얼굴 참조와 DeLight 배경·캐릭터 통합본으로 교체했다. 사용하지 않는 C 통합·리라이트·마스크 자산은 제거했다.
- 상대 depth는 현재 채택 경로에서 제외했다. Part 7 체크포인트와 통합 관리 노트는 RGB 장면·카메라판·마스크·그림자 포함 포즈·Stage 2 착장 다중 참조 이식의 역할 분리만 현재 기준으로 맞췄다.
- 세부 실험 순서와 파일 나열은 `result.json`과 Git 이력으로 옮기고, 이 릴리즈노트는 날짜별 구조 변경과 채택·제외 판단만 남기도록 압축했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.02`.

### v2026.09.01

- 이전 Scene A 중심의 LaMa·알파 합성·하모나이즈 체인과 그 전용 자산을 제거하고, 그림자 포함 컷아웃과 Qwen Image Edit 2511 다중 참조 이식을 기본 경로로 바꿨다.
- 착장 추출·Try-On 비교는 P7-5.12로 분리하고, P7-5.4에는 카메라판·포즈·identity·배경·조명 역할만 남겼다. 출처와 참고 자료 및 `result.json` 확인 기준을 추가했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.01`.

### v2026.08.30–31

- 공식 Qwen Image Edit 2511 Multiple-Angles LoRA를 카메라판 생성 기준으로 정하고, 그 결과에서 Grounding DINO·SAM2 마스크와 흰 배경 포즈 컷아웃을 생성하도록 순서를 고정했다.
- 그림자 생성, 인물 제거 배경판, Studio DeLight, 다중 참조 통합, Relight를 분리해 각각의 관찰 범위와 한계를 기록했다. 전역 얼굴 이식과 일부 Try-On 결과는 기준 경로로 채택하지 않았다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.31`.

### v2026.08.18–29

- Qwen-Image T2I 장면, Qwen Image Edit 카메라·마스크·편집 단계를 역할별 독립 코드로 재구성했다. RGB·상대 depth는 장면·공간 입력이며 얼굴·착장·사지 비율을 보장하지 않는다는 경계를 본문에 명시했다.
- 카메라 축 복합, 이전 guide·ControlNet 경로, 상태 기반 승인 관리는 현재 기본 경로에서 제외했다. 현재 기록은 입력 역할, 실행 조건, 관찰된 일치·이탈을 남기는 방식으로 전환했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.29`.

### v2026.08.01–09

- 초기에는 ComfyUI, LoRA, ControlNet, OpenPose, 3D blockout 등 여러 일관성·포즈 제어 후보를 비교했다. 동일 인물성, 극단 포즈, 가림·접지 조건을 함께 만족하지 못한 경로와 관련 자산은 본문 기준에서 제거했다.
- 이 초기 비교는 현재 기본 경로의 품질 근거가 아니라, 구조 입력·캐릭터 참조·편집 영역을 분리해야 했던 이유를 남긴 이력이다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.09`.
