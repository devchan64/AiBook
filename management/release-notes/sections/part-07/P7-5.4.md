# Section Release Note

- Section ID: `P7-5.4`
- Source File: `docs/parts/part-07/chapter-05/section-04.md`

### v2026.07.31
- 변경 이유: Part 7 Section 메타데이터가 현재 가이드의 blockquote 형식과 어긋나 있어 규칙 준수 정리를 진행했다.
- 본문 반영: 제목 아래 `Section ID`, `Version` 두 줄을 blockquote 형식으로 정리하고, 본문 `Version`을 `v2026.07.31`로 갱신했다. 원고 학습 흐름과 실습 내용은 변경하지 않았다.
- 번역 동기화 메모: 향후 번역본도 같은 Section ID와 blockquote 메타데이터 형식을 사용해야 한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.31`


### v2026.07.26
- 정합성 점검: 본문 `Version`을 `v2026.07.26`으로 맞췄다.
- 번역 반영 상태: 기존 공개 번역 구조 유지
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-07/chapter-05/section-04.md` `v2026.07.26`


### v2026.07.23
- 변경 이유: Part 7 예제 중 실제 벡터 데이터베이스와 라이브러리를 사용하면 학습밀도가 높아질 후보로 P7-5.4를 선정했다.
- 본문 반영 내용: 순수 Python bucket 모의 실험을 ChromaDB 기반 예제로 교체하고, scikit-learn `TfidfVectorizer`로 계산한 로컬 TF-IDF 임베딩을 ChromaDB에 직접 넣어 외부 모델 다운로드 없이 실행되게 했다.
- 학습 품질 보강: 문서, 임베딩, 메타데이터를 함께 저장한 뒤 metadata filter로 검색 대상 bucket을 조정하면서 후보 문서 수, top-k 포함률, 누락된 exact 후보, vector DB top-k를 함께 읽도록 구성했다.
- 관련 자산: `docs/assets/part-07/chapter-05/p7-5-rag-documents.csv`
- 참고 자료: Chroma add-data, metadata filtering 문서와 scikit-learn feature extraction, cosine similarity 문서를 연결했다.
- 검증: `.venv/bin/python`으로 섹션의 Python 코드 블록을 실행해 임베딩 모양 `(18, 393)`, 후보 문서 수 `6/9/16`, top-k 포함률 `0.6/0.8/1.0` 출력이 본문 예시와 일치함을 확인했다.
- 번역 동기화 메모: 번역본 작성 시 ChromaDB, vector database, embedding, metadata filter, top-k recall 성격의 설명을 같은 개념 경계로 맞춘다.

### v2026.07.22
- 본문 작성: TBD 자리표시자를 제거하고, P7-5 공통 RAG 문서 CSV를 재사용하는 ANN 검색 설정 실습으로 본문을 채웠다.
- 코드 품질 보강: 실제 ANN 라이브러리 구현 대신 순수 Python bucket 기반 approximate 검색 예제를 추가해, exact top-k와 설정별 approximate top-k를 같은 실행 기록에서 비교하도록 했다.
- 학습 품질 보강: `좁은 후보군`, `균형 후보군`, `넓은 후보군` 설정을 비교해 후보 문서 수, top-k 포함률, 누락된 exact 후보가 함께 바뀌는 장면을 보여 주도록 구성했다.
- 자료 확인성 보강: 입력 CSV와 참고 자료 CSV 링크에 `csv-preview`를 추가했다.
- 검증: Python 코드 블록 1개를 실행해 exact top-k, 설정별 후보 문서 수 6/9/16, top-k 포함률 0.6/0.8/1.0 출력이 본문 예시와 일치함을 확인했다.

### v2026.07.20
- 저작권 검토: Part 7 Chapter 5 모듈 순차 검토 범위에서 ANN 검색 설정 실습 자리표시자 본문을 확인했다. 현재 본문은 P6-11.2에서 파생되는 향후 실습 범위 안내만 담고 있으며, 외부 자료나 자산을 사용하지 않는다.
- 출처 사용 점검: 외부 자료 직접 인용, 외부 도표·표·코드·데이터 재사용은 확인하지 않았다.
- 관련 자산: 없음.
- 본문 반영: 제목 아래 메타데이터를 저장소 공통 형식인 평문 `Section ID`, `Version` 두 줄로 바로잡고, 본문 `Version`을 `v2026.07.20`으로 갱신했다.
- 번역 동기화 메모: 향후 본문 작성과 번역본 생성 시 ANN 라이브러리 문서, 벤치마크 데이터, 인덱스 설정 예시의 출처와 라이선스를 별도로 확인해야 한다.

### v2026.07.19
- 신규 Section 준비: P6-11.2에서 파생되는 실제 ANN 검색 설정 실습을 Part 7 프로젝트 실습으로 다루기 위해 TBD 자리표시자 파일을 추가했다.
- 본문 작성 메모: 실제 작성 시 후보 수, 검색 지연, top-k 포함률, 버전 필터, RAG 근거 품질을 함께 기록하는 프로젝트 실습으로 확장한다.
