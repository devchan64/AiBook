# Section Release Note

- Section ID: `P7-5.1`
- Source File: `docs/parts/part-07/chapter-05/section-01.md`

### v2026.07.12
- 추가 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다.
- 본문 반영: 검색과 생성의 구분, 근거 기록, 출처 식별자, 질문-검색-답변 흐름을 `체크리스트` 하나로 정리했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 벡터 데이터베이스 운영 최적화, 임베딩 모델 비교, 고급 reranking, API 비용 최적화 같은 생략 목록을 걷어내고, 이번 절이 `질문 -> 검색 -> 근거 -> 답변 -> 출처`로 RAG 프로젝트 문서를 시작하는 기준을 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 검증 handoff는 `P7-5.2`, 뒤 프로젝트의 공통 실패 기록 handoff는 `P7-7.2`로 이어지게 분명히 했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-5.1 closes the RAG-project start structure first and hands search/answer validation to P7-5.2.

### v2026.07.18
- 추가 반영: RAG 문서 CSV 자산 경로를 본문 링크로 바꿔, 검색 후보 설명과 문서 조각 원문을 바로 오가며 읽을 수 있게 했다.
- 변경 이유: 기존 P7-5.1 예제가 문서 3개와 단순 토큰 겹침 비교 수준에 머물러, 검색 후보 사이의 경쟁과 근거 선택이 왜 중요한지 보여 주기에는 너무 장난감처럼 읽혔다.
- 본문 반영: 문서 집합을 6개 청크로 확장하고, 질문도 `검색 후보와 선택 근거를 왜 답변보다 먼저 기록해야 하는가`처럼 후보 경쟁이 실제로 갈리는 형태로 바꾸었다.
- 본문 반영: Python 예제는 `최고 문서 1개 선택` 구조에서 `상위 검색 후보 목록`, `선택한 근거 2개`, `근거 답변 기록`을 함께 남기는 구조로 다시 썼다.
- 추가 반영: 결과 해석도 단일 최고 점수보다 `후보 경쟁 -> 선택 기준 -> 답변 구성`을 읽는 표 중심으로 다시 정리했다.
- 추가 반영: 본문 말미의 `작은 예제` 표현을 `축약된 실습` 수준으로 조정해, 예제 크기보다 근거 기록 습관이 먼저 읽히도록 톤을 맞췄다.
- 추가 반영: 체크리스트의 `축약된 실습` 표현도 걷어내고, 예제 규모보다 출처 문서 식별 습관 자체가 핵심이라는 문장으로 더 직접 정리했다.
- 추가 반영: 실습용 문서 조각을 `docs/assets/part-07/chapter-05/p7-5-rag-documents.csv`로 분리하고, Python 코드는 인라인 딕셔너리 대신 실제 파일을 읽는 구조로 바꿨다.
- 추가 반영: `입력 파일` 소절을 따로 세워 CSV 경로, 한 행의 의미, 핵심 열을 먼저 읽고 그다음 Python 예제로 넘어가도록 구조를 분리했다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 질문 문장과 근거 선택 규칙을 바꿨을 때 검색 후보 순서와 답변 근거가 어떻게 달라지는지 직접 실험하도록 보강했다.
- 번역 동기화 메모: Future translations should preserve the stronger P7-5.1 example where multiple retrieval candidates compete and only direct-answer evidence is selected.
