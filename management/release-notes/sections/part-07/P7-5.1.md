# Section Release Note

- Section ID: `P7-5.1`
- Source File: `docs/parts/part-07/chapter-05/section-01.md`

### v2026.08.18

- 기본 해상도 고정: 화풍 참조 생성 스크립트의 기본 캔버스를 1024×1024로 설정했다. 비정사각형 해상도는 비교 실험에서 환경 변수로 명시할 때만 사용한다.
- 화풍 계약 압축·재승인: 이후 후보 생성에 쓰는 공통 화풍 프롬프트를 v3로 올리고, 핵심 조건을 47단어에서 30단어로 줄였다. v3 계약의 1번 아트리움(`seed=420713`, 결합 prompt 85단어), 2번 courtyard(`seed=420702`, 72단어), 3번 도심(`seed=420703`, 70단어)을 사람 승인했다. 1·3번은 필수 행, 2번은 보조 행으로 manifest의 같은 행 원본을 교체했다. 초기 v1 실행 전문과 단어 수는 재현 기록으로 보존한다.
- 원고·기록 정합성: 세 실행 JSON의 최종 상태를 `approved_for_downstream_reference`로, 각 행 상태를 `approved`로 갱신하고, 원고의 30스텝 표·승인 원본 표에 세 이미지를 반영했다.
- 생성기 전환: `p7_5_1_regenerate_local_gpu_style_references.py`를 Qwen Image와 Nunchaku FP4 r128 transformer 실행으로 바꾸고, 기본 추론 횟수를 30스텝으로 설정했다. 실행 기록에는 해상도·step·prompt 전문·prompt 단어 수·시간·GPU 메모리를 남긴다.
- 기본 운용점 기록: 최초 1024×1024 아트리움 30스텝 비교(`seed=420713`, v1 prompt 102단어, 행 생성 99.8초)를 기준 사례로 보관하고, v3 재생성 1번 및 2·3번 행의 시간·메모리·prompt 단어 수도 함께 기록했다.
- 정리: 이번 전환에서 만든 4·10·20·40·50스텝 후보와 실행 기록은 제거하고, 30스텝 후보 PNG와 실행 JSON만 남겼다.
- 번역 동기화 메모: Qwen Image 실행 구성, 30스텝 기본값, 1~3번 승인 원본과 manifest 교체를 동기화해야 한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.18`.

### v2026.08.10

- 승인 자산 추가: `seed=420820`, 12 step 세라믹 스튜디오 후보를 사람 승인하여 안정 자산명 `p7-5-1-style-ceramics-studio-afternoon-local-gpu-v1.png`으로 전환하고 승인 manifest의 보조 행으로 추가했다.
- 승인 범위 확장: `420810`, `420817`, `420814`, `420818`, `420703`, `420702`, `420819`, `420815`, `420708`, `420704`, `420816`, `420706`, `420709`, `420813`, `420707` 후보를 사람 승인했다. 기존 장면 7개는 새 승인 원본으로 교체하고, 신규 장소 8개를 보조 행으로 추가했다. 구조를 단순화해 재생성한 `420812` 항구 테라스와 `420811` 높은 로비 도서관도 보조 행으로 승인했다.
- 원고 반영: 승인 원본을 열 장으로 갱신하고, 화풍 원본 표를 빈 칸 없이 4열·4열·2열로 재배치했다.
- 번역 동기화 메모: 승인 원본 수와 세라믹 스튜디오 보조 행, 이미지 표 배치를 동기화해야 한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.10`.

### v2026.08.09

- 이력 압축: 현재 원고·자산 추적과 번역 동기화에 필요한 배경 화풍 참조 셋의 범위, 승인 원본, 실행·검수 경계만 남겼다. 세부 prompt 조정, 표 배치, 중간 후보와 폐기 과정의 반복 기록은 제거했다.

### v2026.08.08

- 현재 범위 확정: P7-5.1을 인물 생성이 아닌 **배경 화풍 참조 셋의 생성·사람 검수·승인**으로 한정했다. 공통 화풍 조건은 `p7-5-1-style-prompt-contract.json`으로 분리하고, 장소·시간·카메라는 장면 행 prompt로 유지했다. 다음 단계에 남기는 공개 자산은 승인 PNG와 `p7-5-1-approved-style-reference-pack.json` manifest이며, 검수 ledger는 로컬 기록으로 분리한다.
- 번역 동기화 메모: 배경 전용 범위, 공통 계약과 장면 변수의 분리, 사람 검수와 manifest의 역할을 함께 반영한다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.08`.

### v2026.08.04

- 승인 참조 셋 확정: 로컬 GPU 생성 원본 9개를 사람 승인했다. 실내·실외, 새벽·낮·해질녘·밤·우천 야간, high/low/eye-level/oblique/overhead 계열을 담은 5개 필수 행과 4개 보조 행으로 화풍 계약의 반복성을 검수한다. 이 원본과 승인 manifest가 현재 다음 단계 입력 검토의 기준이다.
- 번역 동기화 메모: 승인 원본이 배경 화풍 계약의 검수 근거이며, 타일 이미지나 미승인 후보가 다음 단계 입력이 아니라는 경계를 유지한다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.08`.
