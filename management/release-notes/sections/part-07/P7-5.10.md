# Section Release Note

- Section ID: `P7-5.10`
- Source File: `docs/parts/part-07/chapter-05/section-10.md`

### v2026.09.20

- 본문에 핵심 검수 결과와 누적 3200스텝·강도 0.75 권장값을 요약하고, 사용자 얼굴·머리 방향 판단을 기존 AI 보존 항목 관찰과 구분했다.
- 검수 요약·45개 스텝 비교·12개 강도 비교시트를 각각 내용보기로 연결했다. 상세 시트의 Mira 토르소 참조와 입력을 대조하는 방법을 설명하고 반복 조건·기존 사례 해설은 펼치기 안으로 옮겼다.
- 공개 다운로드는 기존 1600스텝 가중치임을 유지하고, 완료된 누적 3200스텝 후속 실험과 구분했다.
- 관련 자산: `bfs-camera-366-review.md`, `bfs-camera-366-step-review.md`, `bfs-camera-366-scale-review.md`.
- 번역 반영 상태: 해당 Section의 영어·중국어 원고는 아직 없다.

### v2026.09.18

- 010에서 입력에 없던 가슴 포켓이 추가된 것을 확인해 검수 MD와 원고에 의상 보존 오류로 반영했다.

- 실제 학습 분할에서 고른 입력·목표 3쌍을 추가하고, 서로 다른 입력이 5.2의 같은 측면 목표를 공유하는 대응을 설명했다. 남아 있던 구배포 학습량 설명도 현재 347쌍 학습 결과로 바로잡았다.

- 본문에 008·023·009·022의 입력·결과 샘플을 추가해 얼굴·헤어 변환, 의상 보존 오류, 기존 목선 유지 사례를 구분했다.

- 022 입력에 이미 V형 중앙 트임이 있음을 확인해 목선 변경 판정을 철회하고, 원고·검수 MD·Hugging Face 샘플 설명을 정정했다.

- 얼굴·헤어·표정에 큰 특이점이 없다는 사용자 검수 판단과 일부 의상 보존 오류를 원고·검수 MD에 반영했다. 합성 검수 시트를 제거하고 MD의 원본 비교를 검수 기준으로 정리했다.

- 외부 입력 45쌍을 시각 검수해 관리번호별 의견을 기록했다. 캐릭터 변환과 의상·표정·소품·몸 비율 보존을 구분하고, 사용자 최종 판정 및 스텝 증가 효과의 검증과 분리했다.

- Hugging Face 가중치를 366쌍 데이터셋의 1600스텝 모델로 갱신하고 학습 정보·45장 비교표·고정 다운로드 리비전을 반영했다. 진행 중인 누적 3200스텝 학습과 배포 모델을 구분했다.

- 새 1600스텝 LoRA의 외부 입력 45장 시각 검수표를 연결하고, 저장 상태를 복원해 누적 3200스텝까지 이어 학습하는 경로를 반영했다.

- 과거 LoRA 평가 이미지·기록·원고 결과를 삭제하고, 현재 366쌍 데이터셋의 새 학습과 후속 평가 흐름으로 정리했다.

- 생성 출력은 training-images의 입력·목표 폴더와 generation-records로 고정했다. 출력 경로 덮어쓰기를 제거하고 과거 카탈로그는 재사용 확인에만 읽도록 정리했다.

- 원고를 현재 4개 기준 파일과 366쌍 목록에 맞춰 정리했다. 과거 저장 경로·수량·진행 표현을 수정하고 검수표 링크는 데이터셋 목록표 하나로 통일했다.

- 생성 코드·생성 JSON·LoRA 코드·데이터셋 JSON의 4개 기준 파일로 통합했다. 후보 선택 기능, 학습 설정과 경로 호환 정보를 흡수하고 별도 파일을 삭제했다.

- 생성 조건·선택 JSON 4개를 관리번호 528개의 단일 생성 JSON으로 통합했다. 규칙 선택과 관리번호 옵션을 연결하고 완료·제외 결과의 재사용을 검증했다.

- 데이터셋 이미지 생성 진입점을 공용 생성기 하나로 통일하고, 미사용 비교표 갱신 코드와 개별 생성기의 오래된 경로 매핑을 삭제했다.

- 입력·목표 후보 비교표 4개를 삭제하고 확정 데이터셋 366쌍 검수표만 유지했다.

- 구버전 193쌍 데이터셋과 입력·목표 검수 JSON을 삭제하고 현재 366쌍 목록으로 참조를 통일했다. 새 학습 명령과 과거 배포 LoRA의 학습 조건을 구분했다.

- 단순 토르소 목표를 5.2 원본 재사용으로 확정하고, 별도 맞춤 목표 13개와 생성 설정·전용 처리 기능을 삭제했다.

- 확정 학습 이미지 412개(입력 366·공유 목표 46)를 training-images 하위로 통합하고, 기존 참조는 심볼릭 링크로 유지했다.

- 보강 입력 180개 중 사용자 제외 7개를 뺀 173개를 사용 확정했다. 생성 JSON에 관리번호·입력·목표·결과 ID를 연결하고 기존 193쌍과 통합한 366쌍의 무결성·분할을 검증했다.

- 보강 입력 180개·맞춤 Mira 목표 13개 검수표를 연결하고, 생성 완료와 검수 대기를 구분했다. 첫 24개 입력의 채택 및 목표 11개 재사용 결정은 유지한다.

### v2026.09.17

- 추가 입력 24개 중 11개는 기존 Mira 목표를 재사용하고 13개만 맞춤 목표 생성 대상으로 남겼다. 통합 목록은 준비 완료 204쌍·목표 대기 13개로 갱신하고 생성 대기 작업을 재등록했다.

- 기존 193쌍과 사용자 채택 입력 24개를 통합 217개 데이터셋으로 관리한다. 새 목표 대기 상태를 구분하고 미완료 쌍의 학습 준비를 차단하며, 기존 193쌍 기록은 재현용으로 유지한다.

- 첫 24개 입력의 사용자 채택과 대응 Mira 목표 추가 생성을 반영했다. 공용 생성기에 순서가 명시된 복수 참조를 지원하고, 목표 생성 카탈로그의 입력·목표 연결을 후보 선택 코드에 연결했다.

- 보강 후보 24개 비교표에 추가 시각 검수 결과를 기록했다. 포즈 변경 012와 입 벌림·의상·몸통 변화 의심 항목을 관리번호로 구분하고, 최종 폐기 결정과 분리했다.

- 보강 입력 24개의 입력·목표 비교표를 관리번호와 함께 연결하고, 완료 항목을 제외한 나머지 156개 생성 상태를 반영했다. 시각적 채택 판정은 별도 검수로 남긴다.

- 현재 입력 생성 목록의 training_caption에서 BFS 표현을 제거하고 Mira의 얼굴·헤어·화풍으로 명시했다. 기존 학습·평가 기록은 유지하고 이미지 생성 조건 해시와 향후 학습 캡션을 구분했다.

- 과거 자산 경로 해석 함수를 공용 생성 코드에 흡수하고 별도 경로 보조 파일을 제거했다. 후보 선택·학습 준비도 같은 함수를 사용한다.

- 자산 최상위 JSON을 15개에서 8개로 정리했다. 조합 목록에 저장 경로·폐기 정보를 통합하고 중복 평문·실행 설정·과거 보충 목록을 제거했다. 기존 조건은 정규화 기준 해시로 검증하며 이미지·결과·검수 기록은 유지한다.

- 사용자 요청으로 외부 입력 45장 평가와 비교표 갱신을 중단했다. 생성된 출력 17장과 결과 JSON·실행 계획·상태·비교표를 삭제하고 본문 링크와 진행 설명을 정리했다. 입력 45장과 생성 기록은 보존한다. 번역 반영 상태: 대응 번역본 없음.

### v2026.09.17

- 기존 입력 220개·목표 128개와 새 15방향 보강 후보 180개를 조건 정의·명시적 조합 구조로 통일했다. 기존 평문 원본과 프롬프트·시드·참조의 완전 일치, 폐기·완료 항목 재사용을 확인하고 원고의 실행 경로를 조합 JSON으로 연결했다.

### v2026.09.16

- 입력·목표 후보 생성기를 하나로 통합하고 실행·선택 조건을 JSON으로 분리했다. 폐기 정책 자동 적용, 완료 이미지 재사용, 5.2의 15방향 토르소를 활용한 얼굴 비율 보강 후보 180개의 생성 준비를 반영했다.

- sec-10 이관 후 이전 sec-12 경로에 남은 결과 JSON 호환 링크 32개를 이관 목록 기준으로 갱신했다. docs 전체의 끊어진 링크와 정적 파일 복사를 확인했다.

- 완료 자산 802개를 `sec-10`으로 이전하고 Section 파일명·현재 문서 링크·Python import·후보 카탈로그를 갱신했다. 진행 중인 45장 평가의 입력·출력, 평가·비교표 코드와 기준 데이터셋은 `sec-12`에 유지했다. 과거 생성 JSON·지문은 보존하고 `p7-5-10-asset-migration.json`과 경로 해석 코드로 이전 경로를 연결한다. 번역 반영 상태: 대응 번역본 없음.

- 내용보기 레이블 앞에 파일 종류 태그(Python·JSON·CSV·Markdown)와 내용보기·내용닫기 동작 태그를 표시하고 펼침·닫힘 상태에서도 유지하도록 공통 UI를 수정했다.

- 도입부에 공개 LoRA 빠른 실습을 배치했다. 브라우저 직접 다운로드·고정 리비전 curl 명령·기반 모델 준비·512 참조 처리·적용 조건과 검수 기준을 안내한다.

- 5.12 원고를 보충학습 5.10으로 이전했다. 캐릭터 일관성을 목적에 두고 LoRA·BFS 콘셉트, 이미지 생성·데이터셋 구성·학습·검증 19쌍 평가·외부 입력 45장 추가 평가 순서로 정리했다. 이후 완료 자산을 sec-10으로 이전하고 진행 중 평가 자산은 sec-12에 유지한다.
- Hugging Face 모델 카드를 5.10의 캐릭터 일관성 목표·고정 리비전 빠른 다운로드·로컬 어댑터 적용 예제·외부 입력 45장 추가 평가 범위에 맞춰 공개 갱신했다. 가중치는 유지하며 5.10 Pages 주소는 아직 404여서 배포 대기로 명시했다.
- 이전 편집·실행 이력은 P7-5.12 릴리즈노트에 보존한다.
