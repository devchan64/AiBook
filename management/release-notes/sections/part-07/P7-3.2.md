# Section Release Note

- Section ID: `P7-3.2`
- Source File: `docs/parts/part-07/chapter-03/section-02.md`

### v2026.07.07
- 본문 반영: 공통 검토 구조 설명을 `검토`, `검토 이유`, `사실` 중심으로 다시 쓰고, 공개 출력은 `검토 요약`, `오류 사례 기록`, `다음 조치 목록`, `확신 차이` 같은 한국어 표현으로 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-3.2 공개 설명과 출력에서는 한국어 개념 설명을 먼저 두고 영어 내부 키는 드러내지 않는 편이 좋다.

### v2026.07.12
- 추가 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다.
- 본문 반영: 오류 사례의 의미, 데이터/표현/모델 가설, 애매한 확률 출력, 다음 개선 항목을 `체크리스트` 하나로 통합했다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 confusion matrix 전체 계산, explainability 기법, saliency map/feature visualization 같은 생략 목록을 걷어내고, 이번 절이 `틀린 샘플을 다음 반복의 데이터/표현/모델 질문으로 바꾸는 최소 오류 사례 구조`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 오류 사례 기록의 후속 handoff를 `P7-4.2`, `P7-7.2`로 직접 연결해 Part 7 전체의 공통 실패 기록 구조가 다시 보이게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-3.2 closes the error-case record structure first and hands richer evaluation/failure writing to later project sections.

### v2026.07.18
- 추가 반영: 말미 체크리스트를 `오류 샘플`, `검토 이유`, `가설 층위`, `구조 질문`, `다음 조치` 다섯 칸으로 압축해, 오류 사례가 다음 구조 선택 실험을 여는 기록으로 읽히게 정리했다.
- 추가 반영: 본문의 두 Mermaid 도식을 `docs/assets/part-07/chapter-03/*.mmd` 자산으로 분리하고, Chapter 3 자산 폴더 README에 모델 선택 도식 운영 범위를 기록했다.
- 추가 반영: 약한 스크래치 사례에서 `더 큰 모델부터 붙인다`는 빠른 판단을 멈추고 데이터·공간 구조 질문을 먼저 거치게 하는 사례 문단과 Mermaid 흐름도를 보강했다.
- 추가 반영: 범위 문단의 진행형 메타 표현을 줄이고, 오류 사례를 데이터·표현·구조 질문으로 나눠 구조 계열 비교로 넘기는 현재 절 역할이 먼저 보이게 다듬었다.
- 추가 반영: `작은 이미지 분류기`, `작은 패치`, `작은 표본`처럼 실습 강도를 낮춰 보일 수 있는 표현을 `기본 분류기`, `현재 패치`, `표본이 제한된 실습`으로 정리해 현재 절의 구조 비교 판단이 먼저 보이게 다듬었다.
- 추가 반영: 구조 계열 해설에서 `Part 4에서 본` 같은 직접 회수 표현을 줄이고, 약한 결함 사례 앞에서 왜 공간 구조 보존 질문이 먼저 열리는지만 남겨 현재 절의 비교 초점을 더 직접 정리했다.
- 추가 반영: 약한 결함 사례 해설에서 `읽는 편이 맞습니다` 같은 독법 안내를 줄이고, 현재 절이 원인 확정이 아니라 가설 정리와 구조 질문을 남기는 자리라는 점을 더 직접 드러냈다.
- 추가 반영: `P7-3.1에서...` 식의 절 번호 호출을 줄이고, 현재 이미지 분류 실습의 약한 결함 사례를 바로 받아 구조 계열 비교로 들어가도록 도입 문장을 다듬었다.
- 추가 반영: 도입과 범위 안내에서 `P7-3.1에서는 ...` 식의 순차 요약을 줄이고, 오류 사례를 구조 선택 질문으로 바꾸는 현재 절의 역할이 먼저 보이게 정리했다.
- 추가 반영: 절의 앞단을 `오류 사례 기록` 중심에서 `같은 실패가 왜 CNN, 순차 모델, attention 계열 비교 질문으로 이어지는가` 중심으로 다시 세워 현재 목차 제목과 본문 전개를 맞췄다.
- 추가 반영: 구조 계열 비교표와 `데이터 / 입력 표현 / 구조 계열` 3층위 표를 보강해, 약한 결함 사례가 왜 순차 모델보다 CNN 질문을 먼저 열고 attention 계열은 어떤 경우에 뒤따르는지 눈으로 비교하게 정리했다.
- 추가 반영: Section 제목을 `오류 사례 분석`에서 `CNN, 순차 모델, attention 계열 비교`로 바꾸고, 오류 사례 해석이 구조 선택 질문으로 이어진다는 현재 목차 역할을 제목에 반영했다.
- 추가 반영: 회고에 쓰는 세 CSV 자산 경로를 모두 링크로 바꿔, 평가 기록·패치 기준 파일·다음 조치 파일을 본문에서 바로 열어볼 수 있게 했다.
- 변경 이유: P7-3.1 예제가 8x8 표면 점검 패치 기반으로 강화되면서, 기존 P7-3.2의 `4x4 혼합 막대` 오류 사례는 바로 앞 절과 이어지지 않았다.
- 본문 반영: 오류 사례를 `평가-결함-약함` 오분류로 바꾸고, 약한 스크래치와 그림자 변화가 섞일 때 왜 데이터/표현/모델 가설을 분리해 남겨야 하는지 다시 정리했다.
- 본문 반영: Python 예제와 실행 결과도 약한 결함 사례, 학습 데이터 강도 부족, 그림자 변형 추가 같은 실제 다음 조치 중심으로 갱신했다.
- 추가 반영: `작은 장난감 데이터` 같은 표현을 `제한된 synthetic 점검 패치`로 바꿔, 오류 분석이 실제 점검 시나리오의 축소판으로 읽히도록 톤을 맞췄다.
- 추가 반영: 오류 사례 요약과 다음 조치 목록을 `docs/assets/part-07/chapter-03/p7-3-error-review.csv`, `docs/assets/part-07/chapter-03/p7-3-followup-actions.csv`로 분리하고, 본문 Python 예제가 파일 읽기 이후의 회고 생성 흐름에 집중하도록 다시 정리했다.
- 추가 반영: 다른 Part 7 절과 같은 읽기 리듬이 보이도록 실행 결과 해설 표지를 `## 결과를 어떻게 읽는가`로 통일했다.
- 추가 반영: `입력 파일` 소절을 따로 세워 평가 기록, 학습 패치, 다음 조치 파일이 각각 어떤 역할을 하는지 먼저 읽고 코드로 넘어가도록 구조를 분리했다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 확률 차이와 다음 조치 목록을 직접 바꾸며 오류 해석의 무게중심이 어떻게 달라지는지 실험하도록 보강했다.
- 번역 동기화 메모: Future translations should keep P7-3.2 aligned with the model-family comparison framing, where the weak-defect case opens a CNN-first question rather than remaining a generic error-analysis section.
