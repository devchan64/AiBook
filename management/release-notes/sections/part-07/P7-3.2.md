# Section Release Note

- Section ID: `P7-3.2`
- Source File: `docs/parts/part-07/chapter-03/section-02.md`

### v2026.07.19
- 변경 이유: Part 7 연습 Section의 학습밀도 보강 이력을 원고 가이드 기준에 맞춰 압축 정리했다.
- 본문 반영: 범위·목표·연습 표지와 본문 안의 절 진행 지시 표현을 내용형 기록 기준으로 바꾸고, 실행 기록·결과 해석·프로젝트 기록 예시가 설명과 해설 안에서 읽히도록 정리했다.
- 번역 동기화 메모: 향후 번역본도 진행 안내 제목보다 Section별 질문, 판단 기준, 실행 결과 해석, 프로젝트 기록 예시가 먼저 보이게 맞춘다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 원문 기준 버전: `v2026.07.19`

### v2026.07.07
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 공통 검토 구조 설명을 `검토`, `검토 이유`, `사실` 중심으로 다시 쓰고, 공개 출력은 `검토 요약`, `오류 사례 기록`, `다음 조치 목록`, `확신 차이` 같은 한국어 표현으로 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-3.2 공개 설명과 출력에서는 한국어 개념 설명을 먼저 두고 영어 내부 키는 드러내지 않는 편이 좋다.

### v2026.07.12
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다. / 오류 사례의 의미, 데이터/표현/모델 가설, 애매한 확률 출력, 다음 개선 항목을 `체크리스트` 하나로 통합했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.
### v2026.07.17
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: `이 절의 범위`에서 confusion matrix 전체 계산, explainability 기법, saliency map/feature visualization 같은 생략 목록을 걷어내고, 이번 절이 `틀린 샘플을 다음 반복의 데이터/표현/모델 질문으로 바꾸는 최소 오류 사례 구조`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다. / 오류 사례 기록의 후속 handoff를 `P7-4.2`, `P7-7.2`로 직접 연결해 Part 7 전체의 공통 실패 기록 구조가 다시 보이게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-3.2 closes the error-case record structure first and hands richer evaluation/failure writing to later project sections.

### v2026.07.18
- 변경 이유: P7-3.1 예제가 8x8 표면 점검 패치 기반으로 강화되면서, 기존 P7-3.2의 `4x4 혼합 막대` 오류 사례는 바로 앞 절과 이어지지 않았다.
- 본문 반영: 말미 체크리스트를 `오류 샘플`, `검토 이유`, `가설 층위`, `구조 질문`, `다음 조치` 다섯 칸으로 압축해, 오류 사례가 다음 구조 선택 실험을 여는 기록으로 읽히게 정리했다. / 본문의 두 Mermaid 도식을 `docs/assets/part-07/chapter-03/*.mmd` 자산으로 분리하고, Chapter 3 자산 폴더 README에 모델 선택 도식 운영 범위를 기록했다. / 약한 스크래치 사례에서 `더 큰 모델부터 붙인다`는 빠른 판단을 멈추고 데이터·공간 구조 질문을 먼저 거치게 하는 사례 문단과 Mermaid 흐름도를 보강했다. 등으로 압축 정리했다.
- 번역 동기화 메모: Future translations should keep P7-3.2 aligned with the model-family comparison framing, where the weak-defect case opens a CNN-first question rather than remaining a generic error-analysis section.
