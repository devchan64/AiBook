# Section Release Note

- Section ID: `P7-5.2`
- Source File: `docs/parts/part-07/chapter-05/section-02.md`

### v2026.09.01

- 정면 얼굴 기준의 `1024×1024`, `seed=62294`, 10-step `result.json`에 대응하는 Python 생성 코드를 정면 기준 설명에 발췌했다. 회전 Python 생성기는 카메라 변환 절에 옮겨 한 축의 중국어 카메라 명령, 체스트 참조 입력, 다중 앵글 LoRA와 `result.json` 기록을 함께 읽게 했다. 별도의 `재실행 기록` 절과 재실행 명령은 제거했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.01`.

### v2026.08.30

- 얼굴 전용 파일명이던 공용 identity 계약을 `p7-5-2-character-identity-contract.json`으로 바꾸고, 본문 링크와 정면 얼굴·착장·후속 장면 생성기의 참조를 함께 갱신했다. 계약에는 공통 착장 문장을 추가해 흰색 크롭 재킷, 회색 이너, 청록 와이드 팬츠, 흰색 로우톱 스니커즈를 한 곳에서 관리한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.30`.

### v2026.08.29

- P7-5.7 캐릭터 멀티플 뷰 Section을 P7-5.2로 이관했다. 원고·자산·실행 코드·result JSON·릴리즈노트와 전체 참조를 새 Section ID로 맞추고, P7-5.3 전신·착장 Section과의 링크도 실제 경로로 정정했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.29`.
- 카메라 좌·우 라벨을 기준 정면에서의 카메라 회전으로 명시하고, 비교표 뒤에 이 실행에서 관찰한 특징 유지·이탈을 추가했다. 재실행 기록에는 정면·yaw·pitch를 한 축씩 실행하는 명령과 조작 변수·관찰 항목을 보강했으며, 정면 T2I에 쓰는 Nunchaku Qwen Image 저정밀 transformer 모델 카드도 출처에 추가했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.29`.
- JSON 계약·실행 기록과 Python 생성기는 한 줄짜리 Markdown 링크로 쓴다. 사이트는 로컬 `.json`·`.py` 단독 링크를 자동 `aibook-lazy-source` 패널로 변환하므로, 원고는 이미지 표의 직접 링크·일반 HTML 없이 조건과 해시를 분리해 읽는다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.29`.
- 정면 기준 생성용 `Qwen/Qwen-Image`, 체스트 편집용 `Qwen/Qwen-Image-Edit-2509`, 카메라 변환용 `dx8152/Qwen-Edit-2509-Multiple-angles` LoRA, 저메모리 실행용 Nunchaku FP4 transformer의 역할과 경계를 표로 추가했다. Diffusers는 실행 파이프라인으로, Nunchaku는 별도 기반 모델이 아닌 저정밀 가중치·runtime 구성으로 구분하고, LoRA가 identity·화풍을 독립적으로 정하는 모델이 아니라는 한계를 명시했다. 네 공개 모델 카드의 연결과 확인일을 2026-08-29로 남겼다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.29`.
- Section 제목과 목차 이름을 `캐릭터 멀티플 뷰 생성: identity 기준과 카메라 앵글 분리하기`로 바꿔, 정면 기준·체스트 참조·yaw·pitch 변환을 함께 만드는 이 절의 중심 학습 산출물을 드러냈다. P7-5.3의 연결 문구도 같은 범위로 맞췄다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.29`.

### v2026.08.23

- 새 1024×1024 정면 하이·로우앵글 결과를 각각 입력으로 쓰고, 한 축 yaw `−45°/+45°`를 적용한 네 결과와 대응 result JSON으로 3.3 비교표를 교체했다. yaw와 pitch를 한 prompt에 복합하지 않은 순차 입력 경로다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.23`.
- 정면 체스트 참조에서 다시 만든 1024×1024 하이·로우앵글 8-step 결과와 대응 result JSON으로 P7-5.2의 pitch 비교표를 대체했다. high/low를 yaw와 복합하지 않은 한 축 생성 결과로만 제시한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.23`.
- 카메라 앵글 생성기의 체스트 정면 기준을 `true_cfg_scale=4.0`, 빈 negative prompt, 1024×1024로 다시 생성한 결과 `p7-5-2-qwen-torso-yaw-front-cfg4-front-1024-v4-seed-62294-steps-8.png`와 대응 result JSON으로 교체했다. 원고의 체스트 정면 실행 기록·yaw 비교표와 실행기의 기본 입력을 같은 파일로 맞췄다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.23`.
- 같은 1024×1024 기준 입력에서 `yaw −90°·−45°·+45°·+90°` 8-step 결과와 대응 result JSON을 새로 생성해, 기존 768×768 yaw 비교표·실행 기록을 모두 교체했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.23`.

### v2026.08.21

- 기반 모델 `Qwen/Qwen-Image-Edit-2509`과 다중 앵글 LoRA `dx8152/Qwen-Edit-2509-Multiple-angles`의 역할·근거를 원고에 추가했다. 공식 Qwen 모델 카드의 이미지 편집·인물 일관성 설명과 LoRA 모델 카드의 카메라 명령·일관성 한계를 각각 출처로 연결했다. 로컬 Nunchaku·Lightning 구성은 8GB GPU 실행 조건일 뿐 일반 성능 비교가 아니라는 경계도 명시했다.
- P7-5.3에 섞여 있던 Qwen 정면 얼굴 기준과 identity·화풍·일러스트 계약, 정면 얼굴 생성기, Qwen Edit 2509 다중 앵글 LoRA 생성기, yaw·pitch 얼굴 회전 후보를 P7-5.2로 분리했다. 정면 얼굴은 identity·헤어·화풍을, 다중 앵글 LoRA와 짧은 중국어 카메라 명령은 카메라 회전만 맡는 역할 분리를 명시했다.
- 승인 정면 얼굴 하나를 입력으로 쓴 `seed=62294`, `768×768`, 8-step 좌·우 측면(`yaw −90°/+90°`, `pitch 0°`)을 사람 승인해 안정 PNG·review JSON으로 등록했다. 각 승인은 해당 측면에만 한정하며, 다른 yaw·pitch·표정·전신·장면 조건을 승인하지 않는다.
- 우측 측면은 같은 조건의 v2 후보로 안정 기준을 교체했다. 이전 v1 PNG·review JSON은 현재 승인 체인에서 제거하고 Git 이력으로만 보존한다.
- 같은 입력 조건의 우측 쿼터(`yaw +45°`, `pitch 0°`) v3 8-step 결과도 사람 승인해 안정 PNG·review JSON으로 등록했다. 이 승인은 우측 쿼터 한 장에만 한정한다.
- 같은 입력 조건의 좌측 쿼터(`yaw −45°`, `pitch 0°`) v2 8-step 결과도 사람 승인해 안정 PNG·review JSON으로 등록했다. 이 승인은 좌측 쿼터 한 장에만 한정한다.
- 그림자·하이라이트 지시를 제거한 10-step 정면 얼굴 후보를 사람 승인해 정면 기준 PNG·review JSON으로 교체했다. 승인 범위의 렌더링 기준은 네 값 음영·강한 하이라이트가 아니라 어두운 윤곽선과 평면 색이다.
- 정수리 전체와 머리 위 빈 여백을 명시한 10-step `front-flat-color-crown-margin-v1` 후보를 사람 승인해 정면 기준을 다시 교체했다. 이 기준은 상단이 잘리지 않은 정면 얼굴에만 적용한다.
- P7-5.2 자산은 실행 조건과 사람 판정을 별도 run JSON·review JSON으로 나누지 않고 review JSON 하나로 관리하도록 정리했다. 승인 기준의 실행 원본은 `execution` 필드에 보존한다.
- 회전 후보와 정면 기준 T2I는 사람 승인 기준을 만든 2509 계열 경로만 유지한다. 정면 실행기는 `p7_5_2_qwen_edit_front_head_reference_t2i.py`, 회전 실행기는 `p7_5_2_qwen_camera_angle_2509_probe.py`로 관리한다.
- 다중 앵글 LoRA의 15장 yaw×pitch 실험에서 피치 지시는 `将镜头转为俯视。` 또는 `将镜头转为仰视。` 한 문장으로 분리했다. 카메라 상·하 이동은 별도 프레이밍 변수가 되므로 이 비교 조건에 섞지 않는다. 모델 카드의 짧은 중국어 카메라 명령을 출처로 기록한다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.21`.

### v2026.08.22

- 정면 얼굴 기준을 1024×1024 Qwen T2I 결과 `p7-5-2-qwen-face-head-front-1024-reference-v1-seed-62294-steps-10-size-1024.png`와 대응 result JSON으로 교체했다. P7-5.2 정면 얼굴 생성기의 기본 출력도 같은 1024 크기·파일명 규칙으로 맞췄으며, P7-5.3 전신·착장 생성기는 이 파일을 identity·헤어 입력으로 사용하도록 갱신했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 운영 방식 변경: P7-5는 이미지·guide·실행 결과에 승인·미승인·보류 상태를 부여하거나 이를 다음 단계의 gate로 관리하지 않는다. 이후 기록은 입력 역할, 생성 조건, 관찰된 일치·이탈, 알려진 한계와 다음 비교 질문을 남긴다. 아래의 승인·검수·후보 표현은 당시 이력이며 현재 운영 규칙이 아니다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 기존 얼굴 전용 yaw 회전 표를 체스트 정면 참조 기반의 yaw ±45°·±90° 및 하이앵글 결과로 교체했다. 체스트는 얼굴·헤어·상반신 연결을, 다중 앵글 LoRA와 짧은 카메라 명령은 변환 축을 맡는다는 역할 경계를 본문·체크리스트에 반영했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 체스트 기준 로우앵글 결과와 대응 result JSON을 같은 비교표에 추가해, pitch 축을 하이앵글과 로우앵글 두 방향으로 대조할 수 있게 했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 다방향 체스트 참조를 준비하는 의도를 명시했다. 이후 장면에서 가까운 yaw·pitch의 체스트 참조를 쓰면 얼굴 윤곽·앞머리 가림·귀·목·어깨 연결을 새로 추측할 범위를 줄여 캐릭터 재현 가능성을 높일 수 있으나, 이를 품질 보장으로 해석하지 않는다고 경계를 추가했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 하이앵글 체스트 이미지를 새 입력으로 사용한 좌·우 쿼터(`yaw −45°/+45°`) 결과와 result JSON을 추가했다. pitch와 yaw를 한 prompt에 결합하지 않고, 하이앵글은 입력 이미지 역할로, yaw는 카메라 명령 역할로 분리한 순서를 본문에 기록했다.
- 로우앵글 체스트를 새 입력으로 사용한 좌·우 쿼터(`yaw −45°/+45°`) 결과와 result JSON도 추가했다. 비교표는 `pitch 0°`의 다섯 yaw → `yaw 0°`의 high/low pitch → high/low 입력의 좌우 쿼터 순서로 재구성해 중복 이미지를 제거했다.
- 원고를 `정면 얼굴·체스트 기준` → `LoRA와 한 축의 카메라 명령 역할` → `yaw·pitch 결과 비교` → `관찰 기준` → `재실행 기록` 흐름으로 재배치했다. 모델·런타임 설명을 기준 이미지와 입력 역할 뒤에 두어, 기술 구성보다 재현성 판단의 출발점을 먼저 읽도록 정리했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 차트 검수: 768×768 이미지 다섯 장을 한 표에 넣으면 좁은 화면에서 얼굴 세부를 비교하기 어려워, `yaw −90° → 0°`의 3열과 `yaw +45° → +90°`의 2열 비교표로 나눴다. 각 표는 방향이 증가하는 순서를 유지하며, high/low 표의 라벨도 한국어 본문과 맞췄다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 정면 얼굴 기준부터 체스트 참조, 한 축의 yaw 또는 pitch 변환, result JSON 기록, 네 축 비교와 다음 장면의 입력 선택으로 이어지는 작업 순서도를 Mermaid 자산으로 추가했다. 비교표가 아닌 순서·분기 관계만 맡도록 구성했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 다중 앵글 LoRA의 기술적 역할을 보강했다. LoRA adapter가 기반 편집 모델 위에서 카메라 명령에 반응하는 경향을 더하는 방식, `yaw`·`pitch`가 물리적으로 보정된 3D 각도가 아니라 실험용 명령 라벨이라는 경계, 이동·회전·위아래 보기·렌즈 제어를 한 축씩 분리하는 이유를 설명했다. 저정밀 Nunchaku 실행에서는 전용 LoRA 적용 수·가중치 해시·강도를 result JSON에 기록한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 출처 검수: Qwen 공식 모델 카드의 이미지 편집·인물 일관성·ControlNet 지원 설명, dx8152 모델 카드의 기반 모델·LoRA 로드·카메라 명령·일관성 한계 설명을 본문 주장과 대조했다. PEFT 링크는 실제로 LoRA 문서로 연결되도록 표제와 URL을 정정하고, 세 외부 출처의 확인일을 2026-08-22로 갱신했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 체스트 기반 카메라 앵글 결과로 대체한 기존 얼굴 전용 좌·우 쿼터·측면 PNG와 대응 result JSON 네 쌍을 삭제했다. 정면 얼굴 기준은 유지한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 정면·좌우 쿼터·좌우 측면 얼굴의 원고 링크와 실행 기록 파일명을 `result.json`으로 통일했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 가슴 중간까지 포함한 체스트 정면 참조와, 이를 유일한 이미지 입력으로 쓴 하이앵글 결과를 원고에 추가했다. 체스트 참조는 카메라 회전에서 어깨·상반신 연결을 확인하는 입력이며, 전신·의상 조건은 포함하지 않는다.
- 새 생성 경로에서는 승인·검토 상태를 관리하지 않고, 입력 역할·실행 조건·입출력 해시를 `-result.json`에 기록한다. 카메라 회전 실행기의 기본 참조는 체스트 이미지다.
- 관련 자산: `p7-5-2-qwen-face-torso-chest-v1-seed-62294-steps-10.png`, `p7-5-2-qwen-torso-pitch-high-angle-chest-reference-v1-seed-62294-steps-8.png`.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
