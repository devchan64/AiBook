# Section Release Note

- Section ID: `P7-5.2`
- Source File: `docs/parts/part-07/chapter-05/section-02.md`

### v2026.07.12
- 추가 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다.
- 본문 반영: 검색 실패와 답변 실패 구분, 근거 부족 시 멈춤, 질문별 상태 기록, 환각 억제 규칙을 `체크리스트` 하나로 통합했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 retrieval metric 수식 비교, 자동 평가 프레임워크, reranker 구현 세부 같은 생략 목록을 걷어내고, 이번 절이 `검색 실패와 답변 실패를 어떻게 분리해 다음 수정 질문으로 바꿀 것인가`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 검색/답변 실패 기록의 후속 handoff를 `P7-7.2`로 직접 연결해 Part 7 전체의 공통 실패 기록 구조가 다시 보이게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-5.2 closes the RAG validation record structure first and hands broader failure planning to P7-7.2.

### v2026.07.18
- 추가 반영: Section 제목을 `검색 품질과 답변 검증`에서 `검색 실패와 근거 누락 기록`으로 바꾸고, 검색 단계와 답변 단계의 실패를 분리해 남기는 역할이 더 직접 드러나게 정리했다.
- 추가 반영: 문서 집합과 질문 집합 CSV 자산 경로를 본문 링크로 바꿔, 같은 지식베이스 위에서 질문 상태만 비교하는 구조가 더 직접 보이게 했다.
- 변경 이유: 기존 P7-5.2 예제는 `근거 부족` 상태는 보여 주지만, 검색은 되었는데 답변이 과장될 위험이 있는 경우까지는 충분히 드러내지 못했다.
- 본문 반영: P7-5.1과 같은 확장 문서 집합 위에서 `근거 기반 답변`, `근거 부족`, `답변 과장 위험`의 세 상태를 질문별로 구분해 기록하는 예제로 다시 썼다.
- 본문 반영: Python 예제와 결과 해석도 `MCP는 왜 필요한가?` 같은 문서 범위 밖 질문과 `문서 분할과 재정렬만 있으면 모든 환각이 해결되는가?` 같은 과장 위험 질문을 함께 다루도록 보강했다.
- 추가 반영: 평가표, 템플릿, 나쁜 기록/좋은 기록 대비도 3상태 구조에 맞춰 다시 정리했다.
- 추가 반영: 본문에 남아 있던 `작은 예제` 표현을 `이번 실습` 수준으로 정리해, 예제 축소감보다 검증 구조가 먼저 보이도록 맞췄다.
- 추가 반영: 문서 집합과 질문 집합을 각각 `docs/assets/part-07/chapter-05/p7-5-rag-documents.csv`, `docs/assets/part-07/chapter-05/p7-5-rag-questions.csv`로 분리하고, Python 코드는 두 파일을 읽어 상태별 평가를 수행하도록 정리했다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 질문 강도와 문서 범위를 바꾸며 `근거 부족`과 `답변 과장 위험` 상태가 어떻게 달라지는지 직접 실험하도록 보강했다.
- 추가 반영: P7-5.1 문서 집합이 10개 조각으로 확장된 뒤에도 같은 자산을 공유한다는 점이 어긋나지 않도록, 입력 규모 설명을 확장 문서 집합 기준으로 정리했다.
- 번역 동기화 메모: Future translations should preserve the stronger P7-5.2 example where retrieval failure and answer-overreach risk are recorded as separate states.
