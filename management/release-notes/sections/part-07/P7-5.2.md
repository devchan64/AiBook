# Section Release Note

- Section ID: `P7-5.2`
- Source File: `docs/parts/part-07/chapter-05/section-02.md`

### v2026.08.29

- P7-5.2에 실제 실행 조합을 소개하는 설명을 추가했다. `Qwen/Qwen-Image-Edit-2509`은 다중 이미지 편집, Nunchaku SVDQuant FP4 r128은 로컬 실행용 transformer, `Qwen-Edit-2509-Multiple-angles` LoRA는 2단계 착장의 yaw 보조, `controlnet_aux` OpenPose renderer는 정면 BODY_18 비례 guide 생성으로 역할을 분리했다. 1단계가 native ControlNet이 아니라 body-only OpenPose PNG를 일반 이미지 참조로 넣는 편집 경로라는 점도 명시했다. 다방향 OpenPose는 최종 전신 회전 입력으로 사용하지 않았으므로 원고의 5방향 스켈레톤 표를 제거하고, 정면 v7 맵 하나가 머리·몸통·다리 비율과 프레이밍만 맞추는 기준이라고 바로잡았다. 번역 반영 상태: 한국어 원문만 반영됨. 관련 자산: `p7_5_2_qwen_edit_outfit_stages.py`, 방향별 2단계 착장 `result.json`, `p7_5_2_generate_openpose_turnaround_relation_maps.py`. 원문 기준 버전: `v2026.08.29`.
- P7-5.7의 제목 변경에 맞춰, P7-5.2가 연결하는 범위를 ‘얼굴 정면과 얼굴 회전’에서 ‘얼굴 정면 identity와 캐릭터 멀티플 뷰 생성’으로 정리했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.29`.
- Section 제목을 `캐릭터 identity와 추가 페인팅으로 특징 완성하기`로 바꾸고, 얼굴 identity를 기준으로 전신 비례·기본 의상·재킷·동작을 이전 결과 위에 단계적으로 더하는 편집 경로라는 중심 질문을 도입에 명시했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.29`.

### v2026.08.26

- 원고를 입력 역할 중심으로 재배치했다. 제목과 도입을 얼굴·착장·전신 구조가 서로를 덮어쓰지 않게 하는 질문으로 바꾸고, 정면 1·2단계, 방향별 착장·OpenPose, 동적 앨리웁, 재현 기록 순으로 정리했다. 현재 운영하지 않는 승인·review 관리 표현은 입력 역할·관찰·비교 질문으로 교체했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.26`.

- 2단계 정면 전신 착장과 P7-5.7 정면 토르소를 두 이미지 입력으로 사용한 앨리웁 전신 동적 포즈(`1024×1536`, 20-step)와 `result.json`을 원고에 추가했다. 이 결과는 정면 기준을 대체하지 않는 장면·동작 실험으로 명시하고, 두 입력의 역할과 최소 동작 지시를 설명했다. 번역 반영 상태: 한국어 원문만 반영됨. 관련 자산: `p7-5-2-qwen-edit-fullbody-alley-oop-v1-seed-62294-steps-20.png`. 원문 기준 버전: `v2026.08.26`.

- 2단계 정면 착장을 유일한 이미지 입력으로 사용한 멀티플 앵글 LoRA 회전 기준 4장(−90°·−45°·+45°·+90°, `1024×1536`, 8-step)과 각 `result.json`을 원고에 추가했다. 폐기된 3·4단계 착장·전신 결합 표와 링크는 현재 원고에서 제거했다. 번역 반영 상태: 한국어 원문만 반영됨. 관련 자산: `p7-5-2-qwen-outfit-stage2-yaw_{minus_90,minus_45,plus_45,plus_90}-multiple-angle-v1-seed-62294-steps-8.png`. 원문 기준 버전: `v2026.08.26`.

- 3·4단계 생성 경로를 폐기하고, 해당 미추적 실험 PNG와 `result.json` 20개를 삭제했다. 현재 CLI에는 1단계와 2단계만 노출한다. 원고의 기존 방향별 비교 이력은 별도 자산으로 보존한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.26`.

- 2단계 원고 기준을 풀렝스 와이드 팬츠, 접혀 내려오는 셔츠 칼라, 열린 흰 크롭 재킷을 유지한 `960×1440`, 30-step v3 결과 및 `result.json`으로 교체했다. 3단계의 얼굴 없는 착장 생성은 이 최신 2단계를 입력으로 사용한다. 번역 반영 상태: 한국어 원문만 반영됨. 관련 자산: `p7-5-2-qwen-edit-prompt-style-outfit_stage2_jacket_face-long-trousers-folded-collar-v3-seed-62294-steps-30.png`. 원문 기준 버전: `v2026.08.26`.

- 1단계 원고 기준을 가는 허리와 스니커즈 윗부분까지 오는 풀렝스 와이드 팬츠를 적용한 `960×1440`, 30-step v4 결과 및 `result.json`으로 교체했다. 2단계 생성기는 이 최신 1단계를 착장 입력으로 사용한다. 번역 반영 상태: 한국어 원문만 반영됨. 관련 자산: `p7-5-2-qwen-edit-prompt-style-outfit_stage1_face_openpose-long-trousers-defined-waist-v4-seed-62294-steps-30.png`. 원문 기준 버전: `v2026.08.26`.

- 3단계 착장 기준을 머리 실루엣을 제거한 헤드리스 출력에서, 대머리 머리·귀·목은 남기되 헤어·눈·코·입이 없는 얼굴 없는 출력으로 교체했다. 원고의 자산 표, 설명, PNG와 `result.json` 링크를 새 `1024×1536`, 20-step 결과에 맞췄다. 이어 −90°·−45°·+45°·+90°의 8-step 회전 4장과 각 `result.json`도 같은 기준으로 교체했다. 방향별 전신 표의 −45°는 OpenPose를 제외하고 −45° 착장과 P7-5.7 토르소만 차례로 참조한 `1024×1536`, 30-step 비교 결과로 교체했다. 4단계의 현재 구조 입력은 얼굴 점군을 제외한 정면 body-only OpenPose로 정리했다. FACE_70은 턱선·이목구비 기하를 다시 지정해 토르소 얼굴형과 경쟁하므로 원고 연결과 4단계 입력에서 제외하고 실험 이력으로만 보존한다. 이어 원고의 정면 0° 구조 맵을 양팔을 바깥쪽 아래로 벌리고 손목을 몸통 밖에 남기는 BODY_18 v7로 교체했다. v7은 머리·어깨·골반 폭을 유지하고 다리만 15% 늘렸으며, 템플릿의 신체 부위는 전체 키 기준 정규화 비율로 정의한다. 나머지 4방향은 같은 비율로 다시 생성하기 전까지 기존 비교 맵을 유지한다. 1단계 기준은 같은 v7 구조와 가는 허리를 적용한 `960×1440`, 30-step 결과로 교체했고, 2단계는 이 최신 이미지를 착장 입력으로 사용한다. 번역 반영 상태: 한국어 원문만 반영됨. 관련 자산: `p7-5-2-qwen-edit-prompt-style-outfit_stage1_face_openpose-open-arms-short-long-legs-defined-waist-v3-seed-62294-steps-30.png`, `p7-5-2-qwen-edit-prompt-style-outfit_stage3_faceless_bald-faceless-bald-v1-seed-62294-steps-20.png`, `p7-5-2-qwen-faceless-bald-outfit-yaw_{minus_90,minus_45,plus_45,plus_90}-yaw-v1-seed-62294-steps-8.png`, `p7-5-2-qwen-fullbody-reference-yaw_minus_45-torso-outfit-only-v2-seed-62294-steps-30.png`, `p7-5-2-openpose-fullbody-stage2-open-arms-short-long-legs-v7-yaw+00_pitch+00.png`. 원문 기준 버전: `v2026.08.26`.

### v2026.08.25

- −45° 방향 전신을 `1024×1536`, 20-step으로 교체해 원고 표와 `result.json` 링크에 반영했다. 입력은 화면 방향을 맞춘 +45° body-only OpenPose, −45° 헤드리스 착장, −45° P7-5.7 토르소의 세 자산이며, OpenPose가 전신 원근·관절 구조를 먼저 맡도록 입력 순서를 기록했다. 번역 반영 상태: 한국어 원문만 반영됨. 관련 자산: `p7-5-2-qwen-fullbody-reference-yaw_minus_45-openpose-first-v2-seed-62294-steps-20.png`. 원문 기준 버전: `v2026.08.25`.
- 정면 3단계 기준에 더해 −90°·−45°·+45°·+90° 헤드리스 착장 PNG 4장을 원고에 추가했다. 네 자산은 얼굴·헤어를 제외한 재킷·회색 이너·양손·와이드 8부 팬츠·흰 스니커즈의 방향별 의상 조건만 맡으며, 이미지 표와 `result.json` 링크를 섞지 않았다. 번역 반영 상태: 한국어 원문만 반영됨. 관련 자산: `p7-5-2-qwen-headless-outfit-yaw_{minus_90,minus_45,plus_45,plus_90}-headless-outfit-yaw-v1-seed-62294-steps-8.png`. 원문 기준 버전: `v2026.08.25`.

### v2026.08.24

- 3단계 헤드리스 착장·방향별 P7-5.7 토르소·화면 방향을 맞춘 body-only OpenPose를 세 입력으로 사용한 −90°·−45°·+45°·+90° 전신 `1024×1536`, 30-step PNG와 `result.json`을 원고에 추가했다. OpenPose 투영의 화면 방향이 5.7 카메라 yaw와 반대이므로 부호를 교차 연결한 근거와 ±45° 팔 자세의 개별 검수 필요성을 함께 기록했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.24`.
- P7-5.2 원고에서 PNG와 `result.json`·좌표 JSON을 같은 표 셀에 두지 않도록 배치를 정리했다. 이미지는 전용 블록·이미지 표에 두고, JSON 링크는 바로 아래 한 줄로 분리해 여백을 줄였다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.24`.
- P7-5.2 Multiple-angles LoRA 전신 회전 생성기와 stage2·stage3 회전 PNG·`result.json`을 폐기하고, 원고의 회전 표와 코드 링크를 제거했다. 방향별 전신 구조는 body-only OpenPose로만 관리한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.24`.
- 양팔을 내린 공통 BODY_18 템플릿으로 −90°·−45°·0°·+45°·+90°의 방향별 body-only OpenPose PNG·좌표 JSON과 `result.json`을 생성하고 원고 표에 연결했다. 각 방향 스켈레톤은 회전 전신 결합에서 관절 구조를 맡길 수 있는 입력 후보다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.24`.
- 3단계 헤드리스 착장·P7-5.7 정면 토르소·정면 body-only OpenPose를 순서대로 입력한 `1024×1536`, 10-step 정면 전신과 `result.json`을 원고 기준으로 교체했다. 헤드리스 착장은 의상·양손, 토르소는 얼굴·헤어·화풍, OpenPose는 양팔을 내린 전신 구조를 맡긴다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.24`.
- 양팔을 내린 새 1단계를 입력으로 한 2단계 열린 재킷 전신 `960×1440`, 30-step PNG와 `result.json`을 원고 기준으로 교체했다. 긴소매 끝 아래의 양손과 흰 스니커즈가 프레임 안에 유지되는지를 결과로 확인한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.24`.
- 2단계 열린 재킷 전신을 입력으로 한 3단계 헤드리스 착장 `1024×1536`, 20-step PNG와 `result.json`을 원고에 추가했다. 머리·얼굴 없이 목깃·재킷·이너·양손·바지·신발을 보존해 착장 전용 참조로 분리한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.24`.
- 정면 BODY_18을 양팔을 자연스럽게 내린 대칭 서기 좌표로 재정의하고, 이를 입력으로 만든 1단계 착장 `960×1440`, 30-step PNG와 `result.json`을 원고의 1단계 기준으로 교체했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.24`.
- P7-5.2 body-only OpenPose를 정면 0° 기준 하나로 축소했다. −90°·−45°·+45°·+90°의 PNG·좌표 JSON과 다방향 manifest를 폐기하고, 원고의 5방향 표와 좌표 링크를 정면 구조 맵·좌표 JSON만 남도록 정리했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.24`.

### v2026.08.23

- 5방향 body-only OpenPose를 새 관절 좌표로 교체하고 원고에 반영했다. 오른손은 허리에 두고, 내린 왼팔은 팔꿈치를 몸통 가까이에 둔 뒤 손목을 몸 바깥으로 벌려 가방과 겹치지 않는 구조로 정리했다. 좌·우 상완과 전완은 같은 길이를 유지하며, 각 방향 PNG와 좌표 `result.json`을 갱신했다. 전신 생성기는 같은 5방향 파일을 입력으로 사용하도록 해당 포즈 계약을 코드 주석으로 명시했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.23`.

### v2026.08.22

- `p7_5_2_qwen_edit_fullbody_reference.py`를 폐기하고, 원고의 코드 링크를 Stage 1~3 정면 착장 전용 독립 생성기 `p7_5_2_qwen_edit_outfit_stages.py`로 교체했다. 두 생성기는 실행부를 공유하지 않는다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 2단계 전신과 P7-5.7 정면 머리만 참조하는 3단계 숄더백 전신을 원고에 추가했다. 기존 착장과 양손을 유지하면서 한쪽 어깨 아래에 단일 남색 여성용 언더암 숄더백을 더한 `960×1440`, `30 step` PNG와 `result.json`을 연결했다. 가방 형태·위치는 긍정 지시로만 지정했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 1단계 착장 결과와 P7-5.7 정면 머리만 사용하는 2단계 열린 자켓 전신을 원고에 추가했다. 2단계에서는 OpenPose를 다시 참조하지 않으며, 앞판이 분리된 흰 크롭 자켓·이너 소매 비노출·양손 노출·가방 및 스트랩 부재를 결과 이미지와 `result.json`으로 연결했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 원고의 기준을 1단계 전신 착장으로 교체했다. P7-5.7의 1024×1024 정면 머리 참조와 정면 BODY_18을 입력으로 하여, 회색 슬림 크롭탑·딥틸 여성용 와이드 8부 팬츠·흰 스니커즈만 포함한 `960×1440`, `30 step` 결과와 `result.json`을 연결했다. 자켓·가방·스트랩은 1단계 범위에서 제외했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 원고의 착장·가방 기준을 P7-5.7 정면 머리 입력과 `seed=62294`, `960×1440`, `30 step`으로 만든 목카라 흰 볼레로 재킷·회색 언더버스트 이너·단일 남색 크로스백 결과로 교체했다. 이전 착장 후보 PNG와 `result.json`은 제거했으며, 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 운영 방식 변경: P7-5는 이미지·guide·실행 결과에 승인·미승인·보류 상태를 부여하거나 이를 다음 단계의 gate로 관리하지 않는다. 이후 기록은 입력 역할, 생성 조건, 관찰된 일치·이탈, 알려진 한계와 다음 비교 질문을 남긴다. 아래의 승인·검수·후보 표현은 당시 이력이며 현재 운영 규칙이 아니다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- 원고의 착장·가방과 왼쪽 쿼터 OpenPose 링크에서 `review.json`을 제거하고, 실행·구조 기록은 `result.json`으로 통일했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.22`.
- P7-5.2 전신 생성기의 identity 입력 기본값을 P7-5.7에서 생성한 체스트 정면 참조로 바꿨다. 이 참조는 얼굴·헤어와 목·어깨 연결을 맡고, 착장·OpenPose는 기존처럼 별도 역할로 유지한다. 기존 전신 결과는 재생성하거나 교체하지 않았다.
- 전신 생성기 파일명을 `p7_5_2_qwen_edit_fullbody_reference.py`로 정리하고, 정면·좌우 쿼터·좌우 측면 기본 OpenPose 입력을 `p7_5_2_generate_openpose_turnaround_relation_maps.py`가 만든 오른손 허리 BODY_18 관계 맵 5방향 세트로 교체했다. 기존 중립 서기 guide는 이력 자산으로 유지한다.
- 원고에 오른손 허리 BODY_18의 −90°·−45°·0°·+45°·+90° 구조 맵과 각 방향 좌표 JSON 링크를 추가했다. 회전 결과는 동일한 템플릿의 관계 비교용이며, identity·화풍 입력이 아니다.
- `p7_5_2_generate_openpose_guide.py`는 폐기했다. P7-5.2 스켈레톤은 감지기가 아니라 정규화한 BODY_18 템플릿을 회전·투영하는 relation-map 생성기에서 재현한다.
- P7-5.7 체스트 identity·P7-5.2 착장·정면 허리 손 BODY_18을 결합한 960×1440, 30-step 정면 전신과 `result.json`을 원고의 전신 기준으로 연결했다. 10·20·40·50·60-step 비교 산출물은 보존하지 않는다.
- 전신 생성기의 identity 입력 기본값을 P7-5.7 체스트 참조에서 `p7-5-7-face-front-qwen-reference.png` 정면 머리 참조로 바꿨다. 머리 참조는 identity·헤어만 맡고, 목·어깨·전신 비례·의상·포즈는 각각 전신·착장·OpenPose 입력이 맡는다.
- 정면 머리 참조·착장 참조·정면 허리 손 BODY_18을 결합해 960×1440, 30-step 정면 전신을 재생성하고 원고의 기준 PNG와 `result.json` 링크를 새 실행 기록으로 교체했다.

### v2026.08.21

- 실행 기록 자산의 명칭을 `run.json`에서 `result.json`으로 통일하고, 승인 착장·정면 전신·4방향 전신의 원고 링크를 새 결과 파일에 연결했다. 검수 상태를 담는 `review.json`은 유지했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.21`.
- 얼굴 정면·회전의 원고, identity·화풍 계약, 정면 얼굴 기준, 얼굴 생성기와 다중 앵글 LoRA 생성기를 P7-5.7로 분리했다. P7-5.2에는 착장·가방·전신·body-only OpenPose만 남겼으며, 전신 생성은 P7-5.7의 정면 얼굴을 identity 입력으로 참조한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.21`.

### v2026.08.20

- 폐기한 FLUX·개별 프롭·구형 전신·5방향 OpenPose 자산과 검수 기록을 원고의 현재 기준 설명 및 링크에서 제거했다. 원고는 승인된 Qwen 정면 머리와 착장·가방, 왼쪽 쿼터 전신·body-only OpenPose의 역할을 분리해 설명하도록 재구성했다.
- 회전 얼굴 후보는 승인 자산이 아니라 후보 디렉터리와 run JSON에만 남기고, 방향·identity·헤어·화풍을 함께 사람 검수해야 한다는 기준을 명시했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.20`.
- 같은 정면 앵커에서 생성한 좌우 쿼터·좌우 측면 전신 PNG와 각 run JSON을 원고의 방향 대조 표에 연결했다. 좌측 측면의 기존 review JSON만 별도 검수 기록으로 표시했다.
- 정면·왼쪽 쿼터 body-only OpenPose를 전신 비례와 회전 구조를 대조하는 두 구조 맵으로 원고에 추가했다. identity·화풍·의상 기준이 아니라는 역할 경계를 함께 명시했다.
- 승인된 Qwen 정면 전신 착장 참조와 run JSON을 추가해, 정면 전신의 비례·프레이밍을 회전 전신 후보와 비교하는 기준을 명시했다.
- 현재 Qwen 참조셋 범위에 맞지 않는 구형 캐릭터 LoRA 데이터셋 준비 스크립트를 제거했다. LoRA 입력 구성은 새 승인 범위를 별도 설계·검수하기 전까지 사용하지 않는다.
- 구형 전신 얼굴·소품 보강 스크립트를 제거했다. 이후 전신 후보는 Qwen Edit의 입력 역할을 분리한 현재 생성기로만 새로 검수한다.
- 구형 얼굴 턴어라운드 OpenPose 추출 스크립트를 제거했다. 검수되지 않은 face landmark 후보는 회전 머리 생성 입력으로 사용하지 않는다.
- 정면 머리의 text-to-image 생성기를 Qwen Edit 후보 생성기에서 분리했다. 새 생성기는 이미지 입력 없이 정면 머리 후보와 실행 기록만 만들며, Edit 생성기는 참조 이미지를 쓰는 회전·전신 편집만 맡는다.
- 정면 착장·가방의 text-to-image 생성기도 분리했다. hip 착장과 전신 길이 착장을 선택해 후보·실행 기록을 만들고, Qwen Edit 생성기에는 이미지 참조를 쓰는 편집 target만 남겼다.
- 머리 회전 Qwen Edit 생성기를 분리했다. 정면 머리 identity는 항상 쓰고, OpenPose 등 구조 자산은 자동 생성하지 않으며 사람 승인 자산을 옵션으로 지정할 때만 추가한다.
- 후보 산출물은 실행별 하위 디렉터리를 만들지 않고 chapter asset 루트에 저장하도록 정리했다. 파일명에 생성기·target·실행 조건을 포함해 후보와 승인 자산을 구분한다.

### v2026.08.19

- Qwen 정면 전신 기준 재승인: 새 30 step 착장·가방 앵커, 승인 정면 얼굴, 프레임을 올린 `yaw 0°` body-only OpenPose를 세 입력으로 쓴 `seed=62294`, `960×1440`, `30 step` 후보를 안정 PNG·run JSON·review JSON으로 교체했다. 더 뚜렷한 외곽선·4단 음영·하이라이트 적용은 승인하되, 이 승인은 정면 전신에만 한정한다.
- Qwen 정면 전신 기준 재승인·교체: 승인 Qwen 얼굴·착장 앵커·프레임을 위로 올린 `yaw 0°` body-only OpenPose를 세 입력으로 사용한 `seed=62294`, `960×1440`, `30 step` 후보를 현재 안정 PNG·run JSON·review JSON으로 교체했다. 승인 범위는 정면 전신, 청록 단발·호박색 홍채, 흰 초단 크롭 재킷·회색 이너 탑·딥틸 와이드 팬츠·흰 스니커즈·남색 크로스백 재현에 한정하며, 원고 표의 이미지와 검수 링크도 갱신했다.
- Qwen 착장·가방 기준 재승인: 이미지 입력 없이 공통 일러스트 계약과 `seed=62294`, `768×1152`, `30 step`으로 생성한 의상 전용 후보를 승인해 안정 PNG·run JSON·review JSON으로 교체했다. 흰 초단 크롭 재킷·회색 이너 탑·딥틸 와이드 팬츠·흰 스니커즈·남색 크로스백은 복장 입력으로 사용하며, 앵커의 일반 얼굴·머리·자세는 identity·비례 기준이 아니다.
- 피치 0 5방향 body-only OpenPose 승인: `yaw −90°/−45°/0°/+45°/+90°`의 결정론적 7등신·한 손 허리 비대칭 서기 맵을 사람 승인해 각 PNG·좌표 JSON·검수 기록으로 등록했다. 수평 FOV `30.5°`와 카메라 거리 `10.8`을 코드 상수와 manifest에 함께 기록해 원근·전신 프레이밍의 재현 기준을 분리했다. 원고에는 5방향 contact sheet, manifest, 개별 review JSON과 생성 코드를 연결했다. 이 승인은 body-only 구조에 한정하며 캐릭터 identity·머리·착장·가방을 승인하지 않는다.
- FLUX로 생성해 승인했던 방향 얼굴·전신·리파인 이미지와 review JSON을 모두 제거했다. 해당 18장 기반 LoRA 입력 계획과 원고의 이미지·review 링크를 폐기하고, 새 방향 자산은 Qwen 기준에서 별도 검수 후 다시 등록하도록 정리했다.
- 인체 비율 도메인 적용: `SEVEN_HEAD_DEFAULTS`라는 단일 명칭을 `HUMAN_PROPORTION_PROFILES` 도메인으로 교체하고, 기본 `seven_head_standing` 프로필이 얼굴 높이·폭, 몸통·다리·팔 길이, 전체 신장을 BODY_18·70점 face 좌표에 실제 반영하게 했다.
- Qwen 좌측 측면 전신·구조 맵 승인: 정면 Qwen 얼굴, 승인 Qwen 착장 앵커, `yaw −90°`·`pitch 0°`의 결정론적 7등신 body-only OpenPose를 세 입력으로 사용한 `seed=62294`, `960×1440`, 30 step 후보를 좌측 측면 안정 PNG로 교체했다. 같은 구조 맵도 별도 사람 승인 기록으로 등록했으며, FLUX 출력은 이미지 입력으로 사용하지 않았다. 원고에는 입력 역할·승인 범위·두 review JSON 링크를 반영했다. 번역 동기화 메모: 한국어 원문만 반영됨.
- Qwen 복장 앵커·7등신 정면 전신 기준 승인: 정면 Qwen 얼굴, 이전 승인 Qwen 전신의 복장 역할, pitch 0도의 결정론적 7등신 body-only OpenPose를 세 입력으로 분리한 `seed=62294`, `960×1440`, 30 step 후보를 현재 정면 전신·자켓·가방 안정 PNG로 교체했다. FLUX 복장 참조는 이미지 입력으로 사용하지 않았으며, 원고의 입력 역할과 검수 링크를 새 구조에 맞게 갱신했다.
- 얼굴 회전 시트 OpenPose 추출기 추가: 외부 5×3 얼굴 방향 시트를 셀별로 분리하고, body·hand 없이 face landmark map만 후보로 저장하는 `p7_5_2_extract_face_turnaround_openpose.py`를 추가했다. 모든 출력은 `review_required`이며, 측면·하향 시점의 누락 또는 오배치 점군을 사람 검수한 뒤에만 구조 입력으로 사용한다.
- 정면 OpenPose 구조 기준을 원고에 반영: 익명 중립 정면 전신에서 추출해 사람 승인한 Full과 body-only 맵의 역할을 분리해 설명했다. Full은 body·face·hand 정합 검수용, body-only는 정면 전신 후보의 구조 입력용이며, COCO-18의 코·눈·귀 점만 남는다는 점과 어느 맵도 character identity·착장·화풍 원본이 아니라는 한계를 명시했다.
- Qwen 정면 전신·자켓·가방 기준을 v95로 승인·교체: 승인 Qwen 정면 머리·착장 통합 기준·body-only OpenPose를 분리 입력으로 쓴 `seed=62294`, `960×1440`, 30 step 후보를 현재 안정 PNG와 `review.json`에 등록했다. 10·20·30 step 비교에서 나머지 조건을 고정했으며, 이 target의 기본값을 30 step으로 설정했다. 승인은 정면 전신·착장·가방 재현 범위만 뜻한다.
- OpenPose 준비 코드를 하나로 정리: 익명 7등신 정면 source를 만드는 1단계와 `controlnet_aux` OpenPose 추출 2단계를 `p7_5_2_generate_openpose_guide.py`로 통합했다. body-only를 기본으로 두고 `--include-face`, `--include-hands`로 점군 출력을 선택하며, 기존 승인 v1 자산을 덮지 않는 v2 기본 출력과 `--overwrite` 보호를 추가했다.
- 승인 착장 통합 이미지를 폐기: 정면 전신 v95의 당시 입력 이력은 review 기록에 보존하되, 착장 안정 PNG·검수 기록·v87 후보와 실행 기록은 제거했다. 이후 Qwen 정면·측면 후보는 얼굴 identity와 OpenPose만 이미지 입력으로 쓰며, 의상·가방 조건은 prompt 계약으로 지정한다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.19`.

### v2026.08.18

- Qwen 정면 OpenPose 기준 승인: 중립 배경의 여성 정면 전신 비례 이미지를 `controlnet_aux` OpenPose 검출기로 추출한 Full(body·face·hand)과 body-only(COCO-18) 맵을 사람 승인해 안정 구조 자산으로 등록했다. 정면 전신 후보 생성은 기존 수동 좌표 후보 대신 승인 body-only 맵을 구조 입력으로 사용한다. 두 맵 모두 인물 identity·머리·착장·소품·화풍을 정의하지 않는다.
- Qwen 착장 통합 기준 승인·원고 반영: 이미지 입력 없이 만든 v71 `seed=62294`, `768×1152`, 10 step 착장 통합 후보를 별도 안정 PNG와 검수 기록으로 등록하고 원고에 연결했다. 초단 흰 크롭 재킷·긴소매·회색 이너·맨살 띠·하이웨이스트 딥틸 와이드 팬츠·흰 로우탑 스니커즈·오른쪽 어깨에서 왼쪽 골반으로 메는 단일 네이비 크로스백 스트랩을 승인 범위로 기록했다.
- Qwen 정면 머리·착장 기본값 조정: 현재 사람 승인 v70·v71과 같은 10 step을 `head_front`와 착장 통합 생성기의 기본값으로 설정했다.
- Qwen 착장 통합 기준을 v87로 교체: 사용자가 승인한 faceless·hairless 전시 마네킨 착장 후보를 안정 참조로 반영했다. 이 참조는 의상·가방 조건 전용이며, 캐릭터의 얼굴·머리·비율 조건으로 사용하지 않는다.
- Qwen 정면 머리 재승인: 이미지 입력 없이 만든 v70 `seed=62294`, `768×768`, 10 step 정면 얼굴을 사람 승인해 현재 Qwen 정면 기준으로 교체했다. 중앙 정면 구도·높은 콧대·곧은 코선·주황빛 홍채와 원형 동공·청록 단발을 승인 범위로 기록했으며, 수채화 공통 계약과 분리한 일러스트 계약의 4단 음영·하이라이트만 prompt로 사용했다.
- Qwen 정면 머리·전신 정면 갱신: 이미지 입력 없이 만든 v31 정면 머리(768×768, 20 step)를 사람 승인하고 현재 Qwen 정면 기준으로 교체했다. 이 기준 하나를 identity 입력으로 써 v32 전신 정면(960×1440, 20 step)도 승인했으며, 짧은 흰 크롭 재킷·손목까지 내린 소매·회색 이너 탑·피부 띠·하이웨이스트 와이드 팬츠·운동화·가방을 안정 자산과 원고에 반영했다.
- Qwen 전신 정면 승인: 승인 Qwen 정면 얼굴만 identity 입력으로 쓴 seed=62294, 960×1440, 20 step 후보를 사람 승인했다. 흰 크롭 재킷·회색 이너 탑·청록 와이드 팬츠·흰 운동화·남색 크로스백을 포함하는 별도 안정 자산과 검수 기록으로 등록했으며, 기존 FLUX 기반 전신 앵커는 덮어쓰지 않았다.
- Qwen 정면 기준 재승인: 화풍 계약에서 `Korean webtoon` 표현을 제거한 뒤, 이전 승인 Qwen 정면만 identity 입력으로 사용하는 v18 `seed=62294`, `768×768`, 20 step 후보를 사람 승인해 현재 정면 기준 PNG와 review 기록을 교체했다. 이전 Qwen 정면 입력은 Git revision으로 기록해 재현 이력을 보존한다.
- Qwen 정면 기준 승인: P7-5.1 공통 화풍 계약을 positive prompt로만 결합한 `seed=62294`, `768×768`, 20 step 정면 얼굴을 사람 승인해 별도 Qwen 안정 자산으로 등록했다. 30·40 step 후보는 승인하지 않고 제거했으며, Qwen 후보 생성기의 기본값은 20 step으로 명시했다.
- Qwen 정면 기준 갱신: P7-5.2 인물 전용 화풍 계약과 볼륨 단발 조건을 적용한 role-separated `seed=62294`, `768×768`, 20 step 정면 후보를 사람 승인해 현재 Qwen 기준으로 바꿨다. 이전 Qwen 정면 기준은 이력으로 보존하고, 같은 실행의 좌측 쿼터 후보는 승인하지 않았다.
- Qwen OpenPose 좌측 쿼터 승인: 승인 Qwen 정면 identity와 인물 RGB가 없는 선언형 얼굴·목 OpenPose 맵을 각각 입력으로 쓴 `seed=119431`, `768×768`, 30 step의 얕은 좌측 전면 쿼터를 사람 승인했다. Flux 방향 이미지는 사용하지 않았으며, OpenPose 맵은 회전 구조만 전달한다.
- identity 계약 정리: 눈 형태의 직접 비유와 큰 눈 지시를 제거하고, 자연스러운 비율의 위로 향한 아몬드형 눈매로 정의했다. Qwen 전환 후보도 같은 문구를 사용하며, 이전 실행 기록은 당시 조건의 재현 이력으로 보존한다.
- Qwen 전환 준비: P7-5.1 공통 화풍 계약을 후보 prompt에만 결합하고, 이미지 입력에서는 분리하는 정면 얼굴·전신 파일럿 계획과 생성기를 추가했다. 후보 PNG와 실행 기록은 사람 승인 전까지 커밋·manifest·안정 자산 교체 대상이 아니다.

### v2026.08.10
- 캐릭터 LoRA 역할 정리: P7-5.2는 사람 승인한 방향 얼굴 6장·기본 전신 6장·리파인 전신 6장을 직접 입력으로 준비한다. 이 기준 원본을 활용한 화풍 포함 스포츠 후보 생성·검수는 P7-5.4 실험으로 이관했으며, 후보 PNG와 review JSON은 사람 승인 전까지 학습 입력이 아니다.
- 캐릭터 LoRA 입력 준비: 사람 승인한 방향 얼굴 6장, 기본 전신 6장, 전신 얼굴·소품 리파인 6장을 새로 생성하지 않고 로컬 학습 입력으로 묶는 `p7_5_2_prepare_character_lora_dataset.py`를 추가했다. 스크립트는 PNG 복사 대신 `.tmp/`의 심볼릭 링크와 caption·SHA-256 manifest, 3열×6행 검수용 contact sheet를 만들며, 학습·pose·camera·장면 범위의 자동 승인은 하지 않는다.
- 원고에 18개 원본의 역할, `--plan-only` 검증과 실제 데이터셋 준비 명령을 연결하고, LoRA 학습·평가는 P7-5.4에서 별도로 수행한다는 경계를 명시했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.10`.

### v2026.08.09

- 얼굴·전신·리파인 기준 재승인: `seed=62294`로 정면 얼굴(`6 step`), 다섯 방향 얼굴(`3 step`), 다섯 방향 전신(`6 step`), 여섯 방향 전신 리파인(`3 step`)을 사람 승인하고 안정 PNG와 `review.json`을 교체했다.
- 생성 구조 단순화: 방향 전신은 승인 정면과 좌·우 방향 얼굴 시트를 쓰는 단일 단계로, 전신 리파인은 승인 전신·방향별 얼굴 PNG·통합 착장을 개별 입력으로 쓰는 단일 단계로 정리했다. 폐기된 2차 얼굴 보정, identity JSON 입력, 중간 후보와 옛 4방향 사례는 제거했다.
- 우측 전면 쿼터 리파인은 앞가슴의 단일 스트랩과 먼쪽 골반 뒤로 가려져 외곽만 보이는 가방을 승인 범위로 고정해 안정 자산을 교체했다.
- 원고의 생성 코드·승인 이미지·검수 JSON 배치를 현재 여섯 방향 구조에 맞춰 정리하고, JSON 링크는 일반 2열 표로 분리했다.
- 후보 파일명에서 계약 해시를 제거하고 프로세스별 6자리 실험 코드와 seed·step을 기록하도록 통일했다. 전체 입력 계약과 prompt는 실행 JSON에 보존한다.
- 번역 동기화 메모: 한국어 원문만 반영됨. 번역본은 여섯 방향 승인 구조, 우측 쿼터 가림 관계, 파일명 규칙을 동기화해야 한다.

### v2026.08.08

- 정면 전신은 `960×1440`, 전신 `3 step`과 얼굴 보강 `6 step` 결과를 승인해 방향 생성의 유일한 정면 앵커로 등록했다.
- 좌·우 전면 쿼터·좌·우 측면·후면 전신을 방향별 PNG로 승인했다. 당시 방향 생성은 1차 `3 step`, 2차 얼굴 identity `9 step`을 기본으로 사용했으며 후면은 얼굴 비노출 계약을 따랐다.
- 얼굴 기준을 정면과 좌·우 쿼터·좌·우 측면·후면의 개별 원본 PNG로 교체하고 CodeFormer 복원·`2x` 확대 의존을 폐기했다. 얼굴 공용 identity JSON은 정면·방향 얼굴의 prompt 계약으로 설명했다.
- 소품 기준은 회색 크롭탑, 바지, 신발, 흰 크롭 자켓 전·후면, 레이어 관계, 전·후면 통합 착장과 가방으로 정리하고 `seed=62294` 후보를 승인했다.
- 전신 리파인은 여섯 방향, `960×1440`, 기본 `3/3 step` 구조로 정리해 정면과 좌·우 쿼터·좌·우 측면·후면 안정 사례를 승인했다. 이후 단일 단계화의 근거가 된 2단계 실행 기록은 이 날짜의 이력으로만 남긴다.
- 정면 생성기와 방향 생성기를 분리하고, 원고의 방향별 이미지·라벨을 3열 배치로 정리했다. 미승인 후보, 구형 시트, 동적 앨리웁 실험 자산은 승인 체인에서 제외했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.08`.

### v2026.08.07

- 얼굴 턴어라운드와 방향 전신 생성기의 기본 seed를 정면 얼굴과 같은 `62294`로 통일했다. 이전 seed로 승인한 자산은 자동 재승인하지 않는다고 명시했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.07`.

### v2026.08.06

- 실제 파일이 없는 전체 캐릭터 manifest 대신 얼굴·소품·전신의 개별 승인 PNG와 검수 기록으로 사용 범위를 설명하도록 원고를 재구성했다.
- 얼굴 턴어라운드와 전신은 시트 출력에서 방향별 개별 PNG 생성으로 전환했다. 정면 얼굴을 앵커로 사용하며 결과는 얼굴 회전 또는 전신 방향 범위에서만 승인했다.
- 자켓 전·후면, 자켓-크롭탑 레이어, 전·후면 통합 착장과 가방의 가림 관계를 분리해 승인하고 방향별 전신 리파인 입력으로 연결했다.
- CodeFormer `2x` 얼굴 입력과 다중참조 전신 보강을 시험했으며, 정면·쿼터는 통과 후보를 얻고 측면·후면의 자켓 누락은 방향 전용 prompt와 소품 기준으로 보강했다. 이 구조는 이후 원본 얼굴 직접 참조 방식으로 교체됐다.
- 화풍 참조 농구 점프 예제는 P7-5.3 pose·scene 승인과 분리한 review-only 사례로 기록했으며, 이후 화풍 입력과 관련 자산은 현재 캐릭터 기준 체인에서 제외했다.
- 생성 리포트와 중간 후보는 저장소에서 제거하고, 승인 이미지·입력 기준·재실행 코드만 원고에 연결했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.06`.

### v2026.08.05

- 정면 얼굴에서 시작해 방향 얼굴·소품·정면 전신·방향 전신으로 확장하는 생성·검수 순서를 확정하고, 각 생성 기록에 전체 prompt와 `prompt_word_count`를 남기도록 통일했다.
- 머리핀을 폐기하고 얼굴형·홍채·단발을 기준으로 정면과 방향 얼굴을 승인했다. 회색 크롭탑·바지·신발·자켓·가방과 정면 전신도 기본 비례·복장 범위에서 승인했다.
- 방향별 prompt를 후면·측면·쿼터로 분기해 몸통·사지의 가림과 깊이를 보강하고, 최종 방향 PNG만 승인 자산으로 유지했다.
- 미승인 후보와 중간 단계 PNG를 제거하고, P7-5.1 화풍 원본과 P7-5.2 인물 기준의 역할을 분리했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.05`.

### v2026.08.04

- 정면 얼굴·방향 얼굴·정면 전신·방향 전신 생성기를 분리하고, 생성 소스와 사람 검수 기록을 lazy-source로 연결했다.
- 화풍 기반 초기 얼굴·전신·소품 후보를 비교한 뒤 contact sheet, 중복 생성기, 미승인 후보와 ControlNet 레이아웃 의존 자산을 정리했다.
- 사용자 노출 용어를 `master`에서 `기준`으로 변경했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.04`.

### v2026.08.03

- P7-5.2를 로컬 GPU 원본만 사용하는 캐릭터 참조 셋 단계로 재구성하고 외부 생성 서비스 원본·파생 학습·LoRA 실험을 근거 범위에서 제외했다.
- P7-5.1 화풍 승인과 P7-5.2 인물 identity 승인을 분리하고, 단일 기준 PNG가 P7-5.3의 pose·camera·장면 승인을 대신하지 않는다고 명시했다.
- 캐릭터 기준·표정·전신·train·held-out·실행·검수 기록의 역할과 출처 점검 기준을 정리했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.08.03`.
