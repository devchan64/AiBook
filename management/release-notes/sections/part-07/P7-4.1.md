# Section Release Note

- Section ID: `P7-4.1`
- Source File: `docs/parts/part-07/chapter-04/section-01.md`

### v2026.07.07
- 본문 반영: 도입 문장과 샘플별 출력 키를 `평가 샘플`, `예측 라벨`, `최근접 클래스 거리` 같은 독자용 설명형 표현으로 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-4.1 공개 출력 키를 텍스트 분류 흐름이 바로 읽히는 설명 레이블로 유지해야 한다.

### v2026.07.07
- 본문 반영: 입구 질문 표와 보조 표를 `어휘 기준`, `추가 검토 질문`, `기존 어휘 포함 비율`, `오류 샘플` 같은 한국어 중심 표현으로 정리하고, OOV는 보조 병기로만 남겼다.
- 번역 동기화 메모: 향후 번역본도 P7-4.1 초반 설명에서는 한국어 개념 이름을 먼저 두고 영어 공통 언어는 첫 병기로만 쓰는 편이 좋다.

### v2026.07.12
- 추가 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다.
- 본문 반영: 문장-토큰-벡터-라벨 흐름, 어휘 기준, 일반화 경계, 입력 표현 기록 항목을 `체크리스트` 하나로 정리했다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 BERT 파인튜닝, subword tokenizer 내부 구현, attention 기반 문장 표현, 대규모 라벨 데이터셋 같은 생략 목록을 걷어내고, 이번 절이 `문장 -> 토큰 -> 벡터 -> 라벨 예측`으로 텍스트 프로젝트 문서를 시작하는 기준을 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 토큰화 평가 handoff는 `P7-4.2`, 뒤 검색/평가 프로젝트 공통 입력 기록 handoff는 Part 7 뒤 장들로 이어지게 분명히 했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-4.1 closes the text-project start structure first and hands tokenization-aware evaluation to P7-4.2.

### v2026.07.18
- 추가 반영: Section 제목을 `텍스트 분류 학습 로그 읽기`에서 `loss, metric, 오류 사례 함께 읽기`로 바꾸고, 학습 로그보다 함께 읽어야 하는 판단 재료가 더 먼저 보이게 정리했다.
- 추가 반영: 고객 문의 CSV 자산 경로를 본문 링크로 바꿔, 입력 파일 설명과 실습 데이터 원문을 바로 연결했다.
- 본문 반영: 여섯 개 장난감 문장 기반 예제를 고객 문의 라우팅 실습으로 교체하고, `환불팀 vs 배송팀` 분류 문제로 실제 운영 문맥이 더 보이도록 다시 구성했다.
- 추가 반영: baseline, 팀별 점수, 샘플별 예측, 오류 샘플을 같은 실행 기록으로 묶어 `정확도`만이 아니라 `왜 이 문의가 이 팀으로 갔는가`를 읽을 수 있게 보강했다.
- 추가 반영: 실습용 고객 문의를 `docs/assets/part-07/chapter-04/p7-4-support-routing-dataset.csv`로 분리하고, Python 코드는 인라인 문장 배열 대신 `split` 열 기반 파일 읽기 구조로 바꿨다.
- 추가 반영: `입력 파일` 소절을 따로 세워 CSV 경로, 한 행의 의미, 핵심 열을 먼저 읽고 그다음 Python 예제로 넘어가도록 구조를 분리했다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 `캔슬 -> 취소` 같은 표현 변경과 혼합 문의 추가를 통해 토큰 선택이 팀 점수에 미치는 영향을 직접 실험하도록 보강했다.
- 추가 반영: 절 제목과 범위를 `학습 로그 읽기` 중심으로 다시 잡고, Part 5의 `epoch`, `step`, `update`, `loss`, `accuracy`를 텍스트 분류 실습 로그 위에서 다시 읽게 재구성했다.
- 추가 반영: class profile 점수표 예제를 epoch 기반 softmax 학습 예제로 교체하고, full-batch 학습 로그와 샘플별 확률 출력을 함께 남기도록 바꿨다.
- 추가 반영: `docs/assets/part-07/chapter-04/p7-4-training-log.csv`, `docs/assets/part-07/chapter-04/p7-4-learning-curves-ko.svg`, `docs/assets/part-07/chapter-04/p7_4_training_curves.py`를 추가해 손실 곡선과 기준선 대비 정확도 곡선을 재현 가능한 자산으로 보강했다.
- 추가 반영: 평가 정확도가 고정돼도 손실이 계속 내려가는 장면을 본문 해설과 그래프로 묶어, 숫자 나열 대신 학습 곡선 읽기 기준이 먼저 보이게 정리했다.
- 번역 동기화 메모: Future translations should preserve the stronger customer-support routing scenario and the baseline-vs-model reading structure rather than reverting to toy sentence examples.
