# Section Release Note

- Section ID: `P7-5.11`
- Source File: `docs/parts/part-07/chapter-05/section-11.md`

### v2026.09.16

- BFS 학습의 주기·종료 상태 저장과 별도 패키지 재개를 지원했다. 추가 스텝·누적 스텝을 구분하고 원래 데이터·설정 계승 및 상태 해시 검사를 반영했다.

- 공용 학습 코드에 5.12 편집 쌍의 고정 입력·목표 대응을 추가하고, 반복 목표의 캐시 이름을 쌍별로 분리했다. 기존 A/B 참조 선택은 유지한다.

- 편집 학습 데이터셋 193쌍의 입력·목표·분할을 확인하는 4열 검토표와 구성 JSON을 원고에 연결했다. 학습 174쌍·검증 19쌍, 고유 목표 41장과 미포함 추가 목표 123장을 구분했다.

- 목표 생성 불량 5건을 사용자 확정 폐기하고, 목표 생성 명령에 ID 제외 필터를 적용했다. 원래 생성 조건·코드는 보존하고 비교표·카탈로그를 123개로 갱신했다.

- 목표 후보 128장 검수 비교표를 생성 목록 옆에 연결하고 관리번호와 검수 범위를 안내했다. 생성 예정 표현을 생성 완료로 갱신했다.
- 관련 자산: `sec-11/target-candidate-review.md`, `sec-11/p7-5-11-target-review-v1.json`.
- 번역 반영 상태: 대응 번역본 없음.
- 원문 기준 버전: `v2026.09.16`.

### v2026.09.15

- 입력 표정 보존 조건에 따라 표정 불일치 19건을 사용자 확정 폐기하고 재생성을 금지했다. 총 폐기 27건·보관 193개로 비교표와 원고를 동기화했다.

- 배경 손실·변경 후보 5건을 사용자 확정 폐기하고 재생성 제외 목록에 추가했다. 총 폐기 8건, 보관 212개로 원고·비교표를 갱신했다. 원래 생성 항목·코드는 보존한다.

- 입력 후보 217개의 목표·입력 비교표 Markdown을 원고에 연결하고, 생성 순번에 고정한 관리번호와 현재 검수 의견을 표시했다. 폐기 3건의 번호는 보존한다. 번역 반영 상태: 대응 번역본 없음.

- 입력 후보 3건의 재생성 제외 목록과 이를 적용하는 생성 명령을 반영했다. 원래 생성 항목·코드는 보존한다.

- 생성 후보 디렉터리를 `sec-11/input-images/`와 `sec-11/target-images/`로 구분했다. 원본 해시를 유지하면서 카탈로그·기록·원고·생성 재개 경로를 갱신했다. 기존 기준 목표는 대응 목록의 경로로 연결하며 중복 복사하지 않는다. 번역 반영 상태: 대응 번역본 없음.

- 이미지 본문을 샘플 중심으로 정리했다. 기존 학습 40장 표는 대표 8장으로 줄이고, 같은 목표와 입력 변형 3장을 4열로 제시했다. 전체 자료는 JSON 목록으로 추적하고 생성 후보·학습 채택·본문 샘플 수를 구분했다. 5.12에서는 5.11의 공용 생성 자산과 샘플을 연결한다. 번역 반영 상태: 대응 번역본 없음.

- 학습용 참조 입력 후보 220장과 생성 기록·카탈로그를 `sec-11/candidate-inputs-v2/`로 통합했다. 원본 이미지 해시를 보존하고 현재 경로와 기록 해시를 갱신했으며, 원고의 실행 경로·폴더 안내를 동기화했다. 공용 생성·선택 Python과 참조 입력 생성 조건 JSON도 5.11 자산으로 통합했다. 조건 파일은 `p7-5-11-bfs-input-*.json`으로 이름을 맞추고 원고·재실행 경로를 갱신했다. 후보의 학습 채택은 별도 검수로 결정한다. 번역 반영 상태: 대응 번역본 없음.

### v2026.09.14

- A/B의 800·1600스텝 평가 이미지 12장을 정면·카페·정원 비교표에 반영했다. 전체 30장의 생성·해시 확인 완료와 신규 12장의 시각 검수 대기를 구분하고 원본 실행 계획·결과 기록을 연결했다. 번역 반영 상태: 대응 번역본 없음.

- 입력 후보 220장과 후속 Mira 목표 후보 128장의 생성 계획을 분리하고, 공용 생성 코드에 GPU 대기·부분 실행·후보 카탈로그를 추가했다. 별도 검수 목록에서 채택한 결과만 데이터셋 JSON으로 내보내며 해시·중복·목표별 분할 누수를 검사한다. 계획 수와 실제 생성·채택 수를 구분했다. 번역 반영 상태: 대응 번역본 없음.

- A/B를 같은 목표 40장·캡션에서 학습 참조만 달리하는 구성으로 설명하고, 참조·지시·목표의 역할과 실제 첫 학습 쌍을 학습 설정 앞에 배치했다. B도 Mira 목표 40장을 학습한다는 점, 단색 평가와 학습 조건의 차이, 관찰과 원인 가설의 경계를 명시하고 후반의 중복 설명을 정리했다. 번역 반영 상태: 대응 번역본 없음.

### v2026.09.13

- 원고와 채택 코드에서 사용하지 않는 `p7_5_11_probe_lora.py`와 상위 호환 링크를 삭제하고 모델 인벤토리의 probe 소스 등록을 제거했다. OpenPose 준비 코드는 별도 작업에서 P7-5.16으로 이관했다.
- 원고에서 직접 참조하는 자산만 정리 대상으로 제한했다. 이미 sec-11에 있는 실파일의 기존 호환 링크 73개를 제거하고, 미참조 자산의 호환 링크 77개는 유지했다. 기존 실파일의 위치는 변경하지 않았다.
- 초반을 후보 수집·검토 → 추가 생성 → 결과 검수·학습 목록 확정 → 캡션·참조 구성 → 학습 설정·패키지 준비 → 학습·평가 순서로 재배치했다. 기존 후보 56장, 기존 선정 12장, 추가 생성 40장, 최종 학습 40장의 의미를 각 단계에서 설명하고 코드·목록 링크를 사용하는 위치에 배치했다. 번역 반영 상태: 대응 번역본 없음.

- 자료 안내는 원고의 폴더별 용도 설명과 학습 이미지 40개 목록 JSON으로 통합하고 README 연결을 제거했다. 학습 목록에는 원본 경로·캡션·해시를 담고 검증·보류 후보를 제외했다. 목록 확정 문단에 기존 12장과 추가 생성 28장 전체를 번호·조건 라벨이 있는 4열 이미지 표로 배치했다. 번역 반영 상태: 대응 번역본 없음.

- Python 작성 가이드에 맞춰 원고 사용 코드 3개의 조작 변수·관찰 출력·주요 단계 주석과 가독성을 보강했다. 생성 기본값을 v2로 맞추고 평가 체크포인트·해시를 필수 입력으로 바꿨다. 모델 인벤토리의 소스 경로와 본문 실행 명령을 동기화했다. 번역 반영 상태: 대응 번역본 없음.

- 5.11 전용 자산의 보관 위치를 `sec-11/`로 통합했다. 앞서 보류한 자료와 남은 실행 코드도 이동하고 원고·관리 문서·모델 인벤토리 경로를 갱신했다. `training-images/`는 학습용 PNG 28개만 유지하며, 원본 기록의 해시와 기존 실행 경로의 호환 링크를 보존했다. 번역 반영 상태: 대응 번역본 없음.

- 5.9 표정 자산의 sec-09 이동에 따라 학습 참조 경로와 실행 기록 검증용 해시를 갱신했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.13`.
- 5.2 자료의 sec-02 이동에 따라 원고와 생성 코드의 참조 경로를 갱신했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.13`.
- 데이터 생성·검수·패키지 준비·학습·얼굴 없는 평가 코드와 실행 순서를 안내했다. 학습 40개·평가 4개의 자료와 평가 이미지·원본 기록을 `training-images/`에서 관리하고 이미지 3열 비교를 유지하고 JSON·Python·JSONL 파일 링크를 각각 독립된 행으로 분리했다.
- 최초 A100 정면·장면 평가와 LoRA 강도 변경 실험의 본문·관리 문서 설명을 삭제했다. 정면 1장과 40장 비교는 목표별 반복량 차이를 우선 설명하고 참조 효과의 근거에서 제외했다.
- A/B 실험을 100·200·400·800·1600스텝의 총 30장 비교로 통합했다. 400스텝 단독 비교의 중복을 제거하고 장면별 `스텝 | A | B` 3열로 정리했다. 검수 완료 18장과 대기 12장, 400/1600 학습 실행 경계, 특징 출현과 목표 품질의 차이를 통합 계획·결과 목록·관리 문서에 반영했다.
- 번역 반영 상태: 대응 번역본 없음. 관련 자산: `p7-5-11/datasets/`의 생성 기록·학습 구성 대조·검수 JSON과 평가 코드.

### v2026.09.12

- 사용자 요청으로 삭제한 실험 출력을 재생성하기 위해 40개 조건의 v2 목록을 추가했다. 공통 외형 묘사를 제거하고 변경 대상·보존 범위만 지시하며 측면 참조 교정을 통합했다. 번역 반영 상태: 대응 번역본 없음.

- 보충 본 배치 40개·측면 교정본 1개의 생성 완료를 반영했다. 사용자 오류 목록 31개를 반려로 기록하고 미지정 9개·교정본 1개는 검수 대기로 유지했다. 이목구비·홍채색·피부색 변화로 학습 승인 0장임을 명시했다. 번역 반영 상태: 대응 번역본 없음.

- 2511 보충 후보 40개 JSON과 순차 생성·해시 확인 재개 코드를 추가했다. 학습 후보 32개·평가 전용 8개이며 실제 실행은 CUDA 미인식으로 모델 로드 전에 중단됐다(0/40). 번역 반영 상태: 대응 번역본 없음.

- 5.2 원본 17장을 재검수해 우선 12장·보완 4장·중복 예비 1장으로 재정의했다. 측면을 학습 후보로 복구하고 신규 보충을 의상·배경·조명·사선 표정 조건으로 좁혔다. 전처리·최종 분할·학습은 미실행. 번역 반영 상태: 대응 번역본 없음.

- 정체성 적합성과 중복·구도·검증 배치를 혼동한 기존 24/7/25 분할의 확정을 철회했다. 56장을 재검수 대상으로 전환하고 해당 분할을 학습 승인 근거에서 제외했다. 전신·강한 표정만으로 부적합을 단정하지 않도록 기준을 정정했다. 번역 반영 상태: 대응 번역본 없음.

- 후보 수집·검수 목록 검사·참조/목표 JSONL·TOML 준비와 고정 Musubi 커밋의 2511 캐시·LoRA 학습 실행 코드를 추가했다. 기본 실행은 계획만 출력하며 실제 학습은 별도 옵션으로 실행한다. 그룹 누수·중복·해시 변경·덮어쓰기 방지 등 CPU 테스트를 수행했다. GPU 학습은 미실행. 번역 반영 상태: 대응 번역본 없음.

- Mira 얼굴·헤어를 새 장면에서도 유지하는 2511 캐릭터 LoRA 학습 준비 절을 신설했다. 5.2·5.9 후보 선정, 편집 참조·목표 구성, 소규모 실행 조건과 미적용 비교를 정리했다. 신설 당시 데이터 선정·학습·가중치 생성은 미실행이었으며 설정은 작업 가설로 구분했다. 번역 반영 상태: 대응 번역본 없음.
