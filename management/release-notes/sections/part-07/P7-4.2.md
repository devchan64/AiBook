# Section Release Note

- Section ID: `P7-4.2`
- Source File: `docs/parts/part-07/chapter-04/section-02.md`

### v2026.07.07
- 본문 반영: 검토 요약과 샘플별 출력 키를 `사전에 없는 토큰`, `기존 어휘 포함 비율`, `검토 대상 샘플` 같은 설명형 표현으로 바꾸었다.
- 번역 동기화 메모: 향후 번역본도 P7-4.2 공개 출력 키를 독자용 용어로 유지하고 OOV 설명은 본문 병기로 보완해야 한다.

### v2026.07.07
- 본문 반영: 초반 질문 표, 평가 메모 표, 예제 목적과 기대 출력 문장을 `기존 어휘 포함 비율`, `사전에 없는 토큰`, `검토 대상 문장`, `토큰화 방식` 같은 한국어 중심 표현으로 다시 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-4.2 초반 설명에서는 한국어 개념 요약을 먼저 두고 영어 병기는 보조 장치로 유지해야 한다.

### v2026.07.07
- 본문 반영: 후반 표, 회고 예시, 기억할 관점, 적용 상황, 점검 문장을 `어휘 수`, `토큰화 규칙`, `오류 사례`, `기존 어휘 포함 비율`, `토큰화 방식 변경` 같은 한국어 중심 표현으로 다시 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-4.2 후반부에서는 초반과 같은 한국어 중심 용어 체계를 유지하고 영어 공통 레이블은 병기로만 제한하는 편이 좋다.

### v2026.07.12
- 추가 반영: 분산된 회상·적용 시점 표지를 하나의 `체크리스트` 아래로 통합했다.
- 추가 반영: `Part 5와의 연결`을 `토큰화와 OOV를 다시 읽는 기준`으로 바꿔 현재 절에서 무엇을 다시 읽어야 하는지 제목에 직접 드러나게 했다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 subword tokenizer 병합 규칙, BPE/WordPiece/SentencePiece 내부 비교, precision/recall/F1 심화 계산 같은 생략 목록을 걷어내고, 이번 절이 `정확도 숫자를 입력 표현 기록과 함께 읽는 최소 평가 구조`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 입력 표현 평가의 후속 handoff를 `P7-5.2`, `P7-7.2`로 직접 연결해 Part 7 전체의 공통 검토 기록 구조가 다시 보이게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-4.2 closes the tokenization-aware evaluation record first and hands richer retrieval/failure writing to later project sections.

### v2026.07.18
- 추가 반영: P7-4.1과 공유하는 CSV 자산 경로를 링크로 바꿔, coverage/OOV 평가가 같은 데이터 파일 위에서 이어진다는 점이 더 직접 보이게 했다.
- 본문 반영: `기존 어휘 포함 비율`과 `사전에 없는 토큰` 설명을 고객 문의 라우팅 사례로 다시 풀어쓰고, 같은 low-coverage 샘플도 하나는 오답, 하나는 정답인 장면을 함께 두어 해석 기준을 더 실제적으로 보강했다.
- 추가 반영: `캔슬 후 송장 번호 남아 있어요` 같은 표현 불일치 사례를 중심으로, 오답을 단순 모델 실패가 아니라 표현 불일치와 OOV 문제로도 읽어야 한다는 점을 회고 문장까지 포함해 강화했다.
- 추가 반영: P7-4.1과 같은 `docs/assets/part-07/chapter-04/p7-4-support-routing-dataset.csv`를 읽도록 예제를 정리해, 문의 데이터와 토큰화 평가 코드를 분리하고 같은 데이터 파일 위에서 검토가 이어지게 맞췄다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 동의어 치환과 새 표현 추가를 통해 coverage와 검토 우선순위가 어떻게 달라지는지 직접 실험하도록 보강했다.
- 번역 동기화 메모: Future translations should preserve the low-coverage contrast cases and the distinction between low coverage as a review signal versus an immediate wrong-answer rule.
