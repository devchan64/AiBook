# Section Release Note

- Section ID: `P7-4.2`
- Source File: `docs/parts/part-07/chapter-04/section-02.md`

### v2026.07.20
- Python 예제 주석 보강: 고객지원 라우팅 실패 샘플의 OOV token, vocabulary coverage, 팀별 점수 분해 목적을 코드 블록 첫 줄 주석으로 명시했다.
- 저작권 검토: Part 7 Chapter 4 모듈 순차 검토 범위에서 실패 분해 설명, OOV·coverage 예제, NumPy 기반 분석 예제, 문의 라우팅 synthetic 데이터, Mermaid 흐름도, 체크리스트를 확인했다. 데이터와 도식은 Part 7 실습을 위해 구성한 자체 예시로 판단했다.
- 출처 사용 점검: NumPy 공식 문서는 참고 자료로 명시했고, 외부 문장을 직접 인용하지 않았다. 외부 도표·표·데이터 재사용은 확인하지 않았다.
- 관련 자산: `docs/assets/part-07/chapter-04/p7-4-support-routing-dataset.csv`, `docs/assets/part-07/chapter-04/p7-4-2-coverage-case-flow-ko.mmd`, `docs/assets/part-07/chapter-04/p7-4-2-failure-split-flow-ko.mmd`
- 본문 반영: 제목 아래 메타데이터를 저장소 공통 형식인 평문 `Section ID`, `Version` 두 줄로 바로잡고, 본문 `Version`을 `v2026.07.20`으로 갱신했다.
- 번역 동기화 메모: 대응 번역본이 생기면 메타데이터 형식과 한국어 Mermaid 자산의 언어별 파생본 생성 여부를 함께 확인해야 한다.

### v2026.07.19
- 변경 이유: Part 7 연습 Section의 학습밀도 보강 이력을 원고 가이드 기준에 맞춰 압축 정리했다.
- 본문 반영: 범위·목표·연습 표지와 본문 안의 절 진행 지시 표현을 내용형 기록 기준으로 바꾸고, 실행 기록·결과 해석·프로젝트 기록 예시가 설명과 해설 안에서 읽히도록 정리했다.
- 번역 동기화 메모: 향후 번역본도 진행 안내 제목보다 Section별 질문, 판단 기준, 실행 결과 해석, 프로젝트 기록 예시가 먼저 보이게 맞춘다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 원문 기준 버전: `v2026.07.19`

### v2026.07.07
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 검토 요약과 샘플별 출력 키를 `사전에 없는 토큰`, `기존 어휘 포함 비율`, `검토 대상 샘플` 같은 설명형 표현으로 바꾸었다. / 초반 질문 표, 평가 메모 표, 예제 목적과 기대 출력 문장을 `기존 어휘 포함 비율`, `사전에 없는 토큰`, `검토 대상 문장`, `토큰화 방식` 같은 한국어 중심 표현으로 다시 정리했다. / 후반 표, 회고 예시, 기억할 관점, 적용 상황, 점검 문장을 `어휘 수`, `토큰화 규칙`, `오류 사례`, `기존 어휘 포함 비율`, `토큰화 방식 변경` 같은 한국어 중심 표현으로 다시 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-4.2 후반부에서는 초반과 같은 한국어 중심 용어 체계를 유지하고 영어 공통 레이블은 병기로만 제한하는 편이 좋다.

### v2026.07.12
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 분산된 회상·적용 시점 표지를 하나의 `체크리스트` 아래로 통합했다. / `Part 5와의 연결`을 `토큰화와 OOV를 다시 읽는 기준`으로 바꿔 현재 절에서 무엇을 다시 읽어야 하는지 제목에 직접 드러나게 했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: `이 절의 범위`에서 subword tokenizer 병합 규칙, BPE/WordPiece/SentencePiece 내부 비교, precision/recall/F1 심화 계산 같은 생략 목록을 걷어내고, 이번 절이 `정확도 숫자를 입력 표현 기록과 함께 읽는 최소 평가 구조`를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다. / 입력 표현 평가의 후속 handoff를 `P7-5.2`, `P7-7.2`로 직접 연결해 Part 7 전체의 공통 검토 기록 구조가 다시 보이게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-4.2 closes the tokenization-aware evaluation record first and hands richer retrieval/failure writing to later project sections.

### v2026.07.18
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 말미 체크리스트를 `점수`, `입력 표현`, `오류 샘플`, `재검토 신호`, `다음 개선` 축의 확인 표로 압축해 정확도 이후의 실패 해석 순서가 더 직접 보이도록 정리했다. / 본문의 두 Mermaid 도식을 `docs/assets/part-07/chapter-04/*.mmd` 자산으로 분리하고, Chapter 4 자산 폴더 README에 실패 분해 도식 운영 범위를 기록했다. / `평가-05`, `평가-07`을 함께 보며 `같은 low-coverage라도 오답과 재검토 후보가 갈린다`는 사례 문단을 보강하고, 바로 아래 Mermaid 흐름도로 coverage 해석의 판단 순서를 더 직접 보이게 했다. 등으로 압축 정리했다.
- 번역 동기화 메모: Future translations should preserve that P7-4.2 now frames coverage and OOV as one branch inside a broader failure-decomposition procedure, not as a standalone tokenization explainer.
