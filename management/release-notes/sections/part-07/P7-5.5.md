# Section Release Note

- Section ID: `P7-5.5`
- Source File: `docs/parts/part-07/chapter-05/section-05.md`

### v2026.09.13

- 공통 모듈 의존성을 없애고 각 생성기에 필요한 경로·전처리·해시·환경 기록을 인라인으로 옮겼다. 공통 모듈과 원고 링크를 삭제했으며 새 실행은 각 생성기의 소스 해시로 추적한다. 기존 실행 JSON의 해시는 유지한다. 번역 반영 상태: 한국어 원문에 적용.

- 단계별 소스 15개와 공통 모듈 1개의 원고 연결을 대조했다. 누락된 스크립트 파일 링크는 없으며, 15개 Python 링크에 빠진 lazy-source 속성을 추가해 코드 펼쳐보기를 적용했다. 번역 반영 상태: 한국어 원문에 적용.

- 폐기된 pose_identity 생성 실행부를 삭제하고 사용 중인 함수·상수만 공통 모듈로 분리했다. 12개 생성기의 import와 코드 해시 경로를 변경하고 공통 소스 링크를 추가했다. 번역 반영 상태: 한국어 원문에 적용.

- 분리 재현 코드와 설정에서 기존 투명 PNG 기준 비교를 제거했다. 새 출력의 알파·원본 픽셀 검증과 기존 마스크·흰 배경 비교는 유지하며 원고의 검증 설명을 맞췄다. 번역 반영 상태: 한국어 원문에 적용.

- BFS 생성기 두 개의 머리 크롭 PNG·JSON 경로를 section-05/로 수정하고, 인물 분리 검증 JSON을 지정한 출력 폴더에 직접 저장하도록 경로를 수정했다.

- 전체 원고의 직접 참조와 대조하여 미참조 Python 소스 8개와 result JSON 58개를 추가 삭제했다. 원고에서 참조하는 Python 소스 16개와 result JSON 40개는 유지했다.

- 사용자 지시에 따라 보존 기준을 원고의 직접 이미지 참조로 좁혔다. docs Markdown 전체와 대조해 5.5 폴더의 미참조 PNG 83개를 삭제하고 원고 참조 이미지 42개를 유지했다. 이번 삭제는 이미지에 한정하며 과거 실행 JSON·스크립트에는 삭제된 파일 경로가 남을 수 있다.

- 원고·코드·설정·관리 문서의 참조와 입력 의존성, 이미지의 짝 실행 기록을 추적해 미참조 에셋 7개를 삭제했다. 반복 Relight 결과 PNG·JSON 1쌍, Q4 스타일 계약 실행 JSON 3개, 미참조 Python 2개를 제거했다. 249개 파일은 참조 관계에 따라 유지한다.

- 5.5 전용 이미지·JSON·도식·Python 파일 256개를 assets/part-07/chapter-05/section-05/로 이동하고 원고·스크립트·모델 인벤토리 경로를 갱신했다. 기본 생성 출력도 새 폴더로 지정했다. 기존 결과의 경로는 새 위치로 대응하고 생성 당시 코드 해시는 실행 이력으로 유지한다. 번역 반영 상태: 이번 변경은 한국어 원고에 적용.

### v2026.09.11

- 단계별 입력·결과·보존 한계를 연속 배치하고 재현 명령과 공통 실행 안내를 뒤로 모았다. 외형 적용 경로와 반복 설명을 압축하고 B 얼굴·C 소품의 단계별 비교 과제를 추가했다. 기존 이미지·JSON·명령은 유지했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.11`.

- 체크리스트를 13개 제작·검수 항목에서 파이프라인 흐름, 입력 역할, 결과 관찰을 확인하는 학습 질문 3개로 줄였다. 단계별 세부 설명과 참조 자산은 유지했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.11`.
- 출처와 참고 자료를 작성자·자료명·게시처·URL·확인일 중심으로 통일하고, 실행 조건·검수 설명과 구분했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.11`.
- Python 스크립트 실행 명령을 `bash` 코드 블록으로 정리했다. 기존 물결표 울타리를 백틱 울타리로 통일하고 명령 내용은 유지했다. Bash 구문을 확인했으며 GPU 생성은 실행하지 않았다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.11`.

- 도입을 전체 파이프라인 설명으로 줄이고, 아이덴티티 정의·대상별 경로 표를 분리 결과 뒤로 옮겼다. 분리·마네킨·BFS의 코드를 결과 앞에 배치하고 C 두 인물의 책 보강을 묶었다. 체크리스트도 처리 순서에 맞춰 재배치했다. 기존 이미지·코드·JSON 링크는 유지했다. 번역 반영 상태: 한국어 원문만 반영됨.

- 도입부에 캐릭터·배경의 분리 처리부터 DeLight·합성·텍스트 Relight까지 이어지는 자체 제작 Mermaid 도식을 추가했다. 화살표의 입력·출력 의미와 C 조연의 BFS 생략, 조명 텍스트의 출처를 설명했다. 번역 반영 상태: 한국어 원문만 반영됨.

- 본문을 채택 결과가 다음 단계 입력으로 이어지는 단방향 파이프라인 중심으로 정리했다. BFS 스텝·해상도 비교와 참조 방향 변경, B·C 직접 적용 및 두 이미지 Relight 비교를 본문에서 제거하고 실제 처리 경로의 이미지·JSON·재현값·한계만 유지했다. 실험 자산과 이력 파일은 보존하며 중심 학습 산출물을 갱신했다. 번역 반영 상태: 한국어 원문만 반영됨.

- 출처와 참고 자료를 인접 원고와 같은 목록 형식으로 통일했다. 출처 7건의 제작자·제목·URL·확인일과 근거 설명을 유지하고, 2509 기본 모델과 Relight LoRA를 개별 항목으로 구분했다. 번역 반영 상태: 한국어 원문만 반영됨.

- 미보존 비교 근거를 로컬 GPU에서 재생성했다. C 엘리베이티드 머리 크롭·BFS 10스텝, B 로우뷰 +45° BFS 30스텝, B 두 이미지 Relight 10스텝의 새 이미지·JSON을 원고에 연결했다. B 얼굴 불일치의 개선 부족과 Relight 긴 머리·추가 신발 형태를 재검수했다. Relight 생성기에 비교 옵션을 추가하되 기본 단일 이미지 경로는 유지했다. 번역 반영 상태: 한국어 원문만 반영됨.

- 직접 아이덴티티 적용 결과 네 장의 개별 실행 JSON 링크를 추가했다. C 엘리베이티드 BFS·B 30스텝 BFS·두 이미지 Relight 비교는 결과와 JSON을 현재 보존하지 않은 당시 관찰임을 명시했다. 번역 반영 상태: 한국어 원문만 반영됨.

- 1차 출처 7건을 재확인하고 단계별 근거 표로 정리했다. BFS V5 입력 순서와 DeLight 야외 배경 변화 안내를 연결하고 실행 기록·육안 관찰·모델 설명의 범위를 구분했다. 관리노트의 폐기된 5.5 경로를 현행 흐름으로 갱신했다. 번역 반영 상태: 한국어 원문만 반영됨.

- 검수 결과를 반영해 A 직접 적용, B·C 마네킨 경유, C 조연의 별도 처리 경로를 표로 정리했다. BFS의 역할과 머리 크롭·마스크의 차이를 설명하고, 착장 1차 단계의 범위·B 얼굴 불일치·C 두 인물의 책 유지 여부를 체크리스트에 반영했다. 번역 반영 상태: 한국어 원문만 반영됨.

- 리라이트 결과 뒤의 이전 BFS·DeLight·Relight 테스트 흔적 절을 삭제했다. 해당 절을 안내하던 도입 문장과 체크리스트를 제거하고 출처 설명을 현재 실행 흐름에 맞췄다.

- 합성 결과 한 장과 최초 씬을 보고 작성한 텍스트 조명 지시로 실행한 Relight `text-v2` A/B/C 결과·코드·개별 JSON을 반영했다. Qwen Edit 2509와 dx8152 LoRA 조건, 모델 입력과 프롬프트 근거의 분리, B 가장자리 빛 과장 및 세부 변화의 한계를 기록했다.

- DeLight 배경·캐릭터의 장면별 합성 코드와 A/B/C 10스텝 결과·개별 JSON을 반영했다. A/B 2장·C 3장의 참조 순서와 배치 프롬프트, 재현 명령을 연결하고 A/C 배경 어두워짐 및 B 채도·대비 증가를 검수 관찰로 기록했다.

- 채택 배경판 A/B v1·C v2에 Studio DeLight를 적용한 생성 코드·10스텝 이미지 3장·개별 JSON과 재현 명령을 반영했다. 주변 대상 유지와 대비 감소, A 구름 및 B 노을·태양 소실, C 색조 변화를 기록했다.

- P7-5.4 최종 장면에서 편집 대상 인물과 C의 책을 제거한 배경판 생성 코드·A/B v1 및 C v2 이미지·개별 JSON을 반영했다. 주변 대상 유지 기준, C의 새 추가 오류와 압축 프롬프트 재실행, 가려진 배경을 새로 생성한다는 한계를 명시했다.

- Mira A·B·C의 채택 BFS 결과와 책을 보강한 C 조연에 Studio DeLight를 적용한 코드·10스텝 이미지 4장·개별 실행 JSON을 반영했다. 이미지를 한 표에 배치하고 재현 명령과 입력 흐름을 연결했으며, 그림자는 검수 대상에서 제외했다.

- C 조연의 책 누락에 대응하는 후속 흐름을 아이덴티티 적용 → 책 보강 → DeLight로 명시했다. C Mira의 책 보강과 입력·출력을 구분한다. 조연 책 보강 20스텝 결과·코드·실행 JSON과 재현 명령을 연결했다.

- A·B·C 머리 크롭과 BFS 10스텝 이미지를 한 비교 표로 통합했다. 결과 JSON과 크롭 기록은 표 밖의 개별 링크로 분리하고 장면별 검수 설명은 유지했다.

- B 로우뷰 +45° 머리 크롭 BFS 10스텝 결과를 한계 사례로 반영했다. 참조와 결과의 얼굴이 일치하지 않는다는 검수 판단, 30스텝에서 뚜렷한 개선이 없었다는 관찰, 크롭·실행 JSON과 재현 명령을 추가했다.

- A 로우뷰 0° 머리 크롭 BFS 결과를 채택했다. 512px 크롭·좌표 JSON·1280px 10스텝 결과와 실행 JSON, zero-v3 재현 명령을 원고에 연결했다. 턱과 얼굴 각도 변화도 기록했다. C 채택 결과는 유지하고 B는 비교 중으로 표시했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.11`.

### v2026.09.10

- 문장 안의 소스코드·JSON 링크 7개를 설명 문장과 분리해 각각 독립 문단으로 배치했다. 링크 대상과 기존 `lazy-source` 속성은 유지했다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.10`.

- C의 고해상도 아이레벨 쿼터뷰 얼굴 크롭 BFS 결과를 채택해 원고에 반영했다. 실제 머리 방향을 맞추면서 얼굴 윤곽이 잘 보이는 아이레벨 참조를 우선하는 검수 기준, 크롭·실행 기록과 재현 명령을 추가했다. A 정면·B 좌우 반전 쿼터뷰의 참조 선정과 생성 코드도 연결했다. 눈·입 변화와 윤곽선 차이는 남은 검수 항목으로 기록했다.

- P7-5.2의 640px 15방향 자산 폐기에 맞춰 BFS 참조 표와 생성기 기본 입력을 새 1280px 자료로 교체했다. 기존 BFS 결과와 실행 JSON은 이전 참조의 생성 이력임을 명시했다.

- A 아이덴티티 결과와 C 책 보강 2차 결과에 각도 참조를 교체해 BFS를 적용한 결과를 반영했다. 장면별 참조·결과 비교표, 생성 코드, 3번 실험 재현 명령과 실행 JSON을 연결했다. 강도 감소보다 참조 교체가 효과적이었던 관찰과 A의 과한 턱 들림·C의 눈꺼풀 및 시선 차이를 함께 기록했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- C Mira의 1차 착장 결과에서 누락된 책을 보강한 20스텝 2차 결과를 추가했다. 단일 입력·실제 프롬프트·로컬 GPU 재현 명령·생성기와 실행 JSON을 연결하고, 책과 손의 변화 및 착장·자세의 보존을 기록했다. 원본 책 픽셀의 복원이 아닌 소품 생성이며 얼굴·헤어는 후속 보강 대상임을 구분했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- B Mira와 C Mira의 20스텝 마네킨 생성을 장면별 독립 작업이 아닌 동일 조건의 단일 작업으로 정리했다. 두 결과 이미지는 하나의 2열 비교표에 묶고, lazy-source가 적용되는 각 실행 기록은 표 밖 단독 문단으로 유지했다. 마네킨의 역할·공통 입력 조건·자세 보존의 한계와 장면별 관찰도 흐름에 맞게 다시 썼다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- 누락된 B 마네킨 착장 결과와 실행 기록을 복원하고, 마네킨의 아이덴티티 1차 적용을 B·C 공통 흐름으로 묶었다. 동일한 5.3 최종 착장 참조·프롬프트·재현 명령과 B·C 결과 비교표를 제시하고, 착장·신발 반영과 얼굴·헤어의 후속 보강을 구분했다. B의 포즈·크기 변화도 관찰에 남겼다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- 신규 컷아웃 마네킨 생성기에서 A Scene 선택지·전용 프롬프트·전경 신발 처리 분기를 폐기했다. 생성기는 이제 B 도약 자세와 C 앉은 자세만 대상으로 하며, 원고의 B·C 재현 명령과 일치한다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- 이전 입력에서 수행한 Studio DeLight, BFS Head V5, Relight 테스트 흔적을 각각 A·B·C 3열 결과, 기준 코드, 지연 표시 실행 기록으로 다시 연결했다. 이 세 테스트가 현재 신규 분리·마네킨 경로의 후속 결과가 아님을 본문과 체크리스트에 명시하고, BFS 카드 링크는 실행 기록의 `mr2along/BFS` 저장소로 맞췄다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- 직접 아이덴티티 적용 결과의 관찰을 추가했다. A의 큰 전경 밑창 오류는 현재 조건의 모델 경계로, B·C의 신발 오류는 마네킨 시작 경로를 확인할 이유로, C 조연의 책 소실은 별도 보완 항목으로 기록했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- 직접 아이덴티티 적용의 네 출력 이미지를 원고에 다시 연결했다. 상세 비교 해설과 개별 실행 기록 링크는 복원하지 않고, Scene A·B·C Mira와 C 조연 결과를 4열로 제시했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- 분리 결과의 한계와 재현 안내를 나누고, `Mira와 조연을 분리하는 재현 코드` 소제목 아래에 기준 Python 소스·설정 파일·실행 명령·출력 관찰을 모았다. 기준 소스와 설정 파일은 각각 단독 링크로 lazy-source 패널을 사용한다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- 분리 결과의 마스크·컷아웃 JSON 링크를 각각 링크 하나만 있는 문단으로 분리했다. 사이트 지연 표시기가 적용하는 문단 조건을 충족해 각 기록을 lazy-source 패널로 표시한다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- 분리 결과 표 아래의 대상별 투명 PNG 링크를 원고에서 폐기하고, 마스크·컷아웃 결과 JSON만 지연 표시 링크로 유지했다. 기존 투명 PNG 자산은 이력 추적을 위해 유지했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- 분리 결과의 시각 순서를 `마스크 오버레이 4열 → 마스크 이미지 4열 → 흰 배경 컷아웃 4열`로 재배치했다. 투명 PNG와 마스크·컷아웃 JSON 기록은 표 밖의 대상별 링크로 분리했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- 분리 컷아웃 아래에 대상별 `마스크 → 마스크 오버레이 → 파일·실행 기록` 과정표를 추가했다. 마스크와 오버레이를 이미지로 표시해 선택 영역과 원본 위 위치를 함께 확인할 수 있게 했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- 분리 컷아웃 4열 비교표 아래의 대상별 보조 파일·실행 기록을 접을 수 있는 상세 블록으로 묶어, 기본 본문에서 반복 링크가 차지하던 공간을 줄였다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- `Mira와 조연을 각각 분리한다`의 흰 배경 컷아웃 네 장을 Scene A·B·C Mira와 C 조연 기준 4열 비교표로 배치했다. 투명 PNG·마스크·오버레이와 실행 기록 링크는 표 아래에 유지했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- P7-5.4 최종 장면 입력 A·B·C 이미지를 3열 비교표로 배치했다. 각 입력의 `result.json` 링크는 표 아래에 유지했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- `보충학습: 이전 마네킨·조명·합성 실험` 전체를 원고에서 폐기했다. 이전 마네킨·조명·배경판·다중 참조 통합·리라이트의 이미지, 실행 기록, 코드와 해설은 본문 연결에서 제거하고 기존 생성 자산은 이력 추적을 위해 유지했다. 본문은 C 착장 적용 결과 뒤에 체크리스트로 이어진다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- `B의 포즈·크기 보존을 별도 기준으로 검증한다` 블록의 결과 이미지·실행 기록·생성 코드·재현 명령·실패 해설을 원고에서 폐기했다. 마네킨 착장 경로는 C 결과만 기록하도록 흐름과 체크리스트를 정리했다. 기존 B 관련 생성 자산은 이력 추적을 위해 유지했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- `네 결과에서 반영과 보존을 비교한다` 블록의 A·B·C Mira 및 C 조연 결과 이미지·관찰·실행 기록 링크를 원고에서 폐기했다. 뒤의 B·C 마네킨 경로는 직접 아이덴티티 적용 결과와 독립된 경로로 연결했다. 기존 생성 자산은 이력 추적을 위해 유지했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- P7-5.5의 본문 흐름을 `장면 입력 → 분리 대상과 보존 대상 → 직접 적용 조건 → B·C 마네킨 경로 → C 착장 반영 → B 포즈·크기 보존 검증 → 이전 실험 보충학습`으로 정리했다. 폐기된 A·B·직접 적용 결과를 전제로 한 전환 문장과 체크 항목을 제거했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- B 착장 적용 결과와 `p7-5-5-qwen-2511-mannequin-outfit-b-mira-stage3-v1-size-1280x1280-seed-62294-steps-20-result.json` 링크를 원고에서 폐기했다. B 포즈·크기 보존 비교는 해당 1차 결과를 입력으로 계속 사용하되, 입력·출력 추적은 보강 실행 기록에서 확인한다. 기존 B PNG·JSON 자산은 이력 추적을 위해 유지했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- A 착장 적용 결과와 `A는 신발 바닥면을 참조해 보강한다` 절을 원고에서 폐기했다. 이 두 범위의 비교 이미지·실행 기록·재현 명령·실패 해설은 본문 연결에서 제거하고, 기존 생성 자산은 이력 추적을 위해 유지했다. C 착장 적용과 B 포즈 보존 비교는 유지했다. 번역 동기화: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.10`.

- 폐기한 마네킨 신발 한계 절의 미참조 결과 PNG·JSON 4쌍과 전용 생성기 1개를 삭제하고 모델 인벤토리 등록을 제거했다. 다른 절과 생성기의 입력으로 사용하는 A 마네킨·신발 디자인 자산은 유지했다.

- A 마네킨 신발 우선 적용과 성능 한계 절을 폐기했다. 해당 절의 비교 이미지·JSON 링크, 컷아웃 아웃솔 후속 실험, 성능 부족 결론과 재현 명령을 본문에서 제거하고 연결 문장을 정리했다. 기존 착장 적용 및 착장 후 신발 보강 절은 유지했다. 번역 동기화: 한국어 원문만 반영.

- 배포 준비 strict 빌드에서 발견한 P7-5.3 신발 참조 소제목 앵커를 현재 윗면·바닥면 제목에 맞춰 수정했다.

- A 마네킨 생성의 압축 전후 설명·프롬프트·이미지·JSON 링크를 본문에서 제거하고, 마네킨 생성 재현 명령을 B·C로 한정했다. 기존 A 마네킨을 입력으로 수행한 신발·착장 비교는 생성 절차와 구분해 연결했다. 번역 동기화: 한국어 원문만 반영.

- A 마네킨의 신발 우선 적용 비교를 추가했다. 참조 없는 양발 신발 생성, 신발 디자인 우선 참조 30스텝의 앞쪽만 적용, 교체 지시만 남긴 10스텝의 맨발 잔존을 PNG·JSON으로 연결했다. 원본 A 컷아웃의 아웃솔 교체에서 참조가 별도 객체로 생성된 실패와 재실행 명령을 함께 반영했다.
- 현재 실험 조건에서 지정 부위에 참조 디자인을 안정적으로 적용하는 모델 성능이 부족하다고 정리하되, 부분 성공과 프롬프트·스텝 동시 변경의 한계를 명시했다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.10`.

### v2026.09.09

- A 생성기에 신발 교체 성공 이미지를 출발점으로 삼는 `--sole-only` 모드를 추가하고 v4·v5 20스텝 결과를 비교했다. v5는 입력·코드·설정을 유지하고 원형 홈→삼각형 홈 지시만 변경했다. 두 결과의 운동화 유지·색 변화와 무늬 전이 실패, processor의 프롬프트 잔존 확인을 원고에 기록했다. 입력·출력·코드 해시와 옵션 분기를 검증했다. 번역 동기화: 한국어 원문만 반영.

- A 전용 신발 보강기의 기본 참조를 P7-5.3 신규 밑창으로 바꾸고 20스텝 재생성 PNG·JSON·실행 명령을 반영했다. 발가락 잔존과 밑창 무늬 미반영을 실패로 기록하고, 앞선 단일 이미지 신발 교체 결과와 실제 프롬프트를 연결해 비교했다. 얼굴·헤어 보강은 최종 BFS로 남겼다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.09`.

- B 마네킨 우선 참조 보강의 코드·20스텝 PNG·JSON·재현 명령을 반영했다. 착장 이미지의 크기·포즈를 따르는 실패 결과를 명시하고 참조 순서와 프롬프트 동시 변경의 해석 한계를 구분했다. 번역 동기화: 한국어 원문만 반영.

- A·B·C 마네킨에 5.3 최종 3단계 착장을 적용한 신규 생성기와 20스텝 결과 PNG·JSON을 본문에 연결했다. 입력 순서·실제 프롬프트·재현 명령을 추가하고, A 앞쪽 발 오류와 얼굴·머리 변화, B 크기·포즈 변화, C 착장 반영을 구분했다. 마네킨 이후 착장 1차 적용까지 흐름을 갱신하고 부위별 보강은 아직 수행하지 않았음을 명시했다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.09`.

- A의 압축 프롬프트 20스텝 결과를 마네킨 본문에 반영하고, 신발 밑창이 남은 첫 결과와 비교할 수 있도록 PNG·JSON을 연결했다. 프롬프트 비대화가 반영을 방해했고 압축으로 개선됐다는 판단을 작업 가설로 보존하되, 신발 지시 구체화도 동시에 바뀌어 길이 단독 효과는 분리되지 않았음을 명시했다. A·B·C 재현 명령과 체크리스트를 갱신했다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.09`.

- 원본 컷아웃에서 생성한 B·C 20스텝 마네킨 PNG·JSON과 신규 생성기·재실행 명령을 반영했다. 직접 아이덴티티 적용의 기존 외형 잔존을 중간 단계의 이유로 연결하고, 얼굴·관절·가림 영역 변화와 아직 마네킨에 아이덴티티를 재적용하지 않았음을 구분했다. A 첫 결과는 앞쪽 신발이 남아 전용 프롬프트를 압축·보강했다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.09`.

- 본문 흐름을 신규 장면의 캐릭터 분리 → 아이덴티티 적용 → 결과 비교로 정리했다. 신규 생성기와 30스텝 네 결과 PNG·JSON, Mira의 5.3 최종 3단계 착장 참조와 C 조연의 텍스트 전용 지시를 연결했다. Mira의 참조 반영 한계와 조연의 가림 영역·자세 변화를 명시하고, 이전 마네킨·조명·합성은 보충학습으로 분리했다. 체크리스트와 중심 학습 산출물을 맞췄다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.09`.

- 원고의 인물 분리 자산을 신규 재현 결과로 대체하고, 참조가 끊긴 이전 분리·조명 통합 JSON 2개와 A 조명 결과 PNG·JSON 2개를 삭제했다. 후속 코드·원고의 입력과 그 실행 기록은 유지했다.

- 신규 5.4 장면에서 분리한 A·B·C 미라와 C 조연의 마스크·오버레이·흰 배경·투명 PNG 및 JSON을 원고에 반영했다. 재현 실행 코드와 인물별 입력 해시·좌표·옵션 설정을 추가해 한 명령으로 네 인물을 재분리하도록 했다.
- 로컬 GPU 실제 재실행에서 네 인물의 마스크·흰 배경·투명 PNG가 기준 결과와 픽셀 단위로 일치했다. 알파·원본 픽셀 보존을 검증하고 C 마스크 경계 중복 10픽셀과 가림·미세 경계 한계를 기록했다. 이전 마네킨·착장 보정 기록은 신규 분리 결과와 구분했다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.09`.
- P7-5.4 입력 A·B·C의 PNG·JSON을 신규 `extras-audit-20260909-v1`로 교체했다. 기존 컷아웃·마스크·마네킨은 이전 입력으로 생성한 실험임을 명시하고 실행 기록의 경로·해시는 보존했다. 이전 5.4 extras PNG·JSON 6개를 정리했으며, 이전 입력은 커밋 `82f957926`에서 추적할 수 있다. 새 입력을 사용한 5.5 후속 생성은 이번 변경에 포함하지 않았다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.09`.
- 원고와 후속 입력에서 사용하지 않는 실험 결과 24쌍(PNG·JSON 48개)을 삭제했다. 코드의 동적 파일명 참조와 결과 기록을 확인해 마네킹·뎁스·중간 보정 입력 및 원고 채택 결과는 유지했다. 본문 변경 없음.

### v2026.09.08

- 현재 A·B·C 컷아웃의 조명 중립화 원고를 폐기했다. A·B·C Mira와 C 조연의 조명 보정 결과, 비교표·실행 안내·해당 체크 항목을 삭제했다. C 신발·조연 화풍 보정은 이전 조명 결과에 의존하므로 보충학습으로 옮겨 현재 컷아웃 경로와 구분했다. 인물 분리와 B 마네킨 경로, 기존 생성 자산·코드는 유지했다. 번역 동기화: 한국어 원문만 반영.

- B 머리 길이·신발·바지 보정 원고와 해당 이미지·JSON 연결을 삭제했다. 공통 설명·입력 표·실행 명령을 C 신발과 조연 화풍 보정에 맞추고, B 신발 보정본을 후속 기준 입력으로 지정하던 문장을 제거했다. B 마네킨→Mira 이식 경로는 유지했다. 이번 폐기는 원고 범위이며 기존 생성 자산과 코드는 삭제하지 않았다. 번역 동기화: 한국어 원문만 반영.

- B 원본 컷아웃→얼굴 있는 20스텝 마네킨→P7-5.3 전신 착장 참조의 30스텝 인물 교체 경로를 본문에 반영했다. `pose-person-face-hair-v5-outfit` PNG·결과 JSON, 마네킨 입력·기록, 생성기와 재현 명령을 연결했다. 단발·재킷·넓은 바지 반영과 맨발 잔존·새 바닥 그림자를 구분하고 최종 합성 완료본으로 취급하지 않았다. DeLight 경로와의 차이, 원본 비율 전처리·CUDA 난수 생성기, 이름에 남은 face와 실제 전체 교체 지시의 차이를 설명했다. 번역 동기화: 한국어 원문만 반영. 원문 기준 버전: `v2026.09.08`.

- 마스크 없는 B 바지 v3를 로컬 GPU에서 생성하고 입력·참조·출력 해시를 검증했다. v2보다 발목 노출과 주름이 남았으나 양쪽 밑단 형태가 일치하지 않아 검토 결과로 기록하고 최종 채택은 보류했다. 번역 동기화: 한국어 원문만 반영.

- B 바지 v2의 길이 증가와 주름·음영 손실을 확인해 후속 입력 채택을 철회하고 `b-shoes-v1`을 기준 입력으로 되돌렸다. 생성기 지시를 기존 길이·밑단·발목 노출·주름을 유지하면서 통을 적당히 넓히도록 수정했다. 마스크 없이 수정 대상 외의 내용을 최대한 보존하는 편집 목적과 검수 기준을 명시했다. 번역 동기화: 한국어 원문만 반영.

- B 바지 보정 단계를 추가하고 머리→신발→바지의 입력 연결을 구성했다. 로컬 GPU로 기존 신발 보정본에 참조의 하이웨이스트·넓은 바지통·열린 밑단을 적용한 v2를 원고에 연결했다. 단발·스니커즈·도약 자세의 유지와 주름 단순화의 한계를 기록하고 B의 후속 입력을 갱신했다. 입력·출력 해시와 앞 단계 연결을 확인했다. 번역 동기화: 한국어 원문만 반영.

- 추가 보정 4개를 로컬 GPU에서 1280×1280·20 step·seed 62294로 실행했다. B의 단발→흰 스니커즈 연결, C의 흰 스니커즈, 조연의 선·평면 채색 변경을 개별 PNG·JSON과 함께 반영했다. 자세 보존과 세부선·그림자 변화, 가림 영역의 남은 한계를 기록했다. 입력·출력 해시와 B 단계 연결을 검증했으며 통합시트는 생성하지 않았다. 번역 동기화: 한국어 원문만 반영.

- DeLight 이후 B 머리 길이→B 신발, C 신발, C 조연 화풍을 보정하는 로컬 Qwen 2511 생성기를 추가했다. 기존 Mira 참조의 단발·흰 스니커즈와 C Mira의 선·채색을 기본 참조로 정하고, 작업별 입력 변경·dry-run·덮어쓰기 방지·결과 해시 기록을 구성했다. 코드의 입력 연결과 오류 검사를 확인했다. 번역 동기화: 한국어 원문만 반영.

- 새 컷아웃 4개에 로컬 GPU Qwen Image Edit 2509·Studio DeLight를 적용하고 결과 PNG·JSON, 입력 해시와 재현 명령을 원고에 연결했다. A·B의 음영 완화와 세부 변화, C의 가림 영역 재생성·방향성 그림자, 조연의 화풍 이탈을 구분했다. C 결과를 중립 조명만 적용한 최종 합성 자산으로 취급하지 않는다. 코드에 현재 컷아웃 선택값과 덮어쓰기 방지·CUDA 확인을 추가했다. 번역 동기화: 한국어만 반영, 새 DeLight 비교와 한계 설명을 번역에도 반영해야 함.

- 원고에서 제외한 독립 그림자 생성기 `p7_5_5_qwen_edit_2511_generate_cutout_shadow.py`와 모델 인벤토리의 코드 참조를 제거했다. 보충학습의 후속 결과와 Try-On 코드가 입력으로 사용하는 그림자 PNG 3개와 해당 실행 JSON은 재현을 위해 보존했다.

- 로컬 GPU로 A·B·C의 Mira와 C의 조연을 각각 분리하고, 마스크·오버레이·흰 배경 컷아웃·투명 PNG와 실행 기록을 연결했다. C의 가림 영역은 복원하지 않았으며 경계의 미세 누락을 명시했다. 네 알파 채널과 마스크의 일치, 원본 픽셀 보존, C 인물 마스크의 중복 0픽셀을 확인했다. 인물별 선택점·상자·제외 영역과 재현 명령은 `p7-5-5-character-separation-v1-result.json`에 기록했다.

- P7-5.5의 입력을 P7-5.4 최종 장면 A extras-v5·B extras-v8·C extras-v7로 연결했다. 공용 PNG와 lazy-source 결과 JSON을 직접 참조하고, 장면별 Mira 편집 대상과 보존할 주변 인물·동물·새를 구분했다.
- 별도의 컷아웃 그림자 추가 단계, 해당 이미지·생성기·결과 JSON 연결, 그림자 생성을 선행 조건으로 삼는 안내와 실행 명령을 삭제했다. 현재 흐름은 마스크·컷아웃에서 필요한 캐릭터 보정으로 이어진다.
- 해안·초원·공원 카메라판으로 생성한 기존 후속 결과는 보충학습의 이전 실험으로 구분했다. 실제 입력 이력에 포함된 그림자는 기록을 바꾸지 않고 설명하며, 새 A·B·C의 배경판·통합 결과는 아직 생성하지 않았음을 명시했다.
- 관련 자산: P7-5.4 A extras-v5·B extras-v8·C extras-v7 PNG 및 result JSON. 번역 동기화 메모: 한국어 원문만 반영됨. 새 입력, 인물별 분리 결과와 그림자 단계 삭제를 번역에도 반영해야 함. 원문 기준 버전: `v2026.09.08`.

### v2026.09.05

- 최초 T2I 장면 생성과 Multiple-angles 카메라판 생성 문단을 새 P7-5.4로 분리했다. P7-5.5는 카메라판을 입력으로 받는 마스크·컷아웃, 캐릭터 identity 이식, 배경·조명 통합 경로만 남겼다. 번역 동기화 메모: P7-5.4·P7-5.5의 분리된 링크와 목차 순서를 함께 반영해야 한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.05`.
- P7-5.5 원고가 직접 연결하는 자산 80개와 생성 코드 18개의 식별자를 `p7-5-5-*`·`p7_5_5_*`로 통일했다. 원고, P7-5.12의 재사용 링크, 실행 코드, `result.json`, 모델 인벤토리의 현재 경로도 함께 갱신했다.
- 원고·실행 기록·재현 입력 어디에도 연결되지 않은 후보 이미지 2개와 코드 5개를 제거했다. `result.json`이 입력으로 참조하는 중간 산출물은 재현성을 위해 유지했다.
- 번역 동기화 메모: 공용 자산 경로만 바뀌었으므로 번역본에서 같은 P7-5.5 자산 링크를 사용할 때 새 파일명을 반영한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.05`.

### v2026.09.03

- P7-5.4 원고와 Section 릴리즈노트를 P7-5.5로 이관했다. 원고 제목·Section ID·목차 경로와 Part 7의 현재 Section 참조를 새 번호로 맞췄다. 기존 `p7_5_4_*` 코드·자산 파일명은 실행 이력 식별자로 유지한다. 번역 동기화 메모: P7-5.5 참조와 Chapter 5의 `section-05.md` 경로를 번역본에도 반영해야 한다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.03`.

### v2026.09.02

- 현재 경로를 `카메라판 → 마스크·컷아웃 → 그림자 포함 포즈 → Stage 2 착장 다중 참조 이식 → DeLight → 45도 얼굴·헤어 이식 → 배경판 → DeLight 통합 → Relight`로 정리하고 A·B·C 대표 결과·코드·`result.json`을 연결했다.
- 카메라판은 방위·높이·필요 시 거리 변환을 맡고, 상대 depth는 현재 채택 경로에서 제외했다. 입력 역할과 관찰 범위는 Part 7 체크포인트·통합 관리 노트와 맞췄다.
- 현재 경로에서 호출하지 않는 LaMa ONNX와 Nunchaku FP4 r128의 모델 소개·출처를 제거하고, 배경판은 Qwen Image Edit 2511 직접 편집 경로로 정리했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.02`.

### 이전 이력

- LaMa·알파 합성·하모나이즈, Try-On, 구조 guide·ControlNet 등은 비교 후 현재 기본 경로에서 분리하거나 제외했다. 세부 실험 순서와 파일은 `result.json`과 Git 이력으로 확인한다.
