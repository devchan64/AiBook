# Section Release Note

- Section ID: `P7-3.1`
- Source File: `docs/parts/part-07/chapter-03/section-01.md`

### v2026.07.07
- 본문 반영: 공개 출력 키를 `평가 샘플`, `예측 라벨`, `확신 차이`, `추가 검토 샘플`처럼 독자용 설명형 표현으로 바꾸고, 실행 요약 제목도 한국어로 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-3.1 공개 출력 키를 모델 내부 필드명이 아니라 독자용 설명 레이블로 유지해야 한다.

### v2026.07.07
- 본문 반영: 입구 설명을 `추가 검토 대상`, `입력 모양(shape)`, `확신 차이(margin)` 같은 한국어 중심 표현으로 정리해 공개 독자용 설명이 먼저 읽히게 맞췄다.
- 번역 동기화 메모: 향후 번역본도 P7-3.1 초반 입구 설명에서는 한국어 개념 설명을 먼저 두고 영어 병기는 보조로 유지하는 편이 좋다.

### v2026.07.12
- 추가 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다.
- 본문 반영: 입력 모양, 라벨, 예측값, 오류 사례, 평가 분리 기준을 `체크리스트` 하나로 재구성했다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 CNN 구현, 프레임워크 전체 사용법, 대규모 훈련, GPU 최적화 같은 생략 목록을 걷어내고, 이번 절이 `입력 모양 -> 라벨 -> 학습 -> 예측 -> 오류`로 이미지 프로젝트 문서를 시작하는 기준을 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 오류 사례 handoff는 `P7-3.2`, 뒤 프로젝트의 공통 실패 기록 handoff는 Part 7 뒤 장들로 이어지게 분명히 했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-3.1 closes the image-project start structure first and hands error analysis to P7-3.2.

### v2026.07.18
- 추가 반영: 말미 체크리스트를 `입력 모양`, `라벨`, `학습/평가 분리`, `오류 검토`, `학습 루프` 다섯 칸으로 압축해, 이미지 프로젝트에서 실행 뒤 반드시 남겨야 할 기록 단위가 더 선명하게 보이도록 정리했다.
- 추가 반영: 본문의 세 Mermaid 도식을 `docs/assets/part-07/chapter-03/*.mmd` 자산으로 분리하고, 자산 폴더 README에 현재 한국어 운영 범위와 향후 번역 동기화 기준을 기록했다.
- 추가 반영: 연습 가이드 기준에 맞춰 `직접 바꿔 보며 확인할 것` 소절을 추가하고, 약한 결함 강도·그림자 값·학습 데이터 범위를 바꿔 보며 입력 구조와 오분류 경계를 직접 확인하게 보강했다.
- 추가 반영: `평가 정확도 0.75 + 약한 결함 오답 1건`을 정확도 한 줄로 닫지 않도록, 입력 구조와 오답 패턴을 먼저 보게 하는 사례 문단과 Mermaid 흐름도를 보강했다.
- 추가 반영: 범위 문단의 진행형 메타 표현을 줄이고, 이미지 입력 구조가 기록 형식을 어떻게 바꾸는지 현재 절 질문이 더 직접 보이게 다듬었다.
- 추가 반영: 본문 도식과 후반 설명의 `작은 분류기`, `작은 실습` 표현을 줄이고, 입력 구조와 기본 분류기 선택이 어떤 기록 항목을 남기는지 중심으로 다듬었다.
- 추가 반영: 본문 후반의 `Part 4`, `Part 5` 직접 호명을 줄이고, 이미지 입력 구조 설명과 학습 루프 용어가 같은 프로젝트 기록 안에서 바로 연결되는 방식으로 문장을 다시 정리했다.
- 추가 반영: 결과 해석 문장에서 `다음 절에서 어떤 샘플을 검토할지` 같은 handoff 표현을 줄이고, 약한 결함 샘플 자체가 현재 절의 오류 검토 우선순위로 바로 읽히게 다듬었다.
- 추가 반영: 도입과 범위 문장에서 `P7-2까지는... 이제` 식의 연결을 줄이고, 이미지 입력 구조가 프로젝트 기록 형식을 어떻게 바꾸는가라는 현재 절 질문이 먼저 보이게 정리했다.
- 추가 반영: 다음 절 연결 문장에서 예전 제목 `오류 사례 분석`을 현재 제목 `CNN, 순차 모델, attention 계열 비교`로 바꿔, 이미지 프로젝트 파트의 교차 참조가 실제 목차와 같은 구조 질문을 가리키도록 정리했다.
- 추가 반영: 결과 해석 표의 마지막 handoff 문장도 `오류 사례 분석으로 넘김` 대신 `구조 계열 비교 질문으로 넘김`으로 바꿔, 표 안의 다음 단계 안내도 현재 목차 축과 맞췄다.
- 추가 반영: Section 제목을 `이미지 분류 모델 목표`에서 `입력 구조는 프로젝트를 어떻게 바꾸는가`로 바꾸고, 이미지 사례가 구조 변화 설명의 한 예라는 점이 더 직접 보이게 맞췄다.
- 추가 반영: 표면 패치 CSV 자산 경로를 본문 링크로 바꿔, 대표 패치 설명과 전체 입력 파일을 바로 연결해 읽을 수 있게 했다.
- 변경 이유: 기존 P7-3.1 예제가 4x4 세로/가로 막대 수준에 머물러, 이미지 입력 구조가 실제 프로젝트 준비와 해석 기준을 어떻게 바꾸는지 보여 주기에는 너무 장난감처럼 읽혔다.
- 본문 반영: 예제 질문을 `8x8 grayscale 표면 점검 패치에서 정상 표면과 스크래치 경고를 구분할 수 있는가`로 바꾸고, 예시 패치와 Python 예제를 모두 실제 점검 장면에 더 가까운 구조로 다시 썼다.
- 본문 반영: 학습/평가 데이터를 `정상`, `명확한 결함`, `약한 결함`, `그림자 포함 정상`으로 구성해, 입력 구조와 결함 강도 차이가 평가 해석에 어떻게 드러나는지 보강했다.
- 추가 반영: 결과 해석도 `혼합 막대`가 아니라 `약한 결함 오분류`를 중심으로 다시 정리해, 바로 다음 오류 사례 분석 절과의 연결을 더 실제적으로 맞췄다.
- 추가 반영: 본문에 남아 있던 `작은 이미지 프로젝트`, `장난감 데이터` 같은 표현을 `점검 기록`, `synthetic 점검 패치` 중심 표현으로 정리해 실습 강도를 낮춰 보이지 않게 조정했다.
- 추가 반영: Python 예제의 원시 패치 데이터를 `docs/assets/part-07/chapter-03/p7-3-surface-patches.csv`로 분리하고, 본문 코드가 CSV 읽기와 배열 변환 이후의 학습·평가 흐름에 집중하도록 다시 정리했다.
- 추가 반영: `입력 파일` 소절을 따로 세워 한 행의 의미와 핵심 열을 먼저 읽고, 그다음 8x8 배열 복원과 학습 코드로 넘어가도록 실행 리듬을 분리했다.
- 추가 반영: `이 코드에서 Part 5 용어를 다시 읽기`와 학습 루프 Mermaid 도식을 추가해, 이번 예제가 `full-batch`, `step`, `epoch`, `learning rate`, `optimizer update`, `inference`를 어디서 다시 보여 주는지 직접 연결했다.
- 번역 동기화 메모: Future translations should preserve the stronger P7-3.1 example built around 8x8 inspection patches and weak-defect failure analysis.
