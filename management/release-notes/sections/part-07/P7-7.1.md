# Section Release Note

- Section ID: `P7-7.1`
- Source File: `docs/parts/part-07/chapter-07/section-01.md`

### v2026.07.19
- 변경 이유: 원고 가이드의 메타 진행 표지 축소 기준에 맞춰 연습 도입부 제목을 점검했다.
- 본문 반영: `이 절의 범위`, `이 절의 목표` 표지를 `배포 상태를 나눠 읽기`, `남겨야 할 판단`으로 바꿔 현재 Section의 중심 질문과 기록 기준이 바로 보이도록 정리했다.
- 번역 동기화 메모: 향후 번역본도 generic scope/goal headings보다 section-specific practice judgment headings를 우선한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 원문 기준 버전: `v2026.07.19`

### v2026.07.12
- 추가 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다.
- 추가 반영: `운영 관점의 연결`을 `왜 정적 사이트도 운영 질문이 남는가`로 바꿔 현재 절의 운영 초점을 제목에 직접 드러내게 했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 CDN 최적화, 커스텀 도메인 DNS, 사설 모니터링 시스템 같은 생략 목록을 걷어내고, 이번 절이 `빌드 성공`과 `공개 상태 확인`을 분리해 기록하는 기준을 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 실패 기록 handoff는 `P7-7.2`로 직접 연결되게 맞췄다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-7.1 closes the deployment-check record first and hands failure logging to P7-7.2.

### v2026.07.18
- 추가 반영: 말미 체크리스트를 `브랜치`, `빌드`, `공개 확인`, `재확인`, `운영 관점` 축의 확인 표로 압축해 배포 확인 기록의 최소 단위가 더 분명하게 보이도록 정리했다.
- 추가 반영: 본문의 두 Mermaid 도식을 `docs/assets/part-07/chapter-07/*.mmd` 자산으로 분리하고, 자산 폴더 README에 현재 한국어 운영 범위와 배포 점검 도식 동기화 기준을 기록했다.
- 추가 반영: Actions 초록색 완료를 곧바로 공개 완료로 읽지 않도록, 빌드 기준·Pages deploy·공개 URL 확인을 분리해 보게 하는 사례 문단과 Mermaid 흐름도를 보강했다.
- 추가 반영: 배포 직후 성공과 운영 시작 절의 의미를 설명하는 문장에서 `읽는 편이` 같은 독법 안내를 줄이고, 현재 시점의 확인 신호와 검토 대상 기록이라는 역할을 더 직접 표현했다.
- 추가 반영: 범위 설명과 운영 시작 해설에서 `바로 다음 절`, `뒤 절` 식 연결을 줄이고, 현재 절이 빌드 성공과 공개 상태 확인을 분리해 기록하는 자리라는 점이 먼저 보이게 정리했다.
- 추가 반영: 다음 절 연결 문장에서 예전 제목 `실패 기록과 개선 계획`을 현재 제목 `장애 기록과 다음 반복 계획`으로 바꿔, 배포 확인 절과 운영 회고 절의 handoff가 실제 목차 이름과 같게 이어지도록 정리했다.
- 추가 반영: Section 제목을 `배포와 모니터링 목표`에서 `배포 확인과 상태 점검`으로 바꾸고, 목표 설명보다 실제 확인 단계와 상태 점검이 먼저 보이도록 정리했다.
- 추가 반영: 배포 점검 CSV 자산 경로를 본문 링크로 바꿔, 배포 확인표 설명과 실제 점검 기록 파일을 바로 오가며 읽을 수 있게 했다.
- 변경 이유: 기존 P7-7.1은 배포 확인 절이었지만, 실제 저장소 기준으로 무엇을 실행하고 무엇을 확인해야 하는지보다 일반적인 체크리스트 설명 비중이 커서 실습 강도가 약했다.
- 본문 반영: `dev` 작업 브랜치, `main` 배포 브랜치, `.github/workflows/deploy.yml`의 Pages workflow, `mkdocs.yml`의 공개 URL을 본문 안으로 끌어와 이 저장소의 실제 배포 경로를 중심으로 다시 구성했다.
- 추가 반영: Mermaid 흐름도와 확인표를 `로컬 빌드 -> main 반영 -> GitHub Actions build -> Pages 공개 -> 공개 URL 확인` 구조로 바꾸고, `.venv/bin/python -m mkdocs build`와 공개 URL 확인 같은 실제 점검 항목을 더 직접 드러냈다.
- 추가 반영: `docs/assets/part-07/chapter-07/p7-7-deployment-checks.csv`를 추가하고, 본문 Python 예제가 배포 점검표를 읽어 `공개 확인 완료`와 `재확인 필요` 상태를 실제 기록 파일 기준으로 구분하도록 보강했다.
- 추가 반영: `입력 파일` 소절을 따로 세워 한 배포가 여러 점검 단계 행으로 구성된다는 점을 먼저 설명하고, 그다음 `run_id` 기준 재구성 코드로 넘어가도록 구조를 분리했다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 재확인 플래그와 공개 확인 단계를 직접 바꾸며 최종 배포 상태가 어떻게 달라지는지 실험하도록 보강했다.
- 번역 동기화 메모: Future translations should preserve the repo-specific deployment path and the distinction between local build success, main-triggered deployment, and public-page verification.
