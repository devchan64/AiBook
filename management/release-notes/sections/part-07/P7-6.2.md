# Section Release Note

- Section ID: `P7-6.2`
- Source File: `docs/parts/part-07/chapter-06/section-02.md`

### v2026.07.07
- 본문 반영: 후반 점검 문장을 `권한 범위`, `승인 필요 여부`, `허용 범위`, `다음 행동`, `단계/도구/결과`, `보류(blocked)` 같은 한국어 중심 표현으로 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-6.2 후반 점검 문장에서는 운영 키를 그대로 나열하기보다 한국어 설명을 먼저 두고 영어는 병기로만 유지하는 편이 좋다.

### v2026.07.12
- 추가 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다.
- 추가 반영: `운영 관점의 연결`을 `왜 권한과 로그가 운영 문제인가`로 바꿔 권한·로그 설명의 초점을 직접 드러내도록 정리했다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 감사 시스템, 정책 엔진, 분산 추적 인프라 같은 생략 목록을 앞세우지 않고, 이번 절이 `실행 가능하다`와 `실행해도 된다`를 구분해 기록하는 기준을 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 권한·로그 설명의 후속 handoff를 `P7-7.1`, `P7-7.2`의 배포 후 실패 추적과 운영 회고로 직접 연결했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-6.2 closes permission-and-log recording first and hands post-deployment tracing to P7-7.

### v2026.07.18
- 추가 반영: 말미 체크리스트를 `권한 기록`, `상태 분리`, `실패 로그`, `승인 대기`, `회고 문장` 축의 확인 표로 압축해 blocked와 failure를 다음 행동으로 나누는 기준을 더 분명히 했다.
- 추가 반영: 본문의 Mermaid 도식을 `docs/assets/part-07/chapter-06/p7-6-2-status-split-flow-ko.mmd` 자산으로 분리하고, Chapter 6 자산 폴더 README에 상태 구분 도식 운영 범위를 기록했다.
- 추가 반영: `미완료 단계 둘`을 모두 실패로 뭉개지 않도록, `롤백 실행`과 `감사 로그 첨부 업로드`를 대비하는 사례 문단과 Mermaid 흐름도를 넣어 `blocked`와 `failure`의 다음 행동 차이를 더 직접 보이게 보강했다.
- 추가 반영: 도입에서 `P7-6.1에서는 ...` 식의 순차 설명을 줄이고, 현재 절이 blocked와 failure를 운영 기록에서 어떻게 갈라 읽는가라는 질문 자체로 서도록 정리했다.
- 추가 반영: Section 제목을 `권한, blocked, failure 로그 읽기`에서 `권한, 로그, blocked 상태 검토`로 바꾸고, 로그 상태를 운영 판단 관점에서 검토하는 역할이 더 직접 보이게 맞췄다.
- 추가 반영: 권한·승인 기록 CSV 자산 경로를 본문 링크로 바꿔, 보류 해석과 원시 기록 파일을 곧바로 대조할 수 있게 했다.
- 본문 반영: 권한·로그 예제를 `요약 저장` 보류 수준에서 `프로덕션 롤백 실행` 승인 대기 시나리오로 바꾸어, 조회 도구와 변경 도구의 위험도 차이가 더 직접적으로 읽히게 보강했다.
- 추가 반영: `보류`를 단순 실패가 아니라 승인 절차 미완료 상태로 읽는 기준을 실행 기록, 비교 표, 좋은 기록 예시까지 포함해 강화했다.
- 추가 반영: 권한·승인 단계 데이터를 `docs/assets/part-07/chapter-06/p7-6-permission-log.csv`로 분리하고, 본문 Python 예제를 CSV 읽기 이후의 승인 집계와 보류 해석 중심으로 다시 정리했다.
- 추가 반영: `입력 파일` 소절을 따로 세워 권한, 범위, 승인 여부 열이 왜 먼저 중요하게 읽혀야 하는지 설명하고, 그다음 집계 코드로 넘어가도록 구조를 분리했다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 승인 여부와 범위를 바꾸며 `보류` 상태와 회고 해석이 어떻게 달라지는지 직접 실험하도록 보강했다.
- 추가 반영: `감사 로그 첨부 업로드` 실패 단계를 추가해, 같은 run 안에서 `blocked`와 `failure`가 동시에 생길 수 있다는 점을 실제 로그로 보강했다.
- 추가 반영: 본문 중심 질문을 `권한과 승인`뿐 아니라 `blocked 와 failure 를 어떻게 다르게 읽는가`까지 확장하고, 상태별 비교 표와 회고 문장을 새 시나리오 기준으로 다시 정리했다.
- 추가 반영: Python 예제는 `blocked 단계`와 `failure 단계`를 각각 따로 집계해 출력하도록 바꾸고, 검토 요약도 `추가 승인 필요 도구`와 `재시도 또는 수동 조치 필요 도구`를 분리해 남기도록 수정했다.
- 번역 동기화 메모: Future translations should preserve the production-change approval scenario and the distinction between operational hold and functional failure.
