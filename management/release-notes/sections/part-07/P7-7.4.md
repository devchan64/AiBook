# Section Release Note

- Section ID: `P7-7.4`
- Source File: `docs/parts/part-07/chapter-07/section-04.md`

### v2026.07.31
- 변경 이유: Part 7 Section 메타데이터가 현재 가이드의 blockquote 형식과 어긋나 있어 규칙 준수 정리를 진행했다.
- 본문 반영: 제목 아래 `Section ID`, `Version` 두 줄을 blockquote 형식으로 정리하고, 본문 `Version`을 `v2026.07.31`로 갱신했다. 원고 학습 흐름과 실습 내용은 변경하지 않았다.
- 번역 동기화 메모: 향후 번역본도 같은 Section ID와 blockquote 메타데이터 형식을 사용해야 한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.31`


### v2026.07.26
- 정합성 점검: 본문 `Version`을 `v2026.07.26`으로 맞췄다.
- 번역 반영 상태: 기존 공개 번역 구조 유지
- 관련 자산: 없음
- 원문 기준 버전: `docs/parts/part-07/chapter-07/section-04.md` `v2026.07.26`


### v2026.07.22
- 코드 품질 보강: 확장 경고 등급 CSV에서 `grade-01`부터 `grade-06`까지를 대표 신호로 필터링해 본문이 설명하는 6건 alert grade 연습 기준으로 실행되게 정리했다.
- 학습 품질 보강: 확장 데이터의 기대 등급 불일치가 초심자 예제에 섞이지 않도록 대표 세트만 사용해 `mismatch`가 빈 목록으로 유지되게 했다.
- 자료 확인성 보강: 경고 등급 CSV 링크에 `csv-preview`를 추가했다.
- 검증: Python 코드 블록 1개를 실행해 signal_count 6, watch/review/action 각각 2, mismatch 없음 출력이 본문 예시와 일치함을 확인했다.

### v2026.07.20
- Python 예제 주석 보강: 경고 신호를 단순 threshold 등급과 evidence 기반 등급으로 비교하는 목적을 코드 블록 첫 줄 주석으로 명시했다.
- 저작권 검토: Part 7 Chapter 7 모듈 순차 검토 범위에서 경고 등급 설계 연습, 동작 단위 alert grade synthetic CSV, Python 예제 출력, Mermaid 흐름도, 체크리스트를 확인했다. 데이터와 도식은 Part 7 실습을 위해 구성한 자체 예시로 판단했다.
- 출처 사용 점검: 외부 자료 직접 인용, 외부 도표·표·코드·데이터 재사용은 확인하지 않았다.
- 관련 자산: `docs/assets/part-07/chapter-07/p7-action-unit-alert-grades.csv`, `docs/assets/part-07/chapter-07/p7-7-4-alert-grade-flow-ko.mmd`
- 본문 반영: 제목 아래 메타데이터를 저장소 공통 형식인 평문 `Section ID`, `Version` 두 줄로 바로잡고, 본문 `Version`을 `v2026.07.20`으로 갱신했다.
- 번역 동기화 메모: 대응 번역본이 생기면 메타데이터 형식과 한국어 Mermaid 자산의 언어별 파생본 생성 여부를 함께 확인해야 한다.

### v2026.07.19
- 변경 이유: Part 7 연습 Section의 학습밀도 보강 이력을 원고 가이드 기준에 맞춰 압축 정리했다.
- 본문 반영: 범위·목표·연습 표지와 본문 안의 절 진행 지시 표현을 내용형 기록 기준으로 바꾸고, 실행 기록·결과 해석·프로젝트 기록 예시가 설명과 해설 안에서 읽히도록 정리했다. / 학습밀도 리포트 적용으로 gap, 표본 수, 반복성, 최근성, 독자 영향이 `watch/review/action` 등급으로 이어지는 판단 흐름도와 직접 바꿔 보기 뒤 해석 기준을 추가했다.
- 관련 자산: `docs/assets/part-07/chapter-07/p7-7-4-alert-grade-flow-ko.mmd`
- 번역 동기화 메모: 향후 번역본도 진행 안내 제목보다 Section별 질문, 판단 기준, 실행 결과 해석, 프로젝트 기록 예시가 먼저 보이게 맞춘다. P7-7.4 번역 시 새 Mermaid 도식도 같은 구조로 `-en`, `-zh` 자산을 추가해야 한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 원문 기준 버전: `v2026.07.19`

### v2026.07.18
- 변경 이유: 해당 날짜의 원고 구조와 설명 밀도 보강 이력을 통합 정리했다.
- 본문 반영: 말미 체크리스트를 `gap`, `표본 수`, `반복성`, `등급 의미`, `첫 점검` 축의 확인 표로 압축해 경고 등급을 검토 우선순위로 읽는 기준을 더 분명히 했다. / `P7-7.4 경고 등급 설계 연습`을 추가해, 표본 수, 반복성, 최근성을 함께 읽어 `watch`, `review`, `action` 등급을 설계하는 독립 연습 Section으로 분리했다. / `p7-action-unit-alert-grades.csv`를 추가해 단순 threshold 등급과 근거 기반 등급이 어떻게 달라지는지 비교하도록 만들었다. 등으로 압축 정리했다.
- 관련 자산: `docs/assets/part-07/chapter-07/p7-action-unit-alert-grades.csv`
- 번역 동기화 메모: Future translations should preserve the distinction between `watch`, `review`, and `action` as review-priority labels, not confirmed diagnoses.
- 번역 반영 상태: 한국어 원문만 작성됨.
- 원문 기준 버전: `v2026.07.18`
