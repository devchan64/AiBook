# Section Release Note

- Section ID: `P7-6.1`
- Source File: `docs/parts/part-07/chapter-06/section-01.md`

### v2026.07.19
- 변경 이유: 원고 가이드의 메타 진행 표지 축소 기준에 맞춰 연습 도입부 제목을 점검했다.
- 본문 반영: `이 절의 범위`, `이 절의 목표` 표지를 `도구 실행 루프와 멈춤 상태`, `남겨야 할 판단`으로 바꿔 현재 Section의 중심 질문과 기록 기준이 바로 보이도록 정리했다.
- 번역 동기화 메모: 향후 번역본도 generic scope/goal headings보다 section-specific practice judgment headings를 우선한다.
- 번역 반영 상태: 한국어 원문만 반영됨.
- 원문 기준 버전: `v2026.07.19`

### v2026.07.07
- 본문 반영: `read_toc`, `check_build`, `report_status` 중심의 예시를 `inspect_request`, `run_validation`, `share_result` 중심의 일반 실행 루프로 바꾸고, 해설 문장과 요약 표도 같은 기준으로 정리했다.
- 번역 동기화 메모: 향후 번역본도 request/validation/result 공유 중심의 일반 agent 예시를 유지해야 한다.

### v2026.07.07
- 본문 반영: 예제 도구 이름, 상태 키, 출력 제목, 최종 보고를 `요청 확인`, `결과 점검`, `결과 공유`, `성공`, `종료`, `다음 행동` 같은 독자용 한국어 표현으로 바꾸어 실행 루프 개념이 먼저 읽히게 정리했다.
- 번역 동기화 메모: 향후 번역본도 P7-6.1 예제의 공개 상태 키와 도구 이름은 일반 개념 중심 표현으로 유지하고 내부 영어 상태 문자열은 노출하지 않는 편이 좋다.

### v2026.07.12
- 추가 반영: 메타형 연결 예고 제거와 말미 자기 확인 구조 통합을 같은 기준으로 맞췄다.
- 추가 반영: `MCP와의 연결`을 `왜 도구 인터페이스 표준화가 필요한가`로 바꿔 소제목만 읽어도 현재 절의 설명 초점이 드러나게 했다.
- 번역 동기화 메모: 대응 번역본이 생기면 같은 메타 표지 제거와 단일 checklist 구조를 함께 반영해야 한다.

### v2026.07.17
- 본문 반영: `이 절의 범위`에서 다중 에이전트, 장기 기억, 샌드박스 구현 같은 생략 목록을 걷어내고, 이번 절이 `목표 -> 도구 -> 관찰 -> 다음 행동` 실행 루프의 문서 구조를 닫는 자리라는 점이 먼저 보이도록 다시 정리했다.
- 추가 반영: 권한과 로그는 `P7-6.2`, 배포 이후 운영 기록과 실패 추적은 `P7-7.1`, `P7-7.2`로 이어지게 handoff를 분명히 했다.
- 번역 동기화 메모: Future translations should preserve the scope handoff where P7-6.1 closes the agent-loop document structure first and hands permissions/logging to P7-6.2.

### v2026.07.18
- 추가 반영: 말미 체크리스트를 `실행 루프`, `상태 구분`, `blocked 해석`, `상태 보고`, `다음 행동` 축의 확인 표로 압축해 미완료 run 기록 기준이 더 직접 보이도록 정리했다.
- 추가 반영: 본문의 두 Mermaid 도식을 `docs/assets/part-07/chapter-06/*.mmd` 자산으로 분리하고, 자산 폴더 README에 현재 한국어 운영 범위와 blocked 분기 동기화 기준을 기록했다.
- 추가 반영: `오류율 급증 + 최근 배포`를 곧바로 즉시 롤백으로 읽지 않도록, 메트릭·배포 시점·승인 상태를 차례로 확인하게 하는 사례 문단과 Mermaid 흐름도를 보강했다.
- 추가 반영: 회고 예시의 `읽는 편이 적절하다` 같은 독법 안내를 줄이고, blocked run을 실패와 구분해 정리하는 운영 기록 문장으로 더 직접 다듬었다.
- 추가 반영: 말미 소제목을 `다음 절로 이어지는 질문`에서 `이어서 점검할 질문`으로 바꿔, 메타형 이동 안내보다 에이전트 실행 기록 점검 축이 먼저 읽히게 정리했다.
- 추가 반영: 범위 설명과 입력 파일 해설의 `앞 절`, `바로 다음` 표현도 줄이고, 현재 run 기록만으로 blocked 판단 구조가 보이게 문장을 다듬었다.
- 추가 반영: Section 제목을 `도구 호출과 blocked 상태 읽기`에서 `계획, 도구 호출, 승인 흐름 실습`으로 바꾸고, blocked 상태를 포함한 실행 흐름 전체를 읽는 자리라는 현재 목차 역할을 제목에 반영했다.
- 추가 반영: 에이전트 단계 기록 CSV 자산 경로를 본문 링크로 바꿔, 실행 루프 설명과 실제 단계 파일을 바로 연결해 읽을 수 있게 했다.
- 본문 반영: `요청 확인 -> 결과 점검 -> 결과 공유` 수준의 일반 예제를 `결제 API 장애 티켓 -> 메트릭 조회 -> 배포 이력 조회 -> 대응 초안 작성` 흐름으로 교체해, 실제 운영 티켓을 따라 도구를 고르는 장면이 보이도록 다시 구성했다.
- 추가 반영: 조회 도구만으로 바로 변경하지 않고, 관찰을 근거로 안전한 다음 행동을 정하는 에이전트 루프가 중심이라는 점을 표와 실행 기록에서 더 분명히 드러냈다.
- 추가 반영: 단계별 도구 결과를 `docs/assets/part-07/chapter-06/p7-6-agent-triage-steps.csv`로 분리하고, 본문 Python 예제가 CSV 읽기 이후의 실행 기록 재구성과 최종 보고에 집중하도록 정리했다.
- 추가 반영: `입력 파일` 소절을 따로 세워 한 행이 에이전트 루프의 한 단계라는 점과 핵심 열 의미를 먼저 읽고, 그다음 계획 목록과 실행 기록 생성 코드로 넘어가도록 구조를 분리했다.
- 추가 반영: `직접 바꿔 보며 확인할 것` 소절을 추가해 단계 순서와 관찰 문장을 직접 바꾸며 왜 현재 루프가 더 안전한 결론을 만드는지 실험하도록 보강했다.
- 추가 반영: 실행 단계를 6개로 확장해 `승인 요청 작성`, `사용자 상태 보고 작성`, `승인 응답 대기 확인`을 실제 단계로 추가하고, run 이 승인 대기 때문에 blocked 상태로 끝날 수 있다는 점을 직접 드러냈다.
- 추가 반영: 본문 중심 질문을 `도구 호출`만이 아니라 `어디서 blocked 로 멈추는가`까지 포함하도록 다시 정리하고, blocked 단계 표와 최종 보고를 같은 예제 안에서 읽게 보강했다.
- 추가 반영: Python 예제는 `blocked 단계`를 따로 집계해 출력하도록 바꾸고, 최종 보고도 `완료 단계 수`와 `blocked 단계 수`를 함께 남기도록 수정했다.
- 번역 동기화 메모: Future translations should preserve the incident-triage scenario and the read-observe-decide flow instead of reverting to a generic toy agent sequence.
