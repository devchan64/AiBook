# Section Release Note

- Section ID: `P7-5.9`
- Source File: `docs/parts/part-07/chapter-05/section-09.md`

### v2026.09.12

- 39개 채택 표정을 10개 비교 묶음으로 재구성했다. 초기 24개·추가 12개·개별 추가 결과라는 구분과 단계표를 제거하고, 이미지 바로 아래에 움직임 차이·겹침·외형 변화를 설명했다. 윙크·안도·하품과 삐짐·볼 부푼 삐짐을 함께 배치했다. 실행 명령을 공통 안내로 모으고 메모리·시간은 현재 채택한 39개 실행 기록으로 다시 집계했다. 에셋 참조 집합·중복 없는 39개 표정·Markdown 표 구조 확인. 번역 반영 상태: 대응 번역본 없음.

- 사용자 승인으로 경멸 이미지·실행 JSON을 lower-lip-relaxed 결과로 교체했다. 윗입술 비대칭 유지와 아랫입술 쏠림 감소, 남아 있는 돌출을 관찰로 반영했다. 본문 결과 수는 39컷을 유지한다. 원문 기준 버전: v2026.09.12. 번역 반영 상태: 대응 번역본 없음.

- 사용자 요청으로 최신 윙크를 본문에 추가하고 하품 반영 상태를 확인해 결과를 39컷으로 맞췄다. 윙크의 감긴 눈 방향이 지시와 반대인 점을 명시했다. 경멸은 부자연스러운 입술·치아 결과를 삭제하고 기준 프롬프트를 다문 입의 약한 편측 긴장으로 교체해 재생성한다. 번역 반영 상태: 대응 번역본 없음.

- 하품 단일 결과의 이미지·실행 JSON·재현 명령과 시각 검수 내용을 본문에 반영했다. 본문 비교 조건을 38개로 갱신하고 생성 시간을 추가했다. 강도 단계나 시간적 동작을 검증한 결과와 구분했다. 번역 반영 상태: 대응 번역본 없음.

- 사용자 지시로 통합 spec을 표정별 기준 프롬프트 하나씩 총 39개로 정리했다. profiles·retired_designs·legacy_sources를 제거하고 생성기의 --profile 옵션을 삭제했다. 원고·실행 안내를 --expressions 선택 방식으로 동기화했다. 기존 결과 JSON은 실행 당시 기록으로 유지한다. 번역 반영 상태: 대응 번역본 없음.

- 사용자 지시로 하품 강도 단계 설계를 폐기하고 기존 단계별 자산을 `.tmp/discarded/`로 이동했다. 제공된 샘플의 얼굴 움직임을 짧게 기술한 단일 `yawn-reference-v1` 설계를 통합 spec에 추가했다. 샘플 사진은 원고 자산이나 모델 입력에 포함하지 않고 Mira 원본을 편집한다. 원고 설계 목록 동기화. 번역 반영 상태: 대응 번역본 없음.

- 하품 지시를 핵심 동작 중심의 짧은 문구로 교체한 `yawn-intensity-v3`를 통합 spec에 추가했다. 중간 단계의 과한 세로 벌림·검은 입안 지시를 작은 타원형 입과 부드러운 혀·윗니 음영으로 교체하고 같은 조건으로 재생성한다. 원고의 설계 목록을 동기화했다. 번역 반영 상태: 대응 번역본 없음.

- 사용자 지시로 참는 웃음 실험을 종료했다. v1·v2 이미지·실행 JSON과 통합 spec의 활성·폐기 설계를 삭제하고 원고에서 수정 설계 안내를 제거했다. 실패 검수 이력만 관리 문서에 남겼다. 번역 반영 상태: 대응 번역본 없음.

- 실패한 하품 3단계 `yawn-intensity-v1`을 사용자 지시로 폐기하고 PNG·JSON을 `.tmp/discarded/p7-5-9/yawn-intensity-v1/`로 이동했다. 통합 spec에 하품 동작과 턱 내림·입술 분리를 앞세운 `yawn-intensity-v2`를 추가했다. 중간 단계부터 재검증하며 원고의 설계 목록을 동기화했다. 번역 반영 상태: 대응 번역본 없음.

- 윙크 v2를 사용자 지시로 폐기하고 PNG·JSON을 `.tmp/discarded/p7-5-9/wink-v2/`로 이동했다. 통합 spec의 `wink-v3`에 감긴 눈 아래 윗볼 상승·피부 압축을 명시하고 원고의 설계 목록을 갱신했다. AU46·AU6은 실험 설계 참고이며 필수 조합이나 측정값이 아니다. 번역 반영 상태: 대응 번역본 없음.

- 볼 부푼 삐짐 `puffed-pout-v2` 이미지와 실행 JSON을 본문에 반영해 비교 결과를 37컷으로 확장했다. 별도 소제목을 ‘볼 부푼 삐짐’으로 명시하고 결과를 표에 배치했다. 과도한 볼 팽창 감소와 목표 움직임 약화를 함께 기술하고 bash 재실행 명령을 추가했다. 목차·학습 체크포인트의 결과 수를 동기화했다. 원문 기준 버전: `v2026.09.12`. 번역 반영 상태: 대응 번역본 없음.

- 기존 참는 웃음 v1을 사용자 지시로 폐기하고 PNG·JSON을 `.tmp/discarded/p7-5-9/suppressed-laugh-v1/`로 옮겼다. 통합 spec에 약한 볼·입꼬리 상승과 입술 압착을 조합한 `suppressed-laugh-v2`를 추가하고 원고의 설계 목록을 갱신했다. AU는 설계 참고이며 감정 판정 공식이나 측정값이 아니다. 번역 반영 상태: 대응 번역본 없음.

- 기존 하품 v1을 사용자 지시로 폐기하고 통합 spec의 생성 대상에서 제외했다. PNG·JSON은 `.tmp/discarded/p7-5-9/yawn-v1/`로 옮기고, 대체 설계 `yawn-intensity-v1`의 약한·중간·큰 하품을 사용한다. 원고의 설계 목록을 동기화했다. 번역 반영 상태: 대응 번역본 없음.

- 사용자 지시로 볼 부푼 삐짐 v1을 폐기하고 생성 대상에서 제외했다. 기존 PNG·JSON은 `.tmp/discarded/`로 옮기고 통합 spec의 `retired_designs`에 사유와 대체 설계 `puffed-pout-v2`를 기록했다. 원고의 설계 버전 설명을 동기화했다. 번역 반영 상태: 대응 번역본 없음.

- 사용자 지시로 윙크 v1을 폐기 처리했다. 통합 spec의 `distinct-v1` 생성 대상에서 제외하고 `retired_designs`에 이력만 보관하며 `wink-v2`를 대체 설계로 지정했다. 원고의 설계 버전 설명과 결과 추적 문서를 갱신했다. 번역 반영 상태: 대응 번역본 없음.

- 표정 설계 JSON 7개를 `p7-5-9-mira-expression-spec.json` 한 파일로 통합했다. 생성기는 `--profile`로 버전을 선택하며 원고와 관리 문서의 명령·링크를 갱신했다. 기존 실행 JSON은 당시 경로·해시를 그대로 보존하고 통합 파일의 `legacy_sources`에서 이전 파일과 설계 버전을 대응시킨다. 일곱 버전의 통합 전후 dry-run 프롬프트·설정 일치를 확인했다. 원문 기준 버전: `v2026.09.12`. 번역 반영 상태: 대응 번역본 없음.

- 수정 경멸·혐오 계열 4컷과 추가 일상 반응 12컷을 반영해 본문을 36컷으로 확장했다. 이미지와 실행 JSON은 각각 독립 링크로 연결하고 `--spec` 재실행 명령을 추가했다. 코 붉어짐 감소와 움직임 약화, 추가 표정의 의도 불일치·중복 가능성을 관찰로 명시했다. 같은 날 앞서 보류한 네 컷은 수정 결과로 대체했다. 관련 자산: expansion-v1 15쌍과 nose-wording-v1 혐오 1쌍 및 설계 JSON. 번역 반영 상태: 한국어만 작성, 대응 번역 없음. 원문 기준 버전: `v2026.09.12`.

- 사용자 검수에 따라 경멸·반감·혐오·극심한 혐오 네 컷과 해당 결과 링크를 본문 비교 표에서 보류하고, 나머지 다섯 계열·20컷을 반영했다. 24개 생성 자산과 실행 기록은 보존한다. 코 찡그림 조건에서 나타난 붉어짐을 프롬프트·실행 기록과 대조하고, 확인된 사실과 원인 가설·미실행 비교 실험을 구분했다. 원문 기준 버전: `v2026.09.12`. 번역 반영 상태: 한국어만 작성, 대응 번역본 없음.

- 대표 여섯 조건을 검수한 뒤 같은 설정으로 전체 24개 조건을 생성했다. 본문 결과를 계열별 4열 이미지 표와 개별 실행 JSON 링크로 확장하고, 단계 차이가 작은 표정·코 붉어짐·강한 주름·눈물 같은 자국·턱과 목의 외형 변화를 관찰로 기록했다. 24개 생성 완료와 학습 자료 채택 판단을 구분했다.
- 실행 조건은 1024×1024·20스텝·seed 62294·CFG 4.0·BF16·순차 CPU 오프로딩이다. 완료된 24회 추론 시간 합계는 약 2시간 27분 12초이며 모델 로딩과 중단된 실행은 제외했다. 24개 조건 목록, PNG 로딩·크기·서로 다른 출력 해시, 입력·생성기·도우미·설계 JSON 해시를 실행 기록과 대조했다. 관련 자산: 표정 PNG 24장, 실행 JSON 24개, 표현 설계 JSON, Python 생성기. 모델 인벤토리에 생성기를 등록했다. 중단된 실행의 미완료 결과는 본문에 포함하지 않았다.
- 사용자 표정 목록을 참고해 6개 계열·4단계의 24개 조건으로 확장했다. 각 표정의 독립적인 영어 움직임 지시와 AU 참고를 JSON으로 분리했다. AU 참고값은 측정값이 아니며, 네 단계는 FACS 강도 척도가 아님을 명시했다. 원문 기준 버전: `v2026.09.12`. 번역 반영 상태: 한국어만 작성, 영어·중국어 대응 원고 없음.

### v2026.09.11

- 기존 StoryDiffusion 원고의 폐기 후 같은 Section ID를 보충학습 ‘Mira의 표정 바꾸기’에 사용했다. P7-5.2 BF16 정면 머리를 공통 입력으로 여섯 표정을 독립 편집하는 Qwen-Image-Edit-2511 생성기·실행 명령·비교 기준을 추가했다. 번역 반영 상태: 한국어 원문만 작성, 영어·중국어 대응 원고 없음.
- 사용자 지시에 따라 기존 P7-5.9 원고를 폐기하고 배포 목차·독자용 목차·학습 체크포인트에서 제거했다. 대체 원고는 작성하지 않았다. 실험 자산과 조사 자료는 보존했다. 번역 반영 상태: 대응 영어·중국어 원고 없음.
- 기존 SeaArt 포트 멀티뷰 실험을 연속 장면 생성 목표 미달·공식 CSA 재현 여부 미확인으로 정리했다. 성공 사례와 재실행 안내를 본문에서 제거하고, ‘8GB에서 검증된 StoryDiffusion 보강’ 결론을 철회했다. 기존 자산은 실험 이력으로 보존했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.11`.
- 출처와 참고 자료를 작성자·자료명·게시처·URL·확인일 중심으로 통일하고, 실행 조건·검수 설명과 구분했다. 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.11`.

### v2026.09.02

- StoryDiffusion의 묶음 단위 character·style 일관성과 단일 이미지의 선화·pose 보존을 분리해 설명하고, 멀티뷰 VAE 앵커 보강의 실행 기록을 연결했다.
- 8GB 환경에서의 단일 캐릭터 멀티뷰 보강 관찰은 남기되, 빈 잠재값에서 새 구도를 만드는 일반 경로로 확대하지 않았다.
- 상태 등급 표현을 관찰된 일치·이탈 표현으로 정리했다.
- 번역 반영 상태: 한국어 원문만 반영됨. 원문 기준 버전: `v2026.09.02`.
