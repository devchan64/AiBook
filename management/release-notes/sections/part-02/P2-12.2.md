# Section Release Note

- Section ID: `P2-12.2`
- Source File: `docs/parts/part-02/chapter-12/section-02.md`

### v2026.07.17
- 핵심 변경: `이 절의 범위`에서 정제·결측치·범위 밖 항목을 앞세우던 문장을 줄이고, 이번 절이 닫는 질문인 열 선택, 행 필터, 집계, `groupby` 구분이 먼저 보이도록 정리했다.
- 번역 동기화 메모: 영어 `section-02.en.md`와 중국어 간체 `section-02.zh.md`도 같은 범위 안내 구조로 후속 동기화가 필요하다.

### v2026.07.09
- 핵심 변경: `이 절의 범위`에서 중심 질문을 먼저 세우고, `P2-12.3`과 이후 Pandas/모델 입력 준비 구간으로 이어지는 짧은 읽기 흐름을 추가해 표 읽기 동작의 역할 차이를 더 직접적으로 보이게 다듬었다.

### v2026.07.10
- 핵심 변경: 한국어 본문 `P2-12.2`의 표 읽기 흐름 Mermaid를 한글 중심 라벨로 다시 정리해, 전체 표에서 열 선택, 행 선택, 조건 필터링, 집계로 이어지는 질문 흐름이 본문 설명과 같은 언어로 바로 이어지게 맞췄다.
- 번역 동기화 메모: 이번 변경은 한국어 본문 도식 라벨 보정이다. 영어판과 중국어간체판은 각 언어 흐름에 맞춰 별도로 유지할 수 있다.

### v2026.07.11
- 핵심 변경: 두 Mermaid 도식을 각각 `table-reading-flow-ko.mmd`, `table-processing-flow-ko.mmd` include로 전환하고, 대응 영문 원본 자산 쌍을 `docs/assets/part-02/chapter-12/`에 함께 추가했다. 영어판과 중국어간체판 본문도 같은 자산 세트를 include하도록 맞추고, 대응 `-zh.mmd` 자산을 추가했다. 추가로 같은 도식 세트의 영문·한국어·중국어 자산을 도식별 하위 디렉터리로 묶어, 언어별 파일을 한곳에서 관리하도록 소스트리를 정리했다.
- 번역 동기화 메모: English and Simplified Chinese pages now include the shared Mermaid asset set, with `-en.mmd` as the canonical source and `-ko.mmd` / `-zh.mmd` as synchronized derivatives.

### v2026.07.12
- 핵심 변경: 한국어 원문 말미의 `이 절에서 기억할 관점`, `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`를 `체크리스트` 하나로 통합하고, 열 선택, `loc`/`iloc`, 조건 필터링, 집계와 `groupby`를 표 읽기 기준으로 한 번에 점검하도록 정리했다.
- 번역 반영: 영어판과 중국어 간체판 말미의 메타형 회고 블록과 연결 예고 표를 제거하고 `Checklist`/`检查清单`을 한국어 원문 기준으로 맞추며 `Version`을 `v2026.07.12`로 동기화했다.
