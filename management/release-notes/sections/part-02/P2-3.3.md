# Section Release Note

- Section ID: `P2-3.3`
- Source File: `docs/parts/part-02/chapter-03/section-03.md`

### v2026.07.05

- 변경 이유: Section 버전 관리 규칙 적용, 내부 참조 정합성 보정. / 행렬 곱 절의 메모형 블록 추가 정리. / 행렬 곱 도입 전개 정리. / 챕터 단위 재방문 안내의 인덱스 표기 정리.
- 본문 반영: 메타데이터 추가, `P2-3.4`를 `P2-3.5`로 수정. / 도입 질문, 위치별 곱/행렬 곱 구분, 가중합·선형 변환·재사용 요약을 번호 목록과 일반 문장으로 다시 정리했다. / 가중합 재사용 도입 요약을 번호 목록으로 정리. / `P2-6`처럼 섹션 ID처럼 보이던 표기를 `Part 2 Chapter 6` 형식으로 정리.
- 번역 동기화 메모: 번역본이 생기면 같은 `Section ID` 기준으로 메타데이터와 Chapter 3 내부 참조를 함께 맞춰야 한다. / 향후 반영 필요.
- 번역 반영 상태: 미작성.
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.05`

### v2026.07.06

- 변경 이유: Part 2 안에서 행렬 곱과 선형 변환을 다시 읽는 대표 상세 설명 위치를 분명히 하고, 가중합 설명과 코드 실습 절의 경계를 더 선명하게 하기 위해 정리.
- 본문 반영: `행렬 곱(matrix multiplication)`, `가중합(weighted sum)`, `가중치 행렬(weight matrix)`, `선형 변환(linear transformation)`, `입출력 차원(input/output dimension)` 대표 설명 위치 문단 추가. / 핵심 용어 재소개 표 추가. / 행렬식·역행렬과 NumPy 실습 세부는 현재 절 범위가 아니라는 경계 문장 보강.
- 번역 동기화 메모: 번역본이 생기면 대표 상세 설명 위치 문단, 용어 재소개 표, 범위 경계 문장을 같은 `Section ID` 기준으로 함께 맞춰야 한다. / 향후 반영 필요.
- 번역 반영 상태: 미작성.
- 관련 자산: `docs/reference/concept-glossary.md`
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-2

- 변경 이유: Part 2 시작 페이지와 앞 절들이 이미 읽기 기준을 제공하는데도 이 절에 `처음 읽을 때는`, `처음 단계에서는` 식의 메타 도입이 반복되어, Part 안의 설명 중복을 줄일 필요가 있었다.
- 본문 반영: 행렬 곱 입문 설명과 `이 절의 범위`에서 반복 메타 도입 문장을 제거하고, `행렬 곱·가중합·가중치 행렬·선형 변환·입출력 차원` 표와 뒤 절 연결 표는 유지해 절의 역할이 바로 보이도록 정리했다.
- 번역 동기화 메모: 번역본이 생기면 메타 도입 축약과 표 중심 구성도 같은 `Section ID` 기준으로 함께 맞춰야 한다. / 향후 반영 필요.
- 번역 반영 상태: 미작성.
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06`

### v2026.07.06-3

- 변경 이유: 사례 마무리 문장에 `유용합니다` 같은 완화형 표현이 남아 있어, 행렬 곱을 재사용 가능한 가중합 구조로 읽는 관점을 직접 서술형으로 더 맞출 필요가 있었다.
- 본문 반영: 사례 1의 마무리 해설을 직접 설명형 문장으로 정리해 `입력을 다른 표현 공간으로 바꾸는 재사용 가능한 계산`이라는 핵심 관점이 본문 자체로 드러나게 맞췄다.
- 번역 동기화 메모: preserve the matrix-multiplication takeaway as direct explanatory prose rather than softening phrasing. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.06-3`
