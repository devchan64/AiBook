# Section Release Note

- Section ID: `P2-2.4`
- Source File: `docs/parts/part-02/chapter-02/section-04.md`

### v2026.07.08

- 변경 이유: 초심자 기준에서는 로그와 지수를 모르면 Part 4의 로그 손실, Part 5의 활성화 함수, Part 6의 softmax 설명이 갑자기 불투명해지는 문제가 있었다.
- 본문 반영: `로그(log)와 지수(exp)는 왜 반복해서 등장하는가` Section을 실제 본문 역할을 하도록 확장해, 비율 변화 표, 곱셈을 로그 합으로 읽는 예시, 점수에서 softmax와 log loss로 이어지는 짧은 계산 장면을 추가했다.
- 추가 정리: 개념사전 입구를 연결하고, `뒤 Part 예고`보다 `점수 해석 언어` 자체가 먼저 보이도록 목표와 소제목 문장을 정리했다.
- 번역 반영 상태: 향후 번역 반영 필요

### v2026.07.09

- 변경 이유: 독자가 `exp`, `log`, 확률 곱, log loss, softmax를 각각 이해해도 하나의 계산 장면으로 다시 묶지 못하면 뒤 Part에서 `점수 -> 비율 -> 손실` 연결이 다시 끊길 가능성이 남아 있었다.
- 본문 반영: `먼저 붙잡을 한 장면` 표, `곱셈/로그/점수/손실` 질문 표, softmax 준비 단계 요약 표, `-log(p)` 직관 표, 기억할 관점과 짧은 점검을 추가했다.
- 번역 동기화 메모: 한국어 원문에서 로그와 지수를 뒤 Part 점수 해석 장면과 직접 연결하는 요약 표와 점검 질문이 보강되었으므로, 번역본에서도 같은 계산 흐름을 유지해야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.09`
