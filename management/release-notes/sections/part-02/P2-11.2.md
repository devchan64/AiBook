# Section Release Note

- Section ID: `P2-11.2`
- Source File: `docs/parts/part-02/chapter-11/section-02.md`

### v2026.07.09
- 핵심 변경: `shape`, 행/열, 샘플/특징을 한 장면으로 다시 묶는 요약 블록과 데이터셋 언어 대응 표를 추가하고, `shape = (샘플 수, 특징 수)` 관점이 뒤의 `X`, `y` 읽기로 이어진다는 문장을 보강했다.

### v2026.07.10
- 핵심 변경: 영어판 공용 자산을 `docs/en/assets -> ../assets` 연결을 통해 locale 경로에서 다시 읽게 정리하고, `docs/parts/part-02/chapter-11/section-02.en.md`의 도식·예제 링크가 `en/assets/...`를 안정적으로 가리키도록 맞췄다.

### v2026.07.12
- 핵심 변경: 한국어 원문 말미의 `이 절에서 기억할 관점`, `짧은 점검`, `언제 이 관점을 먼저 떠올려야 하는가`를 `체크리스트` 하나로 통합하고, 인덱싱·슬라이싱·축 구분을 점검 항목으로 재구성했다.
- 번역 반영: 영어판과 중국어 간체판의 `Version`을 한국어 원문 기준 `v2026.07.12`로 동기화하고, 체크리스트 구조를 원문 기준으로 다시 맞췄다.

### v2026.07.17
- 핵심 변경: `이 절의 범위`를 생략 공지형 문장보다 `배열에서 어느 값을 고르고 어느 방향으로 계산하는가`라는 현재 절의 역할이 먼저 보이도록 다시 정리했다.
- 핵심 변경: 고급 인덱싱, 불리언 마스크, `np.newaxis`, view/copy 세부 차이는 `P2-11.4` 보충학습 handoff로 분리해 현재 절의 기본 읽기 축이 먼저 남도록 정리했다.
