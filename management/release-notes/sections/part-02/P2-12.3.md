# Section Release Note

- Section ID: `P2-12.3`
- Source File: `docs/parts/part-02/chapter-12/section-03.md`

### v2026.07.09

- 변경 이유: 독자가 `X`, `y`, sample, feature, shape를 각각 이해해도 한 장면으로 다시 묶지 못하면 Part 3의 첫 학습 코드에서 다시 막힐 가능성이 있었다. 또한 `train에서만 기준을 배운다`는 원칙이 설명은 되지만 순서 감각으로는 덜 남아 있었다.
- 본문 반영: `X`, `y`, `shape`를 함께 읽는 시작 블록을 추가하고, `잘못된 순서 vs 안전한 순서`를 도식과 표로 다시 정리했다. `X.shape`, `y.shape`가 샘플 수와 특징 수를 어떻게 드러내는지도 직접 연결했다.
- 추가 정리: 도입부의 핵심 정의 문장을 인용문 블록 대신 일반 문장으로 정리해 집필 톤 정합성을 맞췄다.
- 번역 동기화 메모: 기존 영문·중문 번역본이 데이터셋 직관만 짧게 요약한 상태여서, 한글 원문의 `X`/`y` 분리, 식별자·정답 열 구분, train/validation/test 분리, 누수 방지 순서, Pandas와 scikit-learn의 역할 구분, 사례와 점검 문항까지 같은 구조와 질문 흐름으로 다시 보강했다.
- 번역 반영 상태: 영문/중문 반영 완료
- 관련 자산: `docs/en/parts/part-02/chapter-12/section-03.md`, `docs/zh/parts/part-02/chapter-12/section-03.md`, `mkdocs.yml`
- 원문 기준 버전: `v2026.07.09`

### v2026.07.10

- 변경 이유: 구조 표지가 현재 원칙의 `사례 및 예시` 축과 어긋난 부분을 정리할 필요가 있었다.
- 본문 반영: `사례로 보기` 표지를 `사례 및 예시`로 통일하고 Section 버전을 갱신했다. 내용 추가 없이 구조 표지만 정리했다.
- 번역 동기화 메모: 향후 번역본이나 다른 언어 원고가 생기면 같은 위치의 구조 표지도 `사례 및 예시` 축으로 맞춰야 한다.
- 번역 반영 상태: 향후 반영 필요
- 관련 자산: 없음
- 원문 기준 버전: `v2026.07.10`
