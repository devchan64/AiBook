# Section Release Note

- Section ID: `REFERENCE-concept-glossary`
- Source File: `docs/reference/concept-glossary.md`

### v2026.07.18

- 변경 이유: 개념사전 전반에서 서로 다른 층위의 개념들이 짧은 정의만으로 남아 있어, 재학습용 참조 문서로서의 구분력과 학습 밀도를 높일 필요가 있었다.
- 본문 반영: 상단 메타데이터를 정비한 뒤, 모델·데이터·학습·평가·검색·운영·도구 사용·버전 관리·기초 프로그래밍·기초 수학/통계·노트북 실행·지식표현·프롬프트 설계 계열 표제어의 `뜻`과 `왜 중요한가`를 단계적으로 보강했다. 이번 범위에서는 검색 구조, 연결 구조, 실행 허용 범위, 운영 기록, 서비스 성능 지표, 표 데이터 읽기, 배열 계산, 학습-평가 분리, 생성 설정, 시퀀스 모델, 도구 실행 제어, 확률 해석, 기초 통계, 버전 관리, 노트북 실행 상태, 자료구조 선택, 시각화 구분, 규칙 기반 판단, 강화학습, 에이전트 층위, 실행 환경 분리 같은 재학습 핵심 흐름을 넓게 정리했다.
- 본문 반영: 특히 `LLM`, `BERT`, `GPT`, `대화형 LLM`, `BPE`, `비전 트랜스포머`, `레이어 정규화`, `배치 정규화`, `병렬 처리`, `넘파이`, `newaxis`, `노트북`, `드롭아웃`, `dtype`, `규칙 기반 시스템`, `강화학습`, `강화학습 에이전트`, `가상환경`, `고정 길이 벡터`, `게이트 순환 유닛`, `자료구조`, `벡터화`, `검색 증강 생성`, `검색 인덱스`, `그레이더`, `근거`, `환각`, `확률 모델`, `확률 추정값`, `Figure`, `Axes`, `groupby`, `Git` 등에서 `무엇을 뜻하는가`뿐 아니라 `무엇과 구분해야 하는가`, `왜 다음 Part 이해에 다시 등장하는가`를 더 분명히 적었다.
- 본문 반영: `브로드캐스팅`, `바운딩 박스`, `분산 표현`, `매핑`, `말뭉치`도 추가로 풀어 써서, 배열 축 정렬 해석 흐름, 검출과 분류 출력 구조 구분 흐름, 희소 기호와 연속 표현 구분 흐름, 표현 체계 변환 흐름, 학습 자료 성격과 모델 성향 연결 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `문장·문단·문서 임베딩`, `멀티헤드 어텐션`, `미세조정`, `LoRA`, `adapter`, `QLoRA`도 추가로 보강해, 긴 텍스트 단위 표현 해석 흐름, 다중 관계 시선 해석 흐름, 사전학습-적응 단계 구분 흐름, 효율적 조정 전략 비교 흐름을 더 또렷하게 연결했다.
- 본문 반영: `반복`, `셸`, `셸 스크립트`, `앱`, `옵티마이저`도 추가로 보강해, 반복 제어와 벡터화 대비 흐름, 명령 해석기와 터미널 구분 흐름, 운영 절차 자동화 해석 흐름, 모델-제품 경험 구분 흐름, gradient 방향과 업데이트 규칙 구분 흐름을 더 분명하게 읽을 수 있게 했다.
- 본문 반영: `WaveNet`, `YOLO`, `업무 자동화`, `언어 모델`, `정보 검색`도 추가로 보강해, 직접 계보와 주변 근거 구분 흐름, 검출 파이프라인과 end-to-end 예측 구분 흐름, 업무 단계 분해 기반 자동화 흐름, 언어 모델과 대화형 제품 구분 흐름, 검색과 생성 역할 분리 흐름을 더 또렷하게 연결했다.
- 본문 반영: `출력 구조`, `재실행 가능한 기록`, `요청 실행 기록`, `최신성`, `작업 폴더`도 추가로 보강해, 예측 결과와 업무 산출물 설계 구분 흐름, 보기 좋은 노트북과 다시 돌릴 수 있는 기록 구분 흐름, 로그 조각과 요청 단위 실행 복원 흐름, 사실성과 시점 유효성 구분 흐름, 상대 경로와 실행 위치 해석 흐름을 더 분명하게 읽을 수 있게 했다.
- 본문 반영: `문서 재현성`, `사실 주장`, `사실성`도 추가로 보강해, 본문-자산-설정-배포 재구성 흐름, 검증 대상 문장 선별 흐름, 자연스러움과 사실 일치 여부 구분 흐름을 더 또렷하게 연결했다.
- 본문 반영: `목표`, `메타데이터`, `맥락`, `시스템`, `실행 환경(execution environment)`도 추가로 보강해, 작업 완료선과 문제 정의 구분 흐름, 의미 검색과 문서 운영 정보 결합 흐름, 지시문과 배경 조건 해석 흐름, 모델과 전체 운영 단위 구분 흐름, 모델 한계와 환경 제약 분리 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `sparse attention`, `보강`, `생성형 AI`, `AI`, `운영`도 추가로 보강해, 긴 문맥 처리의 구조적 절충 흐름, 검색과 입력 결합 설계 흐름, 출력 범주와 모델 계열 구분 흐름, 상위 분야와 하위 기술 층위 구분 흐름, 개발과 지속 운영 관리 구분 흐름을 더 분명하게 읽을 수 있게 했다.
- 본문 반영: `토큰화`, `통계적 언어 모델`, `생성`, `생성 모델`, `정확도`도 추가로 보강해, 언어를 계산 단위로 자르는 관문 흐름, 빈도 기반 언어 예측에서 신경망 언어 모델로 넘어가는 계보 흐름, 선택 문제와 생성 문제 구분 흐름, 생성 출력 평가 축 흐름, 기본 지표와 지표 한계 구분 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `사실`, `설명 기능`, `프롬프트`, `프롬프트 엔지니어링`, `Chain-of-thought`도 추가로 보강해, 현재 관찰과 일반 규칙 연결 흐름, 설명 가능성과 근거 경로 재구성 흐름, 입력 설계와 모델 갱신 구분 흐름, 프롬프트 실험 반복 흐름, 추론 경로 노출과 사실 검증 구분 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `시그마`, `스칼라`, `학습된 표현`, `표현 학습`, `하이퍼파라미터`도 추가로 보강해, 반복 합산 표기와 전체 요약값 해석 흐름, 큰 텐서 계산과 단일 판단값 구분 흐름, 내부 의미 공간 형성 흐름, 수작업 특징과 표현 학습 구분 흐름, 학습 내부값과 바깥 설정값 구분 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `열`, `ndarray`, `ndim`, `휴리스틱 함수`, `하네스`도 추가로 보강해, 표 변수 축과 역할 구분 흐름, 계산 가능한 배열 객체 해석 흐름, 값 개수와 축 개수 구분 흐름, 유망도 정렬 함수 해석 흐름, 데모 실행과 반복 측정 환경 구분 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `결정트리`, `그래디언트 부스팅`, `행`, `해시 테이블`, `히스토그램`, `환경 변수`, `회귀`도 추가로 보강해, 질문 기반 분할과 일반화 한계 구분 흐름, 독립 평균형 앙상블과 순차 오차 보정형 앙상블 구분 흐름, 표의 한 줄과 현실 사례 단위 대응 흐름, 순차 탐색과 키 기반 접근 구분 흐름, 구간 분포 읽기와 시각화 해석 한계 흐름, 코드 로직과 실행 환경 설정 분리 흐름, 분류와 연속 수치 예측 평가 구분 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `관찰`, `보상`, `정책`, `탐험`, `휴리스틱 점수`, `활용`, `행동`도 추가로 보강해, 행동 뒤 새 정보 획득 흐름, 결과 피드백과 장기 목표 정렬 흐름, 반복 선택 기준 해석 흐름, 정보 수집과 즉시 성과 균형 흐름, 점수와 확률 의미 구분 흐름, 탐험-활용 긴장 해석 흐름, 내부 사고와 바깥 실행 단위 구분 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `샘플링`, `temperature`, `유사도 검색`, `유사도`, `정책 기반 강화학습`, `평가 지표`, `토큰`도 추가로 보강해, 후보 분포와 실제 출력 선택 구분 흐름, 생성 다양성과 모델 지식 구분 흐름, 키워드 검색과 벡터 검색 연결 흐름, 의미적 근접성과 사실 정답 구분 흐름, value-based 접근과 policy 직접 최적화 접근 비교 흐름, 수치 평가와 업무 판단 기준 연결 흐름, 문자 길이와 실제 계산 단위 구분 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `참조`, `추상 자료형`, `테이블`, `테스트`, `특징 선택`, `필터링`, `전체 비교`, `풀링`도 추가로 보강해, 이름과 객체 공유 관계 해석 흐름, 동작 규칙과 내부 구현 분리 흐름, 행-열 표 구조와 입력 구성 연결 흐름, 검증과 최종 점검 분리 흐름, 입력 후보 축소와 일반화 판단 흐름, 조건 선택과 후속 집계 해석 흐름, 정확한 기준선 검색과 근사 검색 대비 흐름, 공간 축소와 핵심 반응 보존 절충 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `키`, `얕은 복사`, `패스 환경 변수`, `패키지`, `복사`, `Python 인터프리터`, `pip`, `파이프`도 추가로 보강해, 위치 기반 접근과 이름 기반 접근 구분 흐름, 바깥 컨테이너와 안쪽 객체 공유 구분 흐름, 실행 파일 탐색 경로와 설치 여부 분리 흐름, 언어와 외부 라이브러리 구분 흐름, 안전한 사본과 메모리 비용 절충 흐름, 언어와 실행 프로그램 구분 흐름, 설치 도구와 실행 환경 연결 흐름, 명령 결과 소비 흐름 해석을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `랜덤포레스트`, `평가 모드`, `편향 항`, `퍼셉트론`, `파라메트릭 메모리`, `프롬프트의 한계`, `반환값`도 추가로 보강해, 병렬 앙상블과 순차 보정 앙상블 구분 흐름, 학습 계산과 평가 계산 분리 흐름, 기울기 조정값과 기본 위치 조정값 구분 흐름, 신경망 기본 계산 단위 해석 흐름, 내부 기억과 외부 검색 근거 구분 흐름, 입력 표현 개선과 구조적 한계 구분 흐름, 화면 출력과 다음 계산 재료 구분 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `캐싱`, `클라이언트`, `품질`, `평가`, `로지스틱 회귀`, `자동 평가`, `사람 평가`, `평균 제곱 오차`도 추가로 보강해, 재계산과 재사용 절충 흐름, 요청 시작 주체와 처리 주체 분리 흐름, 보기 좋은 결과와 실제 사용 가능 결과 구분 흐름, 측정 절차와 문제 정의 연결 흐름, 선형 분류 기준선과 임계값 해석 흐름, 반복 채점과 맥락 해석 역할 분담 흐름, 평가자 기준 합의 필요 흐름, 큰 오차 민감도와 손실 해석 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `재실행 가능한 기록`, `요청 실행 기록`, `회고`, `Chain-of-thought`, `self-consistency`, `automatic prompt optimization`도 추가로 보강해, 보기 좋은 노트와 다시 검증 가능한 기록 구분 흐름, 로그 조각과 요청 단위 실행 복원 흐름, 소감 정리와 다음 반복 설계 구분 흐름, 중간 추론 노출과 사실 보장 구분 흐름, 경로 합의와 외부 근거 확인 구분 흐름, 프롬프트 감각 수정과 평가 루프 기반 탐색 구분 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: `표본`, `표준편차`, `표준오차`, `표현`, `활성값`, `활성화 함수`, `학습률`, `확률 추론`도 추가로 보강해, 손에 든 데이터와 전체 대표성 구분 흐름, 평균 크기와 변동성 구분 흐름, 데이터 퍼짐과 추정 흔들림 구분 흐름, 원본 입력과 내부 좌표계 구분 흐름, 최종 답과 중간 반응값 구분 흐름, 층 수와 비선형 반응 곡선 역할 구분 흐름, 이동 방향과 이동 크기 구분 흐름, 확정 규칙과 불확실성 기반 판단 구분 흐름을 더 선명하게 읽을 수 있게 했다.
- 본문 반영: 중복으로 남아 있던 `불리언 마스크` 블록도 정리해, 모델-제품-도구 층위 구분 흐름, 토큰 분절 기준 해석 흐름, 배치 통계와 위치별 정규화 구분 흐름, 이미지 패치 토큰화 해석 흐름, 행렬 중심 대량 계산 해석 흐름, 서비스형 대화 LLM 층위 해석 흐름, 배열 shape 조정 해석 흐름, 상호작용 문서 실행 상태 해석 흐름, 장기 보상과 연속 선택 해석 흐름, 서비스형 에이전트와 강화학습 에이전트 층위 구분 흐름을 더 안정적으로 연결했다.
- 번역 동기화 메모: 영어판 개념사전에도 이번에 보강한 모델·데이터·평가·검색·운영·도구 사용·기초 프로그래밍·기초 수학/통계 계열 표제어의 설명 밀도를 같은 수준으로 맞출 필요가 있다. 특히 `LLM`, `BPE`, `Vision Transformer`, `layer normalization`, `parallel processing`, `NumPy`, `np.newaxis`, `notebook`, `conversational LLM`, `dropout`, `dtype`, `rule-based system`, `reinforcement learning`, `reinforcement learning agent`, `virtual environment`, `fixed-length vector`, `GRU`, `search index`, `grader`, `retrieval-augmented generation`, `evidence`, `hallucination`, `Figure`, `Axes`, `groupby`, `Git` 주변의 구분 설명과 해석 흐름을 한국어판과 대응되게 반영해야 한다.
- 번역 반영 상태: 향후 번역 반영 필요

### v2026.07.16

- 변경 이유: Part 5 개편을 파일 경로와 Section ID까지 반영하면서 개념사전의 중심 Section과 등장 Section도 새 위치에 맞춰야 했다.
- 본문 반영: `배치(batch)`, `배치 정규화(batch normalization)`, `수치 안정성(numerical stability)`, `초기화(initialization)`, `평가 모드(evaluation mode)`, `학습 모드(training mode)` 관련 Section 참조를 새 P5-6/P5-8 구조에 맞게 갱신했다.
- 번역 반영 상태: 향후 번역 반영 필요

### v2026.07.10

- 변경 이유: `P3-2.1` 등에서 `#glossary-dataset`으로 직접 링크를 거는데, `데이터셋(dataset)` 표제어에는 명시적 앵커가 없어 링크 안정성이 heading slug에 의존하고 있었다.
- 본문 반영: `데이터셋(dataset)` 표제어 앞에 `<a id="glossary-dataset"></a>`를 추가해, 개념사전 직접 링크가 다른 핵심 표제어와 같은 방식으로 안정적으로 동작하도록 맞췄다.
- 번역 반영 상태: 향후 번역 반영 필요

### v2026.07.08

- 변경 이유: Part 4 Chapter 7 재배치 이후 특징 선택 관련 보충학습의 등장 Section 번호가 바뀌어 개념사전 참조 위치를 다시 맞출 필요가 있었다.
- 본문 반영: `특징 선택(feature selection)` 항목과 `차원 축소(dimensionality reduction)` 항목의 등장 Section 목록에 `P4-7.4`를 반영해, 필터·래퍼·차원 축소 구분 보충학습의 새 번호 체계를 맞췄다.

### v2026.07.11

- 변경 이유: Part 5에 학습 안정화 보충학습 `P5-6.3`이 추가되면서, 해당 절의 핵심 용어인 `배치 정규화(batch normalization)`, `수치 안정성(numerical stability)`, `초기화(initialization)`를 개념사전에서도 바로 다시 찾을 수 있어야 했다.
- 본문 반영: 개념사전에 `배치 정규화(batch normalization)`, `수치 안정성(numerical stability)`, `초기화(initialization)` 항목을 추가하고 중심 Section을 `P5-6.3`으로 연결했다.
- 번역 반영 상태: 향후 번역 반영 필요
