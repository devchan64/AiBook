# Section Release Note

- Section ID: `REFERENCE-concept-glossary`
- Source File: `docs/reference/concept-glossary.md`

### v2026.07.08

- 변경 이유: `비교 리포트(comparison report)` 항목의 `등장 Section`이 Part 3 시작 페이지의 실제 재등장 위치를 아직 반영하지 못해, 대표 산출물 spine의 초반 호출 위치가 추적 목록에서 빠져 있었다.
- 본문 반영: `비교 리포트(comparison report)`의 `등장 Section`에 `P3-index`를 추가해 Part 3 오버뷰에서 이 표제어가 반복 구조의 일부로 다시 쓰이는 위치를 반영했다.

- 변경 이유: Part 3 핵심 표제어의 `등장 Section` 추적이 실제 본문 재등장 범위를 일부 놓치고 있어, 개념사전이 대표 위치와 재등장 위치를 추적하는 기준표 역할을 충분히 수행하지 못했다.
- 본문 반영: `검토 후보 큐(review queue)` 항목의 `등장 Section`에 `P3-4.2`를, `출력 구조(output structure)` 항목의 `등장 Section`에 `P3-4.3`을 추가해 Part 3 본문에서의 실제 구조적 재등장을 반영했다.

- 변경 이유: `기준선(baseline)` 항목이 Part 3 본문에서 쓰는 `상태 비교 기준` 뜻과 충돌한 채 `기준 모델` 문맥으로만 정의되어, 개념사전이 오히려 Part 3 독자를 잘못된 평가 문맥으로 보내고 있었다.
- 본문 반영: `기준선(baseline)`을 Part 3의 비교 기준 뜻으로 다시 정의하고 중심 Section을 `P3-7.3`으로 옮겼다. 함께 볼 개념과 등장 Section도 상태 비교 문맥에 맞게 조정했다. 동시에 Part 4의 평가 baseline은 `기준 모델(baseline model)` 표제어를 새로 추가해 `P4-8.2`를 중심 Section으로 분리했다.

- 변경 이유: `목표 라벨 후보(target candidate)` 항목의 `등장 Section`이 현재 Part 3 본문 재등장 범위를 아직 따라가지 못해, 판단 축 압축 이후 목표 후보 표를 다시 호출하는 구간이 누락되어 있었다.
- 본문 반영: `목표 라벨 후보(target candidate)`의 `등장 Section`에 `P3-8.5`를 추가해, 여러 비교 열을 검토 우선순위 후보 축으로 묶은 뒤 목표 후보 표 구분을 다시 호출하는 위치를 반영했다.

- 변경 이유: `검토 후보 큐(review queue)` 항목의 `등장 Section`이 현재 Part 3 본문 재등장 범위를 아직 따라가지 못해, 개념사전이 실제 반복 사용 위치를 충분히 추적하지 못하고 있었다.
- 본문 반영: `검토 후보 큐(review queue)`의 `등장 Section`에 `P3-9.1`, `P3-9.6`을 추가해, 문제 유형 구분과 라벨 의미 안정성 점검 구간에서의 실제 재등장을 반영했다.

- 변경 이유: `검토 후보 큐(review queue)`가 Part 3의 반복 핵심 산출물인데도 개념사전 표제어가 없어, 비교 리포트와 목표 라벨 후보 사이의 중심 산출물 spine을 사전에서 다시 찾기 어려웠다.
- 본문 반영: `검토 후보 큐(review queue)` 표제어를 새로 추가하고, 이를 사람이 무엇부터 먼저 볼지 우선순위를 붙여 정렬한 운영 선택용 산출물로 정의했다. 중심 Section은 `P3-9.3`으로 두고, `P3-8.1`부터 `P3-9.5`, `P3-summary`까지의 재등장 위치를 함께 연결했다.
- 변경 이유: Part 3 핵심 표제어의 `등장 Section`이 실제 재등장 범위를 충분히 따라가지 못해, 개념사전이 대표 설명 위치와 재등장 위치를 추적하는 기준표 역할을 약하게 만들고 있었다.
- 본문 반영: `데이터 모델링`, `출력 구조`, `중간 표현`, `목표 라벨 후보` 항목의 `등장 Section`을 실제 Part 3 본문 재등장 위치에 맞게 보강했다. `중심 Section`과 정의는 유지하고, `P3-index`, `P3-1.3`, `P3-3.1`, `P3-5.1`, `P3-6.1`, `P3-9.1`, `P3-summary` 같은 누락 위치를 추적 목록에 반영했다.
- 변경 이유: Part 3 핵심 표제어의 `등장 Section`이 실제 본문 재등장 범위를 충분히 따라가지 못해, 개념사전이 대표 설명 위치와 재등장 위치를 추적하는 기준표 역할을 약하게 만들고 있었다.
- 본문 반영: `기준선`, `비교 리포트`, `목표 라벨 후보`, `샘플`, `출력 구조`, `타깃`, `특징` 항목의 `등장 Section`을 실제 Part 3 재등장 위치에 맞게 보강했다. 정의와 `중심 Section`은 유지하고, 누락되었던 Chapter 3, 4, 7, 8, 9 구간의 반복 사용 위치를 추적 목록에 반영했다.
- 번역 동기화 메모: preserve the expanded Part 3 recurrence lists for baseline, comparison report, target candidate, sample, output structure, target, and feature without changing their glossary definitions or center sections. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

- 변경 이유: `목표 라벨 후보(target candidate)`가 Part 3 후반의 반복 핵심 개념인데도 개념사전 표제어가 없어, 대표 Section와 재등장 Section를 추적하는 규칙과 어긋났다.
- 본문 반영: `목표 라벨 후보(target candidate)` 표제어를 새로 추가하고, 이를 확정 정답 이전 단계의 결과 후보 열로 정의했다. 중심 Section은 `P3-9.3`으로 두고, `P3-9.4`, `P3-9.5`, `P3-9.6`, `P3-9.10`, `P3-summary` 등을 재등장 위치로 연결했다.
- 번역 동기화 메모: preserve the new glossary entry for target candidate with P3-9.3 as its anchor and keep its relation to output structure, targets, and later label stabilization explicit. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

- 변경 이유: `중간 표현(intermediate representation)`이 Part 3 Chapter 5, 6, 요약 페이지까지 반복 핵심 개념처럼 쓰이는데도 개념사전 표제어가 없어, 대표 Section와 재등장 Section를 추적하는 규칙과 어긋났다.
- 본문 반영: `중간 표현(intermediate representation)` 표제어를 새로 추가하고, 이를 원시 로그와 숫자 특징 사이의 사람 주도 입력 재표현으로 정의했다. 중심 Section은 `P3-6.2`로 두고, `P3-5.3`, `P3-6.3`, `P3-index`, `P3-summary`를 재등장 위치로 연결했다.
- 번역 동기화 메모: preserve the new glossary entry for intermediate representation with P3-6.2 as its anchor and keep its Part 3 recurrence across raw-sequence interpretation, feature design, and summary sections explicit. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

- 변경 이유: `출력 구조(output structure)`가 Part 3 전반에서 반복 핵심 개념처럼 쓰이는데도 개념사전 표제어가 없어, AGENTS와 개념사전 가이드의 후보 관리 원칙과 어긋났다.
- 본문 반영: `출력(output)` 아래에 `출력 구조(output structure)` 표제어를 새로 추가하고, 중심 Section을 `P3-2.2`로 두었다. 함께 볼 개념과 Part 3 재등장 위치도 같이 정리해 결과 형식 설계 축이 다시 추적되게 맞췄다.
- 번역 동기화 메모: preserve the new glossary entry for output structure with P3-2.2 as its anchor and keep its relation to comparison reports, targets, and sample-level design explicit. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

- 변경 이유: Part 3 개념사전 점검 과정에서 `데이터 모델링`, `비교 리포트`, `타깃`, `특징`, `특징 선택` 항목이 중복 `등장 Section`을 갖거나 이미 사라진 `P3-10.1` 같은 위치를 계속 가리키는 문제가 드러났다.
- 본문 반영: 중복 `등장 Section` 줄을 정리하고, 존재하지 않는 `P3-10.1` 참조를 현재 Part 3 본문에서 실제로 다시 중요한 위치로 교체했다. `특징 선택(feature selection)`에는 현재 Part 3의 재등장 위치인 `P3-6.1`도 추가했다.
- 번역 동기화 메모: preserve the cleaned Part 3 cross-references for data modeling, comparison report, target, feature, and feature selection, and remove stale references to the retired P3-10 structure. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`

- 변경 이유: Part 3 핵심 개념 도입 Section에 개념사전 직접 링크를 보강하려면, 표제어별 세부 앵커를 안정적으로 가리킬 수 있는 기준점이 필요했다.
- 본문 반영: `데이터 모델링`, `샘플`, `특징`, `기준선`, `비교 리포트`, `타깃` 표제어 앞에 명시적 앵커를 추가해 Part 3 본문이 개념사전 페이지 전체가 아니라 해당 표제어 위치로 바로 연결되게 정리했다.
- 번역 동기화 메모: preserve the explicit entry anchors for the Part 3 core glossary terms so later language editions can keep direct term-level links stable. / pending
- 번역 반영 상태: not-started
- 관련 자산: 없음.
- 원문 기준 버전: `v2026.07.08`
