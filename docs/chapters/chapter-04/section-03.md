# 4.3 특징(feature), 표현(representation), 파라미터(parameter)

이 절은 4.2에서 정한 입력과 데이터가 모델 내부에서 어떤 형태로 다뤄지는지 설명하기 위한 자리입니다.

## 작성 예정

- 특징(feature): 모델이 입력에서 사용하는 값
- 표현(representation): 입력을 계산하기 쉬운 형태로 바꾼 결과
- 파라미터(parameter): 학습 과정에서 조정되는 모델 내부 값

이 절은 별도 근거 검토 후 작성합니다.
