```mermaid
flowchart TD
    H[People define the input structure] --> M[The model receives the input]
    M --> Q{Does this model learn internal representations?}
    Q -->|Yes| R[Use learned representations]
    Q -->|No| F[Learn relationships using the supplied features]
```
