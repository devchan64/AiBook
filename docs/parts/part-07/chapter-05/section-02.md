# P7-5.2 검색 품질과 답변 검증

> Section ID: `P7-5.2`
> Version: `v2026.07.12`

P7-5.1에서는 RAG 프로젝트의 최소 흐름을 만들었습니다. 하지만 RAG는 `검색을 붙였다`고 끝나는 구조가 아닙니다.

실제 프로젝트에서는 곧바로 다음 문제가 나옵니다.

- 검색이 엉뚱한 문서를 가져오면?
- 검색 결과는 맞지만 답변이 과장되면?
- 질문이 문서 범위 밖이면?

그래서 이번 절은 `검색 품질`과 `답변 검증`을 따로 기록하는 법을 다룹니다.

이 절의 목적은 RAG가 실패했을 때, 검색이 문제인지 답변이 문제인지 구분해 프로젝트 문서에 남기는 데 있습니다.

## 이 절의 범위

- RAG 프로젝트에서 검색 품질은 무엇을 뜻하는가?
- 답변이 문서 근거를 벗어났는지 어떻게 점검할 수 있는가?
- 검색 실패와 답변 실패를 왜 따로 적어야 하는가?

이 절은 다음 내용은 깊게 다루지 않습니다.

- 검색 평가 지표(retrieval metric)의 엄밀한 수식 비교
- 자동 평가 프레임워크
- 재정렬기(reranker) 모델의 세부 구현

이 절에서는 검색 실패와 답변 실패를 문서에 남기는 최소 기준까지만 다룹니다. 더 넓은 평가 구조는 뒤의 P7-7.2 실패 기록과 개선 계획에서 다시 회수하고, 검색 결과를 숫자로 비교하는 평가 지표(retrieval metric)의 수식과 검색 후보 순서를 다시 정렬하는 재정렬기(reranker) 구현 세부는 현재 본편 범위 밖으로 둡니다.

RAG 검증은 이 절에서 검색 실패와 답변 실패를 나눠 읽는 습관으로 구체화됩니다. 이 절은 그 두 실패를 따로 기록하는 기준을 고정합니다.

Part 7에서 `근거(evidence)`, `검토(review)`, `평가(evaluation)`의 역할이 다시 겹쳐 보이면 이 절과 [개념사전](../../../reference/concept-glossary.md)으로 돌아와 구분을 다시 잡는 편이 안전합니다.

## 이 절의 목표

- 검색 실패와 답변 실패를 구분해 기록할 수 있습니다.
- 근거 없는 답변을 `문서 범위 밖`이라는 형태로 안전하게 처리하는 기준을 세울 수 있습니다.
- RAG 프로젝트 평가표에 최소 무엇이 들어가야 하는지 정리할 수 있습니다.

## 왜 두 실패를 구분해야 하나

RAG 프로젝트에서는 최소 두 종류의 실패가 있습니다.

| 실패 유형 | 설명 |
| --- | --- |
| 검색 실패 | 관련 문서가 상위 후보에 들어오지 못한다 |
| 답변 실패 | 문서는 찾았지만 답변이 근거를 벗어나거나 과장한다 |

이 둘을 섞으면 원인 분석이 어려워집니다.

예를 들어:

- 검색이 틀렸다면 문서를 어떻게 나눴는지, 질문 표현을 어떻게 다시 썼는지, 임베딩(embedding)과 검색 색인(index)을 어떻게 잡았는지 봐야 합니다.
- 답변이 과장됐다면 프롬프트(prompt), 답변 형식, 출처 규칙, 응답 중단 규칙을 봐야 합니다.

검색 실패와 답변 실패를 가를 때 바로 필요한 판단 기준을 표로 고정하면 다음과 같습니다.

| 질문 | 짧은 답 |
| --- | --- |
| 실패를 왜 둘로 나누는가? | 원인과 수정 지점이 다르기 때문 |
| 검색 실패는 무엇을 뜻하는가? | 관련 문서를 못 찾은 상태 |
| 답변 실패는 무엇을 뜻하는가? | 문서는 있었지만 답이 근거를 벗어난 상태 |

여기에 한 가지를 더 붙이면 RAG 프로젝트의 평가 메모가 훨씬 단단해집니다. `최고 점수` 하나만 적는 데서 끝내지 말고, `현재 검색 기준선`, `질문별 다시 볼 대상`, `문서 범위 밖으로 멈춘 이유`를 같이 남기는 것입니다. 그래야 다음 반복에서 검색을 바꿔야 하는지, 답변 규칙을 바꿔야 하는지 더 빨리 구분할 수 있습니다.

| 평가 메모에 같이 남길 것 | 왜 필요한가 |
| --- | --- |
| 현재 검색 기준선 | 다음 실험에서 검색 규칙이 무엇과 달라졌는지 비교하기 위해서입니다. |
| 다시 볼 질문 목록 | 어떤 질문을 먼저 다시 점검할지 고정하기 위해서입니다. |
| 멈춘 이유 | 근거 부족인지, 검색 실패인지, 답변 과장 위험인지 분리하기 위해서입니다. |
| 다음 수정 질문 | 문서 분할, 질의 재작성, 프롬프트 중 어디를 먼저 바꿀지 정하기 위해서입니다. |

## 작은 검증 규칙 만들기

입문 프로젝트에서는 거대한 평가 시스템 대신 다음처럼 단순한 규칙부터 둘 수 있습니다.

1. 검색된 문서가 질문과 직접 관련 있는가?
2. 답변 문장이 검색된 문장에 실제로 포함된 정보만 말하는가?
3. 문서에 없는 내용이면 `문서에서 확인되지 않는다`고 답하는가?

이 세 줄은 작은 RAG 프로젝트의 `최소 검증 규칙`으로 그대로 문서에 들어갈 수 있습니다.

이 세 규칙만 있어도 프로젝트 문서의 품질이 크게 올라갑니다.

## 예제: 문서 범위 밖 질문

이번 절에서는 일부러 문서 범위 밖 질문을 넣어 보겠습니다.

- 문서 집합에는 `MCP가 무엇인가`에 대한 정보가 없습니다.
- 그런데 사용자가 `MCP는 왜 필요한가?`라고 물어봅니다.

이때 좋은 RAG 프로젝트는 억지로 아는 척하는 답을 만들지 않고, `현재 검색된 문서 기준으로는 답할 근거가 부족하다`고 기록해야 합니다.

## Python 예제

이번 예제의 목적은 검색 점수와 답변 가능 여부를 함께 출력하는 것입니다. 이번에는 질문별로 검색 후보, 답변 상태, 수정 필요 여부가 남는 평가 기록을 만들겠습니다.

이 기록이 필요한 이유는 `답이 틀렸다`에서 멈추지 않고, 그 실패가 `문서를 못 찾은 것인지`, `근거가 부족해 멈춘 것인지`, `답변 단계에서 과장한 것인지`를 다음 수정 때 다시 분리해 볼 수 있게 하기 위해서입니다.

- 문제 상황: 질문마다 검색 실패와 답변 실패를 구분해 기록한다.
- 입력: 문서 조각 3개, 질문 2개
- 기대 출력: 질문별 최고 점수, 답변 상태, 수정 필요 여부, 후보 문서 목록
- 확인할 개념:
  - 검색 점수와 답변 상태를 함께 봐야 실패 위치를 구분할 수 있다
  - 근거가 없으면 멈추는 답변도 평가 결과에 포함된다
  - 질문별 후보 문서를 남겨야 다음 수정 지점을 다시 찾기 쉽다

```python
documents = {
    "문서-1": "RAG는 외부 문서를 검색해 모델 입력에 넣는 구조다.",
    "문서-2": "임베딩은 텍스트를 벡터로 바꾸어 유사도 비교를 가능하게 한다.",
    "문서-3": "프롬프트만으로는 최신 문서 근거를 보장할 수 없다.",
}

questions = [
    "RAG가 왜 필요한가?",
    "MCP는 왜 필요한가?",
]

def 토큰_정리(token):
    token = token.replace("?", "").replace(".", "")
    for keyword in ["RAG", "MCP"]:
        if token.startswith(keyword):
            return keyword
    return token

def 토큰화(text):
    return {토큰_정리(token) for token in text.split()}

평가_기록 = []
for question in questions:
    질문_토큰 = 토큰화(question)
    검색_후보 = []
    for 문서_식별자, text in documents.items():
        overlap = len(질문_토큰 & 토큰화(text))
        검색_후보.append({
            "문서 식별자": 문서_식별자,
            "근거 점수": overlap,
            "문장": text,
        })

    검색_후보.sort(key=lambda x: x["근거 점수"], reverse=True)
    최고_후보 = 검색_후보[0]

    if 최고_후보["근거 점수"] == 0:
        answer = "현재 검색된 문서에서는 이 질문에 답할 근거를 찾지 못했다."
        answer_label = "근거 부족"
    else:
        answer = (
            f"{최고_후보['문장']} 이 문장 범위 안에서만 답하면, "
            "외부 근거를 붙이기 위해 검색이 필요하다고 설명할 수 있다."
        )
        answer_label = "근거 기반 답변"

    평가_기록.append({
        "질문": question,
        "최고 근거 문서": 최고_후보["문서 식별자"],
        "근거 점수": 최고_후보["근거 점수"],
        "답변 상태": answer_label,
        "답변": answer,
        "다시 보기 필요": "예" if 최고_후보["근거 점수"] == 0 else "아니오",
        "검색 후보": 검색_후보,
    })

검토_요약 = {
    "질문 수": len(평가_기록),
    "근거 기반 답변 수": sum(
        row["답변 상태"] == "근거 기반 답변" for row in 평가_기록
    ),
    "근거 부족 답변 수": sum(
        row["답변 상태"] == "근거 부족"
        for row in 평가_기록
    ),
    "다시 볼 질문": [
        row["질문"] for row in 평가_기록 if row["다시 보기 필요"] == "예"
    ],
}

print("요약 기록 =", 검토_요약)
print("질문별 기록 =")
for row in 평가_기록:
    print(row)
```

실행 결과 예시는 다음과 같습니다.

```text
요약 기록 = {'질문 수': 2, '근거 기반 답변 수': 1, '근거 부족 답변 수': 1, '다시 볼 질문': ['MCP는 왜 필요한가?']}
질문별 기록 =
{'질문': 'RAG가 왜 필요한가?', '최고 근거 문서': '문서-1', '근거 점수': 1, '답변 상태': '근거 기반 답변', '답변': 'RAG는 외부 문서를 검색해 모델 입력에 넣는 구조다. 이 문장 범위 안에서만 답하면, 외부 근거를 붙이기 위해 검색이 필요하다고 설명할 수 있다.', '다시 보기 필요': '아니오', '검색 후보': [{'문서 식별자': '문서-1', '근거 점수': 1, '문장': 'RAG는 외부 문서를 검색해 모델 입력에 넣는 구조다.'}, {'문서 식별자': '문서-2', '근거 점수': 0, '문장': '임베딩은 텍스트를 벡터로 바꾸어 유사도 비교를 가능하게 한다.'}, {'문서 식별자': '문서-3', '근거 점수': 0, '문장': '프롬프트만으로는 최신 문서 근거를 보장할 수 없다.'}]}
{'질문': 'MCP는 왜 필요한가?', '최고 근거 문서': '문서-1', '근거 점수': 0, '답변 상태': '근거 부족', '답변': '현재 검색된 문서에서는 이 질문에 답할 근거를 찾지 못했다.', '다시 보기 필요': '예', '검색 후보': [{'문서 식별자': '문서-1', '근거 점수': 0, '문장': 'RAG는 외부 문서를 검색해 모델 입력에 넣는 구조다.'}, {'문서 식별자': '문서-2', '근거 점수': 0, '문장': '임베딩은 텍스트를 벡터로 바꾸어 유사도 비교를 가능하게 한다.'}, {'문서 식별자': '문서-3', '근거 점수': 0, '문장': '프롬프트만으로는 최신 문서 근거를 보장할 수 없다.'}]}
```

## 결과를 어떻게 읽는가

두 번째 질문이 이 절의 핵심입니다.

- 평가 기록에서 두 번째 질문은 최고 점수가 0이고, 답변 상태는 `근거 부족`, 다시 보기 필요 여부는 `예`로 남습니다.
- 즉, 현재 문서 집합과 현재 검색 규칙 기준으로는 답 근거가 없습니다.
- 이때도 억지로 설명을 만들면 그것은 RAG 품질 문제가 아니라 `검증 규칙 부재` 문제입니다.

여기서도 해석을 너무 빨리 닫으면 안 됩니다. `최고 점수 = 0`은 이번 작은 예제에서 `현재 문서 집합`, `현재 토큰 겹침 검색`, `현재 질문 표현` 기준으로 근거를 찾지 못했다는 신호이며, 다음 반복에서는 문서 추가, 질의 재작성, 검색 규칙 변경을 각각 분리해 다시 확인할 수 있습니다.

따라서 좋은 RAG 프로젝트는 `답을 잘하는 것`뿐 아니라 `근거가 없을 때 멈출 수 있는 것`도 포함해야 합니다.

이 기록은 RAG 프로젝트의 기준점(baseline)을 더 넓게 읽게도 해 줍니다. 어떤 경우 기준점은 단순 키워드 겹침 검색일 수 있고, 어떤 경우에는 `현재 문서 집합 + 현재 검색 규칙 + 현재 답변 중단 규칙` 전체가 비교 기준선이 됩니다. 다음 반복에서 검색기를 바꾸거나 검색 후보 순서를 다시 정렬하는 재정렬기(reranker)를 붙일 때는 바로 이 기준선과 무엇이 달라졌는지 비교해야 개선을 말할 수 있습니다.

이 결과는 다음 세 줄로 요약할 수 있습니다.

- 최고 점수가 0이면 현재 기준선에서는 근거를 찾지 못했다는 뜻이다
- 근거가 없을 때 멈추는 것도 품질이다
- `근거 기반 답변`과 `근거 부족` 상태를 질문별 기록으로 남겨야 다음 수정이 쉬워진다
- 따라서 검색 품질과 답변 품질을 따로 적어야 한다

## 프로젝트 평가표 예시

작은 RAG 프로젝트라면 최소한 다음 표를 남길 수 있습니다.

| 질문 | 최고 근거 문서 | 근거 점수 | 답변 상태 |
| --- | --- | ---: | --- |
| RAG가 왜 필요한가? | 문서-1 | 1 | 근거 기반 답변 |
| MCP는 왜 필요한가? | 문서-1 | 0 | 근거 부족 |

이 표가 있으면 검색 실패와 답변 실패를 나중에 따로 다시 볼 수 있습니다.

이 표는 Part 7의 RAG 프로젝트에서 평가 기록의 최소 구조로 읽을 수 있습니다.

이 평가표를 `비교 가능한 기록 묶음`으로 다시 적으면 다음과 같습니다.

| 기록 묶음 | 지금 남기는 이유 |
| --- | --- |
| 검색 기준선 | 다음 검색 실험과 비교하기 위해 |
| 질문별 최고 후보와 답변 상태 | 실패 위치를 질문 단위로 다시 읽기 위해 |
| 수정 필요 질문 목록 | 다음 반복에서 먼저 수정할 대상을 고정하기 위해 |
| 회고 문장 | 검색 문제와 답변 문제를 한 문단으로 구분해 남기기 위해 |

## 평가 기록의 최소 구조

평가 기록은 아래와 같은 최소 구조만 있어도 기능합니다.

```text
### 요약 기록
- 질문 수:
- 근거 기반 답변 수:
- 근거 부족 답변 수:
- 다시 볼 질문:

### 질문별 기록
| 질문 | 최고 근거 문서 | 근거 점수 | 답변 상태 | 다시 보기 필요 | 메모 |
| --- | --- | ---: | --- | --- | --- |
|  |  |  | 근거 기반 답변 / 근거 부족 | 예 / 아니오 |  |
```

이 템플릿에서 중요한 점은 화려한 평가 지표를 많이 넣는 일이 아니라, 질문별로 `검색 결과`, `답변 상태`, `수정 필요 여부`가 한 줄에 같이 남는가입니다.

이번 절의 예시를 같은 구조로 정리하면 다음과 같습니다.

```text
### 요약 기록
- 질문 수: 2
- 근거 기반 답변 수: 1
- 근거 부족 답변 수: 1
- 다시 볼 질문:
  - MCP는 왜 필요한가?

### 질문별 기록
| 질문 | 최고 근거 문서 | 근거 점수 | 답변 상태 | 다시 보기 필요 | 메모 |
| --- | --- | ---: | --- | --- | --- |
| RAG가 왜 필요한가? | 문서-1 | 1 | 근거 기반 답변 | 아니오 | 답변 가능 |
| MCP는 왜 필요한가? | 문서-1 | 0 | 근거 부족 | 예 | 문서 범위 밖 질문 |
```

## 나쁜 평가 기록과 좋은 평가 기록

RAG 평가 기록도 자주 너무 짧거나, 반대로 근거 없이 `잘 됐다`는 감상으로 끝납니다. 아래처럼 대비해 보면 기준이 더 분명해집니다.

| 구분 | 예시 |
| --- | --- |
| 나쁜 기록 | `검색은 됐고 답도 대체로 괜찮았다. 이상한 질문은 나중에 보자.` |
| 좋은 기록 | `질문 2개 중 1개는 근거 기반 답변이었고, 'MCP는 왜 필요한가?'는 근거 점수 0으로 근거 부족 상태였다. 현재 문서 집합과 현재 검색 규칙 기준으로 근거를 찾지 못했으므로 답변을 확장하지 않고 다시 볼 질문에 남긴다.` |

나쁜 기록은 분위기만 남고, 어떤 질문이 막혔는지와 다음 수정 지점이 보이지 않습니다. 좋은 기록은 `질문별 상태`, `근거 부족 여부`, `지금 멈춘 이유`, `다음 수정 대상`이 함께 남습니다.

## 체크리스트

다음처럼 RAG 답변은 나오지만 무엇이 실패했는지 위치가 흐릴 때 이 절의 관점을 먼저 떠올리는 편이 좋습니다.

- 가장 관련 높은 문서는 뽑혔지만 그 문서가 실제로 질문에 충분한 근거인지 구분하지 않은 경우
- 근거 부족 질문에도 답을 계속 확장해 쓰고 있어 멈춤 규칙이 없는 경우
- 검색기, 프롬프트, 문서 집합 중 어디를 먼저 바꿔야 할지 기록이 분리되지 않은 경우

이때는 새 검색 기법을 붙이기 전에 `질문별 상태`, `근거 부족 여부`, `멈춘 이유`, `다음 수정 대상`을 먼저 남겨야 합니다.

- RAG 프로젝트는 검색 실패와 답변 실패를 구분해야 한다는 점을 설명할 수 있는가?
- 근거가 없을 때 멈추는 것도 품질이라는 점을 말할 수 있는가?
- 평가표에는 질문, 검색 결과, 점수, 답변 상태가 함께 있어야 한다는 점을 설명할 수 있는가?
- 검증 규칙이 없으면 RAG도 쉽게 환각 구조가 된다는 점을 말할 수 있는가?
- 검색 실패와 답변 실패를 구분해 적을 수 있는가?
- 근거가 없는 질문에 대해 안전한 답변 규칙을 세웠는가?
- 평가표에 질문/검색결과/답변상태를 남겼는가?
- 환각을 줄이는 규칙을 한 문장으로 설명할 수 있는가?

## 출처와 참고 자료

- OpenAI, `Retrieval`, OpenAI API Docs, 확인 날짜: 2026-06-29. [https://developers.openai.com/api/docs/guides/retrieval](https://developers.openai.com/api/docs/guides/retrieval){: target="_blank" rel="noopener noreferrer" }

이 절의 문서 조각과 질문 예시는 프로젝트 실습을 위해 만든 자체 예시입니다.
