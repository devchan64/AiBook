# Part 1 마무리. AI 개론과 지형도 정리

Part 1은 AI를 다시 공부하기 전에 전체 지형을 잡기 위한 구간이었습니다. 세부 알고리즘을 구현하거나 수식을 깊게 증명하기보다, 이후 학습에서 반복해서 만나게 될 용어와 관점을 먼저 정리했습니다.

이 Part에서 가장 중요한 목표는 AI를 하나의 유행어나 하나의 제품 이름으로 보지 않는 것입니다. AI는 규칙 기반 접근, 탐색, 휴리스틱, 확률적 판단, 데이터 기반 학습, 딥러닝, 생성형 AI, LLM, 에이전트, 서비스 운영, 사회적 책임이 겹쳐 있는 넓은 분야입니다.

## 이 파트의 목적

Part 1의 목적은 기술을 깊게 끝내는 것이 아니라, 이후 Part의 학습이 어디로 이어지는지 잃지 않게 만드는 공통 지도를 세우는 데 있었습니다.

## 이 파트의 목표

이 Part를 마친 뒤에는 AI 관련 용어와 서비스 경험을 같은 층위로 섞지 않고, 큰 지도 안에서 다시 배치해 읽을 수 있어야 합니다.

## 이 파트에서 다룬 핵심 흐름

Part 1의 흐름은 다음처럼 정리할 수 있습니다.

> AI라는 말의 범위
> -> 규칙 기반 접근과 기호 기반 AI
> -> 탐색, 지식 표현, 확률 추론
> -> 데이터에서 패턴을 배우는 머신러닝
> -> 표현을 학습하는 딥러닝
> -> 생성형 AI와 LLM
> -> 프롬프트, 임베딩, RAG, 에이전트
> -> 서비스 아키텍처와 운영 제약
> -> 윤리, 저작권, 보안, 실무 적용, 전망 검토

이 흐름은 “옛 기술이 사라지고 새 기술이 완전히 대체했다”는 뜻이 아닙니다. 시대에 따라 중심이 되는 접근이 달라졌고, 어떤 개념은 이름과 위치를 바꾸어 지금도 계속 사용됩니다.

예를 들어 탐색(search)과 휴리스틱(heuristic)은 초기 AI의 문제 해결 방식에서 중요했지만, 현대 AI에서도 후보를 줄이고 계산 비용을 낮추는 사고방식으로 다시 나타납니다. 확률(probability)과 불확실성(uncertainty)은 예측, 분류, 생성 결과를 이해할 때 계속 등장합니다.

이때 Part 1 안에서 먼저 닫아 둔 중요한 경계도 있습니다. 불확실성(uncertainty), 확률(probability), 확률적 과정(stochastic)은 같은 말이 아니며, 휴리스틱(heuristic)은 확률 모델(probabilistic model)과 다른 역할을 합니다. 이 구분을 먼저 잡아 두어야 뒤 Part에서 분류 점수, calibration, 생성 설정, 프롬프트 휴리스틱을 같은 층위로 섞지 않게 됩니다. calibration, confidence, uncertainty estimation의 세부 차이는 여기서 길게 확장하지 않고, P3-6.4 보충학습의 첫 해설과 P3-15.3의 서비스 판단 연결로 넘기는 현재 구조를 유지합니다.

생성형 AI와 LLM의 관계도 같은 방식으로 읽어야 합니다. 생성형 AI(generative AI)는 텍스트, 이미지, 오디오, 코드처럼 새 콘텐츠를 만들어 내는 더 넓은 범주이고, LLM(large language model)은 그 안에서 언어 모델링과 Transformer 계열을 중심으로 발전한 대표 모델 흐름입니다. 이 경계는 Part 5에서 `토큰 -> Transformer -> GPT` 본류와 `LLM 발전사`, `BERT 계열 비교` 배경 축으로 다시 확인합니다.

마찬가지로 9장에서도 직접 계보와 주변 근거를 나눴습니다. CNN, GPU, YOLO, WaveNet은 딥러닝 확산의 주변 근거로 남기고, LLM의 직접 계보는 언어 모델링, Seq2Seq, Attention, Transformer로 둡니다. 이 경계 덕분에 Part 4의 CNN과 Transformer, Part 5의 GPT와 BERT 설명도 서로 충돌하지 않고 이어집니다.

추천, 순위화, 제어, 검색 서비스, 자율주행 같은 예시도 같은 원칙으로 읽습니다. 현재 Part 1에서는 이 사례들을 `문제 유형을 구분하게 해 주는 작은 장면`으로만 두고, 각 도메인의 기술사나 구현 세부로 확장하지 않습니다.

기호 기반 AI와 규칙 기반 접근의 대표 이름도 마찬가지입니다. Logic Theorist, GPS, MYCIN, DENDRAL은 `초기 흐름을 붙잡게 하는 대표 사례`로만 두고, 현대 규칙 기반 시스템도 권한, 정책, 절차 같은 일반 구조 예시 수준으로만 남겨 Part 1이 사례 카탈로그처럼 읽히지 않게 유지합니다.

## 반드시 기억할 개념

Part 1에서 가장 오래 가져가야 할 개념은 구분입니다.

| 구분 | 기억할 관점 |
| --- | --- |
| AI와 머신러닝 | AI는 더 넓은 분야이고, 머신러닝은 데이터에서 패턴을 배우는 접근입니다. |
| 머신러닝과 딥러닝 | 딥러닝은 신경망을 이용해 표현을 학습하는 머신러닝의 중요한 흐름입니다. |
| 딥러닝과 LLM | LLM은 딥러닝의 여러 발전 중 언어 모델링과 Transformer 계열에서 강하게 성장한 흐름입니다. |
| 학습과 모델 실행 | 학습(learning)은 모델 내부 값을 바꾸는 과정이고, 모델 실행(inference)은 학습된 모델을 사용하는 과정입니다. |
| `추론` 관련 표현 | inference는 모델 실행, reasoning은 사고 과정, prediction은 모델 출력, generation은 생성 결과로 먼저 나눠 읽습니다. |
| 프롬프트와 모델 | 프롬프트(prompt)는 모델의 동작을 지정하는 입력이지, 모델 내부 지식이나 동작을 완전히 통제하는 장치가 아닙니다. |
| RAG와 에이전트 | RAG는 외부 정보를 검색해 입력에 보태는 구조이고, 에이전트(agent)는 목표를 여러 작업 흐름으로 이어 가는 구조입니다. |
| 모델과 서비스 | 실제 AI 서비스는 모델 하나가 아니라 앱, 데이터, 도구, 권한, 로그, 평가, 운영 제약이 함께 작동하는 구조입니다. |

이 구분이 잡히면 이후 Part에서 수학, 코드, 모델, 데이터, 서비스 구조를 배울 때 현재 개념이 전체 지도에서 어디에 놓이는지 확인할 수 있습니다.

## 오해하기 쉬운 지점

Part 1에서 특히 조심해야 할 오해는 다음입니다.

- AI를 LLM과 같은 말로 보지 않습니다.
- 생성형 AI를 모든 AI의 최종 형태처럼 설명하지 않습니다.
- 규칙 기반 접근을 낡아서 사라진 방식으로만 보지 않습니다.
- 휴리스틱을 확률 모델과 같은 것으로 섞지 않습니다.
- 라벨(label)을 정답이라는 말로만 이해하지 않습니다.
- 임베딩(embedding)을 사람이 직접 읽을 수 있는 의미 정의로 오해하지 않습니다.
- 프롬프트(prompt)를 모델을 완전히 제어하는 명령문으로 오해하지 않습니다.
- 에이전트(agent)를 무제한 자율 실행 시스템으로 단정하지 않습니다.
- AI 전망을 개인적 추측이나 AI가 만든 문장만으로 작성하지 않습니다.

이 책에서는 이런 오해를 줄이기 위해 한국어 표현과 영어 원어를 함께 확인합니다. 특히 `추론`, `모델`, `파라미터`, `생성`, `층위`처럼 문맥에 따라 의미가 달라지는 말은 계속 구분해야 합니다.

그중 `추론`은 가장 자주 다시 확인해야 하는 말입니다. Part 1에서는 inference를 `모델 실행`, reasoning을 `논리적 추론`, prediction을 `모델 출력`, generation을 `생성`으로 우선 나눠 둡니다. Part 5로 가면 next-token prediction, reasoning처럼 보이는 응답 생성, 평가 문맥의 `모델 결과`를 읽을 때 이 구분이 다시 필요합니다.

## 이 파트에서 설명하는 범위와 설명하지 않을 범위

Part 1은 AI 전체 지형과 공통 용어를 설명하는 데 집중했습니다. 따라서 개별 알고리즘의 세부 수식, 딥러닝 내부 계산, LLM 서비스 구현 세부는 여기서 끝내지 않고 뒤 Part로 넘깁니다.

## 앞선 Part와의 연결

Part 1은 이 책의 첫 본문 파트이므로, 앞선 기술 Part를 잇기보다 이후 학습 전체의 출발 기준을 세우는 역할을 했습니다.

## 이후 Part로의 연결

Part 1에서 만든 지도는 Part 2의 기초 복구, Part 3의 머신러닝 공통 구조, Part 4의 딥러닝, Part 5의 LLM과 생성형 AI, Part 6의 프로젝트까지 계속 재사용됩니다.

이 연결을 더 짧게 기억하면 다음과 같습니다.

| Part 1에서 잡은 질문 | 뒤에서 다시 읽는 Part |
| --- | --- |
| 모델, 데이터, 파라미터, 학습, inference는 어떻게 계산 언어로 옮겨지는가? | Part 2 |
| 라벨, 구조, 보상의 차이는 실제 머신러닝 문제에서 어떻게 드러나는가? | Part 3 |
| 표현 학습과 신경망 구조 차이는 왜 CNN, RNN, Transformer로 갈라지는가? | Part 4 |
| 토큰, GPT, RAG, agent는 왜 하나의 서비스 구조로 이어지는가? | Part 5 |
| 평가, 기록, 회고는 실제 프로젝트 문서에서 어떻게 남겨야 하는가? | Part 6 |

이 표를 붙잡고 읽으면 Part 1에서 모든 세부를 다 이해하지 못해도 괜찮다는 점이 더 분명해집니다. 이 Part의 책임은 본문 설명을 끝내는 일이 아니라, 뒤 Part에서 무엇을 다시 읽어야 하는지 길을 잃지 않게 만드는 일입니다.

## 이 파트에서 끝내지 않는 질문

Part 1은 다음 질문을 의도적으로 열어 둡니다.

- 수학과 코드로 이 개념들을 어떻게 다시 확인할 것인가?
- 머신러닝과 딥러닝의 공통 계산 구조는 무엇인가?
- LLM과 생성형 AI는 서비스 구조 안에서 어떻게 작동하는가?

이 질문들은 이후 Part에서 실제 설명과 실습으로 회수합니다.

## 다음 파트로 넘어가기 전에 확인할 질문

Part 2로 넘어가기 전에 다음 질문에 답할 수 있으면 충분합니다.

- AI, 머신러닝(machine learning), 딥러닝(deep learning), 생성형 AI(generative AI), LLM(large language model)의 관계를 큰 그림으로 설명할 수 있는가?
- 규칙 기반 접근(rule-based approach)과 학습 기반 접근(learning-based approach)의 차이를 말할 수 있는가?
- 데이터(data), 특징(feature), 표현(representation), 모델(model), 파라미터(parameter), 학습(learning), 모델 실행(inference)의 관계를 대략 설명할 수 있는가?
- 불확실성(uncertainty), 확률(probability), 확률적 과정(stochastic)이 같은 말이 아님을 설명할 수 있는가?
- 프롬프트(prompt), 임베딩(embedding), 벡터 검색(vector search), RAG, 에이전트(agent)가 서로 다른 역할을 가진다는 점을 설명할 수 있는가?
- AI 서비스가 모델 하나만으로 만들어지지 않는다는 점을 설명할 수 있는가?
- 개인적 작업 가설과 외부 근거가 있는 사실 주장을 구분할 수 있는가?

이 질문에 모두 완벽하게 답할 필요는 없습니다. Part 1의 목적은 정답을 암기하는 것이 아니라, 이후 학습에서 다시 돌아올 지도를 만드는 것입니다.

## 다음 파트와 연결되는 관점

Part 2에서는 기초 복구로 넘어갑니다. 여기서 말하는 기초는 수학을 깊게 증명하는 능력이 아니라, AI 문서와 코드를 읽기 위한 최소 언어입니다.

Part 1에서 다룬 모델, 데이터, 표현, 파라미터, 학습, 모델 실행은 Part 2에서 수식과 코드로 다시 나타납니다.

> 모델이 입력을 받는다.
> 입력은 숫자, 벡터, 행렬로 표현된다.
> 학습은 값을 조정한다.
> 조정은 손실과 최적화의 언어로 설명된다.
> 확률과 통계는 불확실한 데이터를 다루는 언어가 된다.
> 코드는 이 계산을 재현하는 도구가 된다.

따라서 Part 2는 AI를 수학으로 어렵게 만들기 위한 구간이 아닙니다. Part 1에서 만든 지도를 실제 계산 언어로 조금씩 확인하기 위한 구간입니다.

## Part 1을 마치며

Part 1을 마쳤다면 AI를 하나의 단어가 아니라 여러 층위(level)의 개념으로 볼 준비가 된 것입니다.

이제 중요한 태도는 두 가지입니다.

첫째, 개인적 직관을 버리지 않습니다. 오래전에 배운 기억, 실무에서 느낀 의문, 최근 도구를 쓰며 생긴 감각은 학습의 좋은 출발점입니다.

둘째, 그 직관을 사실처럼 단정하지 않습니다. 표준 용어, 외부 근거, 실제 코드, 작은 실습을 통해 검토하면서 이해를 조정합니다.

Part 2부터는 이 태도를 유지한 채 수학, 코드, 데이터 계산을 다시 회복합니다.

## 출처와 참고 자료

이 문서는 Part 1 전체 내용을 요약한 자체 정리입니다. 외부 자료를 직접 인용하지 않았습니다.
