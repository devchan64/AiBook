## 输出层(output layer)

- 含义: 输出层是神经网络的最后一层，负责产生模型的最终预测。根据任务不同，它可以输出连续值、类别分数，或像概率一样的值。
- 为什么重要: 隐藏层主要用来形成内部表征，而输出层直接决定最后的数字应该怎样解释。因此，输出层激活必须和任务类型、目标格式、损失函数一起阅读。
- 相关概念: `输出(output)`, `激活函数(activation function)`, `损失函数(loss function)`, `softmax`
- 核心 Section: `P5-3.6`
