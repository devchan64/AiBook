<a id="key"></a>

## 键

- 含义：键是在字典等映射结构中查找值所用的名称或标识符。列表索引指向位置，而键更像带有意义的名称标签。
- 为什么重要：配置、JSON 和元数据通常必须把键和值一起读才有意义。同一个值连接到不同键时，含义可能完全不同。
- 相关概念：`字典`，`映射`，`值`
- 中心 Section：`P2-8.3`
- 出现 Section：`P2-8.4`, `P2-9.4`
