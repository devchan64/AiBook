<a id="dbscan"></a>

## DBSCAN

- 含义: DBSCAN 是一种基于密度的聚类算法。它从稠密邻域形成聚类，并可以把低密度点留作噪声。
- 为什么重要: 和把每个点都放进固定数量聚类的方法不同，DBSCAN 可以把类似异常值的点留在分组之外。这个概念帮助读者区分中心型分组和密度型结构发现。
- 相关概念: `聚类(clustering)`, `密度(density)`, `噪声(noise)`, `异常值(outlier)`
- 中心 Section: `P4-17.1`
- 出现 Section:
