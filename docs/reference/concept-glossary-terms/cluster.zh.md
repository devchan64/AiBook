<a id="cluster"></a>

## 聚类簇

- 含义: 聚类簇是在当前距离、相似度或密度规则下，被看作彼此相似的一组数据点。它是算法发现的分组，不是人预先定义的正确类别。
- 为什么重要: 聚类输出很容易被误读成现实里的固定类别。理解这个概念，才能在解释 `cluster 0`、`cluster 1` 之前，先检查这些分组到底有什么共同特征。
- 相关概念: `聚类(clustering)`, `聚类标签(cluster label)`, `相似度(similarity)`
- 中心 Section: `P4-17.1`
- 出现 Section:
