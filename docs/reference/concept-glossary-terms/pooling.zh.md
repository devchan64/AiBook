<a id="pooling"></a>

## 池化

- 含义: 池化是在 feature map 中把局部区域的反应压缩成更小摘要的运算。常见方式包括只保留局部最大值，或取局部平均值。
- 为什么重要: 池化能在缩小空间尺寸的同时保留重要局部反应，让 CNN 逐步读取更大的视觉线索。它也提醒读者：有些场景更关心“某个特征是否出现”，而不是它出现的精确位置。
- 相关概念: `卷积`, `feature map`, `CNN`
- 核心 Section: `P5-11.2`

