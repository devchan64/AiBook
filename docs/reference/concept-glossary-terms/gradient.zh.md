<a id="gradient"></a>

## 梯度

- 含义：梯度是由多个偏导数组成的向量，表示某个值通常是损失相对于多个参数如何变化。它像一个方向信号，提示参数变化会怎样影响损失。
- 为什么重要：梯度把损失和学习连接起来。它说明参数不是随机改变的，而是沿着预计能降低损失的方向更新。没有梯度，就很难把梯度下降和反向传播理解为具体计算过程。
- 相关概念：`偏导数`，`向量`，`模型训练`
- 中心 Section：`P2-4.3`
- 出现 Section：`P2-4.4`，`P2-6.3`，`P5-5.1`，`P5-5.2`，`P5-6.1`，`P5-7.1`
