<a id="kv-cache"></a>

## KV 缓存

- 含义：KV 缓存会保存先前 token 已经计算出的 key/value 表示，使后续生成 token 时可以复用，而不必从头重新计算。
- 为什么重要：长上下文生成如果每一步都重新计算全部过去上下文，速度会明显变慢。KV 缓存把生成速度和成本连接到系统如何复用先前计算。
- 相关概念：`查询-键-值`，`上下文窗口`，`长上下文`
- 中心 Section：`P6-4.3`
- 出现 Section：`P5-14.2`
