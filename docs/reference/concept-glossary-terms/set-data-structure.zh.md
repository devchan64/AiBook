<a id="set-data-structure"></a>

## 集合(set)

- 含义: 集合是一种以成员是否包含为中心的数据结构，而不是以顺序或重复次数为中心。它适合回答某个值是否存在、两个组共享哪些项、去重后还剩什么。
- 为什么重要: 集合首先问的是“是否包含”，而不是“在第几个位置”。理解集合可以把成员检查、去重、类别比较同顺序列表处理区分开。
- 相关概念: `数据结构(data structure)`, `操作(operation)`, `字典(dictionary)`
- 核心 Section: `P2-9.1`
- 出现 Section:
