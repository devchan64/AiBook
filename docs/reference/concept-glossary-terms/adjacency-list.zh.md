<a id="adjacency-list"></a>

## 邻接列表(adjacency list)

- 含义: 邻接列表用每个节点直接连接的邻居列表来表示图。它把图中的边转换成类似表或字典的结构，让人能快速读出一个节点可以连接到哪里。
- 为什么重要: 图不只是图片，也需要被存储和查询。邻接列表把关系数据具体化为“每个节点的邻居”，为搜索、推荐、链接分析等问题打基础。
- 相关概念: `图(graph)`, `节点(node)`, `边(edge)`
- 核心 Section: `P2-9.3`
- 出现 Section:
