<a id="hash-table"></a>

## 哈希表

- 含义：哈希表是一种用哈希规则把键映射到候选存储位置的数据结构，从而快速找到对应的值。Python 的字典就是常见例子，可用于 `用户 ID -> 信息`、`设置名 -> 设置值` 这样的映射。
- 为什么重要：哈希表解释了按键查找为什么不同于按顺序扫描列表。当程序需要根据名称、标识符或配置键快速读取和更新值时，这种结构非常合适。
- 相关概念：`键`，`数据结构`，`运算`
- 中心 Section：`P2-9.4`
- 出现 Section：`P2-9.1`
