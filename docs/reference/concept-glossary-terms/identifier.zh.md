<a id="identifier"></a>

## 标识符

- 含义: 标识符是用来区分某一行、案例、用户、学生、事件或对象的值。它通常说明“是哪一个对象”，而不是模型应该学习的通用模式。
- 为什么重要: 如果不加判断地把标识符作为输入特征，模型可能会依赖姓名或编号，而不是学习可复用的规律。理解标识符，才能在准备数据集时区分输入特征、目标列和标识符列。
- 相关概念: `样本`, `特征`, `目标`
- 核心 Section: `P2-12.3`
