<a id="array"></a>

## 数组

- 含义：数组是一种让值能按位置和索引读取的结构。在数值计算中，它通常指沿轴排列、具有形状的一块数值，并可被整体处理。
- 为什么重要：数组不只是容器，也是计算形状。理解数组能为 shape、axis、广播、切片和向量化运算做准备，也能把简单值列表和可成组计算的数据排列区分开来。
- 相关概念：`表`，`索引`，`数据结构`
- 中心 Section：`P2-9.2`
- 出现 Section：`P2-11.1`，`P2-11.2`，`P2-12.1`
