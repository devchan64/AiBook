<a id="key-based-structure"></a>

## 键基结构(key-based structure)

- 含义: 键基结构以键而不是位置为中心来查找和管理值。字典和哈希表是典型例子，核心问题是这个名称或 ID 对应哪个值。
- 为什么重要: 只用线性结构和非线性结构，不足以说明以键查找为优势的结构。这个概念把顺序处理、关系遍历和按名称查找分开。
- 相关概念: `字典(dictionary)`, `哈希表(hash table)`, `键(key)`
- 核心 Section: `P2-9.1`
- 出现 Section: `P2-9.4`
