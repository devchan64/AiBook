<a id="stack"></a>

## 栈(stack)

- 含义: 栈是以后进先出规则工作的数据结构或抽象数据类型。核心不是存了什么，而是最后放入的值先被取出。
- 为什么重要: 调用流程、撤销、临时保存等问题都需要这种顺序规则。理解栈可以把它同先进先出的队列区分开。
- 相关概念: `队列(queue)`, `线性结构(linear structure)`, `数据结构(data structure)`
- 核心 Section: `P2-9.4`
- 出现 Section: `P2-9.1`, `P4-15.1`
