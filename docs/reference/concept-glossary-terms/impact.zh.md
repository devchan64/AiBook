<a id="impact"></a>

### 影响(impact)

- 含义: 影响是 AI 输出到达人、组织、服务流程或环境后产生的实际结果。它关注输出被使用之后改变了什么。
- 为什么重要: 同样的预测、推荐或生成结果，在不同场景中可能带来完全不同的风险和责任。影响让读者不只看模型输出，还要看输出进入现实判断后的后果。
- 相关概念: `输出(output)`, `目标(goal)`, `风险(risk)`, `责任(accountability)`
- 核心 Section: `P1-1.1`
- 出现 Section: `P1-1.1`, `P1-1.2`
