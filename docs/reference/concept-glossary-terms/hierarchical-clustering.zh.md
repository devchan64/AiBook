<a id="hierarchical-clustering"></a>

## 层次聚类

- 含义: 层次聚类是一种逐步展示数据点或小分组如何靠近并合并的聚类方法。
- 为什么重要: 它不要求一开始就把聚类数固定成一个答案，而是让读者观察多个分组层级。理解这个概念，才能把聚类结果读成“什么先合并”的线索，而不只是“最后分成几组”。
- 相关概念: `聚类(clustering)`, `聚类簇(cluster)`, `距离(distance)`, `谱聚类(spectral clustering)`
- 中心 Section: `P4-17.3`
- 出现 Section:
