<a id="search-query"></a>

## search query

- Meaning: A search query is the new input currently being compared or used to retrieve candidates. In nearest-neighbor comparison, it is compared with stored training examples to find nearby candidates; in search, it is the reference expression used to retrieve candidates.
- Why it matters: The query separates `the new case being judged` from `the stored reference cases`. With the same training data, changing the query position or representation can change which neighbors are selected and therefore change the result.
- Related concepts: `nearest neighbor`, `distance`, `search`
- Core Section: `P4-12.1`
- Appears in: `P4-12.1`
