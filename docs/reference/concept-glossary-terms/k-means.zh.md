<a id="k-means"></a>

## k-means

- 含义: k-means 是一种聚类算法。它先给定聚类数量，再反复把点分配给最近的中心点，并更新这些中心点。
- 为什么重要: k-means 说明无监督学习里仍然有人需要先做的选择，例如聚类数量和用于计算距离的特征空间。它适合作为快速分组基准，但不会自动发现现实世界里的类别名称。
- 相关概念: `聚类(clustering)`, `聚类簇(cluster)`, `中心点(centroid)`
- 中心 Section: `P4-17.1`
- 出现 Section: `P4-3.2`
