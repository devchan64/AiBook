<a id="format-consistency"></a>
<a id="glossary-format-consistency"></a>

### 格式一致性(format consistency)

- 含义: 格式一致性，是指意义相同的值使用稳定的格式、单位和书写规则，使后续比较和排序不发生偏移。
- 为什么重要: 如果键格式、时间格式、单位或字符串规则混在一起，同一个对象可能被读成不同对象，时间顺序也可能被误读。先检查格式一致性，才能把数据质量问题和问题表示设计问题分开。
- 相关概念: `数据质量检查`, `标识符`, `行`
- 核心 Section: `P3-2.3`
