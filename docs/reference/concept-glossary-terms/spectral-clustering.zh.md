<a id="spectral-clustering"></a>

## 谱聚类

- 含义: 谱聚类会先把邻近关系表示成图，再根据图中的连接结构形成分组。
- 为什么重要: 当弯曲或不规则结构很难只靠中心点或原始坐标距离来读取时，它可能有帮助。但它对相似度图的构造方式很敏感，所以不能因为形状复杂就把结果当成自动正确答案。
- 相关概念: `聚类(clustering)`, `图(graph)`, `连接结构(connectivity)`, `层次聚类(hierarchical clustering)`
- 中心 Section: `P4-17.3`
- 出现 Section:
