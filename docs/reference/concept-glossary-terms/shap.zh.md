<a id="shap"></a>

## SHAP

- 含义: SHAP 是一种模型解释方法，试图把单次预测拆成各个特征的贡献。
- 为什么重要: 特征重要度总结的是整个模型的使用量，而 SHAP 用来追问某一条样本里哪些特征把预测往上或往下推。
- 相关概念: `特征重要度(feature importance)`, `部分依赖图(PDP)`, `解释边界`
- 中心 Section: `P4-15.2`
- 出现 Section: `P4-15.2`
