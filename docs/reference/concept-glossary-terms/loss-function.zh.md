<a id="loss-function"></a>

## 损失函数

- 含义：损失函数把预测错误转换成训练要尽量降低的数字。
- 为什么重要：训练需要一个可降低的数值目标。损失函数定义模型把哪种错误视为代价。
- 相关概念：`误差`，`目标函数`，`梯度下降`
- 中心 Section：`P2-6.2`
- 出现 Section：`P2-6.3`, `P2-13.2`, `P2-15.1`, `P5-4.1`, `P5-4.2`
