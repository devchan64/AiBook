<a id="correlated-features"></a>

## 相关特征

- 含义: 相关特征是会朝相似方向一起变化，或携带近似信息的特征。
- 为什么重要: 即使某个特征的重要度看起来低，另一个相关特征也可能已经在提供类似信息。因此只按单列重要度排名来解释，容易产生误读。
- 相关概念: `特征重要度(feature importance)`, `置换重要度(permutation importance)`, `相关关系(correlation)`
- 中心 Section: `P4-15.2`
- 出现 Section: `P4-15.2`
