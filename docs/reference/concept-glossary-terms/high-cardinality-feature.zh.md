<a id="high-cardinality-feature"></a>

## 高基数特征

- 含义: 高基数特征是拥有大量不同取值的特征，例如客户 ID、订单编号、原始时间戳。
- 为什么重要: 在树模型中，取值种类很多的特征容易把训练数据切得很细，所以它的重要度可能高于真实泛化贡献。
- 相关概念: `特征重要度(feature importance)`, `过拟合(overfitting)`
- 中心 Section: `P4-15.2`
- 出现 Section: `P4-15.2`
