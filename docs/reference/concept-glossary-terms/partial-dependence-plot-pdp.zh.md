<a id="partial-dependence-plot-pdp"></a>

## 部分依赖图(PDP)

- 含义: 部分依赖图展示当某个特征值变化时，模型预测平均会怎样变化。
- 为什么重要: 特征重要度问的是 `模型大量使用了什么`；PDP 更直接地问 `这个值变动时，预测方向怎样移动`。
- 相关概念: `特征重要度(feature importance)`, `SHAP`, `因果推断(causal inference)`
- 中心 Section: `P4-15.2`
- 出现 Section: `P4-15.2`
